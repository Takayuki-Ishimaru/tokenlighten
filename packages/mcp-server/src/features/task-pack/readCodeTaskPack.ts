/**
 * readCodeTaskPack.ts — v0.7 task closure pack builder.
 *
 * Returns a compact, handle-bearing closure pack that identifies the surfaces
 * an agent needs to complete a task, without returning full file bodies.
 *
 * Design ref: DESIGN-v0.7-mcp-task-closure.md §"Improvement 1: Task Surface Pack".
 */

import { basename } from "node:path";
import { AsyncLocalStorage } from "node:async_hooks";
import {
  artifactQueryTokenVariants,
  buildBasenameFrequency,
  discoverArtifactFiles,
  extractIdentifiers,
  inferQueryProjectScope,
  importEdgeCandidates,
  isWithinRoleSearchScope,
  locateTaskContext,
  matchBasenameTokens,
  projectRootOf,
  roleSearchScopePrefixes,
  setActiveRootWorkspace,
  SIBLING_SEED_WEAK_WHY,
} from "../locator/locateTaskContext.js";
import {
  ARTIFACT_EXTS,
  createWalkOmissions,
  genericTextDiscoveryEnabled,
  walkCodeFiles,
} from "../../tools/walkRepo.js";
import { buildCompactTree } from "../../tools/exploreTree.js";
import { getCurrentDiff } from "../../tools/getCurrentDiff.js";
import { handleTable, shaOfBytes, shaOfText } from "../../util/handles.js";
import { classifySurface, surfaceInventory, deriveTokenVariants } from "../../util/impact.js";
import { readFileSafe, safeResolve, isWithin, resolveReal, statReadTargetSync } from "../../util/safePath.js";
import { decodeTextBuffer } from "../../util/textDecode.js";
import { languageForPath } from "../../util/languages.js";
import { elideDocComments } from "../../util/formatCompress.js";
import {
  attachEvidenceCompletion,
  calleesOf,
  emitEvidenceShadow,
} from "./evidenceShadow.js";
import { isEnumLikeQuery, stripPathSpans, tokenizeQuery } from "../../util/queryShape.js";
import { expandOneHop } from "./openUniverseExpansion.js";
import {
  createScanContentCache,
  deriveIdentifierVariantProbes,
  escapeRegExp,
  scanLiteral,
} from "../search/find/findText.js";
import { extractSymbolsFromFile, extractSymbolsFromLines, resolveCallerByteCeiling, DEFAULT_RESPONSE_BYTE_FLOOR } from "../../tools/readCodeModes.js";
import { queryRequestsTestEvidence, selectQueryEvidence } from "../../tools/queryEvidence.js";
import { resolveSemanticWiring, type SemanticWiringResolution } from "../../tools/semanticWiringResolver.js";
import {
  buildVerificationManifest,
  findBuildEntry,
  probeToolchain,
  surfaceToolchainDomain,
  type VerificationManifest,
} from "../../util/verificationPack.js";
import { isNativeExtPath, widenNativeRangeString } from "../../util/nativeSymbolRange.js";
import { dominantRoot } from "../../util/dominantRoot.js";
import { countLines, sliceLinesToText } from "../../util/countLines.js";
import {
  dropLowestRankedHeadingIndexEntry,
  isMarkdownPath,
  markdownOutline,
  markdownSectionAtLine,
  parseMarkdownHeadings,
} from "../../util/markdownSections.js";
import {
  attachDocSliverNavigation,
  attachDocSliverSurfaceHonesty,
  attachDocSliverZoom,
  buildDocSliverHeadingIndex,
  clearDocSliverNavigation,
  docSliverFocusFromSurface,
  docSliverRouteClause,
  planDocSliver,
  DOC_SLIVER_HEADINGS_CAP_BYTES,
  DOC_SLIVER_ROUTE_MARKER,
  type DocSliverPlan,
} from "./docSliver.js";
import {
  deriveNextFromPlan,
  enforceContinuationBudget,
  MAX_STAGE_CALLS,
  nextStringToCall,
  type ContinuationCall,
  type ContinuationPlan,
} from "../../util/continuation.js";
import { collectSymbols, type CollectedSymbolKind } from "../../symbols/collectSymbols.js";
import { recordPackChecks, getPackChecks, tokenizeForEpoch, deriveCheckId, recordConcernTokens, recordServedEditAdmissibility, isCandidateListPackPending, recordServedRange, beginServeCall, servedFindWindowHasUnservedLines, type PackCheckRecord } from "../../state/session.js";
import {
  recordServedSurfaces,
  queryServedSurfaces,
  statKindCached,
  anyFileExistsCached,
  shouldAttachVerification,
  shouldAttachServedEarlier,
  shouldAttachFrontierIndex,
  recordAwaitingInputLatch,
  consultAwaitingInputLatch,
  clearAwaitingInputLatch,
  recordFunctionalValidationObligation,
  getFunctionalValidationObligation,
  consultExecutedLocate,
  consultExecutedSearch,
  hasExecutedNext,
  nextFingerprint,
  normalizeContractLane,
  DEFAULT_CONTRACT_LANE,
  dirExistsCached,
  clearExecutedNextForLane,
  clearExecutedNextForWorkspace,
  recordServedWindow,
  servedWindowHasUnservedLines,
  clearServedWindowsForScope,
  type ServedSurfaceEntry,
  type AwaitingInputLatch,
} from "../../util/packServeLog.js";
import { currentSessionLane, laneScopedKey } from "../../util/laneKey.js";
import { computeClosureStateSafe } from "../../util/closureTracking.js";
import { mustFetchPackCap } from "../../util/mustFetch.js";
import {
  coveragePackerEnabled,
  coveragePackerV2Enabled,
  interfaceAuthorityEnabled,
  noteProofCompletionPack,
  proofCompletionEnabled,
} from "../../util/flags.js";
import { selectCoverageOrderedEntries, estimateBodyBytes } from "./coveragePacker.js";
import { buildInterfaceAuthoritySurfaces } from "./interfaceAuthority.js";
import { selectCoverageOrderedEntriesV2, type CoverageObligationV2 } from "./coveragePackerV2.js";
import {
  clearPriorPackObligations,
  queryPriorPackObligations,
  recordPriorPackObligations,
  type PriorObligationRecord,
} from "./priorPackStore.js";
import {
  clearTaskContract,
  clearTaskContractsForLane,
  queryTaskContract,
  recordExpansionExplicitGap,
  taskContractDischargeCertificate,
  taskContractDigest,
  taskContractGapProjection,
  recordEvidenceExpansion,
  recordServedConcernEvidence,
  recordServedRoleEvidence,
  recordTaskContract,
  resetTaskContractStoreForTest,
} from "./taskContractStore.js";
import { hasOpenUniverseIntent, isAdditiveEnumIntent } from "./openUniverseIntent.js";
import { trace, isTraceEnabled, currentTraceCallId } from "../../util/trace.js";
import { xlsxRoster, xlsxTable } from "../../office/xlsx.js";
import { docxSections } from "../../office/docx.js";
import { pptxSlides } from "../../office/pptx.js";
import { pdfPages } from "../../office/pdf.js";
import { prepareOfficeDocument } from "../../office/decrypt.js";
import { preflightZip } from "../../office/zipPreflight.js";
import {
  extractOoxmlVisualInventory,
  hasOoxmlVisualInventory,
  renderOoxmlVisualInventory,
} from "../../office/ooxmlVisuals.js";
import { csvTable } from "../../office/csv.js";
import type {
  ImpactCandidate,
  ImpactSurface,
  LocateCandidateDetail,
  TaskChangeContract,
  TaskChangeObligation,
  TaskChangeObligationKind,
  TaskCallBudget,
  TaskCapabilityGap,
  TaskDecisionEvidenceModel,
  TaskEvidenceGraph,
  TaskEvidenceNode,
  TaskEvidenceRelation,
  TaskEvidenceRelationKind,
  TaskExecutionContract,
  TaskFalsificationReport,
  TaskProfile,
  TaskProfileBinding,
  TaskProfileRequest,
  TaskReadinessCertificate,
  TaskReadinessEvidence,
  TaskReadinessObligation,
  TaskReadinessRisk,
  TaskSemanticClosure,
  TaskVerificationRecipe,
  TaskWorkspaceState,
  TaskWiringEndpoint,
  TaskWiringEvidence,
  TaskWiringProfile,
} from "@tokenlighten/types";
import * as fs from "fs";
import * as path from "path";
import type {
  ArtifactKind,
  ArtifactTaskPackSection,
  ArtifactTaskPackSectionEntry,
  ArtifactTaskPackSurface,
  ConcernCoverage,
  TaskPackArgs,
  TaskPackPathEntry,
  TaskPackResult,
  TaskPackResultSurface,
  TaskPackSurface,
} from "./model.js";
export type {
  ArtifactKind,
  ArtifactTaskPackSection,
  ArtifactTaskPackSurface,
  ConcernCoverage,
  TaskPackArgs,
  TaskPackResult,
  TaskPackResultSurface,
  TaskPackSurface,
} from "./model.js";
import {
  artifactSectionCandidates,
  artifactSectionEntryIds,
  codeTaskPackSurfaces,
  fitArtifactSectionToBytes,
  isArtifactTaskPackSurface,
} from "./artifactSections.js";
export { isArtifactTaskPackSurface } from "./artifactSections.js";
import { applyCanonicalTaskDecision, discoveryBundleNext, hasServedZoomAffordance } from "./canonicalDecision.js";
export {
  applyCanonicalTaskDecision,
  canonicalTaskDecisionInvariantViolations,
  deriveCanonicalTaskDecision,
  enforceCanonicalTaskDecisionAtExit,
  hasCertificateBinding,
  hasServedZoomAffordance,
  type CanonicalDecisionFenceReport,
  type CanonicalTaskDecision,
  type CanonicalTaskDecisionKind,
} from "./canonicalDecision.js";


// ---------------------------------------------------------------------------
// Constants
// ---------------------------------------------------------------------------

/**
 * Serialized JSON byte cap for a task_pack response, at the LOWEST
 * confidence tier (no code-bearing surfaces). Scales up via
 * capForResult() when high-confidence/required surfaces carry embedded
 * code, so the pack can close the task in one read instead of forcing
 * follow-up slice calls — while staying far short of a full-file dump.
 *
 * Host-safe serve budget (2026-08-17): 22 KiB leaves room for the protocol
 * envelope so desktop clients keep the pack inline instead of externalizing it
 * to content.json, while preserving the minimum multi-surface skeleton.
 * Code-bearing tiers remain independently bounded by capForResult().
 */
export const MAX_TASK_PACK_BYTES = 22 * 1024;

/**
 * Upper bound of the confidence-scaled cap (see capForResult()). Kept at the
 * 32 KiB transport-safe ceiling so it stays >= the raised base floor
 * (MAX_TASK_PACK_BYTES): a code-bearing pack must never be trimmed HARDER than
 * a code-less discovery pack, which is exactly the inversion a base-only bump
 * would introduce (base 24576 > old high 16384). Raised 16384 -> 32768.
 */
const MAX_TASK_PACK_BYTES_HIGH_CONFIDENCE = 32768;

/** Three independent concerns need an additive but still bounded proof tier. */
const MAX_TASK_PACK_BYTES_MULTI_CONCERN = 32768;

/**
 * Hard transport-safe ceiling for semantic wiring. Claude Code persists larger
 * MCP payloads out-of-band, which turns an otherwise valid pack into an
 * unparseable tool result for the solver.
 */
const MAX_TASK_PACK_BYTES_SEMANTIC_WIRING = 32768;
/**
 * Constructed receivers carry post-edit structural and executable proof.
 *
 * B2a ceiling decision (2026-08-01, second pass): RAISED 32768 -> 49152 for
 * construct-receiver proof packs ONLY. Numbers, not vibes: the measured T13
 * alternative to one complete pack was a 10-call, 72-77KB full-read cascade
 * plus 6-8 api turns per rep — strictly costlier than +16KB of one-shot
 * transport. Clamp safety: the only observed client clamp is token-based
 * (~25k tokens; the 2026-07-31 T05c incident clamped a 52KB single-line
 * markdown blob at roughly 2.5 chars/token). A 47-49KB wiring pack is JSON +
 * C-family code at ~3 chars/token ≈ 16k tokens — comfortably inside — and
 * semanticSignalRegression.spec.ts still pins >= 1536B of headroom under the
 * NEW ceiling so envelope creep is caught before it re-saturates. Measured at
 * this ceiling: hub 4.7KB->16984B complete, produced-class header 122B->1631B
 * complete, pack 47178B, phase prepared. The wiring-envelope diet (evidence
 * graph + structural checks + certificate ≈ 15.7KB ≈32% of this budget) stays
 * a worthwhile follow-up but is no longer the blocker.
 */
const MAX_TASK_PACK_BYTES_CONSTRUCT_PROOF = 49152;

/**
 * B2a: per-surface completeness bar for a construct-receiver wiring frontier —
 * a required surface at or under this size is served WHOLE (no teaser, no
 * remaining_ranges). Larger files keep the anchored window plus disclosed
 * remaining_ranges.
 */
const WIRING_COMPLETE_SURFACE_BYTES = 24576;
/** Companion line bound for WIRING_COMPLETE_SURFACE_BYTES (bytes are the real gate). */
const WIRING_COMPLETE_SURFACE_LINES = 4096;
/**
 * B2a: bytes of COMPLETE bodies a construct-receiver pack may spend on
 * NON-priority surfaces.
 *
 * The two priority surfaces — the construction hub and the produced class's
 * declaration — are never refused: they are the measured triggers (all three
 * T13 reps opened with `mode=full` of the hub, and the declaration arrived as a
 * 122-byte stub). Everything else draws from this budget, which is what keeps
 * the pack inside MAX_TASK_PACK_BYTES_CONSTRUCT_PROOF once the ~15KB semantic
 * envelope (evidence graph, structural checks, certificate) is accounted for.
 * A surface refused here is NOT dropped — it keeps the anchored window plus
 * disclosed remaining_ranges it has always had, which is strictly better than
 * having trimToCap strip its body to nothing.
 */
const CONSTRUCT_COMPLETE_EXTRA_BUDGET = 10240;

/**
 * A2 (v0.12, DESIGN-v0.12-plan.md §2 柱A) — EDIT-HUNK FRONTIER.
 *
 * Anchored-window budget of a construction-hub excerpt. Extracted verbatim from
 * the literal `6 * 1024` the Wave-4 ladder used, so the radius the ladder picks
 * is bit-for-bit the one it picked before A2.
 */
const HUB_EXCERPT_BUDGET_BYTES = 6 * 1024;
/**
 * A2: bytes the VERBATIM (comment-preserving) landing-region window may add on
 * top of HUB_EXCERPT_BUDGET_BYTES.
 *
 * WHY 4096: `buildSemanticTaskWiringProfile` already spends exactly 4096 bytes
 * per REVIEW surface on this same construct-receiver path (`reviewEvidenceBytes`).
 * The edit-hunk window is therefore capped at one review surface's worth of
 * bytes — an existing evidence cap on this exact code path, not a new budget
 * concept. It is enforced TWICE: on the DELTA over the plain excerpt (the hunk
 * can never cost more than one review surface) and on the absolute total, and
 * only ever at the radius whose plain build already fit — so A2 can never
 * displace or shrink other frontier evidence beyond what the pre-A2 cap logic
 * already did.
 */
const HUB_EDIT_HUNK_MAX_BYTES = 4096;
/**
 * A2: landing-window radii tried widest-first until the window's VERBATIM text
 * fits HUB_EDIT_HUNK_MAX_BYTES. No radius fitting means no hunk, i.e. today's
 * behavior — the excerpt is never widened on a guess.
 */
const HUB_EDIT_HUNK_RADII = [24, 16, 10, 6] as const;

/** Multiple independently required Office inputs may share one bounded pack. */
const MAX_TASK_PACK_BYTES_ARTIFACT_BUNDLE = 32768;

// ---------------------------------------------------------------------------
// T1 (2026-08-27 field-eval): caller/env/client-profile byte governance.
//
// AsyncLocalStorage (not a bare module `let`): buildTaskPack is async and
// awaits real file I/O between minting this value and capForResult's read of
// it (the artifact prefetch phase runs before buildTaskPackCore even starts;
// trimToCap's own shedding loop runs deep inside buildTaskPackCore). A bare
// variable set at entry and cleared at exit would leak across two
// buildTaskPack calls for DIFFERENT workspaces/clients that happen to
// interleave on the event loop while each awaits its own I/O --
// mcp/transport/modernHttp.ts's HTTP leg can hold more than one call open at
// once. ALS gives every buildTaskPack invocation its own isolated view, with
// no ceiling parameter threaded through fitsInCap's internal call sites or
// capForResult's other callers (attachPrefetchedArtifactSections,
// applyDocSliverHonesty, the packCapBytes diagnostics in
// dedupeTrimAndPersist) -- every one of them keeps calling capForResult(x)
// exactly as before; only capForResult's OWN body changed (see
// rawCapForResult below).
// ---------------------------------------------------------------------------
const packByteCeilingStorage = new AsyncLocalStorage<number | undefined>();

/** The active caller/env/profile byte ceiling for the buildTaskPack() call in progress, or undefined outside one (or when nothing constrained it) -- see packByteCeilingStorage's doc comment. */
function activePackByteCeiling(): number | undefined {
  return packByteCeilingStorage.getStore();
}

/** rawCapForResult's type-specific tier, narrowed (never widened) to whatever byte ceiling is active -- see packByteCeilingStorage's doc comment. The identity function when no ceiling is active, which is every call today's replay corpus makes. */
function clampToActiveCeiling(typeSpecificCap: number): number {
  const ceiling = activePackByteCeiling();
  if (ceiling === undefined) return typeSpecificCap;
  return Math.max(DEFAULT_RESPONSE_BYTE_FLOOR, Math.min(typeSpecificCap, ceiling));
}

/**
 * A1 (2026-07-30) — inline budget for the artifact SURFACES of a pack that is
 * not an artifact_build (any profile: answer, multi_concern, generic, …).
 *
 * Live defect (T14 two-deck audit): the pack resolved both .pptx decks to
 * `{kind:"artifact", size, handle, extract}` STUBS and served zero slide text,
 * in a 2.9 KB response — with ~21 KB of the pack budget unused — so the caller
 * spent two extra round trips extracting content the server had already
 * located. The distributed guide's own contract already promises the opposite
 * ("artifact packs need content-bearing artifact_sections per source"), so this
 * is the contract being honored, not a new one.
 *
 * Two caps, both deliberately well inside the pack ceiling so artifact content
 * can never crowd out code surfaces: 8 KiB for any ONE artifact (enough for
 * several whole slides / a docx section / a real sheet preview) and 16 KiB for
 * all of them together. Anything past a cap is served as a PREFIX plus the
 * exact per-section follow-up call, never dropped to a stub.
 */
const ARTIFACT_SURFACE_INLINE_CAP_BYTES = 8192;
const ARTIFACT_SURFACE_INLINE_TOTAL_BYTES = 16384;

/** Same bound artifact_build's own input selection uses (selectArtifactBuildInputs). */
const MAX_INLINED_ARTIFACT_SURFACES = 4;

/** `remaining_sections` stays a pointer, not an inventory. */
const MAX_REMAINING_SECTION_IDS = 6;

/**
 * Per-surface embedded-code byte cap (keeps any single slice bounded).
 * Turn-economy serve budget (2026-07-24): raised 4096 -> 12288 so a single
 * relevant surface can ship a real code body inline (2-3 such surfaces fill
 * the raised pack cap) instead of degenerating into a handle the agent must
 * re-slice in a follow-up turn. Oversized surfaces STILL trim to a centered
 * window + remaining_ranges (centeredSliceForCap) — never drop to code-less.
 */
const MAX_SURFACE_CODE_BYTES = 12288;

/** Maximum surfaces when all roles are distinct. */
const MAX_SURFACES_DISTINCT = 6;
// Serve-honesty (2026-08-01): enrichment passes (query-named files, header/
// source pairs) on a multi_concern pack may fill past the base surface cap —
// a 3-concern fix pack legitimately owns 3 primaries + pairs + a named
// contract doc. The pack byte budget still governs total size, and the
// byte-budget trim discloses anything it sheds.
const MULTI_CONCERN_ENRICHMENT_MAX_SURFACES = MAX_SURFACES_DISTINCT + 2;
const MAX_SEMANTIC_WIRING_SURFACES = 12;

/** Kept separate from every source/text language extension registry. */
const ARTIFACT_EXT_SET: ReadonlySet<string> = new Set(ARTIFACT_EXTS);

/** Confidence at/above which a non-required surface still embeds code. */
const HIGH_CONFIDENCE_THRESHOLD = 0.7;

/**
 * B4: a file at/under BOTH of these bounds is "tiny" — small enough that a
 * partial-range handle on it (the live tiny-stub failure) is strictly worse than a
 * whole-file handle: the agent inevitably needs the rest of a tiny stub, and a
 * narrow handle forces a re-slice. When a range-kind candidate lands on a tiny
 * file, the surface HANDLE is minted over the whole file (1-EOF) even though
 * the displayed `range`/`code` slice stays narrow. Mirrors the small-file
 * thresholds used elsewhere (readCodeSmallFile) — hardcoded here rather than
 * imported to avoid a module cycle (readCodeSmallFile itself imports from this
 * layer); kept in sync by comment.
 */
const TINY_FILE_MAX_BYTES = 8192;
const TINY_FILE_MAX_LINES = 250;

// ---------------------------------------------------------------------------
// Per-pack file read/parse memo (efficiency): every pack build reads the same
// handful of files several times over — sliceCode, tinyFileWholeRange,
// widenNativeSymbolRange, buildContractDocOutline, definitionVsDerivedAdjustment
// and (in the seeded flow) the seed reads all hit the same paths. A single
// per-call cache keyed by workspace-relative path collapses each file to at
// most one readFileSync and one collectSymbols() per pack. Threaded as an
// OPTIONAL last argument everywhere: undefined preserves the original
// read-every-time behavior (safe for any caller that does not supply one).
// ---------------------------------------------------------------------------

type ParsedSymbols = Awaited<ReturnType<typeof collectSymbols>>;

/** Sentinel distinguishing "read, but failed" from "not yet read". */
const READ_FAILED = Symbol("read-failed");

class FileReadCache {
  private readonly content = new Map<string, string | typeof READ_FAILED>();
  private readonly symbols = new Map<string, ParsedSymbols | null>();

  /** Read `relPath` under `workspace` once; subsequent calls return the memo. Returns undefined on any I/O error OR undecodable content (P3, 2026-08-27: BOM-sniffed/NUL-probed via decodeTextBuffer, never a raw utf8 mis-decode); cached as a failure so it is not retried. */
  read(workspace: string, relPath: string): string | undefined {
    const hit = this.content.get(relPath);
    if (hit !== undefined) return hit === READ_FAILED ? undefined : hit;
    let raw: string | typeof READ_FAILED;
    try {
      const decoded = decodeTextBuffer(fs.readFileSync(path.join(workspace, relPath)));
      raw = decoded === null ? READ_FAILED : decoded;
    } catch {
      raw = READ_FAILED;
    }
    this.content.set(relPath, raw);
    return raw === READ_FAILED ? undefined : raw;
  }

  /** Seed the cache with content already read elsewhere (e.g. the seeded-pack parallel reads), so downstream slice/widen calls reuse it. */
  seed(relPath: string, content: string): void {
    if (!this.content.has(relPath)) this.content.set(relPath, content);
  }

  /** Parse `relPath`'s symbols once (given its already-cached content + language); memoized including the null (parse-failure) result. */
  async parsedSymbols(text: string, lang: string, relPath: string): Promise<ParsedSymbols | null> {
    const hit = this.symbols.get(relPath);
    if (hit !== undefined) return hit;
    let parsed: ParsedSymbols | null;
    try {
      parsed = await collectSymbols(text, lang, {});
    } catch {
      parsed = null;
    }
    this.symbols.set(relPath, parsed);
    return parsed;
  }
}

/** Read `relPath` under `workspace`, using `cache` when supplied. Returns undefined on I/O error or undecodable content (P3, 2026-08-27). */
function readCached(workspace: string, relPath: string, cache?: FileReadCache): string | undefined {
  if (cache) return cache.read(workspace, relPath);
  try {
    return decodeTextBuffer(fs.readFileSync(path.join(workspace, relPath))) ?? undefined;
  } catch {
    return undefined;
  }
}

// ---------------------------------------------------------------------------
// Code slicing (A2 item 1): populate surface.code with the exact range
// text instead of leaving the field permanently unset. Bounded per-slice so
// one huge symbol cannot blow the whole pack's budget; the trim ladder in
// trimToCap() still protects/drops code as a last resort under the overall
// cap.
// ---------------------------------------------------------------------------

/**
 * Read `path` under `workspace` and return the 1-based inclusive line range
 * `range` ("start-end"). Returns undefined on any I/O error, a malformed
 * range, or when the slice would exceed MAX_SURFACE_CODE_BYTES (the caller
 * then falls back to a handle+skeleton-only surface — no embedded `code`).
 *
 * DESIGN-v0.8 §C3: the cap check runs on the RAW slice (unchanged semantics —
 * a slice too big to read at all still falls back to handle+skeleton-only);
 * elideDocComments then runs on the slice that IS returned, collapsing
 * multi-line doc/comment blocks to a compact marker. This is task_pack's
 * ONE choke point for embedded code (candidateToSurface, buildPartialPack,
 * and buildDiffTaskPack all call sliceCode), so eliding here covers every
 * task_pack surface without a second call site. Read-only/display text —
 * never used for handle sha or edit preconditions (edits always re-read the
 * real file via the surface's `handle`, never this embedded `code` string).
 */
/**
 * Wave 4 (2026-07-24 T10 forensics): a slice whose ENTIRE range is doc/comment
 * text elides down to a bare `doc elided L<a>-<b>` marker — a served surface
 * with zero real content ("# doc elided L1-13" was an edit-frontier surface's
 * whole code). When elision leaves only markers, the comments ARE the content;
 * serve the raw text instead.
 */
function elisionLeftMarkersOnly(elided: string): boolean {
  const lines = elided.split("\n");
  let sawMarker = false;
  for (const line of lines) {
    const trimmed = line.trim();
    if (trimmed.length === 0) continue;
    if (/^(?:\/\*\s*doc elided L\d+-\d+\s*\*\/|#\s*doc elided L\d+-\d+)$/.test(trimmed)) {
      sawMarker = true;
      continue;
    }
    return false;
  }
  return sawMarker;
}

function sliceCode(workspace: string, relPath: string, range: string, cache?: FileReadCache): string | undefined {
  const m = range.match(/^(\d+)-(\d+)$/);
  if (!m) return undefined;
  const start = parseInt(m[1]!, 10);
  const end = parseInt(m[2]!, 10);
  if (!Number.isFinite(start) || !Number.isFinite(end) || start < 1 || end < start) return undefined;

  const raw = readCached(workspace, relPath, cache);
  if (raw === undefined) return undefined;
  // T1b (v0.13, UTF-16 3-way read-parity wave): sliceLinesToText restores
  // raw's own trailing newline when [start,end] reaches EOF -- see its doc
  // comment (util/countLines.ts). Fixes task_pack evidence for a whole-file
  // range coming back one trailing newline short of the SAME file read via
  // mode=full path=/handle= (utf16ReadParity.spec.ts / the release
  // rehearsal's utf16 scenario three_way_consistent check).
  const slice = sliceLinesToText(raw, start, end);
  if (slice.length === 0) return undefined;
  if (Buffer.byteLength(slice, "utf8") > MAX_SURFACE_CODE_BYTES) return undefined;
  // item 11: pass the slice's true file start line so elision markers carry
  // file line numbers, not slice-relative ones.
  const elided = elideDocComments(slice, languageForPath(relPath), start);
  return elisionLeftMarkersOnly(elided) ? slice : elided;
}

// Comment-heavy implementation ranges can exceed the raw per-surface cap even
// though their complete executable content is compact. Try elision before the
// byte decision so a causal multi-method slice is not needlessly cut in half.
// This never raises the fixed response cap: the compressed result must still
// fit MAX_SURFACE_CODE_BYTES.
function sliceCodeAfterCommentElision(
  workspace: string,
  relPath: string,
  range: string,
  cache?: FileReadCache,
  executableOnlyFallback = false,
): string | undefined {
  const match = range.match(/^(\d+)-(\d+)$/);
  if (!match) return undefined;
  const start = Number(match[1]);
  const end = Number(match[2]);
  if (!Number.isInteger(start) || !Number.isInteger(end) || start < 1 || end < start) {
    return undefined;
  }
  const raw = readCached(workspace, relPath, cache);
  if (raw === undefined) return undefined;
  const selected = raw.split(/\r?\n/).slice(start - 1, end).join("\n");
  if (selected.length === 0) return undefined;
  let compact = elideDocComments(selected, languageForPath(relPath), start);
  if (
    elisionLeftMarkersOnly(compact)
    && Buffer.byteLength(selected, "utf8") <= MAX_SURFACE_CODE_BYTES
  ) {
    compact = selected;
  }
  if (Buffer.byteLength(compact, "utf8") <= MAX_SURFACE_CODE_BYTES) return compact;
  if (!executableOnlyFallback) return undefined;

  // If comment elision markers are still too large, keep every executable and
  // preprocessor line while dropping standalone commentary and blank padding.
  // Inline comments remain attached to their statements. This is still a
  // complete code slice: only non-executable prose is removed.
  let inBlockComment = false;
  const executableOnly = compact.split("\n").filter((line) => {
    const trimmed = line.trim();
    if (inBlockComment) {
      if (trimmed.includes("*/")) inBlockComment = false;
      return false;
    }
    if (trimmed.startsWith("/*")) {
      if (!trimmed.includes("*/")) inBlockComment = true;
      return false;
    }
    if (trimmed.startsWith("//") || trimmed === "") return false;
    return true;
  }).join("\n");
  return Buffer.byteLength(executableOnly, "utf8") <= MAX_SURFACE_CODE_BYTES
    ? executableOnly
    : undefined;
}

/** Keep every range minted by a task pack within the current file bounds. */
function clampRangeToFile(
  workspace: string,
  relPath: string,
  range: string,
  cache?: FileReadCache,
): string {
  const match = range.match(/^(\d+)-(\d+)$/);
  if (!match) return range;
  const raw = readCached(workspace, relPath, cache);
  if (raw === undefined) return range;
  const lineCount = countLines(raw);
  if (lineCount === 0) return range;

  const start = Math.min(Math.max(1, parseInt(match[1]!, 10)), lineCount);
  const end = Math.min(Math.max(start, parseInt(match[2]!, 10)), lineCount);
  return `${start}-${end}`;
}

function elideCausalLineCommentRuns(source: string, startLine: number): string {
  const lines = source.split(/\r?\n/);
  const out: string[] = [];
  let runStart = -1;
  const flush = (endExclusive: number): void => {
    if (runStart < 0) return;
    out.push(`// comments elided L${startLine + runStart}-${startLine + endExclusive - 1}`);
    runStart = -1;
  };
  const causalComment = /\b(?:race|mutex|lock(?:ed|ing)?|atomic|concurrent|partially[- ]written|silently lost|stale)\b/i;
  for (let index = 0; index < lines.length; index++) {
    const line = lines[index]!;
    const comment = /^\s*\/\//.test(line);
    if (comment && !causalComment.test(line)) {
      if (runStart < 0) runStart = index;
      continue;
    }
    flush(index);
    out.push(line);
  }
  flush(lines.length);
  return out.join("\n");
}

// T3 (2026-08-27, v0.12 wave D2, DEFECT B): max width of one side of the
// `remaining_ranges` disclosure centeredSliceForCap emits when the served
// window does not reach a file boundary. See the fix's own doc comment at
// the emission site for why this must never be the raw, unbounded leftover.
const MAX_REMAINING_RANGE_CHUNK_LINES = 200;

/**
 * DESIGN-v0.9 §4.1 cliff fix: return a match-CENTERED, symbol-boundary-aware
 * trimmed chunk for an oversize `range` instead of `undefined` — a relevant
 * surface over MAX_SURFACE_CODE_BYTES must still ship SOME body (a trimmed
 * window is strictly better than the pre-v0.9 code-less surface). Generalizes
 * the wholeTinyFileFitsCap measure/clamp:
 *   - center on `centerLine` — the line that made the surface relevant (a
 *     candidate's matched line, or a diff hunk's anchor) — falling back to the
 *     `range` midpoint when none is given or it lands outside the range;
 *   - grow a WHOLE-LINE window symmetrically (alternating up/down, greedily
 *     filling whichever side still fits) until neither side's next line fits
 *     the cap;
 *   - report the uncovered spans of the ORIGINAL range as `remaining_ranges`
 *     (0, 1, or 2 "<start>-<end>" spans on the SAME handle — one re-slice each).
 * The window stays within [start,end] by construction, so for a symbol-kind
 * surface (whose `range` IS the symbol) it never spills past the symbol —
 * "symbol-boundary-aware" in the only sense this layer can know boundaries.
 *
 * Returns { code, remaining_ranges:[] } when the whole range already fits (the
 * sliceCode fast path, so callers can use this unconditionally). Returns
 * undefined ONLY for a genuine I/O error, a malformed range, or an empty slice
 * — NEVER "solely because of the cap" (the §4.1 acceptance bar).
 */
export function centeredSliceForCap(
  workspace: string,
  relPath: string,
  range: string,
  cache?: FileReadCache,
  centerLine?: number,
  maxBytes = MAX_SURFACE_CODE_BYTES,
): { code: string; remaining_ranges: string[] } | undefined {
  const m = range.match(/^(\d+)-(\d+)$/);
  if (!m) return undefined;
  const start = parseInt(m[1]!, 10);
  const end = parseInt(m[2]!, 10);
  if (!Number.isFinite(start) || !Number.isFinite(end) || start < 1 || end < start) return undefined;

  const raw = readCached(workspace, relPath, cache);
  if (raw === undefined) return undefined;
  const fileLines = raw.split(/\r?\n/);
  const clampedEnd = Math.min(end, fileLines.length);
  if (clampedEnd < start) return undefined;
  const lang = languageForPath(relPath);

  // Whole-range fast path: fits the cap outright → serve it all (elided), no
  // remaining ranges. Delegates to sliceCode so the fits-case elide/cap
  // semantics stay single-sourced (sliceCode remains the shared fit helper;
  // this function only adds the OVERSIZE trim-and-report behavior on top).
  const whole = fileLines.slice(start - 1, clampedEnd).join("\n");
  if (whole.length === 0) return undefined;
  if (Buffer.byteLength(whole, "utf8") <= maxBytes) {
    const fit = maxBytes === MAX_SURFACE_CODE_BYTES
      ? sliceCode(workspace, relPath, range, cache)
      : elideDocComments(whole, lang, start);
    if (fit !== undefined) return { code: fit, remaining_ranges: [] };
  }

  // Comment-heavy implementations can exceed the raw cap while their
  // executable body fits after the same elision used for returned slices.
  // Test that representation before fragmenting the handle.
  const compactWhole = elideDocComments(whole, lang, start);
  if (Buffer.byteLength(compactWhole, "utf8") <= maxBytes) {
    return { code: compactWhole, remaining_ranges: [] };
  }
  const causalMarkerCount = (compactWhole.match(/\b(?:data race|race condition|mutex|partially[- ]written|silently lost|stale pointer)\b/gi) ?? []).length;
  if (causalMarkerCount >= 2) {
    const causalCompact = elideCausalLineCommentRuns(compactWhole, start);
    if (Buffer.byteLength(causalCompact, "utf8") <= maxBytes) {
      return { code: causalCompact, remaining_ranges: [] };
    }
  }

  // Oversize: grow a whole-line window centered on the relevant line.
  let center = centerLine !== undefined && centerLine >= start && centerLine <= clampedEnd
    ? centerLine
    : Math.round((start + clampedEnd) / 2);
  if (center < start) center = start;
  if (center > clampedEnd) center = clampedEnd;

  const lineBytes = (n: number): number => Buffer.byteLength(fileLines[n - 1] ?? "", "utf8");
  // The center line is always included (even if it alone exceeds the cap — a
  // code-less surface is worse than one over-long line).
  let winStart = center;
  let winEnd = center;
  let bytes = lineBytes(center);
  let growUp = true;
  for (;;) {
    const canUp = winStart > start;
    const canDown = winEnd < clampedEnd;
    const upBytes = canUp ? lineBytes(winStart - 1) + 1 : Infinity; // + newline
    const downBytes = canDown ? lineBytes(winEnd + 1) + 1 : Infinity;
    const upFits = canUp && bytes + upBytes <= maxBytes;
    const downFits = canDown && bytes + downBytes <= maxBytes;
    if (!upFits && !downFits) break;
    // Alternate for centering, but only pick a side that actually fits.
    const pickUp = upFits && (!downFits || growUp);
    if (pickUp) { winStart -= 1; bytes += upBytes; } else { winEnd += 1; bytes += downBytes; }
    growUp = !growUp;
  }

  const windowText = fileLines.slice(winStart - 1, winEnd).join("\n");
  const remaining: string[] = [];
  // T3 (2026-08-27, v0.12 wave D2, DEFECT B -- field-eval report): a leftover
  // span reported verbatim can be thousands of lines wide on a huge file
  // (observed: a 23k-line file's discover-next named mode=slice range=1-7032
  // -- not an actionable zoom, and the exact non-actionable-next symptom
  // AGENTS.md's own protocol text warns every caller to expect a SMALL
  // slice, never a re-fetch-most-of-the-file range). Cap each side to the
  // chunk NEAREST the just-served window -- the part a caller asking to
  // "see more" almost always means -- rather than the raw remainder; a
  // caller that genuinely needs the far end can always re-slice again from
  // there. Both canonicalDecision.ts's servedDocumentZoom (the discover-next
  // builder) and every other remaining_ranges reader take this verbatim, so
  // capping it here is sufficient without touching any downstream consumer.
  if (winStart > start) {
    const chunkStart = Math.max(start, winStart - MAX_REMAINING_RANGE_CHUNK_LINES);
    remaining.push(`${chunkStart}-${winStart - 1}`);
  }
  if (winEnd < end) {
    const chunkEnd = Math.min(end, winEnd + MAX_REMAINING_RANGE_CHUNK_LINES);
    remaining.push(`${winEnd + 1}-${chunkEnd}`);
  }
  return { code: elideDocComments(windowText, lang, winStart), remaining_ranges: remaining };
}

/**
 * B4: when `relPath` is a TINY file (≤ TINY_FILE_MAX_BYTES and
 * ≤ TINY_FILE_MAX_LINES), return a whole-file range "1-<EOF>" so the surface
 * HANDLE covers the entire stub instead of the narrow `range`. Returns the
 * ORIGINAL range unchanged when the file is not tiny, is unreadable, or is
 * empty (nothing to clamp to). Read-only for handle minting AND — item 4
 * (tiny-file whole embed) — as the candidate for a tiny surface's DISPLAY
 * range/embedded `code`, gated by wholeTinyFileFitsCap so the whole-file slice
 * only becomes the display range when it fits the per-surface byte cap.
 */
function tinyFileWholeRange(workspace: string, relPath: string, range: string, cache?: FileReadCache): string {
  const raw = readCached(workspace, relPath, cache);
  if (raw === undefined) return range;
  if (Buffer.byteLength(raw, "utf8") > TINY_FILE_MAX_BYTES) return range;
  // BUG FIX (bench transcript forensics): raw.split(/\r?\n/).length counts a
  // PHANTOM final segment for content ending in a trailing newline (nearly
  // every file) — a 50-line file yielded lineCount=51, minting handle range
  // "1-51", which edit_code's OWN bounds check (util/countLines.ts's
  // countLines(), matching applyEditsMulti.ts's countLogicalLinesEntry())
  // then rejects as out of bounds for a 50-line file. countLines() is the
  // trailing-newline-aware count both the mint and the check now share.
  const lineCount = countLines(raw);
  if (lineCount > TINY_FILE_MAX_LINES || lineCount === 0) return range;
  return `1-${lineCount}`;
}

/**
 * Item 4 (tiny-file whole embed): true when embedding `relPath`'s whole file
 * as the code slice stays within the per-surface byte cap (MAX_SURFACE_CODE_BYTES).
 * A tiny file (by line/total-byte thresholds) can still be DENSE enough that
 * its whole body exceeds the per-slice cap — sliceCode would then reject it and
 * the surface would ship with NO code at all, strictly worse than the narrow
 * keyword window. Gating the whole-file DISPLAY range on this keeps the narrow
 * window in that case. Read-only; fail-closed (an unreadable file returns
 * false, so the caller keeps the widened window). Note: sliceCode's own cap
 * still runs afterward, so this is the display-range decision, not the final
 * embed guarantee.
 */
function wholeTinyFileFitsCap(workspace: string, relPath: string, wholeRange: string, cache?: FileReadCache): boolean {
  const raw = readCached(workspace, relPath, cache);
  if (raw === undefined) return false;
  const m = wholeRange.match(/^(\d+)-(\d+)$/);
  if (!m) return false;
  const end = parseInt(m[2]!, 10);
  const lines = raw.split(/\r?\n/);
  const slice = lines.slice(0, Math.min(end, lines.length)).join("\n");
  return Buffer.byteLength(slice, "utf8") <= MAX_SURFACE_CODE_BYTES;
}

/**
 * F1 (2026-08-07, round-trip-aware supply, cont'd): a high-confidence
 * candidate's `servedRange` is a locate-time GUESS at the relevant window —
 * literalConcernCandidates (and friends) center a fixed-width window on the
 * matched/causal-evidence line, not on a byte budget. When that guess
 * already covers a STRICT MAJORITY of the file's lines, promoting to the
 * whole file only tops off a window the heuristic already judged dominant;
 * a genuinely narrow, deliberately-scoped window (a small match inside a
 * small-but-mostly-irrelevant file) is left untouched. Returns the
 * whole-file range only as a CANDIDATE — the caller must still confirm it
 * fits the same per-surface cap via centeredSliceForCap before using it, so
 * this function never itself decides a byte budget.
 */
export function wholeFileRangeIfDominant(
  workspace: string,
  relPath: string,
  servedRange: string,
  cache?: FileReadCache,
): string | undefined {
  const m = /^(\d+)-(\d+)$/.exec(servedRange);
  if (!m) return undefined;
  const start = parseInt(m[1]!, 10);
  const raw = readCached(workspace, relPath, cache);
  if (raw === undefined) return undefined;
  const total = countLines(raw);
  if (total <= 0) return undefined;
  const end = Math.min(parseInt(m[2]!, 10), total);
  if (start <= 1 && end >= total) return undefined; // already the whole file
  const served = Math.max(0, end - start + 1);
  if (served * 2 <= total) return undefined; // not a majority — leave the deliberate window alone
  return `1-${total}`;
}

/** A large markdown file above this size gets section-widened (item 11) instead of left as a narrow keyword window or read whole natively. */
const LARGE_MARKDOWN_BYTES = 8192;

/** Cap on a widened markdown section's line count, so one huge section cannot swallow the whole surface budget. */
const MAX_MARKDOWN_SECTION_LINES = 150;
/** Minimum context around a Markdown hit when its section is short. */
const DOC_ANCHOR_RADIUS_LINES = 20;
/** Small file-complement threshold for one-shot Markdown embedding. */
const DOC_INLINE_REMAINDER_MAX_LINES = 60;

/**
 * Widen a candidate range inside a large markdown file to cover its whole
 * enclosing section (from the nearest heading at/above the range's start
 * line, to the next heading of the same-or-higher level) instead of a
 * fixed keyword-centered window. This is what lets task_pack hand the
 * agent the relevant slice of a big CONTRACT.md/SPEC.md as a normal
 * handle-bearing surface instead of leaving the agent to Read the whole
 * 50K+ char file natively to find the right section.
 *
 * Only applies to files at/above LARGE_MARKDOWN_BYTES — a small doc file
 * is already cheap to return in full via its existing range.
 */
function wholeMarkdownRangeIfSmallRemainder(
  workspace: string,
  relPath: string,
  range: string,
  cache?: FileReadCache,
): string | undefined {
  if (!isMarkdownPath(relPath)) return undefined;
  const m = /^(\d+)-(\d+)$/.exec(range);
  if (!m) return undefined;
  const raw = readCached(workspace, relPath, cache);
  if (raw === undefined) return undefined;
  const total = countLines(raw);
  const start = Number(m[1]);
  const end = Number(m[2]);
  if (!Number.isInteger(start) || !Number.isInteger(end) || start < 1 || end < start || total < end) {
    return undefined;
  }
  const remainder = (start - 1) + (total - end);
  return remainder <= DOC_INLINE_REMAINDER_MAX_LINES ? `1-${total}` : undefined;
}

function widenMarkdownSectionRange(
  workspace: string,
  relPath: string,
  range: string,
  cache?: FileReadCache,
): string {
  if (!isMarkdownPath(relPath)) return range;
  const m = range.match(/^(\d+)-(\d+)$/);
  if (!m) return range;
  const rangeStart = parseInt(m[1]!, 10);
  const rangeEnd = parseInt(m[2]!, 10);
  // The incoming range is typically a fixed keyword-centered window
  // (matchLine - 10 .. matchLine + 10, see buildCandidateDetails), so its
  // MIDPOINT recovers the actual matched line — using range.start directly
  // would anchor on the window's edge instead of the match itself (e.g. a
  // match on line 11 arrives as range "1-21"; the midpoint, 11, is correct;
  // range.start, 1, is not — it would land on an unrelated heading above
  // the true match, such as the document's own title).
  const matchLine = Math.round((rangeStart + rangeEnd) / 2);

  const raw = readCached(workspace, relPath, cache);
  if (raw === undefined) return range;
  if (Buffer.byteLength(raw, "utf8") < LARGE_MARKDOWN_BYTES) return range;

  const lineCount = countLines(raw);
  const headings = parseMarkdownHeadings(raw);
  const section = markdownSectionAtLine(headings, matchLine);
  if (!section) return range;
  const sectionEnd = Math.min(section.endLine, lineCount);
  const sectionLines = sectionEnd - section.line + 1;
  // A complete section is preferable when it fits the bounded anchor. For
  // short sections, also include a real context window around the hit; this
  // prevents a one-line heading/body match from becoming a one-line surface.
  if (sectionLines <= MAX_MARKDOWN_SECTION_LINES) {
    const contextStart = Math.max(1, matchLine - DOC_ANCHOR_RADIUS_LINES);
    const contextEnd = Math.min(lineCount, matchLine + DOC_ANCHOR_RADIUS_LINES);
    return `${Math.min(section.line, contextStart)}-${Math.max(sectionEnd, contextEnd)}`;
  }
  // A huge section cannot fit as one bounded anchor. Keep the hit plus at
  // least twenty lines of surrounding document context instead.
  const contextStart = Math.max(1, matchLine - DOC_ANCHOR_RADIUS_LINES);
  const contextEnd = Math.min(lineCount, matchLine + DOC_ANCHOR_RADIUS_LINES);
  return `${contextStart}-${contextEnd}`;
}

/** Max heading lines carried in a contract doc's outline field (deliverable 3). */
const MAX_CONTRACT_OUTLINE_HEADINGS = 30;

/** Max characters of a single heading's own text kept in the outline (post "#" markers). */
const MAX_CONTRACT_OUTLINE_HEADING_CHARS = 70;

/**
 * DESIGN-v0.8 §A5 deliverable 3: for a matched contract-doc candidate
 * (`why === "doc-contract-match"`, minted by locateTaskContext's
 * addMarkdownContractCandidates), build a compact heading OUTLINE of the
 * WHOLE document — each real ATX or Setext heading with its 1-based line number —
 * alongside the matched section widenMarkdownSectionRange already slices.
 * A multi-topic contract doc (a live fixture's contract doc covers several
 * unrelated bug topics under one file) otherwise leaves the agent with only the ONE
 * section that happened to match this query's terms and no way to see
 * what ELSE the doc covers without a native full-file read — the outline
 * is what turns a follow-up need into a targeted `read_file mode=slice`
 * over a known line range instead of another native dump.
 *
 * Reuses the shared fenced-code-aware Markdown heading parser used by section
 * widening, and follows the existing `outline: string[]` convention from readCodeSmallFile.ts
 * (`"L<n>: <heading text>"` entries) rather than inventing a new shape.
 *
 * Returns undefined when the path is not markdown, the file cannot be
 * read, or it has no headings at all — callers omit the field rather than
 * serialize an empty/meaningless array.
 */
function buildContractDocOutline(workspace: string, relPath: string, cache?: FileReadCache): string[] | undefined {
  if (!isMarkdownPath(relPath)) return undefined;

  const raw = readCached(workspace, relPath, cache);
  if (raw === undefined) return undefined;

  const outline = markdownOutline(
    parseMarkdownHeadings(raw),
    MAX_CONTRACT_OUTLINE_HEADINGS,
    MAX_CONTRACT_OUTLINE_HEADING_CHARS,
  );
  return outline.length > 0 ? outline : undefined;
}

// widenNativeSymbolRange (C/C++ enclosing-symbol range widener) and its
// MAX_NATIVE_SYMBOL_LINES cap now live in util/nativeSymbolRange.ts, shared
// with locateTaskContext.ts's equivalent widener — see that module's doc
// comment. This call site passes `cache` (structurally compatible with
// FileReadCache's `read`/`parsedSymbols` shape) so repeated widening calls
// within one pack build stay memoized, matching prior behavior exactly.

/**
 * FIX-B3: coerce one paths[] entry to the canonical object shape — a bare
 * string becomes `{path: <string>}`; an already-object entry passes through
 * unchanged. Mirrors server.ts's mode="full" batch-paths coercion
 * (`typeof p === "object" && p !== null ? String(p.path) : String(p)`), which
 * task_pack's own dispatch had not applied, silently turning a supplied bare
 * string into an empty path (see TaskPackArgs.paths's doc comment).
 */
function normalizePathEntry(p: TaskPackPathEntry | string): TaskPackPathEntry {
  return typeof p === "string" ? { path: p } : p;
}

type DiscoveredArtifactFile = ReturnType<typeof discoverArtifactFiles>[number];

interface MintedArtifactSurface {
  file: DiscoveredArtifactFile;
  bytes: Buffer;
  surface: ArtifactTaskPackSurface;
  credentialRef?: string;
  credentialPassword?: string;
}

/** Mint the exact discovery surface/handle shape shared with mode=artifact. */
function mintArtifactSurface(
  file: DiscoveredArtifactFile,
  workspace: string,
  args?: Pick<TaskPackArgs, "credentialRef" | "credentialPassword">,
): MintedArtifactSurface | undefined {
  try {
    // Stat-first (FIFO/device/oversize) before materializing a discovered
    // artifact: this read happens before any container preflight can run.
    statReadTargetSync(file.absPath, workspace);
    const bytes = fs.readFileSync(file.absPath);
    const handle = handleTable.upsert({
      kind: "file",
      path: file.relPath,
      workspaceRoot: workspace,
      sha: shaOfBytes(bytes),
    });
    return {
      file,
      bytes,
      ...(args?.credentialRef !== undefined ? { credentialRef: args.credentialRef } : {}),
      ...(args?.credentialPassword !== undefined ? { credentialPassword: args.credentialPassword } : {}),
      surface: {
        path: file.relPath,
        basename: path.posix.basename(file.relPath),
        kind: "artifact",
        // .tsv normalizes to the "csv" kind so a single csv branch downstream
        // (extractArtifactBuildSection) serves both; every other artifact ext
        // maps 1:1 to its kind (.xlsx→"xlsx", .pdf→"pdf", …).
        artifactKind: (file.ext === ".tsv" ? "csv" : file.ext.slice(1)) as ArtifactKind,
        size: bytes.byteLength,
        handle: handle.id,
        extract: `read_file mode=artifact path=${file.relPath}${
          args?.credentialRef !== undefined
            ? ` credentialRef=${JSON.stringify(args.credentialRef)}`
            : ""
        }`,
      },
    };
  } catch {
    return undefined;
  }
}

/** Mint strict discovery-only surfaces through mode=artifact's file-handle shape. */
function collectArtifactSurfaces(
  args: TaskPackArgs,
  workspace: string,
): ArtifactTaskPackSurface[] {
  const foundByPath = new Map<string, DiscoveredArtifactFile>();
  const query = args.query ?? args.symbol ?? "";
  const discoverSeed = (seed: string): DiscoveredArtifactFile[] => {
    const explicitArtifact = (ARTIFACT_EXTS as readonly string[])
      .includes(path.posix.extname(seed).toLowerCase());
    return discoverArtifactFiles(workspace, {
      path: seed,
      ...(explicitArtifact ? { includeAll: true } : { query }),
    });
  };
  if (args.paths !== undefined && args.paths.length > 0) {
    for (const entry of args.paths.map(normalizePathEntry)) {
      for (const file of discoverSeed(entry.path)) {
        foundByPath.set(file.relPath, file);
      }
    }
  } else if (args.path) {
    for (const file of discoverSeed(args.path)) foundByPath.set(file.relPath, file);
  } else {
    for (const file of discoverArtifactFiles(workspace, { query })) {
      foundByPath.set(file.relPath, file);
    }
  }

  return [...foundByPath.values()]
    .sort((a, b) =>
      artifactBuildRelevance(b, args, query) - artifactBuildRelevance(a, args, query)
        || a.relPath.localeCompare(b.relPath)
    )
    .slice(0, MAX_SURFACES_DISTINCT)
    .map((file) => mintArtifactSurface(file, workspace, args)?.surface)
    .filter((surface): surface is ArtifactTaskPackSurface => surface !== undefined);
}

const AGENT_GUIDE_PRIORITY = new Map<string, number>([
  ["agents.md", 0],
  ["claude.md", 1],
]);

function normalizedRequestPath(value: string): string {
  return value.replace(/\\\\/g, "/").replace(/^\.\//, "").replace(/\/$/, "").toLowerCase();
}

/**
 * Keep one same-directory agent guide by default so equivalent AGENTS/CLAUDE
 * sections do not consume two pack slots. Exact caller-named files remain
 * authoritative: naming both keeps both, while naming one makes it win.
 */
export function dedupeAgentGuideSurfaces(
  surfaces: TaskPackSurface[],
  args?: TaskPackArgs,
): TaskPackSurface[] {
  const explicitlyNamed = new Set<string>();
  if (args?.path) {
    const requested = normalizedRequestPath(args.path);
    if (AGENT_GUIDE_PRIORITY.has(path.posix.basename(requested))) explicitlyNamed.add(requested);
  }
  for (const entry of args?.paths?.map(normalizePathEntry) ?? []) {
    const requested = normalizedRequestPath(entry.path);
    if (AGENT_GUIDE_PRIORITY.has(path.posix.basename(requested))) explicitlyNamed.add(requested);
  }

  const groups = new Map<string, TaskPackSurface[]>();
  for (const surface of surfaces) {
    const normalized = normalizedRequestPath(surface.path);
    if (!AGENT_GUIDE_PRIORITY.has(path.posix.basename(normalized))) continue;
    const dir = path.posix.dirname(normalized);
    groups.set(dir, [...(groups.get(dir) ?? []), surface]);
  }

  const dropped = new Set<string>();
  for (const guides of groups.values()) {
    if (guides.length < 2) continue;
    const named = guides.filter((guide) => explicitlyNamed.has(normalizedRequestPath(guide.path)));
    const keep = new Set(
      (named.length > 0 ? named : [guides.reduce((best, guide) => {
        const bestRank = AGENT_GUIDE_PRIORITY.get(path.posix.basename(best.path.toLowerCase())) ?? 99;
        const rank = AGENT_GUIDE_PRIORITY.get(path.posix.basename(guide.path.toLowerCase())) ?? 99;
        return rank < bestRank ? guide : best;
      })]).map((guide) => guide.handle),
    );
    for (const guide of guides) if (!keep.has(guide.handle)) dropped.add(guide.handle);
  }
  return dropped.size === 0 ? surfaces : surfaces.filter((surface) => !dropped.has(surface.handle));
}

// ---------------------------------------------------------------------------
// A1 (2026-07-30) — CONTENT-BEARING ARTIFACT SURFACES.
//
// collectArtifactSurfaces (above) attaches every resolved Office/PDF file as a
// discovery STUB: path + size + handle + an `extract` call string. For an
// artifact_build pack that is fine — attachArtifactBuildSections inlines the
// extraction beside it. For every OTHER profile it was the whole story: the T14
// two-deck audit resolved both .pptx decks, served ZERO slide text in a 2.9 KB
// response (~21 KB of pack budget unused), certified `awaiting-input`, and cost
// two extra round trips to read content the server had already located. The
// distributed guide's contract already says "artifact packs need content-bearing
// artifact_sections per source"; this is that contract, honored everywhere.
//
// WHY A PREFETCH: extraction is async (Office/PDF parsing) but the pack's shared
// exit — dedupeTrimAndPersist, which attaches the artifact surfaces and then
// computes coverage, readiness obligations, the evidence model and the execution
// contract — is synchronous. Inlining AFTER that exit would serve content beside
// a contract that still says "unresolved: artifact content, ask the user", i.e.
// worse than the round trip it saves. So the async entry point prefetches the
// extractions FIRST (gated on the args/query actually naming an artifact file,
// so a code-only task pays nothing) and the sync exit consumes them BEFORE any
// readiness decision. Module-level, workspace-keyed, overwritten per call and
// cleared in a `finally` — the same idiom lastPackFingerprints uses.
// ---------------------------------------------------------------------------

interface PrefetchedArtifactSections {
  /** Pack-visible entries, already fitted to the per-artifact and total budgets. */
  entries: ArtifactTaskPackSectionEntry[];
  /** One `inlined[]` stamp per entry, same order. */
  stamps: string[];
  /** Normalized paths the caller named — trimToCap sheds these LAST. */
  callerNamed: Set<string>;
}

const artifactSectionPrefetch = new Map<string, PrefetchedArtifactSections>();

/** Test/reset hook parity with the other module-level pack caches. */
export function resetArtifactSectionPrefetch(): void {
  artifactSectionPrefetch.clear();
}

/** Same compaction artifactBuildRelevance uses for its basename-in-query score. */
function compactForMatch(value: string): string {
  return value.toLowerCase().replace(/[^a-z0-9\u3040-\u30ff\u3400-\u9fff]+/g, "");
}

const ARTIFACT_EXT_IN_TEXT_RE = /\.(?:docx|xlsx|pptx|pdf|csv|tsv)\b/i;

/**
 * Cheap, I/O-FREE gate: could this request name an artifact file at all? A pack
 * for a pure code task must not pay an artifact walk or an Office parse, so the
 * prefetch runs only when the args carry an artifact-extension path, or the
 * query itself names an artifact file or an Office kind ("slide deck", "Excel").
 */
function mayNameCallerArtifact(args: TaskPackArgs, query: string): boolean {
  const named = [
    ...(args.path !== undefined ? [args.path] : []),
    ...(args.paths ?? []).map(normalizePathEntry).map((entry) => entry.path),
  ];
  if (named.some((candidate) => ARTIFACT_EXT_SET.has(path.extname(candidate).toLowerCase()))) {
    return true;
  }
  return ARTIFACT_EXT_IN_TEXT_RE.test(query) || requestedArtifactKinds(query).length > 0;
}

/**
 * The artifacts this REQUEST named, as normalized paths. Deliberately narrow —
 * an artifact merely surfaced by query-token relevance stays a stub (that is the
 * candidate-ambiguity case the pack must not pre-commit to). Naming means one of:
 *   - the path appears in `paths[]` / `path` (exact caller identification), or
 *   - the file's basename, or its stem, appears verbatim in the query.
 */
function callerNamedArtifacts(
  surfaces: readonly ArtifactTaskPackSurface[],
  args: TaskPackArgs,
  query: string,
): Set<string> {
  const explicit = new Set(
    [
      ...(args.path !== undefined ? [args.path] : []),
      ...(args.paths ?? []).map(normalizePathEntry).map((entry) => entry.path),
    ]
      .filter((candidate) => ARTIFACT_EXT_SET.has(path.extname(candidate).toLowerCase()))
      .map(normalizedRequestPath),
  );
  const compactQuery = compactForMatch(query);
  const named = new Set<string>();
  for (const surface of surfaces) {
    const normalized = normalizedRequestPath(surface.path);
    if (explicit.has(normalized)) {
      named.add(normalized);
      continue;
    }
    const basename = path.posix.basename(surface.path);
    const stem = basename.slice(0, basename.length - path.posix.extname(basename).length);
    const compactBase = compactForMatch(basename);
    const compactStem = compactForMatch(stem);
    if (
      (compactBase.length > 0 && compactQuery.includes(compactBase))
      || (compactStem.length >= 4 && compactQuery.includes(compactStem))
    ) {
      named.add(normalized);
    }
  }
  return named;
}

/**
 * Reconstruct the minted shape extractArtifactBuildSection needs, keeping the
 * surface's own handle.
 *
 * THE CREDENTIAL TRAVELS (2026-08-14). `collectArtifactSurfaces` returns the
 * plain `ArtifactTaskPackSurface`, which has no slot for a credential, so this
 * reconstruction dropped the pair `mintArtifactSurface` had already resolved —
 * and `extractArtifactBuildSection` then called `prepareOfficeDocument(bytes,
 * undefined)` on an ENCRYPTED workbook, failed to decrypt, and returned only
 * the `next` hint. Measured: the identical query over a PLAINTEXT .xlsx inlines
 * `artifact_sections` with the sheet's rows; over the encrypted copy with a
 * valid `credentialRef` it inlined nothing and the pack degraded to
 * `await_input:"no-grounded-call-remains"`. The password is never stored on the
 * surface and never serialized — it rides only this in-memory wrapper, which is
 * exactly what the `credentialRef` indirection exists to guarantee.
 */
function remintPackArtifact(
  surface: ArtifactTaskPackSurface,
  workspace: string,
  args?: Pick<TaskPackArgs, "credentialRef" | "credentialPassword">,
): MintedArtifactSurface | undefined {
  try {
    const absPath = path.join(workspace, surface.path);
    statReadTargetSync(absPath, workspace);
    const bytes = fs.readFileSync(absPath);
    return {
      file: {
        relPath: surface.path,
        absPath,
        language: "default",
        ext: path.posix.extname(surface.path).toLowerCase(),
        kind: "artifact",
      },
      bytes,
      ...(args?.credentialRef !== undefined ? { credentialRef: args.credentialRef } : {}),
      ...(args?.credentialPassword !== undefined ? { credentialPassword: args.credentialPassword } : {}),
      surface,
    };
  } catch {
    return undefined;
  }
}

/**
 * The exact ONE call that fetches the entries a bounded inline left out,
 * addressed through the artifact's own handle so the caller never re-finds it.
 * mode=artifact addresses docx by heading, pptx by slide number, pdf by page;
 * a sheet has no sub-selector, so the whole sheet is named instead.
 */
function remainingArtifactSectionsCall(
  surface: ArtifactTaskPackSurface,
  section: ArtifactTaskPackSection,
  remaining: readonly string[],
): string {
  const base = `read_file mode=artifact handle=${surface.handle}`;
  if ("sheet" in section) return `${base} sheet=${section.sheet} as=json`;
  if (section.kind === "docx") return `${base} sections=${JSON.stringify(remaining.slice(0, MAX_REMAINING_SECTION_IDS))}`;
  if (section.kind === "pptx") {
    const slides = remaining
      .map((id) => /^Slide\s+(\d+)/.exec(id)?.[1])
      .filter((slide): slide is string => slide !== undefined)
      .slice(0, MAX_REMAINING_SECTION_IDS);
    return slides.length > 0 ? `${base} slides=${JSON.stringify(slides)}` : base;
  }
  const pages = remaining
    .map((id) => /^page-(\d+)$/.exec(id)?.[1])
    .filter((page): page is string => page !== undefined)
    .slice(0, MAX_REMAINING_SECTION_IDS);
  return pages.length > 0 ? `${base} pages=${JSON.stringify(pages)}` : base;
}

/** Bound the id list so `remaining_sections` stays a pointer, not an inventory. */
function boundedRemainingIds(remaining: readonly string[]): string[] {
  if (remaining.length <= MAX_REMAINING_SECTION_IDS) return [...remaining];
  return [
    ...remaining.slice(0, MAX_REMAINING_SECTION_IDS),
    `+${remaining.length - MAX_REMAINING_SECTION_IDS} more`,
  ];
}

/**
 * One pack-visible entry: the section fitted to `capBytes`, plus — only when the
 * fit had to cut something — the handle, the unserved ids, the exact one-call
 * follow-up, and a note saying what was cut.
 */
function buildArtifactSectionEntry(
  surface: ArtifactTaskPackSurface,
  section: ArtifactTaskPackSection,
  capBytes: number,
): ArtifactTaskPackSectionEntry {
  const fit = fitArtifactSectionToBytes(section, capBytes);
  const entry: ArtifactTaskPackSectionEntry = { path: surface.path, section: fit.section };
  if (!fit.partial) return entry;
  entry.content_completeness = "partial";
  entry.handle = surface.handle;
  if (fit.remaining.length > 0) {
    const served = artifactSectionEntryIds(fit.section).length;
    entry.remaining_sections = boundedRemainingIds(fit.remaining);
    entry.next = remainingArtifactSectionsCall(surface, fit.section, fit.remaining);
    entry.note = `inlined ${served} of ${served + fit.remaining.length} extracted sections`
      + ` (${ARTIFACT_SURFACE_INLINE_CAP_BYTES}B per-artifact inline cap); fetch the rest with next`;
  } else {
    entry.next = remainingArtifactSectionsCall(surface, fit.section, []);
    entry.note = "extraction was truncated at the extractor cap; fetch the full section with next";
  }
  return entry;
}

/**
 * Extract + budget-fit the caller-named artifacts of this request. Runs before
 * the pack is built (see the block comment above); stores nothing when the gate
 * fails, no artifact is caller-named, or every extraction came back empty.
 */
async function prefetchArtifactSurfaceSections(
  args: TaskPackArgs,
  workspace: string,
): Promise<void> {
  const query = args.query ?? args.symbol ?? args.path ?? "";
  if (!mayNameCallerArtifact(args, query)) return;
  let surfaces: ArtifactTaskPackSurface[];
  try {
    surfaces = collectArtifactSurfaces(args, workspace);
  } catch {
    return;
  }
  if (surfaces.length === 0) return;
  const callerNamed = callerNamedArtifacts(surfaces, args, query);
  if (callerNamed.size === 0) return;
  const selected = surfaces
    .filter((surface) => callerNamed.has(normalizedRequestPath(surface.path)))
    .slice(0, MAX_INLINED_ARTIFACT_SURFACES);

  const entries: ArtifactTaskPackSectionEntry[] = [];
  const stamps: string[] = [];
  let usedBytes = 0;
  for (const surface of selected) {
    const remainingBudget = ARTIFACT_SURFACE_INLINE_TOTAL_BYTES - usedBytes;
    if (remainingBudget <= 0) break;
    const minted = remintPackArtifact(surface, workspace, args);
    if (minted === undefined) continue;
    let extraction: ArtifactBuildExtraction;
    try {
      extraction = await extractArtifactBuildSection(minted, query);
    } catch {
      continue;
    }
    if (extraction.section === undefined || extraction.sectionId === undefined) continue;
    // Both budgets are measured over the WHOLE entry (path + honesty fields, not
    // just the section body), so the pack-visible total is what the caps promise.
    // The section is fitted twice at most: once to the nominal cap, then — if the
    // assembled entry's own overhead pushed it past the remaining total — again
    // to the cap minus that measured overhead.
    let entry = buildArtifactSectionEntry(
      surface,
      extraction.section,
      Math.min(ARTIFACT_SURFACE_INLINE_CAP_BYTES, remainingBudget),
    );
    let entryBytes = Buffer.byteLength(JSON.stringify(entry), "utf8");
    if (entryBytes > remainingBudget) {
      const overhead = entryBytes - Buffer.byteLength(JSON.stringify(entry.section), "utf8");
      const retryCap = remainingBudget - overhead;
      if (retryCap <= 0) break;
      entry = buildArtifactSectionEntry(surface, extraction.section, retryCap);
      entryBytes = Buffer.byteLength(JSON.stringify(entry), "utf8");
      if (entryBytes > remainingBudget) break;
    }
    entries.push(entry);
    stamps.push(`artifact-section:${surface.path}#${extraction.sectionId.slice(0, 120)}`);
    usedBytes += entryBytes;
  }
  if (entries.length === 0) return;
  artifactSectionPrefetch.set(workspace, { entries, stamps, callerNamed });
}

/**
 * Inline the prefetched extractions into a pack that carries the matching
 * artifact SURFACES. Called from the shared pack exit BEFORE coverage/readiness
 * so the certificate is computed over the artifact content actually served.
 *
 * Skipped when the pack already carries artifact content (`section` /
 * `artifact_sections` — the artifact_build flow, which does its own bundling)
 * and for any entry whose artifact is not a surface of THIS pack: an `inlined[]`
 * stamp must stay verifiable against the same response (attachSupply §11.4).
 * The whole-pack ceiling is the artifact-bundle tier, the highest transport-safe
 * cap that exists, so artifact bodies can never push a pack past it.
 */
function attachPrefetchedArtifactSections(result: TaskPackResult, workspace: string): void {
  if (result.pack_unchanged === true) return;
  if (result.section !== undefined || result.artifact_sections !== undefined) return;
  const prefetch = artifactSectionPrefetch.get(workspace);
  if (prefetch === undefined || prefetch.entries.length === 0) return;
  const artifactPaths = new Set(
    (result.surfaces as TaskPackResultSurface[])
      .filter(isArtifactTaskPackSurface)
      .map((surface) => normalizedRequestPath(surface.path)),
  );
  const entries: ArtifactTaskPackSectionEntry[] = [];
  const stamps: string[] = [];
  for (const [index, entry] of prefetch.entries.entries()) {
    if (!artifactPaths.has(normalizedRequestPath(entry.path))) continue;
    const candidate: TaskPackResult = {
      ...result,
      artifact_sections: [...entries, entry],
      inlined: [...(result.inlined ?? []), ...stamps, prefetch.stamps[index]!],
    };
    const bytes = Buffer.byteLength(JSON.stringify(candidate), "utf8");
    if (bytes > Math.max(capForResult(candidate), MAX_TASK_PACK_BYTES_ARTIFACT_BUNDLE)) break;
    entries.push(entry);
    stamps.push(prefetch.stamps[index]!);
  }
  if (entries.length === 0) return;
  result.artifact_sections = entries;
  result.inlined = [...(result.inlined ?? []), ...stamps];
}

/** Normalized paths of the artifacts THIS request named, for trimToCap's shed order. */
function prefetchedCallerNamedArtifacts(workspace: string): ReadonlySet<string> {
  return artifactSectionPrefetch.get(workspace)?.callerNamed ?? new Set<string>();
}

interface GenericTaskPackFallback {
  attempted: boolean;
  paths: string[];
  probe?: string;
}

/**
 * Last-resort generic-text discovery for task_pack.
 *
 * This deliberately runs only after the semantic locator has produced no rich
 * or weak candidate. It therefore cannot perturb an already-resolved pack's
 * ranking, surface bytes, route, or call count. The walker may also return
 * tracked files for callers that request a union, so the server-issued
 * `generic-text` kind is the authority for this unknown/extensionless lane.
 */
function discoverGenericTaskPackFallback(
  args: TaskPackArgs,
  query: string,
  workspace: string,
): GenericTaskPackFallback {
  if (!genericTextDiscoveryEnabled() || args.lang !== undefined) {
    return { attempted: false, paths: [] };
  }

  const omissions = createWalkOmissions();
  const files = walkCodeFiles(workspace, {
    ...(args.path ? { subPath: args.path } : {}),
    includeGenericText: true,
    respectGitignore: true,
    omissions,
  }).filter((file) => file.kind === "generic-text");

  // An exact caller path is stronger evidence than query wording. If it is a
  // policy-admissible generic text file, serve it directly even when the query
  // merely asks to inspect/read it and contains no literal from its body.
  if (args.path && !isDirectoryWithin(args.path, workspace)) {
    const explicitPath = normalizedRequestPath(args.path);
    const explicit = files.find((file) => file.relPath === explicitPath);
    if (explicit !== undefined) {
      return { attempted: true, paths: [explicit.relPath] };
    }
  }

  const probes = orderedUniqueTokens([
    ...(args.symbol ? [args.symbol] : []),
    ...concreteIdentifierTokens(query),
    ...concernAnchorTokens(query),
  ])
    .filter((token) => token.length >= 4)
    .slice(0, 6);
  if (files.length === 0 || probes.length === 0) {
    return { attempted: true, paths: [], ...(probes[0] ? { probe: probes[0] } : {}) };
  }

  const contentCache = createScanContentCache();
  const byPath = new Map<string, { probes: Set<string>; firstLine: number }>();
  for (const probe of probes) {
    const matches = scanLiteral(probe, workspace, {
      caseInsensitive: true,
      files,
      contentCache,
    });
    for (const match of matches) {
      const current = byPath.get(match.path) ?? { probes: new Set<string>(), firstLine: match.line };
      current.probes.add(probe.toLowerCase());
      current.firstLine = Math.min(current.firstLine, match.line);
      byPath.set(match.path, current);
    }
  }

  const paths = [...byPath.entries()]
    .sort((left, right) =>
      right[1].probes.size - left[1].probes.size
      || left[1].firstLine - right[1].firstLine
      || left[0].localeCompare(right[0])
    )
    .slice(0, MAX_SURFACES_DISTINCT)
    .map(([relPath]) => relPath);
  return { attempted: true, paths, probe: probes[0] };
}

// ---------------------------------------------------------------------------
// Main export
// ---------------------------------------------------------------------------

/**
 * Async entry point. Prefetches caller-named artifact extractions (A1) so the
 * synchronous pack exit can serve them as content, then builds the pack.
 */
export async function buildTaskPack(
  args: TaskPackArgs,
  workspace: string,
): Promise<TaskPackResult> {
  artifactSectionPrefetch.delete(workspace);
  // T1: resolve the effective byte ceiling ONCE per call and hold it for the
  // entire async lifetime of this call (prefetch AND core) -- see
  // packByteCeilingStorage's doc comment above for why this is an ALS run()
  // rather than a plain local variable capForResult could not see anyway.
  const byteCeiling = resolveCallerByteCeiling(
    args.maxBytes,
    args.maxTokens,
    args.clientDefaultByteCeilingHint,
  );
  return packByteCeilingStorage.run(byteCeiling, async () => {
    try {
      await prefetchArtifactSurfaceSections(args, workspace);
    } catch {
      // A failed prefetch must never fail the pack — it degrades to the
      // pre-A1 behavior (artifact stubs + the `extract` call).
      artifactSectionPrefetch.delete(workspace);
    }
    try {
      return await buildTaskPackCore(args, workspace);
    } finally {
      artifactSectionPrefetch.delete(workspace);
    }
  });
}

// ---------------------------------------------------------------------------
// T2 (2026-08-27, v0.12 wave D2): pre-pipeline single-site short circuit.
//
// A natural-language generic edit query often already names an exact
// workspace file inline ("packages/backend/src/telemetry/estimator.ts の
// PACKET_LOSS_FAILURE を 0.12->0.15") without using the structured `paths`
// argument. Before this, such a query fell all the way through to the
// pathless multi-layer locator (locateTaskContext) -- the same expensive,
// whole-repository candidate search a completely unscoped query pays for --
// even though the ONE relevant file and the ONE relevant identifier are both
// already unambiguously named. When that identifier/literal resolves to
// exactly one occurrence in that one file, singleSiteShortCircuitSeed hands
// buildSeededTaskPack an EXPLICIT, identifier-centered range (never the whole
// file, never a caller-inferred anchor-focus window), so the served pack
// stays small end-to-end while still running through the exact same
// certificate/obligation/canonical-decision machinery every other seeded
// pack does. This is deliberately the "narrow the pipeline's evidence"
// design, not a hand-rolled execution_contract: buildTaskExecutionContract's
// obligation/falsification/risk/certificate chain is reused unmodified via
// the shared dedupeTrimAndPersist exit, so every canonicalDecision invariant
// is satisfied by construction rather than re-approximated here.
//
// Fails OPEN (returns undefined -> the normal profile routing below runs
// unchanged) on any ambiguity: more or fewer than one existing path named in
// the query, more or fewer than one occurrence of the bound literal/
// identifier in that file, or a query shaped like a batch/independent-
// concern/rename request. The batch/rename exclusion signals are exactly the
// ones attachSingleSiteUniqueMatchFastPath already trusts for the identical
// safety reason: such a request must never be approximated as a single-site
// edit, however cleanly a path happens to parse out of its text.
// Byte, not line, budget for the identifier-centered slice -- see the
// centeredSliceForCap call below for why (dense vs. sparse real code lines).
const SINGLE_SITE_SHORT_CIRCUIT_MAX_CODE_BYTES = 700;
const SINGLE_SITE_SHORT_CIRCUIT_PATH_RE = /(?:[A-Za-z]:)?(?:[\w.-]+[/\\])+[\w.-]+\.[A-Za-z][A-Za-z0-9]{0,9}\b/g;

function singleSiteShortCircuitSeed(
  query: string,
  workspace: string,
): TaskPackPathEntry | undefined {
  if (query.trim().length === 0) return undefined;
  if (hasIndependentEditConcernClauses(query) || hasExplicitIndependentConcernMarker(query)) return undefined;
  if (/(?:,|;|\n)\s*(?:and\s+)?(?:also\s+)?(?:fix|add|implement|change|replace|rename|refactor|wire|connect|update|create)\b/iu.test(query)
    || /(?:、|。|\n)\s*(?:さらに|加えて)?[^\n]{0,80}(?:修正|追加|実装|変更|置換|改名|接続|作成)/u.test(query)) return undefined;
  if (/\b(?:rename|renamed|renaming)\b/iu.test(query) || /(?:改名|リネーム)/u.test(query)) return undefined;

  const candidates: Array<{ relPath: string; real: string }> = [];
  const seenRelPaths = new Set<string>();
  for (const match of query.matchAll(SINGLE_SITE_SHORT_CIRCUIT_PATH_RE)) {
    const relPath = match[0]!.replace(/\\/g, "/").replace(/^\.\//, "");
    if (seenRelPaths.has(relPath)) continue;
    seenRelPaths.add(relPath);
    const abs = safeResolve(relPath, workspace);
    if (abs === undefined) continue;
    try {
      const real = fs.realpathSync(abs);
      if (!isWithin(real, resolveReal(workspace)) || !fs.statSync(real).isFile()) continue;
      candidates.push({ relPath, real });
    } catch {
      continue;
    }
    if (candidates.length > 1) break;
  }
  if (candidates.length !== 1) return undefined;
  const { relPath, real } = candidates[0]!;

  let buf: Buffer;
  try {
    buf = fs.readFileSync(real);
  } catch {
    return undefined;
  }
  const content = decodeTextBuffer(buf);
  if (content === null) return undefined;

  // Bind to a literal already provable inside this exact file: the parsed
  // replacement's search text when T1 recognized one, else the strongest
  // explicit identifier the query names. Neither is inferred from prose --
  // both come from the same conservative extractors the rest of this file
  // already trusts.
  const parsedReplacement = exactReplacementFromQuery(query);
  const needle = parsedReplacement === undefined
    ? undefined
    : "identifierOldValue" in parsedReplacement
      ? parsedReplacement.identifierOldValue
      : parsedReplacement.search;
  const identifierCandidates = needle !== undefined ? [needle] : explicitCodeIdentifiers(query);
  if (identifierCandidates.length === 0) return undefined;

  let matchLine: number | undefined;
  for (const identifier of identifierCandidates) {
    if (identifier.length === 0) continue;
    if (content.split(identifier).length - 1 !== 1) continue;
    const offset = content.indexOf(identifier);
    matchLine = content.slice(0, offset).split(/\r?\n/).length;
    break;
  }
  if (matchLine === undefined) return undefined;

  // Byte-budgeted, not line-budgeted: dense real code (long lines) and
  // sparse code (short lines) both land under the same response-size target
  // this way, reusing the exact growth algorithm every other slice in this
  // file already goes through (centeredSliceForCap) rather than a second,
  // hand-rolled approximation of it. Seed a throwaway cache with the buffer
  // already decoded above so this does not re-read the file from disk.
  const totalLines = content.split(/\r?\n/).length;
  const sliceCache = new FileReadCache();
  sliceCache.seed(relPath, content);
  const slice = centeredSliceForCap(
    workspace, relPath, `1-${totalLines}`, sliceCache, matchLine, SINGLE_SITE_SHORT_CIRCUIT_MAX_CODE_BYTES,
  );
  if (slice === undefined) return undefined;
  // Recover the served bounds from centeredSliceForCap's remaining_ranges
  // by POSITION relative to matchLine, not by boundary VALUE: T3's own
  // DEFECT B fix (2026-08-27) caps each side's disclosed width, so a
  // "before" entry's start is no longer guaranteed to be exactly 1 (nor an
  // "after" entry's end exactly totalLines) once the true leftover exceeds
  // that cap -- matching on those exact values silently produced the
  // WRONG served range (falling back to the whole-file default) the
  // moment a real target file was large enough to trigger the cap.
  let start = 1;
  let end = totalLines;
  for (const remaining of slice.remaining_ranges) {
    const bounds = /^(\d+)-(\d+)$/.exec(remaining);
    if (bounds === null) continue;
    const [, remStart, remEnd] = bounds.map(Number) as [number, number, number];
    if (remEnd < matchLine) start = remEnd + 1;
    else if (remStart > matchLine) end = remStart - 1;
  }
  return { path: relPath, range: `${start}-${end}` };
}

async function buildTaskPackCore(
  args: TaskPackArgs,
  workspace: string,
): Promise<TaskPackResult> {
  const query = args.query ?? args.symbol ?? args.path ?? "";

  // F-R14 (2026-08-22, DESIGN-v0.11 §9 residual): a create-with-content
  // query's fenced block is the new file's own body, not a locating signal
  // — see queryNamesCreateTarget/stripFencedBlocksForLocating below for the
  // gate and the strip. `locatingQuery` feeds the locator/evidence/concern/
  // wiring consumers in this function; `query` itself is untouched and
  // still feeds create-target resolution (dedupeTrimAndPersist's
  // effectiveQuery -> resolveCreateTargetFromQuery), the wire task.query
  // echo, profile binding, and the pack fingerprint/dedupe ledger. A
  // non-create query or a create query with no fenced block leaves
  // locatingQuery === query (byte-identical).
  const locatingQuery = queryNamesCreateTarget(query)
    ? stripFencedBlocksForLocating(query)
    : query;

  // Guard 2 (2026-07-12b decoy-fix forensics): harvest this query's FIX-A-
  // filtered concern-anchor tokens into the session's rolling set so a later
  // PARTIAL slice of a file can warn when the interesting region — the one
  // this very query is about — sits outside what got served (see
  // readCodeModes.ts resolveSlice's range path / buildConcernNote). Fired
  // unconditionally, before any redirect/cache-dedup/branch below, so every
  // call into buildTaskPack contributes regardless of which branch ends up
  // serving it.
  //
  // W9 (root-leak forensics): harvest from args.query / args.symbol only — a
  // bare `path` fallback (no query/symbol given) is a structural locator the
  // pack already resolves, not a free-text concern; recording it made a
  // path-only task_pack call harvest its own directory name as if it were a
  // query. Whatever text IS harvested is also scrubbed of the workspace
  // root's own folder name and any path-shaped spans BEFORE tokenization
  // (concernHarvestText), so a query that happens to mention the project by
  // folder name ("fix the m365-drive-mount project ...") cannot leak that
  // name in as a concern token either.
  const concernHarvestSource = args.query ?? args.symbol ?? "";
  // F-R14: concernHarvestSource mirrors `query` whenever args.query/symbol is
  // what fed it (the common case for a create-with-content request) — reuse
  // the already-stripped locatingQuery then, rather than re-deriving a
  // second strip decision. The rarer args.path-fallback case
  // (concernHarvestSource === "") is intentionally left untouched, matching
  // the existing W9 exclusion of the path fallback from concern harvesting.
  const locatingConcernHarvestSource = concernHarvestSource === query
    ? locatingQuery
    : concernHarvestSource;
  recordConcernTokens(workspace, concernAnchorTokens(concernHarvestText(workspace, locatingConcernHarvestSource)));

  // General project-root model (DESIGN-v0.8 §A2): register this workspace so
  // the pack's own projectRootOf(path) root-grouping (dominant-root vote for
  // verify-hint scoping, in-root inventory, native-wiring confinement) uses
  // the same marker/VCS-based resolver the locator does — not a bench-specific
  // path boundary. buildSeededTaskPack / the diff path all run under this.
  setActiveRootWorkspace(workspace);

  // Agents sometimes use the prose sentinel "auto" as though it were a real
  // path, or pass broad guessed directories before the target project has been
  // located. Neither is exact-file evidence. Drop the sentinel and, for a
  // strongly shaped task that names one uniquely resolvable project root,
  // convert directory-only guesses into that project exploration scope. Exact
  // file paths remain authoritative.
  const cleanedPath = args.path && normalizedRequestPath(args.path) !== "auto"
    ? args.path
    : undefined;
  const cleanedEntries = (args.paths ?? [])
    .map(normalizePathEntry)
    .filter((entry) => normalizedRequestPath(entry.path) !== "auto");
  const cleanedProfile = bindTaskProfile(args.taskProfile, query).selected;
  const directoryLike = (candidatePath: string): boolean =>
    isDirectoryWithin(candidatePath, workspace) || path.extname(candidatePath) === "";
  const directoryEntries = cleanedEntries.filter((entry) => directoryLike(entry.path));
  const exactFileEntries = cleanedEntries.filter((entry) => !directoryLike(entry.path));
  const directoryOnly = cleanedEntries.length > 0
    && cleanedEntries.every((entry) => directoryLike(entry.path));
  const inferredNamedProjectScope = cleanedProfile !== "generic"
    && !args.symbol
    && (!cleanedPath || directoryLike(cleanedPath))
    ? inferQueryProjectScope(workspace, locatingQuery)
    : undefined;
  const directoryGuessesMissNamedScope = inferredNamedProjectScope !== undefined
    && directoryEntries.length > 0
    && !directoryEntries.some((entry) =>
      isUnderAncestor(inferredNamedProjectScope, normalizedRequestPath(entry.path))
    );
  const namedProjectScope = inferredNamedProjectScope !== undefined
    && (
      cleanedEntries.length === 0
      || directoryOnly
      // Preserve exact files such as a caller-named contract, but do not let
      // an unrelated broad directory guess confine a strongly named project
      // out of existence. This repairs mixed calls like [generic-dir,
      // root-contract] without weakening exact-file authority.
      || directoryGuessesMissNamedScope
    )
      ? inferredNamedProjectScope
      : undefined;
  const repairedEntries = namedProjectScope
    ? directoryOnly || cleanedEntries.length === 0
      ? [{ path: namedProjectScope }]
      : [
          { path: namedProjectScope },
          ...exactFileEntries.filter((entry) =>
            normalizedRequestPath(entry.path) !== normalizedRequestPath(namedProjectScope)
          ),
        ]
    : cleanedEntries;
  args = {
    ...args,
    ...(namedProjectScope
      ? { path: undefined, paths: repairedEntries }
      : {
          path: cleanedPath,
          paths: cleanedEntries.length > 0 ? cleanedEntries : undefined,
        }),
  };

  // Some clients emit both path and paths=[path] for the same already-known
  // answer target. That duplicate is one exact file, not a multi-file seeded
  // request. Normalize it before branch routing so semantic-responsibility
  // resolution is not accidentally bypassed by a syntactic duplicate.
  if (cleanedProfile === "answer" && !args.symbol) {
    const entries: TaskPackPathEntry[] = [
      ...(args.path ? [{ path: args.path }] : []),
      ...(args.paths ?? []).map(normalizePathEntry),
    ];
    const unique = new Map(
      entries.map((entry) => [normalizedRequestPath(entry.path), entry] as const),
    );
    const only = [...unique.values()][0];
    if (
      args.path !== undefined
      && unique.size === 1
      && only !== undefined
      && !directoryLike(only.path)
      && entries.every((entry) => entry.range === undefined && entry.symbol === undefined)
    ) {
      args = { ...args, path: only.path, paths: undefined };
    }
  }

  // B1c (2026-08-01 retrieval-scope): a candidate choice is still PENDING and
  // the caller re-asked with nothing but the query. Re-deriving from scratch
  // reproduces the same ambiguity — a second candidate list over the same
  // files, which is the degradation T05c rep0 spent turns on. Bind the pending
  // candidates as paths so the re-pack CONVERGES on them (the seeded flow then
  // serves their bodies and, with the choice resolved, the brake releases).
  // Never overrides a caller who supplied their own scope, and never fires
  // outside an armed brake (a resolved choice, an edit, or taskEpoch:"new" all
  // clear it — see state/session.ts's candidatePackPending).
  if (
    !args.path
    && !args.symbol
    && !(args.paths?.length)
    && query.trim().length > 0
    && isCandidateListPackPending(workspace)
  ) {
    const pending = pendingCandidatePaths.get(path.resolve(workspace));
    // Only a RESTATEMENT of the pending request may inherit its candidates —
    // the session brake stays armed across an unrelated next task, and binding
    // that task to the previous one's files would be a hard mis-scope.
    const bindable = pending !== undefined && isPendingCandidateRestatement(pending, query)
      ? pending.paths.filter((rel) => {
          const abs = safeResolve(rel, workspace);
          return abs !== undefined && fs.existsSync(abs);
        })
      : [];
    if (bindable.length > 0) {
      args = { ...args, paths: bindable.map((rel) => ({ path: rel })) };
    }
  }

  if (shouldRedirectToOverview(args, locatingQuery)) {
    return {
      mode: "task_pack",
      coverage: "partial",
      surfaces: [],
      missing: [],
      ok: false,
      reason: "broad-overview-query",
      next: "read_file mode=overview",
      alternatives: [
        "read_file mode=overview",
        "read_file mode=map query=<specific subsystem>",
        "search_files action=symbols query=<specific symbol>",
      ],
    };
  }

  // Ensure the repo handle exists so read_code can canonicalize against it.
  handleTable.upsert({ kind: "repo", workspaceRoot: workspace });

  // -------------------------------------------------------------------------
  // Behavior 3: task_pack re-call dedup. Compute the request fingerprint ONCE
  // and, if this call exactly re-issues the last served pack for this
  // workspace AND every surface file is unchanged, return the sub-1KB compact
  // acknowledgement instead of recomputing the whole pack. Placed before every
  // build branch (seeded / diff / locate) so any flow's prior pack can dedup a
  // repeat. A differing "extra" arg (path/symbol/lang/limit) or any changed
  // surface file makes tryServeCachedPack return undefined -> full recompute.
  // -------------------------------------------------------------------------
  // PI-09 close-out: `force_serve:true` says the caller's context was
  // compacted, so every proof this dedup rests on ("you already hold these
  // bytes") is proof about a context that no longer exists. The two receipt
  // paths below are skipped outright — the SAFE direction, more bytes never
  // fewer — while the record capture at the end of the build is untouched, so
  // the caller's next ordinary call dedups against the freshly served pack.
  const fingerprint = computePackFingerprint(args, workspace);
  const forceServe = args.forceServe === true;
  const cached = forceServe ? undefined : tryServeCachedPack(workspace, args, fingerprint);
  if (cached !== undefined) return cached;
  // iter-3 F2: the exact-fingerprint dedup misses a re-issue that was merely
  // truncated mid-word (or tokenizes slightly differently) though it names the
  // same working set. Recognize such a near-identical re-ask and serve the same
  // compact receipt, gated on path-subset ∧ high token overlap ∧ unchanged
  // workspace so distinct-concern packs never collapse into one another.
  const semanticDup = forceServe ? undefined : tryServeSemanticDuplicatePack(workspace, args);
  if (semanticDup !== undefined) return semanticDup;

  const initialProfile = bindTaskProfile(args.taskProfile, query).selected;

  if (!args.path && !args.symbol && !(args.paths?.length)) {
    const docPath = rootMarkdownTitleMatch(workspace, query);
    if (docPath !== undefined) {
      return buildSeededTaskPack({ ...args, paths: [docPath], taskProfile: "answer" }, query, workspace);
    }
  }

  // T2: scoped to the exact shape attachSingleSiteUniqueMatchFastPath already
  // trusts (a plain, non-specialized generic edit request naming no
  // structured path) so every other profile's routing below is completely
  // unaffected, and placed before it so a real hit never pays for the
  // multi_concern/wiring/artifact_build/answer builders' own discovery cost.
  if (
    initialProfile === "generic"
    && !args.path
    && !args.symbol
    && !(args.paths?.length)
  ) {
    const shortCircuitSeed = singleSiteShortCircuitSeed(query, workspace);
    if (shortCircuitSeed !== undefined) {
      return buildSeededTaskPack({ ...args, paths: [shortCircuitSeed] }, query, workspace);
    }
  }

  // A multi-concern request must keep its concern boundaries when the caller
  // narrows a follow-up with paths=[...]. Generic seeded routing discards the
  // per-concern contract and can collapse several independent defects into one
  // role-ranked surface set. Try the specialized builder before path seeding;
  // it applies the supplied paths as a hard file scope below.
  if (initialProfile === "multi_concern") {
    const multiConcernPack = await buildMultiConcernTaskPack(args, query, workspace);
    if (multiConcernPack !== undefined) return multiConcernPack;
  }

  const suppliedPathEntries = (args.paths ?? []).map(normalizePathEntry);
  const directoryScopesOnly = suppliedPathEntries.length > 0
    && suppliedPathEntries.every((entry) => directoryLike(entry.path));
  const singleDirectoryScope = suppliedPathEntries.length === 1
    && isDirectoryWithin(suppliedPathEntries[0]!.path, workspace)
    ? suppliedPathEntries[0]!.path
    : undefined;
  const scopedSpecializedArgs = singleDirectoryScope
    ? { ...args, path: singleDirectoryScope, paths: undefined }
    : args;

  // A pathless propagation request often names the definition and several
  // consumers explicitly (enum/type, aggregate, UI component, etc.). Resolve
  // those identifiers into a bounded seed union before the generic locator
  // chooses a single primary. The seeded builder then applies the normal
  // required-role, style, closure, and change-contract machinery.
  // D10 (2026-08-14): `TL_EVIDENCE_RELATIONS` is deleted; relation-backed
  // propagation is unconditional.
  if (
    initialProfile === "change_propagation"
    || (
      (!(args.paths?.length) || directoryScopesOnly)
      && initialProfile === "wiring"
    )
  ) {
    const propagationPack = await buildPropagationTaskPack(args, query, workspace);
    if (propagationPack !== undefined) return propagationPack;
  }

  // A single directory in paths[] is a search scope, not a declaration that
  // every file below it is already an exact candidate. Preserve specialized
  // answer/artifact selection inside that scope before the exact-file seeded
  // branch below.
  if ((!(args.paths?.length) || directoryScopesOnly) && initialProfile === "artifact_build") {
    const artifactArgs = singleDirectoryScope !== undefined
      ? scopedSpecializedArgs
      : { ...args, paths: undefined };
    const artifactPack = await buildArtifactTaskPack(artifactArgs, query, workspace);
    if (artifactPack !== undefined) return artifactPack;
  }
  if ((!(args.paths?.length) || directoryScopesOnly) && initialProfile === "answer") {
    const answerArgs = singleDirectoryScope !== undefined
      ? scopedSpecializedArgs
      : { ...args, paths: undefined };
    const answerPack = await buildAnswerTaskPack(answerArgs, query, workspace);
    if (answerPack !== undefined) return answerPack;
  }

  // -------------------------------------------------------------------------
  // A1: caller-supplied paths[] seeding. When the agent already knows the
  // exact files a task needs (e.g. replaying a prior locate, or following a
  // partial pack's own surfaces), seed those files as surfaces directly
  // instead of running the pathless locator, which returns cross-fixture and
  // host-repo noise when a query happens to text-match unrelated files.
  // Placed BEFORE the symptom-shaped diff routing and the pathless locate
  // call: an explicit paths[] is a stronger signal than either.
  // -------------------------------------------------------------------------
  if (args.paths && args.paths.length > 0) {
    return buildSeededTaskPack(args, query, workspace);
  }

  // DESIGN-v0.9 §8 WS3: specialized profiles branch before generic locator
  // augmentation. artifact_build wins before answer/generic because an
  // implement-from-document query necessarily carries a positive edit marker.
  // A builder that cannot bind (notably: no discoverable artifact) falls
  // through to the generic pack, preserving the low-confidence contract.
  const profile = initialProfile;
  if (profile === "artifact_build") {
    const artifactPack = await buildArtifactTaskPack(args, query, workspace);
    if (artifactPack !== undefined) return artifactPack;
  }
  if (profile === "answer") {
    const answerPack = await buildAnswerTaskPack(args, query, workspace);
    if (answerPack !== undefined) return answerPack;
  }

  // -------------------------------------------------------------------------
  // Symptom-shaped (fix/bug) routing: when the query describes a defect
  // rather than a feature addition, the working tree's own drift from its
  // git baseline is almost always the actual answer (the changed lines ARE
  // the bug), so return DRIFT surfaces instead of running the enum-variant
  // template against files the query happens to text-match. Falls through
  // to the normal locate flow when there is no usable diff (clean tree, no
  // git repo, or query is not path/symbol-scoped in a way locate wants).
  // -------------------------------------------------------------------------
  if (!args.path && !args.symbol && isSymptomShapedQuery(query)) {
    const diffPack = await buildDiffTaskPack(query, workspace);
    if (diffPack) {
      // DESIGN-v0.8 coverage-honesty (Part 5): a symptom/bug query records its
      // advisory check line as a token-less record (never reported open by
      // closure tracking), so the diff flow still populates session checks.
      const diffRecords: PackCheckRecord[] = (diffPack.checks ?? []).map((desc) => ({ id: deriveCheckId({ desc }), desc }));
      return dedupeTrimAndPersist(workspace, diffPack, { query, checkRecords: diffRecords, args });
    }
  }

  // Locate task surfaces using the existing multi-layer locator.
  const locateResult = await locateTaskContext(workspace, {
    action: "locate",
    query: locatingQuery,
    ...(args.path ? { path: args.path } : {}),
    ...(args.symbol ? { symbol: args.symbol } : {}),
    ...(args.lang ? { lang: args.lang } : {}),
    limit: Math.min(args.limit ?? MAX_SURFACES_DISTINCT, MAX_SURFACES_DISTINCT),
  });

  if (!locateResult.hit) {
    const requiredRoles = requiredSurfacesForTask(locatingQuery, workspace, args.surfaceRoles);
    // M3: a pathless request can leave the locator with only weak
    // candidates even when its query uniquely names one project subtree. The
    // resolver's inference is deliberately conservative (undefined unless it
    // has one high-confidence root); when it does bind, re-enter the existing
    // seeded path exactly once so the normal readiness/coverage machinery owns
    // the result. This does NOT promote the legacy partial pack to complete.
    const inferredScope = !args.path
      && !args.symbol
      && !(args.paths?.length)
      ? inferQueryProjectScope(workspace, locatingQuery)
      : undefined;
    if (inferredScope !== undefined) {
      const retried = await buildSeededTaskPack(
        { ...args, paths: [{ path: inferredScope }] },
        query,
        workspace,
      );
      // A directory-scoped retry that still cannot surface any evidence is an
      // abstain, not a license to hide the original locator's honest partial
      // candidate list. Fall through to that legacy path unchanged.
      if (retried.surfaces.length > 0) {
        const result: TaskPackResult = {
          ...retried,
          scope_inferred: { path: inferredScope, reason: "pathless-locator-abstain" },
        };
        // The seeded builder cached its internal paths[] call. Rebind the
        // cache to the caller's original pathless request, exactly as the
        // existing propagation/identity seeded paths do.
        captureServedPack(workspace, args, computePackFingerprint(args, workspace), result);
        return result;
      }
    }

    // Check if the abstain response has candidate details we can use.
    const details = locateResult.candidateDetails;
    if (details && details.length > 0) {
      // Build partial surfaces from candidate details using surface grouping.
      return buildPartialPack(details, args, query, workspace);
    }
    // turn-economy wave 3 (V1, requirement b): the locator abstained WITHOUT rich
    // candidateDetails but still ranked weak top candidates it could not
    // confidently pick. Rather than hand back a bare pointer that costs the caller
    // a whole extra turn to fetch those files (the very anti-pattern this wave
    // closes), SEED them as real surfaces so their code ships NOW and the caller
    // can judge them. Skipped when (a) the evidence was OUT-OF-ROOT — the
    // re-scope `next` below is the right guidance, not in-scope noise — or (b) the
    // query is a create/placement request, whose answer is the create ROUTE that
    // the bare envelope resolves downstream in dedupeTrimAndPersist (seeding
    // unrelated files would bury it under noise surfaces).
    if (!locateResult.rootSuggestion && !hasCreateOrPlacementIntent(locatingQuery)) {
      const weakPaths: string[] = [];
      const seenWeak = new Set<string>();
      for (const cand of locateResult.candidates ?? []) {
        if (seenWeak.has(cand.path)) continue;
        seenWeak.add(cand.path);
        const abs = safeResolve(cand.path, workspace);
        if (abs !== undefined && fs.existsSync(abs)) weakPaths.push(cand.path);
      }
      if (weakPaths.length > 0) {
        return buildSeededTaskPack({ ...args, paths: weakPaths.map((p) => ({ path: p })) }, query, workspace);
      }
    }

    // P1 generic-text lane: only the final, truly empty semantic state may
    // widen to unknown-extension / extensionless UTF-8 files. A hit is seeded
    // immediately so this same call serves content plus an edit-grade handle;
    // supported semantic hits above never pay for or observe this fallback.
    const genericFallback = discoverGenericTaskPackFallback(args, locatingQuery, workspace);
    if (genericFallback.paths.length > 0) {
      return buildSeededTaskPack(
        { ...args, paths: genericFallback.paths.map((p) => ({ path: p })) },
        query,
        workspace,
      );
    }

    // No candidate details — return empty pack with redirect. route.action
    // is always locate_missing_surfaces here (no surfaces), so
    // blocking_next_steps is dropped (item 3) in favor of the single `next`
    // hint below (item 4).
    const missingRoles = locateResult.missing ?? args.surfaceRoles ?? ["contract", "api"];
    const noSurfaceMissing = mergeUnique(missingRoles.map(String), requiredRoles);
    const { lines: checkLines, records: checkRecords } = buildCompletionChecks(locatingQuery, [], noSurfaceMissing, workspace);
    // General root model (DESIGN-v0.8 §A5): if the locator determined the
    // query's strong identifiers only matched OUT-OF-ROOT candidates, the
    // caller is almost certainly scoped to the wrong subtree — tell them to
    // re-scope (cwd or paths) at that project root instead of re-running the
    // same pathless locate, which would just return the same junk.
    const fallbackQuery = (genericFallback.probe ?? query).replace(/\"/g, "").slice(0, 80);
    const next = locateResult.rootSuggestion
      ? `re-scope to ${locateResult.rootSuggestion} (cwd or paths=[...]) then read_file mode=task_pack query="${continuationQuery(query)}"`
      : genericFallback.attempted
        ? `search_files action=find query="${fallbackQuery}"`
        : `search_files action=locate query="${continuationQuery(query)}"`;
    const base: TaskPackResult = {
      mode: "task_pack",
      coverage: "partial",
      // DESIGN-v0.8 coverage-honesty: an abstain with no usable candidate
      // details left every required role uncovered — name it "missing-roles".
      coverage_reason: "missing-roles",
      surfaces: [],
      missing: noSurfaceMissing,
      required_surfaces: requiredRoles,
      missing_required_surfaces: requiredRoles,
      route: locateResult.rootSuggestion
        ? { action: "locate_missing_surfaces", reason: `strong query tokens matched only outside the current scope; re-scope to ${locateResult.rootSuggestion}`, max_additional_tl_calls: 1 }
        : buildRoute([], requiredRoles, "partial", noSurfaceMissing, locatingQuery, "missing-roles"),
      checks: checkLines,
      verify: discoverVerificationHints(workspace, locatingQuery, []),
      next,
    };
    return dedupeTrimAndPersist(workspace, base, { query, checkRecords, args });
  }

  // Build surfaces from primary + related candidates using surface grouping.
  const rawCandidates: ImpactCandidate[] = [
    ...locateResult.primary,
    ...locateResult.related,
  ];
  // Efficiency: one per-pack read/parse memo shared by every file-touching
  // step below, so each file is read once and parsed at most once per pack.
  const cache = new FileReadCache();
  // Item 9: for enum/value-addition tasks, boost definition sites and
  // demote pure-derivation sites (e.g. z.enum(Object.values(X))) before
  // role-diversity selection runs, so the exhaustive-initializer
  // definition wins over an auto-deriving consumer that needs no edit.
  const allCandidates = preferDefinitionSites(workspace, locatingQuery, rawCandidates, cache);

  const selectedCandidates = selectClosureCandidates(
    allCandidates,
    locateResult.primary.map((c) => c.path),
  );
  // A4 "stop reporting partial-by-budget": candidates dropped here were
  // FOUND, just trimmed for exceeding MAX_SURFACES_DISTINCT — that is not a
  // missing-required-role situation, so their PATHS go under the
  // non-alarming `trimmed` key, not into `missing` (which previously
  // branded a pack "partial" merely for having more candidates than the
  // surface budget, independent of whether anything required was absent).
  const trimmedPaths = allCandidates
    .map((c) => c.path)
    .filter((_p, idx) => !selectedCandidates.some((sc) => sc.path === allCandidates[idx]?.path));

  // Build surface objects.
  const surfaces: TaskPackSurface[] = [];
  // DESIGN-v0.8 coverage-honesty: scope the ui/style requirement to the
  // located candidates' own dominant root (whole located pool, the strongest
  // root signal we have), not the whole workspace.
  const requiredRoles = requiredSurfacesForTask(
    locatingQuery, workspace, args.surfaceRoles, allCandidates.map((c) => c.path),
  );

  const builtSurfaces = await Promise.all(
    selectedCandidates.map((candidate) => candidateToSurface(candidate, workspace, requiredRoles, locatingQuery, cache)),
  );
  surfaces.push(...builtSurfaces);
  const protectedSurfaces = new Set<TaskPackSurface>();
  await augmentConcernGroupSurfaces(surfaces, locatingQuery, workspace, requiredRoles, cache, protectedSurfaces);
  await augmentNativeSurfaceClosure(surfaces, workspace, locatingQuery, requiredRoles, cache);
  await augmentNativeWiringSurface(surfaces, workspace, cache);
  await augmentNativeIdentifierCooccurrenceSurface(surfaces, workspace, locatingQuery, requiredRoles, cache);
  // Item 1: resolve a REQUIRED "style" role to a NAMED surface (handle + code)
  // instead of leaving it a bare `missing:["style"]` the agent has to guess at.
  // B1a (2026-08-01 retrieval-scope): the role/style search anchors on the
  // LOCATED cluster, not on the running `surfaces` array — otherwise an earlier
  // augmenter's own pick widens the scope for every augmenter after it.
  const anchorPaths = selectedCandidates.map((c) => c.path);
  await augmentStyleSurface(surfaces, locatingQuery, workspace, requiredRoles, cache, anchorPaths);
  // FIX-B2: attempt every OTHER still-missing required role in this SAME
  // call — see the function's own doc comment for the surfaceRoles-retry
  // root cause this closes.
  await augmentMissingRequiredRoleSurfaces(surfaces, locatingQuery, workspace, requiredRoles, cache, undefined, anchorPaths);
  // B1c (2026-08-01 retrieval-scope): a file the QUERY names by name is a
  // stated requirement, not a candidate to be ranked — serve it content-bearing.
  await augmentQueryNamedFileSurfaces(surfaces, locatingQuery, workspace, requiredRoles, cache, protectedSurfaces);
  // DESIGN-v0.8 coverage-honesty (Part 5 B-1): promote a config/data/doc
  // surface whose already-read content already names the enum's new value.
  promoteVariantSurfaces(surfaces, locatingQuery, workspace, cache);
  finalizePrimarySurface(surfaces);

  const missingRequired = requiredRoles.filter((role) => !surfaces.some((s) => s.role === role));
  // `missing` now reports ONLY genuinely-missing required roles — budget
  // trims are reported separately under `trimmed` (see above).
  const allMissing = mergeUnique(missingRequired, []);

  // FIX-A: a broader "known identifier" pool for the concern gate's OR-clause
  // ("matches an identifier that actually exists in the repo index/symbol
  // map") — the pre-trim located candidates (path basenames + symbols), which
  // is strictly BIGGER than the final selected `surfaces` (some candidates
  // were dropped by the surface budget/role-diversity selection, but they
  // were still real files the locator found for this query).
  const knownIdentifiers = new Set<string>(
    allCandidates.flatMap((c) => {
      const base = path.basename(c.path).replace(/\.[^.]+$/, "").toLowerCase();
      return c.symbol ? [base, c.symbol.toLowerCase()] : [base];
    }),
  );

  // DESIGN-v0.8 coverage-honesty: compute the unmatched-concern token list
  // ONCE and reuse it for the coverage label, the route flip, and the
  // per-token blocking_next_steps — so all three agree by construction.
  const unmatchedConcern = unmatchedConcernTokens(locatingQuery, surfaces, knownIdentifiers);

  // DESIGN-v0.8 coverage-honesty: three-way coverage + telemetry reason,
  // replacing the old binary complete|partial mapping.
  // Issue #3 dominance tie-break: computed once, reused below (after `result`
  // exists) to serve the winner's body — see `applyDominancePromotion`.
  const dominantSurfaceIndex = resolveDominantSurfaceIndex(surfaces);
  const { coverage: coverageStr, reason: coverageReason, dominancePromoted } = deriveCoverage(
    {
      completeness: locateResult.completeness,
      confidence: locateResult.confidence,
      primaryCount: locateResult.primary.length,
      ...(locateResult.missingSurfaces ? { missingSurfaces: locateResult.missingSurfaces } : {}),
    },
    missingRequired,
    unmatchedConcern,
    dominantSurfaceIndex,
  );

  const route = buildRoute(surfaces, missingRequired, coverageStr, allMissing, locatingQuery, coverageReason);

  // DESIGN-v0.8 coverage-honesty (Part 3): the single best `next` for this
  // coverage state (batch read of code-less handles, direct edit, targeted
  // re-locate, or a scoped find) — no longer always a bare single slice.
  const nextHint = nextHintForCoverage(
    surfaces, coverageStr, coverageReason, locatingQuery, missingRequired, unmatchedConcern,
  );

  // Only populate blocking_next_steps when there is actual per-token content
  // — the route can also flip to locate_missing_surfaces via missingRequired
  // alone (unmatchedConcernTokens then empty), which must not serialize a
  // vacuous empty array.
  const blockingNextSteps = route.action === "locate_missing_surfaces"
    ? buildBlockingNextSteps(locatingQuery, surfaces)
    : [];

  const { lines: checkLines, records: checkRecords } = buildCompletionChecks(locatingQuery, surfaces, allMissing, workspace, unmatchedConcern);
  const result: TaskPackResult = {
    mode: "task_pack",
    coverage: coverageStr,
    ...(coverageReason ? { coverage_reason: coverageReason } : {}),
    surfaces,
    missing: allMissing,
    ...(trimmedPaths.length > 0 ? { trimmed: trimmedPaths } : {}),
    required_surfaces: requiredRoles,
    ...(missingRequired.length > 0 ? { missing_required_surfaces: missingRequired } : {}),
    route,
    ...(blockingNextSteps.length > 0 ? { blocking_next_steps: blockingNextSteps } : {}),
    checks: checkLines,
    verify: discoverVerificationHints(workspace, locatingQuery, surfaces),
    ...(nextHint ? { next: nextHint } : {}),
  };
  if (dominancePromoted) applyDominancePromotion(result, surfaces, dominantSurfaceIndex, workspace);

  attachPartialTree(result, workspace);
  const evidenceExpansion = await importEvidenceExpansion(workspace, locateResult.primary, locatingQuery, surfaces);
  return dedupeTrimAndPersist(workspace, result, {
    ...(protectedSurfaces.size > 0 ? { protectedSurfaces } : {}),
    query,
    checkRecords,
    args,
    // DESIGN-v0.9 §4.3: recompute next/route if trimToCap flips coverage.
    recompute: { query, missingRequired, unmatchedConcern },
    // DESIGN-v0.9 §4.6b: share the pack's read cache for codeless-surface inlining.
    cache,
    evidenceExpansion,
  });
}

/**
 * WS3 answer profile: locate once, then materialize only the locator's primary
 * candidates and related symbols. Generic concern/role/style/wiring
 * augmentation is intentionally absent: an answer pack has no edit closure to
 * assemble, and every returned surface must already carry its body.
 *
 * Undefined is reserved for a truly empty locator result. An ambiguous answer
 * stays inside this profile as a bounded candidate-confirmation pack; falling
 * through to the generic flow would add edit-role/artifact augmentation to a
 * read-only investigation and reorder the locator's best candidate.
 */
async function buildAnswerTaskPack(
  args: TaskPackArgs,
  query: string,
  workspace: string,
): Promise<TaskPackResult | undefined> {
  let locateResult = await locateTaskContext(workspace, {
    action: "locate",
    query,
    ...(args.path ? { path: args.path } : {}),
    ...(args.symbol ? { symbol: args.symbol } : {}),
    ...(args.lang ? { lang: args.lang } : {}),
    limit: Math.min(args.limit ?? MAX_SURFACES_DISTINCT, MAX_SURFACES_DISTINCT),
  });
  let rankingQuery = query;
  if (!locateResult.hit) {
    const recoveryQuery = answerRecoveryQuery(query);
    if (recoveryQuery !== undefined && recoveryQuery.toLowerCase() !== query.trim().toLowerCase()) {
      const recovered = await locateTaskContext(workspace, {
        action: "locate",
        query: recoveryQuery,
        ...(args.path ? { path: args.path } : {}),
        ...(args.symbol ? { symbol: args.symbol } : {}),
        ...(args.lang ? { lang: args.lang } : {}),
        limit: Math.min(args.limit ?? MAX_SURFACES_DISTINCT, MAX_SURFACES_DISTINCT),
      });
      const currentCandidates: ImpactCandidate[] = [...(locateResult.candidateDetails ?? [])];
      const recoveredCandidates: ImpactCandidate[] = recovered.hit
        ? [...recovered.primary, ...recovered.related]
        : [...(recovered.candidateDetails ?? [])];
      const currentHasImplementation = currentCandidates.some((candidate) => !isNonImplementationAnswerCandidate(candidate));
      const recoveredHasImplementation = recoveredCandidates.some((candidate) => !isNonImplementationAnswerCandidate(candidate));
      if (recoveredHasImplementation && (!currentHasImplementation || recoveryQuery.length < query.trim().length)) {
        locateResult = recovered;
        rankingQuery = recoveryQuery;
      }
    }
  }
  // 2026-08-21 answer-path-inherits-locator fix (W4-B, defect 1): the text
  // recovery above can still leave an "ambiguous" abstain when the
  // candidate pool is crowded by same- or higher-confidence noise a plain-
  // word query cannot out-rank (a bench/fixture's loose prose match, or a
  // spec file that quotes the query text verbatim, both out-score a real
  // "query-symbol" hit on raw locator confidence). When the query
  // explicitly NAMES one candidate's own file — its basename appears in the
  // query as a whole word — and that candidate already carries a resolved
  // `.symbol` from a real structural locator match (not a symbol-less
  // prose/variant-text hit), retry the locator anchored on that exact file
  // + symbol: `symbol` makes Layer 1 resolve it directly (bypassing the
  // confidence race entirely), and scoping `path` to the file's DIRECTORY
  // (not the bare file) keeps sibling files in the walk so import-edge
  // related surfaces can still resolve. A successful, matching hit REPLACES
  // `locateResult` so the primary + required-related handling below runs
  // exactly as it would for a query the locator resolved cleanly on the
  // first try.
  if (!locateResult.hit) {
    const anchor = pickNamedFileAbstainAnchor(locateResult.candidateDetails ?? [], query);
    if (anchor !== undefined && anchor.symbol !== undefined) {
      const anchorSymbol = anchor.symbol;
      const anchored = await locateTaskContext(workspace, {
        action: "locate",
        query,
        path: path.dirname(anchor.path),
        symbol: anchorSymbol,
        ...(args.lang ? { lang: args.lang } : {}),
        limit: Math.min(args.limit ?? MAX_SURFACES_DISTINCT, MAX_SURFACES_DISTINCT),
      });
      if (
        anchored.hit
        && anchored.primary.some((p) => p.path === anchor.path && p.symbol === anchorSymbol)
      ) {
        locateResult = anchored;
      }
    }
  }
  const cache = new FileReadCache();
  const selected: ImpactCandidate[] = [];
  const seen = new Set<string>();
  const primaryCount = locateResult.hit ? locateResult.primary.length : 0;
  const locatorCandidates: ImpactCandidate[] = locateResult.hit
    ? [...locateResult.primary, ...locateResult.related]
    : rankAbstainCandidates(locateResult.candidateDetails ?? []);
  // 2026-08-21 answer-path-inherits-locator fix (W4-B, defect 1, rule b/c):
  // once the locator resolves a primary, its own verdict — the primary
  // itself plus any related surface it flagged `required` (an import-edge
  // callee or a query-symbol neighbour) — must win a guaranteed front seat
  // in the served evidence. Recorded here, before rankAnswerEvidenceCandidates
  // re-scores everything from its own independent content/focus signal, so
  // a textually louder but structurally uninvolved extra (a facet/
  // responsibility hit) can never crowd a required surface out of a capped
  // selection — see orderByLocatorVerdict below.
  const requiredLocatorPaths = new Set<string>(
    locateResult.hit
      ? [
          ...locateResult.primary.map((c) => c.path),
          ...locateResult.related.filter((c) => c.required === true).map((c) => c.path),
        ]
      : [],
  );
  const primaryLocatorPath = locateResult.hit ? locateResult.primary[0]?.path : undefined;
  const responsibilityCandidates = await discoverAnswerResponsibilityCandidates(
    args,
    query,
    workspace,
    cache,
  );
  const locatedCandidates = [...locatorCandidates, ...responsibilityCandidates]
    .filter((candidate, index, all) => all.findIndex((other) =>
      other.path === candidate.path
      && other.symbol === candidate.symbol
      && other.range === candidate.range
    ) === index);
  // A successful locator hit can still be only a keyword-sized range on the
  // exact class/module named by the caller. Rank and refocus both hit and
  // abstain candidates so explicit identifiers expand to their implementation
  // symbol before the answer contract is certified.
  const rankingEvidenceQuery = rankingQuery === query
    ? query
    : `${query}\n${rankingQuery}`;
  const rankedAnswerCandidates = await rankAnswerEvidenceCandidates(
    locatedCandidates,
    rankingEvidenceQuery,
    workspace,
    cache,
  );
  const explicitAnswerNames = new Set(explicitCodeIdentifiers(query).map(normIdent));
  rankedAnswerCandidates.sort((a, b) => {
    const namedPath = (entry: RankedAnswerEvidenceCandidate): number => Number(
      explicitAnswerNames.has(normIdent(path.basename(
        entry.candidate.path,
        path.extname(entry.candidate.path),
      ))),
    );
    return namedPath(b) - namedPath(a);
  });
  if (
    rankedAnswerCandidates[0] !== undefined
    && !isNonImplementationAnswerCandidate(rankedAnswerCandidates[0].candidate)
    && !/\b(?:test|tests|spec|fixture|bench|benchmark)\b/i.test(query)
  ) {
    const implementationOwned = rankedAnswerCandidates.filter((entry) =>
      !isNonImplementationAnswerCandidate(entry.candidate)
      || (
        entry.candidate.symbol !== undefined
        && explicitAnswerNames.has(normIdent(entry.candidate.symbol))
      )
    );
    rankedAnswerCandidates.splice(0, rankedAnswerCandidates.length, ...implementationOwned);
  }
  // 2026-08-21 answer-focus precision fix, item (b): see
  // dropWeakCrossRootAndNonSourceAnswerCandidates — must run AFTER the
  // implementation-ownership filter above (which already needs real
  // candidates to compare) and BEFORE `candidates`/`selected`/`surfaces` are
  // derived from rankedAnswerCandidates below, so a weak/off-root/non-source
  // entry never reaches the served pack. `weakEvidenceDropped` feeds the
  // coverage-honesty override further down.
  const weakEvidenceDropped = dropWeakCrossRootAndNonSourceAnswerCandidates(rankedAnswerCandidates);
  // C-1 (wave 4): when the caller literally named an identifier and no ranked
  // candidate carries it, run the literal scan the model would otherwise be
  // routed into (confirm_candidates -> search_files find) and serve the
  // definition directly. Absent identifiers are remembered for the readiness
  // obligation's decision-grade absence wording.
  const verifiedAbsentIdentifiers = await recoverExplicitIdentifierAnswerCandidates(
    args,
    query,
    workspace,
    rankedAnswerCandidates,
    cache,
  );
  const recoveredExactAnswer =
    rankedAnswerCandidates[0]?.candidate.why === "answer-explicit-identifier-recovery";
  const strongEvidenceAnswer = rankingQuery !== query
    && !locateResult.hit
    && rankedAnswerCandidates[0]?.strongEvidence === true
    && !isNonImplementationAnswerCandidate(rankedAnswerCandidates[0].candidate);
  const topResponsibility = rankedAnswerCandidates[0];
  const responsibilityRunnerUp = rankedAnswerCandidates[1];
  // 2026-08-21 answer-path-inherits-locator fix (W4-B, defect 1, rule c):
  // "semantic-responsibility" resolution corrects the locator's SYMBOL-level
  // focus within a file it already got right just as often as it resolves a
  // colloquial identifier the codebase never literally used (T03's fixture:
  // "OrderOrchestrator" -> OrderService.java) — semanticSignalRegression's
  // T03 pin is exactly the former: the locator DOES resolve OrderService.java
  // as primary, just anchored on the wrong member (`cancel`, a strong lexical
  // match but not what the query asks about); responsibility-discovery names
  // the SAME FILE and must still be allowed to trigger the multi-facet
  // member-expansion below (materializeNamedAnswer) that widens a narrow,
  // wrongly-focused single-method hit into the real class-level answer. What
  // rule (c) actually forbids is a DIFFERENT file — one with no structural
  // relation to the locator's primary — winning the sole-answer slot on raw
  // textual density alone. Gate on that (path identity), not on
  // `locateResult.hit`/content-strength, which cannot tell the two apart:
  // both look like "a strong-evidence responsibility hit at rankedAnswerCandidates[0]".
  const responsibilityDisplacesLocatorPrimary = locateResult.hit
    && primaryLocatorPath !== undefined
    && topResponsibility !== undefined
    && topResponsibility.candidate.path !== primaryLocatorPath;
  const responsibilityResolved = !responsibilityDisplacesLocatorPrimary
    && topResponsibility !== undefined
    && responsibilityCandidates.some((candidate) => candidate.path === topResponsibility.candidate.path)
    && topResponsibility.strongEvidence
    && (
      responsibilityRunnerUp === undefined
      || !responsibilityRunnerUp.strongEvidence
      || topResponsibility.score - responsibilityRunnerUp.score >= 1
    );
  const strongRecoveredAnswer = responsibilityResolved || strongEvidenceAnswer
    || (!locateResult.hit && isStrongRecoveredAnswerCandidate(rankedAnswerCandidates, rankingEvidenceQuery));
  const exactLocatedPaths = new Set(
    rankedAnswerCandidates
      .filter((entry) => entry.exactIdentifier)
      .map((entry) => entry.candidate.path),
  );
  const topAnswerPathStem = rankedAnswerCandidates[0]
    ? normIdent(path.basename(
        rankedAnswerCandidates[0].candidate.path,
        path.extname(rankedAnswerCandidates[0].candidate.path),
      ))
    : "";
  const exactIdentifierNames = new Set(explicitCodeIdentifiers(query).map(normIdent));
  const exactNamedFileAnswer = exactIdentifierNames.has(topAnswerPathStem)
    && rankedAnswerCandidates[0] !== undefined
    && !isNonImplementationAnswerCandidate(rankedAnswerCandidates[0].candidate);
  // An exact identifier locates the start of an exhaustive scan; it cannot
  // collapse the open universe to one answer surface.
  const exactLocatedAnswer = !hasOpenUniverseIntent(query) && (exactNamedFileAnswer
    || (recoveredExactAnswer && exactLocatedPaths.size === 1)
    || (locateResult.hit
      && rankedAnswerCandidates[0]?.exactIdentifier === true
      && exactLocatedPaths.size === 1));
  const candidates = exactLocatedAnswer
    ? [rankedAnswerCandidates[0]!.candidate]
    : strongEvidenceAnswer
    ? rankedAnswerCandidates
        .filter((entry) => entry.strongEvidence && !isNonImplementationAnswerCandidate(entry.candidate))
        .slice(0, 3)
        .map((entry) => entry.candidate)
    : strongRecoveredAnswer
    ? [rankedAnswerCandidates[0]!.candidate]
    : orderByLocatorVerdict(rankedAnswerCandidates, requiredLocatorPaths, primaryLocatorPath);
  const selectionLimit = locateResult.hit
    ? exactLocatedAnswer ? 1 : MAX_SURFACES_DISTINCT
    : strongEvidenceAnswer
    ? 3
    : strongRecoveredAnswer
    ? 1
    : 3;
  if (candidates.length === 0) return undefined;
  // 2026-08-21 directory-scoped-answer fix (Issue #4 follow-up): a candidate
  // past the locator's own primary/required-related front (index >=
  // primaryCount) with no resolved `.symbol` is USUALLY locator context (an
  // import-edge neighbour or sibling-file range carried along for
  // background, not a real answer) — the loop below still drops those. But
  // rankAnswerEvidenceCandidates already distinguishes that weak case from a
  // genuinely strong, symbol-less hit: `strongEvidence: true` means its own
  // evidence scan found a concentrated, literal match for the query's own
  // terms (selectQueryEvidence), just inside a block with no enclosing
  // function/method/class to name as `.symbol` — a top-level test body
  // (`describe(...) { it(...) { ... } }`) being the common case. Treating
  // that the same as ordinary locator noise silently dropped the exact
  // regression test a directory-scoped answer query asked for once
  // orderByLocatorVerdict (W4-B) front-loads the locator's primary and pushes
  // every other candidate — regardless of strength — past primaryCount.
  // Recorded by path (rankAnswerEvidenceCandidates already dedupes to one
  // entry per path via its own `focusedPaths` guard) so the exemption
  // survives whichever branch above produced `candidates` (all of them
  // trace back to rankedAnswerCandidates).
  const strongEvidencePaths = new Set(
    rankedAnswerCandidates.filter((entry) => entry.strongEvidence).map((entry) => entry.candidate.path),
  );
  for (const [index, candidate] of candidates.entries()) {
    // Related range-only hits are locator context, not a related symbol
    // body — unless already certified strong evidence (see above).
    if (
      locateResult.hit
      && index >= primaryCount
      && candidate.symbol === undefined
      && !responsibilityCandidates.some((responsibility) => responsibility.path === candidate.path)
      && !strongEvidencePaths.has(candidate.path)
    ) continue;
    const range = candidate.range ?? `${candidate.line}-${candidate.line}`;
    const key = `${candidate.path}\0${candidate.symbol ?? ""}\0${range}`;
    if (seen.has(key)) continue;
    seen.add(key);
    selected.push(candidate);
    if (selected.length >= selectionLimit) break;
  }
  if (selected.length === 0) return undefined;

  const built = await Promise.all(
    selected.map((candidate) =>
      candidateToSurface(candidate, workspace, [], query, cache, { answerProfile: true })
    ),
  );
  const surfaces = built.filter(
    (surface): surface is TaskPackSurface =>
      typeof surface.code === "string" && surface.code.length > 0,
  );
  if (surfaces.length === 0) return undefined;

  const materializeNamedAnswer = responsibilityResolved || exactLocatedAnswer || explicitAnswerNames.has(normIdent(path.basename(
    surfaces[0]!.path,
    path.extname(surfaces[0]!.path),
  )));
  if (
    materializeNamedAnswer
    && (
      surfaces[0]?.content_completeness === "partial"
      || requiredQueryFacets(query).some((facet) =>
        !(surfaces[0]?.code ?? "").toLowerCase().includes(facet.toLowerCase())
      )
    )
  ) {
    const primary = rankedAnswerCandidates[0]!.candidate;
    const content = readCached(workspace, primary.path, cache);
    const focus = content === undefined
      ? undefined
      : await selectAnchorFocus(content, primary.path, query, cache);
    const lang = languageForPath(primary.path);
    const parsed = content !== undefined && lang
      ? await cache.parsedSymbols(content, lang, primary.path)
      : null;
    const queryFacets = requiredQueryFacets(query);
    const lines = content?.split(/\r?\n/) ?? [];
    const primaryBounds = primary.range?.match(/^(\d+)-(\d+)$/);
    const primaryStart = Number(primaryBounds?.[1] ?? 1);
    const primaryEnd = Number(primaryBounds?.[2] ?? lines.length);
    const rankedMembers = (parsed ?? [])
      .filter((symbol) =>
        (symbol.kind === "method" || symbol.kind === "function")
        && (
          materializeNamedAnswer
          // A semantic-responsibility match proves the enclosing file/class,
          // even when the locator's initial keyword window happened to name a
          // sibling method such as cancel(). Rank all callable members in
          // that proved owner so requested facets can come from place().
          ||
          !primary.symbol
          || symbol.enclosingSymbol?.name === primary.symbol
          // Some language backends return flat method symbols. An exact-file
          // class range still gives us a safe structural enclosure.
          || (symbol.startLine >= primaryStart && symbol.endLine <= primaryEnd)
        )
      )
      .map((symbol) => {
        const body = lines.slice(symbol.startLine - 1, symbol.endLine).join("\n").toLowerCase();
        const facetHits = queryFacets.filter((facet) => body.includes(facet.toLowerCase())).length;
        const executableFacetLines = queryFacets.flatMap((facet) => {
          const matches: number[] = [];
          for (let line = symbol.signatureStartLine ?? symbol.startLine; line <= symbol.endLine; line++) {
            if ((lines[line - 1] ?? "").toLowerCase().includes(facet.toLowerCase())) matches.push(line);
          }
          return matches;
        });
        const facetWindows = queryFacets.flatMap((facet) => {
          // B1d (2026-08-01 retrieval-scope): a facet's real behavior lives at
          // its COLLABORATOR CALL SITE (`shippingService.requestLabel(...)`),
          // not wherever the word happens to appear. The first-match scan below
          // used to stop at any lexical hit, so a `shipping` COST field in the
          // class head won the window and the actual shipping call in the tail
          // was never served — while hasExactNamedAnswerFacetEvidence then read
          // that head match as "the facet is bundled" (T03). Scan for a call
          // site FIRST; fall back to the plain lexical match only when the
          // symbol has none.
          const scan = (
            accept: (source: string) => boolean,
          ): Array<{ facet: string; line: number; range: string }> => {
            for (let line = symbol.signatureStartLine ?? symbol.startLine; line <= symbol.endLine; line++) {
              const source = lines[line - 1] ?? "";
              if (/^\s*(?:\/\/|\/\*|\*)/.test(source) || !hasIdentifierSegment(source, facet)) continue;
              if (!accept(source)) continue;
              return [{ facet, line, range: `${Math.max(symbol.startLine, line - 2)}-${Math.min(symbol.endLine, line + 3)}` }];
            }
            return [];
          };
          const callSite = scan((source) => isFacetCallSiteLine(source, facet));
          return callSite.length > 0 ? callSite : scan(() => true);
        });
        const centerLine = executableFacetLines.length > 0
          ? Math.round((Math.min(...executableFacetLines) + Math.max(...executableFacetLines)) / 2)
          : symbol.signatureStartLine ?? symbol.startLine;
        return {
          name: symbol.name,
          range: `${symbol.startLine}-${symbol.endLine}`,
          signatureRange: `${symbol.signatureStartLine ?? symbol.startLine}-${Math.min(symbol.endLine, (symbol.signatureEndLine ?? symbol.startLine) + 1)}`,
          centerLine,
          facetHits,
          facetWindows,
          score: lexicalOverlapFraction(tokenizeForEpoch(query), tokenizeForEpoch(symbol.name)),
        };
      })
      .filter((item) => item.facetHits > 0 || item.score > 0)
      .sort((a, b) => {
        const facetDelta = b.facetHits - a.facetHits;
        if (facetDelta !== 0) return facetDelta;
        if (a.facetHits > 0) {
          return Number.parseInt(a.range, 10) - Number.parseInt(b.range, 10);
        }
        return b.score - a.score;
      });
    const focusedMembers = (focus ? [focus.best, ...focus.runnerUps] : []).map((item) => ({
      ...item,
      centerLine: Number.parseInt(item.range.split("-", 1)[0] ?? "1", 10),
      facetWindows: [] as Array<{ facet: string; line: number; range: string }>,
      signatureRange: item.range,
    }));
    const members = [...rankedMembers, ...focusedMembers]
      .filter((item, index, all) =>
        item.name.toLowerCase() !== surfaces[0]!.symbol?.toLowerCase()
        && all.findIndex((candidate) => candidate.name === item.name && candidate.range === item.range) === index
      )
      .slice(0, 3);
    // Keep an exact large-class answer as one coherent surface: append bounded,
    // query-ranked member bodies to the class header/table slice. Separate
    // same-file surfaces are vulnerable to pack trimming and make the client
    // reconstruct a structural answer from several handles.
    const primarySurface = surfaces[0]!;
    for (const member of members) {
      const distinctFacetWindows = member.facetWindows.filter((window, index, all) =>
        all.findIndex((candidate) => candidate.range === window.range) === index
      );
      if (
        queryFacets.length > 1
        && new Set(member.facetWindows.map((window) => window.facet)).size === queryFacets.length
      ) {
        let added = 0;
        const signatureStart = Number.parseInt(member.signatureRange, 10);
        const signatureEnd = Number.parseInt(member.signatureRange.split("-")[1] ?? member.signatureRange, 10);
        const signatureMarker = `\n\n/* query-focused member ${member.name} (${member.signatureRange}) */\n`;
        const signatureSnippet = lines.slice(signatureStart - 1, signatureEnd).join("\n");
        if (
          Buffer.byteLength((primarySurface.code ?? "") + signatureMarker + signatureSnippet, "utf8")
          <= MAX_SURFACE_CODE_BYTES
        ) {
          primarySurface.code = (primarySurface.code ?? "") + signatureMarker + signatureSnippet;
        }
        for (const window of distinctFacetWindows) {
          const current = primarySurface.code ?? "";
          const marker = `\n\n/* query facet ${window.facet} (${window.range}) */\n`;
          const snippet = lines.slice(
            Number.parseInt(window.range, 10) - 1,
            Number.parseInt(window.range.split("-")[1] ?? window.range, 10),
          ).join("\n");
          if (
            Buffer.byteLength(current + marker + snippet, "utf8") > MAX_SURFACE_CODE_BYTES
          ) break;
          primarySurface.code = current + marker + snippet;
          added++;
        }
        if (added === distinctFacetWindows.length) {
          primarySurface.required = false;
          primarySurface.content_completeness = "partial";
          delete primarySurface.outline;
          break;
        }
      }
      const current = primarySurface.code ?? "";
      const marker = `\n\n/* query-focused member ${member.name} (${member.range}) */\n`;
      const remainingBudget = MAX_SURFACE_CODE_BYTES
        - Buffer.byteLength(current, "utf8")
        - Buffer.byteLength(marker, "utf8");
      if (remainingBudget < 512) break;
      const focused = centeredSliceForCap(
        workspace,
        primary.path,
        member.range,
        cache,
        member.centerLine,
        remainingBudget,
      );
      if (!focused) continue;
      primarySurface.code = current + marker + focused.code;
      primarySurface.required = true;
      primarySurface.remaining_ranges = [...new Set([
        ...(primarySurface.remaining_ranges ?? []),
        ...focused.remaining_ranges,
      ])];
      primarySurface.content_completeness = "partial";
    }
  }

  // B1c (2026-08-01 retrieval-scope): an ANSWER pack that could not resolve one
  // target ships a candidate list — and T05c's live candidate lists contained
  // none of the three files the query named. Serve those first, content-bearing.
  await augmentQueryNamedFileSurfaces(surfaces, query, workspace, [], cache);

  // Issue #3 dominance tie-break: computed once, reused below (after `result`
  // exists) to serve the winner's body — see `applyDominancePromotion`.
  const dominantSurfaceIndex = resolveDominantSurfaceIndex(surfaces);
  const { coverage, reason, dominancePromoted } = exactLocatedAnswer
    ? { coverage: "focused" as const, reason: undefined, dominancePromoted: undefined }
    : locateResult.hit
    ? deriveCoverage(
        {
          // 2026-08-21 answer-focus precision fix (coverage honesty): the
          // pack's own admission filter had to drop weak-token,
          // non-dominant-root, or non-source-role evidence to reach this
          // surface set — a pack built partly FROM discarded evidence must
          // not report "complete" even when the locator itself said so.
          completeness: weakEvidenceDropped ? "partial" : locateResult.completeness,
          confidence: locateResult.confidence,
          primaryCount: locateResult.primary.length,
          ...(locateResult.missingSurfaces ? { missingSurfaces: locateResult.missingSurfaces } : {}),
        },
        [],
        [],
        dominantSurfaceIndex,
      )
    : strongRecoveredAnswer
      ? { coverage: "focused" as const, reason: undefined, dominancePromoted: undefined }
      : { coverage: "partial" as const, reason: "candidate-list" as const, dominancePromoted: undefined };
  const needsConfirmation = !locateResult.hit && !strongRecoveredAnswer;
  const topRange = surfaces[0]!.range.match(/^(\d+)-(\d+)$/);
  const confirmationNext = topRange
    ? `read_file mode=slice handle=${surfaces[0]!.handle} range=${Math.max(1, Number(topRange[1]) - 40)}-${Number(topRange[2]) + 40}`
    : `read_file mode=slice handle=${surfaces[0]!.handle}`;
  if (responsibilityResolved && surfaces[0]) {
    surfaces[0].symbol = path.basename(
      surfaces[0].path,
      path.extname(surfaces[0].path),
    );
  }
  const result: TaskPackResult = {
    mode: "task_pack",
    coverage,
    ...(reason ? { coverage_reason: reason } : {}),
    surfaces,
    missing: [],
    route: {
      action: needsConfirmation ? "confirm_candidates" : "answer_from_handles",
      reason: needsConfirmation
        ? "answer candidates are ambiguous; inspect the top handle before answering"
        : "query reads as explain/answer — no edit expected",
      max_additional_tl_calls: needsConfirmation ? 1 : 0,
    },
    ...(responsibilityResolved && explicitCodeIdentifiers(query)[0] && surfaces[0]
      ? {
          answer_resolution: {
            strategy: "semantic-responsibility" as const,
            requested_identifier: explicitCodeIdentifiers(query)[0]!,
            resolved_handle: surfaces[0].handle,
            // B1d (2026-08-01 retrieval-scope): `matched_facets` used to echo
            // the QUERY's facet list verbatim — it asserted a match nothing had
            // checked, and downstream (dedupeTrimAndPersist's answer stop) that
            // unverified assertion short-circuited the real facet check. T03
            // shipped `matched_facets:["payment","inventory","shipping"]` while
            // every shippingService.* call site sat in remaining_ranges. Report
            // only the facets this surface's SERVED text actually evidences.
            matched_facets: requiredQueryFacets(query).filter((facet) =>
              surfaceContainsToken(surfaces[0]!, facet)
              && !facetEvidenceStillUnserved(surfaces[0]!, facet, workspace)
            ),
          },
        }
      : {}),
    ...(needsConfirmation ? { next: confirmationNext } : {}),
  };
  if (dominancePromoted) applyDominancePromotion(result, surfaces, dominantSurfaceIndex, workspace);

  const evidenceExpansion = await importEvidenceExpansion(workspace, locateResult.hit ? locateResult.primary : [], query, surfaces);
  const finalResult = dedupeTrimAndPersist(workspace, result, { args, evidenceExpansion });
  if (verifiedAbsentIdentifiers.length > 0) {
    verifiedAbsentIdentifiersByResult.set(finalResult, verifiedAbsentIdentifiers);
    if (result !== finalResult) verifiedAbsentIdentifiersByResult.set(result, verifiedAbsentIdentifiers);
  }
  return finalResult;
}

function isTestConcernPath(relPath: string): boolean {
  return /(?:^|\/)(?:__tests__|tests?|specs?)(?:\/|$)/i.test(relPath)
    || /\.(?:spec|test)\.[^.]+$/i.test(relPath);
}

function hasIdentifierSegment(text: string, token: string): boolean {
  const lowerText = text.toLowerCase();
  const lowerToken = token.toLowerCase();
  let from = 0;
  while (from < lowerText.length) {
    const index = lowerText.indexOf(lowerToken, from);
    if (index < 0) return false;
    const before = text[index - 1];
    const first = text[index];
    const after = text[index + token.length];
    const beforeBoundary = before === undefined
      || !/[A-Za-z0-9]/.test(before)
      || (/[a-z0-9]/.test(before) && first !== undefined && /[A-Z]/.test(first));
    const afterBoundary = after === undefined
      || !/[A-Za-z0-9]/.test(after)
      || (/[a-z0-9]/.test(text[index + token.length - 1] ?? "") && /[A-Z]/.test(after));
    if (beforeBoundary && afterBoundary) return true;
    from = index + 1;
  }
  return false;
}

const CONCERN_DEFECT_EVIDENCE = [
  /\bdata race\b/i,
  /\brace condition\b/i,
  /\bwithout (?:holding|taking|acquiring|locking)\b/i,
  /\bsilently lost\b/i,
  /\bpartially[- ]written\b/i,
  /\bstale (?:pointer|state|value)\b/i,
  /\b(?:incorrect|wrong|broken|bug|fixme|todo)\b/i,
] as const;

function concernDefectEvidence(
  content: string,
  hitLines: ReadonlySet<number>,
): { score: number; lines: number[] } {
  const lines = content.split(/\r?\n/);
  let score = 0;
  const evidenceLines = new Set<number>();
  const scoredMarkers = new Set<string>();
  for (const hitLine of hitLines) {
    const start = Math.max(0, hitLine - 81);
    const end = Math.min(lines.length, hitLine + 80);
    for (let index = start; index < end; index++) {
      for (const marker of CONCERN_DEFECT_EVIDENCE) {
        if (marker.test(lines[index] ?? "")) {
          evidenceLines.add(index + 1);
          const markerKey = `${index}:${marker.source}`;
          if (!scoredMarkers.has(markerKey)) {
            scoredMarkers.add(markerKey);
            score += marker.source.includes("race") || marker.source.includes("without") ? 4 : 2;
          }
        }
      }
    }
  }
  return { score: Math.min(12, score), lines: [...evidenceLines].sort((a, b) => a - b) };
}

function concernLayerPathAdjustment(query: string, relPath: string): number {
  const lowerQuery = query.toLowerCase();
  const lowerPath = relPath.toLowerCase();
  const wantsBackend = /\b(?:backend|server-side)\b/.test(lowerQuery) || query.includes("バックエンド");
  const wantsFrontend = /\b(?:frontend|client-side)\b/.test(lowerQuery) || query.includes("フロントエンド");
  const wantsShared = /\bshared\b/.test(lowerQuery) || query.includes("共有");
  const wantsValidation =
    /\b(?:validation|validate|validator|schema|parse)\b/.test(lowerQuery)
    || /バリデーション|検証/.test(query);
  if (wantsValidation) {
    if (/(?:^|\/)(?:routes?|controllers?|schemas?|validation|middleware)(?:\/|$)/.test(lowerPath)) {
      return 22;
    }
    if (/(?:^|\/)repositories?(?:\/|$)/.test(lowerPath)) return -4;
  }
  if (wantsBackend) {
    if (/(?:^|\/)(?:backend|server|api)(?:\/|$)/.test(lowerPath)) return 12;
    if (/(?:^|\/)(?:frontend|web|ui)(?:\/|$)/.test(lowerPath)) return -8;
  }
  if (wantsFrontend) {
    if (/(?:^|\/)(?:frontend|web|ui)(?:\/|$)/.test(lowerPath)) return 12;
    if (/(?:^|\/)(?:backend|server|api)(?:\/|$)/.test(lowerPath)) return -8;
  }
  if (wantsShared && /(?:^|\/)(?:shared|common)(?:\/|$)/.test(lowerPath)) return 10;
  // (2026-08-02 anti-overfit, DESIGN-v0.10 §5 / spec-multiconcern-vocab §4.2)
  // TWO path-ranking nudges used to sit here and are both gone:
  //
  //   `wantsControl`  — /\b(?:control|controller|steering)\b/ OR
  //                     /制御|ステアリング/, ±24/−10 toward control/ over
  //                     math|util|support (§1a row 1).
  //   `wantsModeTransition` — see below.
  //
  // Both were EN/JA word pairs reverse-engineered from one fixture's symptoms
  // (139dbe74) and both were RANKING bonuses, never gates. "Fixture vocabulary
  // in server code is an automatic reject" outranks the fact that a nudge is
  // low-stakes: a nudge tuned to one benchmark is still tuning to one
  // benchmark, and it silently mis-ranked every report phrased any other way.
  // Neither is replaced — concernAnchorTokens' literal-identifier extraction
  // and literalConcernCandidates' co-occurrence score already rank these
  // candidates from the caller's own words plus repository evidence.
  //
  // A `wantsModeTransition` nudge used to sit here, gated by
  // /\bmode\b…\btransition\b/ OR /モード…遷移/ — an EN/JA word pair
  // reverse-engineered from one fixture's third symptom (139dbe74). It was a
  // ±24/±6 RANKING bonus, never a gate, and concernAnchorTokens' literal
  // identifier extraction plus literalConcernCandidates' co-occurrence score
  // already carry that weight structurally. Deleted rather than translated:
  // the fix for task-specific vocabulary is removal, not more languages
  // (editIntentForRole precedent).
  return 0;
}

function concernFacetEvidenceScore(query: string, content: string, relPath: string): number {
  const haystack = `${relPath}\n${content}`;
  let score = 0;
  const aggregationShaped = /\b(?:stats?|statistics|aggregate|aggregation|counts?|summary)\b/i.test(query)
    || /統計|集計|件数/.test(query);
  const validationShaped = /\b(?:validation|validate|validator|schema)\b/i.test(query)
    || /バリデーション|検証/.test(query);
  if (
    aggregationShaped
    && /\b(?:stats?|statistics|aggregate|aggregation|counts?|summary|by[A-Z]\w*|per[A-Z]\w*)\b/i.test(haystack)
  ) {
    score += 10;
  }
  if (
    aggregationShaped
    && /(?:^|\/)(?:admin|analytics|metrics?|reports?)(?:\/|[A-Z_.-])/i.test(relPath)
  ) {
    score += 12;
  }
  if (
    validationShaped
    && /\b(?:coerce|normalize|normalise|allowed|isValid|validate|validator|parse|safeParse|nativeEnum|switch|case)\b/i.test(haystack)
  ) {
    score += 12;
  } else if (
    validationShaped
    && /\b(?:validation|schema|parse|safeParse)\b/i.test(haystack)
  ) {
    score += 4;
  }
  return score;
}

async function literalConcernCandidates(
  workspace: string,
  group: ConcernGroup,
  onlyFiles: ReadonlySet<string>,
): Promise<ImpactCandidate[]> {
  interface HitGroup {
    path: string;
    lines: Set<number>;
    tokens: Set<string>;
    linesByToken: Map<string, Set<number>>;
    defectEvidence: number;
    defectLines: number[];
    facetEvidence: number;
  }
  const byPath = new Map<string, HitGroup>();
  for (const token of group.tokens.filter((value) => value.length >= 3).slice(0, 6)) {
    const matches = scanLiteral(token, workspace, {
      caseInsensitive: true,
      onlyFiles: new Set(onlyFiles),
    });
    for (const match of matches) {
      if (/^[a-z0-9_]+$/i.test(token) && !hasIdentifierSegment(match.text ?? "", token)) continue;
      const entry = byPath.get(match.path) ?? {
        path: match.path,
        lines: new Set<number>(),
        tokens: new Set<string>(),
        linesByToken: new Map<string, Set<number>>(),
        defectEvidence: 0,
        defectLines: [],
        facetEvidence: 0,
      };
      entry.lines.add(match.line);
      entry.tokens.add(token);
      const tokenLines = entry.linesByToken.get(token) ?? new Set<number>();
      tokenLines.add(match.line);
      entry.linesByToken.set(token, tokenLines);
      byPath.set(match.path, entry);
    }
  }
  let entries = [...byPath.values()];
  const nonTests = entries.filter((entry) => !isTestConcernPath(entry.path));
  if (nonTests.length > 0) entries = nonTests;
  await Promise.all(entries.map(async (entry) => {
    const content = await readFileSafe(entry.path, workspace);
    // Causal warning markers are useful only when lexical grounding is weak.
    // With several independent query tokens they can belong to an unrelated
    // defect elsewhere in the same file and must not outweigh co-occurrence.
    const defect = content === null || group.tokens.length > 1
      ? { score: 0, lines: [] }
      : concernDefectEvidence(content, entry.lines);
    entry.defectEvidence = defect.score;
    entry.defectLines = defect.lines;
    entry.facetEvidence = content === null
      ? 0
      : concernFacetEvidenceScore(group.query, content, entry.path);
  }));
  const score = (entry: HitGroup): number => {
    const lowerPath = entry.path.toLowerCase();
    const pathHits = group.tokens.filter((token) => lowerPath.includes(token)).length;
    const sourceBonus = /\.(?:c|cc|cpp|cxx|go|java|js|jsx|py|rs|ts|tsx)$/i.test(entry.path) ? 1 : 0;
    return entry.tokens.size * 10
      + pathHits * 4
      + Math.min(5, entry.lines.size)
      + sourceBonus
      + concernLayerPathAdjustment(group.query, entry.path)
      + entry.defectEvidence
      + entry.facetEvidence;
  };
  entries.sort((a, b) => score(b) - score(a) || a.path.localeCompare(b.path));

  return entries.slice(0, 3).map((entry) => {
    const anchorLines = [...entry.linesByToken.values()]
      .map((lines) => [...lines].sort((a, b) => a - b))
      .sort((a, b) => a.length - b.length)[0] ?? [...entry.lines].sort((a, b) => a - b);
    let cluster: number[] = [];
    for (let index = 0; index < anchorLines.length; index++) {
      const candidateCluster = anchorLines.slice(index).filter((line) => line - anchorLines[index]! <= 40);
      // Prefer the later cluster on ties: leading file comments frequently
      // name the concept while the executable implementation is farther down.
      if (candidateCluster.length >= cluster.length) cluster = candidateCluster;
    }
    const line = cluster[0] ?? anchorLines[0] ?? 1;
    // Concurrency/race fixes usually span the producer, guarded state, and
    // consumer/commit method. A single-symbol window hides the actual mutex
    // and invites agents to invent one, so keep a bounded cross-method slice.
    // Structural only (2026-08-02 anti-overfit): an identifier the CALLER
    // literally typed, or the content-side defect scan around the hit lines.
    // Two natural-language arms used to follow — EN concurren*/mutex/atomic/
    // lost-update and JA 競合/排他/同時/重な/片方…失わ — both reverse-engineered
    // from one fixture's symptom wording. concernDefectEvidence already reads
    // the FILE for `data race`/`partially-written`/`silently lost`/…, which is
    // the same conclusion drawn from evidence instead of from phrasing.
    const raceShaped = group.tokens.includes("race")
      || entry.defectEvidence >= 4;
    const lastMatchedLine = Math.max(...entry.lines, cluster.at(-1) ?? line);
    const range = raceShaped
      ? entry.defectLines.length > 0
        ? `${Math.max(1, entry.defectLines[0]! - 40)}-${Math.max(entry.defectLines.at(-1)!, lastMatchedLine) + 60}`
        : `${Math.max(1, line - 140)}-${(cluster.at(-1) ?? line) + 140}`
      : `${Math.max(1, line - 12)}-${(cluster.at(-1) ?? line) + 12}`;
    return {
      path: entry.path,
      line,
      range,
      surface: classifySurface(entry.path),
      why: "concern-literal",
      // Preserve the repository-observed co-occurrence score so callers can
      // reject a lexical tie instead of breaking it with a domain alias or
      // lexicographic filename order.
      confidence: Math.min(0.99, 0.45 + score(entry) * 0.01),
      required: true,
    };
  });
}

/** Minimum span for a served window to count as spanning several methods. */
const CROSS_METHOD_MIN_SPAN_LINES = 60;

/**
 * True when a concern's served window spans several method bodies.
 *
 * literalConcernCandidates widens a concern's range to a cross-method window
 * EXACTLY when its structural race signal fired (`defectEvidence >= 4`, or the
 * caller literally typed `race`), and leaves it at a ±12-line single-symbol
 * window otherwise. The width therefore IS that verdict carried forward, which
 * is why the downstream sites can read it instead of re-running a
 * natural-language test of their own (2026-08-02 anti-overfit).
 */
function isCrossMethodConcernRange(range: string | undefined): boolean {
  const parsed = /^(\d+)-(\d+)$/.exec(range ?? "");
  if (parsed === null) return false;
  return Number(parsed[2]) - Number(parsed[1]) + 1 >= CROSS_METHOD_MIN_SPAN_LINES;
}

// NOT ADDED (2026-08-02, measured): an "the companion must share >=1 concern
// anchor token" admission rule. It is self-defeating here — the companion is
// BY DEFINITION the file the reporter did not name, so requiring it to contain
// a word the reporter typed removes exactly the pairing this function exists
// to produce. Measured on the T05c fixture: concern ①'s anchors reduce to
// ["saturation"], which lives in pid.cpp and NOT in rate_controller.cpp, so
// the rule deleted the legitimate control-layer companion (and with it the
// concern's whole reason to look across the layer boundary). See the report
// for the affinity-based alternative that addresses cross-concern bleed
// without this contradiction.
async function layeredControlCompanionCandidates(
  workspace: string,
  _group: ConcernGroup,
  onlyFiles: ReadonlySet<string>,
  existing: readonly ImpactCandidate[],
): Promise<ImpactCandidate[]> {
  // Structural admission (2026-08-02 anti-overfit, spec-multiconcern-vocab
  // §4.1). This used to require the query to contain one of
  // control|controller|steering|制御|ステアリング AND one of
  // saturat*|anti-windup|integrat*|clamp*|飽和|戻ら|蓄積 — eight words
  // reverse-engineered from one fixture's first symptom, which silently
  // dropped this pairing for every bug report in every other phrasing and
  // every other language. The 7-marker content scan below (`evidence >= 4`)
  // was always the real decision; the word test was a redundant pre-filter in
  // front of it. Now: attempt the probe whenever the leading candidate is
  // native, and let the file's own contents decide.
  const isControlLayer = (candidatePath: string): boolean =>
    /(?:^|\/)(?:control|controllers?)(?:\/|$)/i.test(candidatePath);
  const isMathLayer = (candidatePath: string): boolean =>
    /(?:^|\/)(?:math|numeric|algorithm)(?:\/|$)/i.test(candidatePath);
  const primary = existing[0];
  // The concern must ALREADY sit on the control⇄math boundary for a crossing
  // to mean anything. This is what the deleted word gate was really buying:
  // scope. Without a scope test the probe fires for every concern and can
  // hand a control-layer file to a concern that already has its own literal
  // primary elsewhere (measured: the mode-transition concern was reassigned
  // to rate_controller.cpp). Requiring the primary to be native AND on one
  // side of the boundary reproduces that scope from the file tree instead of
  // from the query's wording — and generalizes, because it no longer depends
  // on the reporter naming the layer.
  if (primary === undefined || !isNativeExtPath(primary.path)) return [];
  if (!isControlLayer(primary.path) && !isMathLayer(primary.path)) return [];
  // Decide from the leading causal owner, not from any lexical runner-up: a
  // mixer or unrelated file under /control/ can otherwise masquerade as the
  // missing stateful controller layer.
  const wantControl = primary === undefined || !isControlLayer(primary.path);
  const paths = [...onlyFiles]
    .filter((candidatePath) => isNativeExtPath(candidatePath))
    .filter((candidatePath) => /\.(?:c|cc|cpp|cxx)$/i.test(candidatePath))
    .filter((candidatePath) => wantControl ? isControlLayer(candidatePath) : isMathLayer(candidatePath));

  const ranked: Array<ImpactCandidate & { score: number }> = [];
  await Promise.all(paths.map(async (candidatePath) => {
    const content = await readFileSafe(candidatePath, workspace);
    if (content === null) return;
    const markers = [
      /(?:^|[^a-z0-9])i_state_/i,
      /\bintegr(?:al|ator)/i,
      /\bclamp/i,
      /\b(?:out|output)_?limit/i,
      /\b(?:update|step)\s*\(/i,
      /\berror\b/i,
      /\bdt\b/i,
    ];
    const evidence = markers.filter((marker) => marker.test(content)).length;
    if (evidence < 4) return;
    const lines = content.split(/\r?\n/);
    // Anchor the companion at the stateful control callable, not the first
    // file-level marker. Large native modules commonly declare limits near
    // the top and implement update()/step() much later; centering on that
    // declaration produced a truthful file candidate whose embedded slice
    // omitted the actual edit site.
    const callableIndexes = lines
      .map((sourceLine, index) => ({ sourceLine: sourceLine.trim(), index }))
      .filter(({ sourceLine }) =>
        !/^(?:\/\/|\/\*|\*)/.test(sourceLine)
        && /\b(?:update|step)\s*\(/i.test(sourceLine)
      )
      .map(({ index }) => index);
    const neighborhoodScore = (index: number): number => {
      const neighborhood = lines
        .slice(Math.max(0, index - 12), Math.min(lines.length, index + 81))
        .join("\n");
      return markers.filter((marker) => marker.test(neighborhood)).length;
    };
    const matchedIndex = callableIndexes
      .sort((left, right) => neighborhoodScore(right) - neighborhoodScore(left))[0]
      ?? lines.findIndex((line) =>
        /i_state_|integr(?:al|ator)|clamp|(?:out|output)_?limit/i.test(line)
      );
    const line = Math.max(1, matchedIndex + 1);
    const pathScore = /(?:rate|attitude|position|velocity|pid).*controller/i.test(candidatePath)
      ? 10
      : /controller/i.test(candidatePath)
        ? 6
        : 0;
    const score = evidence * 6 + pathScore;
    ranked.push({
      path: candidatePath,
      line,
      range: `${Math.max(1, line - 24)}-${line + 80}`,
      surface: classifySurface(candidatePath),
      why: "concern-causal-layer",
      confidence: Math.min(0.94, 0.5 + score * 0.01),
      required: true,
      score,
    });
  }));
  ranked.sort((left, right) =>
    right.score - left.score || left.path.localeCompare(right.path)
  );
  return ranked.slice(0, 1).map(({ score: _score, ...candidate }) => candidate);
}

interface PropagationSeedHit {
  path: string;
  line: number;
  role: string;
  tokens: Set<string>;
  definitionScore: number;
}

export async function propagationSeedPaths(
  args: TaskPackArgs,
  query: string,
  workspace: string,
): Promise<string[]> {
  const newValues = new Set(extractEnumValueTokens(query).map(normIdent));
  const identifiers = orderedUniqueTokens([
    ...(args.symbol ? [args.symbol] : []),
    ...concreteIdentifierTokens(query),
  ])
    .filter((token) => token.length >= 4 && !newValues.has(normIdent(token)))
    .slice(0, 6);
  if (identifiers.length === 0) return [];

  // An explicit path remains a hard scope. Without one, exact identifier
  // matches are safe to follow across package roots: cross-layer propagation
  // commonly spans shared contracts, services, and frontends in a monorepo.
  const explicitScope = args.path
    ? normalizedRequestPath(args.path).replace(/\/$/, "")
    : undefined;
  const explicitPathUnion = (args.paths ?? []).map(normalizePathEntry).map((entry) => entry.path);
  const allowedPathUnion = explicitPathUnion.length > 0
    ? new Set(explicitPathUnion.flatMap((scope) =>
        walkCodeFiles(workspace, { subPath: scope }).map((file) => file.relPath)
      ))
    : undefined;
  const byPath = new Map<string, PropagationSeedHit>();

  for (const token of identifiers) {
    for (const match of scanLiteral(token, workspace, { caseInsensitive: true })) {
      if (
        explicitScope
        && match.path !== explicitScope
        && !match.path.startsWith(explicitScope + "/")
      ) {
        continue;
      }
      if (allowedPathUnion && !allowedPathUnion.has(match.path)) continue;
      if (!hasIdentifierSegment(match.text ?? "", token)) continue;
      const current = byPath.get(match.path) ?? {
        path: match.path,
        line: match.line,
        role: classifySurface(match.path),
        tokens: new Set<string>(),
        definitionScore: -1,
      };
      current.tokens.add(token);
      const adjustment = definitionVsDerivedAdjustment(workspace, match.path, match.line);
      if (
        adjustment > current.definitionScore
        || (adjustment === current.definitionScore && match.line < current.line)
      ) {
        current.line = match.line;
        current.definitionScore = adjustment;
      }
      byPath.set(match.path, current);
    }
  }

  const roleOrder = new Map(
    ["contract", "api", "domain", "data", "ui", "style", "config", "test", "doc", "unknown"]
      .map((role, index) => [role, index]),
  );
  const ranked = [...byPath.values()].sort((a, b) => {
    const tokenDelta = b.tokens.size - a.tokens.size;
    if (tokenDelta !== 0) return tokenDelta;
    const definitionDelta = b.definitionScore - a.definitionScore;
    if (definitionDelta !== 0) return definitionDelta;
    const testDelta = Number(isTestConcernPath(a.path)) - Number(isTestConcernPath(b.path));
    if (testDelta !== 0) return testDelta;
    const roleDelta = (roleOrder.get(a.role) ?? 99) - (roleOrder.get(b.role) ?? 99);
    if (roleDelta !== 0) return roleDelta;
    return a.line - b.line || a.path.localeCompare(b.path);
  });

  const selected: PropagationSeedHit[] = [];
  const selectedPaths = new Set<string>();
  const propagationScope = args.path ?? inferQueryProjectScope(workspace, query) ?? null;
  // Cross-package enum fan-out with a real stylesheet needs one compact style
  // surface plus enough wire budget for the prepared contract. Keep the code
  // frontier to four edit sites; delegation-only route/review candidates are
  // lower value than the style family that the UI mapping actually names.
  const maxSeedPaths = isEnumLikeQueryShaped(query)
    && roleInventoryFor(workspace, propagationScope).has("style")
    ? MAX_SURFACES_DISTINCT - 2
    : MAX_SURFACES_DISTINCT;
  const add = (hit: PropagationSeedHit | undefined): void => {
    if (!hit || selectedPaths.has(hit.path) || selected.length >= maxSeedPaths) return;
    selected.push(hit);
    selectedPaths.add(hit.path);
  };

  // Enum members usually co-locate in one definition. Spend one slot on that
  // definition rather than allowing every old member to select a different
  // downstream consumer. Explicit compound identifiers (for example a named
  // component or service) remain independently required.
  const enumIdentifiers = identifiers.filter((token) => /^[A-Z][A-Z0-9_]{2,}$/.test(token));
  if (enumIdentifiers.length > 0) {
    add(ranked
      .filter((hit) => enumIdentifiers.some((token) => hit.tokens.has(token)))
      .sort((a, b) => b.definitionScore - a.definitionScore || ranked.indexOf(a) - ranked.indexOf(b))[0]);
  }
  const compoundIdentifiers = identifiers
    .filter((token) => !enumIdentifiers.includes(token) && /[a-z][A-Z]/.test(token))
    .sort((a, b) => b.length - a.length);
  for (const token of compoundIdentifiers) add(ranked.find((hit) => hit.tokens.has(token)));
  if (hasExplicitConcernList(query)) {
    const hardScopes = (args.paths ?? []).map(normalizePathEntry).map((entry) => entry.path);
    const scopedFiles = new Set(
      (hardScopes.length > 0
        ? hardScopes.flatMap((scope) => walkCodeFiles(workspace, { subPath: scope }))
        : walkCodeFiles(workspace, explicitScope ? { subPath: explicitScope } : {}))
        .map((file) => file.relPath),
    );
    for (const group of propagationFacetGroups(query)) {
      const candidates = await literalConcernCandidates(workspace, group, scopedFiles);
      const candidate = candidates[0];
      if (!candidate) continue;
      add({
        path: candidate.path,
        line: candidate.line,
        role: candidate.surface as string,
        tokens: new Set(group.tokens),
        definitionScore: 0,
      });
      const spansCreateValidation = (/\bcreat(?:e|es|ed|ing)\b/i.test(group.query) || group.query.includes("作成"))
        && (/\b(?:validation|validate|validator|schema)\b/i.test(group.query) || /バリデーション|検証/.test(group.query));
      const requestAlsoNeedsAggregation = /\b(?:stats?|statistics|aggregate|aggregation|counts?|summary)\b/i.test(query)
        || /統計|集計|件数/.test(query);
      if (spansCreateValidation && maxSeedPaths === MAX_SURFACES_DISTINCT && !requestAlsoNeedsAggregation) {
        const boundary = candidates.find((entry) => entry.path !== candidate.path);
        if (boundary) {
          add({
            path: boundary.path,
            line: boundary.line,
            role: boundary.surface as string,
            tokens: new Set(group.tokens),
            definitionScore: 0,
          });
        }
      }
    }
    const aggregationPattern = /\b(?:stats?|statistics|aggregate|aggregation|counts?|summary)\b/i;
    const aggregationJapanesePattern = /統計|集計|件数/;
    const aggregationClause = query
      .replace(/\r/g, "\n")
      .split(/[。；;\n]+/)
      .map((clause) => clause.replace(/^\s*(?:[-*]|\d+[.)]|[①②③④⑤⑥⑦⑧⑨⑩])\s*/, "").trim())
      .find((clause) => aggregationPattern.test(clause) || aggregationJapanesePattern.test(clause));
    // The bounded facet list can be filled by earlier bullets. Recover an
    // explicitly requested aggregate directly from its clause so a trailing
    // statistics sentence cannot be dropped merely because it was unbulleted.
    const aggregationGroup = aggregationClause
      ? { query: aggregationClause, tokens: concernAnchorTokens(aggregationClause) }
      : propagationFacetGroups(query).find((group) =>
          aggregationPattern.test(group.query) || aggregationJapanesePattern.test(group.query)
        );
    if (aggregationGroup && aggregationGroup.tokens.length > 0) {
      const candidates = await literalConcernCandidates(workspace, aggregationGroup, scopedFiles);
      const aggregate = candidates[0];
      if (aggregate && !selectedPaths.has(aggregate.path)) {
        // Preserve an explicitly requested aggregate over a same-role
        // delegation boundary selected earlier (for example a route that only
        // forwards into a schema). The aggregate owns state that must actually
        // change, while the forwarding surface is review-only.
        if (selected.length >= maxSeedPaths) {
          const removed = selected.pop();
          if (removed) selectedPaths.delete(removed.path);
        }
        add({
          path: aggregate.path,
          line: aggregate.line,
          role: aggregate.surface as string,
          tokens: new Set(aggregationGroup.tokens),
          definitionScore: 0,
        });
      }
    }
    if (selected.length >= 2) return selected.map((hit) => hit.path);
  }
  for (const token of identifiers) add(ranked.find((hit) => hit.tokens.has(token)));
  for (const role of roleOrder.keys()) {
    if (!selected.some((hit) => hit.role === role)) add(ranked.find((hit) => hit.role === role));
  }
  for (const hit of ranked) add(hit);

  return selected.length >= 2 ? selected.map((hit) => hit.path) : [];
}

async function buildPropagationTaskPack(
  args: TaskPackArgs,
  query: string,
  workspace: string,
): Promise<TaskPackResult | undefined> {
  const paths = await propagationSeedPaths(args, query, workspace);
  if (paths.length < 2) return undefined;
  const result = await buildSeededTaskPack({ ...args, paths }, query, workspace);
  // buildSeededTaskPack captures the internal seeded request. Replace that
  // cache key with the caller's original pathless request so an exact retry
  // still receives the compact pack-unchanged response.
  captureServedPack(workspace, args, computePackFingerprint(args, workspace), result);
  return result;
}

async function buildMultiConcernTaskPack(
  args: TaskPackArgs,
  query: string,
  workspace: string,
): Promise<TaskPackResult | undefined> {
  const groups = concernGroupsForQuery(query);
  if (groups.length < 2) return undefined;
  // Enum/role fan-out and explicit Markdown-contract references are one
  // cross-surface change, not independent symptoms; keep their established
  // required-role, section-selection, and closure machinery via binding fallback.
  if ((isEnumLikeQueryShaped(query) && !hasExplicitIndependentConcernMarker(query)) ||
      (/\b[\w.-]+\.md\b/i.test(query) && !hasExplicitConcernList(query))) {
    return undefined;
  }

  const cache = new FileReadCache();
  const explicitPaths = (args.paths ?? [])
    .map(normalizePathEntry)
    .map((entry) => entry.path)
    .filter((entryPath) => entryPath.length > 0);
  const explicitScope = explicitPaths.length > 0
    ? commonAncestorDir(explicitPaths.map((entryPath) =>
        isDirectoryWithin(entryPath, workspace) ? `${entryPath}/.` : entryPath
      ))
    : undefined;
  const effectiveScope = args.path ?? explicitScope ?? inferQueryProjectScope(workspace, query);
  // paths[] is a hard union, not merely a ranking hint. This keeps a scoped
  // retry from reintroducing same-token files from sibling applications.
  const concernFiles = new Set(
    (explicitPaths.length > 0
      ? explicitPaths.flatMap((entryPath) =>
          walkCodeFiles(workspace, { subPath: entryPath }).map((file) => file.relPath)
        )
      : walkCodeFiles(workspace, effectiveScope ? { subPath: effectiveScope } : {}).map((file) => file.relPath)),
  );
  const ambiguityByGroup = new Map<number, string[]>();
  const matchesByGroup = await Promise.all(groups.map(async (group, groupIndex) => {
    let candidates = await literalConcernCandidates(workspace, group, concernFiles);
    if (candidates.length === 0) {
      const located = await locateTaskContext(workspace, {
        action: "locate",
        query: group.query,
        ...(effectiveScope ? { path: effectiveScope } : {}),
        ...(args.symbol ? { symbol: args.symbol } : {}),
        ...(args.lang ? { lang: args.lang } : {}),
        limit: 3,
      });
      candidates = (located.hit
        ? [...located.primary, ...located.related]
        : rankAbstainCandidates(located.candidateDetails ?? []))
        .filter((candidate) => concernFiles.has(candidate.path));
    }

    const causalPrimaryPath = candidates[0]?.path;
    const causalCompanions = await layeredControlCompanionCandidates(
      workspace,
      group,
      concernFiles,
      candidates.slice(0, 3),
    );
    if (causalCompanions.length > 0) {
      candidates = candidates.map((candidate) =>
        candidate.path === causalPrimaryPath
          ? { ...candidate, why: "concern-causal-layer" }
          : candidate
      );
      const seenPaths = new Set(causalCompanions.map((candidate) => candidate.path));
      // The user-visible controller owns the output state and should lead the
      // edit frontier; a math primitive remains the bounded second layer.
      const companionIsControl = /(?:^|\/)(?:control|controllers?)(?:\/|$)/i
        .test(causalCompanions[0]!.path);
      candidates = companionIsControl
        ? [...causalCompanions, ...candidates.filter((candidate) => !seenPaths.has(candidate.path))]
        : [...candidates, ...causalCompanions.filter((candidate) =>
            !candidates.some((existing) => existing.path === candidate.path)
          )];
    }

    if (
      candidates[0]?.why === "concern-literal"
      && candidates[1]?.why === "concern-literal"
      && Math.abs(candidates[0].confidence - candidates[1].confidence) < 0.001
    ) {
      // No query/repository evidence distinguishes these files. Returning a
      // lexicographic winner would merely replace the removed fixed alias
      // table with a different guess, so keep this concern unresolved.
      ambiguityByGroup.set(groupIndex, candidates.slice(0, 3).map((candidate) => candidate.path));
      candidates = [];
    }

    const selectedCandidates: ImpactCandidate[] = [];
    const seenCandidates = new Set<string>();
    for (const candidate of candidates) {
      const range = candidate.range ?? `${candidate.line}-${candidate.line}`;
      const key = `${candidate.path}\0${candidate.symbol ?? ""}\0${range}`;
      if (seenCandidates.has(key)) continue;
      seenCandidates.add(key);
      selectedCandidates.push(candidate);
      if (selectedCandidates.length >= 3) break;
    }

    const built = await Promise.all(selectedCandidates.map((candidate) =>
      candidateToSurface(
        { ...candidate, required: true },
        workspace,
        [],
        query,
        cache,
        {
          // 2026-08-02 anti-overfit: read the width literalConcernCandidates
          // already chose from structural evidence, instead of re-testing the
          // query against the same EN/JA word list a third time.
          causalMultiMethod: isCrossMethodConcernRange(candidate.range)
            || group.tokens.includes("race"),
        },
      )
    ));

    const seenSurfaces = new Set<string>();
    return built.filter((surface) => {
      const key = `${surface.path}\0${surface.symbol ?? ""}\0${surface.range}`;
      if (seenSurfaces.has(key)) return false;
      // Meta vocabulary such as "JSON", "auto", or "regression"
      // describes the retrieval problem but is not enough to prove that the
      // served surface addresses the concern. Require at least one substantive
      // group token in the actual path/symbol/body before declaring coverage.
      const evidenceTokens = group.tokens.filter((token) =>
        // R0 (2026-08-21, W4-A): compare case-insensitively — group.tokens
        // (concernAnchorTokens) may now carry a case-preserved identifier
        // (e.g. "buildInitializeInstructions") since orderedUniqueTokens
        // stopped silently lowercasing its output; META_CONCERN_EVIDENCE_TOKENS
        // is lowercase-only.
        !META_CONCERN_EVIDENCE_TOKENS.has(token.toLowerCase())
      );
      const matches = surface.why === "concern-causal-layer"
        || evidenceTokens.some((token) => concernTokenMatchesSurface(token, surface));
      if (matches) seenSurfaces.add(key);
      return matches;
    });
  }));

  const surfaceKey = (surface: TaskPackSurface): string =>
    `${surface.path}\0${surface.symbol ?? ""}\0${surface.range}`;
  const surfaces: TaskPackSurface[] = [];
  const selectedKeys = new Set<string>();
  const addSurface = (surface: TaskPackSurface): void => {
    if (surfaces.length >= MAX_SURFACES_DISTINCT) return;
    const key = surfaceKey(surface);
    if (selectedKeys.has(key)) return;
    selectedKeys.add(key);
    surfaces.push(surface);
  };

  // Preserve one edit-grade surface per covered concern by default. A
  // stateful saturation defect is the bounded exception: the externally
  // visible controller and its reusable numerical primitive can each own a
  // different half of anti-windup semantics. Keep exactly one cross-layer
  // companion when repository evidence found both, rather than treating the
  // helper as a locator runner-up or forcing another discovery turn.
  // (`index` is gone with the per-group query gate this loop used to consult;
  // the pairing decision is now made entirely from the surfaces themselves.)
  for (const matches of matchesByGroup) {
    const primary = matches[0];
    if (!primary) continue;
    addSurface(primary);
    // Structural admission, same rationale as layeredControlCompanionCandidates
    // above — this was a verbatim second copy of the same eight-word EN/JA
    // gate. The cross-layer pairing test below (native extension AND a
    // control⇄math directory crossing) is the structural signal it was
    // pre-filtering, and it stands on its own.
    if (!isNativeExtPath(primary.path)) continue;
    const primaryInControl = /(?:^|\/)(?:control|controllers?)(?:\/|$)/i.test(primary.path);
    const primaryInMath = /(?:^|\/)(?:math|numeric|algorithm)(?:\/|$)/i.test(primary.path);
    const companion = matches.slice(1).find((surface) => {
      if (!isNativeExtPath(surface.path)) return false;
      const inControl = /(?:^|\/)(?:control|controllers?)(?:\/|$)/i.test(surface.path);
      const inMath = /(?:^|\/)(?:math|numeric|algorithm)(?:\/|$)/i.test(surface.path);
      return (primaryInControl && inMath) || (primaryInMath && inControl);
    });
    if (companion) addSurface(companion);
  }

  // A concurrency defect often spans private state declared in a native
  // header and methods implemented in its source. The source alone can expose
  // the race while hiding the repository's existing mutex/atomic primitive,
  // which invites an incompatible synchronization mechanism. Add exactly one
  // header companion for such a concern; other concerns keep the one-surface
  // budget.
  for (const [index, group] of groups.entries()) {
    const primary = matchesByGroup[index]?.[0];
    if (!primary || surfaces.length >= MAX_SURFACES_DISTINCT) continue;
    // 2026-08-02 anti-overfit: a third, slightly-drifted copy of the same
    // EN/JA race word list used to gate here. The concern's own served window
    // already encodes that verdict structurally (see isCrossMethodConcernRange).
    if (!isCrossMethodConcernRange(primary.range) && !group.tokens.includes("race")) continue;
    const companionSet = [primary];
    await augmentNativeSurfaceClosure(companionSet, workspace, query, [], cache);
    for (const companion of companionSet.slice(1)) {
      // For a race fix the state declaration and its repository-native
      // synchronization primitive are part of the edit proof, not optional
      // review context. Keeping the header required also prevents response
      // compaction from retaining an implementation handle that refers to a
      // surface no longer present in the pack.
      companion.required = true;
      addSurface(companion);
    }
  }

  // B1c parity (2026-08-01): the multi_concern builder was the one pack path
  // that never force-included files the query literally names (T05c rep2: the
  // query said "CONTRACT.md に FR/BL/FL/BR の規定があるはず" and the pack
  // neither served nor disclosed it, then certified prepared/complete). A
  // stated requirement outranks ranked candidates, so this runs even when
  // every concern is covered, ahead of pair enrichment in cap priority.
  await augmentQueryNamedFileSurfaces(
    surfaces, query, workspace, [], cache, undefined, MULTI_CONCERN_ENRICHMENT_MAX_SURFACES,
  );

  // Serve the header/source sibling for EVERY native concern primary, not only
  // concurrency-flavored ones. A header routinely DECLARES the contract (sign
  // conventions, units, locking rules) that its source visibly violates —
  // serving the source alone yields self-consistent-but-wrong evidence (T05c
  // rep2: mixer.cpp's flipped yaw table cited "CONTRACT §7.6" as authoritative
  // while the contradicting mixer.hpp went unserved, so an obedient
  // prepared-fence caller judged bug ② absent). Enrichment pairs stay
  // required:false — they must never veto readiness — and the byte-budget trim
  // may still shed them, now with an explicit re-request disclosure.
  const addEnrichmentSurface = (surface: TaskPackSurface): void => {
    if (surfaces.length >= MULTI_CONCERN_ENRICHMENT_MAX_SURFACES) return;
    const key = surfaceKey(surface);
    if (selectedKeys.has(key)) return;
    selectedKeys.add(key);
    surfaces.push(surface);
  };
  for (const [index] of groups.entries()) {
    if (surfaces.length >= MULTI_CONCERN_ENRICHMENT_MAX_SURFACES) break;
    const primary = matchesByGroup[index]?.[0];
    if (!primary || !isNativeExtPath(primary.path)) continue;
    if (!surfaces.some((surface) => surface.path === primary.path)) continue;
    const pairPaths = nativePairCandidates(primary.path);
    if (surfaces.some((surface) => pairPaths.includes(surface.path))) continue;
    const companionSet = [primary];
    await augmentNativeSurfaceClosure(companionSet, workspace, query, [], cache);
    for (const companion of companionSet.slice(1)) addEnrichmentSurface(companion);
  }

  const concerns: ConcernCoverage[] = groups.map((_group, index) => {
    const handles = [...new Set(
      matchesByGroup[index]!
        .filter((surface) => selectedKeys.has(surfaceKey(surface)))
        .map((surface) => surface.handle),
    )];
    return {
      id: `c${index + 1}`,
      status: handles.length > 0 ? "covered" : "needs-followup",
      handles,
    };
  });
  const unresolved = concerns.flatMap((coverage, index) =>
    coverage.status === "needs-followup"
      ? [{ coverage, group: groups[index]! }]
      : []
  );
  const concernAmbiguities = [...ambiguityByGroup.entries()]
    .sort(([left], [right]) => left - right)
    .map(([index, candidates]) => ({ id: `c${index + 1}`, candidates }));
  const ambiguousIds = new Set(concernAmbiguities.map((ambiguity) => ambiguity.id));
  const discoverableUnresolved = unresolved.filter(({ coverage }) => !ambiguousIds.has(coverage.id));
  const representativeTokenFor = (group: ConcernGroup): string =>
    // R0 (2026-08-21, W4-A): case-insensitive compare — see orderedUniqueTokens.
    group.tokens.find((token) => !WEAK_CONCERN_TOKENS.has(token.toLowerCase())) ?? group.tokens[0]!;

  const result: TaskPackResult = {
    mode: "task_pack",
    coverage: unresolved.length > 0 ? "partial" : "complete",
    ...(unresolved.length > 0 ? { coverage_reason: "concerns-uncovered" as const } : {}),
    surfaces,
    missing: [],
    concerns,
    ...(concernAmbiguities.length > 0 ? { concern_ambiguities: concernAmbiguities } : {}),
    ...(discoverableUnresolved.length > 0 ? {
      blocking_next_steps: discoverableUnresolved.map(({ group }) =>
        `search_files action=find query="${representativeTokenFor(group)}"`
      ),
    } : {}),
    route: {
      action: concernAmbiguities.length > 0
        ? "confirm_candidates"
        : unresolved.length > 0
          ? "locate_missing_surfaces"
          : "edit_from_handles",
      reason: concernAmbiguities.length > 0
        ? `ambiguous concern candidate(s): ${concernAmbiguities.map(({ id }) => id).join(",")}`
        : unresolved.length > 0
        ? `unresolved concern(s): ${unresolved.map(({ coverage }) => coverage.id).join(",")}`
        : "all concern surfaces located",
      max_additional_tl_calls: discoverableUnresolved.length > 0
        ? Math.ceil(discoverableUnresolved.length / MAX_STAGE_CALLS)
        : 0,
    },
  };

  if (discoverableUnresolved.length > 0) {
    const calls: ContinuationCall[] = discoverableUnresolved.map(({ coverage, group }) => {
      if (coverage.handles.length > 0) {
        return { tool: "read_file", arguments: { handles: coverage.handles } };
      }
      const representative = representativeTokenFor(group);
      return {
        tool: "search_files",
        arguments: { action: "find", query: representative },
      };
    });
    const plan: ContinuationPlan = { version: 1, stages: [] };
    for (let index = 0; index < calls.length; index += MAX_STAGE_CALLS) {
      plan.stages.push({
        execution: "parallel",
        calls: calls.slice(index, index + MAX_STAGE_CALLS),
      });
    }
    const bounded = enforceContinuationBudget(plan);
    if (bounded) {
      result.continuation = bounded;
      const next = deriveNextFromPlan(bounded);
      if (next !== undefined) result.next = next;
    }
  }

  return dedupeTrimAndPersist(workspace, result, {
    protectedSurfaces: new Set(surfaces),
    args,
  });
}

interface ArtifactBuildExtraction {
  section?: ArtifactTaskPackSection;
  sectionId?: string;
  next: string;
}

function artifactBuildRelevance(file: DiscoveredArtifactFile, args: TaskPackArgs, query: string): number {
  const candidate = file.relPath.toLowerCase();
  const basename = path.posix.basename(candidate);
  const queryLower = query.toLowerCase();
  const queryCompact = queryLower.replace(/[^a-z0-9\u3040-\u30ff\u3400-\u9fff]+/g, "");
  const basenameCompact = basename.replace(/[^a-z0-9\u3040-\u30ff\u3400-\u9fff]+/g, "");
  const rawTokens = queryLower.match(/[a-z0-9]+/g)?.filter((token) => token.length >= 3) ?? [];
  const tokenPositions = new Map<string, number>();
  for (const [index, token] of rawTokens.entries()) {
    for (const variant of artifactQueryTokenVariants(token)) {
      const previous = tokenPositions.get(variant);
      if (previous === undefined || index < previous) tokenPositions.set(variant, index);
    }
  }
  let score = 0;
  for (const [token, index] of tokenPositions) {
    if (candidate.includes(token)) score += 10 + Math.max(0, 8 - index);
  }
  if (basenameCompact.length > 0 && queryCompact.includes(basenameCompact)) score += 100;
  if (args.path) {
    const requested = args.path.replace(/\\/g, "/").replace(/^\.\//, "").toLowerCase();
    if (candidate === requested) score += 200;
    else if (candidate.startsWith(requested.replace(/\/$/, "") + "/")) score += 50;
  }
  return score;
}

function discoverArtifactBuildFiles(
  args: TaskPackArgs,
  query: string,
  workspace: string,
): DiscoveredArtifactFile[] {
  const found = new Map<string, DiscoveredArtifactFile>();
  for (const file of discoverArtifactFiles(workspace, { query })) found.set(file.relPath, file);
  if (args.path) {
    for (const file of discoverArtifactFiles(workspace, { path: args.path, includeAll: true })) {
      found.set(file.relPath, file);
    }
  }
  return [...found.values()].sort((a, b) =>
    artifactBuildRelevance(b, args, query) - artifactBuildRelevance(a, args, query)
      || a.relPath.localeCompare(b.relPath)
  );
}

function relevantSheetName(
  sheets: Array<{ name: string }>,
  query: string,
): string | undefined {
  const queryLower = query.toLowerCase();
  const queryCompact = queryLower.replace(/[^a-z0-9\u3040-\u30ff\u3400-\u9fff]+/g, "");
  let best: { name: string; score: number } | undefined;
  for (const sheet of sheets) {
    const nameLower = sheet.name.toLowerCase();
    const compact = nameLower.replace(/[^a-z0-9\u3040-\u30ff\u3400-\u9fff]+/g, "");
    const terms = nameLower.match(/[a-z0-9\u3040-\u30ff\u3400-\u9fff]+/g) ?? [];
    const metadataPenalty = /^(?:meta|metadata|readme|cover|instructions|index|summary)$/i.test(nameLower)
      && sheets.length > 1 ? 50 : 0;
    const score = (compact.length > 0 && queryCompact.includes(compact) ? 100 : 0)
      + terms.reduce((total, term) => total + (term.length >= 2 && queryLower.includes(term) ? 10 : 0), 0);
    const adjustedScore = score - metadataPenalty;
    if (!best || adjustedScore > best.score) best = { name: sheet.name, score: adjustedScore };
  }
  return best?.name ?? sheets[0]?.name;
}

async function extractArtifactBuildSection(
  artifact: MintedArtifactSurface,
  query: string,
): Promise<ArtifactBuildExtraction> {
  const artifactPath = artifact.surface.path;
  const baseNext = `read_file mode=artifact path=${artifactPath}${
    artifact.credentialRef !== undefined
      ? ` credentialRef=${JSON.stringify(artifact.credentialRef)}`
      : ""
  }`;
  const maxChars = mustFetchPackCap(MAX_TASK_PACK_BYTES);
  let artifactBytes: Uint8Array = artifact.bytes;

  if (
    artifact.surface.artifactKind === "xlsx"
    || artifact.surface.artifactKind === "docx"
    || artifact.surface.artifactKind === "pptx"
  ) {
    const prepared = await prepareOfficeDocument(
      artifact.bytes,
      artifact.credentialPassword,
    );
    if (!prepared.ok) return { next: baseNext };
    artifactBytes = prepared.bytes;
    // Mandatory OOXML container preflight (CWE-400/409): this is the third
    // entry point into the office parsers besides mode=artifact and
    // extract_office_text, and it must not hand un-preflighted bytes to any
    // of them. On refusal the pack degrades to the mode=artifact next call,
    // which reports the exceeded limit as a structured error.
    const containerPreflight = await preflightZip(artifactBytes);
    if (!containerPreflight.ok) return { next: baseNext };
  }

  const officeArtifactKind = artifact.surface.artifactKind === "xlsx"
    || artifact.surface.artifactKind === "docx"
    || artifact.surface.artifactKind === "pptx"
    ? artifact.surface.artifactKind
    : undefined;
  const visualInventory = officeArtifactKind
    && /\b(?:chart|graph|image|video|media|visual|caption|series|category|alt[ -]?text)\b|グラフ|チャート|画像|動画|図表|図形|図|代替テキスト|系列|カテゴリ/i.test(query)
    ? await extractOoxmlVisualInventory(artifactBytes, officeArtifactKind, { preflighted: true })
    : undefined;
  const visualFields = visualInventory && hasOoxmlVisualInventory(visualInventory)
    ? { visuals: visualInventory }
    : {};

  if (artifact.surface.artifactKind === "xlsx") {
    const roster = await xlsxRoster(artifactBytes);
    if (!roster.ok) return { next: baseNext };
    const sheet = relevantSheetName(roster.sheets, query);
    if (!sheet) {
      if (!visualInventory || !hasOoxmlVisualInventory(visualInventory)) return { next: baseNext };
      return {
        section: {
          sheet: "Visual inventory",
          range: "",
          columns: [],
          rows: [],
          truncated: visualInventory.truncated,
          ...visualFields,
        },
        sectionId: "Visual inventory",
        next: baseNext,
      };
    }
    const next = `${baseNext} sheet=${sheet} as=json`;
    const table = await xlsxTable(artifactBytes, { sheet, as: "json" });
    if (!table.ok) return { next };
    return {
      section: {
        sheet: table.sheet,
        range: table.range,
        columns: table.columns,
        rows: table.rows,
        truncated: table.truncated || visualInventory?.truncated === true,
        ...visualFields,
      },
      sectionId: table.sheet,
      next,
    };
  }

  if (artifact.surface.artifactKind === "docx") {
    let extracted = await docxSections(artifactBytes, { query, maxChars });
    if (
      extracted.ok
      && extracted.sections.reduce((total, section) => total + section.text.trim().length, 0) < 80
    ) {
      extracted = await docxSections(artifactBytes, { maxChars });
    }
    if (!extracted.ok) return { next: baseNext };
    if (extracted.sections.length === 0) {
      const visualText = visualInventory ? renderOoxmlVisualInventory(visualInventory) : "";
      if (!visualText) return { next: baseNext };
      extracted.sections.push({ heading: "Visual inventory", text: visualText });
    }
    const terms = significantQueryTokens(query);
    const ranked = [...extracted.sections].sort((a, b) => {
      const score = (section: { heading: string; text: string }): number => {
        const combined = `${section.heading} ${section.text}`.toLowerCase();
        const matches = terms.reduce(
          (total, term) => total + (combined.includes(term.toLowerCase()) ? 1 : 0),
          0,
        );
        const titleOnlyPenalty = section.text.trim().length < 80 ? 20 : 0;
        const preamblePenalty = section.heading === "(Preamble)" ? 5 : 0;
        return matches * 10 + Math.min(section.text.trim().length, 1_000) / 100
          - titleOnlyPenalty - preamblePenalty;
      };
      return score(b) - score(a);
    });
    const sectionId = ranked[0]!.heading;
    return {
      section: {
        kind: "docx",
        sections: ranked,
        truncated: extracted.truncated || visualInventory?.truncated === true,
        ...visualFields,
      },
      sectionId,
      next: `${baseNext} sections=${JSON.stringify([sectionId])}`,
    };
  }

  if (artifact.surface.artifactKind === "pptx") {
    let extracted = await pptxSlides(artifactBytes, { query, maxChars });
    if (
      extracted.ok
      && extracted.slides.reduce((total, slide) => total + slide.text.trim().length, 0) < 80
    ) {
      // Same fallback the docx branch above already has, and for the same
      // reason: pptxSlides DROPS every slide whose text shares no term with the
      // query, so a caller-named deck written in different vocabulary than the
      // request ("What does the deck say about X?" against slides that never use
      // the request's connective words) extracted to nothing and the pack fell
      // back to a content-free stub. Re-extract unfiltered instead.
      extracted = await pptxSlides(artifactBytes, { maxChars });
    }
    if (!extracted.ok) return { next: baseNext };
    if (extracted.slides.length === 0) {
      const visualText = visualInventory ? renderOoxmlVisualInventory(visualInventory) : "";
      if (!visualText) return { next: baseNext };
      extracted.slides.push({ heading: "Visual inventory", text: visualText });
    }
    const sectionId = extracted.slides[0]!.heading;
    const slide = sectionId.match(/^Slide\s+(\d+)/)?.[1];
    return {
      section: {
        kind: "pptx",
        slides: extracted.slides,
        truncated: extracted.truncated || visualInventory?.truncated === true,
        ...visualFields,
      },
      sectionId,
      next: slide ? `${baseNext} slides=${JSON.stringify([slide])}` : baseNext,
    };
  }

  if (artifact.surface.artifactKind === "csv") {
    // csv/tsv is a single table — reuse the xlsx sheet-shaped section so it
    // flows through the identical bounded-candidate machinery
    // (artifactSectionCandidates' "sheet" in section table path, meaningful(),
    // fitArtifactSections). `sheet` carries the file basename as the logical
    // table name; `ext` forces the tab delimiter for .tsv inside csvTable.
    const table = csvTable(artifact.bytes, { ext: artifact.file.ext });
    if (!table.ok || table.columns.length === 0) return { next: baseNext };
    const sectionId = path.posix.basename(artifactPath);
    return {
      section: {
        sheet: sectionId,
        range: table.range,
        columns: table.columns,
        rows: table.rows,
        truncated: table.truncated,
      },
      sectionId,
      next: baseNext,
    };
  }

  let extracted = await pdfPages(artifact.bytes, {
    query,
    maxChars,
    ...(artifact.credentialPassword !== undefined
      ? { password: artifact.credentialPassword }
      : {}),
  });
  if (
    extracted.ok
    && extracted.pages.reduce((total, page) => total + page.text.trim().length, 0) < 80
  ) {
    // Same query-filter fallback as the docx/pptx branches above.
    extracted = await pdfPages(artifact.bytes, {
      maxChars,
      ...(artifact.credentialPassword !== undefined
        ? { password: artifact.credentialPassword }
        : {}),
    });
  }
  if (!extracted.ok || extracted.pages.length === 0) return { next: baseNext };
  const sectionId = `page-${extracted.pages[0]!.page}`;
  return {
    section: { kind: "pdf", pages: extracted.pages, truncated: extracted.truncated },
    sectionId,
    next: `${baseNext} pages=${JSON.stringify([String(extracted.pages[0]!.page)])}`,
  };
}

const ARTIFACT_KIND_MARKERS: ReadonlyArray<{
  kind: ArtifactKind;
  markers: readonly RegExp[];
}> = [
  { kind: "xlsx", markers: [/\bxlsx\b/i, /\bexcel\b/i, /\bspreadsheet\b/i, /\bworkbook\b/i] },
  { kind: "docx", markers: [/\bdocx\b/i, /\bword document\b/i] },
  { kind: "pptx", markers: [/\bpptx\b/i, /\bpowerpoint\b/i, /\bslide deck\b/i] },
  { kind: "pdf", markers: [/\bpdf\b/i] },
  { kind: "csv", markers: [/\bcsv\b/i, /\btsv\b/i, /\bcomma-separated\b/i, /\btab-separated\b/i] },
];

function requestedArtifactKinds(query: string): ArtifactKind[] {
  return ARTIFACT_KIND_MARKERS
    .map(({ kind, markers }) => ({
      kind,
      index: Math.min(...markers.map((marker) => query.search(marker)).filter((index) => index >= 0)),
    }))
    .filter((entry) => Number.isFinite(entry.index))
    .sort((a, b) => a.index - b.index)
    .map(({ kind }) => kind)
    .slice(0, 2);
}

/**
 * B2f (2026-08-01 serving-completeness): does this artifact's own NAME carry a
 * token the query used — i.e. is it an input axis the request explicitly named?
 *
 * Deliberately narrower than artifactBuildRelevance: only query-token ↔ filename
 * evidence counts (no args.path boost, no kind priors), so the test says exactly
 * "the request named this file's subject", never "this file ranked well".
 */
function artifactNamedByQuery(file: DiscoveredArtifactFile, query: string): boolean {
  // Match the file's OWN name, extension stripped. Neither the containing
  // directory ("docs/") nor the extension ("xlsx") identifies an input axis —
  // both are shared by every sibling artifact, so including them would make any
  // query that says "xlsx" pull in every workbook in the tree.
  const lower = file.relPath.toLowerCase();
  const stem = path.posix.basename(lower).replace(/\.[^.]+$/, "");
  if (stem.length === 0) return false;
  const stemCompact = stem.replace(/[^a-z0-9぀-ヿ㐀-鿿]+/g, "");
  const queryLower = query.toLowerCase();
  const queryCompact = queryLower.replace(/[^a-z0-9぀-ヿ㐀-鿿]+/g, "");
  if (stemCompact.length > 0 && queryCompact.includes(stemCompact)) return true;
  // A token that also names one of this file's own FOLDERS is describing where
  // the file lives, not which axis it is ("the design DOCX" points at docs/
  // design/, and must not promote docs/design/system-design.docx over the
  // methodology doc already selected for that kind).
  const directorySegments = new Set(path.posix.dirname(lower).split("/").filter(Boolean));
  const rawTokens = queryLower.match(/[a-z0-9]+/g)?.filter((token) => token.length >= 3) ?? [];
  for (const token of rawTokens) {
    if (directorySegments.has(token)) continue;
    for (const variant of artifactQueryTokenVariants(token)) {
      if (variant.length >= 3 && !directorySegments.has(variant) && stem.includes(variant)) return true;
    }
  }
  return false;
}

function selectArtifactBuildInputs(
  artifacts: readonly MintedArtifactSurface[],
  query: string,
  args: TaskPackArgs,
): MintedArtifactSurface[] {
  const explicitlyNamed = [
    ...(args.path ? [args.path] : []),
    ...(args.paths ?? []).map(normalizePathEntry).map((entry) => entry.path),
  ]
    .map(normalizedRequestPath)
    .filter((artifactPath) => ARTIFACT_EXT_SET.has(path.extname(artifactPath).toLowerCase()));
  if (explicitlyNamed.length > 0) {
    const byPath = new Map(
      artifacts.map((artifact) => [
        normalizedRequestPath(artifact.surface.path),
        artifact,
      ] as const),
    );
    const selected: MintedArtifactSurface[] = [];
    const seen = new Set<string>();
    for (const artifactPath of explicitlyNamed) {
      if (seen.has(artifactPath)) continue;
      const artifact = byPath.get(artifactPath);
      if (artifact !== undefined) {
        selected.push(artifact);
        seen.add(artifactPath);
      }
    }
    if (selected.length > 0) return selected.slice(0, 4);
  }

  const ranked = [...artifacts].sort((a, b) =>
    artifactBuildRelevance(b.file, args, query) - artifactBuildRelevance(a.file, args, query)
      || a.surface.path.localeCompare(b.surface.path)
  );
  const requested = requestedArtifactKinds(query);
  const byKind = requested
    .map((kind) => ranked.find((artifact) => artifact.surface.artifactKind === kind))
    .filter((artifact): artifact is MintedArtifactSurface => artifact !== undefined);
  // B2f (2026-08-01 serving-completeness): the kind-based pick takes at most ONE
  // artifact per requested KIND, so a task naming two independent inputs of the
  // same kind lost one of them. Live defect (2026-07-31-semantic-signal5-2,
  // T10): the occupation-code workbook was already a listed pack SURFACE with an
  // `extract` hint, yet artifact_requirements named only the rate table and the
  // methodology doc — so every rep paid one extra mode=artifact call for an
  // input axis the task text had spelled out. Any artifact the QUERY names by
  // token is an input axis and joins the requirement set, whatever its kind.
  const selected = [...byKind];
  const chosen = new Set(selected.map((artifact) => artifact.surface.path));
  for (const artifact of ranked) {
    if (selected.length >= MAX_INLINED_ARTIFACT_SURFACES) break;
    if (chosen.has(artifact.surface.path)) continue;
    if (!artifactNamedByQuery(artifact.file, query)) continue;
    selected.push(artifact);
    chosen.add(artifact.surface.path);
  }
  if (selected.length > 0) return selected;
  return requested.length === 0 ? ranked.slice(0, 1) : [];
}

interface ExtractedArtifactSection {
  artifact: MintedArtifactSurface;
  extraction: ArtifactBuildExtraction & { section: ArtifactTaskPackSection; sectionId: string };
}

function artifactSectionStamp(item: ExtractedArtifactSection): string {
  return `artifact-section:${item.artifact.surface.path}#${item.extraction.sectionId.slice(0, 120)}`;
}

function fitArtifactSections(
  result: TaskPackResult,
  extracted: readonly ExtractedArtifactSection[],
): Array<{ path: string; section: ArtifactTaskPackSection }> | undefined {
  const variants = extracted.map((item) => artifactSectionCandidates(item.extraction.section));
  let fitted: Array<{ path: string; section: ArtifactTaskPackSection }> | undefined;
  const visit = (index: number, selected: ArtifactTaskPackSection[]): void => {
    if (fitted !== undefined) return;
    if (index < variants.length) {
      for (const section of variants[index] ?? []) visit(index + 1, [...selected, section]);
      return;
    }
    const bundled = selected.map((section, itemIndex) => ({
      path: extracted[itemIndex]!.artifact.surface.path,
      section,
    }));
    const inlined = extracted.map(artifactSectionStamp);
    const candidate: TaskPackResult = bundled.length === 1
      ? { ...result, section: bundled[0]!.section, inlined }
      : { ...result, artifact_sections: bundled, inlined };
    delete candidate.next;
    if (fitsInCap(candidate)) fitted = bundled;
  };
  visit(0, []);
  return fitted;
}

async function attachArtifactBuildSections(
  result: TaskPackResult,
  artifacts: readonly MintedArtifactSurface[],
  query: string,
  args: TaskPackArgs,
): Promise<{ complete: boolean; truncated: boolean }> {
  const selected = selectArtifactBuildInputs(artifacts, query, args);
  result.artifact_requirements = selected.map((artifact) => artifact.surface.path);
  const extracted: ExtractedArtifactSection[] = [];
  let firstMissingNext: string | undefined;
  for (const artifact of selected) {
    const extraction = await extractArtifactBuildSection(artifact, query);
    if (extraction.section && extraction.sectionId) {
      extracted.push({
        artifact,
        extraction: extraction as ArtifactBuildExtraction & {
          section: ArtifactTaskPackSection;
          sectionId: string;
        },
      });
    } else if (firstMissingNext === undefined) {
      firstMissingNext = extraction.next;
    }
  }

  const requiredCount = Math.max(1, selected.length);
  let fitted = extracted.length > 0 ? fitArtifactSections(result, extracted) : undefined;
  // B2f: step DOWN one section at a time instead of collapsing straight to one.
  // With three named input axes (T10: rate table + occupation codes +
  // methodology) the all-or-one fallback threw away two small sheets to keep a
  // single large one; the small ones are exactly what fits.
  for (let take = extracted.length - 1; fitted === undefined && take >= 1; take--) {
    fitted = fitArtifactSections(result, extracted.slice(0, take));
  }
  if (fitted === undefined || fitted.length === 0) {
    if (firstMissingNext) result.next = firstMissingNext;
    return { complete: false, truncated: false };
  }

  const attached = extracted.slice(0, fitted.length);
  result.inlined = attached.map(artifactSectionStamp);
  if (fitted.length === 1) {
    result.section = fitted[0]!.section;
    delete result.artifact_sections;
  } else {
    result.artifact_sections = fitted;
    delete result.section;
  }
  const complete = fitted.length >= requiredCount && selected.length >= requiredCount;
  if (complete) {
    result.missing = result.missing.filter((item) => item !== "artifact-section");
    delete result.next;
  } else {
    result.next = firstMissingNext ?? extracted[fitted.length]?.extraction.next ?? result.next;
  }
  return {
    complete,
    truncated: fitted.some(({ section }) => section.truncated),
  };
}

function bundledArtifactPaths(result: TaskPackResult): string[] {
  if (result.artifact_sections) return result.artifact_sections.map(({ path: artifactPath }) => artifactPath);
  if (!result.section) return [];
  const stamped = (result.inlined ?? [])
    .map((stamp) => stamp.match(/^artifact-section:(.+)#/)?.[1])
    .filter((artifactPath): artifactPath is string => artifactPath !== undefined)
    .slice(0, 1);
  if (stamped.length > 0) return stamped;
  return result.surfaces
    .filter(isArtifactTaskPackSurface)
    .map((surface) => surface.path)
    .slice(0, 1);
}

function hasRequiredArtifactSections(result: TaskPackResult, query: string): boolean {
  const bundled = bundledArtifactPaths(result);
  if (bundled.length === 0) return false;
  const sectionsByPath = new Map(
    result.artifact_sections?.map(({ path: artifactPath, section }) => [
      normalizedRequestPath(artifactPath),
      section,
    ] as const) ?? [],
  );
  if (result.section !== undefined && bundled[0] !== undefined) {
    sectionsByPath.set(normalizedRequestPath(bundled[0]), result.section);
  }
  const meaningful = (section: ArtifactTaskPackSection | undefined): boolean => {
    if (section === undefined) return false;
    if ("sheet" in section) return section.columns.length > 0 && section.rows.length > 0;
    if (section.kind === "docx") {
      return section.sections.reduce((total, item) => total + item.text.trim().length, 0) >= 80;
    }
    if (section.kind === "pptx") {
      return section.slides.reduce((total, item) => total + item.text.trim().length, 0) >= 80;
    }
    return section.pages.reduce((total, item) => total + item.text.trim().length, 0) >= 80;
  };
  const exactRequirements = result.artifact_requirements?.map(normalizedRequestPath) ?? [];
  if (exactRequirements.length > 0) {
    return exactRequirements.every((artifactPath) => meaningful(sectionsByPath.get(artifactPath)));
  }
  const required = requestedArtifactKinds(query);
  if (required.length === 0) return true;
  return required.every((kind) =>
    [...sectionsByPath.entries()].some(([artifactPath, section]) =>
      path.extname(artifactPath).toLowerCase().slice(1) === kind && meaningful(section)
    )
  );
}

/** DESIGN-v0.9 §8: one artifact extract + one implementation target, no crawl. */
async function buildArtifactTaskPack(
  args: TaskPackArgs,
  query: string,
  workspace: string,
): Promise<TaskPackResult | undefined> {
  const discovered = discoverArtifactBuildFiles(args, query, workspace);
  if (discovered.length === 0) return undefined;

  const artifacts = discovered
    .slice(0, MAX_SURFACES_DISTINCT)
    .map((file) => mintArtifactSurface(file, workspace, args))
    .filter((artifact): artifact is MintedArtifactSurface => artifact !== undefined);
  if (artifacts.length === 0) return undefined;

  const namedCodeFile = query.match(/\b[A-Za-z_$][A-Za-z0-9_$.-]*\.(?:c|cc|cpp|cxx|go|java|js|jsx|py|rs|ts|tsx)\b/i)?.[0];
  const namedCodeStem = namedCodeFile
    ? path.basename(namedCodeFile, path.extname(namedCodeFile))
    : undefined;
  const targetQuery = args.symbol ?? namedCodeStem ?? moduleShapedQueryTokens(query)[0] ?? query;
  const locatorPath = args.path && !ARTIFACT_EXT_SET.has(path.extname(args.path).toLowerCase())
    ? args.path
    : undefined;
  const locateResult = await locateTaskContext(workspace, {
    action: "locate",
    query: targetQuery,
    ...(locatorPath ? { path: locatorPath } : {}),
    ...(args.symbol ? { symbol: args.symbol } : {}),
    ...(args.lang ? { lang: args.lang } : {}),
    limit: Math.min(args.limit ?? MAX_SURFACES_DISTINCT, MAX_SURFACES_DISTINCT),
  });

  const cache = new FileReadCache();
  const fallbackCandidates: ImpactCandidate[] = !locateResult.hit
    ? (locateResult.candidateDetails ?? []).map((detail) => ({
        path: detail.path,
        line: detail.line,
        range: detail.range,
        ...(detail.symbol ? { symbol: detail.symbol } : {}),
        surface: detail.surface as ImpactSurface,
        why: detail.why,
        confidence: detail.confidence,
        handle: detail.handle,
      }))
    : [];
  const locatedPool: ImpactCandidate[] = locateResult.hit
    ? [...locateResult.primary, ...locateResult.related]
    : fallbackCandidates;
  const exactNamedPath = namedCodeFile
    ? walkCodeFiles(workspace, {}).find((file) =>
        path.basename(file.relPath).toLowerCase() === path.basename(namedCodeFile).toLowerCase()
      )?.relPath
    : undefined;
  const exactNamedContent = exactNamedPath ? readCached(workspace, exactNamedPath, cache) : undefined;
  const exactNamedCandidate: ImpactCandidate | undefined = exactNamedPath && exactNamedContent !== undefined
    ? {
        path: exactNamedPath,
        line: 1,
        range: `1-${countLines(exactNamedContent)}`,
        surface: classifySurface(exactNamedPath),
        why: "filename-match",
        confidence: 1,
        required: true,
      }
    : undefined;
  const candidatePool: ImpactCandidate[] = exactNamedCandidate
    ? [exactNamedCandidate, ...locatedPool.filter((candidate) => candidate.path !== exactNamedCandidate.path)]
    : locatedPool;
  const codeSurfaces: TaskPackSurface[] = [];
  const seenCodePaths = new Set<string>();
  const maxCodeSurfaces = Math.max(
    1,
    Math.min(3, MAX_SURFACES_DISTINCT - artifacts.length),
  );
  for (const candidate of candidatePool) {
    if (codeSurfaces.length >= maxCodeSurfaces) break;
    if (seenCodePaths.has(candidate.path)) continue;
    if (ARTIFACT_EXT_SET.has(path.extname(candidate.path).toLowerCase())) continue;
    const target = await candidateToSurface(
      {
        ...candidate,
        required: codeSurfaces.length === 0,
        confidence: Math.max(candidate.confidence, HIGH_CONFIDENCE_THRESHOLD),
      },
      workspace,
      [],
      query,
      cache,
    );
    if (typeof target.code !== "string" || target.code.length === 0) continue;
    seenCodePaths.add(target.path);
    codeSurfaces.push(target);
  }

  const createTarget = codeSurfaces.length === 0
    ? resolveExplicitArtifactCreateTarget(args, query, workspace)
    : undefined;
  const createNote = codeSurfaces.length === 0
    ? createTarget
      ? `create ${createTarget.path}; the parent directory is uniquely proved by existing package files`
      : buildCreateTargetNote(query, codeSurfaces, workspace)
    : undefined;
  const hasImplementationFrontier = codeSurfaces.length > 0 || createTarget !== undefined;
  const result: TaskPackResult = {
    mode: "task_pack",
    coverage: hasImplementationFrontier ? "focused" : "partial",
    coverage_reason: hasImplementationFrontier ? "single-site" : "missing-roles",
    surfaces: codeSurfaces,
    missing: [
      ...(hasImplementationFrontier ? [] : ["implementation-target"]),
      "artifact-section",
    ],
    route: {
      action: hasImplementationFrontier ? "edit_from_handles" : "locate_missing_surfaces",
      reason: createTarget
        ? "new-file target directory is uniquely proved; fetch bounded artifact sections before creating it"
        : codeSurfaces.length > 0
        ? "implementation target is handle-backed; fetch the bounded artifact section if it is not inlined"
        : "artifact build still needs an implementation target",
      max_additional_tl_calls: 1,
    },
    ...(createNote ? { create_note: createNote } : {}),
    ...(createTarget ? { create_target: createTarget } : {}),
    next: artifacts[0]!.surface.extract,
  };
  (result.surfaces as TaskPackResultSurface[]).push(...artifacts.map((artifact) => artifact.surface));

  const bundle = await attachArtifactBuildSections(result, artifacts, query, args);
  if (bundle.complete && result.route && hasImplementationFrontier) {
    result.route = {
      action: "edit_from_handles",
      reason: bundle.truncated
        ? "bounded artifact previews and implementation frontier are bundled; edit without another discovery call"
        : "artifact sections and implementation frontier are bundled for editing",
      max_additional_tl_calls: 0,
    };
  }

  return dedupeTrimAndPersist(workspace, result, {
    protectedSurfaces: new Set(codeSurfaces),
    args,
    artifactSurfacesAlreadyAttached: true,
  });
}

// ---------------------------------------------------------------------------
// A1: caller-supplied paths[] seeding.
//
// mode=pack already honors paths[]; task_pack silently ignored it, so an
// agent that handed task_pack the exact correct files got 0 of them back
// (the locator ran pathless instead and returned cross-fixture/host noise),
// then escaped to native reads. This builds surfaces DIRECTLY from the
// supplied paths — never dropping one, whatever the byte budget does to its
// embedded code — and runs the locator only to fill required roles the seeds
// did not cover, confined to the seeds' own common ancestor directory so it
// cannot reintroduce the same cross-project noise A1 exists to avoid.
// ---------------------------------------------------------------------------

/** Soft cap on how many seeded surfaces may carry embedded code; the rest go handle+skeleton-only. Mirrors MAX_SURFACES_DISTINCT (the same budget the locate path uses) but does NOT bound the seeded surface COUNT — every supplied path still gets a surface. */
const MAX_SEEDED_CODE_SURFACES = MAX_SURFACES_DISTINCT;

/**
 * Workspace-relative POSIX directory that contains every path in `relPaths`.
 * Returns "" when the paths share no ancestor below the workspace root
 * itself (locator fill is then confined to the whole workspace, i.e.
 * effectively unconfined — this only happens for seeds scattered across
 * unrelated top-level directories, which is rare and safe to leave
 * unscoped rather than wrongly excluding a legitimate top-level file).
 */
function commonAncestorDir(relPaths: string[]): string {
  if (relPaths.length === 0) return "";
  const segLists = relPaths.map((p) => p.split("/").slice(0, -1)); // drop the basename
  let common = segLists[0]!;
  for (const segs of segLists.slice(1)) {
    let i = 0;
    while (i < common.length && i < segs.length && common[i] === segs[i]) i++;
    common = common.slice(0, i);
    if (common.length === 0) break;
  }
  return common.join("/");
}

/** True when `relPath` lies at or under the ancestor directory `dir` (both workspace-relative POSIX). An empty `dir` scopes to the whole workspace (no filtering). */
function isUnderAncestor(relPath: string, dir: string): boolean {
  if (dir === "") return true;
  return relPath === dir || relPath.startsWith(dir + "/");
}

/**
 * True when `relPath` resolves to a DIRECTORY inside `workspace` (safe-path
 * contained). Used by the seeded flow to tell a caller-supplied directory
 * entry (readFileSafe returned null because fs.readFile threw EISDIR) apart
 * from a genuinely missing/out-of-workspace path — the former is a usable
 * confinement scope for a locate, the latter is a bare `missing` entry.
 * Synchronous (matches the surrounding fs style) and never throws.
 */
function isDirectoryWithin(relPath: string, workspace: string): boolean {
  const abs = safeResolve(relPath, workspace);
  if (abs === undefined) return false;
  try {
    const real = fs.realpathSync(abs);
    if (!isWithin(real, resolveReal(workspace))) return false;
    return fs.statSync(real).isDirectory();
  } catch {
    return false;
  }
}

async function buildSeededTaskPack(
  args: TaskPackArgs,
  query: string,
  workspace: string,
): Promise<TaskPackResult> {
  const selectedProfile = bindTaskProfile(args.taskProfile, query).selected;
  const answerProfile = selectedProfile === "answer";
  // FIX-B3: normalize every entry to the canonical {path,...} shape ONCE —
  // downstream code (SeedInfo.entry, toRead, seeds, prioritized) can then
  // keep dereferencing `.path`/`.range`/`.symbol`/`.purpose` exactly as
  // before, regardless of whether the caller supplied bare strings, objects,
  // or a mix of both.
  const entries = args.paths!.map(normalizePathEntry);
  // DESIGN-v0.8 coverage-honesty: the caller-supplied paths ARE the located
  // set, so scope the ui/style requirement to their dominant root.
  // An explicit answer profile is evidence retrieval, not change planning.
  // Do not infer edit-only api/test/style obligations from explanatory prose.
  const requiredRoles = answerProfile
    ? []
    : requiredSurfacesForTask(
        query, workspace, args.surfaceRoles, entries.map((e) => e.path).filter((p) => p.length > 0),
      );
  // Efficiency: one per-pack read/parse memo, seeded below with the seed
  // reads so downstream slice/widen calls never re-read the same file.
  const cache = new FileReadCache();

  // 1. Read every supplied path. Missing/outside-workspace entries are
  //    recorded in `missing` and never fabricated as a surface.
  // Efficiency: the reads run in PARALLEL (Promise.all) rather than awaited
  // one at a time — a paths[] of N files no longer serializes N disk reads.
  // De-dupe and the P4 malformed-entry guard run first (order-preserving),
  // then the survivors are read concurrently.
  type SeedInfo = { entry: TaskPackPathEntry; content: string; role: ImpactSurface };
  const missing: string[] = [];
  // Caller-supplied directories that produced no surface at all. Tracked apart
  // from `missing` because a PATH gap and a ROLE gap need different follow-ups:
  // only a re-seed scoped to the directory can close this one (see the
  // unexpandedDirNext hint below).
  const unresolvedDirs: string[] = [];
  const seededArtifacts: MintedArtifactSurface[] = [];
  const seenPaths = new Set<string>();
  const seenEntries = new Set<string>();
  const toRead: TaskPackPathEntry[] = [];
  for (const entry of entries) {
    const relPath = entry.path;
    // P4: a malformed paths[] entry (missing/empty path) must SURFACE in
    // `missing` so the caller sees the entry was dropped — silently
    // `continue`ing left the agent believing a supplied path was honored.
    if (relPath.length === 0) {
      missing.push("(invalid paths[] entry: missing path)");
      continue;
    }
    // Distinct symbols/ranges in one file are distinct caller requirements.
    // De-duping only by path silently discarded every entry after the first
    // and could certify a pack without serving the requested implementation
    // functions. Read the file once below, but preserve each distinct slice.
    const entryKey = [relPath, entry.range ?? "", entry.symbol ?? ""].join("\u0000");
    if (seenEntries.has(entryKey)) continue;
    seenEntries.add(entryKey);
    seenPaths.add(relPath);
    if (ARTIFACT_EXT_SET.has(path.extname(relPath).toLowerCase())) {
      if (seededArtifacts.some((item) => item.surface.path === relPath)) continue;
      const found = discoverArtifactFiles(workspace, { path: relPath, includeAll: true });
      const exact = found.find((file) => file.relPath === relPath);
      const minted = exact ? mintArtifactSurface(exact, workspace, args) : undefined;
      if (minted) seededArtifacts.push(minted);
      else missing.push(relPath);
      continue;
    }
    toRead.push(entry);
  }
  const uniqueReadPaths = [...new Set(toRead.map((entry) => entry.path))];
  const contentByPath = new Map(await Promise.all(
    uniqueReadPaths.map(async (relPath) => [relPath, await readFileSafe(relPath, workspace)] as const),
  ));
  const readResults = toRead.map((entry) => ({ entry, content: contentByPath.get(entry.path) ?? null }));
  const seeds: SeedInfo[] = [];
  // Behavior 2: caller-supplied paths[] entries that resolve to a DIRECTORY
  // (readFileSafe null → in-workspace directory) are not surfaces themselves —
  // they are a confinement scope. Held here and resolved via a directory-
  // confined locate below (fill candidates), so a supplied dir stops being a
  // bare `missing` entry with an empty pack.
  const dirEntries: TaskPackPathEntry[] = [];
  for (const { entry, content } of readResults) {
    if (content === null) {
      if (isDirectoryWithin(entry.path, workspace)) {
        dirEntries.push(entry);
      } else {
        missing.push(entry.path);
      }
      continue;
    }
    cache.seed(entry.path, content);
    seeds.push({ entry, content, role: classifySurface(entry.path, entry.symbol) });
  }

  // 2. Priority order for the embed budget: required-role seeds first (item
  //    3b — "required-role seeded surfaces embed first"), then the rest in
  //    supplied order. Both groups still get a surface either way; this only
  //    decides which ones get embedded `code` vs handle+skeleton-only.
  const requiredSet = new Set(requiredRoles);
  const prioritized = [...seeds].sort((a, b) => {
    const aReq = requiredSet.has(a.role) ? 0 : 1;
    const bReq = requiredSet.has(b.role) ? 0 : 1;
    if (aReq !== bReq) return aReq - bReq;
    return seeds.indexOf(a) - seeds.indexOf(b); // stable: preserve supplied order within each group
  });

  // 3. Build ImpactCandidates and convert via the SAME candidateToSurface
  //    used by the locate path, so handle-minting, the embed gate, and
  //    likely_edits stay identical. Candidates within the first
  //    MAX_SEEDED_CODE_SURFACES get a high confidence (clears the
  //    candidateToSurface embed gate); the rest get a low confidence so they
  //    land as handle+skeleton-only (widen via `read_code mode=slice
  //    handle=<handle>`) — sliceCode's own per-surface byte cap (and
  //    trimToCap's byte cap below) still bound how much code actually
  //    survives even within the "wants embed" group.
  const seededSurfaces: TaskPackSurface[] = await Promise.all(
    prioritized.map(async (seed, idx) => {
      // BUG FIX: was raw.split(/\r?\n/).length, which counts a phantom final
      // segment for trailing-newline content and mints a whole-file range
      // one line past the end (see util/countLines.ts doc comment).
      const lineCount = countLines(seed.content);
      let range = seed.entry.range ?? (seed.entry.symbol ? undefined : `1-${Math.max(1, lineCount)}`);
      let symbol = seed.entry.symbol;
      let focusedLine = 1;
      // 11a parity (2026-07-30): a caller-EXPLICIT range is a zoom window —
      // anchor the embed at its start so a remaining_ranges continuation
      // serves the NEXT chunk. A midpoint-centered window re-serves bytes on
      // both sides and the follow-up's remaining_ranges never advance.
      const explicitSeedStart = seed.entry.range === undefined
        ? undefined
        : /^(\d+)-\d+$/.exec(seed.entry.range)?.[1];
      if (explicitSeedStart !== undefined) focusedLine = parseInt(explicitSeedStart, 10);
      const identifierContext =
        seed.role === "test"
        && seed.entry.range === undefined
        && seed.entry.symbol === undefined
          ? queryIdentifierContext(seed.content, query)
          : undefined;
      if (identifierContext !== undefined) {
        range = identifierContext.range;
        focusedLine = identifierContext.line;
      }
      // Anchor-focus embedding (gap: transcript-verified in run 2026-07-10b;
      // selectAnchorFocus + friends are defined just below this
      // function). A caller-named FILE seed with no explicit range/symbol
      // defaults to the whole-file range above, but candidateToSurface only
      // embeds `code` when the slice fits MAX_SURFACE_CODE_BYTES — a file
      // over that cap silently ships a code-less pack even though `query`
      // often names exactly what it needs. When the caller gave neither a
      // range nor a symbol, supplied a non-empty query, and this seed's
      // content overflows the embed cap, re-point the seed at its single
      // best query-matching symbol instead of the whole file. Never a
      // guess: selectAnchorFocus returns undefined (today's whole-file
      // behavior, unchanged) when nothing scores above zero overlap.
      let anchorFocus: AnchorFocusResult | undefined;
      if (
        identifierContext === undefined &&
        seed.entry.range === undefined &&
        seed.entry.symbol === undefined &&
        query.trim().length > 0 &&
        Buffer.byteLength(seed.content, "utf8") > MAX_SURFACE_CODE_BYTES
      ) {
      anchorFocus = await selectAnchorFocus(seed.content, seed.entry.path, query, cache);
        if (anchorFocus) {
          range = anchorFocus.best.range;
          symbol = anchorFocus.best.name;
        }
      }
      const wantsEmbed = idx < MAX_SEEDED_CODE_SURFACES;
      const candidate: ImpactCandidate = {
        path: seed.entry.path,
        line: focusedLine,
        ...(range ? { range } : {}),
        ...(seed.entry.range !== undefined ? { callerRange: true } : {}),
        ...(symbol ? { symbol } : {}),
        surface: seed.role,
        why: seed.entry.purpose ?? (identifierContext ? "query-identifier-test-match" : "caller-supplied"),
        confidence: wantsEmbed ? 0.85 : 0.3,
        required: requiredSet.has(seed.role),
      };
      const surface = await candidateToSurface(
        candidate, workspace, requiredRoles, query, cache, { answerProfile },
      );
      // Transparency + a ready-to-run follow-up (see
      // attachAnchorFocusAffordance — factored out verbatim so the
      // fill/dir locator-candidate paths attach the identical affordance).
      if (anchorFocus) attachAnchorFocusAffordance(surface, anchorFocus, seed.entry.path, workspace);
      return surface;
    }),
  );
  // Snapshot the surfaces built DIRECTLY from caller-supplied paths (object
  // identity, not array position) before augmentation/locator-fill append
  // more surfaces — augmentNativeSurfaceClosure splices new sibling surfaces
  // into the middle of this array, so a position-based prefix would no
  // longer correspond to "the seeded ones" afterward.
  const protectedSurfaces = new Set(seededSurfaces);
  // Native header/source pairing (e.g. a supplied .cpp without its .hpp)
  // reuses the existing closure augmenter — same as the locate path. Its
  // spliced-in siblings are NOT in `protectedSurfaces` (they were not
  // caller-supplied) and stay droppable under trimToCap.
  if (!answerProfile) {
    await augmentNativeSurfaceClosure(seededSurfaces, workspace, query, requiredRoles, cache);
    await augmentNativeWiringSurface(seededSurfaces, workspace, cache);
    await augmentNativeIdentifierCooccurrenceSurface(seededSurfaces, workspace, query, requiredRoles, cache);
  }

  // 4. Locator fill for required roles the seeds did not cover, confined to
  //    the seeds' own common ancestor directory (never the whole workspace)
  //    so a required-role gap cannot reintroduce cross-project noise. The
  //    locator commonly ABSTAINS ("ambiguous") on a short synthetic/narrow
  //    scope rather than returning hit:true — same as the top-level
  //    buildTaskPack flow, candidateDetails on an abstain are still usable
  //    fill candidates, so both branches are folded into one candidate list
  //    here rather than only handling the hit:true case.
  const coveredRoles = new Set(seededSurfaces.map((s) => s.role));
  const stillMissingRequired = requiredRoles.filter((r) => !coveredRoles.has(r));
  // Ancestor is computed from FOUND paths only (`seeds`, not `seenPaths`) —
  // a typo'd/nonexistent supplied path must not drag the confinement
  // boundary out to a broader, less-precise ancestor than the real seeds
  // actually share. Behavior 2: caller-supplied DIRECTORY entries are scope
  // evidence too — each participates as the directory ITSELF (a synthetic
  // "<dir>/." child makes commonAncestorDir's basename-drop yield the dir),
  // so a dir-only paths[] confines the required-role fill to that dir instead
  // of degenerating to "" (whole-workspace) because `seeds` is empty.
  const ancestor = commonAncestorDir([
    ...seeds.map((s) => s.entry.path),
    ...dirEntries.map((d) => `${d.path}/.`),
  ]);

  // IMPROVEMENT F: when a task QUERY is present and the caller volunteered FILE
  // paths[] (not a directory scope), those paths are ADDITIVE SEEDS and the
  // relatedness-gated CONCERN-GROUP closure (augmentConcernGroupSurfaces below)
  // must run under the SAME budgets as the query-only (pathless) flow — never
  // confined to the seeds' own common-ancestor subtree. The 2026-07-12d
  // `ancestor` confinement made volunteering MORE (clustered) paths SHRINK the
  // discovered closure below what the identical task found with ONE path (a
  // query-related concern surface OUTSIDE the seeds' subtree was dropped).
  //
  // Confinement is LIFTED ONLY for augmentConcernGroupSurfaces: it locates a
  // real, query-matched surface PER concern group, so unconfining it can only
  // add GENUINELY-related closure, never fabricate an unrelated file. The
  // required-role FILL (step-4 above) and augmentMissingRequiredRoleSurfaces
  // (below) deliberately KEEP their `ancestor` confinement — those fill by
  // structural role hint and can pull in a textually-unrelated sibling, which is
  // exactly the false-coverage inflation the 2026-07-12d confinement closed.
  // A caller-supplied DIRECTORY keeps confinement (explicit "look only here"),
  // and a pure path-fetch with NO query stays confined (focused). "" = unconfined.
  const concernClosureScope: string =
    query.trim().length > 0 && seeds.length > 0 && dirEntries.length === 0
      ? ""
      : ancestor;
  // B1a (2026-08-01 retrieval-scope): the caller's OWN territory. `ancestor`
  // collapses it to one directory (which for paths spanning several project
  // trees is their shared parent — i.e. permission to search every sibling
  // tree); these keep the individual anchors so roleSearchScope can narrow to
  // the ancestor-children that actually hold one. Directory entries contribute
  // as the directory itself (the synthetic "/." child makes commonAncestorDir's
  // basename-drop yield the dir, same trick `ancestor` above uses).
  const callerAnchorPaths: string[] = [
    ...seeds.map((s) => s.entry.path),
    ...dirEntries.map((d) => `${d.path}/.`),
  ];
  // Confinement for a directory the QUERY names: the caller's own family root.
  // (Their individual parent directories would be too tight — a query naming
  // "packages/shared" must still resolve when the caller seeded
  // "packages/shared/src/enums.ts".)
  const callerScopePaths: string[] = ancestor !== "" ? [ancestor] : [];

  if (stillMissingRequired.length > 0 && query.trim().length > 0) {
    const fillResult = await locateTaskContext(workspace, {
      action: "locate",
      query,
      ...(ancestor ? { path: ancestor } : {}),
      limit: MAX_SURFACES_DISTINCT,
    });
    const fillRaw: ImpactCandidate[] = fillResult.hit
      ? [...fillResult.primary, ...fillResult.related]
      : (fillResult.candidateDetails ?? []).map((d) => ({
          path: d.path,
          line: d.line,
          range: d.range,
          ...(d.symbol ? { symbol: d.symbol } : {}),
          surface: d.surface,
          why: d.why,
          confidence: d.confidence,
        }));
    // Defensive re-confinement: locateTaskContext's own hard-scope filter
    // already restricts to `ancestor` when set, but this stays explicit and
    // self-contained rather than relying solely on that internal filter, and
    // also excludes anything already seeded. Sort best-first (confidence
    // desc) so the fill loop below picks the strongest candidate per role.
    const fillCandidates = fillRaw
      .filter((c) => isUnderAncestor(c.path, ancestor) && !seenPaths.has(c.path))
      .filter((c) => stillMissingRequired.includes(c.surface))
      .sort((a, b) => b.confidence - a.confidence);
    // Fill exactly one surface per still-missing required role — shrinks as
    // roles get covered so two same-role candidates (e.g. types.ts AND
    // schema.ts both classifying "contract") do not both get added.
    const rolesToFill = new Set(stillMissingRequired);
    for (const c of fillCandidates) {
      if (!rolesToFill.has(c.surface)) continue;
      rolesToFill.delete(c.surface);
      // 2026-07-10c anchor-focus reach: a fill candidate on a >4KB file with
      // no symbol pin gets re-pointed at its best query-matching symbol so
      // the filled surface embeds that body (instead of a code-less window
      // whose widened slice overflows the cap) — see
      // anchorFocusForLocatorCandidate. Zero overlap keeps `c` as-is.
      const anchored = await anchorFocusForLocatorCandidate(c, workspace, query, cache);
      const surface = await candidateToSurface(
        anchored?.candidate ?? c, workspace, requiredRoles, query, cache, { answerProfile },
      );
      if (anchored) attachAnchorFocusAffordance(surface, anchored.focus, c.path, workspace);
      seededSurfaces.push(surface);
      coveredRoles.add(c.surface);
    }
  }

  // Behavior 2: resolve caller-supplied DIRECTORY entries via a locate confined
  // to each directory. A supplied dir is a "look here" scope, not a surface —
  // fold whatever it yields in as fill candidates (same shape/discipline as the
  // required-role fill above: MAX_SURFACES_DISTINCT-bounded, one surface per
  // role, never a seed already covered). If the confined locate yields nothing
  // (or there is no query to locate with), the directory becomes a clearer
  // `missing` marker telling the caller HOW to make it usable — never a bare
  // path that reads as "file not found".
  for (const dirEntry of dirEntries) {
    const dir = dirEntry.path;
    let foldedAny = false;
    if (query.trim().length > 0) {
      const dirResult = await locateTaskContext(workspace, {
        action: "locate",
        query,
        path: dir,
        limit: MAX_SURFACES_DISTINCT,
      });
      const dirRaw: ImpactCandidate[] = dirResult.hit
        ? [...dirResult.primary, ...dirResult.related]
        : (dirResult.candidateDetails ?? []).map((d) => ({
            path: d.path,
            line: d.line,
            range: d.range,
            ...(d.symbol ? { symbol: d.symbol } : {}),
            surface: d.surface,
            why: d.why,
            confidence: d.confidence,
          }));
      const dirFiles = new Set(walkCodeFiles(workspace, { subPath: dir }).map((file) => file.relPath));
      const explicitCodeIdentities = explicitCodeIdentifiers(query);
      const delimitedCodeIdentities = delimitedExplicitCodeIdentifiers(query);
      const singularExplicitCodeIdentity = delimitedCodeIdentities.length === 1
        && !isMultiSurfaceQuery(query);
      const explicitCodeIdentityKeys = new Set(delimitedCodeIdentities.map(normIdent));
      const rawShapeIdentifiers = (query.match(/\b[A-Za-z_$][A-Za-z0-9_$-]*\b/g) ?? [])
        .filter(isWiringIdentifierShape);
      const explicitDirIdentifiers = [
        ...explicitCodeIdentities,
        ...rawShapeIdentifiers,
      ].filter((identifier, index, all) =>
        all.findIndex((candidate) => normIdent(candidate) === normIdent(identifier)) === index
      );
      for (const identifier of explicitDirIdentifiers.slice(0, 6)) {
        const codeIdentity = explicitCodeIdentityKeys.has(normIdent(identifier));
        const exactMatches = scanLiteral(identifier, workspace, {
          caseInsensitive: false,
          onlyFiles: dirFiles,
        }).filter((match) => hasIdentifierSegment(match.text ?? "", identifier));
        exactMatches.sort((left, right) =>
          definitionVsDerivedAdjustment(workspace, right.path, right.line, cache)
          - definitionVsDerivedAdjustment(workspace, left.path, left.line, cache)
          || Number(isImplementationPath(right.path)) - Number(isImplementationPath(left.path))
          || left.path.localeCompare(right.path)
          || left.line - right.line
        );
        if (codeIdentity) {
          const definitions: ImpactCandidate[] = [];
          for (const relPath of [...new Set(exactMatches.map((match) => match.path))]) {
            const content = cache.read(workspace, relPath);
            if (content === undefined) continue;
            const symbols = await extractSymbolsFromFile(content, relPath, 256, [identifier]);
            for (const symbol of symbols) {
              if (normIdent(symbol.name) !== normIdent(identifier)) continue;
              const classified = classifySurface(relPath, symbol.name);
              definitions.push({
                path: relPath,
                line: symbol.line,
                range: symbol.range,
                symbol: symbol.name,
                surface: singularExplicitCodeIdentity
                  && classified === "unknown"
                  && !isTestConcernPath(relPath)
                    ? "domain"
                    : classified,
                why: "explicit-query-identifier",
                confidence: 1,
                ...(singularExplicitCodeIdentity ? { required: true } : {}),
              });
            }
          }
          const nonTestDefinitions = definitions.filter((candidate) => !isTestConcernPath(candidate.path));
          const preferredDefinitions = nonTestDefinitions.length > 0 ? nonTestDefinitions : definitions;
          preferredDefinitions.sort((left, right) =>
            Number(isImplementationPath(right.path)) - Number(isImplementationPath(left.path))
            || left.path.localeCompare(right.path)
            || left.line - right.line
          );
          if (preferredDefinitions.length > 0) {
            for (const definition of preferredDefinitions.slice(0, MAX_SURFACES_DISTINCT)) {
              const existingIndex = dirRaw.findIndex((candidate) => candidate.path === definition.path);
              if (existingIndex >= 0) dirRaw[existingIndex] = definition;
              else dirRaw.push(definition);
            }
            continue;
          }
        }
        const exact = exactMatches[0];
        if (exact === undefined) continue;
        const literalCandidate: ImpactCandidate = {
          path: exact.path,
          line: exact.line,
          range: `${exact.line}-${exact.line}`,
          surface: classifySurface(exact.path),
          why: "explicit-query-identifier",
          confidence: 1,
        };
        const existingIndex = dirRaw.findIndex((candidate) => candidate.path === exact.path);
        if (existingIndex >= 0) dirRaw[existingIndex] = literalCandidate;
        else dirRaw.push(literalCandidate);
      }
      // Confine to the supplied dir (defense-in-depth beyond locate's own
      // hard-scope filter), drop anything already seeded/covered, best-first.
      // W5-B (2026-08-21, R3 text-floor regression fix): also drop a
      // "header-source-pair" candidate here — a DERIVED pairing (added
      // because a SIBLING file matched, not because this file itself was a
      // query hit), which this confined loop's own role-fill treats as an
      // ordinary direct hit. The dedicated, properly scope-confined pairing
      // step (augmentNativeSurfaceClosure, later in the pipeline) already
      // owns header/source pairing FOR WHATEVER SURFACE SET SURVIVES here —
      // letting a bare header win a role slot in THIS loop let a
      // declaration-only surface masquerade as "the query's own hit" for a
      // role no real hit claimed, which downstream concern-coverage checks
      // (concernGroupMatchesSurfaces) then read as the concern already
      // being satisfied on nothing but an incidental path-substring tie.
      const dirCandidates = dirRaw
        .filter((c) => isUnderAncestor(c.path, dir) && !seenPaths.has(c.path) && c.why !== "header-source-pair")
        .sort((a, b) =>
          b.confidence - a.confidence
          || Number(b.why === "explicit-query-identifier") - Number(a.why === "explicit-query-identifier")
          // W5-B: an IMPLEMENTATION file wins a same-confidence tie over a
          // declaration/header — a short (post-R3, 4-7 char) discriminating
          // token now commonly matches a symbol's declaration and its
          // definition equally, which previously only a longer/rarer token
          // did (and always favored whichever body used it more precisely,
          // rarely a bare tie). Mirrors the same isImplementationPath
          // preference this file already applies to explicit-identifier
          // definition ranking above (preferredDefinitions.sort).
          || Number(isImplementationPath(b.path)) - Number(isImplementationPath(a.path))
          || a.path.localeCompare(b.path)
        );
      // Role-fill discipline: one surface per role not already covered, and
      // never exceed the overall surface budget.
      const dirRolesFilled = new Set<ImpactSurface>();
      for (const c of dirCandidates) {
        if (seededSurfaces.length >= MAX_SURFACES_DISTINCT) break;
        // 2026-07-10c anchor-focus reach: same re-point as the required-role
        // fill loop above — real packs open with paths=["<dir>"]+query, so
        // THIS loop is where a >4KB file's candidate must consult the query
        // for symbol selection. Zero overlap keeps today's behavior.
        const anchored = await anchorFocusForLocatorCandidate(c, workspace, query, cache);
        const explicitAnchor = (anchored?.focus.best.score ?? 0) >= ANCHOR_FOCUS_EXPLICIT_IDENTIFIER_WEIGHT;
        const explicitIdentity = singularExplicitCodeIdentity
          && c.why === "explicit-query-identifier"
          && c.symbol !== undefined;
        if (
          (coveredRoles.has(c.surface) || dirRolesFilled.has(c.surface))
          && !explicitAnchor
          && !explicitIdentity
        ) continue;
        dirRolesFilled.add(c.surface);
        seenPaths.add(c.path);
        const selectedCandidate = anchored?.candidate ?? c;
        const surface = await candidateToSurface(
          {
            ...selectedCandidate,
            why: explicitIdentity ? "explicit-query-identifier" : "caller-supplied-dir",
          },
          workspace,
          requiredRoles,
          query,
          cache,
          { answerProfile },
        );
        if (anchored) attachAnchorFocusAffordance(surface, anchored.focus, c.path, workspace);
        seededSurfaces.push(surface);
        coveredRoles.add(c.surface);
        foldedAny = true;
      }
    }
    // The marker means "this directory contributed NOTHING to the pack". The
    // required-role fill above is also ancestor-confined (the supplied dirs
    // participate in the ancestor), so it may already have surfaced files from
    // INSIDE this dir before this loop ran — that dir has been honored, and
    // re-flagging it as unusable would be wrong. Emit the marker only when no
    // surface at all lies under the dir.
    const dirRepresented =
      foldedAny ||
      seededSurfaces.some((s) => isUnderAncestor(s.path, dir)) ||
      discoverArtifactFiles(workspace, { path: dir, includeAll: true }).length > 0;
    if (!dirRepresented) {
      // Short marker, not a remedy sentence. `missing` is consumed downstream
      // as a list of surface ROLE names, and the old prose rode that path into
      // `surfaceRoles=["<dir>/ (directory — pass cwd=…)"]` — a filter no pack
      // can ever satisfy, so the caller looped. The trailing "/ (directory)"
      // still separates an unusable DIRECTORY from a not-found file (a bare
      // path reads as the latter), while the remedy itself now ships as a
      // machine-executable call in `next`/`next_call` — see unexpandedDirNext.
      missing.push(`${dir}/ (directory)`);
      unresolvedDirs.push(dir);
    }
  }

  // A caller-spelled code identity is stronger than a prose/token candidate
  // found in the same directory. Keep supporting surfaces, but make the one
  // uniquely matching definition the primary edit frontier. Without this
  // reorder a directory locator's earlier same-role decoy owns surfaces[0],
  // and the otherwise exact implementation is presented as a secondary
  // candidate, which reopens discovery.
  const explicitIdentityPrimary = dirEntries.length > 0
    && delimitedExplicitCodeIdentifiers(query).length === 1
    && !isMultiSurfaceQuery(query)
    ? uniqueExplicitIdentityCandidate(seededSurfaces, query)
    : undefined;
  if (explicitIdentityPrimary !== undefined) {
    const primary = explicitIdentityPrimary.candidate;
    primary.required = true;
    for (const surface of seededSurfaces) {
      if (surface !== primary && surface.required !== true) surface.required = false;
    }
    const primaryIndex = seededSurfaces.indexOf(primary);
    if (primaryIndex > 0) {
      seededSurfaces.splice(primaryIndex, 1);
      seededSurfaces.unshift(primary);
    }
  }

  // Registration-reach fix (2026-07-12c forensics): a caller-supplied
  // DIRECTORY only becomes concrete surfaces once the dirEntries loop above
  // runs — for a pure-dir pack `seededSurfaces` is still EMPTY at the top of
  // this function, so augmentConcernGroupSurfaces/augmentMissingRequiredRoleSurfaces
  // must sit HERE (after seed assembly genuinely completes for a dir-scoped
  // pack), not alongside the native-closure/wiring/cooccurrence trio above
  // (which already runs before this point and is therefore a structural
  // no-op for a pure-dir pack — those three are pre-existing/out of scope
  // for this fix; the required-role-fill loop and this pair are what a
  // dir-seeded pack actually gets a second chance from). Same six-function
  // pipeline the pathless flow (buildTaskPack) and the abstain-candidate
  // flow (buildPartialPack) already run, reused verbatim — only the call
  // SITE moved to match this flow's own seed-assembly order.
  //
  // Reuses the SAME `protectedSurfaces` set already seeded with the
  // caller-named files above (empty for a pure-dir pack): concern-group
  // additions are appended (never reordered ahead of what is already
  // present) and become protected themselves, exactly mirroring the
  // pathless flow's own behavior — trimToCap's Phase 1 still sheds
  // unprotected surfaces from the END first, so anything this pass adds to
  // a FILE-seeded pack is shed before the caller's own named files, and for
  // a pure-dir pack (nothing protected at all) it is still the newest,
  // last-appended content and so the first to go.
  //
  // 2026-07-12d confinement fix (accepted-invariant-breach repair): both
  // calls below now receive `ancestor` — the caller-supplied entries' own
  // common ancestor directory computed above, empty string when the seeds
  // share no ancestor (both augmenters already treat "" as "no additional
  // restriction" via isUnderAncestor). Before this fix the two calls were
  // deliberately UNCONFINED (no scope/ancestor parameter at all), so a
  // concern-group or required-role candidate found ANYWHERE in the
  // workspace could get appended to a caller-SEEDED pack and — since
  // coverage is recomputed from the post-augmentation surface set below —
  // silently promote coverage from partial to complete on material the
  // caller never named. Passing `ancestor` closes that gap: a candidate
  // outside the caller's own named territory is dropped by the augmenter's
  // own defensive isUnderAncestor filter, never added, never
  // coverage-bearing. The other two call sites (buildTaskPack's pathless
  // flow, buildPartialPack's abstain-candidate flow) still call both
  // functions with no scope argument — there is no caller-named scope to
  // respect there, so they keep today's unconfined behavior unchanged.
  if (!answerProfile) {
    await augmentConcernGroupSurfaces(seededSurfaces, query, workspace, requiredRoles, cache, protectedSurfaces, concernClosureScope, callerAnchorPaths);

    // Item 1: resolve a REQUIRED "style" role the seeds/fill did not cover to a
    // NAMED surface (handle + code), scoped to the seeds' dominant root.
    // B1a (2026-08-01 retrieval-scope): the CALLER's own paths are the anchors —
    // a role fill on an explicit-paths pack must stay inside the territory the
    // caller named (and any directory the query itself named), never spill into
    // a sibling project tree that shares only a distant ancestor.
    await augmentStyleSurface(seededSurfaces, query, workspace, requiredRoles, cache, callerAnchorPaths, callerScopePaths);
    // FIX-B2 (ported alongside augmentConcernGroupSurfaces above, same
    // rationale): attempt every OTHER still-missing required role in this SAME
    // call — see augmentMissingRequiredRoleSurfaces's own doc comment. Runs
    // LAST of the six, matching its role as the final catch-all in both other
    // flows: it recomputes stillMissingRequiredRoles fresh, so it only ever
    // acts on roles that survive the required-role-fill loop, the dirEntries
    // loop, and augmentStyleSurface above. Also confined to `ancestor` (same
    // 2026-07-12d fix as augmentConcernGroupSurfaces above).
    await augmentMissingRequiredRoleSurfaces(seededSurfaces, query, workspace, requiredRoles, cache, ancestor, callerAnchorPaths);
    // DESIGN-v0.8 coverage-honesty (Part 5 B-1): promote a config/data/doc seed
    // whose already-read content already names the enum's new value.
    promoteVariantSurfaces(seededSurfaces, query, workspace, cache);
  }

  // 2026-07-10a forensics: a PURE-directory pack (no caller-named files — every
  // surface was the server's own pick) put the decoy's TEST file in the
  // primary slot, so `next` said "edit_file handle=<the test>". A test/doc
  // surface must not headline a pack the caller gave no file for when an
  // implementation-role surface exists — the primary slot drives next +
  // likely_edits.
  const pureDirScope = seeds.length === 0 && dirEntries.length > 0;
  if (pureDirScope && seededSurfaces.length >= 2) {
    const first = seededSurfaces[0]!;
    if (first.role === "test" || first.role === "doc") {
      const implIdx = seededSurfaces.findIndex(
        (s) => s.role === "domain" || s.role === "api" || s.role === "config" || s.role === "contract",
      );
      if (implIdx > 0) {
        const [impl] = seededSurfaces.splice(implIdx, 1);
        seededSurfaces.unshift(impl!);
      }
    }
  }
  finalizePrimarySurface(seededSurfaces);

  // 5. Route/checks/verify/coverage, same shape as the locate path.
  // Derive from the ACTUAL surface set (not the running `coveredRoles` tally)
  // so a role added late by augmentStyleSurface (item 1) counts as covered.
  const missingRequired = requiredRoles.filter((r) => !seededSurfaces.some((s) => s.role === r));
  const allMissing = mergeUnique(missing, missingRequired);
  // DESIGN-v0.8 coverage-honesty: a seeded pack is complete when it covers
  // everything asked for; otherwise a required role (or a nonexistent supplied
  // path) is missing — "missing-roles".
  let coverageStr: "complete" | "partial" = allMissing.length === 0 ? "complete" : "partial";
  let coverageReason: TaskPackResult["coverage_reason"] | undefined =
    coverageStr === "partial" ? "missing-roles" : undefined;
  // 2026-07-11c false-"complete" (dir-seeded concern gap): a
  // caller-supplied DIRECTORY (dirEntries.length>0 — whether or not file
  // seeds are ALSO present) is a "look here" scope, not an earned file list
  // — its surfaces are locator picks, same trust level as a pathless
  // locate. Run 2026-07-11c's live pack claimed complete on a 3-symptom
  // query while surfacing an off-target header, an unrelated boilerplate
  // startup file, and two test slices — none of the planted-bug files — and
  // the agent trusted "complete", stopped searching, and edited a decoy.
  // Reuses the IDENTICAL unmatchedConcernTokens the pathless locate path
  // already gates on (deriveCoverage rule 2 / buildRoute's own A4 flip),
  // computed over the FINAL surface set (after augmentStyleSurface/
  // promoteVariantSurfaces/reordering) so late augmenters are accounted
  // for. Packs seeded ONLY by caller-NAMED files (dirEntries.length===0)
  // keep the pre-11c earned-trust rule unchanged — naming an exact file is
  // a stronger signal than a directory scope, and this gate never
  // second-guesses it.
  let unmatchedConcern: string[] = [];
  if (dirEntries.length > 0 && query.trim().length > 0) {
    unmatchedConcern = unmatchedConcernTokens(query, seededSurfaces);
    if (coverageStr === "complete" && unmatchedConcern.length > 0) {
      coverageStr = "partial";
      coverageReason = "concerns-uncovered";
    }
  }
  // 2026-07-10a pure-dir false-"complete": the seeded complete rule ("caller's
  // paths ARE the located set") is earned by caller-NAMED files. A pure-dir
  // pack's surfaces are locator picks — same trust level as a pathless
  // locate — and that run's pack claimed complete while embedding only a
  // test, two headers and a doc (zero implementation bodies), steering the
  // agent to the decoy. Claiming complete now additionally requires at least
  // one embedded implementation-body surface. 10c tightening: the first cut
  // ("role not test/doc, not a C/C++ header") let a ONE-LINE __init__.py
  // stub (role "unknown") count as a body and re-opened the false-complete
  // live — the role must now be a recognized implementation role and the
  // embedded code non-trivial. 11c tightening: role+size alone was STILL not
  // enough — an implementation-role boilerplate file with ZERO relation to
  // the query (startup.c, sharing no token with the 11c query) also passed
  // this test, so the qualifying surface must now also be GROUNDED in the
  // query (surfaceGroundedInQuery) whenever the query is non-blank; a blank
  // query keeps the pre-11c ungrounded behavior (nothing to ground against).
  const IMPLEMENTATION_BODY_ROLES: ReadonlySet<string> = new Set(["domain", "api", "config", "contract"]);
  let dirPackDemotionCheck: string | undefined;
  if (pureDirScope && coverageStr === "complete") {
    const queryGiven = query.trim().length > 0;
    // The surfaces' OWN deepest common ancestor directory is stripped before
    // grounding (see surfaceGroundedInQuery's doc comment) — every surface in
    // a pureDirScope pack shares at least the caller-supplied directory, and
    // often a deeper shared subtree too (e.g. every firmware file living
    // under ".../firmware/"). Left unstripped, a query that simply names the
    // project/directory ("acme firmware...") would trivially ground every
    // surface regardless of actual relevance.
    const groundingScope = commonAncestorDir(seededSurfaces.map((s) => s.path));
    const scopePrefixes = groundingScope.length > 0 ? [groundingScope] : [];
    const hasImplementationBody = seededSurfaces.some(
      (s) =>
        s.code !== undefined &&
        countLines(s.code) > 3 &&
        IMPLEMENTATION_BODY_ROLES.has(s.role) &&
        !/\.(h|hpp|hh|hxx)$/i.test(s.path) &&
        (!queryGiven || surfaceGroundedInQuery(s, query, scopePrefixes)),
    );
    if (!hasImplementationBody) {
      coverageStr = "partial";
      coverageReason = "candidate-list";
      // 11c: with grounding in play the pack may well EMBED an implementation
      // body (the irrelevant startup.c did) — the honest complaint is that no
      // embedded body relates to the query.
      dirPackDemotionCheck = queryGiven
        ? "dir-scoped pack embeds no query-grounded implementation body — slice the implicated source before editing"
        : "dir-scoped pack embeds no implementation body — slice the implicated source before editing";
    }
  }
  const route = buildRoute(seededSurfaces, missingRequired, coverageStr, allMissing, query, coverageReason);
  // 2026-07-31: a caller-supplied directory that produced no surface is a PATH
  // gap, and every hint nextHintForCoverage can emit is role- or token-shaped,
  // so none of them close it. Name the call that does — re-seed the pack scoped
  // to that directory — as the last-resort hint. Ranked BELOW the role/token
  // hints: a genuinely missing required role is the better lead when both hold.
  // JSON.stringify per item, not bare interpolation: these are caller-supplied
  // paths, and a path may legally contain a double quote. Raw interpolation made
  // parseQuotedList stop at it and hand back a TRUNCATED path that can itself
  // exist — a wrong-file read that looks like a success. (Role and handle lists
  // nearby are safe: neither alphabet admits a quote.)
  // D1 (2026-08-01 probe sweep): the old remedy re-issued THIS call (same
  // query + same dirs) — for a caller whose invocation WAS query+those dirs
  // that is an identity next_call, and an obedient agent loops on it. A dir
  // that contributed nothing needs an INVENTORY step, not a re-scan: name the
  // tree call for the first unresolved dir instead.
  const unexpandedDirNext = unresolvedDirs.length > 0
    ? `search_files action=tree path=${unresolvedDirs[0]}`
    : undefined;
  const nextHint = nextHintForCoverage(
    seededSurfaces, coverageStr, coverageReason, query, missingRequired, unmatchedConcern,
  ) ?? unexpandedDirNext;

  const { lines: checkLines, records: checkRecords } = buildCompletionChecks(query, seededSurfaces, allMissing, workspace);
  if (dirPackDemotionCheck !== undefined) {
    checkLines.unshift(dirPackDemotionCheck);
    if (checkLines.length > MAX_COMPLETION_CHECKS) checkLines.length = MAX_COMPLETION_CHECKS;
  }
  // Item 4: computed over the FINAL (post-augmentation) surface set, same as
  // every other coverage-facing computation above — a token the pre-
  // augmentation seeds/dirEntries missed but augmentConcernGroupSurfaces (or
  // the required-role fill) went on to actually find must not be reported as
  // a create-target.
  const createNote = buildCreateTargetNote(query, seededSurfaces, workspace);
  const result: TaskPackResult = {
    mode: "task_pack",
    coverage: coverageStr,
    ...(coverageReason ? { coverage_reason: coverageReason } : {}),
    surfaces: seededSurfaces,
    missing: allMissing,
    required_surfaces: requiredRoles,
    ...(missingRequired.length > 0 ? { missing_required_surfaces: missingRequired } : {}),
    route,
    checks: checkLines,
    verify: discoverVerificationHints(workspace, query, seededSurfaces),
    ...(nextHint ? { next: nextHint } : {}),
    ...(createNote ? { create_note: createNote } : {}),
  };

  // A caller that already knows the artifact and implementation paths should
  // receive the same one-turn artifact_build contract as the pathless
  // specialized builder. Previously paths[] forced the generic seeded branch:
  // artifact handles were appended only at the final wire choke point, no
  // section was extracted, yet route/change_contract still claimed ready.
  // That contradictory shape produced coverage=complete + edit_from_handles +
  // execution_contract=awaiting-input in the T10 Claude run.
  if (selectedProfile === "artifact_build" && seededArtifacts.length === 0) {
    const discovered = new Map<string, DiscoveredArtifactFile>();
    for (const entry of entries) {
      for (const file of discoverArtifactFiles(workspace, { path: entry.path, query })) {
        discovered.set(file.relPath, file);
      }
    }
    const ranked = [...discovered.values()].sort((a, b) =>
      artifactBuildRelevance(b, args, query) - artifactBuildRelevance(a, args, query)
        || a.relPath.localeCompare(b.relPath)
    );
    for (const file of ranked.slice(0, MAX_SURFACES_DISTINCT)) {
      const minted = mintArtifactSurface(file, workspace, args);
      if (minted) seededArtifacts.push(minted);
    }
  }
  if (selectedProfile === "artifact_build" && seededArtifacts.length > 0) {
    const explicitFileTokens = (query.match(/[A-Za-z_$][A-Za-z0-9_$.-]*\.(?:c|cc|cpp|cxx|go|java|js|jsx|py|rs|ts|tsx)\b/gi) ?? [])
      .map((fileName) => normIdent(path.basename(fileName, path.extname(fileName))));
    const explicitTargetTokens = new Set([
      ...moduleShapedQueryTokens(query).map((token) => normIdent(token)),
      ...explicitFileTokens,
    ]);
    let explicitTargets = seededSurfaces.filter((surface) =>
      explicitTargetTokens.has(normIdent(path.basename(surface.path, path.extname(surface.path))))
    );
    if (explicitTargets.length === 0 && explicitTargetTokens.size > 0) {
      const hardScopes = (args.paths ?? []).map(normalizePathEntry).map((entry) => entry.path);
      const files = hardScopes.length > 0
        ? hardScopes.flatMap((scope) => walkCodeFiles(workspace, { subPath: scope }))
        : walkCodeFiles(workspace, args.path ? { subPath: args.path } : {});
      const exactFile = files.find((file) =>
        explicitTargetTokens.has(normIdent(path.basename(file.relPath, path.extname(file.relPath))))
      );
      if (exactFile) {
        const content = readCached(workspace, exactFile.relPath, cache);
        if (content !== undefined) {
          const target = await candidateToSurface({
            path: exactFile.relPath,
            line: 1,
            range: `1-${Math.max(1, countLines(content))}`,
            surface: classifySurface(exactFile.relPath),
            why: "query-identifier-implementation-match",
            confidence: 1,
            required: true,
          }, workspace, [], query, cache);
          seededSurfaces.unshift(target);
          explicitTargets = [target];
        }
      }
    }
    if (explicitTargets.length > 0) {
      for (const surface of explicitTargets) protectedSurfaces.add(surface);
      seededSurfaces.splice(0, seededSurfaces.length, ...explicitTargets);
    }
    if (!result.missing.includes("artifact-section")) result.missing.push("artifact-section");
    const bundle = await attachArtifactBuildSections(result, seededArtifacts, query, args);
    if (bundle.complete && seededSurfaces.length > 0) {
      result.coverage = "focused";
      result.coverage_reason = "single-site";
      if (explicitTargets.length > 0) {
        delete result.content_sufficiency;
        delete result.content_completeness;
      }
      result.route = {
        action: "edit_from_handles",
        reason: bundle.truncated
          ? "bounded artifact previews and caller-supplied implementation handles are bundled"
          : "artifact sections and caller-supplied implementation handles are bundled for editing",
        max_additional_tl_calls: 0,
      };
      delete result.continuation;
    }
  }

  // A1: seeded surfaces (see `protectedSurfaces` above) are protected from
  // removal — trimToCap may degrade their code to handle+skeleton under
  // budget pressure but must never drop the surface itself. Native-pair
  // siblings and locator-filled roles are NOT in `protectedSurfaces` and
  // stay droppable, same as any other pack.
  // Answer packs can prove an explicitly named identifier absent only after
  // scanning the bounded source universe. Register that fact before the shared
  // readiness contract is built; recording it after dedupe would leave the
  // current response permanently stuck at act-on-served-evidence.
  const verifiedAbsent = proofCompletionEnabled() && answerProfile
    ? explicitCodeIdentifiers(query).filter((identifier) =>
        scanLiteral(identifier, workspace, { caseInsensitive: false }).length === 0
      )
    : [];
  if (verifiedAbsent.length > 0) verifiedAbsentIdentifiersByResult.set(result, verifiedAbsent);
  attachPartialTree(result, workspace);
  const finalResult = dedupeTrimAndPersist(workspace, result, {
    ...(protectedSurfaces.size > 0 ? { protectedSurfaces } : {}),
    query,
    checkRecords,
    args,
    // DESIGN-v0.9 §4.3: recompute next/route if trimToCap flips coverage.
    recompute: { query, missingRequired, unmatchedConcern },
    // DESIGN-v0.9 §4.6b: share the pack's read cache for codeless-surface inlining.
    cache,
  });
  if (verifiedAbsent.length > 0) verifiedAbsentIdentifiersByResult.set(finalResult, verifiedAbsent);
  return finalResult;
}

// ---------------------------------------------------------------------------
// Anchor-focus embedding for large caller-named file seeds (gap: transcript-
// verified in run 2026-07-10b — see buildSeededTaskPack's seed loop
// above, which is the sole call site). Scoring idea salvaged from the
// retired proto/src/mcp/core.ts's buildAnchorFocus (read-only reference, not
// imported); reworked here as a single top-scoring PICK — rather than a
// budget-packed multi-body embed — so the picked symbol's exact range can
// become the seed candidate's own `range`/`symbol`, keeping
// candidateToSurface's slice-equals-served-range (and served-range-equals-
// minted-handle) contract intact.
// ---------------------------------------------------------------------------

/**
 * Cap on how many of a file's top-level symbols are scored. Deliberately
 * higher than extractSymbolsFromFile's own default (16) — a real file large
 * enough to trip this path (content over MAX_SURFACE_CODE_BYTES) commonly
 * has more than 16 top-level declarations, and a symbol past the scan
 * cutoff is invisible to scoring, not just low-scoring.
 */
// Answer recovery may start from only a filename-level candidate in a large
// implementation module. Keep the scan bounded, but cover substantially more
// than the declaration roster returned to callers; the real task-pack module
// has its readiness builder well past the first 64 declarations.
const ANCHOR_FOCUS_MAX_SYMBOLS = 256;

/** Lexical-overlap weight for a query token found in the SYMBOL NAME — a name hit is a strong, cheap-to-trust relevance signal. */
const ANCHOR_FOCUS_NAME_WEIGHT = 3;

/** Prefer a symbol whose own name is covered by the query, not merely a body that repeats query prose. */
const ANCHOR_FOCUS_SYMBOL_COVERAGE_WEIGHT = 5;

/** Edit-shaped queries should prefer executable definitions over interfaces/types carrying the same words. */
const ANCHOR_FOCUS_CALLABLE_WEIGHT = 2;

/** An identifier explicitly written in the query outranks incidental prose overlap. */
const ANCHOR_FOCUS_EXPLICIT_IDENTIFIER_WEIGHT = 100;

/** MCP protocol vocabulary is not a request to locate a same-named code symbol. */
const PROTOCOL_IDENTIFIERS = new Set([
  "mcp",
  "read_file",
  "edit_file",
  "search_files",
  "task_pack",
  "execution_contract",
  "next_call",
  "answer_ready",
  "edit_ready",
  "needs_followup",
]);

/** Generic tool-surface names are too broad to improve a second locate pass. */
const ANSWER_RECOVERY_GENERIC_PROTOCOL_IDENTIFIERS = new Set([
  "read_file",
  "edit_file",
  "search_files",
  "task_pack",
]);

/** Plain semantic terms that become useful when paired with structured identifiers. */
const ANSWER_RECOVERY_SEMANTIC_TERMS = new Set([
  "readiness",
  "continuation",
  "closure",
  "coverage",
]);

/**
 * Strip broad prose/tool names from an answer query before one bounded recovery
 * locate. This prevents a high-scoring bench helper from project-root-locking
 * the candidate pool away from the named protocol implementation.
 */
function isPascalCaseIdentifier(value: string): boolean {
  if (value.length < 2 || value.charCodeAt(0) < 65 || value.charCodeAt(0) > 90) return false;
  let hasLower = false;
  let hasBoundary = false;
  for (let i = 1; i < value.length; i++) {
    const code = value.charCodeAt(i);
    if (code >= 97 && code <= 122) hasLower = true;
    if (code >= 65 && code <= 90) hasBoundary = true;
    else if (!((code >= 48 && code <= 57) || (code >= 97 && code <= 122) || code === 95)) return false;
  }
  return hasLower && hasBoundary;
}

function answerRecoveryQuery(query: string): string | undefined {
  const tokens = query.match(/[A-Za-z_$][A-Za-z0-9_$]*/g) ?? [];
  const seen = new Set<string>();
  const recovered: string[] = [];
  for (const token of tokens) {
    const lower = token.toLowerCase();
    if (lower === "mcp" || ANSWER_RECOVERY_GENERIC_PROTOCOL_IDENTIFIERS.has(lower)) continue;
    const protocol = PROTOCOL_IDENTIFIERS.has(lower);
    const semantic = ANSWER_RECOVERY_SEMANTIC_TERMS.has(lower);
    const codeShaped = token.includes("_")
      || token.includes("$")
      || /^[a-z][A-Za-z0-9]*[A-Z][A-Za-z0-9]*$/.test(token)
      || isPascalCaseIdentifier(token);
    if (!protocol && !semantic && !codeShaped) continue;
    if (seen.has(lower)) continue;
    seen.add(lower);
    recovered.push(token);
  }
  return recovered.length >= 2 ? recovered.join(" ") : undefined;
}

/** Lexical-overlap weight for a query token found in the symbol's BODY text — bodies are long and noisier, so a body-only hit counts for less than a name hit. */
const ANCHOR_FOCUS_BODY_WEIGHT = 1;

/** How many next-best-scoring symbols to name as a ready-to-run follow-up alongside the picked one. */
const ANCHOR_FOCUS_MAX_RUNNER_UPS = 4;

interface AnchorFocusCandidate {
  name: string;
  range: string;
  score: number;
  /**
   * D2 (2026-08-07): set when this candidate is the file's LEADING comment
   * block rather than a parsed symbol. Only affects display wording — the
   * candidate competes on the ordinary lexical score like any other.
   */
  kind?: CollectedSymbolKind | "module-header";
}

interface AnchorFocusResult {
  best: AnchorFocusCandidate;
  runnerUps: AnchorFocusCandidate[];
}

/**
 * Preserve code-shaped identifiers exactly as the caller wrote them. These
 * names are both late-symbol scan targets and a readiness honesty check.
 * Ordinary prose words are deliberately excluded.
 *
 * W9 (2026-08-22): `query` is scrubbed of generic path spans FIRST — a path
 * SEGMENT ("takayuki", "packages") is not an explicit code identifier just
 * because it happens to sit inside a `/`-joined mention in prose, and a
 * multi-segment path can otherwise contain a PascalCase-shaped trailing
 * directory/file name (e.g. ".../TokenLighten/...") that would falsely read
 * as one. This obligation source directly feeds `explicit-identifier`
 * obligations AND `uncoveredIdentifierFindCall`'s batched `find` `next` — an
 * uncovered path segment could otherwise make a pack that could never
 * certify (identifier provably absent from every served surface), or waste
 * a `find` call on a token that is not a real identifier. A bare file stem
 * with no directory prefix (`policy.ts`) is unaffected — see stripPathSpans.
 */
function explicitCodeIdentifiers(query: string): string[] {
  const cleaned = stripPathSpans("", query);
  const matches = cleaned.match(/[A-Za-z_$][A-Za-z0-9_$]*/g) ?? [];
  const backticked = new Set(
    [...cleaned.matchAll(/`([A-Za-z_$][A-Za-z0-9_$]*)`/g)]
      .map((match) => match[1]!.toLowerCase()),
  );
  const parenthesizedPascal = new Set(
    [...cleaned.matchAll(/\(\s*([A-Z][a-z][A-Za-z0-9]*)\s*\)/g)]
      .map((match) => match[1]!.toLowerCase()),
  );
  const seen = new Set<string>();
  const out: string[] = [];
  for (const token of matches) {
    const key = token.toLowerCase();
    const codeShaped = token.includes("_")
      || token.includes("$")
      || /^[a-z][A-Za-z0-9]*[A-Z][A-Za-z0-9]*$/.test(token)
      || isPascalCaseIdentifier(token)
      || /^[A-Z][A-Z0-9_]{2,}$/.test(token)
      || backticked.has(key)
      || parenthesizedPascal.has(key);
    if (!codeShaped) continue;
    if (PROTOCOL_IDENTIFIERS.has(key)) continue;
    if (seen.has(key)) continue;
    seen.add(key);
    out.push(token);
  }
  return out;
}

/** Identities whose code intent is explicit rather than inferred from shape. */
function delimitedExplicitCodeIdentifiers(query: string): string[] {
  const delimited = new Set([
    ...[...query.matchAll(/`([A-Za-z_$][A-Za-z0-9_$]*)`/g)].map((match) => normIdent(match[1]!)),
    ...[...query.matchAll(/\(\s*([A-Z][a-z][A-Za-z0-9]*)\s*\)/g)].map((match) => normIdent(match[1]!)),
  ]);
  return explicitCodeIdentifiers(query).filter((identifier) => delimited.has(normIdent(identifier)));
}

interface QueryIdentifierContext {
  range: string;
  line: number;
}

/** Prefer the test block that mentions the requested symbol over file helpers. */
function queryIdentifierContext(content: string, query: string): QueryIdentifierContext | undefined {
  const evidence = selectQueryEvidence(content, query, { preferTestBlocks: true });
  if (evidence?.kind === "test-block") {
    return { range: evidence.range, line: evidence.line };
  }
  const identifiers = explicitCodeIdentifiers(query);
  if (identifiers.length === 0) return undefined;
  const lines = content.split(/\r?\n/);
  const queryTokens = tokenizeForEpoch(query);
  let best: { line: number; score: number } | undefined;
  for (let index = 0; index < lines.length; index++) {
    const lower = lines[index]!.toLowerCase();
    if (!identifiers.some((identifier) => lower.includes(identifier.toLowerCase()))) continue;
    const start = Math.max(0, index - 12);
    const end = Math.min(lines.length, index + 13);
    const score = lexicalOverlapFraction(queryTokens, tokenizeForEpoch(lines.slice(start, end).join("\n")));
    if (best === undefined || score > best.score) best = { line: index + 1, score };
  }
  if (best === undefined) return undefined;
  const start = Math.max(1, best.line - 12);
  const end = Math.min(lines.length, best.line + 12);
  return { range: `${start}-${end}`, line: best.line };
}

const QUERY_FOCUS_TEXT_EXTENSIONS = new Set([
  ".json", ".jsonc", ".jsonl", ".yaml", ".yml", ".toml", ".txt", ".log", ".csv", ".tsv",
]);

function refocusCandidateForQuery(
  candidate: ImpactCandidate,
  workspace: string,
  query: string,
  cache?: FileReadCache,
): ImpactCandidate {
  if (query.trim().length === 0) return candidate;
  const testFocus = candidate.surface === "test" && queryRequestsTestEvidence(query);
  const textFocus = QUERY_FOCUS_TEXT_EXTENSIONS.has(path.extname(candidate.path).toLowerCase());
  if (!testFocus && !textFocus) return candidate;
  const content = readCached(workspace, candidate.path, cache);
  if (content === undefined) return candidate;
  const evidence = selectQueryEvidence(content, query, {
    path: candidate.path,
    preferTestBlocks: testFocus,
  });
  if (evidence === undefined) return candidate;
  return {
    ...candidate,
    line: evidence.line,
    range: evidence.range,
    symbol: undefined,
    handle: undefined,
    why: testFocus ? "query-evidence-test-match" : "query-evidence-data-match",
    confidence: Math.max(candidate.confidence, 0.85),
  };
}

/**
 * Score `filePath`'s top-level symbols (extractSymbolsFromFile — the same
 * symbol-listing helper resolveDigest already uses) by lexical overlap
 * against `query`, and pick the top-scoring symbol whose body fits
 * MAX_SURFACE_CODE_BYTES.
 *
 * Overlap is the fraction of `query`'s tokenizeForEpoch tokens also present
 * in a candidate's own tokenizeForEpoch tokens (the same fraction-of-query
 * shape as the retired prototype's lexicalOverlap, rebuilt on this
 * codebase's existing tokenizer instead of hand-rolling a second one),
 * scored twice per symbol — once against its NAME (ANCHOR_FOCUS_NAME_WEIGHT)
 * and once against its BODY text (ANCHOR_FOCUS_BODY_WEIGHT) — and summed.
 *
 * Returns undefined when no symbol scores above zero overlap, OR when every
 * positively-scored symbol's body overflows MAX_SURFACE_CODE_BYTES (picking
 * one then would still ship no code — never guess); the caller keeps
 * today's whole-file-range default in both cases.
 */
async function selectAnchorFocus(
  content: string,
  filePath: string,
  query: string,
  cache?: FileReadCache,
): Promise<AnchorFocusResult | undefined> {
  const queryTokens = tokenizeForEpoch(query);
  if (queryTokens.length === 0) return undefined;
  const explicitIdentifiers = explicitCodeIdentifiers(query);
  const explicitIdentifierSet = new Set(explicitIdentifiers.map((name) => name.toLowerCase()));
  const lang = languageForPath(filePath);
  const parsed = lang
    ? cache
      ? await cache.parsedSymbols(content, lang, filePath)
      : await collectSymbols(content, lang, {}).catch(() => null)
    : null;
  const structuralSymbols: Array<{ name: string; range: string; line: number; kind?: CollectedSymbolKind }> =
    (parsed ?? []).slice(0, ANCHOR_FOCUS_MAX_SYMBOLS).map((symbol) => ({
      name: symbol.name,
      range: `${symbol.startLine}-${symbol.endLine}`,
      line: symbol.startLine,
      // Real parser kind, kept ONLY so the substantive-vs-header/const
      // selection preference below (2026-08-21 answer-focus precision fix)
      // can tell a function/method/class apart from a const/type/interface
      // anchor. Absent on the extractSymbolsFromFile regex fallback, which
      // makes that preference an intentional no-op on unparsed languages.
      kind: symbol.kind,
    }));
  const symbols: Array<{ name: string; range: string; line: number; kind?: CollectedSymbolKind }> =
    structuralSymbols.length > 0
      ? structuralSymbols
      : await extractSymbolsFromFile(content, filePath, ANCHOR_FOCUS_MAX_SYMBOLS, explicitIdentifiers);
  if (symbols.length === 0) return undefined;

  const lines = content.split(/\r?\n/);

  // F-V13-8 (2026-08-30, independent-verification remediation): literal-first
  // anchor. A query that names an exact, unique quoted/diff-style replacement
  // (exactReplacementFromQuery — the same 8192B-capped, multi-quote-form
  // parser attachSingleSiteUniqueMatchFastPath and the pre-pipeline T2 short
  // circuit both already trust) is unambiguous positional evidence a
  // lexical-overlap score can still lose: vocabulary shared with an
  // unrelated symbol elsewhere in the file can outscore the symbol that
  // actually contains the one place the literal occurs (observed: an
  // explicit path + a quoted literal + a line-number hint anchored on the
  // wrong symbol, dozens of lines away from the real target). When the
  // literal (or, for the "set IDENT to NEW" form with no stated old value,
  // the bare identifier text) occurs in `content` EXACTLY ONCE — checked
  // with two indexOf calls, no regex, no normalization — route straight to
  // the symbol enclosing that occurrence (reusing the `symbols` list already
  // built above) and skip scoring entirely. Zero or multiple occurrences
  // fall through unchanged to the lexical scoring below: uniqueness is the
  // only signal trusted here, and choosing among several hits is left to the
  // scoring/precondition machinery downstream — never guessed here.
  const literalExact = exactReplacementFromQuery(query);
  const literalText = literalExact === undefined
    ? undefined
    : "identifierOldValue" in literalExact
    ? literalExact.identifierOldValue
    : literalExact.search;
  if (literalText !== undefined && literalText.length > 0) {
    const firstIdx = content.indexOf(literalText);
    if (firstIdx !== -1 && content.indexOf(literalText, firstIdx + 1) === -1) {
      const matchLine = content.slice(0, firstIdx).split(/\r?\n/).length;
      let enclosing: (typeof symbols)[number] | undefined;
      let enclosingSpan = Infinity;
      for (const sym of symbols) {
        const bounds = sym.range.split("-");
        const start = parseInt(bounds[0] ?? "", 10);
        const end = parseInt(bounds[1] ?? "", 10);
        if (!Number.isFinite(start) || !Number.isFinite(end) || start > matchLine || end < matchLine) continue;
        const span = end - start;
        if (span < enclosingSpan) {
          enclosing = sym;
          enclosingSpan = span;
        }
      }
      if (enclosing !== undefined) {
        return {
          best: {
            name: enclosing.name,
            range: enclosing.range,
            score: ANCHOR_FOCUS_EXPLICIT_IDENTIFIER_WEIGHT,
            ...(enclosing.kind ? { kind: enclosing.kind } : {}),
          },
          runnerUps: [],
        };
      }
    }
  }

  const scored: Array<AnchorFocusCandidate & { fits: boolean; exactIdentifier: boolean }> = [];
  const seenSymbolRanges = new Set<string>();
  for (const sym of symbols) {
    const symbolRangeKey = `${sym.name.toLowerCase()}\0${sym.range}`;
    if (seenSymbolRanges.has(symbolRangeKey)) continue;
    seenSymbolRanges.add(symbolRangeKey);
    const bounds = sym.range.split("-");
    const startLine = parseInt(bounds[0] ?? "", 10);
    const endLine = parseInt(bounds[1] ?? "", 10);
    if (!Number.isFinite(startLine) || !Number.isFinite(endLine) || startLine < 1 || endLine < startLine) continue;
    const body = lines.slice(startLine - 1, endLine).join("\n");
    const symbolTokens = tokenizeForEpoch(sym.name);
    const nameOverlap = lexicalOverlapFraction(queryTokens, symbolTokens);
    const symbolCoverage = lexicalOverlapFraction(symbolTokens, queryTokens);
    const bodyOverlap = lexicalOverlapFraction(queryTokens, tokenizeForEpoch(body));
    const declaration = body.split(/\r?\n/, 1)[0] ?? "";
    const callable = /\b(function|method|def|fn|fun)\b|=>/.test(declaration) ? 1 : 0;
    const typeOnly = /\b(interface|type|enum)\b/.test(declaration) ? 1 : 0;
    const explicitIdentifier = explicitIdentifierSet.has(sym.name.toLowerCase());
    const lexicalScore = nameOverlap * ANCHOR_FOCUS_NAME_WEIGHT
      + symbolCoverage * ANCHOR_FOCUS_SYMBOL_COVERAGE_WEIGHT
      + bodyOverlap * ANCHOR_FOCUS_BODY_WEIGHT;
    const score = explicitIdentifier
      ? ANCHOR_FOCUS_EXPLICIT_IDENTIFIER_WEIGHT + lexicalScore + callable * ANCHOR_FOCUS_CALLABLE_WEIGHT - typeOnly
      : lexicalScore <= 0
      ? 0
      : lexicalScore + callable * ANCHOR_FOCUS_CALLABLE_WEIGHT - typeOnly;
    if (score <= 0) continue;
    scored.push({
      name: sym.name,
      range: sym.range,
      score,
      ...(sym.kind ? { kind: sym.kind } : {}),
      fits: Buffer.byteLength(body, "utf8") <= MAX_SURFACE_CODE_BYTES,
      exactIdentifier: explicitIdentifier,
    });
  }
  // D2 (2026-08-07): the file's leading comment block competes as a candidate.
  // Symbols were the only anchor candidates, so on a query whose answer lives
  // in the module header (how a script is invoked, what the file is for, the
  // arguments it takes) a token-matched internal symbol always won the window
  // and the header stayed unserved in remaining_ranges.
  //
  // Scored with the SAME term structure as a symbol, no new weights and no
  // usage-vocabulary keywords anywhere: a header's "name" is the file itself
  // (that is what it documents), so the basename supplies the name/coverage
  // terms and the block text supplies the body term. It carries no callable
  // bonus because it is not callable. A header whose text does not match the
  // query scores 0 and is dropped by the shared `score <= 0` rule, so this is
  // inert on the overwhelming majority of queries; when it does score but
  // loses, it still reaches the caller as a ready-to-run slice in the
  // surface's runner-up `outline`.
  const header = leadingHeaderBlockRange(content);
  if (header !== undefined) {
    const headerName = path.basename(filePath);
    const headerNameTokens = tokenizeForEpoch(headerName);
    const headerScore =
      lexicalOverlapFraction(queryTokens, headerNameTokens) * ANCHOR_FOCUS_NAME_WEIGHT
      + lexicalOverlapFraction(headerNameTokens, queryTokens) * ANCHOR_FOCUS_SYMBOL_COVERAGE_WEIGHT
      + lexicalOverlapFraction(queryTokens, tokenizeForEpoch(header.text)) * ANCHOR_FOCUS_BODY_WEIGHT;
    if (headerScore > 0) {
      scored.push({
        name: headerName,
        range: header.range,
        score: headerScore,
        kind: "module-header",
        fits: Buffer.byteLength(header.text, "utf8") <= MAX_SURFACE_CODE_BYTES,
        exactIdentifier: false,
      });
    }
  }
  if (scored.length === 0) return undefined;
  // Highest score first; Array.prototype.sort is stable (ES2019+), so
  // symbols tied on score keep extractSymbolsFromFile's source-file order.
  scored.sort((a, b) => b.score - a.score);

  // Prefer a compact callable, but never discard an exact named class/module
  // solely because its body is large. candidateToSurface can serve a bounded
  // head plus member handles for that symbol.
  //
  // 2026-08-21 answer-focus precision fix: ANCHOR_FOCUS_SYMBOL_COVERAGE_WEIGHT
  // rewards the FRACTION of a symbol's own name tokens covered by the query —
  // a 2-3-token constant or the module header clears that fraction far more
  // easily than a longer, more specific function/method name, so a header or
  // a constant could out-score the real function that answers the query on
  // lexical coverage alone. When nothing was EXPLICITLY named (an exact
  // identifier already always sorts first via its dedicated +100 weight and
  // must never be demoted here), prefer the highest-scored SUBSTANTIVE symbol
  // — function/method/class — over a header/const/type/interface/local, and
  // only fall back to a non-substantive one when no substantive candidate
  // scored at all (this keeps D2's header-as-last-resort behavior intact).
  const hasExplicitIdentifierWinner = scored[0]?.exactIdentifier === true;
  // Exemption: a query that substantially NAMES THE FILE ITSELF (at least
  // half of the file's own basename tokens appear in the query — e.g. "what
  // does the coverage packer do" naming coveragePacker.ts) is asking about
  // the MODULE as a whole, and its header genuinely is the answer — the
  // opposite case from a header that merely happens to share vocabulary with
  // an unrelated same-file constant/symbol. Only exempts a header that is
  // ALREADY the top-scored candidate; it never promotes a losing header.
  const topIsFileNamedHeader = !hasExplicitIdentifierWinner
    && scored[0]?.kind === "module-header"
    && (() => {
        const baseTokens = tokenizeForEpoch(scored[0]!.name);
        return baseTokens.length > 0 && lexicalOverlapFraction(baseTokens, queryTokens) >= 0.5;
      })();
  const substantiveFitIdx = hasExplicitIdentifierWinner || topIsFileNamedHeader
    ? -1
    : scored.findIndex((s) => s.fits && (s.kind === "function" || s.kind === "method" || s.kind === "class"));
  const bestIdx = substantiveFitIdx >= 0 ? substantiveFitIdx : scored.findIndex((s) => s.fits);
  const resolvedBestIdx = bestIdx >= 0 ? bestIdx : scored.findIndex((s) => s.exactIdentifier);
  if (resolvedBestIdx === -1) return undefined;

  const best = scored[resolvedBestIdx]!;
  const runnerUps = [...scored.slice(0, resolvedBestIdx), ...scored.slice(resolvedBestIdx + 1)]
    .slice(0, ANCHOR_FOCUS_MAX_RUNNER_UPS)
    .map(({ name, range, score }) => ({ name, range, score }));

  return {
    best: {
      name: best.name,
      range: best.range,
      score: best.score,
      ...(best.kind ? { kind: best.kind } : {}),
    },
    runnerUps,
  };
}

/**
 * D2 (2026-08-07): line span of the file's LEADING comment block — the module
 * header that conventionally documents what the file is and how it is used.
 *
 * selectAnchorFocus scored only PARSED SYMBOLS, so a header could never be an
 * anchor candidate: on a usage/how-to query the answer sat in the header while
 * a token-matched internal symbol won the window and the header stayed inside
 * remaining_ranges (observed 2026-08-07 on an operator-runbook query). This is
 * a general indexing gap, not a usage-query special case — the block competes
 * on the ordinary lexical score and wins only when its own text matches.
 *
 * Recognizes the leading run of comment lines after an optional shebang and
 * leading blanks, for the comment syntaxes that cover the languages this
 * server serves (C-family block/line, hash, triple-quoted docstring, SQL/Lua
 * `--`, SGML `<!-- -->`, lisp `;`). Returns undefined when the file opens with
 * code, when the block is shorter than ANCHOR_FOCUS_HEADER_MIN_LINES (a bare
 * one-line banner is noise, not documentation), or when the whole file is
 * comments (a prose/doc file — its own pack paths already serve it).
 */
const ANCHOR_FOCUS_HEADER_MIN_LINES = 3;

function leadingHeaderBlockRange(content: string): { range: string; text: string } | undefined {
  const lines = content.split(/\r?\n/);
  let i = 0;
  if (lines[0]?.startsWith("#!")) i = 1;
  while (i < lines.length && lines[i]!.trim() === "") i++;
  if (i >= lines.length) return undefined;

  const BLOCK_OPENERS: Array<{ open: string; close: string }> = [
    { open: "/*", close: "*/" },
    { open: '"""', close: '"""' },
    { open: "'''", close: "'''" },
    { open: "<!--", close: "-->" },
  ];
  const LINE_TOKENS = ["//", "#", "--", ";"];
  // `#` opens a comment in Python/Ruby/shell/YAML/TOML but a PREPROCESSOR
  // DIRECTIVE in C/C++/ObjC. Without this, a translation unit's leading
  // `#include` run reads as a three-line "module header" — and worse, it
  // extends a genuine leading license block over the include list.
  const PREPROCESSOR_RE =
    /^#\s*(?:include|import|define|undef|ifdef|ifndef|if|elif|else|endif|pragma|error|warning|line)\b/;

  const first = i;
  let last = -1;
  let closing: string | undefined;
  for (; i < lines.length; i++) {
    const trimmed = lines[i]!.trim();
    if (closing !== undefined) {
      last = i;
      if (trimmed.includes(closing)) closing = undefined;
      continue;
    }
    if (trimmed === "") continue;
    const opener = BLOCK_OPENERS.find(({ open }) => trimmed.startsWith(open));
    if (opener) {
      last = i;
      // A single-line block (`/** x */`) closes on its own line; only treat
      // the delimiter as open when no closer follows the opener text.
      const rest = trimmed.slice(opener.open.length);
      if (!rest.includes(opener.close)) closing = opener.close;
      continue;
    }
    if (PREPROCESSOR_RE.test(trimmed)) break;
    if (LINE_TOKENS.some((token) => trimmed.startsWith(token))) {
      last = i;
      continue;
    }
    break;
  }
  // An unterminated block delimiter means the scanner mis-read the syntax
  // (e.g. a `'''`-quoted value at file head) — never claim that span.
  if (closing !== undefined || last < first) return undefined;
  if (last - first + 1 < ANCHOR_FOCUS_HEADER_MIN_LINES) return undefined;
  // Whole file is header: not an anchor decision this function should make.
  if (lines.slice(last + 1).every((line) => line.trim() === "")) return undefined;
  return {
    range: `${first + 1}-${last + 1}`,
    text: lines.slice(first, last + 1).join("\n"),
  };
}

/**
 * Fraction (0..1) of `queryTokens` also present in `candidateTokens`. Both
 * are expected to already be tokenizeForEpoch output (deduplicated), so this
 * is a plain Set-membership tally — mirrors the retired prototype's
 * lexicalOverlap (proto/src/mcp/core.ts's buildAnchorFocus, via
 * proto/src/core/lexical.ts's `hits / taskWords.size`) without hand-rolling
 * a second tokenizer.
 */
function lexicalOverlapFraction(queryTokens: string[], candidateTokens: string[]): number {
  if (queryTokens.length === 0) return 0;
  const candidateSet = new Set(candidateTokens);
  let hits = 0;
  for (const t of queryTokens) if (candidateSet.has(t)) hits++;
  return hits / queryTokens.length;
}

/**
 * Ready-to-run `read_file mode=slice handle=<id>` hints for the anchor-focus
 * runner-up symbols — mints one real "symbol"-kind handle per named
 * runner-up (same handleTable.upsert shape candidateToSurface itself uses)
 * so each hint is immediately actionable without a further locate/digest
 * round-trip, and names the symbol so the agent can pick by relevance
 * instead of calling all of them.
 */
function anchorFocusRunnerUpOutline(
  runnerUps: AnchorFocusCandidate[],
  filePath: string,
  workspace: string,
): string[] {
  return runnerUps.map((r) => {
    const handle = handleTable.upsert({
      kind: "symbol",
      path: filePath,
      range: r.range,
      symbol: r.name,
      workspaceRoot: workspace,
    });
    return `${r.name}: read_file mode=slice handle=${handle.id}`;
  });
}

/**
 * Shared attach step for an anchor-focus hit, applied to the ALREADY-BUILT
 * surface (never touches candidateToSurface/whySummary): names the pick in
 * `why` instead of leaving it looking like an ordinary caller-supplied/
 * locator tag, and reuses TaskPackSurface's existing `outline` (string[])
 * affordance for the ready-to-run runner-up hints. Factored verbatim out of
 * buildSeededTaskPack's FILE-seed loop (behavior unchanged there) so the
 * locator-candidate path below attaches the exact same affordance.
 */
function attachAnchorFocusAffordance(
  surface: TaskPackSurface,
  focus: AnchorFocusResult,
  filePath: string,
  workspace: string,
): void {
  const callerScoped = /^caller-supplied(?:-dir)?$/.test(surface.why ?? "");
  const focusWhy = focus.best.kind === "module-header"
    ? `anchor-focus: query-matched module header of ${focus.best.name}`
    : focus.best.score >= ANCHOR_FOCUS_EXPLICIT_IDENTIFIER_WEIGHT
    ? `anchor-focus: explicit query identifier ${focus.best.name}`
    : `anchor-focus: query-matched symbol ${focus.best.name}`;
  // Anchor focusing changes the selected range, not the authority of the
  // caller-provided path. Preserve that provenance in the existing compact
  // `why` field so readiness cannot mistake a scoped artifact for an
  // untrusted repository-wide token hit.
  surface.why = callerScoped ? `${focusWhy}; caller-supplied` : focusWhy;
  if (focus.runnerUps.length > 0) {
    surface.outline = anchorFocusRunnerUpOutline(focus.runnerUps, filePath, workspace);
  }
}

/**
 * Anchor-focus for LOCATOR-produced candidates — the required-role FILL and
 * caller-supplied-DIR expansion loops in buildSeededTaskPack. Run 2026-07-10c:
 * selectAnchorFocus fired in 0/10 arm-A cells because its only call site was
 * the direct FILE-seed path, while every real pack call opens with
 * paths=["<dir>"]+query — those candidates reach the two loops instead, and
 * neither consulted the query for symbol selection, so a >4KB file embedded
 * nothing and agents fell back to slice chains.
 *
 * Trigger (all must hold): the candidate carries no SYMBOL pin (a
 * symbol-anchored candidate is already locate-precise — never overridden;
 * a locator range is NOT treated as a pin, because these loops' candidates
 * always arrive with a synthetic text window / candidateDetails range, so a
 * literal no-range gate would keep the feature dead — the 10c finding);
 * `query` is non-empty; and the candidate FILE's content (read through the
 * per-pack FileReadCache — no second disk read; sliceCode reuses the memo)
 * overflows MAX_SURFACE_CODE_BYTES.
 *
 * On a hit, returns the candidate re-pointed at the top pick's own
 * symbol+range — so the embedded code and the minted handle range match
 * exactly — with any pre-minted locator `handle` DROPPED (it still points at
 * the old window; candidateToSurface would otherwise reuse it verbatim since
 * the new range equals the new rawRange) and confidence lifted over the
 * embed gate (an anchor hit means the query names this symbol; same
 * Math.max bump augmentMissingRequiredRoleSurfaces applies). Returns
 * undefined — caller keeps today's behavior for that candidate — on zero
 * overlap or when any trigger condition fails.
 */
async function anchorFocusForLocatorCandidate(
  candidate: ImpactCandidate,
  workspace: string,
  query: string,
  cache: FileReadCache,
): Promise<{ candidate: ImpactCandidate; focus: AnchorFocusResult } | undefined> {
  // An ordinary symbol-pinned locator candidate is already precise. The
  // directory resolver's explicit-identifier candidate is different: its
  // symbol came from the caller and its literal line may still be a comment.
  // On an oversized file, run anchor focus so the exact parsed body and the
  // normal runner-up affordance survive (small files are widened directly by
  // candidateToSurface's parsed-symbol path).
  if (candidate.symbol !== undefined && candidate.why !== "explicit-query-identifier") return undefined;
  if (query.trim().length === 0) return undefined;
  const content = cache.read(workspace, candidate.path);
  if (content === undefined) return undefined;
  if (Buffer.byteLength(content, "utf8") <= MAX_SURFACE_CODE_BYTES) return undefined;
  const focus = await selectAnchorFocus(content, candidate.path, query, cache);
  if (!focus) return undefined;
  const { handle: _staleWindowHandle, ...rest } = candidate;
  return {
    candidate: {
      ...rest,
      range: focus.best.range,
      symbol: focus.best.name,
      confidence: Math.max(candidate.confidence, HIGH_CONFIDENCE_THRESHOLD),
    },
    focus,
  };
}

// ---------------------------------------------------------------------------
// A4: symptom-shaped (fix/bug) queries route to working-tree drift instead
// of the enum-variant template. The query wording that decides a bug fix
// ("windup", "race condition", "sign flip", ...) rarely text-matches
// the files that actually need to change, but `git diff` against the
// working tree's own baseline almost always points straight at them,
// because the bug (or its fix) IS the drift. task_pack already had the
// capability (explore action=diff / getCurrentDiff) — it just never routed
// symptom-shaped queries there.
// ---------------------------------------------------------------------------

/** Words that mark a query as describing a defect rather than a feature addition. */
const SYMPTOM_WORDS =
  /\b(bug|fix|fixes|fixing|broken|breaks|regression|race\s*condition|deadlock|crash(?:es|ing)?|leak(?:s|ing)?|windup|overflow|underflow|off[- ]by[- ]one|incorrect|wrong|invalid|stale|flaky|flapping|hang(?:s|ing)?|null\s*pointer|undefined\s*behaviou?r|corrupt(?:ed|ion)?|does\s*not\s*work|doesn'?t\s*work|not\s*working|fails?|failing|failure|error(?:s)?|misbehav|unexpected|reversed|inverted|swapped)\b/i;

/** True when the query reads like a bug-fix task rather than a feature addition. */
function isSymptomShapedQuery(query: string): boolean {
  return SYMPTOM_WORDS.test(query);
}

/**
 * Resolve the git repo root for `workspace` and the POSIX-relative prefix
 * from that root down to `workspace` (e.g. workspace
 * ".../repo/apps/webapp" under repo root
 * ".../repo" -> prefix "apps/webapp/"). Returns "" when
 * `workspace` IS the git root (no prefix to strip) or when the root
 * cannot be determined.
 *
 * `git diff` always emits paths relative to the REPO ROOT, never the `-C`
 * cwd, even when the diff itself is scoped with a pathspec — so this
 * prefix must be stripped before treating a diff path as workspace-
 * relative (the shape every other surface/handle/sliceCode call expects).
 */
async function gitWorkspacePrefix(workspace: string): Promise<string> {
  const { execFile } = await import("child_process");
  const toplevel = await new Promise<string | null>((resolve) => {
    execFile("git", ["-C", workspace, "rev-parse", "--show-toplevel"], { timeout: 5000 }, (err, stdout) => {
      resolve(err ? null : stdout.trim());
    });
  });
  if (!toplevel) return "";
  const resolvedWorkspace = fs.realpathSync(workspace);
  const resolvedToplevel = fs.realpathSync(toplevel);
  if (resolvedWorkspace === resolvedToplevel) return "";
  const rel = path.relative(resolvedToplevel, resolvedWorkspace).split(path.sep).join("/");
  return rel.length > 0 ? `${rel}/` : "";
}

/**
 * Build a task_pack response from the working tree's git drift: each
 * changed file becomes a surface anchored on the EDIT SITE (the first
 * changed hunk, not the file header), carrying the current on-disk code
 * for that range so the agent can act without an extra slice call.
 *
 * Returns null when there is no usable diff (no git repo, clean tree, or
 * every changed file is excluded/binary) so the caller falls through to
 * the normal locate flow.
 *
 * IMPORTANT: `path: "."` scopes the diff to the workspace directory
 * itself. `workspace` is very often NOT its own git root (e.g. a
 * benchmark fixture nested inside a monorepo, or any subproject inside a
 * larger repo) — `git -C <workspace> diff HEAD` with no pathspec walks up
 * to the enclosing repo's root and returns EVERY change anywhere in that
 * repo, not just this task's fixture/subtree. Scoping to "." keeps drift
 * surfaces confined to the workspace the caller actually asked about; the
 * returned paths still need gitWorkspacePrefix() stripped since git always
 * reports repo-root-relative paths regardless of scoping.
 */
async function buildDiffTaskPack(query: string, workspace: string): Promise<TaskPackResult | null> {
  const diff = await getCurrentDiff({ path: "." }, workspace);
  if (diff.error || diff.files.length === 0) return null;

  const prefix = await gitWorkspacePrefix(workspace);

  const requiredRoles: string[] = [];
  const surfaces: TaskPackSurface[] = [];

  for (const file of diff.files) {
    if (surfaces.length >= MAX_SURFACES_DISTINCT) break;
    if (file.status === "deleted") continue; // nothing left on disk to slice
    if (prefix && !file.path.startsWith(prefix)) continue; // defensive: outside workspace despite scoping
    const relPath = prefix ? file.path.slice(prefix.length) : file.path;
    if (/\/(dist|build|__pycache__|node_modules)\//.test(relPath)) continue;

    // Anchor the range on the first changed hunk (the actual edit site),
    // not the file header — widen a few lines of context around it.
    const firstHunk = file.hunks[0];
    const anchorLine = firstHunk ? firstHunk.newStart : 1;
    const anchorLen = firstHunk ? Math.max(firstHunk.newLines, 1) : 20;
    const startLine = Math.max(1, anchorLine - 3);
    const endLine = anchorLine + anchorLen + 3;
    const range = `${startLine}-${endLine}`;

    const entry = handleTable.upsert({
      kind: "range",
      path: relPath,
      range,
      workspaceRoot: workspace,
    });

    const role = classifySurface(relPath) as string;
    // DESIGN-v0.9 §4.1/§4.5: an oversize drift region gets a hunk-anchor-centered
    // trimmed body + remaining_ranges instead of the code-less cliff.
    const embed = centeredSliceForCap(workspace, relPath, range, undefined, anchorLine);
    const hunkCount = file.hunks.length;
    const why = hunkCount > 1
      ? `working-tree drift vs git baseline (${hunkCount} changed regions)`
      : "working-tree drift vs git baseline";

    surfaces.push({
      role,
      handle: entry.id,
      path: relPath,
      range,
      required: true,
      why,
      edit_intent: "resolve the reported symptom in this changed region; compare against the git baseline for intended behavior",
      done_check: "the reported symptom no longer reproduces and the change matches the intended baseline behavior",
      ...(embed ? { code: embed.code } : {}),
      ...(embed && embed.remaining_ranges.length > 0
        ? { content_completeness: "partial" as const, remaining_ranges: embed.remaining_ranges }
        : {}),
    });
    if (!requiredRoles.includes(role)) requiredRoles.push(role);
  }

  if (surfaces.length === 0) return null;

  const primaryHandle = surfaces[0]!.handle;
  // DESIGN-v0.8 coverage-honesty: a complete diff pack IS the whole located
  // answer (every changed file on disk), so it reads "complete"; a truncated
  // diff genuinely omits changed files — partial / "diff-truncated".
  const coverage: "complete" | "partial" = diff.truncated ? "partial" : "complete";
  const coverageReason: TaskPackResult["coverage_reason"] | undefined =
    diff.truncated ? "diff-truncated" : undefined;
  // P2 fix: route through the A4-hardened buildRoute instead of a hardcoded
  // confident `edit_from_handles / 0`. A truncated diff means the pack does
  // NOT cover every changed file — coverage is partial and `missing` carries
  // "additional-changed-files", so buildRoute's partial branch emits an honest
  // reason that names the truncation (max_additional_tl_calls stays 0 only
  // when nothing is missing). An empty query is passed deliberately: the diff
  // surfaces ARE the located answer, so the concern-token flip must not fire
  // on the caller's symptom wording (which rarely text-matches the changed
  // file paths — the whole reason this flow exists).
  const missing = diff.truncated ? ["additional-changed-files"] : [];
  const route = buildRoute(surfaces, [], coverage, missing, "", coverageReason);
  return {
    mode: "task_pack",
    coverage,
    ...(coverageReason ? { coverage_reason: coverageReason } : {}),
    surfaces,
    missing,
    required_surfaces: requiredRoles,
    route,
    // DESIGN-v0.8 §C1 item 4: capped at 1 entry (MAX_COMPLETION_CHECKS);
    // both prior checks folded into one line rather than dropping either.
    checks: [
      "confirm each drift surface's change matches the reported symptom, then re-run `search_files action=diff` to confirm no new regression",
    ],
    verify: discoverVerificationHints(workspace, query, surfaces),
    next: `read_file mode=slice handle=${primaryHandle}`,
  };
}

function shouldRedirectToOverview(args: TaskPackArgs, query: string): boolean {
  if (args.path || args.symbol || (args.surfaceRoles && args.surfaceRoles.length > 0)) {
    return false;
  }
  const raw = query.trim();
  if (raw.length === 0) return false;
  const lower = raw.toLowerCase();

  // Do not steal clear edit tasks just because they mention "overview mode".
  if (/^\s*(implement|add|fix|change|update|rename|remove|delete)\b/.test(lower)) {
    return false;
  }

  if (/どんなシステム|全体像|概要|アーキテクチャ|構成を.*教えて|実装を.*見て.*教えて/.test(raw)) {
    return true;
  }

  const overviewPhrase =
    /\b(architecture|implementation|system|repo|repository|codebase)\s+(overview|map|summary)\b/.test(lower) ||
    /\b(overview|map|summary)\s+(of|for)\s+(the\s+)?(architecture|system|repo|repository|codebase)\b/.test(lower);
  if (overviewPhrase) return true;

  if (/\b(what\s+(kind|type)\s+of\s+system|explain|understand|summarize|describe)\b/.test(lower) &&
      /\b(system|repo|repository|codebase|architecture|implementation)\b/.test(lower)) {
    return true;
  }

  return false;
}

/**
 * Cheap pathless-doc fast path. A strong root Markdown filename/title match is
 * better evidence than a repository-wide token scan and avoids the slow,
 * misleading test-name match seen for design/version questions.
 */
function rootMarkdownTitleMatch(workspace: string, query: string): string | undefined {
  if (/^\s*(implement|add|fix|change|update|rename|remove|delete)\b/i.test(query)) return undefined;
  const queryTokens = new Set(tokenizeForEpoch(query).filter((token) => token.length > 1));
  if (queryTokens.size < 3) return undefined;
  let entries: fs.Dirent[];
  try {
    entries = fs.readdirSync(workspace, { withFileTypes: true });
  } catch {
    return undefined;
  }
  const ranked = entries
    .filter((entry) => entry.isFile() && isMarkdownPath(entry.name))
    .map((entry) => {
      let title = "";
      try {
        const abs = path.join(workspace, entry.name);
        statReadTargetSync(abs, workspace);
        // P3 (2026-08-27): decode via BOM/NUL-aware decodeTextBuffer, never a
        // raw utf8 read — an undecodable file is skipped like any other probe
        // failure, never served as garbled title text.
        const decoded = decodeTextBuffer(fs.readFileSync(abs));
        if (decoded === null) return undefined;
        title = decoded.split(/\r?\n/, 1)[0] ?? "";
      } catch {
        return undefined;
      }
      const targetTokens = new Set(tokenizeForEpoch(`${entry.name} ${title}`));
      const matches = [...queryTokens].filter((token) => targetTokens.has(token)).length;
      return { path: entry.name, matches, ratio: matches / queryTokens.size };
    })
    .filter((entry): entry is { path: string; matches: number; ratio: number } => entry !== undefined)
    .filter((entry) => entry.matches >= 3 && entry.ratio >= 0.5)
    .sort((a, b) => b.matches - a.matches || b.ratio - a.ratio || a.path.localeCompare(b.path));
  if (ranked.length === 0) return undefined;
  if (ranked.length > 1 && ranked[0]!.matches === ranked[1]!.matches && ranked[0]!.ratio === ranked[1]!.ratio) {
    return undefined;
  }
  return ranked[0]!.path;
}

// ---------------------------------------------------------------------------
// Build partial pack from abstain candidateDetails (surface-grouped).
// ---------------------------------------------------------------------------

const SURFACE_ROLE_PRIORITY: Record<string, number> = {
  contract: 0,
  api: 1,
  domain: 2,
  data: 3,
  ui: 4,
  style: 5,
  config: 6,
  test: 7,
  doc: 8,
  unknown: 9,
};

// ---------------------------------------------------------------------------
// C2 helpers: multi-surface detection + bounded per-role candidate cap.
//
// A single-target task keeps exactly one candidate per role. A multi-surface
// task (several distinct concerns joined in one query) may keep a few
// high-confidence same-role candidates so a multi-file fix does not collapse
// to a single surface. Everything stays bounded (MAX_SURFACES_DISTINCT + byte
// cap) and deterministic.
// ---------------------------------------------------------------------------

/** Per-role candidate cap for a multi-surface partial pack. */
const PER_ROLE_CAP_DEFAULT = 2;
const PER_ROLE_CAP_TOP = 3;

/**
 * Heuristic: does the query describe more than one distinct change? Split on
 * clause separators (commas, "and"/"plus"/"then", semicolons) and count
 * substantial clauses. Deliberately domain-agnostic — it keys off sentence
 * shape, not project vocabulary. Requires several words overall so a short
 * two-word query ("rename foo") is never treated as multi-surface.
 */
function isMultiSurfaceQuery(query: string): boolean {
  const trimmed = query.trim();
  if (trimmed.split(/\s+/).length < 6) return false;
  const clauses = trimmed
    .split(/\s*(?:,|;|\band\b|\bplus\b|\bthen\b|\bas well as\b)\s*/i)
    .map((c) => c.trim())
    .filter((c) => c.split(/\s+/).length >= 2);
  return clauses.length >= 2;
}

/** Max candidates to keep for `role`; the highest-priority role gets one more. */
function perRoleCap(role: string, topRole: string | undefined): number {
  return role === topRole ? PER_ROLE_CAP_TOP : PER_ROLE_CAP_DEFAULT;
}

/**
 * A candidate detail carrying its ORIGINAL position in the locator's ranked
 * output. The locator already ranks candidates best-first; that ordering
 * encodes signal (name matches, symbol hits, scope proximity) that must not be
 * thrown away when the pack breaks confidence ties. Path-alphabetical is only a
 * last-resort determinism fallback for candidates that also share a rank.
 */
type RankedDetail = LocateCandidateDetail & { __rank: number };

/**
 * Deterministic ranking within a role:
 *   1. confidence desc (primary signal),
 *   2. locator rank asc (preserve the locator's own best-first ordering on
 *      equal confidence — dropping this discarded the locator's ranking and
 *      let alphabetically-earlier-but-less-relevant files win the slot),
 *   3. path asc (final determinism fallback only for identical rank+confidence).
 */
function compareDetails(a: RankedDetail, b: RankedDetail): number {
  const confidenceDiff = b.confidence - a.confidence;
  if (Math.abs(confidenceDiff) > 0.001) return confidenceDiff;
  if (a.__rank !== b.__rank) return a.__rank - b.__rank;
  return a.path < b.path ? -1 : a.path > b.path ? 1 : 0;
}

function uniqueExplicitIdentityCandidate<
  T extends { path: string; symbol?: string; confidence?: number },
>(candidates: T[], query: string): { candidate: T; exactSymbol: boolean } | undefined {
  const explicitIdentities = new Set(explicitCodeIdentifiers(query).map(normIdent));
  if (explicitIdentities.size === 0) return undefined;
  const matches = candidates
    .map((candidate, index) => {
      const symbol = normIdent(candidate.symbol ?? "");
      const basename = normIdent(path.basename(candidate.path, path.extname(candidate.path)));
      const exactSymbol = symbol.length > 0 && explicitIdentities.has(symbol);
      const exactBasename = basename.length > 0 && explicitIdentities.has(basename);
      return { candidate, index, exactSymbol, matches: exactSymbol || exactBasename };
    })
    .filter((entry) => entry.matches);
  const matchedPaths = [...new Set(matches.map((entry) => entry.candidate.path))];
  if (matchedPaths.length !== 1) return undefined;
  const best = matches
    .filter((entry) => entry.candidate.path === matchedPaths[0])
    .sort((a, b) =>
      Number(b.exactSymbol) - Number(a.exactSymbol)
      || (b.candidate.confidence ?? 0) - (a.candidate.confidence ?? 0)
      || a.index - b.index
    )[0]!;
  return { candidate: best.candidate, exactSymbol: best.exactSymbol };
}

async function buildPartialPack(
  details: LocateCandidateDetail[],
  args: TaskPackArgs,
  query: string,
  workspace: string,
): Promise<TaskPackResult> {
  // 1. Drop only truly unusable candidates (generated/build outputs).
  // A surface classification of "unknown" is NOT a reason to drop a
  // candidate — classifySurface's role heuristics are necessarily
  // incomplete, and a candidate the locator was confident enough to
  // surface is still useful for the agent to inspect even if this pack
  // cannot name its role. Dropping it here previously caused the pack to
  // report a real, already-found file as a "missing required surface" and
  // send the agent on an unnecessary extra locate/task_pack call.
  // Stamp each candidate with its ORIGINAL locator rank (best-first) BEFORE any
  // reordering below, so confidence ties preserve the locator's ranking instead
  // of collapsing to path-alphabetical (which dropped the top-ranked file).
  // Efficiency: one per-pack read/parse memo shared across every file-touching
  // step (widen/slice/outline/tiny-clamp/preferDefinitionSites) — the same
  // detail path is otherwise re-read and re-parsed at each of them.
  const cache = new FileReadCache();
  const ranked: RankedDetail[] = details.map((d, i) => ({ ...d, __rank: i }));
  const filtered = ranked.filter((d) => !/\/(dist|build|__pycache__|node_modules)\//.test(d.path));
  // Item 9: boost definition sites / demote pure-derivation sites before
  // picking the "best per role" candidate below.
  const boosted = preferDefinitionSites(workspace, query, filtered, cache);
  // Collapse duplicate candidateDetails that point at the SAME file (the
  // locator commonly returns the same path several times at different line
  // ranges). Keeping the single highest-confidence entry per path frees the
  // per-role budget for genuinely distinct files instead of spending it on
  // repeats of one file — important for multi-file (C2) fix packs.
  const bestByPath = new Map<string, RankedDetail>();
  for (const d of boosted) {
    const existing = bestByPath.get(d.path);
    if (!existing || d.confidence > existing.confidence) bestByPath.set(d.path, d);
  }
  const pool = [...bestByPath.values()];

  // A locator abstain can mix a strong code-identity hit with prose-only
  // decoys. For example, a user-spelled `(Compute)` symbol can lose to an API
  // error string that merely repeats the requested account-type names. When
  // exactly one distinct candidate path has an exact symbol/basename identity
  // match, ambiguity is resolved by repository identity rather than text
  // density. Re-enter the normal seeded pipeline so readiness, caps, handles,
  // and closure remain identical to an explicit caller-provided path.
  //
  // This deliberately does not guess when two paths define the same requested
  // identity: those remain a candidate-list for the caller to disambiguate.
  const identityMatch = uniqueExplicitIdentityCandidate(pool, query);
  if (identityMatch !== undefined) {
    const { candidate: matched, exactSymbol } = identityMatch;
    const result = await buildSeededTaskPack({
      ...args,
      path: undefined,
      symbol: undefined,
      paths: [{
        path: matched.path,
        ...(exactSymbol ? { symbol: matched.symbol } : {}),
      }],
    }, query, workspace);
    // buildSeededTaskPack captures the internal identity-seeded request.
    // Replace that cache key with the caller's original ambiguous/pathless
    // request so a retry remains a compact, stable pack-unchanged response.
    captureServedPack(workspace, args, computePackFingerprint(args, workspace), result);
    return result;
  }

  // 2. Group by surface role. A single-target task keeps exactly the best
  //    candidate per role (compact, handle-first). A MULTI-SURFACE task
  //    (e.g. "fix the checksum overflow AND the parser sign AND the scheduler
  //    mutex") legitimately needs several same-role files — three separate
  //    domain-logic fixes all classify as "domain", so a strict one-per-role
  //    map would surface only one of them and hide the rest. Keep up to a
  //    small, bounded number of high-confidence candidates per role in that
  //    case (C2), still capped overall by MAX_SURFACES_DISTINCT and the pack
  //    byte cap. Selection is deterministic: confidence desc, then path asc.
  const multiSurface = isMultiSurfaceQuery(query);
  const byRoleList = new Map<string, RankedDetail[]>();
  for (const d of pool) {
    const list = byRoleList.get(d.surface);
    if (list) list.push(d);
    else byRoleList.set(d.surface, [d]);
  }

  // 3. Sort roles by priority, and within each role keep up to K candidates.
  const sortedRoles = [...byRoleList.keys()].sort(
    (a, b) => (SURFACE_ROLE_PRIORITY[a] ?? 9) - (SURFACE_ROLE_PRIORITY[b] ?? 9)
  );
  const topRole = sortedRoles[0];
  const sortedEntries: Array<[string, RankedDetail]> = [];
  for (const role of sortedRoles) {
    const perRole = multiSurface ? perRoleCap(role, topRole) : 1;
    const ranked = [...(byRoleList.get(role) ?? [])].sort(compareDetails);
    for (const detail of ranked.slice(0, perRole)) {
      sortedEntries.push([role, detail]);
    }
  }

  // 4. Build surfaces, capped at MAX_SURFACES_DISTINCT.
  const surfaces: TaskPackSurface[] = [];
  const coveredRoles = new Set<string>();
  const seenPaths = new Set<string>();
  // DESIGN-v0.8 coverage-honesty: scope ui/style requirement to the abstain
  // candidates' dominant root (the located set for this partial pack).
  const requiredRoles = requiredSurfacesForTask(
    query, workspace, args.surfaceRoles, details.map((d) => d.path),
  );

  // -------------------------------------------------------------------------
  // V10-09 SEAM (TL_COVERAGE_PACKER, default OFF) — the ONE place in this file
  // where a ranked candidate list becomes the served set.
  //
  // OFF: `sortedEntries` above is served verbatim, so the pack stays
  // byte-identical to the relevance-first behaviour the wire baselines and the
  // replay corpus pin. There is no second branch anywhere downstream.
  //
  // ON: the same pool is re-selected for coverage-per-token by
  // `coveragePacker.ts`, which consumes this task's own state DIRECTLY — the
  // query's concern clauses, the cross-call functional-validation obligation,
  // the served-range ledger, and the required roles computed just above. It
  // never reads the advisory reasoning IR. Selection happens BEFORE the
  // downstream budget ladder and does not touch its per-kind required sets.
  // -------------------------------------------------------------------------
  let packedEntries = sortedEntries;
  if (coveragePackerEnabled()) {
    const concernGroups = concernGroupsForQuery(query);
    const concernVocabulary = [...new Set(concernGroups.flatMap((group) => group.tokens))];
    const validation = getFunctionalValidationObligation(workspace, tokenizeForEpoch(query));
    const requestedPaths = [
      ...(args.path !== undefined ? [args.path] : []),
      ...(args.paths ?? []).map((entry) => (typeof entry === "string" ? entry : entry.path)),
    ];
    const queryLower = query.toLowerCase();
    // Shared by both the v1 and v2 (V11-03) branches below — same projection,
    // same base context, so the two selections stay directly comparable and
    // this seam's diff stays a branch, not a duplicated pipeline.
    const coverageProjection = {
      path: (d: RankedDetail) => d.path,
      role: (d: RankedDetail) => d.surface,
      rank: (d: RankedDetail) => d.__rank,
      confidence: (d: RankedDetail) => d.confidence,
      bytes: (d: RankedDetail) => estimateBodyBytes(d.range, cache.read(workspace, d.path), MAX_SURFACE_CODE_BYTES),
      // A direct reference: the caller named the path, or the query names the
      // file itself. Both are shed-forbidden at pack level.
      direct: (d: RankedDetail) => requestedPaths.includes(d.path)
        || queryLower.includes(path.basename(d.path).toLowerCase())
        || queryLower.includes(path.basename(d.path, path.extname(d.path)).toLowerCase()),
      priorServed: (d: RankedDetail) => {
        const span = parseSurfaceSpan(d.range);
        return span !== undefined
          && !servedFindWindowHasUnservedLines(workspace, d.path, span.start, span.end);
      },
      concernTokens: (d: RankedDetail) => {
        const haystack = `${d.path} ${d.symbol ?? ""} ${d.why}`.toLowerCase();
        return concernVocabulary.filter((token) => haystack.includes(token.toLowerCase()));
      },
      // V11-03: symbol name feeds coveragePackerV2's concern classification
      // and overload dedup-exemption inference. No effect on the v1 call
      // below — v1's projection type does not read this field.
      symbol: (d: RankedDetail) => d.symbol,
    };
    const coverageBaseContext = {
      requiredRoles,
      requiredPaths: [
        ...requestedPaths,
        ...(validation !== undefined ? [validation.targetPath] : []),
      ],
      concerns: concernGroups.map((group, index) => ({ id: `concern-${index + 1}`, tokens: group.tokens })),
      byteBudget: MAX_SURFACE_CODE_BYTES * 2,
      hasWriteFrontier: hasRequestedMutationIntent(query),
    };
    const functionalValidationObligation = validation !== undefined
      ? [{ id: "functional-validation", open: true, required: true, paths: [validation.targetPath] }]
      : [];
    // V11-03 (requires TL_COVERAGE_PACKER also on — see util/flags.ts's
    // "V11-03 addendum"): coveragePackerV2 additionally folds in the OPEN
    // change_contract obligations the SECOND seam (dedupeTrimAndPersist,
    // below) recorded for this task from a PRIOR pack, via priorPackStore.
    // This is the read half of the obligation-threading loop the second seam
    // writes; a first pack in a task (nothing recorded yet) sees an empty
    // list here and behaves exactly like a fresh v2 selection.
    if (coveragePackerV2Enabled()) {
      const priorObligations: CoverageObligationV2[] = queryPriorPackObligations(
        workspace, tokenizeForEpoch(query),
      ).map((o) => ({
        id: `prior:${o.id}`,
        open: o.open,
        required: o.required,
        roles: [o.role],
        paths: [o.path],
      }));
      const packedV2 = selectCoverageOrderedEntriesV2(pool, coverageProjection, {
        ...coverageBaseContext,
        obligations: [...functionalValidationObligation, ...priorObligations],
      });
      if (packedV2.entries.length > 0) packedEntries = packedV2.entries;
      // Trace-only (util/trace.ts self-gates on TL_TRACE) — never wired, per
      // coveragePackerV2.ts's module header and this workstream's brief.
      trace("coverage_packer_v2_decision", {
        level: packedV2.output.level,
        stop_reason: packedV2.output.stopReason,
        fallback_to_v1: packedV2.output.fallbackToV1,
        body_count: packedV2.output.body.length,
        inventory_count: packedV2.output.inventory.length,
        concern_coverage: packedV2.output.concernCoverage,
        complementarity_promotions: packedV2.output.complementarityPromotions.length,
        prior_obligations: priorObligations.length,
      }, workspace);
    } else {
      const packed = selectCoverageOrderedEntries(pool, coverageProjection, {
        ...coverageBaseContext,
        obligations: functionalValidationObligation,
      });
      // An empty selection would be a silently emptied pack; fall back to the
      // relevance-first order rather than serve nothing.
      if (packed.entries.length > 0) packedEntries = packed.entries;
    }
  }

  // Convert each selected (role, detail) pair to an ImpactCandidate and
  // delegate to the SAME candidateToSurface used by buildSeededTaskPack,
  // instead of re-implementing range widening, handle minting, the embed
  // gate, likely_edits, and outline inline a second time (this path and
  // buildSeededTaskPack's had already drifted once — the B4 tiny-file
  // clamp needed patching in both independently). LocateCandidateDetail has
  // no `required` field of its own; candidateToSurface derives `required`
  // from `requiredRoles.includes(candidate.surface)` (OR'd with
  // `candidate.required`, left unset here), which is exactly this loop's
  // own `required` calculation, so passing requiredRoles through has the
  // identical effect.
  for (const [role, detail] of packedEntries) {
    if (surfaces.length >= MAX_SURFACES_DISTINCT) break;
    // Multiple same-role candidates may share a path (e.g. two hits in the
    // same file at different ranges); keep the pack's one-surface-per-path
    // invariant regardless of the per-role expansion above.
    if (seenPaths.has(detail.path)) continue;
    seenPaths.add(detail.path);
    const candidate: ImpactCandidate = {
      path: detail.path,
      line: detail.line,
      range: detail.range,
      ...(detail.symbol ? { symbol: detail.symbol } : {}),
      surface: role as ImpactSurface,
      why: detail.why,
      confidence: detail.confidence,
      handle: detail.handle,
    };
    surfaces.push(await candidateToSurface(candidate, workspace, requiredRoles, query, cache));
    coveredRoles.add(role);
  }
  const protectedSurfaces = new Set<TaskPackSurface>();
  // B1a (2026-08-01 retrieval-scope): anchors = the abstain candidates this
  // pack was built from, snapshotted BEFORE any augmenter appends its own pick.
  const partialAnchorPaths = surfaces.map((s) => s.path);
  await augmentConcernGroupSurfaces(surfaces, query, workspace, requiredRoles, cache, protectedSurfaces);
  await augmentNativeSurfaceClosure(surfaces, workspace, query, requiredRoles, cache);
  await augmentNativeWiringSurface(surfaces, workspace, cache);
  await augmentNativeIdentifierCooccurrenceSurface(surfaces, workspace, query, requiredRoles, cache);
  // Item 1: resolve a REQUIRED "style" role to a NAMED surface (handle + code).
  await augmentStyleSurface(surfaces, query, workspace, requiredRoles, cache, partialAnchorPaths);
  // FIX-B2: attempt every OTHER still-missing required role in this SAME
  // call — see augmentMissingRequiredRoleSurfaces's doc comment for the
  // surfaceRoles-retry root cause this closes.
  await augmentMissingRequiredRoleSurfaces(surfaces, query, workspace, requiredRoles, cache, undefined, partialAnchorPaths);
  // B1c (2026-08-01 retrieval-scope): the abstain-candidate flow is exactly the
  // one that ships candidate LISTS, so a file the query names by name must be
  // in that list content-bearing — see augmentQueryNamedFileSurfaces (T05c).
  await augmentQueryNamedFileSurfaces(surfaces, query, workspace, requiredRoles, cache, protectedSurfaces);
  for (const surface of surfaces) {
    coveredRoles.add(surface.role);
  }
  // DESIGN-v0.8 coverage-honesty (Part 5 B-1): promote a config/data/doc
  // surface whose already-read content already names the enum's new value.
  promoteVariantSurfaces(surfaces, query, workspace, cache);
  finalizePrimarySurface(surfaces);

  // 5. Determine missing roles.
  // `missing` uses a broader heuristic (fall back to contract/api/domain when
  // nothing was explicitly required) so coverage can still read "partial" when
  // a common role is uncovered. But the REQUIRED framing must stay
  // self-consistent: missing_required_surfaces must always be a subset of
  // required_surfaces. When requiredRoles is empty, nothing is required, so
  // missing_required_surfaces must be empty too — reporting
  // required_surfaces:[] alongside missing_required_surfaces:["api"] is a
  // contradiction (the field claims "api" is required-and-missing when no
  // role was required at all).
  const allExpected = requiredRoles.length > 0 ? requiredRoles : ["contract", "api", "domain"];
  const missing = allExpected.filter((r) => !coveredRoles.has(r));
  const missingRequired = missing.filter((r) => requiredRoles.includes(r));

  // DESIGN-v0.8 coverage-honesty: this path was reached via the locator's
  // AMBIGUOUS abstain — the primary target was never confidently resolved, so
  // the pack is always "partial", never "complete"/"focused" (it stopped
  // claiming complete-when-nothing-missing, which was the self-contradiction
  // the taxonomy closes). The reason names WHAT is unresolved, in the same
  // precedence buildRoute itself uses so the label and route agree:
  //   missing required role → "missing-roles" (route: locate_missing_surfaces)
  //   else unmatched concern → "concerns-uncovered" (route: locate_missing_surfaces)
  //   else → "candidate-list" (route: confirm_candidates), UNLESS one ranked
  //     surface strictly dominates the rest on role/evidence/source-ness (see
  //     `resolveDominantSurfaceIndex`'s doc comment, issue #3) — a genuinely
  //     resolved choice is not an open one. The reason is then dropped
  //     (coverage stays "partial", never "focused"/"complete": those would
  //     suppress `frontier_index`, and the dominated candidate must stay
  //     inventoried) and `buildRoute` falls through past its "candidate-list"
  //     branch to the plain partial-coverage route below, unmodified.
  const unmatchedConcern = unmatchedConcernTokens(query, surfaces);
  const coverageStr = "partial" as const;
  const dominantAbstainSurface = missingRequired.length === 0 && unmatchedConcern.length === 0
    ? resolveDominantSurfaceIndex(surfaces)
    : -1;
  const coverageReason: TaskPackResult["coverage_reason"] =
    missingRequired.length > 0
      ? "missing-roles"
      : unmatchedConcern.length > 0
        ? "concerns-uncovered"
        : dominantAbstainSurface !== -1
          ? undefined
          : "candidate-list";

  // A4: route now depends on coverage/allMissing/query, so it can flip to
  // locate_missing_surfaces on unmatched CONCERN tokens (or confirm_candidates
  // on a candidate-list) even though `next` below points at the returned
  // handles first — blocking_next_steps carries the per-token follow-up when
  // the flip fires.
  const route = buildRoute(surfaces, missingRequired, coverageStr, missing, query, coverageReason);
  // 6. Build next hint — coverage-driven (candidate-list batches the ranked
  // handles; a missing-roles flip re-locates the role; concerns-uncovered
  // finds the token), falling back to a bare locate only when no surface
  // exists at all.
  const next = nextHintForCoverage(
    surfaces, coverageStr, coverageReason, query, missingRequired, unmatchedConcern,
  ) ?? `search_files action=locate query="${continuationQuery(query)}"`;
  // blocking_next_steps was previously ALWAYS dropped here (item 3): route
  // honesty (A4) now populates it, but ONLY when the route actually flipped
  // to locate_missing_surfaces AND there is real per-token content (the
  // flip can also come from missingRequired alone, in which case
  // unmatchedConcernTokens is empty and a vacuous [] must not serialize).
  const blockingNextSteps = route.action === "locate_missing_surfaces"
    ? buildBlockingNextSteps(query, surfaces)
    : [];

  const { lines: checkLines, records: checkRecords } = buildCompletionChecks(query, surfaces, missing, workspace, unmatchedConcern);
  const result: TaskPackResult = {
    mode: "task_pack",
    coverage: coverageStr,
    coverage_reason: coverageReason,
    surfaces,
    missing,
    required_surfaces: requiredRoles,
    ...(missingRequired.length > 0 ? { missing_required_surfaces: missingRequired } : {}),
    route,
    ...(blockingNextSteps.length > 0 ? { blocking_next_steps: blockingNextSteps } : {}),
    checks: checkLines,
    verify: discoverVerificationHints(workspace, query, surfaces),
    next,
  };
  if (dominantAbstainSurface !== -1) {
    applyDominancePromotion(result, surfaces, dominantAbstainSurface, workspace);
  }

  attachPartialTree(result, workspace);
  return dedupeTrimAndPersist(workspace, result, {
    ...(protectedSurfaces.size > 0 ? { protectedSurfaces } : {}),
    query,
    checkRecords,
    args,
    // DESIGN-v0.9 §4.3: recompute next/route if trimToCap flips coverage.
    recompute: { query, missingRequired, unmatchedConcern },
    // DESIGN-v0.9 §4.6b: share the pack's read cache for codeless-surface inlining.
    cache,
  });
}

// ---------------------------------------------------------------------------
// Candidate selection: preserve role diversity, then keep same-role closure
// slots that usually represent distinct work surfaces (admin aggregation,
// repository/search, transport/sender, etc.).
// ---------------------------------------------------------------------------

// ---------------------------------------------------------------------------
// Item 9: for enum/value-addition tasks, prefer the DEFINITION site (and
// its exhaustive initializers) over sites that merely DERIVE from the
// definition and need no edit when a new value is added.
// ---------------------------------------------------------------------------

/**
 * Generic "this line is a pure derivation, not a definition" shape check.
 * Matches constructs that automatically reflect a new enum/const-object
 * value without any code change: Object.values(...)/Object.keys(...) over
 * the enum, `keyof typeof X`, a type inferred FROM a schema
 * (`z.infer<typeof X>` and equivalents), or a bare re-export/import of the
 * symbol. These are language-shape patterns (mostly TS/JS, since that is
 * where "derive a schema/type from a const object" is idiomatic), not any
 * project's specific symbol names.
 */
function looksLikeDerivedFromDefinition(line: string): boolean {
  return /Object\.(values|keys|entries)\s*\(|keyof\s+typeof\b|\.infer<|z\.nativeEnum\(|^\s*export\s+type\s+\w+\s*=\s*\(typeof\b|^\s*export\s*\{[^}]*\}\s*from\b|^\s*import\s+.*\bfrom\b/.test(line);
}

/**
 * Generic "this line/block DEFINES the enum/value family" shape check:
 * an actual enum keyword, or an object/array literal assignment that is
 * about to enumerate members (the exhaustive-initializer shape). Matches
 * across TS/JS const-object enums, native `enum`, Python Enum classes, and
 * Java/Kotlin/C++ enum declarations — again purely by syntax shape.
 */
function looksLikeDefinitionSite(line: string): boolean {
  return /^\s*export\s+(const|enum)\s+\w+\s*(:|=|\{)|^\s*enum\s+(class\s+)?\w+\s*\{|^\s*class\s+\w+\s*\(\s*Enum\s*\)\s*:/.test(line);
}

/**
 * Read the candidate's own line (1-based) from disk and classify it as a
 * definition site, a derived site, or neither. Returns 0 (neutral), +1
 * (definition — boost), or -1 (pure derivation — demote). Best-effort: any
 * I/O failure or out-of-range line returns neutral rather than throwing.
 */
function definitionVsDerivedAdjustment(workspace: string, relPath: string, line: number, cache?: FileReadCache): number {
  const raw = readCached(workspace, relPath, cache);
  if (raw === undefined) return 0;
  const lines = raw.split(/\r?\n/);
  const idx = line - 1;
  if (idx < 0 || idx >= lines.length) return 0;
  const target = lines[idx]!;
  if (looksLikeDefinitionSite(target)) return 1;
  if (looksLikeDerivedFromDefinition(target)) return -1;
  return 0;
}

/**
 * For enum-like queries, adjust each candidate's confidence so that a
 * DEFINITION site consistently outranks a DERIVED site that happens to
 * share the same surface role and text-match the query — without this,
 * `z.enum(Object.values(TicketPriority))` (which needs no edit at all when
 * a value is added) can outscore the actual `enums.ts` declaration purely
 * because the query's literal tokens appear more densely near the derived
 * usage site.
 */
function preferDefinitionSites<T extends { path: string; line: number; symbol?: string; confidence: number }>(
  workspace: string,
  query: string,
  candidates: T[],
  cache?: FileReadCache,
): T[] {
  if (!isEnumLikeQuery(query)) return candidates;
  return candidates.map((c) => {
    if (c.symbol) return c; // exact symbol hits are already precise; leave as-is
    const adjustment = definitionVsDerivedAdjustment(workspace, c.path, c.line, cache);
    if (adjustment === 0) return c;
    const delta = adjustment > 0 ? 0.25 : -0.35;
    return { ...c, confidence: Math.max(0, Math.min(1, c.confidence + delta)) };
  });
}

function candidatePathKey(c: ImpactCandidate): string {
  return c.path;
}

// Generic architectural-role vocabulary only — deliberately excludes any
// domain-specific protocol/algorithm acronym (e.g. a wire-protocol name or
// an estimator algorithm's initialism), which would only ever match one
// project's vocabulary instead of a general directory/file naming pattern.
function closureSlot(c: ImpactCandidate): string {
  const base = path.basename(c.path).toLowerCase();
  const dir = path.dirname(c.path).toLowerCase();
  if (/(admin|stats|aggregate)/.test(base + dir)) return `${c.surface}:admin`;
  if (/(search|query|filter)/.test(base + dir)) return `${c.surface}:search`;
  if (/(repository|repo|store)/.test(base + dir)) return `${c.surface}:repository`;
  if (/(transition|state|lifecycle)/.test(base + dir)) return `${c.surface}:state`;
  if (/(route|controller|handler)/.test(base + dir)) return `${c.surface}:route`;
  return `${c.surface}:${base.replace(/\.[^.]+$/, "")}`;
}

function isClosureImportant(c: ImpactCandidate): boolean {
  // "import-edge": the definition the primary DELEGATES TO (locator issue #2).
  // It shares the primary's role by construction — a callee usually sits in
  // its caller's layer — so the role-diversity pass above can never reach it,
  // and without this it would be found and then silently dropped.
  if (["structural", "enum-family", "presentation-family", "import-edge"].some((w) => c.why.includes(w))) {
    return true;
  }
  return /(admin|stats|aggregate|search|filter|repository|transition|lifecycle|route|controller|handler)/i.test(c.path);
}

function compareCandidates(a: ImpactCandidate, b: ImpactCandidate): number {
  const roleDiff = (SURFACE_ROLE_PRIORITY[a.surface] ?? 9) - (SURFACE_ROLE_PRIORITY[b.surface] ?? 9);
  if (roleDiff !== 0) return roleDiff;
  const confidenceDiff = b.confidence - a.confidence;
  if (Math.abs(confidenceDiff) > 0.001) return confidenceDiff;
  return a.path < b.path ? -1 : a.path > b.path ? 1 : 0;
}

function selectClosureCandidates(
  allCandidates: ImpactCandidate[],
  primaryPaths: readonly string[] = [],
): ImpactCandidate[] {
  const sorted = [...allCandidates].sort(compareCandidates);
  const selected: ImpactCandidate[] = [];
  const selectedPaths = new Set<string>();
  const selectedRoles = new Set<string>();
  const selectedSlots = new Set<string>();

  function add(c: ImpactCandidate): void {
    if (selected.length >= MAX_SURFACES_DISTINCT) return;
    const pathKey = candidatePathKey(c);
    if (selectedPaths.has(pathKey)) return;
    selected.push(c);
    selectedPaths.add(pathKey);
    selectedRoles.add(c.surface);
    selectedSlots.add(closureSlot(c));
  }

  // R0 (2026-08-21, W4-A pack-augmentation-pollution forensics): a candidate
  // reached only through weak evidence (SIBLING_SEED_WEAK_WHY — a plain
  // full-text/variant/reference hit, never a targeted symbol/name/filename
  // match) AND sitting outside the primary's own project root must not win a
  // surface ROLE SLOT merely because nothing else offers that role. Before
  // this fix, the diversity passes below picked the best-scoring candidate
  // per role/slot unconditionally — including one the out-of-root demotion
  // pass in locateTaskContext.ts had already driven to (near) zero
  // confidence. "One representative of every role" is not evidence the role
  // is genuinely covered; an honestly-missing role (surfaced downstream by
  // requiredSurfacesForTask/missing_required_surfaces) is the correct
  // outcome when only worthless evidence exists for it. Scoped to weak AND
  // out-of-root together, never either alone, so a genuine cross-package
  // symbol/filename hit still fills a role the primary's own root lacks.
  const scopePrefixes = roleSearchScopePrefixes(primaryPaths);
  const isTrustworthyRoleFiller = (c: ImpactCandidate): boolean =>
    !SIBLING_SEED_WEAK_WHY.has(c.why) || isWithinRoleSearchScope(c.path, scopePrefixes);

  for (const c of sorted) {
    if (!selectedRoles.has(c.surface) && isTrustworthyRoleFiller(c)) add(c);
  }

  for (const c of sorted) {
    if (selected.length >= MAX_SURFACES_DISTINCT) break;
    const slot = closureSlot(c);
    if (!selectedSlots.has(slot) && isClosureImportant(c) && isTrustworthyRoleFiller(c)) add(c);
  }

  return selected;
}

function mergeUnique(left: string[], right: string[]): string[] {
  return [...new Set([...left, ...right].filter((v) => v.length > 0))];
}

// ---------------------------------------------------------------------------
// A3 item 6: never require (or later report missing) a surface role that
// does not structurally exist in this workspace. A C++ firmware repo has no
// "ui"/"style" role at all — demanding those surfaces (and then telling the
// agent to burn 2-3 more task_pack calls locating them) is actively wrong
// guidance, independent of what the query happens to say.
// ---------------------------------------------------------------------------

/**
 * DESIGN-v0.8 coverage-honesty: the surface-role inventory of a workspace (or
 * one project root within it), computed from a SINGLE style-inclusive walk and
 * MEMOIZED per (workspace, root) key. This kills the double full-walk
 * requiredSurfacesForTask used to pay (two `workspaceHasRole` calls, each a
 * fresh whole-tree walk) and, when a root is supplied, scopes the "does this
 * role exist" answer to the candidates' OWN root so host-repo CSS cannot make
 * "style" look present for a firmware-fixture task.
 *
 * The cache is module-level and keyed by the exact realpath+root string; a
 * single pack build reuses one walk across every requiredSurfacesForTask /
 * workspaceHasRole call. Not test-reset-sensitive: a fixture written under a
 * fresh tmpdir has a distinct key, and role membership for a given directory
 * tree does not change within a process's lifetime in practice — but the
 * exported hook below clears it so a test that mutates a reused path in place
 * still sees fresh inventory.
 */
const roleInventoryCache = new Map<string, ReadonlySet<ImpactSurface>>();

/** Test-only reset hook: clear the memoized per-(workspace,root) role inventory. */
export function resetRoleInventoryCache(): void {
  roleInventoryCache.clear();
}

/**
 * Role inventory for `workspace` scoped to `root` (a `projectRootOf` value, or
 * `null` for the whole workspace). One style-inclusive walk per distinct
 * (workspace, root) key, memoized. The `.css/.scss/.less` extras mirror the
 * old workspaceHasRole trick — the default code-file walk excludes them, so a
 * real stylesheet would otherwise be invisible to a "style" role check.
 */
function roleInventoryFor(workspace: string, root: string | null): ReadonlySet<ImpactSurface> {
  const key = workspace + "|" + (root ?? "__ALL__");
  const hit = roleInventoryCache.get(key);
  if (hit !== undefined) return hit;
  const allPaths = walkCodeFiles(workspace, { extraExts: [".css", ".scss", ".less"] }).map((f) => f.relPath);
  const rootIsMarker = root !== null && allPaths.some((candidatePath) => projectRootOf(candidatePath) === root);
  const inventoryPaths = root !== null && !rootIsMarker
    ? allPaths.filter((candidatePath) => isUnderAncestor(candidatePath, root))
    : allPaths;
  const inv = surfaceInventory(
    inventoryPaths,
    projectRootOf,
    rootIsMarker ? root : null,
    (p) => classifySurface(p),
  );
  roleInventoryCache.set(key, inv);
  return inv;
}

/**
 * PREFIX-scoped role inventory — roleInventoryFor's sibling for a scope that
 * is a common-ancestor DIRECTORY rather than a marker root. rootOf-equality
 * would match nothing under an ancestor, while the prefix test also counts
 * files in NESTED sibling roots — exactly what a cross-subpackage required-
 * role fill needs (a monorepo's "contract" role usually lives in a sibling
 * package of the located surfaces). `scope === ""` is the whole workspace.
 */
function roleInventoryUnder(workspace: string, scope: string): ReadonlySet<ImpactSurface> {
  const key = workspace + "|prefix:" + scope;
  const hit = roleInventoryCache.get(key);
  if (hit !== undefined) return hit;
  const inv = surfaceInventory(
    walkCodeFiles(workspace, { extraExts: [".css", ".scss", ".less"] })
      .map((f) => f.relPath)
      .filter((p) => isUnderAncestor(p, scope)),
    projectRootOf,
    null,
    (p) => classifySurface(p),
  );
  roleInventoryCache.set(key, inv);
  return inv;
}

/**
 * The directory scope a required-role FILL may search (09e pollution
 * hardening — see augmentMissingRequiredRoleSurfaces):
 *   - every surface in ONE root        → that root ("" = single-project
 *     workspace, where an unscoped fill is the legitimate behavior);
 *   - surfaces span SEVERAL roots      → their common-ancestor directory
 *     (the project-family root, e.g. the fixture/monorepo dir above its
 *     subpackages) — never the whole workspace;
 *   - no surfaces / no common ancestor → null: the caller must SKIP the
 *     fill, because with no anchor at all any pick is a cross-project guess.
 */
export function roleFillScope(surfaces: TaskPackSurface[]): string | null {
  if (surfaces.length === 0) return null;
  const roots = new Set(surfaces.map((s) => projectRootOf(s.path)));
  if (roots.size === 1) return [...roots][0]!;
  const ca = commonAncestorDir(surfaces.map((s) => s.path));
  return ca !== "" ? ca : null;
}

/**
 * B1a (2026-08-01 retrieval-scope): the role/style/test search scope. ONE
 * definition, shared with the locator's own family scans — see
 * roleSearchScopePrefixes in features/locator/locateTaskContext.ts for the
 * contract and the T09 forensics that motivate it. Re-exported here because
 * this module is where the pack-layer tests reach for it.
 */
export { roleSearchScopePrefixes };

/** True when `relPath` lies inside ANY of `prefixes` (see isWithinRoleSearchScope). */
function isUnderAnyPrefix(relPath: string, prefixes: readonly string[] | null | undefined): boolean {
  return isWithinRoleSearchScope(relPath, prefixes);
}

/**
 * B1a (2026-08-01 retrieval-scope): DIRECTORIES the query literally names.
 *
 * A request that says "packages/shared 配下の…" has told the server exactly
 * where to look, but nothing consumed that: inferQueryProjectScope matches
 * whole PROJECT-ROOT names only and its stop-token list deliberately drops
 * generic segments ("packages", "shared", "apps"), so a multi-segment directory
 * spelled in the query bound no scope at all. Any workspace directory whose
 * relative path ENDS WITH the named segment sequence resolves it, so the caller
 * may spell either the workspace-relative path or the tail they know.
 *
 * Bounded and conservative: at most MAX_QUERY_NAMED_DIR_SCOPES resolved
 * directories, only multi-segment names (a bare "src" is not a location), and —
 * when `confinement` is supplied (caller `paths`) — only directories inside it.
 */
const MAX_QUERY_NAMED_DIR_SCOPES = 4;

export function queryNamedDirectoryScopes(
  query: string,
  workspace: string,
  confinement?: readonly string[],
): string[] {
  if (query.trim().length === 0) return [];
  const named = [...new Set(
    (query.match(/[A-Za-z0-9_.-]+(?:\/[A-Za-z0-9_.-]+)+/g) ?? [])
      .map((token) => token.replace(/^\.\//, "").replace(/\/+$/, ""))
      .filter((token) => token.includes("/") && !/\.[A-Za-z0-9]+$/.test(token)),
  )];
  if (named.length === 0) return [];
  // Directory inventory derived from the SAME walk the rest of the pack uses,
  // so an ignored/out-of-scope tree can never be named into existence.
  const dirs = new Set<string>();
  for (const file of walkCodeFiles(workspace, {})) {
    let dir = path.posix.dirname(file.relPath);
    while (dir !== "" && dir !== ".") {
      if (dirs.has(dir)) break;
      dirs.add(dir);
      dir = path.posix.dirname(dir);
    }
  }
  const resolved: string[] = [];
  for (const token of named) {
    for (const dir of [...dirs].sort()) {
      if (dir !== token && !dir.endsWith(`/${token}`)) continue;
      if (confinement !== undefined && confinement.length > 0 && !isUnderAnyPrefix(dir, confinement)) continue;
      if (!resolved.includes(dir)) resolved.push(dir);
      if (resolved.length >= MAX_QUERY_NAMED_DIR_SCOPES) return resolved;
    }
  }
  return resolved;
}

/**
 * B1a (2026-08-01 retrieval-scope): the final role/style/test search scope —
 * the anchor cluster's own prefixes UNIONED with every directory the query
 * literally names (and, when the caller supplied paths, confined to them).
 * Returns null only when neither source yields a scope, i.e. "skip the fill".
 */
function roleSearchScope(
  anchorPaths: readonly string[],
  query: string,
  workspace: string,
  callerScopes?: readonly string[],
): string[] | null {
  const anchorPrefixes = roleSearchScopePrefixes(anchorPaths);
  const named = queryNamedDirectoryScopes(query, workspace, callerScopes);
  const union = [...new Set([...(anchorPrefixes ?? []), ...named])];
  if (union.length === 0) return anchorPrefixes;
  // A "" prefix (single-project workspace) already covers everything; keep it
  // alone rather than pretending the named directories narrowed anything.
  if (union.includes("")) return [""];
  return union.sort();
}

/**
 * True when at least one file in the workspace classifies to `role`. Now
 * backed by the memoized whole-workspace inventory (one walk, reused) instead
 * of a fresh early-exit walk per call. Whole-workspace scope is kept for this
 * shim's callers (buildCompletionChecks' phantom-role guard) — they reason
 * about "could this role exist anywhere here", not per-root presence.
 */
function workspaceHasRole(workspace: string, role: ImpactSurface): boolean {
  return roleInventoryFor(workspace, null).has(role);
}

/**
 * The enum/variant-shape keyword set the pack's role-derivation keys on
 * (mirrors queryShape.ts's isEnumLikeQuery vocabulary AND the locator's
 * MULTI_SURFACE_KEYWORDS). Kept as a token SET here so a camelCase-only query
 * ("TicketPriority byTicket") matches after tokenizeForEpoch splits the hump.
 */
const ENUM_SHAPE_TOKENS: ReadonlySet<string> = new Set(["enum", "priority", "role"]);

/**
 * Item 5 (pack-side camelCase-robust enum gate): a strict SUPERSET of the
 * imported `isEnumLikeQuery` that also fires on camelCase/snake/kebab identifier
 * forms. The imported classifier uses bare English word-boundary regexes
 * plus a guarded `status` case, and a word boundary does NOT fall
 * inside a camelCase hump — so "TicketPriority byTicket" (the live TP4
 * regression) matched nothing and the whole enum role/check derivation was
 * skipped. Tokenizing via `tokenizeForEpoch` (the SAME normalization the
 * locator's looksLikeMultiSurface now uses — camelCase/snake/kebab split +
 * lowercase) and matching the keyword SET makes the gate fire on those forms
 * while staying a strict superset: it can only ADD enum recognition (the
 * `isEnumLikeQuery` OR-arm preserves every case that already matched), never
 * remove one, so no role-derivation semantics change for queries that were
 * already recognized. Language-agnostic: Japanese task prose with English
 * identifiers still tokenizes the identifier and matches.
 */
function isEnumLikeQueryShaped(query: string): boolean {
  if (isEnumLikeQuery(query)) return true;
  for (const t of tokenizeForEpoch(query)) {
    if (ENUM_SHAPE_TOKENS.has(t)) return true;
  }
  return false;
}

/**
 * Required surface roles for a task.
 *
 * DESIGN-v0.8 coverage-honesty: ui/style are added only when they
 * STRUCTURALLY EXIST in the located candidates' own dominant project root
 * (`candidatePaths` → `projectRootOf` majority) — not merely somewhere in the
 * whole workspace, which let host-repo CSS make "style" required for a
 * firmware-fixture task. When no candidates are known yet (the pre-locate
 * abstain call), `candidatePaths` is empty and the inventory falls back to the
 * whole workspace (root=null), preserving the prior best-effort behavior.
 */
function requiredSurfacesForTask(
  query: string,
  workspace: string,
  explicit?: string[],
  candidatePaths?: ReadonlyArray<string>,
): string[] {
  // P0 defect 2 (requirement model shrinks with the query). Requirements are
  // MONOTONE within a task epoch: every role an EARLIER pack of this same task
  // already required stays required, so a narrowed continuation cannot certify
  // `coverage:"complete"` against a universe that shrank along with its own
  // query text. Union-only — this can add a requirement, never drop one — and
  // fail-closed: with no stored contract (cold store, queryless pack, restart)
  // `carried` is empty and the two exits below are exactly today's behavior.
  // Applied to the caller-supplied `explicit` branch too: a continuation that
  // forwards `surfaceRoles=["api"]` is narrowing the ASK, not retracting what
  // the epoch already established.
  const carried = queryTaskContract(workspace, tokenizeForEpoch(query))?.requiredRoles ?? [];
  if (explicit && explicit.length > 0) return mergeUnique(explicit.map(String), [...carried]);

  const required = new Set<ImpactSurface>();

  // Scope the ui/style presence check to the dominant root of the located
  // candidates when we have any; otherwise fall back to the whole workspace.
  const candidateRoots = candidatePaths && candidatePaths.length > 0
    ? [...new Set(candidatePaths.map((candidatePath) => projectRootOf(candidatePath)))]
    : [];
  const crossRootAncestor = candidatePaths && candidateRoots.length > 1
    ? commonAncestorDir(candidatePaths.map((candidatePath) => `${candidatePath}/__surface__`))
    : undefined;
  const root = candidatePaths && candidatePaths.length > 0
    ? candidateRoots.length === 1
      ? candidateRoots[0]!
      : crossRootAncestor || null
    : null;
  const inventory = roleInventoryFor(workspace, root);

  // Enum/variant-shaped tasks: a new value typically needs its contract
  // definition and the code paths that enumerate it updated. ui/style are
  // ONLY added when the root actually has presentation code — no
  // acronym/all-caps-token trigger here (that matched incidental tokens
  // like protocol/algorithm names in unrelated bug-fix queries).
  // Item 5: camelCase-robust gate (isEnumLikeQueryShaped) — a query like
  // "TicketPriority byTicket" (no word-boundary "priority") now derives the
  // same contract/api/ui/style requirements as "ticket priority".
  if (isEnumLikeQueryShaped(query)) {
    required.add("contract");
    required.add("api");
    if (inventory.has("ui")) required.add("ui");
    if (inventory.has("style")) required.add("style");
  }

  if (hasRenameRequestMarker(query)) {
    required.add("api");
    required.add("test");
  }

  // The monotone carry-forward (see `carried` above) joins the per-call
  // inference here, before the shared priority ordering, so a carried role is
  // indistinguishable downstream from one this call inferred itself.
  for (const role of carried) required.add(role as ImpactSurface);

  return [...required].sort(
    (a, b) => (SURFACE_ROLE_PRIORITY[a] ?? 9) - (SURFACE_ROLE_PRIORITY[b] ?? 9)
  );
}

// ---------------------------------------------------------------------------
// A4: route honesty. `missingRequired` alone is a bad proxy for "reading is
// done" — for bug-fix queries it is usually empty even when the pack covers
// only one of several bug sites. The self-contradiction (coverage:partial +
// "0 more calls needed") is exactly the escape license this design closes.
// A separate, INDEPENDENT signal — unmatched query concern tokens — decides
// whether the returned surfaces actually address what the query asked for.
// ---------------------------------------------------------------------------

/**
 * Tiny generic stopword set for concern-token extraction. Deliberately NOT
 * the full STOP_WORDS list from locateTaskContext.ts (which also drops
 * domain/code vocabulary like "function"/"error"/"array") — a concern token
 * IS allowed to be code vocabulary; only filler words are dropped here. Not
 * merged with findText.ts's FIND_STOP_WORDS (see that set's own comment) —
 * the two lists already disagreed on some filler words before this
 * consolidation, and the consolidation is the shared pipeline
 * (util/queryShape.ts's tokenizeQuery), not the word lists.
 */
const CONCERN_STOP_WORDS = new Set([
  "the", "and", "for", "with", "this", "that", "from", "into", "when",
  "where", "which", "should", "have", "been", "will", "your", "about",
]);

const CONCERN_NOISE_WORDS = new Set([
  ...CONCERN_STOP_WORDS,
  "add", "adds", "added", "adding", "bug", "bugs", "fix", "fixes", "fixed", "fixing",
  "wrong", "broken", "normal", "abnormal", "incorrect", "invalid", "reported", "report",
  "symptom", "symptoms", "complete", "completion", "criteria", "root", "cause", "causes",
  "identify", "identified", "update", "updated", "connect", "connected", "connection",
  "reflect", "reflected", "reuse", "reused", "existing", "using", "path", "paths",
  "file", "files", "side", "task", "tasks", "code", "works", "work", "working",
  "wire", "wired", "wiring",
  "all", "each", "every", "separate", "independent", "one", "two", "three",
  "first", "second", "third", "fourth", "fifth",
]);

const META_CONCERN_EVIDENCE_TOKENS = new Set([
  "auto", "json", "query", "semantic", "generic", "helper", "regression", "implementation",
  "candidate", "ranking", "routing", "skeleton", "prefix", "range", "slice", "slicing", "symbol",
  "test", "tests", "mode", "large", "file", "files", "exact", "current",
]);

const WEAK_CONCERN_TOKENS = new Set([
  "controller", "manager", "service", "handler", "status", "system", "mode", "state",
  "input", "output", "task", "health",
]);

interface ConcernGroup {
  query: string;
  tokens: string[];
}

/**
 * Extract "significant query tokens": split on non-word characters,
 * lowercase, keep tokens of length >= 4, drop CONCERN_STOP_WORDS. These are
 * the tokens a returned pack is expected to visibly address — not every
 * query word (short/filler words carry no locate-able concern), but enough
 * to catch a query like "fix the failsafe windup mode transition" naming
 * several distinct bug sites. Thin wrapper over the shared pipeline in
 * util/queryShape.ts ("simple" mode — no camelCase splitting, no
 * distinctiveness sort, unlike findText.ts's "identifier" mode) with this
 * file's own minLen/stop-word list.
 *
 * W9: `query` is scrubbed of generic path spans FIRST (stripPathSpans("",
 * query)) — a path segment mentioned in prose is not a locate-able concern
 * word (see the module comment above concernHarvestText).
 */
function significantQueryTokens(query: string): string[] {
  return tokenizeQuery(stripPathSpans("", query), { minLen: 4, stopWords: CONCERN_STOP_WORDS, mode: "simple" });
}

// R0 (2026-08-21, W4-A): dedupe case-insensitively but PRESERVE the
// first-seen original casing on output — matching moduleShapedQueryTokens'
// established idiom (key = lowercase, pushed value = original). Before this
// fix, every token was silently lowercased on the way out, so a camelCase
// identifier concernAnchorTokens's own concreteIdentifierTokens had
// carefully case-preserved (e.g. "buildInitializeInstructions") lost its
// casing here, then failed identifierShapedConcernSet's case-sensitive
// `shaped.has(t)` filter (shaped stores lowercase) while a separately-
// produced ALL-LOWERCASE duplicate of the SAME token (from
// significantQueryTokens' "simple" tokenizer, which lowercases at the
// source) survived that filter — reporting a served, already-anchored
// symbol as an "uncovered concern" purely because of a casing mismatch
// between two representations of the identical token.
function orderedUniqueTokens(tokens: string[]): string[] {
  const seen = new Set<string>();
  const out: string[] = [];
  for (const token of tokens) {
    const key = token.toLowerCase();
    if (key.length === 0 || seen.has(key)) continue;
    seen.add(key);
    out.push(token);
  }
  return out;
}

function markedConcernClauses(query: string, marker: RegExp): string[] {
  const matches = [...query.matchAll(marker)];
  return matches
    .map((match, index) => {
      const start = (match.index ?? 0) + match[0].length;
      const end = matches[index + 1]?.index ?? query.length;
      // A blank paragraph after the final list item is task-level completion
      // guidance, not part of that last concern.
      return query.slice(start, end).split(/\n\s*\n/, 1)[0]!.trim();
    })
    .filter((part) => part.length >= 12)
    .slice(0, 4);
}

function splitConcernClauses(query: string): string[] {
  const normalized = query.replace(/\r/g, "\n");
  // Explicit lists are authoritative concern boundaries. Extract only the
  // marked entries: the former newline splitter also promoted the preamble and
  // completion criteria into synthetic concerns (a live three-symptom report
  // became four locates).
  const circled = markedConcernClauses(normalized, /(?:^|[\s:;；])\s*[①②③④⑤⑥⑦⑧⑨⑩]\s*/g);
  if (circled.length >= 2) return circled;
  // Inline incident reports commonly use "bugs: (1) ... (2) ... (3) ...".
  // Treat two or more parenthesized numeric markers as authoritative list
  // boundaries just like newline lists; ordinary one-off parentheticals do
  // not bind because markedConcernClauses must return at least two entries.
  const parenthesized = markedConcernClauses(normalized, /(?:^|[\s:;；])\(\d{1,2}\)\s*/g);
  if (parenthesized.length >= 2) return parenthesized;
  const listed = markedConcernClauses(normalized, /(?:^|\n)\s*(?:[-*]|\d+[.)])\s+/g);
  if (listed.length >= 2) return listed;

  const sentenceParts = normalized
    .split(/[。;；\n]+/)
    .map((part) => part.trim())
    .filter((part) => part.length >= 12);
  const commaParts = sentenceParts.length === 1
    ? sentenceParts[0]!
      .split(/[,、]/)
      .flatMap((part) => part.split(/\s+and\s+/i))
      .map((part) => part.trim())
      .filter((part) => part.length >= 12)
    : sentenceParts;
  // Retrieval and coverage still need the semantic clauses even when the
  // overall edit verb appears only in the preamble (for example "bugs: parser,
  // encoder, scheduler"). Whether those clauses are safe to bind as the stricter
  // multi_concern profile is decided separately by classifyTaskProfile.
  return commaParts.length >= 2 ? commaParts.slice(0, 4) : [];
}

function hasExplicitConcernList(query: string): boolean {
  const normalized = query.replace(/\r/g, "\n");
  return markedConcernClauses(normalized, /(?:^|[\s:;；])\s*[①②③④⑤⑥⑦⑧⑨⑩]\s*/g).length >= 2
    || markedConcernClauses(normalized, /(?:^|[\s:;；])\(\d{1,2}\)\s*/g).length >= 2
    || markedConcernClauses(normalized, /(?:^|\n)\s*(?:[-*]|\d+[.)])\s+/g).length >= 2;
}

function hasExplicitIndependentConcernMarker(query: string): boolean {
  return /\b(?:independent|unrelated|separate|distinct)\b[^.!?\n]{0,40}\b(?:bugs?|defects?|issues?)\b/i.test(query)
    || /(?:関連しない|独立した|別々の|別の)(?:[^。\n]{0,16})?(?:バグ|不具合|問題)/.test(query)
    || /(?:バグ|不具合|問題)(?:[^。\n]{0,16})?(?:独立|別々|無関係)/.test(query);
}

function hasIndependentEditConcernClauses(query: string): boolean {
  const clauses = splitConcernClauses(query);
  return clauses.filter((part) =>
    hasRequestedMutationIntent(part) && concreteIdentifierTokens(part).length > 0
  ).length >= 2;
}

// ---------------------------------------------------------------------------
// concernHarvestText / filterConcernQueryEntries — workspace-identity
// scrubbing for concern-token harvest sources (W9, 2026-08-22 root-leak
// forensics).
//
// concernAnchorTokens tokenizes whatever free text it is handed with no
// awareness of the workspace's own path — by design, since it also has to
// work on arbitrary bug-report prose. That is exactly the hazard: a
// workspace root whose folder name reads as ordinary words when
// hyphen/underscore/dot-split (e.g. "m365-drive-mount") turns into concern
// tokens ("m365", "drive", "mount") the moment that name is mentioned
// anywhere in the harvested text — including a bare `path` fallback (see
// buildTaskPackCore), which IS a path under the workspace root and therefore
// names the root outright. Those tokens then collide with unrelated
// identifiers later (buildConcernNote / buildSmallFileConcernNote in
// readCodeModes.ts) and fire a spurious "session-query tokens ... hit
// outside served range" note on a file that has nothing to do with the
// query.
//
// Filtering the derived TOKENS after the fact cannot be precise: a plain
// word like "mount" is a legitimate concern token in a mounting-related bug
// report, so any rule keyed on the word alone either keeps the leak or drops
// real concerns — precision over breadth. The fix instead scrubs the SOURCE
// TEXT before tokenization of exactly two things that are unambiguously not
// free-text concerns: the workspace root's own folder name, and path-shaped
// spans (the task pack already resolves real paths structurally elsewhere —
// a path SPAN mentioned in prose carries no free-text concern of its own, so
// the whole span is dropped, not narrowed to a file stem). Standalone
// dictionary words are left untouched.
//
// W9 (2026-08-22, find-next path-token leak): the same hazard exists for
// EVERY identifier/concern-token extractor in this file, not just the
// concern-harvest source above — a query that mentions an absolute path in
// prose ("…in /Users/…/TokenLighten/packages/… how does X work") had its
// path SEGMENTS tokenized as if they were free-text/identifier content,
// which could surface as a wasted `search_files action=find` `next`
// suggestion (or, worse, an `explicit-identifier` obligation the pack could
// never certify, since the segment is provably absent from every served
// surface). The scrub is now `util/queryShape.ts`'s workspace-agnostic
// `stripPathSpans("", text)` — see that module for the full rule set — used
// by every low-level identifier/token extractor below (explicitCodeIdentifiers,
// concreteIdentifierTokens, significantQueryTokens, concernAnchorTokens,
// dottedOrScopedIdentifiersIn) so every caller of those shared primitives is
// fixed at the source, plus the few `next`-builders that tokenize `query`
// directly instead of going through a shared primitive (nextCallForUnresolved,
// wiringCandidateTokens). `concernHarvestText` itself is now a thin wrapper
// over the same shared `stripPathSpans`, with its workspace-root rules intact.
// ---------------------------------------------------------------------------

/**
 * Remove workspace-identity text from a concern-harvest source BEFORE
 * tokenization — see the module comment above. A no-op on already-clean
 * prose: absent a literal root basename or a path-shaped span, the text
 * passes through unchanged. Thin wrapper over the shared, workspace-agnostic
 * `stripPathSpans` (util/queryShape.ts) — kept exported under its original
 * name since it is still the concern-harvest-specific entry point (recordConcernTokens)
 * and existing tests import it by this name.
 */
function rootBasenameVocabulary(workspaceRoot: string): string[] {
  const root = basename((workspaceRoot ?? "").trim());
  // F-R20 (2026-08-23): ANY multi-segment root name is project identity, not
  // ordinary prose — camelCase (`M365DriveMounter`) and hyphen/underscore/
  // dot-segmented (`m365-drive-mount`) roots get the same fragment guard.
  // Splitting only camelCase roots used to leave hyphen-style segments as a
  // "legacy precision guarantee", but a segment like "mount" or "drive" is
  // exactly as much the project's own namespace when the folder name spells
  // it with hyphens as when it spells it in camelCase — leaving it as
  // "ordinary prose" let a bare queries[] entry or prose mention of that
  // word seed a concern token that then matched the whole project (the
  // NetResourceMountPoint false-fire class). Gate on the RAW segment count
  // (before the length filter below) so a root like "my-app" still counts
  // as two segments even though "my" is later dropped for being under 3
  // characters — a single-word root (e.g. "ledger") contributes nothing
  // here, since its one segment IS the literal basename
  // concernHarvestText/filterConcernQueryEntries already strip/compare
  // directly.
  const rawParts = root
    .replace(/([a-z0-9])([A-Z])/g, "$1 $2")
    .split(/[^A-Za-z0-9]+/)
    .filter((part) => part.length > 0);
  if (rawParts.length < 2) return [];
  const parts = rawParts.map((part) => part.toLowerCase()).filter((part) => part.length >= 3);
  return [...new Set(parts)];
}

export function concernHarvestText(workspaceRoot: string, text: string): string {
  let cleaned = stripPathSpans(workspaceRoot, text);
  for (const word of rootBasenameVocabulary(workspaceRoot)) {
    cleaned = cleaned.replace(new RegExp(`(?<![-.])\\b${escapeRegExp(word)}\\b(?![-.])`, "gi"), " ");
  }
  return cleaned;
}

/**
 * search_files find `queries[]` entries are recorded VERBATIM (they are
 * already literal identifier tokens by contract — see the call site's own
 * doc in server.ts), so there is no free-text span to scrub. Drop an entry
 * that IS, in its entirety, the workspace root's basename or one of its
 * multi-segment name-words (rootBasenameVocabulary — any camelCase or
 * hyphen/underscore/dot segmentation, F-R20), that is itself a >=4-char
 * prefix of one of those words (so "mount" drops against root word
 * "mounter"), or that contains a path separator (a sign the caller passed a
 * path through queries[] rather than a literal token). A longer identifier
 * that merely CONTAINS a root word as a substring (e.g.
 * "NetResourceMountPoint") is left alone.
 */
export function filterConcernQueryEntries(workspaceRoot: string, queries: readonly string[]): string[] {
  const base = basename((workspaceRoot ?? "").trim()).toLowerCase();
  const rootWords = rootBasenameVocabulary(workspaceRoot);
  return queries.filter((q) => {
    const token = q.trim().toLowerCase();
    if (/[/\\]/.test(q)) return false;
    if (base && token === base) return false;
    if (rootWords.some((word) => token === word || (token.length >= 4 && word.startsWith(token)))) return false;
    return true;
  });
}

/** Han / Hiragana / Katakana. */
const CJK_ANCHOR_CHAR_RE = /[぀-ヿ㐀-䶿一-鿿]/u;

/**
 * Is this token usable as a CONCERN ANCHOR — a claim that the repository must
 * contain something matching it?
 *
 * 2026-08-27 (field-eval integration). `util/queryShape.ts` began routing
 * `extractCjkTokens` into BOTH of its shared tokenizers, so a CJK-bearing
 * query now contributes CJK runs AND their 2-character bigrams
 * (`センサー` -> also `セン`, `ンサ`) to every consumer of
 * `concreteIdentifierTokens` / `significantQueryTokens`. For RETRIEVAL that is
 * the point and it stays: a Japanese query could previously match nothing at
 * all, and find/locate recall is exactly what those fragments are for.
 *
 * Concern anchoring is a different axis with the opposite polarity. An anchor
 * is not a search term — it is an OBLIGATION: `concernTokenMatchesSurfaces`
 * must find it in a served surface's path, symbol, or body, or the concern
 * stays uncovered and the pack is demoted. This function's own FIX-A rule
 * already states the standard ("a clause whose only unmatched wording is
 * free-text prose ... contributes NOTHING here"), and a symptom description in
 * Japanese prose — still less a sub-word kana bigram — is exactly that prose,
 * not an identifier the repository ever promised to contain.
 *
 * Measured on the aeroctl three-symptom fixture: the CJK fragments crowded the
 * real ASCII identifiers out of this function's `limit`-capped output, so
 * `augmentConcernGroupSurfaces` stopped resolving the mixer and mode-transition
 * clusters, two of three concerns went permanently uncovered, and a pack that
 * had certified `complete` fell to `awaiting-input` / choose-candidate. That is
 * an obligation nothing in an ASCII codebase can ever discharge.
 *
 * Scoped to the anchor axis ONLY — this changes no tokenizer and removes
 * nothing from retrieval, so it restores pre-wave parity here while leaving the
 * CJK recall win intact everywhere else.
 *
 * DELIBERATE NON-GOAL: a codebase whose identifiers or comments are themselves
 * CJK could in principle ground a CJK anchor (`concernTokenMatchesSurface`
 * substring-matches surface bodies). Admitting those would need a grounding
 * test — membership in the located candidates' own vocabulary, the
 * `knownIdentifiers` allowance `identifierShapedConcernSet` already takes —
 * not a shape test, because CJK has no case/underscore/dot shape signal to
 * separate an identifier from a noun. Until that evidence is plumbed here,
 * refusing to mint the obligation is the honest side to fail on.
 */
function isConcernAnchorable(token: string): boolean {
  return !CJK_ANCHOR_CHAR_RE.test(token);
}

export function concernAnchorTokens(text: string, limit = 6): string[] {
  // W9: strip generic path spans ONCE, up front — concreteIdentifierTokens
  // and significantQueryTokens already strip internally (for their OTHER
  // direct callers), so this is belt-and-suspenders for them, but it is the
  // ONLY scrub the inline compactCodeTerms extraction below gets.
  const cleaned = stripPathSpans("", text);
  const compounds = [...cleaned.matchAll(/[A-Za-z0-9]+(?:[-.][A-Za-z0-9]+)+/g)]
    .map((match) => match[0]!.toLowerCase())
    // Natural-language distributives such as "per-priority" are prose, not
    // identifier atoms: retain the meaningful component (priority) so a
    // propagation facet can still bind repository symbols such as byPriority.
    .filter((token) => !token.startsWith("per-"));
  const compoundParts = new Set(compounds.flatMap((token) => token.split(/[-.]/)));
  const concrete = concreteIdentifierTokens(cleaned).filter((token) => !compoundParts.has(token.toLowerCase()));
  const simple = significantQueryTokens(cleaned).filter((token) => !CONCERN_NOISE_WORDS.has(token) && !compoundParts.has(token.toLowerCase()));
  const compactCodeTerms = tokenizeQuery(cleaned, {
    minLen: 3,
    stopWords: CONCERN_STOP_WORDS,
    mode: "simple",
  }).filter((token) => /^[a-z0-9]{3}$/i.test(token) && !CONCERN_NOISE_WORDS.has(token));
  // Query-derived only. Repository vocabulary grounding happens after the
  // walk/symbol evidence is available; production code carries no benchmark-
  // or domain-specific translation table here.
  return orderedUniqueTokens([...compounds, ...concrete, ...simple, ...compactCodeTerms])
    .filter(isConcernAnchorable)
    .slice(0, limit);
}

function concernGroupsForQuery(query: string): ConcernGroup[] {
  const clauses = splitConcernClauses(query);
  const groups = clauses
    .map((clause) => ({ query: clause, tokens: concernAnchorTokens(clause) }))
    .filter((group) => group.tokens.length > 0);
  return groups.length >= 2 ? groups : [];
}

function propagationFacetGroups(query: string): ConcernGroup[] {
  const listed = concernGroupsForQuery(query);
  const supplemental = query
    .replace(/\r/g, "\n")
    .split(/[。；;\n]+/)
    .map((clause) => clause.replace(/^\s*(?:[-*]|\d+[.)]|[①②③④⑤⑥⑦⑧⑨⑩])\s*/, "").trim())
    .filter((clause) => clause.length >= 8)
    .filter((clause) =>
      /\b(?:backend|frontend|shared|validation|validate|validator|schema|stats?|statistics|aggregate|aggregation|counts?|summary)\b/i.test(clause)
      || /バックエンド|フロントエンド|共有|バリデーション|検証|統計|集計|件数/.test(clause)
    )
    .map((clause) => ({ query: clause, tokens: concernAnchorTokens(clause) }))
    .filter((group) => group.tokens.length > 0);
  const seen = new Set<string>();
  return [...listed, ...supplemental].filter((group) => {
    const key = group.tokens.join("|");
    if (seen.has(key)) return false;
    seen.add(key);
    return true;
  }).slice(0, 6);
}

/**
 * True when `token` appears case-insensitively as a substring of ONE
 * surface's `path` or `symbol` — deliberately NOT the embedded `code`
 * (matching code would be far too loose: nearly every token in an English
 * bug-fix query appears somewhere in nearly any plausible code file, which
 * would defeat the purpose of this check entirely). Factored out of
 * concernTokenMatchesSurfaces (2026-07-11c) so a single-surface caller
 * (surfaceGroundedInQuery, below) reuses the EXACT same match rule instead
 * of a second, potentially-diverging notion of "matches".
 */
function concernTokenMatchesSurface(token: string, s: TaskPackSurface): boolean {
  if (concernTokenMatchesSurfaceIdentity(token, s)) return true;
  // R0 (2026-08-21, W4-A): this function is documented "case-insensitively"
  // (see its doc comment above) — lowercase ONCE, here, so every caller gets
  // that contract regardless of whether IT remembered to lowercase first.
  // Before this fix, `body.includes(token)` compared an already-lowercased
  // `body` against a possibly-mixed-case `token` (e.g. a case-preserved
  // camelCase identifier, now that orderedUniqueTokens no longer scrubs
  // case out of concernAnchorTokens' output) and silently never matched.
  const lowerToken = token.toLowerCase();
  const normalizedToken = normIdent(token);
  const body = `${s.code ?? ""}\n${s.code_unchanged ?? ""}`.toLowerCase();
  return body.includes(lowerToken) || normIdent(body).includes(normalizedToken);
}

function concernTokenMatchesSurfaceIdentity(token: string, s: TaskPackSurface): boolean {
  // R0 (2026-08-21, W4-A): same case-insensitivity fix as
  // concernTokenMatchesSurface, for the same reason — see its comment.
  const lowerToken = token.toLowerCase();
  const normalizedToken = normIdent(token);
  const lowerSymbol = s.symbol?.toLowerCase();
  const normalizedBase = normIdent(path.basename(s.path).replace(/\.[^.]+$/, ""));
  const normalizedSymbol = s.symbol !== undefined ? normIdent(s.symbol) : undefined;
  return s.path.toLowerCase().includes(lowerToken) ||
    (lowerSymbol !== undefined && lowerSymbol.includes(lowerToken)) ||
    normalizedBase.includes(normalizedToken) ||
    (normalizedSymbol !== undefined && normalizedSymbol.includes(normalizedToken));
}

/**
 * True when `token` appears case-insensitively as a substring of ANY
 * surface's `path` or `symbol` — see concernTokenMatchesSurface.
 */
function concernTokenMatchesSurfaces(token: string, surfaces: TaskPackSurface[]): boolean {
  return surfaces.some((s) => concernTokenMatchesSurface(token, s));
}

function concernGroupMatchesSurfaces(group: ConcernGroup, surfaces: TaskPackSurface[]): boolean {
  // Same rule as `isConcernAnchorable`, applied to this gate's own reader of
  // `concreteIdentifierTokens` (2026-08-27). This clause is a VETO: "the clause
  // named concrete identifiers and none of them are here, so it is uncovered".
  // A CJK prose token must not be able to cast that veto for the reason the
  // helper's doc gives — it was never an identifier claim. It can still SATISFY
  // the gate if some other, ASCII identifier in the clause matches.
  const identifiers = concreteIdentifierTokens(group.query).filter(isConcernAnchorable);
  if (identifiers.length > 0 &&
      !identifiers.some((t) => concernTokenMatchesSurfaces(t.toLowerCase(), surfaces))) {
    return false;
  }
  const matched = group.tokens.filter((t) => concernTokenMatchesSurfaces(t, surfaces));
  // R0 (2026-08-21, W4-A): case-insensitive compare — see orderedUniqueTokens.
  return matched.some((t) => !WEAK_CONCERN_TOKENS.has(t.toLowerCase())) || matched.length >= 2;
}

/**
 * The pool of concern-anchor tokens used to test whether a SINGLE surface
 * relates to `query` at all (surfaceGroundedInQuery, below). Mirrors
 * unmatchedConcernTokens's own token-pool choice: the union of every concern
 * clause's tokens when the query splits into >=2 clauses (so a LATER
 * clause's tokens — e.g. "failsafe" in a 3-symptom query — are not silently
 * dropped by the flat extractor's own token-count cap), else the single flat
 * concernAnchorTokens list.
 */
function concernTokenPool(query: string): string[] {
  const groups = concernGroupsForQuery(query);
  if (groups.length >= 2) return orderedUniqueTokens(groups.flatMap((g) => g.tokens));
  return concernAnchorTokens(query, 8);
}

/**
 * Strips a leading `<prefix>/` segment from `relPath` for each `prefix` in
 * `scopePrefixes`, stopping at the first match. A no-op (returns `relPath`
 * unchanged) when nothing matches or no prefixes are supplied.
 */
function stripConfinementPrefix(relPath: string, scopePrefixes: readonly string[]): string {
  for (const prefix of scopePrefixes) {
    if (prefix.length === 0) continue;
    if (relPath.startsWith(`${prefix}/`)) return relPath.slice(prefix.length + 1);
  }
  return relPath;
}

/**
 * 2026-07-11c: true when `surface` is grounded in `query` — at least one of
 * the query's concern-anchor tokens matches this ONE surface's path/symbol
 * (concernTokenMatchesSurface; deliberately not the embedded `code`, same
 * rationale as that function). Used to tighten buildSeededTaskPack's
 * pure-dir "has an implementation body" check: an implementation-role file
 * that shares NOTHING with the query (run 2026-07-11c's irrelevant
 * platform/generic/startup.c, pulled in only because it happened to be a
 * >3-line non-header "config"-role file under the supplied directory) must
 * not be allowed to legitimize a "complete" pack on its own.
 *
 * `scopePrefixes` (typically the pureDirScope surface set's OWN deepest
 * common ancestor directory — see the call site) are stripped from
 * `surface.path` BEFORE matching. Without this, a pureDirScope pack's own
 * shared directory tree trivially "grounds" every surface under it: the
 * 2026-07-11c evidence query literally opens with the project's own name
 * and top directory word, and every surface returned for that dir-scoped
 * pack shares not just the project segment but a deeper shared prefix
 * too — defeating the whole check for exactly the query shape this exists to
 * catch. The surface's basename/symbol are unaffected (a shared ancestor
 * directory never appears in either), so this only changes the full-path
 * substring test.
 */
function surfaceGroundedInQuery(
  surface: TaskPackSurface,
  query: string,
  scopePrefixes: readonly string[] = [],
): boolean {
  const scoped = scopePrefixes.length === 0
    ? surface
    : { ...surface, path: stripConfinementPrefix(surface.path, scopePrefixes) };
  return concernTokenPool(query).some((t) => concernTokenMatchesSurfaceIdentity(t, scoped));
}

// ---------------------------------------------------------------------------
// FIX-A (2026-07-09d forensics, MODE-A "concerns-uncovered" false positives):
// only an IDENTIFIER-SHAPED query token — or one that matches an identifier
// the locator already found for this task — may gate coverage to "partial"
// via unmatchedConcernTokens. Forensics on 12 task_pack responses found 7/12
// demoted to partial purely because a salient English NOUN pulled from the
// agent's free-text query ("class", "lifecycle", "telemetry", "sensor",
// "pricing", "premium", "machine", "transition"...) had no surface literally
// named after it — not because anything was actually missing; agents
// correctly learned to ignore the resulting `search_files find "<noun>"`
// suggestions (6/7 follow-ups were skipped). A free-text noun may still help
// FIND a surface — concernGroupsForQuery/augmentConcernGroupSurfaces keep
// using the full (shape-agnostic) token pool for that search — it just may
// no longer BLOCK coverage on its own.
// ---------------------------------------------------------------------------

/**
 * Raw dotted (`a.b.c`), scoped (`a::b`), or path-like (`a/b/c`) identifier
 * references straight out of the ORIGINAL (un-lowercased, unsplit) query
 * text. tokenizeQuery's separators treat `.`/`::`/`/` as token boundaries, so
 * by the time a token reaches significantQueryTokens/concreteIdentifierTokens
 * this shape is already destroyed — this scans the untouched text instead, so
 * a query like "config.pricing.rate is wrong" or "wire up @scope/pkg" still
 * recognizes its dotted/scoped reference as identifier-shaped.
 *
 * W9 (2026-08-22): a GENUINE filesystem path span (an absolute path, or any
 * bare multi-segment run of >=3 segments — see stripPathSpans) is stripped
 * first, so a many-segment path mentioned in prose is not itself scooped up
 * as one giant "path-like identifier reference". A short reference like
 * "@scope/pkg" (one separator) is unaffected — stripPathSpans only removes
 * anchored or >=3-segment spans, exactly the ones this function was never
 * trying to catch.
 */
function dottedOrScopedIdentifiersIn(text: string): string[] {
  const cleaned = stripPathSpans("", text);
  const re = /[A-Za-z_$][\w$]*(?:(?:\.[A-Za-z_$][\w$]*)+|(?:::[A-Za-z_$][\w$]*)+|(?:\/[A-Za-z_$][\w$.-]*)+)/g;
  return cleaned.match(re) ?? [];
}

/**
 * Lowercased set of tokens from `text` that are IDENTIFIER-SHAPED: CamelCase
 * (an interior case transition), snake_case, an ALL-CAPS run (>=3 chars — the
 * same shape concreteIdentifierTokens already keys on), or a dotted/scoped/
 * path-like reference. `knownIdentifiers` (already-lowercased) is an OPTIONAL
 * extra allowance: a token matching an identifier the locator ALREADY found
 * for this task (e.g. a symbol/basename among the pre-trim candidate pool)
 * counts too, even when the query spelled it as a plain lowercase word — it
 * is provably a real repo identifier, not a guessed noun. Shape is read from
 * the case-preserving extractors (concreteIdentifierTokens, the
 * dotted/scoped scan) since the plain significant-token pipeline
 * (significantQueryTokens) lowercases up front and cannot answer this on its
 * own.
 */
function identifierShapedConcernSet(text: string, knownIdentifiers?: ReadonlySet<string>): Set<string> {
  const out = new Set<string>();
  for (const t of concreteIdentifierTokens(text)) out.add(t.toLowerCase());
  for (const t of dottedOrScopedIdentifiersIn(text)) out.add(t.toLowerCase());
  if (knownIdentifiers) for (const t of knownIdentifiers) out.add(t);
  return out;
}

/**
 * Significant query tokens with NO match in any returned surface's path or
 * symbol, FURTHER restricted (FIX-A) to tokens that are identifier-shaped or
 * match a known repo identifier — see identifierShapedConcernSet. Non-empty
 * means the returned surfaces plausibly do not address everything the query
 * asked for — the honest signal to flip the route to locate_missing_surfaces
 * (as opposed to coverage:partial alone, which fires on 22/22 packs merely
 * from the surface budget and would blanket-flip every pack, wasting locate
 * turns on tasks that were already fine handle-first).
 *
 * `knownIdentifiers` is optional and threaded only where a broader identifier
 * pool is cheaply available (the main locate flow's pre-trim candidate set);
 * every other caller omits it, which is still strictly SAFER than the
 * pre-FIX-A behavior (a subset of tokens can now gate, never a superset).
 */
function unmatchedConcernTokens(
  query: string,
  surfaces: TaskPackSurface[],
  knownIdentifiers?: ReadonlySet<string>,
): string[] {
  // R0 (2026-08-21, W4-A): `shaped` (identifierShapedConcernSet) stores
  // LOWERCASE keys; `group.tokens`/concernAnchorTokens may now carry a
  // case-preserved identifier (orderedUniqueTokens no longer scrubs case).
  // Both `shaped.has(t)` calls below must look up the lowercase form — else
  // a properly-cased, genuinely identifier-shaped token (e.g. the camelCase
  // symbol a query names) fails this filter purely on a case mismatch and
  // silently drops out of the uncovered-concern report instead of either
  // being correctly reported (when truly unmatched) or correctly matched out
  // (via concernTokenMatchesSurfaces, itself now case-insensitive).
  const shaped = identifierShapedConcernSet(query, knownIdentifiers);
  const groups = concernGroupsForQuery(query);
  if (groups.length >= 2) {
    return groups
      .filter((group) => !concernGroupMatchesSurfaces(group, surfaces))
      // FIX-A: the representative token for an unmatched clause must itself
      // be identifier-shaped — a clause whose only unmatched wording is
      // free-text prose (no concrete identifier in it at all) contributes
      // NOTHING here, rather than falling back to reporting its first token
      // (the pre-FIX-A behavior) purely because the clause as a whole did
      // not "match enough".
      .map((group) => group.tokens.find((t) => !concernTokenMatchesSurfaces(t, surfaces) && shaped.has(t.toLowerCase())))
      .filter((t): t is string => t !== undefined)
      .slice(0, 4);
  }
  return concernAnchorTokens(query, 8)
    .filter((t) => !WEAK_CONCERN_TOKENS.has(t.toLowerCase()) || !surfaces.some((s) => s.role !== "unknown"))
    .filter((t) => !concernTokenMatchesSurfaces(t, surfaces))
    .filter((t) => shaped.has(t.toLowerCase()))
    .slice(0, 4);
}

/**
 * Confidence-descending order for an abstain response's `candidateDetails`
 * (unlike a confident hit's `primary`/`related`, `candidateDetails` carries
 * no ranking guarantee). Tie-break prefers an implementation/source file
 * over a pure header/interface declaration — the same "definition over
 * declaration" preference the pureDirScope hasImplementationBody check
 * already applies elsewhere (header extensions excluded there too). This
 * matters specifically for a SCOPED locate call (2026-07-12d confinement):
 * narrowing the walked pool to `scope` can turn what was a confident
 * unscoped hit into a tied "ambiguous" abstain (fewer files to
 * differentiate a header from its own implementation on text-match alone),
 * and an untouched candidateDetails order can then hand back the
 * declaration instead of the definition the caller actually wants.
 */
function rankAbstainCandidates<T extends { path: string; confidence: number }>(candidates: readonly T[]): T[] {
  return [...candidates].sort((a, b) => {
    if (b.confidence !== a.confidence) return b.confidence - a.confidence;
    const aHeader = /\.(h|hpp|hh|hxx)$/i.test(a.path) ? 1 : 0;
    const bHeader = /\.(h|hpp|hh|hxx)$/i.test(b.path) ? 1 : 0;
    return aHeader - bHeader;
  });
}

interface RankedAnswerEvidenceCandidate {
  candidate: ImpactCandidate;
  score: number;
  exactIdentifier: boolean;
  strongEvidence: boolean;
}

function isNonImplementationAnswerCandidate(candidate: ImpactCandidate): boolean {
  // `classifySurface` intentionally labels anything under a fixture root as
  // test data. That is correct for TokenLighten's own repository, but wrong
  // when the caller explicitly scopes the target application under a bench
  // fixture. Use the candidate's repository-relative path semantics here;
  // only actual test directories/files are non-implementation evidence.
  return /(?:^|\/)(?:__tests__|test|tests)(?:\/|$)|(?:^|\/)bench\/workflows(?:\/|$)|\.spec\.[^.]+$/i.test(candidate.path);
}

function answerEvidencePathPenalty(candidate: ImpactCandidate, query: string): number {
  if (/\b(?:test|tests|spec|fixture|bench|benchmark)\b/i.test(query)) return 0;
  return isNonImplementationAnswerCandidate(candidate) ? 3 : 0;
}

function answerBuilderOwnershipBonus(candidate: ImpactCandidate, query: string): number {
  const asksForConstructionMechanism = /\b(?:trace|how|builds?|constructs?|creates?|assembles?|derives?)\b/i.test(query)
    || /どのように|どう作|構築|生成過程/.test(query);
  if (!asksForConstructionMechanism) return 0;
  return /^(?:build|construct|create|assemble|derive|resolve)/i.test(candidate.symbol ?? "")
    ? 2
    : 0;
}

/**
 * See the "2026-08-21 answer-path-inherits-locator fix" comment in
 * buildAnswerTaskPack. Picks a single abstain candidate to re-anchor the
 * locator on: one the query names by its own file (a whole-word basename
 * match), that already carries a resolved symbol from a real structural
 * locator match rather than a symbol-less prose/variant-text hit. Several
 * qualifying candidates (rare — the query would have to name more than one
 * file) fall back to the highest-confidence one; returns undefined when
 * nothing qualifies, leaving the existing candidate-list behavior in place.
 */
function pickNamedFileAbstainAnchor(
  candidates: readonly ImpactCandidate[],
  query: string,
): ImpactCandidate | undefined {
  let best: ImpactCandidate | undefined;
  for (const candidate of candidates) {
    if (candidate.symbol === undefined) continue;
    if (isNonImplementationAnswerCandidate(candidate)) continue;
    const stem = path.basename(candidate.path, path.extname(candidate.path));
    if (stem.length < 3 || !hasIdentifierSegment(query, stem)) continue;
    if (best === undefined || candidate.confidence > best.confidence) best = candidate;
  }
  return best;
}

/**
 * See the "2026-08-21 answer-path-inherits-locator fix" comment in
 * buildAnswerTaskPack. Orders ranked answer candidates so the locator's own
 * verdict — its primary, first, then any related surface it flagged
 * `required` — always occupies the front of the list a capped selection
 * draws from; everything else (answer-path-discovered facet/responsibility
 * extras) follows in the same relative (score) order rankAnswerEvidence-
 * Candidates already produced. A no-op (plain score order) whenever the
 * locator never resolved a primary at all.
 */
function orderByLocatorVerdict(
  ranked: readonly RankedAnswerEvidenceCandidate[],
  requiredPaths: ReadonlySet<string>,
  primaryPath: string | undefined,
): ImpactCandidate[] {
  if (requiredPaths.size === 0) return ranked.map((entry) => entry.candidate);
  const locked: ImpactCandidate[] = [];
  const extra: ImpactCandidate[] = [];
  for (const entry of ranked) {
    (requiredPaths.has(entry.candidate.path) ? locked : extra).push(entry.candidate);
  }
  locked.sort((a, b) => Number(b.path === primaryPath) - Number(a.path === primaryPath));
  return [...locked, ...extra];
}

/**
 * Recover the implementation that actually owns a multi-facet behaviour even
 * when the name in the request is colloquial or stale. Locator candidates are
 * name-first; for an explanation request such as "payment / inventory /
 * shipping", that can rank an analytics `orchestrator` above the service that
 * performs all three operations. This bounded pass ranks responsibility from
 * co-located query facets and structural layer evidence, within the caller's
 * scope, before the normal symbol focuser runs.
 */
async function discoverAnswerResponsibilityCandidates(
  args: TaskPackArgs,
  query: string,
  workspace: string,
  cache: FileReadCache,
): Promise<ImpactCandidate[]> {
  const facets = requiredQueryFacets(query);
  if (facets.length < 2) return [];
  const lifecycleRequested = /\b(?:state\s*machine|transition|lifecycle|workflow)\b/i.test(query)
    || /状態遷移|ライフサイクル/.test(query);
  const ranked: Array<{ candidate: ImpactCandidate; score: number }> = [];
  const files = walkCodeFiles(workspace, args.path ? { subPath: args.path } : {}).slice(0, 160);

  for (const file of files) {
    const surface = classifySurface(file.relPath);
    const candidateBase: ImpactCandidate = {
      path: file.relPath,
      line: 1,
      surface,
      why: "answer-semantic-responsibility",
      confidence: 0.5,
    };
    if (isNonImplementationAnswerCandidate(candidateBase)) continue;
    const content = readCached(workspace, file.relPath, cache);
    if (content === undefined) continue;
    const lower = content.toLowerCase();
    const matched = facets.filter((facet) => lower.includes(facet.toLowerCase()));
    if (matched.length < 2) continue;
    const evidence = selectQueryEvidence(content, query, { path: file.relPath });
    if (evidence === undefined) continue;
    const lifecycleEvidence = lifecycleRequested
      && /\b(?:transition|state|status|setStatus|stateMachine)\b/i.test(content);
    const serviceLayer = /(?:^|\/)(?:services?|domain|core|application)(?:\/|$)/i.test(file.relPath);
    const transportLayer = /(?:^|\/)(?:api|routes?|serializers?|controllers?)(?:\/|$)/i.test(file.relPath);
    const score = matched.length * 4
      + Math.min(4, evidence.score)
      + (lifecycleEvidence ? 3 : 0)
      + (serviceLayer ? 2 : 0)
      - (transportLayer ? 2 : 0);
    ranked.push({
      candidate: {
        ...candidateBase,
        line: evidence.line,
        range: evidence.range,
        confidence: Math.min(0.97, 0.72 + matched.length * 0.06 + (lifecycleEvidence ? 0.05 : 0)),
      },
      score,
    });
  }

  return ranked
    .sort((a, b) => b.score - a.score || a.candidate.path.localeCompare(b.candidate.path))
    .slice(0, 6)
    .map(({ candidate }) => candidate);
}

/**
 * Whether `candidate` already carries a locator-assigned `.symbol` worth
 * protecting from a weaker answer-focus replacement — i.e. at least one
 * token of the symbol's OWN name is also a query token. This subsumes every
 * strong locator anchor (an import-edge delegation target, a direct
 * query-symbol hit, a filename match refined to the outstanding token) by
 * the property they all share, rather than naming the locator's internal
 * `why` tags here. 2026-08-21 answer-focus precision fix.
 */
function hasStrongLocatorAnchor(candidate: ImpactCandidate, queryTokens: readonly string[]): boolean {
  if (candidate.symbol === undefined) return false;
  return tokenizeForEpoch(candidate.symbol).some((token) => queryTokens.includes(token));
}

/**
 * 2026-08-21 answer-focus precision fix, item (b): drop a ranked answer
 * candidate whose only support is a weak (non-explicit, non-strong) token
 * match when it is EITHER a non-source-role surface (style/doc) OR from a
 * project root other than the dominant one — the root(s) carrying the
 * query's explicit-identifier/strong-evidence support. A common/short token
 * (a CSS "cursor:" property, a generic "var"/"color"/"value" symbol name)
 * must not by itself pull a style/doc surface, or a surface from an
 * unrelated root, into an answer pack about code. Returns true when at
 * least one candidate was dropped, so the caller can decline to claim
 * `coverage:"complete"` over a pack built partly from discarded evidence.
 */
function dropWeakCrossRootAndNonSourceAnswerCandidates(
  ranked: RankedAnswerEvidenceCandidate[],
): boolean {
  const hasSupport = (entry: RankedAnswerEvidenceCandidate): boolean =>
    entry.exactIdentifier || entry.strongEvidence;
  const supported = ranked.filter(hasSupport);
  // No entry has explicit/strong support of its own — every candidate is
  // equally weak, so a root/role gate here would be arbitrary rather than a
  // real cross-root inconsistency check. Leave the set alone.
  if (supported.length === 0) return false;
  const rootOf = (entry: RankedAnswerEvidenceCandidate): string =>
    projectRootOf(entry.candidate.path) ?? entry.candidate.path.split("/", 1)[0];
  const dominant = dominantRoot(supported, rootOf);
  const kept = ranked.filter((entry) => {
    if (hasSupport(entry)) return true;
    const role = classifySurface(entry.candidate.path);
    if (role === "doc" || role === "style") return false;
    return dominant === null || rootOf(entry) === dominant;
  });
  if (kept.length === ranked.length) return false;
  ranked.splice(0, ranked.length, ...kept);
  return true;
}

/**
 * A locator abstain still carries useful filename-level candidates. For an
 * answer task, inspect only those bounded candidate files and re-rank their
 * symbols against the whole question. This turns a weak MAX_* filename hit
 * into the actual readiness builder without widening to a repository scan.
 */
async function rankAnswerEvidenceCandidates(
  candidates: readonly ImpactCandidate[],
  query: string,
  workspace: string,
  cache: FileReadCache,
): Promise<RankedAnswerEvidenceCandidate[]> {
  const explicit = new Set(explicitCodeIdentifiers(query).map((identifier) => identifier.toLowerCase()));
  const queryTokens = tokenizeForEpoch(query);
  const focusedPaths = new Set<string>();
  const ranked: RankedAnswerEvidenceCandidate[] = [];

  for (const candidate of candidates) {
    const content = readCached(workspace, candidate.path, cache);
    const focus = content === undefined ? undefined : await selectAnchorFocus(content, candidate.path, query, cache);
    const evidence = content === undefined
      ? undefined
      : selectQueryEvidence(content, query, { path: candidate.path });
    const strongEvidence = evidence !== undefined
      && evidence.score >= 6
      && evidence.matchedTerms.length >= 2;
    const exactFocuses = focus === undefined
      ? []
      : [focus.best, ...focus.runnerUps].filter((item) =>
          explicit.has(item.name.toLowerCase())
        );
    const candidateStem = path.basename(candidate.path, path.extname(candidate.path)).toLowerCase();
    const exactFocus = exactFocuses.length === 0
      ? undefined
      : explicit.has(candidateStem)
        ? [...exactFocuses].sort((a, b) => {
            const span = (item: AnchorFocusCandidate): number => {
              const [start, end] = item.range.split("-").map(Number);
              return Number.isFinite(start) && Number.isFinite(end) ? (end ?? 0) - (start ?? 0) : 0;
            };
            return span(b) - span(a);
          })[0]
        : exactFocuses[0];
    if (exactFocus !== undefined) {
      if (focusedPaths.has(candidate.path)) continue;
      focusedPaths.add(candidate.path);
      const startLine = Number.parseInt(exactFocus.range.split("-", 1)[0] ?? "1", 10);
      ranked.push({
        candidate: {
          ...candidate,
          line: Number.isFinite(startLine) ? startLine : candidate.line,
          range: exactFocus.range,
          symbol: exactFocus.name,
          why: "answer-explicit-symbol-focus",
          confidence: Math.max(candidate.confidence, 0.98),
          handle: undefined,
        },
        score: exactFocus.score + (evidence?.score ?? 0) - answerEvidencePathPenalty(candidate, query),
        exactIdentifier: true,
        strongEvidence,
      });
      continue;
    }
    if (strongEvidence) {
      const isSubstantiveKind = (item: { kind?: string }): boolean =>
        item.kind === "function" || item.kind === "method" || item.kind === "class";
      // 2026-08-21 answer-focus precision fix (file-named-header
      // short-circuit): the query's answer is "this file, generally" only
      // when EVERY term the evidence engine actually matched is already
      // accounted for by the file's OWN name — i.e. the query has no
      // substantive content beyond naming the file ("what does the coverage
      // packer do" naming coveragePacker.ts). A query that ALSO names
      // something else ("which codec does the pipeline choose" — "codec" is
      // not part of pipeline.ts's own name) is asking about a specific
      // behavior/relationship, not the file as a whole, even though it too
      // names the file; that case must fall through to
      // structural/enclosing/containment below so the actual function wins.
      // Decided FIRST, ahead of structural/enclosing/containment, so an
      // unrelated helper that merely happens to reference a type whose OWN
      // name contains the query's words (e.g. every function taking a
      // `CoveragePackerInput` parameter trivially "matches" coverage+packer)
      // can never outrank the file's own header once the query is purely
      // about that file.
      const isFileNamedHeader = (item: { kind?: string; name: string } | undefined): boolean => {
        if (item?.kind !== "module-header" || evidence.matchedTerms.length === 0) return false;
        const baseTokens = new Set(tokenizeForEpoch(item.name).map((t) => t.toLowerCase()));
        return evidence.matchedTerms.every((term) => baseTokens.has(term.toLowerCase()));
      };
      let evidenceFocus: { name: string; range: string; score: number } | undefined =
        isFileNamedHeader(focus?.best) ? focus!.best : undefined;
      // 2026-08-21 answer-focus precision fix: this used to accept ANY scored
      // focus candidate (including a module header or a const/type) whose
      // range merely CONTAINS evidence.line, before ever checking whether a
      // real function/method structurally answers the evidence — and once
      // that (possibly non-substantive) containment hit was accepted, the
      // enclosing-function fallback below was skipped entirely (it only ran
      // when nothing had matched yet). A header spanning lines 1-49 that
      // happens to contain the evidence line pre-empted the real function
      // every time. Now: try the STRUCTURAL (>=3 matched terms, or ALL
      // available matched terms when the query has fewer than 3 — e.g. a CJK
      // query embedding only 1-2 ASCII content words, which can then never
      // reach a fixed 3) and ENCLOSING function/method checks first — both
      // are substantive by construction — and only fall back to a bare
      // line-containment focus match (which may be a header/const) when
      // neither finds anything, i.e. the evidence genuinely sits outside any
      // callable.
      if (evidenceFocus === undefined && content !== undefined) {
        const lang = languageForPath(candidate.path);
        const parsed = lang ? await cache.parsedSymbols(content, lang, candidate.path) : null;
        const contentLines = content.split(/\r?\n/);
        const structural = (parsed ?? [])
          .filter((symbol) =>
            symbol.kind === "method"
            || symbol.kind === "function"
            || (candidate.why === "answer-semantic-responsibility" && symbol.kind === "class")
          )
          .map((symbol) => {
            const body = `${symbol.name}\n${contentLines.slice(symbol.startLine - 1, symbol.endLine).join("\n")}`.toLowerCase();
            return {
              symbol,
              matched: evidence.matchedTerms.filter((term) => body.includes(term.toLowerCase())).length,
            };
          })
          .sort((a, b) =>
            b.matched - a.matched
            || (a.symbol.endLine - a.symbol.startLine) - (b.symbol.endLine - b.symbol.startLine)
            || a.symbol.startLine - b.symbol.startLine
          )[0];
        const structuralFloor = Math.min(3, evidence.matchedTerms.length);
        if (structural && structural.matched >= structuralFloor && structural.matched > 0) {
          evidenceFocus = {
            name: structural.symbol.name,
            range: `${structural.symbol.startLine}-${structural.symbol.endLine}`,
            score: evidence.score + structural.matched,
          };
        }
        if (evidenceFocus === undefined) {
          const enclosing = (parsed ?? [])
            .filter((symbol) =>
              (symbol.kind === "method" || symbol.kind === "function")
              && evidence.line >= symbol.startLine
              && evidence.line <= symbol.endLine
            )
            .sort((a, b) =>
              (a.endLine - a.startLine) - (b.endLine - b.startLine)
            )[0];
          if (enclosing) {
            evidenceFocus = {
              name: enclosing.name,
              range: `${enclosing.startLine}-${enclosing.endLine}`,
              score: evidence.score,
            };
          }
        }
      }
      if (evidenceFocus === undefined && focus !== undefined) {
        // 2026-08-21 answer-focus precision fix (containment substantive
        // preference): a module header's range legitimately CONTAINS the
        // evidence line whenever the query's answer is discussed in the
        // header's own prose — that is a geometrically correct containment,
        // not a bug, but a real function ALSO in this candidate list
        // (focus.runnerUps) that covers the same evidence more usefully must
        // still win (the file-named-header case already short-circuited
        // above, so reaching here means the header — if any — is NOT what the
        // query named). Prefer a substantive (function/method/class)
        // containing item; when none of the geometrically-containing items is
        // substantive, fall back to selectAnchorFocus's own whole-file `best`
        // when it is substantive (more reliable than an arbitrary
        // non-substantive containing item) — but ONLY for a single-focus
        // query (no slash-grouped/quoted required facets). A multi-facet
        // query ("payment / inventory / shipping") already has its own
        // coverage contract: the caller-visible materializeNamedAnswer member-
        // expansion (and the pre-existing symbol-less whole-file serve it
        // expands from) is what is supposed to assemble multi-facet coverage;
        // pinning `best` here would narrow the surface to ONE facet's
        // function and silently drop that contract instead of feeding it.
        // Only settle for a non-substantive containing item as the last
        // resort.
        const containing = [focus.best, ...focus.runnerUps].filter((item) => {
          const bounds = item.range.match(/^(\d+)-(\d+)$/);
          return bounds != null
            && evidence.line >= Number(bounds[1])
            && evidence.line <= Number(bounds[2]);
        });
        evidenceFocus = containing.find(isSubstantiveKind)
          ?? (isSubstantiveKind(focus.best) && requiredQueryFacets(query).length < 2 ? focus.best : undefined)
          ?? containing[0];
      }
      if (evidenceFocus !== undefined) {
        if (focusedPaths.has(candidate.path)) continue;
        focusedPaths.add(candidate.path);
        const startLine = Number.parseInt(evidenceFocus.range.split("-", 1)[0] ?? "1", 10);
        const exactIdentifier = explicit.has(evidenceFocus.name.toLowerCase());
        ranked.push({
          candidate: {
            ...candidate,
            line: Number.isFinite(startLine) ? startLine : candidate.line,
            range: evidenceFocus.range,
            symbol: evidenceFocus.name,
            why: "answer-query-evidence-symbol-focus",
            confidence: Math.max(candidate.confidence, 0.9),
            handle: undefined,
          },
          score: evidence.score + evidenceFocus.score - answerEvidencePathPenalty(candidate, query),
          exactIdentifier,
          strongEvidence: true,
        });
        continue;
      }
      // Anchor preservation: no window in THIS file's evidence scan resolved
      // to any symbol at all — but if the locator already anchored this
      // candidate on a query-relevant symbol (import-edge delegation target,
      // a direct query-symbol hit, or a filename match refined to the token
      // that answers the query — generalized as "the symbol's own name
      // covers a query token", so this needs no knowledge of the locator's
      // internal why-vocabulary), keep that anchor instead of reporting a
      // symbol-less range.
      if (candidate.symbol !== undefined && hasStrongLocatorAnchor(candidate, queryTokens)) {
        if (focusedPaths.has(candidate.path)) continue;
        focusedPaths.add(candidate.path);
        ranked.push({
          candidate: { ...candidate, confidence: Math.max(candidate.confidence, 0.9) },
          score: evidence.score - answerEvidencePathPenalty(candidate, query),
          exactIdentifier: explicit.has(candidate.symbol.toLowerCase()),
          strongEvidence: true,
        });
        continue;
      }
      ranked.push({
        candidate: {
          ...candidate,
          line: evidence.line,
          range: evidence.range,
          symbol: undefined,
          why: "answer-query-evidence-focus",
          confidence: Math.max(candidate.confidence, 0.9),
          handle: undefined,
        },
        score: evidence.score - answerEvidencePathPenalty(candidate, query),
        exactIdentifier: false,
        strongEvidence: true,
      });
      continue;
    }
    // Anchor preservation: a locator-anchored, already-named symbol must not
    // be demoted to a non-substantive selectAnchorFocus guess (module header
    // / const / type / interface) purely because that guess scored higher on
    // lexical coverage — see hasStrongLocatorAnchor above. selectAnchorFocus
    // itself now prefers a substantive best when nothing was explicitly
    // named (2026-08-21), so this only still fires when its OWN best
    // candidate in THIS file is non-substantive.
    if (
      focus !== undefined
      && candidate.symbol !== undefined
      && focus.best.kind !== "function" && focus.best.kind !== "method" && focus.best.kind !== "class"
      && hasStrongLocatorAnchor(candidate, queryTokens)
    ) {
      if (focusedPaths.has(candidate.path)) continue;
      focusedPaths.add(candidate.path);
      ranked.push({
        candidate: { ...candidate, confidence: Math.max(candidate.confidence, 0.9) },
        score: candidate.confidence - answerEvidencePathPenalty(candidate, query),
        exactIdentifier: explicit.has(candidate.symbol.toLowerCase()),
        strongEvidence: false,
      });
      continue;
    }
    if (focus !== undefined) {
      if (focusedPaths.has(candidate.path)) continue;
      focusedPaths.add(candidate.path);
      const startLine = Number.parseInt(focus.best.range.split("-", 1)[0] ?? "1", 10);
      const exactIdentifier = explicit.has(focus.best.name.toLowerCase());
      ranked.push({
        candidate: {
          ...candidate,
          line: Number.isFinite(startLine) ? startLine : candidate.line,
          range: focus.best.range,
          symbol: focus.best.name,
          why: exactIdentifier ? "answer-explicit-symbol-focus" : "answer-semantic-symbol-focus",
          confidence: Math.max(candidate.confidence, exactIdentifier ? 0.98 : 0.85),
          handle: undefined,
        },
        score: focus.best.score - answerEvidencePathPenalty(candidate, query),
        exactIdentifier,
        strongEvidence: false,
      });
      continue;
    }
    ranked.push({
      candidate,
      score: candidate.confidence - answerEvidencePathPenalty(candidate, query),
      exactIdentifier: candidate.symbol !== undefined && explicit.has(candidate.symbol.toLowerCase()),
      strongEvidence: false,
    });
  }

  for (const entry of ranked) {
    entry.score += answerBuilderOwnershipBonus(entry.candidate, query);
  }
  return ranked.sort((a, b) =>
    Number(b.exactIdentifier) - Number(a.exactIdentifier)
    || b.score - a.score
    || b.candidate.confidence - a.candidate.confidence
    || a.candidate.path.localeCompare(b.candidate.path)
  );
}

/**
 * C-1 (2026-07-24 wave 4): literal self-recovery for explicit identifiers.
 *
 * An answer pack that fails to cover an identifier the caller literally named
 * manufactures a second call: the pack lands on candidate-list /
 * confirm_candidates and the model's next move is the exact literal search
 * (`search_files action=find <identifier>`) the server could have run itself.
 * When no ranked candidate carries the identifier, scan the workspace for it,
 * prefer definition-shaped occurrences in implementation files, and inject the
 * enclosing symbol (or a centered window) as an exact-identifier candidate.
 * Identifiers with zero literal occurrences are returned as verified-absent so
 * the readiness obligation can report decision-grade absence instead of the
 * ambiguous "no served evidence".
 *
 * Cost: fires only on the miss path (no exact candidate), <=3 identifiers,
 * one literal scan each — the same order of work locate's own content scan
 * already performs for the query.
 */
const IDENTIFIER_DEFINITION_KEYWORD_RE =
  /\b(?:function|class|def|struct|enum|interface|type|trait|impl|fn|func|module|const|let|var|val|public|private|protected|static|export|abstract|override|virtual|void|int|bool|double|float|auto)\b/;

async function recoverExplicitIdentifierAnswerCandidates(
  args: TaskPackArgs,
  query: string,
  workspace: string,
  ranked: RankedAnswerEvidenceCandidate[],
  cache: FileReadCache,
): Promise<string[]> {
  const identifiers = explicitCodeIdentifiers(query).slice(0, 3);
  if (identifiers.length === 0) return [];
  if (ranked.some((entry) => entry.exactIdentifier)) return [];
  const verifiedAbsent: string[] = [];
  const injected: RankedAnswerEvidenceCandidate[] = [];
  for (const identifier of identifiers) {
    const matches = scanLiteral(identifier, workspace, {
      caseInsensitive: false,
      ...(args.path ? { path: args.path } : {}),
    });
    if (matches.length === 0) {
      verifiedAbsent.push(identifier);
      continue;
    }
    const scored = matches
      .map((match) => {
        const asCandidate: ImpactCandidate = {
          path: match.path,
          line: match.line,
          surface: classifySurface(match.path),
          why: "answer-explicit-identifier-recovery",
          confidence: 0.97,
        };
        const text = match.text ?? "";
        const at = text.indexOf(identifier);
        const before = at >= 0 ? text.slice(0, at) : "";
        const after = at >= 0 ? text.slice(at + identifier.length) : "";
        // Definition-shaped = a declaration keyword precedes the name, or the
        // name is being bound (= / :). A bare `name(` is a CALL — counting
        // calls made every call site a "definition file" and the ambiguity
        // guard then suppressed recovery exactly where it should fire.
        const definitionShaped = IDENTIFIER_DEFINITION_KEYWORD_RE.test(before)
          || /^\s*[=:]/.test(after);
        let score = 0;
        if (IDENTIFIER_DEFINITION_KEYWORD_RE.test(before)) score += 4;
        if (/^\s*[(=:{]/.test(after)) score += 2;
        if (!isNonImplementationAnswerCandidate(asCandidate) && !isTestConcernPath(match.path)) score += 3;
        if (/\.(?:md|markdown|txt|rst|adoc)$/i.test(match.path)) score -= 4;
        return { match, asCandidate, score, definitionShaped };
      })
      .sort((a, b) =>
        b.score - a.score
        || a.match.path.localeCompare(b.match.path)
        || a.match.line - b.match.line
      );
    // Ambiguity guard: inject only when definition-shaped occurrences are
    // confined to exactly ONE file (a unique definition owner). A name whose
    // definition-shaped hits span several files — or that only ever appears
    // as a value/reference — keeps the candidate-choice flow: picking one
    // owner there would silently resolve a genuinely ambiguous investigation.
    const definitionFiles = new Set(
      scored.filter((entry) => entry.definitionShaped).map((entry) => entry.match.path),
    );
    if (definitionFiles.size !== 1) continue;
    const owner = [...definitionFiles][0]!;
    const best = scored.find((entry) => entry.definitionShaped && entry.match.path === owner)!;
    const content = readCached(workspace, best.match.path, cache);
    let range: string | undefined;
    let symbol: string | undefined;
    if (content !== undefined) {
      const lang = languageForPath(best.match.path);
      const parsed = lang ? await cache.parsedSymbols(content, lang, best.match.path) : null;
      const enclosing = (parsed ?? [])
        .filter((sym) => best.match.line >= sym.startLine && best.match.line <= sym.endLine)
        .sort((a, b) => (a.endLine - a.startLine) - (b.endLine - b.startLine))[0];
      if (enclosing) {
        range = `${enclosing.startLine}-${enclosing.endLine}`;
        symbol = enclosing.name;
      } else {
        const total = content.split(/\r?\n/).length;
        const start = Math.max(1, best.match.line - 8);
        const end = Math.min(total, best.match.line + 24);
        range = `${start}-${end}`;
      }
    }
    injected.push({
      candidate: {
        ...best.asCandidate,
        ...(range ? { range } : {}),
        symbol: symbol ?? identifier,
      },
      score: 6 + best.score,
      exactIdentifier: true,
      strongEvidence: true,
    });
  }
  if (injected.length > 0) ranked.unshift(...injected);
  return verifiedAbsent;
}

/**
 * Verified-absent identifiers ride OUTSIDE the wire envelope: the readiness
 * obligation builder consults this map to upgrade its uncovered reason to
 * decision-grade absence. WeakMap keyed by the result object so trim/dedupe
 * passes that mutate-in-place keep the association and clones simply lose it
 * (graceful: the reason falls back to the generic wording).
 */
const verifiedAbsentIdentifiersByResult = new WeakMap<object, readonly string[]>();

function verifiedAbsentIdentifiersFor(result: TaskPackResult): readonly string[] {
  return verifiedAbsentIdentifiersByResult.get(result) ?? [];
}

/** P2(b): dedupes the proof-completion engagement counter to one increment per pack — see buildTaskExecutionContract's own comment at the increment site. */
const proofCompletionCountedResults = new WeakSet<object>();

/**
 * I-7 (2026-08-30 forensics attribution wave): dedupes `enumerated_obligation_summary`
 * to one emission per pack, the SAME pattern proofCompletionCountedResults
 * uses just above and for the same reason — buildReadinessObligations runs at
 * least twice per pack (the falsification probe's own preview call, then
 * buildTaskExecutionContract's real one, which itself can re-evaluate one
 * `result` up to four times), and a count of how many enumerated obligations
 * were minted must describe the PACK, not how many times this builder
 * happened to run over it.
 */
const enumeratedObligationSummaryCountedResults = new WeakSet<object>();

/**
 * I-7: dedupes `receipt_next_repair` to one emission per TOOL CALL, not per
 * invocation of `receiptContractWithoutConsumedSearch`. Unlike the enumerated-
 * obligation summary above there is no stable `result` object to key a
 * WeakSet on here — `canServeCachedTaskPackReceipt`'s read-only preflight
 * (server.ts, `exactPreparedTaskPackReceipt`) calls the SAME
 * `tryServeCachedPack` chain the real serve path later calls again for one
 * logical read_file request, each producing a byte-identical repair. Keyed
 * on `currentTraceCallId()` instead — call_id is monotonic and ALS-scoped
 * per tool call (util/trace.ts), so both invocations of one request share
 * one id and only the first is traced. Bounded FIFO: call_id keeps climbing
 * for the life of the process, so this evicts the oldest entry once the
 * (generously headroomed, concurrent-lane-safe) cap is exceeded rather than
 * growing unboundedly on a long-lived server.
 */
const MAX_TRACED_RECEIPT_REPAIR_CALL_IDS = 32;
const receiptRepairTracedCallIds = new Set<number>();
function shouldTraceReceiptRepairOnce(): boolean {
  const callId = currentTraceCallId();
  // Outside any runWithTraceCall scope (e.g. a direct unit call in a spec) —
  // there is no shared id to dedupe against, so trace every time rather than
  // silently drop a call a test is directly asserting on.
  if (callId === undefined) return true;
  if (receiptRepairTracedCallIds.has(callId)) return false;
  receiptRepairTracedCallIds.add(callId);
  while (receiptRepairTracedCallIds.size > MAX_TRACED_RECEIPT_REPAIR_CALL_IDS) {
    const oldest = receiptRepairTracedCallIds.values().next().value as number | undefined;
    if (oldest === undefined) break;
    receiptRepairTracedCallIds.delete(oldest);
  }
  return true;
}

function isStrongRecoveredAnswerCandidate(
  candidates: readonly RankedAnswerEvidenceCandidate[],
  query: string,
): boolean {
  const top = candidates[0];
  if (top === undefined) return false;
  if (top.exactIdentifier) {
    const runnerUp = candidates[1];
    return runnerUp === undefined
      || !runnerUp.exactIdentifier
      || top.score - runnerUp.score >= 0.35;
  }
  if (top.score < 2.5) return false;
  const runnerUp = candidates[1];
  if (
    runnerUp !== undefined
    && answerBuilderOwnershipBonus(top.candidate, query) > 0
    && answerBuilderOwnershipBonus(runnerUp.candidate, query) === 0
  ) {
    return true;
  }
  if (
    runnerUp !== undefined
    && !/\b(?:test|tests|spec|fixture|bench|benchmark)\b/i.test(query)
    && !isNonImplementationAnswerCandidate(top.candidate)
    && isNonImplementationAnswerCandidate(runnerUp.candidate)
  ) {
    return true;
  }
  // Two independently strong semantic matches are still an ambiguity even if
  // one edges out the other numerically. Promote only when every runner-up is
  // below the same evidence floor.
  return runnerUp === undefined || runnerUp.score < 2.5;
}

async function augmentConcernGroupSurfaces(
  surfaces: TaskPackSurface[],
  query: string,
  workspace: string,
  requiredRoles: string[],
  cache: FileReadCache,
  protectedSurfaces: Set<TaskPackSurface>,
  // 2026-07-12d confinement fix: caller-SEEDED-flow scope (buildSeededTaskPack's
  // own `ancestor`, the caller-supplied entries' common ancestor directory).
  // undefined at the OTHER two call sites (buildTaskPack's pathless flow,
  // buildPartialPack's abstain-candidate flow) — there is no caller-named
  // scope to respect there, so they keep today's unconfined behavior.
  scope?: string,
  // B1a (2026-08-01 retrieval-scope): the caller's own anchor cluster. When the
  // seeded flow deliberately LIFTS `scope` (IMPROVEMENT F: volunteering more
  // clustered paths must not shrink the discovered closure), "" used to mean
  // the WHOLE workspace — including sibling project trees that share nothing
  // with the caller's paths but a distant ancestor. The anchor prefixes keep
  // the cross-package reach IMPROVEMENT F is about while restoring the family
  // boundary. Absent → unchanged behavior.
  anchorPaths?: readonly string[],
): Promise<void> {
  const groups = concernGroupsForQuery(query);
  if (groups.length < 2) return;

  const familyScope = scope === undefined || scope !== ""
    ? undefined
    : roleSearchScope(anchorPaths ?? [], query, workspace);
  const withinScope = (candidatePath: string): boolean =>
    (!scope || isUnderAncestor(candidatePath, scope))
    && isUnderAnyPrefix(candidatePath, familyScope);

  const seen = new Set(surfaces.map((s) => s.path));
  let added = 0;
  for (const group of groups) {
    if (concernGroupMatchesSurfaces(group, surfaces)) continue;
    if (added >= 4) return;

    const located = await locateTaskContext(workspace, {
      action: "locate",
      query: group.query,
      ...(scope ? { path: scope } : {}),
      limit: 3,
    });
    const candidates: ImpactCandidate[] = located.hit
      ? [...located.primary, ...located.related]
      : rankAbstainCandidates(located.candidateDetails ?? []);
    // Defensive re-confinement (same discipline as buildSeededTaskPack's own
    // required-role-fill loop): locateTaskContext's own hard-scope filter
    // already restricts to `scope` when passed, but this stays explicit
    // rather than relying solely on that internal filter. An out-of-scope
    // candidate is dropped here — never added, never coverage-bearing.
    let candidate = candidates.find((c) => !seen.has(c.path) && withinScope(c.path));
    if (!candidate) {
      // R0 (2026-08-21, W4-A): case-insensitive compare — see orderedUniqueTokens.
      for (const token of group.tokens.filter((t) => !WEAK_CONCERN_TOKENS.has(t.toLowerCase())).slice(0, 3)) {
        const tokenLocated = await locateTaskContext(workspace, {
          action: "locate",
          query: token,
          ...(scope ? { path: scope } : {}),
          limit: 3,
        });
        const tokenCandidates: ImpactCandidate[] = tokenLocated.hit
          ? [...tokenLocated.primary, ...tokenLocated.related]
          : rankAbstainCandidates(tokenLocated.candidateDetails ?? []);
        candidate = tokenCandidates.find((c) => !seen.has(c.path) && withinScope(c.path));
        if (candidate) break;
      }
    }
    if (!candidate) continue;

    const surface = await candidateToSurface(
      { ...candidate, required: true, confidence: Math.max(candidate.confidence, 0.7) },
      workspace,
      requiredRoles,
      query,
      cache,
    );
    surfaces.push(surface);
    protectedSurfaces.add(surface);
    seen.add(surface.path);
    added++;
  }
}

/**
 * Build one `explore action=find` suggestion per unmatched concern token, so
 * the agent's next TL call is scoped to exactly what this pack could not
 * confirm coverage for — rather than a vague "locate more" or a full re-run
 * of the same query that already produced this pack.
 */
function buildBlockingNextSteps(query: string, surfaces: TaskPackSurface[]): string[] {
  const tokens = unmatchedConcernTokens(query, surfaces);
  return tokens.map((t) => `search_files action=find query="${t}"`);
}

/**
 * Dominance tie-break (issue #3, 2026-08-21). `deriveCoverage` below brands a
 * pack "candidate-list" whenever the locator returned MULTIPLE primary
 * candidates for one query — genuine ties (a header+impl pair, two same-role
 * files) as well as the far more common case where one candidate obviously
 * outclasses the rest (a `filename-match` hit on an `api`/`domain`-role
 * implementation file next to a single stray `exact-text` hit inside an
 * unrelated `unknown`-role file). Shipping "candidate-list" for the second
 * shape — and therefore `await_input`/`choose-candidate` on the wire
 * (`protocol/decisionWire.ts`'s `projectTaskDecision`) — spends a caller
 * round trip confirming a choice that was never really in doubt.
 *
 * `candidateDominanceClass` scores a surface on three independent axes, most
 * significant first — the "lexicographic class tuple" this fix is named for:
 *
 *   1. role class    — a recognized structural/code role (every `ImpactSurface`
 *                       value except "doc"/"unknown": contract, api, domain,
 *                       data, ui, style, test, config) beats "doc", which
 *                       beats "unknown". `classifySurface` (util/impact.ts) is
 *                       the sole producer of `role`, so this reuses ITS
 *                       vocabulary rather than inventing a parallel one.
 *   2. evidence class — WHY this candidate matched at all. A structural or
 *                       distinctive match (`query-symbol`, `filename-match`,
 *                       `exact-text:distinctive`, `reference` — the strong
 *                       `why` vocabulary locateTaskContext.ts already emits)
 *                       beats a plain lexical hit (`exact-text`, `path-token`,
 *                       `text`), which in turn beats every OTHER `why` this
 *                       file emits (`caller-supplied`, `header-source-pair`,
 *                       `role-fill-scan`, the `answer-*` family, …): those say
 *                       nothing about how well THIS candidate matches the
 *                       query relative to its rivals, so an unrecognized `why`
 *                       sits at the bottom rather than being trusted to break
 *                       a tie. This is also what keeps a directory-seeded pack
 *                       (`why:"caller-supplied"`, never routed through this
 *                       helper in the first place — see the call sites below)
 *                       from ever winning on evidence class alone.
 *   3. source-ness    — a recognized implementation/native source extension
 *                       (`isImplementationPath`, plus the native C/C++
 *                       extension set `NATIVE_EXTS` already used for header
 *                       pairing) beats everything else. No repository-specific
 *                       directory name is consulted, per the design brief.
 *
 * A candidate wins only when it is STRICTLY better than every other candidate
 * on this tuple — `resolveDominantSurfaceIndex` returns -1 on any tie at the
 * best rank, so the genuine-tie case (same role/why/extension — e.g. WP-A
 * test (c)'s header+impl same-stem pair) keeps shipping `choose-candidate`
 * exactly as before. This helper never emits a decision itself: it only stops
 * `deriveCoverage` from asserting an ambiguity that provably was not one.
 * `buildTaskExecutionContract` and `decisionWire.ts`'s `projectTaskDecision`
 * still gate the actual `act.answer`/`act.edit` behind their own unchanged
 * certificate/floor checks — a dominance win with no certificate still lands
 * on `discover` or `await_input`, never a fabricated `act.*`.
 *
 * Deliberately silent on whether the winner already carries a body:
 * `resolveDominantSurfaceIndex` answers only "is the choice resolved". A
 * codeless winner is a real, common shape (`runCandidateInlinePass` below is
 * what a live "candidate-list" pack normally relies on to serve bodies at
 * all) — `applyDominancePromotion`, called right after this at every
 * caller, is what turns "resolved" into "just serve it and proceed" (design
 * point 1) by running the SAME single-handle closure read the inline pass
 * uses, scoped to the one surface that matters now that the choice is not
 * open. Splitting the two keeps this function a pure, cheaply-testable
 * classifier.
 */
const DOMINANCE_STRONG_WHY: ReadonlySet<string> = new Set([
  "query-symbol", "filename-match", "exact-text:distinctive", "reference",
]);
const DOMINANCE_WEAK_WHY: ReadonlySet<string> = new Set([
  "exact-text", "path-token", "text",
]);

function candidateDominanceRoleTier(role: string | undefined): number {
  return role === "doc" ? 1 : role === undefined || role === "unknown" ? 2 : 0;
}

function candidateDominanceEvidenceTier(why: string | undefined): number {
  if (why === undefined) return 2;
  if (DOMINANCE_STRONG_WHY.has(why)) return 0;
  if (DOMINANCE_WEAK_WHY.has(why)) return 1;
  return 2;
}

function candidateDominanceSourceTier(relPath: string): number {
  const ext = path.extname(relPath).toLowerCase();
  return isImplementationPath(relPath) || NATIVE_EXTS.has(ext) ? 0 : 1;
}

function candidateDominanceClass(
  surface: Pick<TaskPackSurface, "role" | "why" | "path">,
): readonly [number, number, number] {
  return [
    candidateDominanceRoleTier(surface.role),
    candidateDominanceEvidenceTier(surface.why),
    candidateDominanceSourceTier(surface.path),
  ];
}

function compareDominanceClass(
  a: readonly [number, number, number],
  b: readonly [number, number, number],
): number {
  return a[0] - b[0] || a[1] - b[1] || a[2] - b[2];
}

/**
 * The index of the unique strictly-best surface among `surfaces`, or -1 when
 * there are fewer than two surfaces, or more than one surface ties the best
 * rank (a genuine tie — see the doc comment above). Silent on whether the
 * winner is already served — see `applyDominancePromotion` below.
 */
function resolveDominantSurfaceIndex(surfaces: readonly TaskPackSurface[]): number {
  if (surfaces.length < 2) return -1;
  const classes = surfaces.map(candidateDominanceClass);
  let bestIdx = 0;
  for (let i = 1; i < classes.length; i++) {
    if (compareDominanceClass(classes[i]!, classes[bestIdx]!) < 0) bestIdx = i;
  }
  const tiedAtBest = classes.some(
    (c, i) => i !== bestIdx && compareDominanceClass(c, classes[bestIdx]!) === 0,
  );
  return tiedAtBest ? -1 : bestIdx;
}

/**
 * "Just serve it and proceed" (design point 1), the two parts `deriveCoverage`
 * cannot do itself: it only picks {coverage, reason}, before the pack's
 * `TaskPackResult` even exists. Called once by each producer right after
 * `result` is assembled (and BEFORE `dedupeTrimAndPersist`, so any embed
 * counts toward its byte-cap trim like any other served body):
 *
 *   1. if `resolveDominantSurfaceIndex` found a winner that is still
 *      codeless, run the identical single-handle form of the closure read
 *      `runCandidateInlinePass` uses for a live candidate-list — reusing
 *      `executeClosureRead` rather than a second embedding mechanism — so the
 *      promoted primary is genuinely actionable in THIS response, not a
 *      promise that trades the confirmation round trip for an equally costly
 *      fetch.
 *   2. demote every OTHER surface to `required: false` — the same
 *      primary/`required:true` vs. related/`required:false` split
 *      `locateTaskContext.ts`'s `buildMultiPrimaryHit` already draws between
 *      its own `primary`/`related` arrays. Undone, `buildTaskExecutionContract`
 *      (`requiredSurfaces = ...filter(required !== false)`) keeps chasing the
 *      LOSER's body next — the choice is resolved, but the contract cannot
 *      tell that a still-required, still-codeless loser is not blocking
 *      evidence. `result.missing`/`_required_surfaces` are role lists (api,
 *      test, …), never a per-candidate ledger, so this never removes a role
 *      that stays genuinely covered by the promoted primary alone.
 *
 * A no-op on every pack that never had a resolvable candidate-list to begin
 * with (`dominantIndex === -1`): `result` is left untouched, byte-identical
 * to before this fix.
 */
function applyDominancePromotion(
  result: TaskPackResult,
  surfaces: readonly TaskPackSurface[],
  dominantIndex: number,
  workspace: string,
): void {
  if (dominantIndex === -1) return;
  const winner = surfaces[dominantIndex];
  if (winner === undefined) return;
  if (!hasServedCode(winner)) {
    executeClosureRead(result, { tool: "read_file", arguments: { handle: winner.handle } }, workspace);
  }
  for (const [index, surface] of surfaces.entries()) {
    if (index !== dominantIndex) surface.required = false;
  }
}

/**
 * DESIGN-v0.8 coverage-honesty: map the main locate flow's signals to the
 * three-way coverage + its telemetry reason, in the exact priority order the
 * workstream spec defines. `unmatched` is the pre-computed unmatched-concern
 * token list (computed ONCE by the caller and reused by buildRoute, so the
 * coverage label and the route can never disagree about concern coverage).
 *
 * Precedence (first match wins):
 *   1. missingRequired>0            → partial / "missing-roles"
 *   2. unmatched>0                  → partial / "concerns-uncovered"
 *   3. locate completeness=complete → complete (no reason)
 *   4. completeness=unknown & conf>=0.7 → focused / "single-site"
 *   5. completeness=partial & primary>1 → partial / "candidate-list", UNLESS
 *      one surface strictly dominates the rest on role/evidence/source-ness
 *      (see `resolveDominantSurfaceIndex` above) — a genuinely resolved
 *      "choice" is not an open one, so the reason is dropped and the pack
 *      stays "partial" (not "focused"/"complete": those suppress
 *      `frontier_index`, and the dominated candidate must stay inventoried —
 *      design point 1/4c). `buildRoute` then falls through past its
 *      "candidate-list" branch to the plain partial-coverage route, an
 *      existing, unmodified path.
 *   6. otherwise → partial / (locate said a role is missing ? "missing-roles" : "candidate-list")
 *
 * `dominantSurfaceIndex` is the caller's OWN precomputed
 * `resolveDominantSurfaceIndex(surfaces)` (-1 by default, for any caller that
 * does not care — rule 5 then behaves exactly as before). It is a parameter
 * rather than a `surfaces` array recomputed in here because the caller needs
 * the SAME index a second time, after `deriveCoverage` returns, to serve the
 * winner's body (`applyDominancePromotion`) — computing it once keeps
 * both call sites agreeing on which surface won by construction.
 */
function deriveCoverage(
  locate: { completeness: "complete" | "partial" | "unknown"; confidence: number; primaryCount: number; missingSurfaces?: string[] },
  missingRequired: string[],
  unmatched: string[],
  dominantSurfaceIndex = -1,
): { coverage: "complete" | "focused" | "partial"; reason?: TaskPackResult["coverage_reason"]; dominancePromoted?: true } {
  if (missingRequired.length > 0) return { coverage: "partial", reason: "missing-roles" };
  if (unmatched.length > 0) return { coverage: "partial", reason: "concerns-uncovered" };
  if (locate.completeness === "complete") return { coverage: "complete" };
  if (locate.completeness === "unknown" && locate.confidence >= HIGH_CONFIDENCE_THRESHOLD) {
    return { coverage: "focused", reason: "single-site" };
  }
  if (locate.completeness === "partial" && locate.primaryCount > 1) {
    if (dominantSurfaceIndex !== -1) {
      // `dominancePromoted` is the ONE place this fired — the caller relies
      // on it (rather than re-deriving the same precondition) to know it may
      // now call `applyDominancePromotion` for `dominantSurfaceIndex`.
      return { coverage: "partial", reason: undefined, dominancePromoted: true };
    }
    return { coverage: "partial", reason: "candidate-list" };
  }
  return {
    coverage: "partial",
    reason: locate.missingSurfaces !== undefined && locate.missingSurfaces.length > 0
      ? "missing-roles"
      : "candidate-list",
  };
}

/** Max handles listed in a batch `read_code handles=[...]` next hint (mirrors MAX_SURFACES_DISTINCT). */
const MAX_BATCH_NEXT_HANDLES = 6;

/**
 * Item 3 (wiring `next` that isn't a dead end): for a two-domain WIRING query
 * ("feed A into B", "wire X into Y") whose SECOND domain / consumer is
 * uncovered, return a `next` that points at the DESTINATION side, never a
 * `explore action=find query=<source symbol>`. Finding the source is a
 * structural dead end here: the source symbol DOES exist and is already
 * surfaced — the actual gap is that the consumer that should reference it does
 * not exist yet, so a find for the source lands right back on the surface the
 * pack already returned.
 *
 * Detection (conservative, pack-local — the locator's two-domain signal is not
 * exposed on its output, so this reconstructs the shape from the query + the
 * returned surfaces):
 *   - `concreteIdentifierTokens(query)` (the SAME extraction
 *     buildWiringCooccurrenceCheck uses) must yield >= 2 concrete identifier
 *     tokens, EXCLUDING any enum new-value token (extractEnumValueTokens) — a
 *     brand-new enum member never maps to a surface and must not masquerade as
 *     an uncovered "destination".
 *   - the SOURCE (earliest such token) maps to a surface (surfaceIndexForToken)
 *     but the DEST (a later such token) does NOT map to any surface, i.e. the
 *     consumer is genuinely absent (the missing wire IS the task).
 *
 * Preference order (per the spec): (a) `explore action=references
 * symbol=<dest>` when a concrete dest token exists; (b) `explore action=find
 * query=<second domain's strongest distinct token>` (the first unmatched
 * concern token) when there is a distinct second-domain word but no clean
 * concrete dest identifier; (c) undefined → caller falls through to its
 * existing surfaceRoles re-pack branch (the last resort). Returns undefined
 * whenever the shape is NOT a two-domain wiring dead end, so every non-wiring
 * partial keeps nextHintForCoverage's existing branches untouched.
 */
function wiringConsumerNext(query: string, surfaces: TaskPackSurface[]): string | undefined {
  if (surfaces.length === 0) return undefined;
  const valueTokens = new Set(extractEnumValueTokens(query).map((t) => normIdent(t)));
  // Concrete identifier tokens in query order, minus enum new-value tokens.
  const tokens = concreteIdentifierTokens(query).filter((t) => !valueTokens.has(normIdent(t)));
  if (tokens.length < 2) return undefined;

  // A token is COVERED when it maps to a surface's basename/symbol OR appears in
  // an already-embedded surface `code` block. The `code` check is what stops a
  // false wiring dead end for an enum+aggregation query: a token like
  // "byTicket" is a FUNCTION inside an already-surfaced aggregation-service file (so
  // its basename does not name it), but the surface's embedded code does — the
  // consumer plainly exists and this is not a missing wire.
  const covered = (tok: string): boolean => {
    if (surfaceIndexForToken(tok, surfaces) !== -1) return true;
    const nt = normIdent(tok);
    return surfaces.some((s) => s.code !== undefined && normIdent(s.code).includes(nt));
  };

  const source = tokens[0]!;
  // A dest-side token is any LATER concrete token that is a genuinely different
  // identifier from the source. Pick the latest such token as the destination
  // (directionality: mentioned later = the thing being wired INTO).
  let dest: string | undefined;
  for (let i = tokens.length - 1; i >= 1; i--) {
    if (normIdent(tokens[i]!) !== normIdent(source)) { dest = tokens[i]; break; }
  }
  if (dest === undefined) return undefined;

  // The two-domain DEAD END: the destination/consumer is genuinely uncovered
  // (no surface names it AND no surface's embedded code contains it). When the
  // consumer already exists among the surfaces there is no dead end — leave
  // `next` to the normal branches.
  if (covered(dest)) return undefined;
  // Require the source to be a real, located domain — otherwise this is not a
  // "source exists, consumer missing" wiring at all; (b) handles the
  // fully-unlocated case below.
  if (covered(source)) {
    // (a) references sweep on the destination-side token: find where the
    // consumer that should receive the wire lives / is referenced.
    return `search_files action=references query=${dest}`;
  }
  // (b) neither end located cleanly: point at the second domain's strongest
  // distinct token (the first query concern with no surface match).
  const unmatched = unmatchedConcernTokens(query, surfaces);
  if (unmatched.length > 0) return `search_files action=find query=${unmatched[0]}`;
  return undefined;
}

/**
 * DESIGN-v0.8 coverage-honesty (Part 3): the single most turn-efficient `next`
 * hint for this pack's coverage state — teaches the agent the exact follow-up
 * that closes the remaining gap in ONE call, rather than a generic single
 * slice. Returns undefined only when there is no surface to point at (caller
 * keeps its own fallback).
 *
 *   - complete/focused: if ≥2 surfaces still lack embedded code, hand back a
 *     BATCH read of every code-less surface handle (the server accepts a
 *     top-level `handles:[...]` array regardless of mode; `next` strings are
 *     how an agent learns this un-advertised affordance). Else if the primary
 *     surface already carries its code, point straight at editing it. Else
 *     fall back to a single slice of the primary.
 *   - candidate-list: batch ranked candidate handles; slice only when one remains.
 *   - missing-roles: re-locate for exactly the missing roles via surfaceRoles.
 *   - concerns-uncovered: an `explore action=find` on the ONE top unmatched
 *     token (find rejects multi-token sentences, so only the first is used).
 */
/**
 * The task query as it may appear inside a `next` hint: LOSSLESS.
 *
 * Every `next` string is contractually EXECUTABLE — AGENTS.md tells the agent
 * to run it verbatim — so whatever query it carries BECOMES the next task
 * query. Five builders used to embed `query.slice(0, 60)` / `.slice(0, 80)`,
 * which cuts mid-word and silently drops the intent-bearing tail: the caller
 * then re-packs a DIFFERENT, truncated task and the original request is
 * unrecoverable. session.ts's own continuation builder already states the
 * rule this restores — "Verbatim, never an excerpt: this call must be
 * EXECUTABLE, and a truncated query is a different task."
 *
 * Two normalizations, neither of which drops a word:
 *   - whitespace collapses to single spaces (a `next` is one line, and a
 *     multi-line query would otherwise break the string it lives in);
 *   - `"` is stripped, the same scrub the sibling `fallbackQuery` already
 *     applies, because the surrounding `query="..."` encoding has no escape
 *     and `nextStringToCall` performs no unescaping.
 *
 * Deliberately NOT a `qref` hint, though the protocol prefers one: the four
 * shapes above must stay parseable by `nextStringToCall`, whose grammar has
 * no `qref` form, and `buildTaskExecutionContract` feeds the PARSED call into
 * `next_call` — the field the wire decision actually projects. Emitting an
 * unparseable qref form would delete the hint from the decision instead of
 * improving it. (The rootSuggestion re-scope hint additionally CANNOT use a
 * qref: a qref is bound to the workspace root it was minted under, and that
 * hint's whole point is moving to a different one.)
 */
// Exported for queryContinuity.spec.ts — the losslessness property is easier
// to pin directly than through the five packs that reach the five call sites.
export function continuationQuery(query: string): string {
  return query.replace(/\s+/gu, " ").replace(/"/g, "").trim();
}

function nextHintForCoverage(
  surfaces: TaskPackSurface[],
  coverage: "complete" | "focused" | "partial",
  reason: TaskPackResult["coverage_reason"] | undefined,
  query: string,
  missingRoles: string[],
  unmatchedTokens: string[],
): string | undefined {
  const primary = surfaces[0];
  if (!primary) return undefined;

  if (coverage === "complete" || coverage === "focused") {
    // code_unchanged surfaces are excluded: their content is already known
    // from the prior pack, so batching them into a re-read wastes the call.
    const codeless = surfaces
      .filter((s) => s.code === undefined && s.code_unchanged === undefined)
      .map((s) => s.handle);
    if (codeless.length >= 2) {
      const list = codeless.slice(0, MAX_BATCH_NEXT_HANDLES).map((h) => `"${h}"`).join(",");
      return `read_file handles=[${list}]`;
    }
    if (primary.code !== undefined) {
      return `edit_file handle=${primary.handle}`;
    }
    return `read_file mode=slice handle=${primary.handle}`;
  }

  if (reason === "candidate-list") {
    // 2026-07-19a: only CODELESS candidates justify a follow-up read — the old
    // unconditional batch re-read of every candidate handle kept pointing at
    // bodies the pack itself already served, refuelling the full/slice thrash
    // it was meant to close. With nothing codeless left, choosing IS the next
    // act, so there is no `next` at all (the execution contract lands on
    // choose-candidate / awaiting-input instead).
    const codeless = [...new Set(
      surfaces
        .filter((surface) => surface.code === undefined && surface.code_unchanged === undefined)
        .map((surface) => surface.handle),
    )].slice(0, MAX_BATCH_NEXT_HANDLES);
    if (codeless.length > 1) {
      return `read_file handles=${JSON.stringify(codeless)}`;
    }
    if (codeless.length === 1) {
      return `read_file mode=slice handle=${codeless[0]}`;
    }
    // B1c (2026-08-01 retrieval-scope): with every candidate body served the
    // choice IS the next act, so there is still no `next` here — the EXECUTABLE
    // recovery for a candidate list whose query named files it could not serve
    // is attached at the shared exit instead (attachCandidateListRecovery),
    // where the workspace is in scope.
    return undefined;
  }

  // Item 1 (named next when the style file was not found): a still-missing
  // "style" role gets a CONCRETE `explore find <derived token stem>` (the CSS
  // custom-property / member stem) — never a bare "locate the style role"
  // re-pack. Only when augmentStyleSurface could NOT name a stylesheet
  // (styleNextForMissingRole is a no-op once a style surface exists).
  if (reason === "missing-roles" && missingRoles.includes("style")) {
    const styleNext = styleNextForMissingRole(query, surfaces);
    if (styleNext) return styleNext;
  }

  // Item 3 (wiring `next` that isn't a dead end): a two-domain wiring query
  // whose consumer is uncovered gets a destination-side hint (references the
  // dest token / finds the second domain's distinct token) INSTEAD of the
  // dead-end `explore action=find <source>` a concerns-uncovered / missing-role
  // partial would otherwise emit. Fires only for that specific shape (see
  // wiringConsumerNext); every other partial falls through to the existing
  // branches below unchanged (which are, for missing-roles, the surfaceRoles
  // re-pack that is item 3's own last-resort option (c)).
  const wiringNext = wiringConsumerNext(query, surfaces);
  if (wiringNext) return wiringNext;

  if (reason === "missing-roles" && missingRoles.length > 0) {
    const roles = missingRoles.map((r) => `"${r}"`).join(",");
    return `read_file mode=task_pack query="${continuationQuery(query)}" surfaceRoles=[${roles}]`;
  }
  if (reason === "concerns-uncovered" && unmatchedTokens.length > 0) {
    return `search_files action=find query=${unmatchedTokens[0]}`;
  }

  // Any other partial (e.g. diff-truncated) keeps a single-slice fallback — but
  // ONLY when that slice can still deliver bytes the caller does not already
  // hold. Re-slicing a fully-served primary is a no-op read, and it was the
  // fuel for exactly the full/slice thrash the two branches above each closed
  // on their own side (complete/focused checks `primary.code !== undefined`;
  // candidate-list went codeless-only in 2026-07-19a). This catch-all had been
  // left behind, so a pack that served every byte of surfaces[0] still told the
  // caller to read it again — observed live 2026-07-31 on a 32-line
  // package.json whose whole body was embedded in the same response. With no
  // progressing read left, returning undefined is the honest answer: it lets
  // the execution contract land on prepared / awaiting-input instead of naming
  // a call that cannot advance discovery.
  const remainingWindow = primary.remaining_ranges?.[0];
  if (remainingWindow) {
    return `read_file mode=slice handle=${primary.handle} range=${remainingWindow}`;
  }
  if (primary.code === undefined && primary.code_unchanged === undefined) {
    return `read_file mode=slice handle=${primary.handle}`;
  }
  return undefined;
}

// ---------------------------------------------------------------------------
// Item 3 (answer/explain routing): a query that only asks the agent to
// EXPLAIN/DESCRIBE something must not route as if an edit were expected —
// "edit_from_handles" reads as an instruction to go change code. Shared by
// buildRoute's every "the pack is done, nothing required is missing" branch
// (the route assembly is shared by both buildTaskPack and
// buildSeededTaskPack), so both flows get the same relabeling for free.
// ---------------------------------------------------------------------------

/**
 * Conservative English+Japanese marker lists. English markers are matched at
 * word boundaries (`\b`) so "add" cannot fire inside "address"; Japanese
 * markers are matched as plain substrings ("\b" is meaningless for non-\w
 * CJK text in JS regex — there is no word-boundary concept to anchor on).
 */
const REFERENCE_ANSWER_EN_RE = /\bfind\s+all\s+references?\b/i;
const REFERENCE_ANSWER_JA_RE = /全参照/u;
const ANSWER_INTENT_EN_RE = /\b(explain|describe|assess|trace|determine|investigate|why|how does|what happens|what conditions|when (?:does|do|is|are|must|should|can|will)|walk me through|where (?:is|are|does|do|did|was|were)|what (?:is|are|does|do|did)|which (?:file|files|function|functions|class|classes|module|modules|method|methods)|understand(?:ing)?|overview|summar(?:y|ies|ize[sd]?|ise[sd]?|izing|ising)|clarify|tell me|show me (?:how|where|what|which)|how (?:is|are|do|did|can|would|should|were|was)|which \w+ (?:does|do|is|are|gets?|selects?|chooses?|uses?|handles?|calls?|owns?))\b/i;
// Descriptive JA noun phrases (「Xの実装」「Xの役割」) ask ABOUT code; request
// forms use を/へ/に/として and are caught by REQUESTED_MUTATION_JA_NOUN_RE
// first (classifyTaskProfile reaches the answer branch only when no mutation
// intent matched), so 「実装」 here cannot flip an actual implement-request.
const ANSWER_INTENT_JA_NOUN_RE = /の(?:実装|定義|役割|仕組み|使い方|場所|中身|挙動|意味|目的)/;
const ANSWER_INTENT_JA: readonly string[] = [
  "説明", "解説", "なぜ", "どういう", "挙動",
  // 2026-08-01 probe sweep: JA interrogatives were unrepresented, so 「教えて
  // ください」/「〜ですか」-shaped questions fell to the no-marker generic
  // fallback and received edit-imperative certificates. Mutation intent is
  // checked FIRST, so 「修正してもらえますか」 still classifies as a change.
  "教えて", "ですか", "でしょうか", "どこ", "何のため",
  // 2026-08-21 §14 tiered-guardrail wave (issue #1): comprehension-shaped
  // read-only asks (「〜を理解したい」「pipelineがどのcodecを選ぶか」) carried
  // none of the markers above, so classifyTaskProfile never even reached its
  // answer branch. bindTaskProfile (below) now honors every inferred
  // "answer" unless hasDefectSymptomMarker also matches the query.
  "理解", "知りたい", "把握", "どの", "どれ", "どのように", "どうやって", "どんな",
  "流れ", "概要", "全体像", "一覧", "何が", "何を", "どこで",
];
const ANALYSIS_INTENT_EN_RE = /\b(?:analy[sz]e|analysis|review|inspect|check|verify|validate|audit|recommend|recommendation|proposal|propose|measure|measurement|evaluate|evaluation|benchmark(?:ing)?|compare|comparison|metrics?|rate|ratio|percentage)\b/i;
const ANALYSIS_INTENT_JA: readonly string[] = [
  "分析", "確認", "調査", "診断", "検証", "測定", "計測", "評価", "比較", "割合", "性能", "メトリクス", "ベンチ", "提案", "改善案", "向上案",
];
const EXPLICIT_NO_EDIT_EN_RE = /\b(?:no|without)\s+(?:code\s+)?(?:changes?|edits?|implementation)(?:\s+(?:are\s+)?(?:needed|required))?\b/gi;
const EXPLICIT_NO_EDIT_JA_RE = /(?:コードの?)?(?:変更|修正|追加|実装|編集|改変)(?:は|が)?(?:不要|なし|要らない|いらない|しない)/g;
const ARTIFACT_BUILD_INTENT_EN_RE = /\b(?:implement|add|build|create|generate|update|change|fix|refactor)\b/i;
const ARTIFACT_BUILD_INTENT_JA: readonly string[] = ["実装", "追加", "作成", "生成", "変更", "修正"];
const ARTIFACT_BUILD_REFERENCE_EN_RE = /\b(?:spreadsheet|xlsx|workbook|sheet|table|document|docx|pptx|pdf|artifact)\b/i;
const ARTIFACT_BUILD_REFERENCE_JA: readonly string[] = ["スプレッドシート", "表", "資料", "シート"];
const WIRING_INTENT_EN_RE = /\b(?:wire|connect|integrate|plumb|route|propagate|feed|forward|expose)\b/i;
const WIRING_INTENT_JA: readonly string[] = [
  "接続", "連携", "配線", "結線", "組み込", "組み入れ", "流し込", "受け渡", "反映させ",
];
// Auto mode cannot delegate semantic intent classification to a model.  Its
// fallback therefore recognizes a REQUEST-SHAPED predicate, not a bag of
// words: the mutation action must be imperative/modal (English) or carry a
// request/volitional ending (Japanese).  Mentioning "fix", "connect", or
// "実装" as a report label or analysis topic is insufficient.
const REQUESTED_MUTATION_EN_RE = /(?:^|[\n.!?;:]\s*|\bthen\s+)(?:[①②③④⑤⑥⑦⑧⑨⑩]|\(?\d{1,2}[.)])?\s*(?:please\s+)?(?:using\b[^,\n]{0,80},\s*)?(?:fix|add|implement|change|replace|rename|refactor|wire|connect|integrate|plumb|route|propagate|feed|forward|expose|create|generate|update)\b|\b(?:please|must|should|need(?:s)?\s+to|want(?:s)?\s+(?:you\s+)?to|can\s+you|could\s+you|would\s+you)\b[^.!?\n]{0,100}\b(?:fix|add|implement|change|replace|rename|refactor|wire|connect|integrate|plumb|route|propagate|feed|forward|expose|create|generate|update)\b/i;
const REQUESTED_MUTATION_EN_CONDITIONAL_RE = /\b(?:when|if|after|once|while|where)\b[^.!?\n,]{0,160},\s*(?:please\s+)?(?:fix|add|implement|change|replace|rename|refactor|wire|connect|integrate|plumb|route|propagate|feed|forward|expose|create|generate|update)\b/i;
const REQUESTED_MUTATION_EN_RELATION_RE = /\b(?:and|then)\s+(?:please\s+)?(?:(?:wire|connect|integrate|plumb|route|propagate|feed|forward|expose)\s+[^,.;\n]{1,100}\s+(?:to|into|with|through)\b|rename\s+[^,.;\n]{1,80}\s+to\s+[^,.;\n]+)/i;
const REQUESTED_MUTATION_JA_RE = /(?:修正|追加|実装|変更|置換|改名|リファクタ|接続|連携|配線|結線|組み込|組み入れ|流し込|受け渡|反映|作成|生成)(?:を)?(?:してください|して下さい|してほしい|して欲しい|したい|しろ|せよ|をお願いします|を行って(?:ください|下さい)?)|直(?:してください|して下さい|してほしい|して欲しい|したい|せ|す)|(?:修正|追加|実装|変更|接続|連携|作成|生成)(?:して)?(?:もらえますか|いただけますか)/;
const REQUESTED_MUTATION_JA_LINE_RE = /(?:^|\n)[^\n]{0,180}(?:を|へ|に)[^\n]{0,40}(?:修正|追加|実装|変更|置換|改名|リファクタ|接続|連携|配線|結線|組み込|組み入れ|流し込|受け渡|反映|作成|生成)(?:する)?\s*[:：。.!]?\s*(?=\n|$)/;
// 体言止め request forms (2026-07-25 run-2 forensics): a JA task written in
// noun-final style — 「…として新規作成。」「…を実装。」 — carries the same
// request force as してほしい but matched NO mutation regex, so the pack lost
// artifact_build/prepared and the solver fell back to a native Write. The
// particle prefix (を/へ/に/として) plus sentence-final position keeps report
// labels (「実装状況を確認して報告」) from matching.
const REQUESTED_MUTATION_JA_NOUN_RE = /(?:を|へ|に|として)[^\n。]{0,40}?(?:新規)?(?:修正|追加|実装|作成|生成|変更|接続|連携|配線|反映)\s*(?:[。.]|$)/;
const PROPAGATION_INTENT_EN_RE = /\b(?:enum|union|schema|contract|exhaustive|every(?:where)?|all\s+(?:callers|references|consumers|definitions|direct\s+callees)|propagat(?:e|ion))\b/i;
const PROPAGATION_INTENT_JA: readonly string[] = [
  "列挙", "全参照", "呼び出し元", "対応漏れ", "すべて", "全て", "各層", "波及",
];

// P1-e (2026-08-28 review-fix wave): hasOpenUniverseIntent/isAdditiveEnumIntent
// moved to openUniverseIntent.ts, the single canonical source this module and
// taskContractStore.ts both reference (see that module's header) — extracted
// rather than cross-imported so there is no cycle between the two. Imported
// above with the other feature imports; re-exported here so every existing
// `from "./readCodeTaskPack.js"` import of either name keeps working.
export { hasOpenUniverseIntent, isAdditiveEnumIntent };

/**
 * True when `query` reads as an explain/answer request AND carries no
 * edit-intent marker at all. Deliberately conservative — a query naming
 * BOTH an answer marker and an edit marker ("explain then fix the bug") is
 * left alone (ambiguous), same as a query with no answer marker at all;
 * buildRoute keeps whatever action its own coverage-driven logic decided in
 * either case.
 */
function hasAnswerIntentMarker(query: string): boolean {
  return (proofCompletionEnabled() && (
    REFERENCE_ANSWER_EN_RE.test(query)
    || REFERENCE_ANSWER_JA_RE.test(query)
  ))
    || ANSWER_INTENT_EN_RE.test(query)
    || ANSWER_INTENT_JA.some((m) => query.includes(m))
    || ANSWER_INTENT_JA_NOUN_RE.test(query)
    || hasAnalysisIntentMarker(query);
}

function hasAnalysisIntentMarker(query: string): boolean {
  return ANALYSIS_INTENT_EN_RE.test(query)
    || ANALYSIS_INTENT_JA.some((marker) => query.includes(marker));
}

function hasRequestedMutationIntent(query: string): boolean {
  const positiveText = query
    .replace(EXPLICIT_NO_EDIT_EN_RE, " ")
    .replace(EXPLICIT_NO_EDIT_JA_RE, " ");
  return REQUESTED_MUTATION_EN_RE.test(positiveText)
    || REQUESTED_MUTATION_EN_CONDITIONAL_RE.test(positiveText)
    || REQUESTED_MUTATION_EN_RELATION_RE.test(positiveText)
    || REQUESTED_MUTATION_JA_RE.test(positiveText)
    || REQUESTED_MUTATION_JA_LINE_RE.test(positiveText)
    || REQUESTED_MUTATION_JA_NOUN_RE.test(positiveText);
}

function hasExplicitNoEditDeclaration(query: string): boolean {
  // The shared regexes carry /g/, so use match() rather than stateful test().
  return query.match(EXPLICIT_NO_EDIT_EN_RE) !== null
    || query.match(EXPLICIT_NO_EDIT_JA_RE) !== null;
}

// Identifier-embedded verbs are not task intent: 「renameSymbol の実装」 names
// rename only inside a camelCase identifier. The old lowercase substring test
// destroyed that boundary ("renamesymbol".includes("rename")); \brename\b on
// the RAW query cannot match inside renameSymbol/RENAME_TABLE (the next char
// is a word char), while prose requests ("rename X to Y", 「改名」) still do.
const RENAME_REQUEST_EN_RE = /\brenam(?:e|es|ed|ing)\b/i;
const RENAME_REQUEST_JA: readonly string[] = ["リネーム", "改名", "名前を変更", "名称を変更", "名称変更"];
function hasRenameRequestMarker(query: string): boolean {
  return RENAME_REQUEST_EN_RE.test(query)
    || RENAME_REQUEST_JA.some((marker) => query.includes(marker));
}

function hasArtifactBuildIntentMarker(query: string): boolean {
  const positiveText = query
    .replace(EXPLICIT_NO_EDIT_EN_RE, " ")
    .replace(EXPLICIT_NO_EDIT_JA_RE, " ");
  const hasEditIntent = ARTIFACT_BUILD_INTENT_EN_RE.test(positiveText)
    || ARTIFACT_BUILD_INTENT_JA.some((marker) => positiveText.includes(marker));
  if (!hasEditIntent) return false;
  return ARTIFACT_BUILD_REFERENCE_EN_RE.test(query)
    || ARTIFACT_BUILD_REFERENCE_JA.some((marker) => query.includes(marker))
    || ARTIFACT_EXTS.some((ext) => query.toLowerCase().includes(ext));
}

function hasWiringIntentMarker(query: string): boolean {
  const positiveText = query
    .replace(EXPLICIT_NO_EDIT_EN_RE, " ")
    .replace(EXPLICIT_NO_EDIT_JA_RE, " ");
  if (positiveText !== query) return false;
  return WIRING_INTENT_EN_RE.test(positiveText)
    || WIRING_INTENT_JA.some((marker) => positiveText.includes(marker));
}

function hasPropagationIntentMarker(query: string): boolean {
  return PROPAGATION_INTENT_EN_RE.test(query)
    || PROPAGATION_INTENT_JA.some((marker) => query.includes(marker));
}

// ---------------------------------------------------------------------------
// 2026-08-23 mixed-intent guard: an explicit mutation request PLUS a distinct
// enumeration/explanation sub-request must not classify as "answer".
//
// T07 (real field query this guard was cut for): "...credit-normal を考慮し
// てないっぽい。修正して、影響の出る型一覧も列挙して。" hasRequestedMutationIntent
// (above) deliberately requires 修正/直/実装/追加/変更/削除 to be followed by an
// explicit ください/て欲しい/たい/しろ/せよ suffix — see the note above
// REQUESTED_MUTATION_JA_NOUN_RE — because a bare te-form ("修正して") is also
// how a REPORT clause reads ("すでに修正して問題ない", nothing to do here). That
// same bare te-form is ALSO the everyday Japanese instruction-chain register
// with no polite suffix at all ("Xして、Yして。": do X, then do Y) — T07's
// actual shape, and hasRequestedMutationIntent stays exactly as strict as
// before for it (every already-recognized mutation-shaped query short-circuits
// on `mutationRequested` above and never reaches this guard).
//
// This predicate only fires for a query that carries BOTH signals: the bare
// mutation verb above AND a SEPARATE sub-question asking to list/explain
// something (列挙/一覧/教えて/説明して; list/enumerate/explain/which) — the
// two-verb instruction chain T07 is shaped like. An enumeration marker with NO
// mutation verb at all (a pure 「〜一覧を教えて」 read, or 「どのcodecを選ぶか」)
// leaves hasMutationVerb false and the whole predicate short-circuits, so the
// §14 guardrail in bindTaskProfile still owns that case exactly as before.
//
// Root-cause note: bindTaskProfile's own §14 guardrail ALREADY downgraded
// T07's inferred "answer" to profile_binding.selected:"generic" (its
// defect-symptom marker "バグ" disqualified the honored-answer arm) — that
// half was never broken. The bug lived one level up: classifyTaskProfile
// itself still returned "answer" for T07, and isAnswerIntentQuery (below)
// reads that RAW result to relabel an edit_from_handles route to
// answer_from_handles, with NO §14 gate of its own — so a pack whose profile
// was already "generic" still shipped decision.kind:"act.answer". Fixing the
// shared classifier below (rather than adding a second guardrail at the route
// layer) closes both consumers at once.
// ---------------------------------------------------------------------------
const MIXED_INTENT_MUTATION_JA: readonly string[] = [
  "修正して", "直して", "実装して", "追加して", "変更して", "削除して",
];
// Symmetric with the JA half above but deliberately NOT clause-anchored
// (unlike REQUESTED_MUTATION_EN_RE, which already recognizes a clause-initial
// or modal-led imperative on its own via hasRequestedMutationIntent — this
// predicate only runs after that stricter check has already failed). It exists
// for the embedded English mirror of the JA bare-te-form shape ("...and once
// you fix the Compute logic, list the affected types"). Requiring an object
// token after the verb keeps a bare mention ("needs a fix.", "the recent
// update.") from matching. The negative lookbehind excludes the same verb used
// as a NOUN with a determiner ("a fix for...", "the recent update...") — "This
// is a fix for the reported crash." must not count — and, separately, as the
// INFINITIVE OBJECT of an explain-shaped request ("explain how TO implement a
// change...") or under negation ("do NOT change code") — both read as
// describing/declining a change, not requesting one. Caught live in this
// change's own review: "explain how to implement a change in
// QuoteOrchestrator; do not change code" (taskProfileBinding.spec.ts) pairs
// "explain" with two now-excluded "implement"/"change" occurrences and must
// stay off this predicate entirely.
const MIXED_INTENT_MUTATION_EN_RE = /(?<!\b(?:a|an|the|this|that|some|any|to|not|never|no)\s)\b(?:fix|implement|add|change|remove|update)\b\s+\S/i;
const MIXED_INTENT_ENUMERATION_JA: readonly string[] = ["列挙", "一覧", "教えて", "説明して"];
const MIXED_INTENT_ENUMERATION_EN_RE = /\b(?:list|enumerate|explain|which)\b/i;

function hasMixedMutationAndEnumerationIntent(query: string): boolean {
  const hasMutationVerb = MIXED_INTENT_MUTATION_JA.some((marker) => query.includes(marker))
    || MIXED_INTENT_MUTATION_EN_RE.test(query);
  if (!hasMutationVerb) return false;
  return MIXED_INTENT_ENUMERATION_JA.some((marker) => query.includes(marker))
    || MIXED_INTENT_ENUMERATION_EN_RE.test(query);
}

/** WS3 binding classifier; every unbound/low-confidence shape stays generic. */
function classifyTaskProfile(query: string): TaskProfile {
  const mutationRequested = hasRequestedMutationIntent(query);
  if (mutationRequested) {
    if (hasArtifactBuildIntentMarker(query)) return "artifact_build";
    if (hasWiringIntentMarker(query)) return "wiring";
    const concernGroups = concernGroupsForQuery(query);
    // Enum fan-out requests frequently enumerate the required layers as a
    // bullet list. Those bullets are one propagation contract, not independent
    // defects. Listed symptoms remain authoritative for non-enum work.
    if (isEnumLikeQueryShaped(query) && !hasExplicitIndependentConcernMarker(query)) {
      return "change_propagation";
    }
    if (
      concernGroups.length >= 2
      && (
        hasIndependentEditConcernClauses(query)
        || hasExplicitIndependentConcernMarker(query)
      )
    ) {
      return "multi_concern";
    }
    if (isEnumLikeQueryShaped(query)) return "change_propagation";
    if (concernGroups.length >= 2 && hasExplicitConcernList(query)) return "multi_concern";
    if (hasPropagationIntentMarker(query)) return "change_propagation";
    return "generic";
  }
  if (hasAnalysisIntentMarker(query) || hasAnswerIntentMarker(query)) {
    // A query that only READS as answer-shaped per the markers above may still
    // carry an explicit mutation request that hasRequestedMutationIntent's
    // stricter regexes miss (see the block comment above
    // hasMixedMutationAndEnumerationIntent). When it ALSO carries a distinct
    // enumeration/explanation sub-request, the mutation intent dominates: this
    // is a change task with a reporting sub-question, not a read-only ask.
    if (hasMixedMutationAndEnumerationIntent(query)) return "generic";
    return "answer";
  }
  return "generic";
}

// ---------------------------------------------------------------------------
// 2026-08-21 §14 tiered guardrail (issue #1, revised twice): the
// ANSWER_INTENT_* markers above were widened so genuinely comprehension-
// shaped queries (「〜を理解したい」, "which codec does the pipeline
// select") classify as "answer" in the first place — previously they carried
// no recognized marker at all and never reached this branch. Widening recall
// alone would just make the §14 misfire guardrail below fire MORE often,
// since every inferred "answer" still fell back to generic. Two follow-on
// refinements to which inferred-"answer" queries that guardrail honors:
//   1. An early revision required a STRONG, unambiguous comprehension marker
//      (understand/explain/overview/...) before honoring — too narrow, since
//      a plain interrogative ("which codec does the pipeline choose for a
//      response?", 「pipelineがどのcodecを選ぶか」) carries an ordinary
//      ANSWER_INTENT_* marker but no "strong" one, so it still paid the
//      round trip for no reason.
//   2. Dropping that requirement entirely (honor unless defect-symptom) was
//      too broad the other way: hasAnswerIntentMarker also ORs in
//      hasAnalysisIntentMarker, so open-ended analysis/investigation asks
//      ("assess/trace/determine/investigate X", "analyze X and propose
//      improvements") — the SYMPTOM-ADJACENT shape the original 2026-07-25
//      misfire guardrail exists to catch (DESIGN-v0.9 §14 item 3, tripped by
//      "why does the checkout total come out wrong?"-shaped asks on edit
//      tasks) — got swept in too.
// The rule below now requires BOTH a genuine interrogative/comprehension
// marker (hasInterrogativeAnswerMarker — ANSWER_INTENT_JA/NOUN_RE plus a
// phrase-gated EN variant, see the comment above that function) AND no
// defect-symptom marker (hasDefectSymptomMarker). Pure analysis-intent
// wording, and a BARE investigation verb with no interrogative clause,
// stays on the §14 fallback exactly as before.
// ---------------------------------------------------------------------------
const DEFECT_SYMPTOM_EN_RE = /\b(?:bugs?|fail(?:s|ed|ing|ure)?|errors?|broken|wrong|incorrect(?:ly)?|crash(?:es|ed|ing)?|regression|doesn't|does not|isn't|not working|unexpected(?:ly)?|mismatch|flaky|hang(?:s|ing)?|leak(?:s|ing)?|missing|ignore(?:s|d)?|skip(?:s|ped)?|duplicate(?:s|d)?|twice|double[- ]?(?:counted|charged|applied)|slow(?:ly)?|timeout(?:s)?|times? out|inconsistent|never|should (?:be|have|not)|supposed to|instead of|even though|despite|why (?:isn't|aren't|doesn't|don't|won't|can't|didn't))\b/i;
const DEFECT_SYMPTOM_JA: readonly string[] = [
  "不具合", "失敗", "エラー", "バグ", "壊れ", "おかしい", "誤", "動かない", "期待通り", "ずれ", "違う", "異常", "落ち",
  "抜け", "漏れ", "無視", "重複", "二重", "遅い", "固まる", "されない", "ならない", "はず", "できない", "出ない", "消え", "消える", "返らない", "効かない", "反映されない",
];

function hasDefectSymptomMarker(query: string): boolean {
  return DEFECT_SYMPTOM_EN_RE.test(query)
    || DEFECT_SYMPTOM_JA.some((marker) => query.includes(marker));
}

// hasAnswerIntentMarker ORs in hasAnalysisIntentMarker (assess/analyze/
// review/measure/propose/investigate-as-analysis-noun, plus the JA analysis
// list), so classifyTaskProfile can still infer "answer" for pure analysis
// wording. That inference must keep paying the §14 round trip under auto
// (see the block comment above DEFECT_SYMPTOM_EN_RE) — only a genuine
// INTERROGATIVE/comprehension marker earns the honored one-call fast path.
//
// ANSWER_INTENT_EN_RE itself also carries four bare investigation verbs
// (assess/trace/determine/investigate) so classifyTaskProfile infers
// "answer" for them too (ANSWER_INTENT_EN_RE is untouched here, so that
// stays exactly as before). But a BARE "assess X" / "trace X" /
// "determine X" / "investigate X" is the same open-ended shape as the
// analysis-intent wording above, not a genuine interrogative. Moving those
// four words into ANALYSIS_INTENT_EN_RE wholesale is NOT safe: it would
// also flip "trace how read_file task_pack builds execution_contract
// readiness..." (a real interrogative that must stay honored) off the fast
// path, AND it would leak into the unrelated analysis-source readiness
// obligation gate (~L13936, `if (hasAnalysisIntentMarker(query))`) that
// reads the very same shared regex. So this predicate uses its own
// INTERROGATIVE_ANSWER_EN_RE: identical to ANSWER_INTENT_EN_RE except those
// four verbs require an explicit "how"/"why" clause ("trace how X builds
// Y") to count; a bare "trace X" does not, and keeps paying the §14 round
// trip like any other analysis-shaped ask.
const INTERROGATIVE_ANSWER_EN_RE = /\b(explain|describe|(?:assess|trace|determine|investigate)\s+(?:how|why)|why|how does|what happens|what conditions|when (?:does|do|is|are|must|should|can|will)|walk me through|where (?:is|are|does|do|did|was|were)|what (?:is|are|does|do|did)|which (?:file|files|function|functions|class|classes|module|modules|method|methods)|understand(?:ing)?|overview|summar(?:y|ies|ize[sd]?|ise[sd]?|izing|ising)|clarify|tell me|show me (?:how|where|what|which)|how (?:is|are|do|did|can|would|should|were|was)|which \w+ (?:does|do|is|are|gets?|selects?|chooses?|uses?|handles?|calls?|owns?))\b/i;

function hasInterrogativeAnswerMarker(query: string): boolean {
  return (proofCompletionEnabled() && (
    REFERENCE_ANSWER_EN_RE.test(query)
    || REFERENCE_ANSWER_JA_RE.test(query)
  ))
    || INTERROGATIVE_ANSWER_EN_RE.test(query)
    || ANSWER_INTENT_JA.some((m) => query.includes(m))
    || ANSWER_INTENT_JA_NOUN_RE.test(query);
}

/**
 * An explicit profile is the caller's structured semantic declaration.  The
 * server only overrides it for the two operation-level safety contradictions:
 * answer + a request-shaped mutation, or a change profile + explicit no-edit.
 * It does not use incidental analysis/wiring words to choose between declared
 * change shapes.
 *
 * 2026-08-21: `auto`'s §14 misfire guardrail below honors an inferred
 * "answer" only when the query carries an interrogative/comprehension
 * marker (hasInterrogativeAnswerMarker) and no defect-symptom marker — pure
 * analysis-intent wording (hasAnalysisIntentMarker) stays on the §14
 * fallback.
 */
function bindTaskProfile(requested: TaskProfileRequest | undefined, query: string): TaskProfileBinding {
  const request = requested ?? "auto";
  const inferred = classifyTaskProfile(query);
  if (request === "auto") {
    // DESIGN-v0.9 §14 pre-registered guardrail, tripped live 2026-07-25
    // (answer-profile misfire 60% vs the 10% ceiling): an INFERRED "answer"
    // falls back to the generic pack unless the query itself DECLARES
    // read-only work (explicit no-edit wording) — the misfires were
    // symptom-question phrasings on edit tasks, not declared-no-edit asks.
    // A caller-pinned taskProfile:"answer" is honored below unless the
    // request itself contains an unmistakable, non-negated mutation command.
    // (.match, not .test: the shared regexes carry /g/ and .test would be
    // lastIndex-stateful across calls.)
    //
    // 2026-08-21 (revised again): explicitNoEdit stays an unconditional
    // override; otherwise an inferred "answer" is honored only when the
    // query carries a genuine interrogative/comprehension marker
    // (hasInterrogativeAnswerMarker) with no defect-symptom wording. An
    // inferred "answer" driven ONLY by analysis-intent wording (assess/
    // analyze/review/measure/propose/investigate a symptom-free subject, no
    // interrogative marker present) still pays the round trip — that shape
    // is exactly what the original 2026-07-25 misfire guardrail protects.
    const explicitNoEdit = hasExplicitNoEditDeclaration(query);
    const interrogativeAnswer = inferred === "answer" && hasInterrogativeAnswerMarker(query);
    const defectSymptom = inferred === "answer" && hasDefectSymptomMarker(query);
    const honorInferredAnswer = explicitNoEdit || (interrogativeAnswer && !defectSymptom);
    const selected = inferred === "answer" && !honorInferredAnswer ? "generic" : inferred;
    return {
      requested: "auto",
      selected,
      source: "inferred",
      confidence: selected === "generic" ? 0.4 : 0.8,
      reason: inferred === "answer" && selected === "generic"
        ? "answer-shaped query served as generic per §14 misfire guardrail; declare taskProfile:\"answer\" for a read-only pack"
        : inferred === "answer" && selected === "answer" && !explicitNoEdit
          ? "answer-shaped query with no mutation or defect-symptom marker; declare taskProfile to override"
          : selected === "generic"
            ? "no high-confidence task-shape marker"
            : `query matched ${selected}`,
    };
  }

  // taskProfile is a structured declaration derived from the user's request,
  // not an authority to contradict the verbatim request itself. A stale or
  // mis-copied `answer` hint on an unmistakable mutation request used to
  // produce a prepared answer pack, after which the caller necessarily edited
  // and counted as an answer-profile misfire. Refine that contradiction using
  // only request-shaped language; repository names, paths, task IDs, and
  // benchmark membership never participate. An explicit no-edit declaration
  // remains authoritative for read-only reviews that discuss possible fixes.
  if (
    request === "answer"
    && !hasExplicitNoEditDeclaration(query)
    && hasRequestedMutationIntent(query)
  ) {
    return {
      requested: request,
      selected: inferred === "answer" ? "generic" : inferred,
      source: "evidence",
      confidence: inferred === "generic" ? 0.85 : 0.9,
      reason: `explicit mutation request contradicts answer hint; refined to ${
        inferred === "answer" ? "generic" : inferred
      }`,
    };
  }

  // The caller supplies taskProfile only for user-declared operation intent.
  // Lexical mutation/no-edit phrases are weaker evidence and must not reverse
  // that declaration. Evidence may still refine one change subtype into
  // another without changing the read-only-versus-write operation.
  if (
    request !== "answer"
    && inferred !== "answer"
    && inferred !== "generic"
    && inferred !== request
  ) {
    return {
      requested: request,
      selected: inferred,
      source: "evidence",
      confidence: 0.85,
      reason: `request-shaped operation evidence refines ${request} to ${inferred}`,
    };
  }
  return {
    requested: request,
    selected: request,
    source: "explicit",
    confidence: 0.98,
    reason: `caller supplied validated ${request} task shape`,
  };
}

function isAnswerIntentQuery(query: string): boolean {
  // 2026-07-25 note: classifyTaskProfile only reaches its "answer" branch when
  // hasRequestedMutationIntent is false, so an edit request that the mutation
  // regexes RECOGNIZE can never relabel here. The T10 run-2 dead end (route
  // answer_from_handles + allowed:["request-user-input"] on an edit task) was
  // a mutation-intent MISS on 体言止め phrasing — fixed at the classifier
  // (REQUESTED_MUTATION_JA_NOUN_RE), which is the root; a stricter gate here
  // was tried and reverted because it broke explicit-answer route coherence.
  return classifyTaskProfile(query) === "answer";
}

/**
 * Every buildRoute branch that would otherwise return `edit_from_handles`
 * (the pack is done; nothing required is missing) funnels through here so
 * the answer/explain relabeling lives in ONE place. `max_additional_tl_calls`
 * semantics are unchanged — every edit_from_handles branch already passes 0
 * (this pack closes the task in zero more calls); the override only swaps
 * `action`/`reason`, never the call-budget number. `query` is optional
 * (some historical callers omit it) — undefined means "ambiguous", which
 * keeps the current action.
 */
function editFromHandlesRoute(reason: string, maxCalls: number, query?: string, zoomNote?: string): NonNullable<TaskPackResult["route"]> {
  if (query !== undefined && isAnswerIntentQuery(query)) {
    // A5 (2026-08-01 signal5-2 T03): the answer rewrite used to DISCARD the
    // caller-composed reason — including the remaining_ranges zoom affordance —
    // so an answer pack with a partial primary claimed closure with no zoom
    // path. Preserve the zoom note and grant the one sanctioned zoom call.
    return {
      action: "answer_from_handles",
      reason: zoomNote !== undefined
        ? `query reads as explain/answer — no edit expected; ${zoomNote}`
        : "query reads as explain/answer — no edit expected",
      max_additional_tl_calls: zoomNote !== undefined ? Math.max(maxCalls, 1) : maxCalls,
    };
  }
  return { action: "edit_from_handles", reason, max_additional_tl_calls: maxCalls };
}

function buildRoute(
  surfaces: TaskPackSurface[],
  missingRequired: string[],
  coverage?: "complete" | "focused" | "partial",
  allMissing?: string[],
  query?: string,
  coverageReason?: TaskPackResult["coverage_reason"],
): NonNullable<TaskPackResult["route"]> {
  if (surfaces.length === 0) {
    return {
      action: "locate_missing_surfaces",
      reason: "no editable handles returned",
      max_additional_tl_calls: 1,
    };
  }
  if (missingRequired.length > 0) {
    return {
      action: "locate_missing_surfaces",
      reason: `missing required surface(s): ${missingRequired.join(",")}`,
      max_additional_tl_calls: 1,
    };
  }

  // Wave 4 (2026-07-24 T10/T13 forensics): a done-route that stays silent
  // while a required surface is disclosed-partial reads as a false "complete"
  // — the model re-probes empirically and learns to distrust the stop signal.
  // Name the zoom affordance in the reason. The call budget stays 0: under the
  // serve-with-state gate a re-slice of a served handle is always available,
  // and max_additional_tl_calls keeps its "calls needed to CLOSE the task"
  // meaning (certification acceptance requires 0 — a zoom affordance is not
  // missing evidence).
  const partialRequired = surfaces.filter((surface) =>
    surface.required !== false
    && (surface.content_completeness === "partial" || (surface.remaining_ranges?.length ?? 0) > 0)
  ).length;
  const zoomNote = partialRequired > 0
    ? `${partialRequired} surface(s) have remaining_ranges (re-slice to zoom, or ONE read_file mode=full of that handle when most of the file is needed)`
    : undefined;
  const doneRoute = (reason: string): NonNullable<TaskPackResult["route"]> =>
    zoomNote !== undefined
      ? editFromHandlesRoute(`${reason}; ${zoomNote}`, 0, query, zoomNote)
      : editFromHandlesRoute(reason, 0, query);

  // DESIGN-v0.8 coverage-honesty: a "focused" pack is a single confident site
  // with no cross-surface fan-out — the pack covers the whole change, so edit
  // directly (no extra TL calls), same trust level as "complete".
  if (coverage === "focused") {
    return doneRoute("single confident site; pack covers it");
  }

  // DESIGN-v0.8 coverage-honesty: a "candidate-list" partial means several
  // plausible primaries were returned and the agent should CONFIRM which one
  // before editing (one targeted slice), not locate more surfaces.
  if (coverageReason === "candidate-list") {
    const focusedAnswerEvidence = query !== undefined
      && isAnswerIntentQuery(query)
      && queryRequestsTestEvidence(query)
      && surfaces.some((surface) =>
        surface.role === "test"
        && /query-(?:identifier|evidence)-test-match/.test(surface.why ?? "")
        && hasServedCode(surface)
      )
      && surfaces.every((surface) => hasServedCode(surface));
    if (focusedAnswerEvidence) {
      return {
        action: "answer_from_handles",
        reason: "implementation and query-focused regression evidence are bundled",
        max_additional_tl_calls: 0,
      };
    }
    return {
      action: "confirm_candidates",
      reason: "multiple candidate primaries returned; confirm the target before editing — if required sites are missing from the candidates, ONE task_pack with paths=[chosen + missing] is sanctioned",
      max_additional_tl_calls: 2,
    };
  }

  // A4: gated flip. coverage:partial alone is NOT sufficient (that fired on
  // 22/22 packs in the DESIGN-v0.8 replay and would waste a locate turn on
  // tasks that were already fine handle-first) — only unmatched CONCERN
  // tokens justify telling the agent it still needs to look further. A4
  // refinement: and ONLY when coverage is not already "complete" — a complete
  // pack covers every required role, so unmatched query filler on top of that
  // (e.g. "fix", "behavior") must not flip an otherwise-done pack into an
  // extra locate call. Undefined coverage (older callers) keeps the prior
  // permissive behavior.
  const unmatched =
    query !== undefined && coverage !== "complete" ? unmatchedConcernTokens(query, surfaces) : [];
  if (unmatched.length > 0) {
    return {
      action: "locate_missing_surfaces",
      reason: `located surfaces returned; concern(s) not covered: ${unmatched.join(",")}`,
      max_additional_tl_calls: unmatched.length,
    };
  }

  // Never claim "covered" while coverage itself reads partial — that is the
  // self-contradiction this design item closes. Falls through to an honest
  // (still edit_from_handles — nothing REQUIRED is missing and every
  // concern token matched) reason instead of the old "covered" phrasing.
  if (coverage === "partial") {
    const reason = allMissing !== undefined && allMissing.length > 0
      ? `located surfaces returned; not all query concerns covered (missing: ${allMissing.join(",")})`
      : "located surfaces returned; not all query concerns covered";
    return doneRoute(reason);
  }

  return doneRoute("required surfaces covered by returned handles");
}

const NATIVE_EXTS = new Set([".c", ".cc", ".cpp", ".cxx", ".h", ".hpp", ".hh", ".hxx"]);
const NATIVE_HEADER_EXTS = new Set([".h", ".hpp", ".hh", ".hxx"]);
const NATIVE_SOURCE_EXTS = [".cpp", ".cc", ".cxx", ".c"];
const NATIVE_PAIR_HEADER_EXTS = [".h", ".hpp", ".hh", ".hxx"];

// isNativeExtPath (DESIGN-v0.8 §A5 deliverable 1: true for a C/C++
// source/header path — a bug-fix pack whose domain/contract surface is a
// NATIVE_EXTS file routinely arrives at low locator confidence even though
// the file IS the fix site, so it forces the embed gate at both
// candidateToSurface and buildPartialPack regardless of confidence score)
// now lives in util/nativeSymbolRange.ts, imported above. NATIVE_EXTS stays
// local here — nativePairCandidates below still needs the raw set for
// header/source pairing, which is out of scope for that consolidation.

function nativePairCandidates(relPath: string): string[] {
  const ext = path.extname(relPath).toLowerCase();
  if (!NATIVE_EXTS.has(ext)) return [];

  const base = relPath.slice(0, relPath.length - ext.length);
  const pairExts = NATIVE_HEADER_EXTS.has(ext) ? NATIVE_SOURCE_EXTS : NATIVE_PAIR_HEADER_EXTS;
  const bases = new Set<string>([base]);
  if (base.includes("/include/")) bases.add(base.replace("/include/", "/src/"));
  if (base.includes("/src/")) bases.add(base.replace("/src/", "/include/"));

  const out: string[] = [];
  for (const b of bases) {
    for (const e of pairExts) out.push(`${b}${e}`);
  }
  return out;
}

/**
 * R0 (2026-08-21, W4-A): `TaskPackSurface.why` is whySummary's HUMAN-READABLE
 * rendering of the locator's raw ImpactCandidate.why code, not the code
 * itself — most SIBLING_SEED_WEAK_WHY entries (e.g. "variant-text" ->
 * "variant token match") get rewritten by it, so comparing a surface's `why`
 * directly against SIBLING_SEED_WEAK_WHY's raw codes silently matches almost
 * nothing. Deriving the comparison set by running whySummary over each raw
 * code keeps ONE definition of "weak evidence" (SIBLING_SEED_WEAK_WHY) as the
 * source of truth instead of hand-duplicating its summarized spellings.
 * whySummary ignores its optional symbol/role params for every entry in this
 * particular set, so calling it with just the raw code is exact, not a guess.
 */
const SIBLING_SEED_WEAK_WHY_SUMMARIZED: ReadonlySet<string> = new Set(
  [...SIBLING_SEED_WEAK_WHY].map((raw) => whySummary(raw)),
);

/**
 * R0 (2026-08-21, W4-A pack-augmentation-pollution forensics): the paths of
 * this pack's TRUSTWORTHY surfaces — required, or reached through a
 * targeted (not SIBLING_SEED_WEAK_WHY-tagged, i.e. not a plain full-text/
 * variant/reference hit) match. Feeds `roleSearchScopePrefixes` for any
 * augmenter that must confine itself to the task's own project root(s)
 * WITHOUT letting a weakly-sourced, possibly-wrong-root surface (a
 * near-zero-confidence text hit that survived role-diversity selection —
 * see readCodeTaskPack.ts's R0 fixes) count as a root vote of its own. A
 * repo hosting many independent projects under one shared parent (routinely
 * true for `bench/fixtures/`) makes this distinction load-bearing: a weak
 * hit's own root is exactly the wrong-root noise the confinement exists to
 * keep OUT of the scope, so it must not also be trusted to WIDEN that scope.
 */
function trustworthySurfaceAnchorPaths(surfaces: readonly TaskPackSurface[]): string[] {
  return surfaces
    .filter((s) => s.required === true || !SIBLING_SEED_WEAK_WHY_SUMMARIZED.has(s.why ?? ""))
    .map((s) => s.path);
}

async function augmentNativeSurfaceClosure(
  surfaces: TaskPackSurface[],
  workspace: string,
  query: string,
  requiredRoles: string[],
  cache?: FileReadCache,
): Promise<void> {
  const seen = new Set(surfaces.map((s) => s.path));
  // R0 (2026-08-21, W4-A pack-augmentation-pollution forensics): confine
  // header/source-pair expansion to the same project root as this task's
  // TRUSTWORTHY surfaces — required, or reached through a targeted (not
  // SIBLING_SEED_WEAK_WHY-tagged, i.e. not a plain full-text/variant/
  // reference hit) match. Defense-in-depth alongside the matching
  // locate-layer fix (locateTaskContext.ts's Layer 6): a native surface that
  // only entered `surfaces` through a weak or foreign-root hit must not
  // cascade into MORE same-(wrong-)root siblings here. `roleSearchScopePrefixes`
  // returns null when no trustworthy anchor exists at all, and
  // `isWithinRoleSearchScope` treats null as unconfined — the same safe,
  // conservative default `augmentStyleSurface`'s anchorPaths already uses.
  const nativeScopePrefixes = roleSearchScopePrefixes(trustworthySurfaceAnchorPaths(surfaces));
  for (const surface of [...surfaces]) {
    if (surfaces.length >= MAX_SURFACES_DISTINCT) return;
    const ext = path.extname(surface.path).toLowerCase();
    if (!NATIVE_EXTS.has(ext)) continue;
    if (!isWithinRoleSearchScope(surface.path, nativeScopePrefixes)) continue;
    for (const pairPath of nativePairCandidates(surface.path)) {
      if (seen.has(pairPath)) continue;
      if (!isWithinRoleSearchScope(pairPath, nativeScopePrefixes)) continue;
      try {
        fs.statSync(path.join(workspace, pairPath));
      } catch {
        continue;
      }
      seen.add(pairPath);
      const surf = classifySurface(pairPath, undefined);
      const fallbackSurface: ImpactSurface = NATIVE_HEADER_EXTS.has(path.extname(pairPath).toLowerCase())
        ? "contract"
        : "domain";
      // DESIGN-v0.8 §A5 deliverable 2: the base "1-30" window below is only
      // a SEED for widenNativeSymbolRange's matchLine anchor (candidateToSurface
      // widens any non-symbol NATIVE_EXTS candidate automatically) — it is
      // no longer the shipped range. A pair surface has no real text match
      // (it was found via header/source pairing, not a query hit), so
      // widenNativeSymbolRange's own fallback (generous ±MAX_NATIVE_SYMBOL_LINES/2
      // window when no enclosing symbol contains the anchor) is what
      // actually recovers content like a file-scope sign table that sits
      // outside any function/class — see its doc comment.
      const pairSurface = await candidateToSurface({
        path: pairPath,
        line: 1,
        range: "1-30",
        surface: surf === "unknown" ? fallbackSurface : surf,
        why: "header-source-pair",
        confidence: 0.65,
        required: false,
      }, workspace, requiredRoles, query, cache);
      const insertAt = Math.min(surfaces.indexOf(surface) + 1, surfaces.length);
      surfaces.splice(insertAt, 0, pairSurface);
      break;
    }
  }
}

/** Global-variable identifier convention: a `g_`-prefixed name (project-agnostic firmware/C convention). */
const GLOBAL_IDENT_RE = /\bg_[A-Za-z_][A-Za-z0-9_]*\b/g;
/** An `extern <type...> <name>;` declaration — the NAME is the shared global whose DEFINITION lives elsewhere. */
const EXTERN_DECL_RE = /\bextern\b[^;{]*?\b([A-Za-z_][A-Za-z0-9_]*)\s*(?:\[[^\]]*\])?\s*;/g;
/** Max native files scanned when hunting for a global's definition (bounds the extra walk). */
const WIRING_SCAN_FILE_CAP = 400;

/**
 * Collect the shared-global identifier candidates named by a native surface:
 * every `g_*` reference in its content, plus every name declared `extern` in
 * it (a header's `extern` decl names a global DEFINED in some .c/.cpp). Reads
 * the surface's whole file (not just its embedded `code` slice, which is
 * range-bounded) so a reference outside the shown window still counts.
 */
function nativeSurfaceGlobals(workspace: string, relPath: string, cache?: FileReadCache): Set<string> {
  const out = new Set<string>();
  const raw = readCached(workspace, relPath, cache);
  if (raw === undefined) return out;
  for (const m of raw.matchAll(GLOBAL_IDENT_RE)) out.add(m[0]);
  if (NATIVE_HEADER_EXTS.has(path.extname(relPath).toLowerCase())) {
    for (const m of raw.matchAll(EXTERN_DECL_RE)) {
      if (m[1]) out.add(m[1]);
    }
  }
  return out;
}

/** True when `text` DEFINES `ident` at file scope: a non-extern declaration/assignment naming it (e.g. `Foo g_bar = ...` or `int g_bar[4] = {...}`). Deliberately excludes `extern` re-declarations. */
function fileDefinesGlobal(text: string, ident: string): boolean {
  const escaped = ident.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
  // A line that names the ident followed by `=`, `[`, `(`, or `;` and is NOT
  // an extern declaration or a bare reference. Anchored per-line to keep it
  // file-scope-ish without a full parse.
  const defRe = new RegExp(`^[^\\n]*\\b${escaped}\\b\\s*(?:\\[[^\\]]*\\])?\\s*(?:=|;|\\{)`, "m");
  for (const line of text.split(/\r?\n/)) {
    if (!line.includes(ident)) continue;
    if (/^\s*extern\b/.test(line)) continue; // extern decl — not a definition
    if (/^\s*\/\//.test(line) || /^\s*\*/.test(line)) continue; // comment line
    if (defRe.test(line)) return true;
  }
  return false;
}

/**
 * DESIGN-v0.8 §A5 deliverable (b): after native header/source pair surfaces are
 * selected, detect a SHARED GLOBAL — a `g_*` identifier (or an `extern`-declared
 * name from a selected header) referenced by ≥2 selected native surfaces — and
 * add ONE additional handle+skeleton surface (NO embedded code) for the file
 * that DEFINES it, within the same dominant project root. Bounded, generic
 * (no fixture-specific strings), and a silent no-op when nothing qualifies,
 * the caps are already full, or no defining file is found.
 */
async function augmentNativeWiringSurface(
  surfaces: TaskPackSurface[],
  workspace: string,
  cache?: FileReadCache,
): Promise<void> {
  if (surfaces.length >= MAX_SURFACES_DISTINCT) return;
  const nativeSurfaces = surfaces.filter((s) => isNativeExtPath(s.path));
  if (nativeSurfaces.length < 2) return;

  // Count how many DISTINCT native surfaces reference each global.
  const refCount = new Map<string, number>();
  for (const s of nativeSurfaces) {
    for (const g of nativeSurfaceGlobals(workspace, s.path, cache)) {
      refCount.set(g, (refCount.get(g) ?? 0) + 1);
    }
  }
  const shared = [...refCount.entries()]
    .filter(([, n]) => n >= 2)
    .map(([g]) => g)
    .sort(); // deterministic
  if (shared.length === 0) return;

  // Confine the definition hunt to the native surfaces' dominant project root,
  // so a same-named global in an unrelated project cannot be dragged in.
  const dominantRoot = dominantRootOf(nativeSurfaces);
  const already = new Set(surfaces.map((s) => s.path));

  const subPath = dominantRoot && dominantRoot !== "." ? dominantRoot : undefined;
  const nativeFiles = walkCodeFiles(workspace, subPath ? { subPath } : {})
    .filter((f) => isNativeExtPath(f.relPath) && !already.has(f.relPath))
    .filter((f) => projectRootOf(f.relPath) === dominantRoot)
    .slice(0, WIRING_SCAN_FILE_CAP);

  for (const ident of shared) {
    for (const f of nativeFiles) {
      const text = readCached(workspace, f.relPath, cache);
      if (text === undefined) continue;
      if (!fileDefinesGlobal(text, ident)) continue;
      if (surfaces.length >= MAX_SURFACES_DISTINCT) return;
      // Handle+skeleton only (NO code embed): mint a whole-file handle so the
      // agent can slice the definition, but do not spend the pack's code
      // budget on the wiring file itself.
      // BUG FIX: was text.split(/\r?\n/).length, which counts a phantom
      // final segment for trailing-newline content and mints a whole-file
      // range one line past the end (see util/countLines.ts doc comment).
      const lineCount = countLines(text);
      const wholeRange = `1-${Math.max(1, lineCount)}`;
      const entry = handleTable.upsert({
        kind: "range",
        path: f.relPath,
        range: wholeRange,
        workspaceRoot: workspace,
      });
      const role = classifySurface(f.relPath, undefined);
      surfaces.push({
        role: role === "unknown" ? "domain" : role,
        handle: entry.id,
        path: f.relPath,
        range: wholeRange,
        why: `defines shared global ${ident} referenced by ${refCount.get(ident)} selected surfaces`,
      });
      return; // ONE additional wiring surface, per the deliverable
    }
  }
}

async function augmentNativeIdentifierCooccurrenceSurface(
  surfaces: TaskPackSurface[],
  workspace: string,
  query: string,
  requiredRoles: string[],
  cache?: FileReadCache,
): Promise<void> {
  if (surfaces.length >= MAX_SURFACES_DISTINCT) return;
  const nativeSurfaces = surfaces.filter((s) => isNativeExtPath(s.path));
  if (nativeSurfaces.length === 0) return;

  const identifiers = orderedUniqueTokens(concreteIdentifierTokens(query))
    .filter((t) => normIdent(t).length >= 4);
  if (identifiers.length < 2) return;

  const dominantRoot = dominantRootOf(nativeSurfaces);
  const subPath = dominantRoot && dominantRoot !== "." ? dominantRoot : undefined;
  const already = new Set(surfaces.map((s) => s.path));
  const nativeFiles = walkCodeFiles(workspace, subPath ? { subPath } : {})
    .filter((f) => isNativeExtPath(f.relPath) && !already.has(f.relPath))
    .filter((f) => projectRootOf(f.relPath) === dominantRoot)
    .slice(0, WIRING_SCAN_FILE_CAP);

  let best: { file: string; line: number; tokens: string[]; score: number } | undefined;
  for (const f of nativeFiles) {
    const text = readCached(workspace, f.relPath, cache);
    if (text === undefined) continue;
    const normalizedText = normIdent(text);
    const present = identifiers.filter((t) => normalizedText.includes(normIdent(t)));
    if (present.length < 2) continue;
    const line = lineWithMostIdentifierMatches(text, present);
    const score = present.length * 100 - line;
    if (best === undefined || score > best.score) {
      best = { file: f.relPath, line, tokens: present, score };
    }
  }
  if (best === undefined) return;

  const range = `${Math.max(1, best.line - 12)}-${best.line + 12}`;
  const role = classifySurface(best.file, undefined);
  surfaces.push(await candidateToSurface(
    {
      path: best.file,
      line: best.line,
      range,
      surface: role === "unknown" ? "domain" : role,
      why: `query identifier co-occurrence (${best.tokens.slice(0, 3).join(", ")})`,
      confidence: 0.74,
    },
    workspace,
    requiredRoles,
    query,
    cache,
  ));
}

function lineWithMostIdentifierMatches(text: string, identifiers: string[]): number {
  const lines = text.split(/\r?\n/);
  let bestLine = 1;
  let bestCount = 0;
  for (let i = 0; i < lines.length; i++) {
    const normalizedLine = normIdent(lines[i]!);
    const count = identifiers.filter((t) => normalizedLine.includes(normIdent(t))).length;
    if (count > bestCount) {
      bestCount = count;
      bestLine = i + 1;
    }
  }
  return bestLine;
}

/**
 * DESIGN-v0.8 coverage-honesty (Part 5, B-1 variant promotion): for an
 * enum-like query, promote an already-selected config/data/doc surface to
 * `required=true` when its ALREADY-READ content (via the shared FileReadCache
 * — no new walks/reads) literally contains a case/separator variant of the
 * enum's new-value token. Such a file (a JSON seed, a data fixture, a doc
 * table) that already mentions the family is a genuine edit site for the new
 * value, not optional context — marking it required pulls its code into the
 * high-confidence embed/cap tier and keeps it off the trim block.
 *
 * Scoped to config/data/doc precisely because contract/api/ui/style are
 * already handled by requiredRoles / native-embed rules; these three are the
 * roles that otherwise silently ride along as low-confidence handle-only
 * surfaces. Generic: variants come from deriveTokenVariants(TOKEN), which is
 * derived purely from the token's own shape (never fixture vocabulary).
 */
function promoteVariantSurfaces(
  surfaces: TaskPackSurface[],
  query: string,
  workspace: string,
  cache: FileReadCache,
): void {
  // Item 5: camelCase-robust enum gate so a "TicketPriority byTicket ONHOLD"
  // query still promotes a config/data/doc surface that names the new value.
  if (!isEnumLikeQueryShaped(query)) return;
  const tokens = extractEnumValueTokens(query);
  if (tokens.length === 0) return;
  // Longer variants first so a short prefix does not shadow a longer one; only
  // meaningful variants (>=2 chars) to avoid trivial single-char matches.
  const variants = [...new Set(tokens.flatMap((t) => deriveTokenVariants(t)))].filter((v) => v.length >= 2);
  if (variants.length === 0) return;

  const promotable = new Set(["config", "data", "doc"]);
  for (const surf of surfaces) {
    if (surf.required) continue;
    if (!promotable.has(surf.role)) continue;
    const content = readCached(workspace, surf.path, cache);
    if (content === undefined) continue;
    if (variants.some((v) => content.includes(v))) {
      surf.required = true;
    }
  }
}

/** Style-file extensions the default code walk excludes (mirrors locate's STYLE_EXTRA_EXTS). */
const STYLE_EXTRA_EXTS = [".css", ".scss", ".less"];

/**
 * Item 1 (resolve the style role to a NAMED surface): when the pack REQUIRES a
 * "style" role but no surface carries it yet, find the best style file in the
 * located surfaces' own dominant project root and add it as a real surface
 * (handle + range + role:"style" + embedded code per the normal budget rules).
 * The live forensic: the pack said `missing:["style"]` as a bare ROLE and no
 * response ever named the actual design-tokens stylesheet, so the agent hunted by a
 * class-name stem, missed, and shipped without the design token. Naming the
 * file — with an editable handle — removes the guess entirely.
 *
 * Style required implies the root HAS at least one stylesheet
 * (requiredSurfacesForTask only adds "style" when the inventory has it), so
 * this normally succeeds; the caller (resolveStyleNextIfUnresolved) handles the
 * genuinely-no-file case with a named `explore find <stem>` next instead.
 *
 * Best-file ranking (deterministic): a stylesheet whose ALREADY-read content
 * literally contains a case/separator variant of the enum's new value token or
 * one of its existing sibling members is the strongest match (it is provably
 * the family's design-token file); then a `tokens.css`/design-token-named file;
 * then alphabetical. Confined to the dominant root so host-repo CSS never leaks
 * in for a fixture-scoped task. A silent no-op when style is not required, a
 * style surface already exists, the caps are full, or no stylesheet is found.
 */
async function augmentStyleSurface(
  surfaces: TaskPackSurface[],
  query: string,
  workspace: string,
  requiredRoles: string[],
  cache: FileReadCache,
  // B1a (2026-08-01 retrieval-scope): the ANCHOR cluster (the query's own
  // primary hits / the caller's own named paths). Omitted by legacy callers,
  // which then anchor on the running surface array as before — but that array
  // already contains earlier augmenters' picks, so one foreign surface widened
  // the scope for every augmenter after it (cascading contamination).
  anchorPaths?: readonly string[],
  callerScopes?: readonly string[],
): Promise<void> {
  if (!requiredRoles.includes("style")) return;
  if (surfaces.some((s) => s.role === "style")) return;
  if (surfaces.length >= MAX_SURFACES_DISTINCT) return;

  // One project root is a strict scope. Cross-package propagation (shared
  // contract + API + web) instead uses the anchors' ancestor-CHILD prefixes so
  // a stylesheet in the web package is not excluded merely because API supplied
  // more candidates than UI — while an unrelated SIBLING tree under the same
  // ancestor (a second product, a fixtures/ directory) stays out. The old
  // multi-root branch used the bare common ancestor, which for a no-shared-
  // ancestor cluster degenerated to "" and searched the WHOLE workspace: that
  // is the cross-tree stylesheet leak this closes.
  const anchors = anchorPaths !== undefined && anchorPaths.length > 0
    ? anchorPaths
    : surfaces.map((surface) => surface.path);
  const scope = roleSearchScope(anchors, query, workspace, callerScopes);
  if (scope === null) return;
  const already = new Set(surfaces.map((s) => s.path));
  const styleFiles = walkCodeFiles(workspace, { extraExts: STYLE_EXTRA_EXTS })
    .map((f) => f.relPath)
    .filter((p) => classifySurface(p) === "style")
    .filter((p) => !already.has(p))
    .filter((p) => isUnderAnyPrefix(p, scope))
    .sort();
  if (styleFiles.length === 0) return;

  // Variants of the enum's new value + its existing sibling members — a
  // stylesheet already naming one is the family's token file (strongest match).
  const valueTokens = extractEnumValueTokens(query);
  const memberVariants = new Set<string>(
    valueTokens.flatMap((t) => deriveTokenVariants(t)).filter((v) => v.length >= 2),
  );
  // Sibling members read from an already-selected contract/style surface's own
  // content, so a query that names only the NEW value (its siblings live in the
  // code) still ranks the right stylesheet.
  for (const s of surfaces) {
    if (s.role !== "contract") continue;
    const c = readCached(workspace, s.path, cache);
    if (c === undefined) continue;
    for (const m of c.match(/\b[A-Z][A-Z0-9_]{1,}\b/g) ?? []) {
      for (const v of deriveTokenVariants(m)) if (v.length >= 2) memberVariants.add(v);
    }
  }

  const score = (p: string): number => {
    const content = readCached(workspace, p, cache);
    if (content !== undefined && memberVariants.size > 0 && [...memberVariants].some((v) => content.includes(v))) {
      return 2; // content-confirmed family token file
    }
    return /(^|\/)(tokens|theme|variables|colors?|design-tokens)\.[^/]+$/i.test(p) ? 1 : 0;
  };
  const best = [...styleFiles].sort((a, b) => score(b) - score(a) || (a < b ? -1 : 1))[0]!;

  // B1a' (2026-08-01, rsc1 spawned-server follow-up): a marker-less workspace
  // makes roleSearchScopePrefixes' single-root branch return "" (the whole
  // workspace), so the scope filter above still admits a stylesheet from an
  // anchor-free sibling TOP-LEVEL tree (other-project/frontend/src/theme.css
  // for a packages/ + apps/ task). Outside the anchors' own top-level trees a
  // NAME-scored pick is not evidence — only a content-confirmed family token
  // file (score 2) may cross. Skipping keeps the pack honestly partial, which
  // beats serving a foreign tree's stylesheet as required:true.
  const anchorTops = new Set(anchors.map((p) => p.split("/")[0] ?? ""));
  const bestTop = best.split("/")[0] ?? "";
  if (score(best) < 2 && !anchorTops.has(bestTop)) return;

  const raw = readCached(workspace, best, cache);
  const lineCount = raw !== undefined ? Math.max(1, countLines(raw)) : 1;
  const rawLines = raw?.split(/\r?\n/) ?? [];
  const familyLine = rawLines.findIndex((line) =>
    [...memberVariants].some((variant) => line.includes(variant))
  ) + 1;
  const anchorLine = familyLine > 0 ? familyLine : 1;
  const styleSurface = await candidateToSurface(
    {
      path: best,
      line: anchorLine,
      range: `${Math.max(1, anchorLine - 12)}-${Math.min(lineCount, anchorLine + 12)}`,
      surface: "style",
      why: "style-family",
      confidence: 0.85,
      required: true,
    },
    workspace,
    requiredRoles,
    query,
    cache,
  );
  surfaces.push(styleSurface);
}

/**
 * Item 1 (named next when no style file exists): the fallback `next` for a pack
 * that STILL lacks a style surface after augmentStyleSurface (genuinely no
 * stylesheet in the root, or the caps were full). Instead of a bare "locate the
 * style role" re-pack, point at an `explore find` for the enum value's derived
 * TOKEN STEM (the CSS custom-property / BEM member form, via findText's
 * deriveIdentifierVariantProbes on the member-derived stem) — the concrete
 * string an agent would grep for. Returns undefined when there is no style gap
 * to name (style covered, or not an enum value we can derive a stem from), so
 * the caller keeps nextHintForCoverage's own hint.
 */
function styleNextForMissingRole(query: string, surfaces: TaskPackSurface[]): string | undefined {
  if (surfaces.some((s) => s.role === "style")) return undefined;
  const valueTokens = extractEnumValueTokens(query);
  if (valueTokens.length === 0) return undefined;
  // The member-derived stem: probe the CSS custom-property / kebab forms of the
  // new value (deriveIdentifierVariantProbes handles single-identifier stems).
  const probes = deriveIdentifierVariantProbes(valueTokens[0]!);
  // Prefer a "--kebab" CSS custom-property probe (the actual design-token form);
  // else the first derived stem; else the kebab of the raw token.
  const stem =
    probes.find((p) => p.startsWith("--")) ??
    probes[0] ??
    kebabTokenVariant(valueTokens[0]!) ??
    valueTokens[0]!.toLowerCase();
  return `search_files action=find query=${stem}`;
}

// ---------------------------------------------------------------------------
// FIX-B1/B2 (2026-07-09d forensics, task_pack round-trip collapse): server-
// side fill for missing REQUIRED surface roles, attempted INSIDE the same
// pack build instead of only suggesting a `surfaceRoles=[...]` retry.
//
// FIX-B1 root cause: `args.surfaceRoles` is read by requiredSurfacesForTask
// (it becomes the explicit required-role list) but was NEVER threaded into
// the underlying candidate SEARCH — LocateInput (packages/types/src/mcp.ts)
// has no role/surface filter at all, and buildTaskPack's locateTaskContext
// call passes only query/path/symbol/lang/limit. A `surfaceRoles=["contract",
// "api"]` retry therefore re-runs the IDENTICAL pathless `query` through the
// IDENTICAL locate pipeline and gets the IDENTICAL candidate pool back — the
// only difference is which roles THIS PACK labels "required". Confirmed
// empirically in the forensics (0/2 real retries changed the missing set at
// all). This is a plumbing miss, not a mistuned knob: there was never a
// role-aware search path for a retry to invoke. Adding a role filter to the
// shared locate contract would be a cross-package change (packages/types)
// well beyond this pack layer, so the fix instead generalizes the ONE
// role-targeted mechanism that already exists here (augmentStyleSurface, for
// "style") to every OTHER required role — see augmentMissingRequiredRoleSurfaces
// below. Running it unconditionally (not only on a surfaceRoles-flagged
// retry) is FIX-B2: it collapses the round trip so the FIRST call already
// finds a role the pathless query's own wording missed, instead of forcing
// the agent through a retry that (pre-fix) was guaranteed to be a no-op.
//
// Bounded to at most 2 extra internal search passes for the WHOLE fill
// (never per-role, so a pack with several missing roles cannot balloon into
// an unbounded number of extra lookups): one combined locateTaskContext call
// biased toward every still-missing role's structural vocabulary, then one
// classify/rank scan of the dominant root that issues NO further locate call
// at all (the same walkCodeFiles + classifySurface mechanism
// augmentStyleSurface already uses, generalized to any role). Only roles
// neither pass can find stay genuinely `missing`.
// ---------------------------------------------------------------------------

/**
 * Structural vocabulary hints per role — generic architectural words (never
 * project-specific vocabulary), used to bias a role-targeted locate pass
 * toward files that plausibly classify into a still-missing role when the
 * query's own wording did not surface one. "style" is deliberately absent:
 * augmentStyleSurface already runs a more precise (member-variant-token
 * aware) search for that role before this function ever sees the pack.
 */
const ROLE_FILL_QUERY_HINTS: Partial<Record<string, string>> = {
  contract: "type interface schema enum model definition contract",
  api: "route handler endpoint controller service repository api",
  domain: "service domain business logic rules transition state",
  data: "repository model schema migration data store seed fixture",
  ui: "component view page screen widget ui",
  test: "test spec unit",
  doc: "readme documentation guide",
  config: "config settings options",
};

/** Required roles (excluding "style", already owned by augmentStyleSurface) not yet covered by any surface. */
function stillMissingRequiredRoles(surfaces: TaskPackSurface[], requiredRoles: string[]): string[] {
  return requiredRoles.filter((r) => r !== "style" && !surfaces.some((s) => s.role === r));
}

/**
 * FIX-B2: attempt to fill still-missing REQUIRED roles (other than "style")
 * IN THIS SAME pack build. Mutates `surfaces` in place (matches every other
 * augment* helper's shape); a role neither pass can find is left missing for
 * the caller's existing `missing`/`missing_required_surfaces` bookkeeping to
 * report honestly — this is an ATTEMPT, not a guarantee.
 */
async function augmentMissingRequiredRoleSurfaces(
  surfaces: TaskPackSurface[],
  query: string,
  workspace: string,
  requiredRoles: string[],
  cache: FileReadCache,
  // 2026-07-12d confinement fix: same caller-SEEDED-flow scope parameter as
  // augmentConcernGroupSurfaces above — undefined at the other two call
  // sites (buildTaskPack, buildPartialPack), which keep today's unconfined
  // behavior. Layered ON TOP of (not replacing) the existing fillScope
  // machinery below: fillScope stays the marker-root-based search/inventory
  // hint it always was (and can legitimately be broader than `scope`, e.g.
  // a monorepo family root), but a candidate must ALSO satisfy `scope` —
  // the caller's own named territory — before it is ever pushed into
  // `surfaces`. This is what keeps a single caller-named FILE from having
  // its required-role gap silently filled from an unrelated SIBLING
  // directory that merely shares the same (or no) marker root.
  scope?: string,
  // B1a (2026-08-01 retrieval-scope): the ANCHOR cluster — see
  // roleSearchScopePrefixes. Omitted by legacy callers, which keep anchoring on
  // the running surface array (and so inherit any earlier augmenter's pick).
  anchorPaths?: readonly string[],
): Promise<void> {
  let missing = stillMissingRequiredRoles(surfaces, requiredRoles);
  if (missing.length === 0 || surfaces.length >= MAX_SURFACES_DISTINCT) return;

  // 09e pollution hardening: dominantRootOf's plurality vote is meaningless
  // exactly when it matters most — surfaces spanning several marker roots
  // (monorepo subpackages each carry their own manifest), where the OLD ""
  // fallback ran pass 1 UNSCOPED and filled a required role from a foreign
  // project (one live pack's "contract" was filled with a foreign
  // project's header). roleFillScope replaces the vote: one shared root → that
  // root; several roots → their common ancestor (the project-family root,
  // which legitimately includes SIBLING subpackages — where a missing
  // contract/api role usually lives); no anchor at all → null, and the fill
  // is SKIPPED rather than guessed workspace-wide.
  // B1a (2026-08-01 retrieval-scope): roleFillScope's single common-ancestor
  // string admitted every SIBLING tree under that ancestor — the cross-fixture
  // `test`/`style` role fills observed in run 2026-07-31-semantic-signal5-2.
  // roleSearchScope narrows the same anchor cluster to the ancestor-CHILD
  // directories that actually contain an anchor, and adds any directory the
  // query literally named.
  const fillScopes = roleSearchScope(
    anchorPaths !== undefined && anchorPaths.length > 0 ? anchorPaths : surfaces.map((s) => s.path),
    query,
    workspace,
    scope !== undefined && scope !== "" ? [scope] : undefined,
  );
  if (fillScopes === null) return;
  // Cheap short-circuit (mirrors requiredSurfacesForTask's own "never demand
  // a role that cannot exist here" discipline): do not spend a search pass
  // hunting for a role this scope structurally has no file for at all.
  // Prefix-scoped (not rootOf-equality): the scope may be a common-ancestor
  // directory that is not itself a marker root.
  const inventory = new Set<ImpactSurface>();
  for (const prefix of fillScopes) {
    for (const role of roleInventoryUnder(workspace, prefix === "." ? "" : prefix)) inventory.add(role);
  }
  missing = missing.filter((r) => inventory.has(r as ImpactSurface));
  if (missing.length === 0) return;
  // The single `path` a locate call accepts — only meaningful when the scope is
  // ONE directory; a multi-prefix scope relies on the explicit per-candidate
  // filter below (locate's own hard scope cannot express a union).
  const fillScope = fillScopes.length === 1 ? fillScopes[0]! : "";

  const seenPaths = new Set(surfaces.map((s) => s.path));

  // Pass 1: ONE combined locate call biased toward every still-missing
  // role's structural vocabulary, confined to the fill scope so it cannot
  // reintroduce cross-project noise.
  const hints = missing.map((r) => ROLE_FILL_QUERY_HINTS[r]).filter((h): h is string => h !== undefined);
  if (hints.length > 0 && query.trim().length > 0) {
    const roleQuery = [query, ...hints].join(" ").slice(0, 400);
    const located = await locateTaskContext(workspace, {
      action: "locate",
      query: roleQuery,
      ...(fillScope !== "" && fillScope !== "." ? { path: fillScope } : {}),
      limit: MAX_SURFACES_DISTINCT,
    });
    const candidates: ImpactCandidate[] = located.hit
      ? [...located.primary, ...located.related]
      : (located.candidateDetails ?? []).map((d) => ({
          path: d.path,
          line: d.line,
          range: d.range,
          ...(d.symbol ? { symbol: d.symbol } : {}),
          surface: d.surface,
          why: d.why,
          confidence: d.confidence,
        }));
    const stillMissingSet = new Set(missing);
    for (const c of [...candidates].sort((a, b) => b.confidence - a.confidence)) {
      if (surfaces.length >= MAX_SURFACES_DISTINCT) break;
      if (seenPaths.has(c.path) || !stillMissingSet.has(c.surface)) continue;
      // Defensive re-confinement (same discipline as the seeded flow):
      // locate's own hard-scope filter already restricts to fillScope when
      // set, but the boundary this pass exists to enforce stays explicit —
      // and for a multi-prefix scope it is the ONLY enforcement.
      if (!isUnderAnyPrefix(c.path, fillScopes)) continue;
      // 2026-07-12d: caller-named scope, AND'd on top of fillScope — see
      // this function's own doc comment above.
      if (scope !== undefined && !isUnderAncestor(c.path, scope)) continue;
      surfaces.push(await candidateToSurface({ ...c, required: true }, workspace, requiredRoles, query, cache));
      seenPaths.add(c.path);
      stillMissingSet.delete(c.surface);
    }
  }

  // Pass 2: classify-and-rank scan of the fill scope — issues NO further
  // locate call at all (a directory walk + classifySurface, the same
  // mechanism augmentStyleSurface already uses for "style") — for whichever
  // roles pass 1 still missed.
  missing = stillMissingRequiredRoles(surfaces, requiredRoles).filter((r) => inventory.has(r as ImpactSurface));
  if (missing.length === 0 || surfaces.length >= MAX_SURFACES_DISTINCT) return;

  const queryTokens = concernAnchorTokens(query, 8);
  const scoreOf = (p: string): number => queryTokens.filter((t) => p.toLowerCase().includes(t)).length;
  for (const role of missing) {
    if (surfaces.length >= MAX_SURFACES_DISTINCT) break;
    const candidates = walkCodeFiles(workspace, {})
      .map((f) => f.relPath)
      .filter((p) => !seenPaths.has(p))
      .filter((p) => isUnderAnyPrefix(p, fillScopes))
      .filter((p) => scope === undefined || isUnderAncestor(p, scope))
      .filter((p) => classifySurface(p) === role)
      .sort((a, b) => scoreOf(b) - scoreOf(a) || (a < b ? -1 : 1));
    const best = candidates[0];
    if (!best) continue;
    const raw = readCached(workspace, best, cache);
    const lineCount = raw !== undefined ? Math.max(1, countLines(raw)) : 1;
    surfaces.push(await candidateToSurface(
      {
        path: best,
        line: 1,
        range: `1-${lineCount}`,
        surface: role as ImpactSurface,
        why: "role-fill-scan",
        confidence: 0.6,
        required: true,
      },
      workspace,
      requiredRoles,
      query,
      cache,
    ));
    seenPaths.add(best);
  }
}

/** Cap on how many query-named files are force-included as surfaces (bounded, deterministic). */
const MAX_QUERY_NAMED_FILES = 3;

/**
 * Extensions the default code walk excludes but a caller routinely NAMES —
 * a contract doc, a config/schema file, a migration. The T05c query named
 * `CONTRACT.md` alongside two C++ files, and a resolution that could only see
 * code extensions would have silently dropped exactly the file carrying the
 * requirement.
 */
const QUERY_NAMED_EXTRA_EXTS = [
  ...STYLE_EXTRA_EXTS,
  ".md", ".markdown", ".json", ".yaml", ".yml", ".toml", ".ini", ".txt", ".sql", ".sh", ".proto",
];

/**
 * B1c (2026-08-01 retrieval-scope): FILES the query literally names.
 *
 * A caller who writes `control/mixer.cpp` (or just `mixer.hpp`, or
 * `CONTRACT.md`) has already done the locating — that file is not a candidate
 * to be ranked, it is a stated requirement. Live forensics (run
 * 2026-07-31-semantic-signal5-2, T05c rep0): follow-up packs whose query named
 * exactly those three files came back as candidate LISTS containing none of
 * them, so the caller re-asked for files it had already named twice.
 *
 * Resolution is by workspace inventory, never by guess: a token must equal a
 * walked file's relative path or be its path SUFFIX (so a bare basename works),
 * and the extension must be alphabetic-leading so version strings ("v0.9") and
 * numeric fragments never look like filenames.
 */
export function queryNamedWorkspaceFiles(
  query: string,
  workspace: string,
  anchorPaths: readonly string[] = [],
): string[] {
  if (query.trim().length === 0) return [];
  const tokens = [...new Set(
    (query.match(/[A-Za-z0-9_$][A-Za-z0-9_$.\-/]*\.[A-Za-z][A-Za-z0-9]{0,5}\b/g) ?? [])
      .map((token) => token.replace(/^\.\//, "")),
  )];
  if (tokens.length === 0) return [];
  const walked = walkCodeFiles(workspace, { extraExts: QUERY_NAMED_EXTRA_EXTS }).map((file) => file.relPath).sort();
  // R0 (2026-08-21, W4-A): a bare basename token (no directory component,
  // e.g. "server.ts") can suffix-match MULTIPLE workspace files when the
  // same name exists under more than one project root (routinely true for a
  // `bench/fixtures/` tree hosting several independent synthetic projects).
  // Without a root preference, `.find()` silently took whichever candidate
  // `walked`'s alphabetical sort put first — an arbitrary, workspace-layout-
  // dependent pick with no relation to the task at hand. When the caller
  // supplies anchor paths (its own already-trusted surfaces), prefer a
  // suffix match inside THEIR root over one outside it; falls back to the
  // prior first-match behavior when no anchors are supplied or none of the
  // candidates is in-root, so recall is never reduced versus before this fix.
  const scopePrefixes = roleSearchScopePrefixes(anchorPaths);
  const resolved: string[] = [];
  for (const token of tokens) {
    const exact = walked.find((rel) => rel === token);
    const suffixMatches = exact === undefined ? walked.filter((rel) => rel.endsWith(`/${token}`)) : [];
    const match = exact
      ?? suffixMatches.find((rel) => isWithinRoleSearchScope(rel, scopePrefixes))
      ?? suffixMatches[0];
    if (match !== undefined && !resolved.includes(match)) resolved.push(match);
    if (resolved.length >= MAX_QUERY_NAMED_FILES) break;
  }
  return resolved;
}

/**
 * B1c: force-include every query-named workspace file as a CONTENT-BEARING
 * surface. Uses the same anchor-focus ranging the good complete packs use
 * (anchorFocusForLocatorCandidate), so a large named file ships the region the
 * task is about instead of a fixed-width sliver of its head.
 *
 * Bounded: never exceeds MAX_SURFACES_DISTINCT, and is a no-op when the query
 * names no resolvable file (the overwhelmingly common case). Additive in the
 * common case — never reorders or drops what the flow already found — EXCEPT
 * (R0, 2026-08-21 W4-A forensics) when the budget is already full: a
 * query-named file is a STATED REQUIREMENT, not a ranked candidate (a file
 * the QUERY names by name, verbatim, with its extension), so it may evict the
 * single weakest present surface (not required, not already protected, not
 * grounded in the query at all — see surfaceGroundedInQuery) to make room,
 * rather than being silently dropped because weaker evidence happened to
 * fill the budget first. A no-op, as before, when every present surface is
 * required/protected/grounded.
 */
async function augmentQueryNamedFileSurfaces(
  surfaces: TaskPackSurface[],
  query: string,
  workspace: string,
  requiredRoles: string[],
  cache: FileReadCache,
  protectedSurfaces?: Set<TaskPackSurface>,
  maxSurfaces: number = MAX_SURFACES_DISTINCT,
): Promise<void> {
  // R0 (2026-08-21, W4-A): disambiguate a bare-basename query-named token
  // (e.g. "server.ts") toward THIS task's own trustworthy root when the same
  // name exists under more than one project — see queryNamedWorkspaceFiles
  // and trustworthySurfaceAnchorPaths' own comments.
  const named = queryNamedWorkspaceFiles(query, workspace, trustworthySurfaceAnchorPaths(surfaces));
  if (named.length === 0) return;
  const already = new Set(surfaces.map((s) => s.path));
  for (const rel of named) {
    if (already.has(rel)) continue;
    if (surfaces.length >= maxSurfaces) {
      const evictIndex = surfaces.findIndex((s) =>
        !s.required && !protectedSurfaces?.has(s) && !surfaceGroundedInQuery(s, query)
      );
      if (evictIndex === -1) continue;
      const [evicted] = surfaces.splice(evictIndex, 1);
      if (evicted) already.delete(evicted.path);
    }
    const content = readCached(workspace, rel, cache);
    if (content === undefined) continue;
    const candidate: ImpactCandidate = {
      path: rel,
      line: 1,
      range: `1-${Math.max(1, countLines(content))}`,
      surface: classifySurface(rel),
      why: "query-named-file",
      confidence: 1,
    };
    const anchored = await anchorFocusForLocatorCandidate(candidate, workspace, query, cache);
    const surface = await candidateToSurface(
      anchored?.candidate ?? candidate, workspace, requiredRoles, query, cache,
    );
    if (anchored) attachAnchorFocusAffordance(surface, anchored.focus, rel, workspace);
    surfaces.push(surface);
    protectedSurfaces?.add(surface);
    already.add(rel);
  }
}

// ---------------------------------------------------------------------------
// Item 4 (create-target note): the task may genuinely require a NEW file,
// not an edit to an existing one. Conservative and honest by construction —
// never fabricates a path, only reports that nothing existing names the
// token.
// ---------------------------------------------------------------------------

/** Minimum length for a query token to be considered a plausible module/file name (see moduleShapedQueryTokens). */
const MODULE_TOKEN_MIN_LEN = 6;

/**
 * Whole (never fragmented) snake_case/camelCase identifier-shaped tokens
 * from `query`, length >= MODULE_TOKEN_MIN_LEN, excluding CONCERN_NOISE_WORDS
 * (generic dev nouns like "wiring"/"task"/"file" must not be mistaken for an
 * intended module name). Deliberately whole-word only (unlike
 * tokenizeQuery's "identifier" mode, which also emits camelCase/snake_case
 * FRAGMENTS) — a fragment like "Estimator" out of "positionEstimator" is a
 * weaker, less precise signal than the compound identifier itself, and this
 * note's whole point is to name the ONE token that plausibly IS a file/module
 * name. Order-preserving, deduped case-insensitively.
 */
function moduleShapedQueryTokens(query: string): string[] {
  const words = query.match(/[A-Za-z_][A-Za-z0-9_]*/g) ?? [];
  const seen = new Set<string>();
  const out: string[] = [];
  for (const w of words) {
    if (w.length < MODULE_TOKEN_MIN_LEN) continue;
    if (!(/_/.test(w) || /[a-z][A-Z]/.test(w))) continue; // snake_case, camelCase, or PascalCase
    const key = w.toLowerCase();
    if (CONCERN_NOISE_WORDS.has(key) || seen.has(key)) continue;
    seen.add(key);
    out.push(w);
  }
  return out;
}

/**
 * True when `token` names neither an existing surface (path/symbol —
 * concernTokenMatchesSurface, the same match rule the concern-token gate
 * already uses) NOR appears inside any served surface's ALREADY-READ
 * embedded code. The `code` check is deliberately included here (unlike
 * concernTokenMatchesSurface's own callers, which stay path/symbol-only to
 * avoid over-loose coverage GATING) — this is a purely advisory note, so the
 * extra check only makes it MORE conservative (fewer false "go create a new
 * module" suggestions when the concept already exists inside a broader file).
 */
function tokenAbsentFromSurfaces(token: string, surfaces: TaskPackSurface[]): boolean {
  const lowerToken = token.toLowerCase();
  const nt = normIdent(token);
  return !surfaces.some(
    (s) => concernTokenMatchesSurface(lowerToken, s) || (s.code !== undefined && normIdent(s.code).includes(nt)),
  );
}

/**
 * Item 4: when a module-shaped query token matches NEITHER a walked file's
 * basename/stem ANYWHERE in the workspace (deliberately unconfined —
 * confining the "does this exist at all" check to the pack's own scope would
 * risk a false "create a new module" note for a file that genuinely exists
 * just outside that scope, which is not an honest claim) NOR any served
 * surface, the task may require creating a new module. Bounded to at most
 * one note — the longest qualifying token, the strongest single signal —
 * and never fabricates a path in the CREATE case. 2026-07-12d: before
 * settling on "create", the same unconfined discipline is applied to a
 * cheap workspace-wide literal-content probe (scanLiteral) — a token whose
 * SYMBOL already exists in a differently-named file yields an honest
 * redirect note instead (see the function body for both shapes).
 */
function buildCreateTargetNote(query: string, surfaces: TaskPackSurface[], workspace: string): string | undefined {
  const candidates = moduleShapedQueryTokens(query);
  if (candidates.length === 0) return undefined;

  const walkedStems = walkCodeFiles(workspace, {}).map((f) =>
    normIdent(path.basename(f.relPath).replace(/\.[^.]+$/, "")),
  );
  const unmatched = candidates.filter((token) => {
    const nt = normIdent(token);
    if (walkedStems.some((stem) => stem.includes(nt))) return false;
    return tokenAbsentFromSurfaces(token, surfaces);
  });
  if (unmatched.length === 0) return undefined;

  const longest = [...unmatched].sort((a, b) => b.length - a.length)[0]!;

  // 2026-07-12d create-note strengthening: a token absent from file
  // BASENAMES and from the pack's own surfaces may still be a real symbol
  // living in a DIFFERENTLY-NAMED file (query names "calculatePriceEstimate"
  // but the function actually lives in pricing.ts) — suggesting "create a
  // new module" there is actively wrong, worse than no note at all. Reuse
  // scanLiteral (the same bounded, already-imported content-probe the
  // find/closure paths use) as a cheap whole-workspace grep for the token's
  // literal text — a symbol definition necessarily contains its own name, so
  // a text hit is a reliable (if slightly over-inclusive) stand-in for "the
  // symbol exists here" without paying for a full parse/skeleton pass.
  // Surfaces already IN the pack are excluded from the hit set — the agent
  // already holds a handle for those, a redirect note would be noise.
  const surfacePaths = new Set(surfaces.map((s) => s.path));
  const contentHits = scanLiteral(longest, workspace, { caseInsensitive: true })
    .map((m) => m.path)
    .filter((p) => !surfacePaths.has(p));
  if (contentHits.length > 0) {
    const nearest = [...new Set(contentHits)].sort(
      (a, b) => a.length - b.length || (a < b ? -1 : 1),
    )[0]!;
    return `"${longest}" exists in ${nearest} — likely extend it rather than create a new module`;
  }

  return `no existing file matches "${longest}" — the task may require creating a new module`;
}

function resolveExplicitArtifactCreateTarget(
  args: TaskPackArgs,
  query: string,
  workspace: string,
): TaskPackResult["create_target"] {
  const match = query.match(
    /([A-Za-z0-9_.-]+(?:\/[A-Za-z0-9_.-]+)*\/)\s*(?:に|へ|under\b|in\b|at\b|to\b)\s*([A-Za-z_$][A-Za-z0-9_$.-]*\.(?:c|cc|cpp|cxx|go|java|js|jsx|py|rs|ts|tsx))/i,
  );
  if (!match) return undefined;
  const requestedDir = match[1]!.replace(/^\.\//, "").replace(/\/$/, "");
  const fileName = path.posix.basename(match[2]!);
  const scopedFiles = walkCodeFiles(workspace, args.path ? { subPath: args.path } : {});
  const matchingDirs = [...new Set(scopedFiles
    .map((file) => path.posix.dirname(file.relPath))
    .filter((directory) => directory === requestedDir || directory.endsWith(`/${requestedDir}`)))];
  if (matchingDirs.length !== 1) return undefined;
  const directory = matchingDirs[0]!;
  const targetPath = path.posix.join(directory, fileName);
  if (fs.existsSync(path.join(workspace, targetPath))) return undefined;
  const directoryEvidence = scopedFiles
    .map((file) => file.relPath)
    .filter((filePath) => path.posix.dirname(filePath) === directory)
    .sort()
    .slice(0, 3);
  if (directoryEvidence.length === 0) return undefined;
  return { path: targetPath, directory_evidence: directoryEvidence };
}

/**
 * W3 (turn-economy wave 2): does the query express intent to CREATE a new file
 * (a real create verb + a file-shaped noun)? Deliberately conservative — bare
 * "new" is excluded (it appears in "new value"/"new behavior"); a create verb
 * must co-occur with a file noun, OR the JA create markers with a file noun.
 */
const CREATE_FILE_VERB_EN_RE = /\b(?:add|create|write|introduce|generate|scaffold|author|make)\b/i;
const CREATE_FILE_NOUN_EN_RE = /\b(?:new\s+)?(?:file|module|test|tests|spec|script|component|fixture|migration|stub)\b/i;
const CREATE_FILE_JA_RE = /(?:作成|新規|新し[いく]|新た(?:に|な)|追加)/;
const CREATE_FILE_NOUN_JA_RE = /(?:ファイル|モジュール|テスト|クラス|スクリプト|フィクスチャ)/;

function hasCreateFileIntent(query: string): boolean {
  const en = CREATE_FILE_VERB_EN_RE.test(query) && CREATE_FILE_NOUN_EN_RE.test(query);
  const ja = CREATE_FILE_JA_RE.test(query) && CREATE_FILE_NOUN_JA_RE.test(query);
  return en || ja;
}

/**
 * turn-economy wave 3 (V1): create OR PLACEMENT intent — broader than
 * hasCreateFileIntent. Also fires on a placement question ("where should X
 * live/go/belong", "where to put X") and on a bare create verb or "new". This
 * looser gate is used ONLY for the STRUCTURALLY-verified dotted-module /
 * extension-less create-target resolution below, whose parent-directory-exists +
 * target-absent + not-an-existing-dir checks are the real safety net — so the
 * gate's job is merely to exclude a pure explain/answer query, not to carry the
 * confidence alone (unlike the prose-extension Path 1, which keeps the stricter
 * verb+noun gate to avoid firing on incidental filenames in explanatory prose).
 */
const CREATE_OR_PLACEMENT_EN_RE =
  /\bwhere\s+(?:should|shall|does|do|to|would|is|are|can|will|might)\b|\b(?:new|add|create|introduce|scaffold|author|generate|write|make|place|put|host|home|belongs?|resides?|lives?|goes?)\b/i;

function hasCreateOrPlacementIntent(query: string): boolean {
  return hasCreateFileIntent(query)
    || CREATE_OR_PLACEMENT_EN_RE.test(query)
    || CREATE_FILE_JA_RE.test(query);
}

/** Code/test file extensions a named create target may carry. */
const CREATE_TARGET_EXT_RE =
  /[\w./-]*[\w-]+\.(?:py|pyi|ts|tsx|js|jsx|mjs|cjs|c|cc|cpp|cxx|h|hpp|hh|go|java|rs|rb|kt|kts|swift|cs|php|scala|sh|txt|md|json|jsonc|yaml|yml|toml|xml|html|css|sql|csv|ini|cfg|env|ps1|psm1|psd1|bat|cmd|graphql|proto|tf|properties)\b/gi;

/**
 * True when `query` shows the same "explicit, extensioned create target"
 * shape resolveCreateTargetFromQuery's Path 1 gates on: hasCreateFileIntent
 * (or a bare JA create verb) co-occurring with at least one
 * CREATE_TARGET_EXT_RE token. F-R14 (2026-08-22, DESIGN-v0.11 §9 residual):
 * this is the gate buildTaskPackCore reuses to decide whether a query's
 * locatingQuery should have its fenced content stripped. Duplicated rather
 * than shared with resolveCreateTargetFromQuery's own inline `if` — that
 * `if` only tests the intent half (an absent extension match there is a
 * no-op empty loop, not a skip past it) — because this predicate
 * deliberately wants the stricter, combined check. Read-only /
 * side-effect-free: uses `.match()` (which always resets a global regex's
 * lastIndex), never `.test()`, on CREATE_TARGET_EXT_RE, so repeated calls
 * never race that regex's own shared lastIndex state.
 */
export function queryNamesCreateTarget(query: string): boolean {
  return (hasCreateFileIntent(query) || CREATE_FILE_JA_RE.test(query))
    && query.match(CREATE_TARGET_EXT_RE) !== null;
}

/**
 * Strip fenced code block(s) (``` ... ```, any/no language tag) out of
 * `text`. F-R14 (2026-08-22, DESIGN-v0.11 §9 residual): a create-with-
 * content task_pack query embeds the NEW file's full body inside a fenced
 * block. That body is not a locating signal — it describes a file that
 * does not exist yet — but every locator/evidence/concern/wiring consumer
 * historically searched and scored against the raw query, so the body's
 * own vocabulary (imports, namespace/class names, ...) pulled unrelated
 * sibling files sharing those tokens into evidence. Tolerates an
 * unterminated trailing fence (stripped to end-of-string — the same
 * leniency an agent's own truncated paste would need). Returns `text`
 * unchanged when it carries no fence marker at all, so a fence-less call
 * site is a pure no-op (byte-identical locatingQuery === query).
 */
export function stripFencedBlocksForLocating(text: string): string {
  if (!text.includes("```")) return text;
  let result = "";
  let cursor = 0;
  for (;;) {
    const openIdx = text.indexOf("```", cursor);
    if (openIdx === -1) {
      result += text.slice(cursor);
      break;
    }
    result += text.slice(cursor, openIdx);
    const closeIdx = text.indexOf("```", openIdx + 3);
    if (closeIdx === -1) break; // unterminated fence: drop opener..EOF
    cursor = closeIdx + 3;
  }
  return result;
}

/**
 * V1: a dotted PYTHON module path token — >=2 identifier segments joined by
 * dots (`pkg.sub.module`). Each segment must start with a letter/underscore so
 * version numbers (`1.2.3`) never match; the parent-dir-exists gate below then
 * filters prose like `e.g.`/`i.e.` and `self.method` (their leading dirs never
 * exist as directories).
 */
const DOTTED_MODULE_TOKEN_RE = /[A-Za-z_][A-Za-z0-9_]*(?:\.[A-Za-z_][A-Za-z0-9_]*)+/g;

/**
 * V1: a slash path-ish token with NO dotted extension (`src/pricing/engine`) —
 * >=2 slash-joined segments. A token that carries an extension (`src/foo.py`) is
 * left to the extensioned Path 1; this pattern's segments exclude `.` so such a
 * token only partially matches and is skipped by the `includes(".")` guard.
 */
const EXTLESS_PATH_TOKEN_RE = /[\w-]+(?:\/[\w-]+)+/g;

/** True when workspace-relative `rel` resolves to an existing directory. */
function isExistingDirRel(workspace: string, rel: string): boolean {
  try {
    return fs.statSync(path.join(workspace, rel)).isDirectory();
  } catch {
    return false;
  }
}

/** Immediate-child sibling code files of `dir` (≤3, sorted) — the create template evidence. */
function directorySiblingEvidence(workspace: string, dir: string): string[] {
  return walkCodeFiles(workspace, dir ? { subPath: dir } : {})
    .map((file) => file.relPath)
    .filter((rel) => path.posix.dirname(rel) === (dir || "."))
    .sort()
    .slice(0, 3);
}

/**
 * Nearest existing ancestor directory of workspace-relative `targetPath`,
 * walking up from its own parent. F-R14 (2026-08-22, DESIGN-v0.11 §9
 * residual): a create target's own directory often does not exist yet
 * (e.g. "tmp/TlProbe.cs" when "tmp/" is new) — capCreateOnlyEvidence below
 * needs an EXISTING directory to scope sibling evidence to. Returns ""
 * (the workspace root) when no ancestor exists short of the root itself,
 * matching the `dir || "."` root convention directorySiblingEvidence and
 * dominantChildExtension already use.
 */
function nearestExistingAncestorDir(workspace: string, targetPath: string): string {
  let dir = path.posix.dirname(targetPath);
  while (dir !== "." && dir !== "" && !isExistingDirRel(workspace, dir)) {
    const parent = path.posix.dirname(dir);
    if (parent === dir) break; // safety net: dirname is idempotent at "."/"/"
    dir = parent;
  }
  return dir === "." ? "" : dir;
}

/**
 * F-R14 (2026-08-22, DESIGN-v0.11 §9 residual): for a pure create-only pack
 * (no independent edit target proven — see assertCreateRoute's own
 * independentEdit gate right above its call site), `surfaces` are meant to
 * be imitation references for the new file, not general workspace evidence.
 * Even with buildTaskPackCore's cleaned locatingQuery, a locator/role-filler
 * pass upstream of create_target resolution can still land on a low-signal
 * "everything weakly matches" state for a short/generic create request in a
 * small workspace and leave unrelated files in `surfaces`. Cap them here to
 * files living DIRECTLY in the target's own directory — or its nearest
 * existing ancestor when the target names a not-yet-created directory —
 * the exact scope directorySiblingEvidence already uses for create_target's
 * own directory_evidence. A target whose directory doesn't exist yet
 * (nearest ancestor is the workspace root) keeps no siblings: an untyped
 * project root has no established convention to imitate, so `surfaces`
 * becomes empty rather than the whole repo.
 *
 * F-V13-7 (2026-08-30, independent-verification remediation): the directory
 * filter alone carried NO COUNT LIMIT — a create target sharing its
 * directory with several existing files served every single one of them
 * (observed: a 37-byte/1-line new-file create pulled in 5 siblings, ~13KB,
 * before the file it was about to author even existed). `directorySiblingEvidence`
 * right above already caps the analogous directory_evidence at 3; this now
 * caps at `maxSiblings` (default 1 — one imitation template is enough to
 * show the convention). Selection is deterministic: sort by path (the same
 * bare `.sort()` directorySiblingEvidence itself uses) before slicing, so
 * which sibling(s) survive never depends on upstream locator ordering.
 */
function capCreateOnlyEvidence(
  surfaces: readonly TaskPackSurface[],
  workspace: string,
  targetPath: string,
  maxSiblings = 1,
): TaskPackSurface[] {
  const scopeDir = nearestExistingAncestorDir(workspace, targetPath) || ".";
  const inScope = surfaces.filter((surface) => path.posix.dirname(surface.path) === scopeDir);
  return inScope
    .sort((a, b) => (a.path < b.path ? -1 : a.path > b.path ? 1 : 0))
    .slice(0, maxSiblings);
}

/** Most common code extension among `dir`'s immediate children (e.g. ".py"), or undefined. */
function dominantChildExtension(workspace: string, dir: string): string | undefined {
  const counts = new Map<string, number>();
  for (const file of walkCodeFiles(workspace, dir ? { subPath: dir } : {})) {
    if (path.posix.dirname(file.relPath) !== (dir || ".")) continue;
    const ext = path.posix.extname(file.relPath);
    if (ext.length > 0) counts.set(ext, (counts.get(ext) ?? 0) + 1);
  }
  let best: string | undefined;
  let bestCount = 0;
  for (const [ext, count] of counts) {
    if (count > bestCount) { best = ext; bestCount = count; }
  }
  return best;
}

/**
 * V1 (turn-economy wave 3): resolve a create target NAMED as a dotted python
 * module (`pkg.sub.module` → `pkg/sub/module.py`) or an extension-less path-ish
 * token (`src/pricing/engine` → `src/pricing/engine.<domExt>`), each gated on
 * its parent PACKAGE DIRECTORY already existing. That structural gate is the
 * safety: it disambiguates module-vs-symbol (`pkg.module.symbol` where
 * `pkg/module.py` is a FILE — not a dir — never resolves), rejects prose dots,
 * and keeps the target inside the caller's real layout. Returns the FIRST such
 * token whose target file is ABSENT (not an existing file or subpackage dir) and
 * is not already a served surface. Extension for the dotted form is `.py` (the
 * dotted-import notation is python by construction); for the slash form it is
 * inferred from the parent's dominant sibling extension (skipped when there is
 * no sibling evidence to infer from — too speculative to guess a language).
 */
function resolveStructuralCreateTarget(
  query: string,
  workspace: string,
  servedPaths: Set<string>,
): { path: string; directory_evidence: string[] } | undefined {
  const consider = (
    target: string,
    parentDir: string,
  ): { path: string; directory_evidence: string[] } | undefined => {
    if (!isExistingDirRel(workspace, parentDir)) return undefined; // package dir must already exist
    if (fs.existsSync(path.join(workspace, target))) return undefined; // exists (file or dir) → edit/subpackage, not create
    if (servedPaths.has(target) || [...servedPaths].some((p) => p.endsWith(`/${target}`))) return undefined;
    return { path: target, directory_evidence: directorySiblingEvidence(workspace, parentDir) };
  };
  // Dotted python module: pkg.sub.mod → pkg/sub/mod.py when pkg/sub/ is a dir.
  for (const token of query.match(DOTTED_MODULE_TOKEN_RE) ?? []) {
    const segments = token.split(".");
    const parentDir = segments.slice(0, -1).join("/");
    const target = `${parentDir}/${segments[segments.length - 1]}.py`;
    const resolved = consider(target, parentDir);
    if (resolved !== undefined) return resolved;
  }
  // Extension-less path-ish token: src/foo/bar → src/foo/bar.<domExt> when src/foo/ is a dir.
  for (const m of query.matchAll(EXTLESS_PATH_TOKEN_RE)) {
    const raw = m[0];
    // D5 fix (2026-08-07): EXTLESS_PATH_TOKEN_RE's [\w-] class structurally
    // EXCLUDES dots, so a token immediately followed by a dotted/extensioned
    // continuation in the SOURCE query (e.g. "checkoutFlow.spec.ts" matches
    // only "checkoutFlow") is silently TRUNCATED rather than rejected —
    // `token.includes(".")` below can never catch this, because the
    // extracted token itself never contains the dot that got cut off.
    // Grafting an inferred extension onto that truncated prefix then mints
    // a WRONG path (observed live: "checkoutFlow.spec.ts" truncated to
    // "checkoutFlow" → synthesized "checkoutFlow.ts", dropping ".spec",
    // even though the real "checkoutFlow.spec.ts" was already a served
    // surface). A "." immediately followed by a word character in the
    // ORIGINAL query right after this match means the source text kept
    // going past a dot this char class cannot cross — skip rather than
    // guess. A bare trailing "." (ordinary sentence punctuation, nothing
    // word-like after it) is NOT this case and must keep resolving as
    // before — this is why the check is a lookahead on the original
    // string, not a property of the (necessarily dot-free) extracted token.
    if (/^\.\w/.test(query.slice(m.index! + raw.length))) continue;
    const token = raw.replace(/^\.\//, "").replace(/\/+$/, "");
    if (token.includes(".")) continue; // carries an extension → handled by Path 1
    const parentDir = path.posix.dirname(token);
    if (parentDir === "." || parentDir.length === 0) continue; // bare basename → Path 1 territory only
    const ext = dominantChildExtension(workspace, parentDir);
    if (ext === undefined) continue; // no sibling evidence → can't infer a language extension
    const resolved = consider(`${token}${ext}`, parentDir);
    if (resolved !== undefined) return resolved;
  }
  return undefined;
}

/**
 * W3: a create-intent query that NAMES a not-yet-existing file must answer with
 * the create route, not a contentless "go search for it" envelope. Two paths:
 *
 *  1) (wave 2, unchanged) the FIRST query-named path with an explicit code/test
 *     EXTENSION, under the strict verb+noun create gate. directory_evidence is
 *     drawn from the deepest EXISTING ancestor directory.
 *  2) (wave 3, V1) a dotted python MODULE (`pkg.sub.module`) or extension-less
 *     path-ish token, under the broader create/placement gate but structurally
 *     verified against an existing parent package directory — see
 *     resolveStructuralCreateTarget. This closes the live "Where should the new
 *     policyarc_actuary.pricing.rating_engine module live" abstain, which named
 *     no extensioned filename and carried no create VERB, so Path 1 alone left a
 *     contentless ~511-byte envelope.
 *
 * Either path resolves ONLY a target that does NOT already exist and is not
 * already a served surface (an existing name is an edit, not a create). Returns
 * undefined for any non-create/non-placement query, or when nothing resolves.
 */
function resolveCreateTargetFromQuery(
  query: string,
  workspace: string,
  surfaces: readonly TaskPackSurface[],
): { path: string; directory_evidence: string[] } | undefined {
  const servedPaths = new Set(surfaces.map((s) => s.path));
  // Path 1 (wave 2): explicit extensioned filename under the strict create gate.
  // W9 (2026-08-22): also fire on a bare JA create verb (作成/新規/...) with NO
  // co-occurring noun, mirroring hasCreateOrPlacementIntent's own bare-JA-verb
  // disjunct below. A JA request that already names its target by an explicit
  // extensioned path (e.g. "tmp/TlProbe.cs を...作成してください") legitimately
  // omits the redundant "ファイル" noun the EN phrasing ("a new FILE
  // tmp/TlProbe.cs") supplies instead — CREATE_TARGET_EXT_RE below still
  // requires an actual extensioned token before anything can be named, and the
  // existing-path/served-surface checks in the loop are unchanged, so widening
  // this gate only lets a genuinely-named, not-yet-existing target through; it
  // never relaxes what may be resolved, only which queries may try. EN
  // behavior is byte-identical: hasCreateFileIntent alone still gates it.
  if (hasCreateFileIntent(query) || CREATE_FILE_JA_RE.test(query)) {
    const seen = new Set<string>();
    for (const raw of query.match(CREATE_TARGET_EXT_RE) ?? []) {
      const candidate = raw.replace(/^\.\//, "").replace(/^["'`(]+|["'`)]+$/g, "");
      if (candidate.length === 0 || seen.has(candidate)) continue;
      seen.add(candidate);
      // A bare basename with no directory is still a valid create target, but a
      // path-shaped token is the strong signal; both are accepted.
      if (fs.existsSync(path.join(workspace, candidate))) continue; // exists → edit, not create
      if (servedPaths.has(candidate) || [...servedPaths].some((p) => p.endsWith(`/${candidate}`))) continue;

      // Deepest existing ancestor directory → sibling evidence for the template.
      let dir = path.posix.dirname(candidate);
      if (dir === ".") dir = "";
      while (dir.length > 0 && !fs.existsSync(path.join(workspace, dir))) {
        dir = path.posix.dirname(dir);
        if (dir === ".") dir = "";
      }
      const directoryEvidence = walkCodeFiles(workspace, dir ? { subPath: dir } : {})
        .map((file) => file.relPath)
        .filter((rel) => path.posix.dirname(rel) === (dir || "."))
        .sort()
        .slice(0, 3);
      return { path: candidate, directory_evidence: directoryEvidence };
    }
  }
  // Path 2/3 (wave 3, V1): dotted-module / extension-less create target under the
  // broader create/placement gate, verified structurally against an existing dir.
  if (hasCreateOrPlacementIntent(query)) {
    const structural = resolveStructuralCreateTarget(query, workspace, servedPaths);
    if (structural !== undefined) return structural;
  }
  return undefined;
}

/**
 * W3: re-assert the concrete create ROUTE on a NON-artifact create-target pack
 * as the FINAL word — a prepared contract legitimately drops a discovery
 * `next`, but a create's next is the edit action itself (`edit_file create:true
 * path=…`), not a discovery call, so it must survive. Called at the emit choke
 * point after all contract/route machinery. Never touches an artifact_build
 * pack (it carries its own artifact-section next) or an answer pack.
 *
 * W9 (2026-08-22): this is the SINGLE authoritative create re-assertion
 * point — it is called again just before the final contract build and once
 * more on the byte-budget fallback envelope, so it must replay EVERY field
 * the create-target block itself sets, not just route/next. A create-unaware
 * pass running between the create-target block and here can otherwise
 * overwrite the others — most concretely reconcileContentSufficiency's
 * default actionTargets rule, which (with change_contract cleared by the
 * create block) falls back to "surfaces[0] is the mandatory edit target" and
 * demotes a never-served LOCATOR hit into a needs-followup verdict, even
 * though that surface is unrelated context for a file that does not exist
 * yet.
 */
export function assertCreateRoute(result: TaskPackResult, profile: TaskProfile, workspace?: string): void {
  if (
    profile === "artifact_build"
    || result.create_target === undefined
    || result.route?.action === "answer_from_handles"
  ) {
    return;
  }
  result.coverage = "focused";
  result.coverage_reason = "single-site";
  result.missing = [];
  result.content_sufficiency = undefined;
  result.blocking_next_steps = undefined;
  result.next = `edit_file create:true path=${result.create_target.path}`;
  // Existing surfaces are reference context for create-only tasks. Keep a
  // frontier only when the request also proves an independent edit target —
  // F-V13-7 (2026-08-30, independent-verification remediation): and even
  // then, keep ONLY that target's own evidence. Before this, ANY independent
  // edit obligation exempted the WHOLE surface set from the create-only diet
  // below (a single unrelated "also edit config.ts" clause let every locator
  // hit through uncapped). `independentEditHandles`/`independentEditPaths`
  // name exactly the wiring/obligation targets that are load-bearing for
  // that edit; every other surface remains pure imitation reference
  // material for the new file and stays subject to capCreateOnlyEvidence.
  const independentEditHandles = new Set(result.wiring?.edit_frontier ?? []);
  const independentEditPaths = new Set(
    (result.change_contract?.obligations ?? [])
      .filter((obligation) => obligation.action === "edit" && obligation.path !== result.create_target?.path)
      .map((obligation) => obligation.path),
  );
  const independentEdit = independentEditHandles.size > 0 || independentEditPaths.size > 0;
  if (!independentEdit) {
    if (result.wiring !== undefined) result.wiring.edit_frontier = [];
    if (result.change_contract !== undefined) {
      result.change_contract.obligations = result.change_contract.obligations.filter((obligation) =>
        obligation.action !== "edit" || obligation.path === result.create_target?.path);
    }
  }
  // F-R14 (2026-08-22, DESIGN-v0.11 §9 residual) + F-V13-7 (2026-08-30): a
  // create-only pack's `surfaces` are imitation references, not general
  // evidence — cap them to the target's own directory (see
  // capCreateOnlyEvidence's doc comment). A surface backing a proven
  // independent edit obligation is load-bearing for that edit, not imitation
  // material, so it is carried through untouched — never handed to the
  // directory-scoped cap — regardless of independentEdit above. This is a
  // SEPARATE decision from the wiring/change_contract clearing just above:
  // diet application and frontier clearing must never be conflated — a pack
  // can legitimately keep a real edit frontier AND still diet its unrelated
  // imitation surfaces. `workspace` is only absent for legacy direct unit
  // calls that hand-build a TaskPackResult with no real filesystem behind
  // it; skip the cap then rather than resolve directories against a
  // workspace that doesn't exist.
  //
  // F-V13-7 follow-up: a surface the CALLER explicitly named (`paths:[...]`,
  // a query-named file, or a paired header/source sibling) is load-bearing
  // for the SAME reason an independent edit target is — dropping it silently
  // desyncs reconcileCallerPathCoverage's "every existing caller path is
  // either served or named in missing" invariant, which then reports the cut
  // sibling as a genuine gap and demotes the whole pack to needs-followup
  // (observed live: `paths:[ServiceA.ts, ServiceB.ts]` + surfaceRoles:["api"]
  // on a create query kept only ServiceA.ts, and the pack fell to
  // locate_missing_surfaces over ServiceB.ts). These markers are exactly what
  // the byte-budget-shedding pass below already treats as "never silently
  // drop an explicitly requested file" — reused here, not reinvented.
  const hasExplicitCallerProvenance = (why: string | undefined): boolean =>
    why !== undefined
    && (why.includes("caller-supplied") || why.includes("query-named-file") || why.includes("header-source-pair"));
  if (workspace !== undefined && result.surfaces.length > 0) {
    const keep = new Set(
      result.surfaces.filter((surface) =>
        independentEditHandles.has(surface.handle)
        || independentEditPaths.has(surface.path)
        || hasExplicitCallerProvenance(surface.why)),
    );
    const dietable = result.surfaces.filter((surface) => !keep.has(surface));
    const cappedList = capCreateOnlyEvidence(dietable, workspace, result.create_target.path);
    const capped = new Set(cappedList);
    result.surfaces = result.surfaces.filter((surface) => keep.has(surface) || capped.has(surface));
    // I-7 (2026-08-30 forensics attribution wave): fires only when the cap
    // actually removed a candidate sibling (F-V13-7's own incident: an
    // uncapped diet once served 5 siblings/~13KB for a 37-byte new file) —
    // a pack whose dietable set already fit under maxSiblings triggers
    // nothing here. `target_dir_class` mirrors capCreateOnlyEvidence's own
    // nearestExistingAncestorDir walk: does the create target's OWN
    // immediate parent directory already exist, or is this an as-yet-uncreated
    // directory with no sibling convention to imitate.
    if (isTraceEnabled() && dietable.length > cappedList.length) {
      const targetDir = path.posix.dirname(result.create_target.path);
      trace(
        "create_evidence_diet",
        {
          candidate_siblings: dietable.length,
          kept_siblings: cappedList.length,
          kept_caller_named: keep.size,
          target_dir_class: isExistingDirRel(workspace, targetDir === "." ? "" : targetDir)
            ? "existing-dir"
            : "new-dir",
        },
        workspace,
      );
    }
  }
  result.route = {
    action: "edit_from_handles",
    reason: "requested new file resolved; create it with edit_file create:true, imitating the served sibling(s)/module",
    max_additional_tl_calls: 0,
  };
  // A locator/concern-extraction pass upstream of create_target resolution can
  // treat the not-yet-existing class/file name as an "unmatched concern" (it
  // can never be found among served surfaces — it doesn't exist yet) and stamp
  // a "concern(s) not covered: <name>" check line. Telling the agent to go
  // locate/confirm the very class it is about to author is never actionable;
  // strip it once the create target is certain.
  result.checks = dropCreateTargetConcernChecks(result.checks, result.create_target.path);
  // W9 follow-up (2026-08-22): served siblings are imitation references ONLY
  // (see the route reason above) — a `likely_edits` hint on one of them (set
  // at surface-emission time, before create_target was known) tells the agent
  // to edit that EXISTING file instead of authoring the new one. The
  // actionable target already lives in `next`/`route`/`create_target`; strip
  // the stale hint from every surface every time the create route is
  // (re)asserted, so no later pass can leave one on the wire.
  for (const surface of result.surfaces) {
    if (surface.likely_edits !== undefined) {
      surface.likely_edits = undefined;
    }
  }
}

/**
 * Remove a "concern(s) not covered: …" check line's mention of the resolved
 * create target's own name — the class/file the agent is about to author can
 * never appear among served surfaces, so it is never a real gap. Drops just
 * the matching token from the comma-separated list; drops the whole line only
 * when that leaves nothing else uncovered, so a genuinely different unmatched
 * concern bundled in the same line still surfaces. A non-matching line (any
 * other check, or a concern line with no mention of the create target) is
 * returned byte-identical.
 */
function dropCreateTargetConcernChecks(
  checks: string[] | undefined,
  createTargetPath: string,
): string[] | undefined {
  if (checks === undefined || checks.length === 0) return checks;
  // normIdent (case- AND underscore-insensitive) so a PascalCase class name
  // like "AltitudeProbe" matches a snake_case file stem like "altitude_probe"
  // — the common C++/Python convention this file's own artifact-target
  // matching (exactArtifactTargetServed) already normalizes the same way.
  const stem = normIdent(path.basename(createTargetPath, path.extname(createTargetPath)));
  const base = normIdent(path.basename(createTargetPath));
  const prefix = "concern(s) not covered: ";
  const suffix = " — locate/confirm before declaring done";
  return checks.flatMap((line) => {
    if (!line.startsWith(prefix) || !line.endsWith(suffix)) return [line];
    const tokens = line.slice(prefix.length, line.length - suffix.length).split(",").map((token) => token.trim());
    const remaining = tokens.filter((token) => {
      const normalized = normIdent(token);
      return normalized !== stem && normalized !== base;
    });
    if (remaining.length === tokens.length) return [line];
    if (remaining.length === 0) return [];
    return [`${prefix}${remaining.join(",")}${suffix}`];
  });
}

function editIntentForRole(query: string, role: string, why?: string): string {
  // DESIGN-v0.8 §A6 deliverable 1: an exhaustive-initializer match carries
  // its own precise guidance regardless of which role it classified into
  // (aggregation-service code can land in "api" or "domain"
  // depending on the project's layout) — checked BEFORE the role-based
  // enum-like branches below so it always wins for this why, not just when
  // role happens to be "api".
  if (why === "exhaustive-initializer") {
    return "update exhaustive initializer/aggregation maps";
  }
  if (why === "concern-causal-layer") {
    return "this surface is the causal layer of the concern named in the task; fix the root cause here rather than compensating downstream, keeping this layer's state and units consistent";
  }

  const lower = query.toLowerCase();

  if (isEnumLikeQuery(query)) {
    if (role === "contract") return "add the value to exported enum/type/schema contracts";
    if (role === "api") return "update validation, transitions, repository/search, and aggregation enumerations";
    if (role === "ui") return "update badges, filters, forms, and rendered labels for the value";
    if (role === "style") return "add matching design tokens and class/style mappings for the value";
  }

  if (lower.includes("rename")) {
    return "update references that intentionally name the symbol";
  }

  return "inspect this handle and apply the minimal scoped edit";
}

function doneCheckForRole(query: string, role: string, why?: string): string {
  if (why === "exhaustive-initializer") {
    return "exhaustive initializer/aggregation map includes the new value";
  }
  if (why === "concern-causal-layer") {
    return "the concern named in the task is resolved at its causal layer and downstream layers no longer compensate for it";
  }

  const lower = query.toLowerCase();

  if (isEnumLikeQuery(query)) {
    if (role === "contract") return "new value is present in exported contract and exhaustive initializers compile";
    if (role === "api") return "validation, transitions, search/repository, and aggregate maps handle the new value";
    if (role === "ui") return "visible UI paths can render/select/filter the new value";
    if (role === "style") return "matching token/class exists for the new value";
  }

  if (lower.includes("rename")) {
    return "old intentional name references are gone and tests/imports still resolve";
  }

  return "target behavior is covered for this surface";
}

/**
 * DESIGN-v0.8 §C1 item 2: the GENERIC role fallback for edit_intent/done_check
 * — what editIntentForRole/doneCheckForRole return for ANY role once the
 * query carries no enum-like or rename shape (their final, role-independent
 * `return`). Computed by calling the same functions with an empty query
 * rather than a hardcoded string literal, so this never drifts out of sync
 * if the fallback wording in editIntentForRole/doneCheckForRole ever changes.
 * `role` does not actually affect the generic-branch result, but is threaded
 * through anyway to mirror the real call shape exactly. `why` is
 * deliberately omitted (undefined) here — the generic fallback must stay
 * why-independent so the DESIGN-v0.8 §A6 exhaustive-initializer branch above
 * always differs from it and survives the `!==` comparison at both call
 * sites.
 */
function genericEditIntent(role: string): string {
  return editIntentForRole("", role);
}
function genericDoneCheck(role: string): string {
  return doneCheckForRole("", role);
}

/**
 * DESIGN-v0.8 §C1 item 1: a surface is PRIMARY when it is the first surface
 * in the pack or is marked `required`. Ensures surfaces[0] always carries
 * likely_edits even when nothing in the pack is required (a free-text query
 * with no enum/rename trigger words) — emission sites already build
 * likely_edits for `required` surfaces inline, so this only needs to
 * backfill the first-surface case they cannot see (each candidate is
 * converted independently, without visibility into final array position).
 * A no-op when surfaces[0] already has likely_edits (the common case: most
 * packs' first surface is also required).
 */
function finalizePrimarySurface(surfaces: TaskPackSurface[]): void {
  const primary = surfaces[0];
  if (!primary || primary.likely_edits !== undefined) return;
  const hints = buildLikelyEdits(primary.role, primary.handle, primary.required ? 1 : 0.5);
  if (hints.length > 0) primary.likely_edits = hints;
}

/**
 * Cap on the number of advisory `checks` strings returned.
 * DESIGN-v0.8 coverage-honesty (Part 5): raised back to 3 — the structured
 * per-role records now carry machine-verifiable closure state (a style token
 * check, a search/filter text check), which is more than one line can hold
 * without losing the load-bearing distinctions. Each line is kept ≤120 chars.
 */
const MAX_COMPLETION_CHECKS = 3;

/** Max characters per completion-check line (Part 5). */
const MAX_CHECK_LINE_CHARS = 120;

/**
 * DESIGN-v0.8 coverage-honesty (Part 5): extract the enum's NEW-VALUE token(s)
 * from the query, generically. Uses the SAME all-caps enum-member shape the
 * locator's style-matching already keys on (`\b[A-Z][A-Z0-9_]{1,}\b`) — query
 * tokens ONLY, never fixture vocabulary. A query like "add a BLOCKED status"
 * yields ["BLOCKED"]; a query with no all-caps token yields [].
 */
function extractEnumValueTokens(query: string): string[] {
  return [...new Set(query.match(/\b[A-Z][A-Z0-9_]{1,}\b/g) ?? [])];
}

/** The `--<kebab>` CSS-custom-property form of an enum value token (e.g. BLOCKED → "--blocked"). */
function kebabTokenVariant(token: string): string | undefined {
  return deriveTokenVariants(token).find((v) => v.startsWith("--"));
}

/**
 * The MATCHER form of a style-check token: the kebab variant with a SINGLE
 * leading dash. Closure satisfaction is a raw substring test, and real
 * design systems namespace their custom properties — the 2026-07-11a live
 * stylesheet defines a namespaced form like `--color-level-low`, which `--low` can never match
 * (the double dash only exists at the property head), making the registered
 * check a PERMANENT false-negative. `-low` matches both the bare form
 * (`--low`) and any `--<namespace>-low` suffix; the leading dash keeps
 * `slow`/`flow`-style incidental words from satisfying it.
 */
function kebabCheckToken(token: string): string | undefined {
  const kebab = kebabTokenVariant(token);
  return kebab === undefined ? undefined : kebab.replace(/^--/, "-");
}

/**
 * Immediate directory of the first api-role surface, if any — used to scope a
 * search/filter text-coverage check's glob to where the api surface lives
 * (e.g. "apps/api/src/routes"), so the check only inspects the relevant tree.
 * Returns undefined when no api surface is identifiable.
 */
function apiSurfaceDir(surfaces: TaskPackSurface[]): string | undefined {
  const api = surfaces.find((s) => s.role === "api");
  if (!api) return undefined;
  const dir = path.dirname(api.path);
  return dir === "." ? undefined : dir;
}

/**
 * Concrete identifier tokens from a query, for the WIRING co-occurrence check,
 * in QUERY WORD ORDER (directionality: "feed A into B" → A precedes B). Uses
 * tokenizeQuery("identifier") — which splits camelCase/PascalCase and
 * snake_case/kebab-case — then keeps ONLY distinctive, identifier-shaped tokens
 * (an interior case transition, an underscore, or an all-caps ≥3 run), never
 * plain English words. This is the "both identifier tokens are concrete" guard:
 * a generic word like "health" alone would NOT qualify, but "isHealthy",
 * "NAV_LINK", or "onboard_pump_health" do.
 *
 * tokenizeQuery("identifier") sorts by distinctiveness (irrelevant here — we
 * need direction, not ranking), so tokens are RE-SORTED by first appearance in
 * the raw query. A split camelCase part (e.g. "Healthy" from "isHealthy") that
 * is a substring of a co-located whole token collapses to the whole token's
 * position, keeping compound identifiers as single ordered ends.
 *
 * W9 (2026-08-22): `query` is scrubbed of generic path spans FIRST. Without
 * it, a PascalCase-shaped path segment (a repo folder like "TokenLighten"
 * mentioned inside an absolute path in prose) passes the camelCase-interior
 * filter below and is treated as a "concrete" identifier — feeding both
 * concernAnchorTokens (via this function) and this function's OWN direct
 * callers (e.g. wiringConsumerNext's wiring-dead-end `find` derivation) a
 * token that names nothing in the repository.
 */
function concreteIdentifierTokens(query: string): string[] {
  const cleaned = stripPathSpans("", query);
  const toks = tokenizeQuery(cleaned, { minLen: 4, stopWords: new Set<string>(), mode: "identifier" }).filter((t) =>
    /[a-z][A-Z]/.test(t) ||        // camelCase interior boundary
    /_/.test(t) ||                  // snake_case
    /^[A-Z0-9]{3,}$/.test(t),       // all-caps token (NAV_LINK, CRC)
  );
  const lowerQuery = cleaned.toLowerCase();
  const posOf = (t: string): number => {
    const i = lowerQuery.indexOf(t.toLowerCase());
    return i === -1 ? Number.MAX_SAFE_INTEGER : i;
  };
  return [...toks].sort((a, b) => posOf(a) - posOf(b));
}

/** Case- and underscore-insensitive normalization for token↔surface matching (matches closure-scan normalization). */
function normIdent(s: string): string {
  return s.toLowerCase().replace(/_/g, "");
}

/**
 * Line window for a wiring co-occurrence check. Both identifiers must land in
 * ONE consumer file within this many lines of each other — wide enough to span
 * a typical read-then-encode block, tight enough that a mention in an unrelated
 * function does not count.
 */
const WIRING_PROXIMITY_LINES = 25;

/**
 * Closure-hardening: emit AT MOST ONE two-token co-occurrence WIRING check when
 * the query names a source-side identifier and a destination-side identifier
 * belonging to two DISTINCT, DIFFERENT-ROLE files. Pairing is ROLE-AWARE, not
 * query-word-order (see WIRING_DEST_ROLE_RANK): the destination is always the
 * {api,ui}-role end. Each end resolves to a served pack surface when one
 * covers it, else — 2026-07-10b for the destination, 2026-07-12b for the
 * source too — to a workspace file found by basename under the identical
 * conservatisms (both endpoints can now be workspace-resolved at once; see
 * the mapped/fallback composition inside the function). The check binds
 * source→destination and scopes its glob to the DESTINATION file: the check
 * is satisfied only when that file contains BOTH identifiers within
 * WIRING_PROXIMITY_LINES lines — proving the source was actually wired into
 * the consumer, not merely mentioned somewhere.
 *
 * Conservative by construction: BOTH tokens must be concrete identifiers
 * (wiringCandidateTokens) AND each end must resolve UNIQUELY (a served
 * surface, or 1-3 in-family basename matches); never fabricated from generic
 * words, and a silent no-op unless a genuine cross-role pair exists. Prefers
 * the wiring surfaces augmentNativeWiringSurface already added (their
 * symbols/paths are in the surface set the matcher scans).
 */
/**
 * Index of the FIRST surface whose `symbol` or path basename contains the
 * normalized concrete token `tok`, or -1 when none matches. Factored out of
 * buildWiringCooccurrenceCheck so item 3's wiringConsumerNext maps tokens to
 * surfaces with the IDENTICAL predicate — the two must agree on which token is
 * "the dest surface" vs "an uncovered consumer".
 */
function surfaceIndexForToken(tok: string, surfaces: TaskPackSurface[]): number {
  const nt = normIdent(tok);
  let bestIndex = -1;
  let bestRank = Number.POSITIVE_INFINITY;
  for (let index = 0; index < surfaces.length; index++) {
    const s = surfaces[index]!;
    const sym = s.symbol ? normIdent(s.symbol) : "";
    const base = normIdent(path.basename(s.path).replace(/\.[^.]+$/, ""));
    const body = relationCodeOnly(s.code ?? s.code_unchanged ?? "");
    const escaped = tok.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
    const bodyHasIdentifier = isWiringIdentifierShape(tok)
      && new RegExp(`\\b${escaped}\\b`).test(body);
    const callableDefinition = definedCallableNames(body)
      .some((name) => normIdent(name) === nt);
    const matches = (sym !== "" && sym.includes(nt))
      || (base !== "" && base.includes(nt))
      || (nt.includes(base) && base.length >= 4)
      || bodyHasIdentifier;
    if (!matches) continue;
    // A callable/symbol definition is stronger than a same-stem header, and
    // executable code is the useful insertion-side surface when both header
    // and implementation were seeded. This is structural and language/file
    // based; no subsystem vocabulary participates in the tie-break.
    const implementation = isImplementationPath(s.path);
    const rank = implementation && (sym === nt || callableDefinition)
      ? 0
      : implementation && bodyHasIdentifier
        ? 1
        : implementation && sym !== "" && sym.includes(nt)
          ? 2
          : implementation
            ? 3
            : sym === nt
              ? 4
              : bodyHasIdentifier
                ? 5
                : sym !== "" && sym.includes(nt)
                  ? 6
                  : 7;
    if (rank < bestRank) {
      bestRank = rank;
      bestIndex = index;
    }
  }
  return bestIndex;
}

/**
 * 2026-07-10c: candidate identifier pool for the wiring check. The shape-only
 * concreteIdentifierTokens filter starved the check to death in practice —
 * both live wiring queries yielded ZERO qualifying tokens (a 3-char acronym
 * like "PLL" dies on the shared minLen:4 cutoff before its ALL-CAPS shape is
 * even tested; a caps-run word like "PLLsync" has no lowercase→uppercase
 * transition; a hyphenated "link-status" fails the /_/ regex) — so
 * registration never happened and mode=closure stayed
 * the total:0 no-op. A query word is therefore ALSO concrete when the
 * WORKSPACE says so: its normalized form (lowercase, _- stripped) equals, or
 * is a >=3-char prefix of, a walked file's basename (PLL → pll.hpp,
 * PLLsync → pll_sync_driver.hpp). Grounding only ADMITS tokens into the
 * candidate pool — every downstream conservatism (source must map to a
 * served surface, destination must resolve uniquely, roles must differ, at
 * most one check) still applies unchanged. Ordered by query position,
 * deduped on normalized form, capped so the destination scan stays bounded.
 */
const WIRING_TOKEN_POOL_CAP = 8;
function isWiringIdentifierShape(token: string): boolean {
  return /^[A-Z][A-Z0-9_]{2,}$/.test(token)
    || /[_-]/.test(token)
    || /[a-z0-9][A-Z]/.test(token);
}

function identifierVocabulary(value: string): string[] {
  const separated = value
    .replace(/([a-z0-9])([A-Z])/g, "$1 $2")
    .replace(/[_\-.\/]+/g, " ");
  return [value, ...separated.split(/\s+/)]
    .map((token) => token.toLowerCase().replace(/[^a-z0-9]/g, ""))
    .filter((token) => token.length >= 2);
}

function observedRepositoryVocabulary(
  walkedRelPaths: ReadonlyArray<string>,
  surfaces: ReadonlyArray<TaskPackSurface>,
): Set<string> {
  const observed = new Set<string>();
  const add = (value: string): void => {
    for (const token of identifierVocabulary(value)) observed.add(token);
  };
  for (const relPath of walkedRelPaths) add(path.basename(relPath, path.extname(relPath)));
  for (const surface of surfaces) {
    if (surface.symbol) add(surface.symbol);
    const body = (surface.code ?? surface.code_unchanged ?? "").slice(0, 8192);
    for (const line of body.split(/\r?\n/).filter((candidate) =>
      /^\s*(?:import\b|#\s*include\b|use\b|from\b)/.test(candidate)
    ).slice(0, 24)) {
      for (const identifier of line.match(/[A-Za-z_$][A-Za-z0-9_$]*/g) ?? []) add(identifier);
    }
    for (const call of body.matchAll(/\b([A-Za-z_$][A-Za-z0-9_$]*)\s*\(/g)) {
      add(call[1]!);
    }
  }
  return observed;
}

/**
 * Emit-time invariant (2026-07-31 T13 rep0 forensics): a response that still
 * reports missing wiring endpoint(s) must either carry a concrete endpoint
 * sweep (a continuation/next_call whose purpose is "locate wiring …") or say
 * WHY none was planned. The silent coverage:"complete" +
 * "missing wiring endpoint(s): destination" contradiction — with a stopword
 * sweep or no sweep at all — is what pushed solvers off TL for the rest of
 * the cell. Runs at the finalizer because trim/reconcile can strip a sweep
 * planned earlier in the pipeline.
 */
function annotateUnsweptWiringEndpoints(result: TaskPackResult): void {
  const wiring = result.wiring;
  if (wiring === undefined || wiring.missing.length === 0 || wiring.note !== undefined) return;
  const isSweep = (call: { tool?: unknown; arguments?: Record<string, unknown> } | undefined): boolean =>
    call?.tool === "search_files"
    && (call.arguments?.["action"] === "find" || call.arguments?.["action"] === "references");
  const stages = result.continuation?.stages ?? [];
  const stageSweep = stages.some((stage) => (stage.calls ?? []).some((call) => isSweep(call)));
  const contractSweep = isSweep(
    result.execution_contract?.next_call as { tool?: unknown; arguments?: Record<string, unknown> } | undefined,
  );
  if (stageSweep || contractSweep) return;
  result.wiring = {
    ...wiring,
    note: "wiring endpoint(s) " + wiring.missing.join(",")
      + " not re-derived from this query (fully-served pack or no identifier-shaped endpoint token);"
      + " the route reflects what this pack served — previously served wiring surfaces stay authoritative",
  };
}

function wiringCandidateTokens(
  query: string,
  walkedRelPaths: ReadonlyArray<string>,
  surfaces: ReadonlyArray<TaskPackSurface> = [],
): string[] {
  // W9 (2026-08-22): scrub generic path spans from `query` FIRST — this
  // function's `shapeConcrete` phase pools raw `.match()`/`tokenizeQuery()`
  // extractions directly, ungrounded against repository vocabulary (that
  // grounding only gates the SECOND, fallback phase below), so a path
  // segment mentioned in prose (e.g. a PascalCase repo folder name) could
  // otherwise be admitted straight into the wiring token pool and become a
  // `find`/`references` `next` for a wiring-source/consumer/insertion gap.
  const cleanedQuery = stripPathSpans("", query);
  const normHere = (s: string): string => s.toLowerCase().replace(/[_-]/g, "");
  const observed = observedRepositoryVocabulary(walkedRelPaths, surfaces);
  const grounded = (tok: string): boolean => {
    const nt = normHere(tok);
    if (nt.length < 3) return false;
    if (observed.has(nt)) return true;
    for (const candidate of observed) {
      if (candidate.startsWith(nt)) return true;
    }
    return false;
  };
  // Two-phase pool: whole shape-concrete identifiers first (the original,
  // highest-quality signal — camelCase/snake/ALLCAPS words the caller typed
  // as identifiers), then workspace-grounded extras. The grounded pass must
  // not admit camel-hump FRAGMENTS of tokens already in the pool: the
  // minLen:3 tokenizer emits both "encodeLinkStatus" and its "Link" fragment,
  // and "Link" grounds against a link_status basename — which silently
  // replaced the precise ["isLocked","encodeLinkStatus"] pair with
  // ["PLL","Link"]. A grounded candidate whose normalized form is a substring
  // of any pooled token is that token's fragment, not new information.
  const lowerQuery = cleanedQuery.toLowerCase();
  const posOf = (t: string): number => {
    const i = lowerQuery.indexOf(t.toLowerCase());
    return i === -1 ? Number.MAX_SAFE_INTEGER : i;
  };
  const seen = new Set<string>();
  const out: string[] = [];
  const admit = (tok: string): boolean => {
    const nt = normHere(tok);
    if (seen.has(nt)) return false;
    for (const existing of seen) {
      if (existing.includes(nt)) return false; // fragment of a pooled token
    }
    seen.add(nt);
    out.push(tok);
    return out.length >= WIRING_TOKEN_POOL_CAP;
  };
  const rawIdentifiers = cleanedQuery.match(/\b[A-Za-z_$][A-Za-z0-9_$-]*\b/g) ?? [];
  const shapeConcrete = [
    ...concreteIdentifierTokens(cleanedQuery),
    ...rawIdentifiers.filter(isWiringIdentifierShape),
  ].filter((token, index, all) =>
    all.findIndex((candidate) => normHere(candidate) === normHere(token)) === index
  ).sort((a, b) => posOf(a) - posOf(b));
  for (const tok of shapeConcrete) {
    if (admit(tok)) return out;
  }
  const raw = tokenizeQuery(cleanedQuery, { minLen: 3, stopWords: new Set<string>(), mode: "identifier" });
  for (const tok of [...raw].sort((a, b) => posOf(a) - posOf(b))) {
    if (!grounded(tok)) continue;
    if (admit(tok)) return out;
  }
  return out;
}

interface ExplicitWiringRoles {
  source?: string;
  consumer?: string;
  adapter?: string;
}

/**
 * Extract directional roles only from explicit wiring grammar. Repository
 * validation still decides whether each token is a unique callable.
 */
function explicitWiringRoles(query: string): ExplicitWiringRoles {
  const identifier = "([A-Za-z_$][A-Za-z0-9_$]*)";
  const wire = new RegExp(
    "\\bwire\\s+" + identifier + "[\\s\\S]*?\\binto\\s+" + identifier
      + "[\\s\\S]*?\\busing\\s+(?:the\\s+)?(?:existing\\s+)?" + identifier,
    "i",
  ).exec(query);
  if (wire !== null) {
    return { source: wire[1], consumer: wire[2], adapter: wire[3] };
  }
  const replace = new RegExp(
    "\\busing\\s+" + identifier + "[\\s\\S]*?\\bin\\s+" + identifier
      + "[\\s\\S]*?\\bfrom\\s+" + identifier,
    "i",
  ).exec(query);
  return replace === null
    ? {}
    : { adapter: replace[1], consumer: replace[2], source: replace[3] };
}

/** Destination-role DISCIPLINE for the wiring check (both branches).
 *
 * Preference order within the allowed set: api first, then ui (10c shape:
 * "… uplink PLLsync …" must pick the uplink task impl over the protocol header).
 *
 * HARD GATE (2026-07-11b): a destination outside {api, ui} does not
 * register at all. That run's inverted check — "the protocol signal must
 * reach the filter in its own definition header" — anchored the
 * destination on the moving signal's own DEFINITION file (domain role) and
 * rode 33 edit echoes without ever being closable; the integration layer
 * the wiring actually lands in was not
 * derivable from that query's tokens, and the honest outcome there is
 * SILENCE, not a born-dead check. Every legitimate registration observed
 * across runs/corpus has an api-layer destination (task-loop impls,
 * encoder impls, routers); ui covers presentation-destination
 * tasks. domain/contract/config/doc destinations are conservative
 * casualties by design. */
const WIRING_DEST_ROLE_RANK: Record<string, number> = { api: 0, ui: 1 };

// 2026-08-07 (D3 fix): roles that must NEVER be treated as a wiring SOURCE,
// even though they are not {api,ui} destinations. Every per-function local
// `isDestRole` below is a NEGATIVE destination gate — config/data/domain/
// contract/unknown all pass `!isDestRole(role)`, which is correct for those
// roles (legitimate producer roles) but WRONG for "test"/"doc": a query that
// merely NAMES a real *.spec.ts or doc file (e.g. "make sure this does not
// break checkoutFlow.spec.ts") must not promote that file into a
// source-side wiring obligation just because it is not {api,ui}. Route
// every wiring SOURCE eligibility check through isEligibleWiringSourceRole —
// never re-add a bare `!isDestRole(...)` source check.
const WIRING_SOURCE_INELIGIBLE_ROLES = new Set<string>(["test", "doc"]);
function isEligibleWiringSourceRole(role: string): boolean {
  return WIRING_DEST_ROLE_RANK[role] === undefined && !WIRING_SOURCE_INELIGIBLE_ROLES.has(role);
}

function structuralWiringDestinationRole(
  relPath: string,
  classified: ImpactSurface,
): ImpactSurface | undefined {
  if (WIRING_DEST_ROLE_RANK[classified] !== undefined) return classified;
  // A query-selected executable implementation can be the integration edge
  // even when its directory has an unseen project-specific name. This role
  // is local to wiring direction; classifySurface remains domain-agnostic.
  return classified === "unknown" && isImplementationPath(relPath) ? "api" : undefined;
}

function structuralWiringSourceRole(
  relPath: string,
  classified: ImpactSurface,
  walked: ReadonlyArray<string>,
): ImpactSurface {
  if (classified !== "unknown" || !isImplementationPath(relPath)) return classified;
  const stem = path.basename(stripModuleExtension(relPath));
  const root = projectRootOf(relPath);
  const hasDeclarationSibling = walked.some((candidate) =>
    candidate !== relPath
    && path.basename(stripModuleExtension(candidate)) === stem
    && !isImplementationPath(candidate)
    && projectRootOf(candidate) === root
  );
  // A declaration+implementation pair is definition evidence. An otherwise
  // unclassified executable-only file is conservatively treated as the
  // integration side during workspace fallback.
  return hasDeclarationSibling ? "contract" : "api";
}

/**
 * One resolved end of a wiring pair, regardless of HOW it was resolved (a
 * served pack surface, or a workspace file found by basename) — the uniform
 * shape both the mapped-surface path and the two workspace-fallback paths
 * (destination: 2026-07-10b, source: 2026-07-12b) produce, so the pairing
 * logic downstream never needs to know which one supplied an endpoint.
 */
type WiringEndpoint = { token: string; path: string; role: ImpactSurface };

/**
 * Resolve ONE candidate token to a workspace file by (extension-stripped,
 * underscore-insensitive) basename match: between 1 and 3 files under
 * `inFamily` must qualify (0 = unresolvable, >3 = ambiguous — both silent),
 * `preferRoot` breaks a tie toward the file nearest that root ("" = no
 * preference, first walked-order match), and the resolved file's
 * classifySurface role must satisfy `roleOk`. Shared primitive behind BOTH
 * fallback directions (2026-07-10b destination; 2026-07-12b source) so
 * "resolvable" has exactly one definition workspace-wide; each caller only
 * supplies its own exclusion, iteration order, and role predicate.
 */
function resolveWiringWorkspaceToken(
  tok: string,
  walked: ReadonlyArray<string>,
  excludePath: string,
  inFamily: (p: string) => boolean,
  preferRoot: string,
  roleOk: (role: ImpactSurface) => boolean,
  roleForPath: (relPath: string) => ImpactSurface | undefined = (relPath) => classifySurface(relPath),
): { path: string; role: ImpactSurface } | undefined {
  const nt = normIdent(tok);
  const matches = walked.filter(
    (p) => p !== excludePath && inFamily(p) && normIdent(path.basename(p).replace(/\.[^.]+$/, "")).includes(nt),
  );
  if (matches.length === 0 || matches.length > 3) return undefined;
  const eligible = matches
    .map((candidatePath) => ({ path: candidatePath, role: roleForPath(candidatePath) }))
    .filter((candidate): candidate is { path: string; role: ImpactSurface } =>
      candidate.role !== undefined && roleOk(candidate.role)
    );
  if (eligible.length === 0) return undefined;
  const pick = eligible.find((candidate) =>
    isUnderAncestor(candidate.path, preferRoot === "." ? "" : preferRoot)
  ) ?? eligible[0]!;
  return pick;
}

/**
 * 2026-07-10b closure-derivation gap, factored out unchanged (2026-07-12b):
 * resolve the DESTINATION against the workspace when no mapped surface
 * qualifies — a wiring check earns its keep exactly when the pack
 * UNDER-covered the destination side (live: source-side surfaces served,
 * the consumer gate absent → no check registered, mode=closure stayed the "no
 * closure checks were registered" no-op, and a leg-dropping trajectory
 * shipped unchallenged). Tries the LATEST-mentioned candidate token first
 * (tail-first — "feed A into B", B is the destination), excluding `source`'s
 * own token/file and any token already tried as a mapped surface ("had its
 * chance above"). Keeps every conservatism of the surface-resolved form:
 * concrete identifiers only, family-scoped (2026-07-11a), role must differ
 * from source's AND pass the hard {api,ui} gate (WIRING_DEST_ROLE_RANK), at
 * most one result, silent when ambiguous (>3 basename matches). Satisfaction
 * semantics are IDENTICAL (both tokens in the destination file within the
 * proximity window) — this only widens when the check can REGISTER, never
 * what CLOSES it. Shared by the mapped-source branch and the 2026-07-12b
 * source-fallback branch below, so both compose through one definition of
 * "destination resolvable" instead of two copies of this loop.
 */
function findWiringDestFallback(
  source: WiringEndpoint,
  tokens: ReadonlyArray<string>,
  surfaces: TaskPackSurface[],
  walked: ReadonlyArray<string>,
  inFamily: (p: string) => boolean,
): WiringEndpoint | undefined {
  const isDestRole = (role: string): boolean => WIRING_DEST_ROLE_RANK[role] !== undefined;
  const srcRoot = projectRootOf(source.path);
  const sourceDir = path.dirname(source.path);
  const outsideSourceDir = (candidatePath: string): boolean =>
    inFamily(candidatePath) && !isUnderAncestor(candidatePath, sourceDir);
  type Cand = WiringEndpoint & { queryIdx: number };
  const cands: Cand[] = [];
  for (let i = tokens.length - 1; i >= 0; i--) {
    const tok = tokens[i]!;
    if (normIdent(tok) === normIdent(source.token)) continue;
    if (surfaceIndexForToken(tok, surfaces) !== -1) continue; // surface-mapped tokens had their chance above
    const res = resolveWiringWorkspaceToken(
      tok, walked, source.path, outsideSourceDir, srcRoot,
      (role) => role !== source.role && isDestRole(role),
      (relPath) => structuralWiringDestinationRole(relPath, classifySurface(relPath)),
    );
    if (res) cands.push({ token: tok, path: res.path, role: res.role, queryIdx: i });
  }
  // Rank viable destinations by executable-code preference, then by latest
  // query position (the original tail-first heuristic) as the tie-break.
  cands.sort((a, b) => {
    const ra = WIRING_DEST_ROLE_RANK[a.role] ?? 9;
    const rb = WIRING_DEST_ROLE_RANK[b.role] ?? 9;
    if (ra !== rb) return ra - rb;
    return b.queryIdx - a.queryIdx;
  });
  return cands[0];
}

/**
 * 2026-07-12b: resolve the SOURCE against the workspace when NO mapped token
 * qualifies as one — `mapped` is empty (neither end reached a served
 * surface: the live dir-seeded shape, e.g. a pack whose surfaces are
 * boot/init files and secondary headers, never the source or consumer module)
 * or every mapped token is ITSELF dest-role (a served api/ui surface exists
 * but nothing non-dest-role does — see bestMappedWiringDest). Mirrors
 * findWiringDestFallback with the two differences wiring direction demands:
 * EARLIEST-mentioned candidate token first (head-first — a source is
 * announced before its consumer in "feed A into B" phrasing; if that token
 * resolves to a dest-role file, it is not a valid source and the next
 * candidate is tried), and the role gate is INVERTED — source must NEVER be
 * {api,ui} (WIRING_DEST_ROLE_RANK; wiring direction is domain/contract/etc.
 * INTO the executable integration layer, never the reverse). A token already
 * tried as a mapped surface is skipped: in this branch that only ever means
 * it mapped to a dest-role surface, which would fail the role gate again.
 */
function hasCallableDeclarationSibling(
  token: string,
  definitionPath: string,
  workspace: string,
  cache?: FileReadCache,
): boolean {
  const declarationStem = stripModuleExtension(definitionPath);
  return [
    `${declarationStem}.d.ts`,
    `${declarationStem}.d.mts`,
    `${declarationStem}.d.cts`,
    `${declarationStem}.d.tsx`,
  ].some((declarationPath) =>
    definedCallableNames(readCached(workspace, declarationPath, cache) ?? "")
      .some((name) => normIdent(name) === normIdent(token))
  );
}

function findWiringSourceFallback(
  tokens: ReadonlyArray<string>,
  surfaces: TaskPackSurface[],
  walked: ReadonlyArray<string>,
  inFamily: (p: string) => boolean,
  workspace: string,
  cache?: FileReadCache,
): WiringEndpoint | undefined {
  const concrete = new Set(tokens.filter(isWiringIdentifierShape).map(normIdent));
  const sourceTokens = concrete.size > 0
    ? tokens.filter((token) => concrete.has(normIdent(token)))
    : tokens;
  for (const tok of sourceTokens) {
    const mappedIndex = surfaceIndexForToken(tok, surfaces);
    const mappedSurface = mappedIndex >= 0 ? surfaces[mappedIndex] : undefined;
    const mappedDefinesToken = mappedSurface !== undefined
      && isImplementationPath(mappedSurface.path)
      && definedCallableNames(servedSurfaceText(mappedSurface))
        .some((name) => normIdent(name) === normIdent(tok));
    if (
      mappedDefinesToken
      && mappedSurface !== undefined
      && !hasCallableDeclarationSibling(tok, mappedSurface.path, workspace, cache)
    ) {
      continue;
    }
    const definitionPaths = workspaceCallableDefinitionPaths(
      tok,
      walked,
      workspace,
      cache,
    ).filter(inFamily);
    if (definitionPaths.length === 1) {
      const definitionPath = definitionPaths[0]!;
      const role = structuralWiringSourceRole(
        definitionPath,
        classifySurface(definitionPath),
        walked,
      );
      const hasCallableDeclaration = hasCallableDeclarationSibling(
        tok,
        definitionPath,
        workspace,
        cache,
      );
      if (isEligibleWiringSourceRole(role) || hasCallableDeclaration) {
        return {
          token: tok,
          path: definitionPath,
          role: hasCallableDeclaration ? "contract" : role,
        };
      }
    }
    // Multiple unrelated implementation bodies are a genuine producer
    // ambiguity. Do not fall back to basename ordering for the same token.
    if (definitionPaths.length > 1) continue;
    const res = resolveWiringWorkspaceToken(
      tok,
      walked,
      "",
      inFamily,
      "",
      (role) => isEligibleWiringSourceRole(role),
      (relPath) => structuralWiringSourceRole(relPath, classifySurface(relPath), walked),
    );
    if (res) return { token: tok, path: res.path, role: res.role };
  }
  return undefined;
}

/**
 * 2026-07-12b: when `source` came from findWiringSourceFallback (no
 * non-dest-role mapped token existed), any entries STILL in `mapped` can only
 * be dest-role ones (a non-dest-role entry would have been picked as source
 * instead) — i.e. a served surface already covers the destination side ("the
 * half-mapped case": dest-role surface mapped, source unmapped). Prefer one
 * of those over ANOTHER workspace walk — it is already a located, served
 * surface — using the SAME rank/latest-query-position tie-break as the
 * mapped-source fast path's destCandidates (no need for the `tokens` array
 * itself: `mapped`'s own element order already IS the query-position order,
 * being built by iterating `tokens` and keeping only the mapped ones).
 */
function bestMappedWiringDest(
  source: WiringEndpoint,
  mapped: ReadonlyArray<{ token: string; surfIdx: number }>,
  surfaces: TaskPackSurface[],
): WiringEndpoint | undefined {
  const isDestRole = (role: string): boolean => WIRING_DEST_ROLE_RANK[role] !== undefined;
  const cands = mapped
    .map((cand, i) => ({
      cand,
      i,
      role: structuralWiringDestinationRole(
        surfaces[cand.surfIdx]!.path,
        asImpactSurface(surfaces[cand.surfIdx]!.role),
      ),
    }))
    .filter(({ cand, role }) =>
      role !== undefined
      && isDestRole(role)
      && normIdent(cand.token) !== normIdent(source.token)
      && !isUnderAncestor(surfaces[cand.surfIdx]!.path, path.dirname(source.path))
    );
  cands.sort((a, b) => {
    const ra = WIRING_DEST_ROLE_RANK[a.role!] ?? 9;
    const rb = WIRING_DEST_ROLE_RANK[b.role!] ?? 9;
    if (ra !== rb) return ra - rb;
    return b.i - a.i;
  });
  const best = cands[0]?.cand;
  if (best === undefined) return undefined;
  const bestSurface = surfaces[best.surfIdx]!;
  return {
    token: best.token,
    path: bestSurface.path,
    role: structuralWiringDestinationRole(bestSurface.path, asImpactSurface(bestSurface.role))!,
  };
}

/**
 * buildWiringCooccurrenceCheck's own return shape: PackCheckRecord (minus
 * `id`, assigned by buildCheckRecords' `push`) PLUS `sourceDir` — the SOURCE
 * endpoint's workspace-relative module directory. Consumed by
 * closureTracking.ts's wiringCheckSatisfied (2026-07-12b satisfaction
 * redesign: a wiring check now closes on a non-comment reference to the
 * source token OUTSIDE this directory, not on dest-file token proximity).
 *
 * `sourceDir` is intentionally NOT added to PackCheckRecord's own declaration
 * (session.ts is owned by a concurrent change this wave, so it is out of
 * scope here) — it rides through session.ts's recordPackChecks/
 * getPackChecks/_mergeCheckRecords as an ordinary extra object property (all
 * three are pass-through — `[...records]`, `byId.set(r.id, r)`, and a direct
 * return of the stored state — none reconstructs a record field-by-field),
 * and buildCheckRecords' `push` (below) forwards it via `{ id, ...r }`, which
 * TypeScript's excess-property check does not apply to (spread-derived
 * properties are exempt). `deriveCheckId` (session.ts) only reads
 * token/tokens/role/glob/desc off its argument, so this field changes NO
 * existing check id.
 */
type WiringCheckDraft = Omit<PackCheckRecord, "id"> & { sourceDir: string };

/** `languageForPath` families where an absent `export` keyword reliably means "cannot be imported elsewhere" — every other language's own visibility rules are out of scope here. */
const WIRING_EXPORT_AWARE_LANGS: ReadonlySet<string> = new Set([
  "typescript", "typescriptreact", "javascript", "javascriptreact",
]);

/**
 * W2A-2 (2026-08-21 wiring false-positive): is `token` — defined in
 * `definingPath` — ALREADY disqualified from a fresh "must be consumed
 * elsewhere" obligation, either because it is a TS/JS symbol with no
 * `export` anywhere in its own file (so no other module could ever import
 * it — the whole "consumed outside <dir>" framing is unsatisfiable by
 * construction) or because a workspace-wide literal scan already finds it
 * CALLED — `token(` — on some line that is not its own declaration?
 *
 * Repro: `bindTaskProfile`, a plain (non-`export`) helper private to
 * readCodeTaskPack.ts with 4 real in-file call sites, produced "wiring:
 * bindTaskProfile must be consumed in non-comment code outside
 * features/task-pack — e.g. in sentinelComment.ts" after an unrelated
 * one-line edit near its signature. The obligation-emission sites below only
 * ever consulted `surfaces`/`mapped` — the handful of files THIS pack
 * happened to have already served as evidence — never the repository at
 * large, so a private helper with every one of its real consumers outside
 * that small set reads as having none. This check is a genuine
 * workspace-wide answer instead.
 *
 * Deliberately narrow so it cannot re-suppress an existing true positive
 * (readCodeTaskPack.spec.ts's QKF/g_config wiring fixtures): the export
 * check only fires for TS/JS (a C++ class with no `export` keyword at all
 * must not be waved through), and the consumer check requires call syntax —
 * `Type.field`/`Type::method` access, a type annotation, or the symbol's own
 * `function token(`/`class token`/`const token =` declaration line never
 * qualifies. A scan that finds NEITHER disqualifying fact stays silent (the
 * caller keeps evaluating candidates, never a false "unconsumed" claim), and
 * a freshly added symbol with zero real call sites anywhere keeps its
 * obligation — the true-positive path this guard must not touch.
 */
function wiringSourceAlreadyResolved(
  token: string,
  definingPath: string,
  workspace: string,
  cache?: FileReadCache,
  // W7 integration fix (2026-08-21): the destination path THIS check draft
  // is about to name as "where consumption is missing", when known. A call
  // site living there is not evidence the wiring is "already resolved
  // ELSEWHERE" — it IS the very connection this check exists to confirm (an
  // "answer"/"fix"-shaped wiring query about an EXISTING producer/consumer
  // relationship, e.g. "fix transmitPacket when sampleSignal returns the
  // wrong boolean", regressed to the weak query-token-only fallback because
  // sampleSignal's own call inside packet_sender.ts — the exact destination
  // this check would report — was counted as disqualifying "already
  // resolved" evidence). A call site in any OTHER file is unaffected and
  // still disqualifies, preserving the original W2A-2 fix intact.
  excludePath?: string,
): boolean {
  const definingLang = languageForPath(definingPath);
  if (definingLang === undefined || !WIRING_EXPORT_AWARE_LANGS.has(definingLang)) {
    // TS/JS only: `function foo(`/`class Foo`/`const foo =`/… reliably marks
    // a DECLARATION line, so a bare `token(` scan can subtract it and count
    // only real call sites. A C-family method has no such marker — an
    // out-of-class definition ("Type::method(") and a header prototype
    // ("returnType method();") both match `token(` with nothing comparably
    // reliable to exclude — so this guard stays silent for every other
    // language rather than misread a declaration/definition pair as
    // consumption (readCodeTaskPack.spec.ts's QKF/isHealthy C++ fixtures are
    // exactly that: a header declaration + one out-of-line definition, zero
    // real callers, and the obligation MUST still fire for them).
    return false;
  }
  const escaped = escapeRegExp(token);
  const definingText = readCached(workspace, definingPath, cache);
  if (definingText !== undefined) {
    const exportedPattern = new RegExp(
      `export\\s+(?:default\\s+)?(?:declare\\s+)?(?:async\\s+)?`
        + `(?:function\\*?|class|const|let|var|interface|type|enum|namespace)\\s+${escaped}\\b`
        + `|export\\s*\\{[^}]*\\b${escaped}\\b[^}]*\\}`,
    );
    if (!exportedPattern.test(definingText)) return true;
  }
  const declarationShape = new RegExp(
    `\\b(?:function\\*?|class|interface|type|enum|namespace|const|let|var)\\s+${escaped}\\b`,
  );
  const callShape = new RegExp(`\\b${escaped}\\s*\\(`);
  const matches = scanLiteral(token, workspace, { caseInsensitive: false });
  return matches.some((match) =>
    match.path !== excludePath
      && typeof match.text === "string"
      && callShape.test(match.text)
      && !declarationShape.test(match.text),
  );
}

function buildWiringCooccurrenceCheck(
  query: string,
  surfaces: TaskPackSurface[],
  workspace: string,
): WiringCheckDraft | undefined {
  if (surfaces.length === 0) return undefined;
  const walked = walkCodeFiles(workspace, {}).map((f) => f.relPath);
  const tokens = wiringCandidateTokens(query, walked, surfaces);
  if (tokens.length < 2) return undefined;

  const explicitRoles = explicitWiringRoles(query);
  if (explicitRoles.source !== undefined && explicitRoles.consumer !== undefined) {
    const sourcePaths = workspaceCallableDefinitionPaths(
      explicitRoles.source, walked, workspace, undefined,
    );
    const consumerPaths = workspaceCallableDefinitionPaths(
      explicitRoles.consumer, walked, workspace, undefined,
    );
    if (
      sourcePaths.length >= 1
      && consumerPaths.length === 1
      && sourcePaths[0] !== consumerPaths[0]
    ) {
      const consumerPath = consumerPaths[0]!;
      return {
        desc: "wiring: " + explicitRoles.source
          + " must feed " + explicitRoles.consumer
          + " through " + (explicitRoles.adapter ?? "the existing adapter"),
        tokens: [explicitRoles.source, explicitRoles.consumer],
        proximity: WIRING_PROXIMITY_LINES,
        glob: consumerPath,
        role: structuralWiringDestinationRole(
          consumerPath,
          classifySurface(consumerPath),
        ) ?? "api",
        sourceDir: path.dirname(sourcePaths[0]!),
      };
    }
  }

  // Map each concrete token (in QUERY ORDER) to the FIRST surface whose symbol
  // or path basename contains it (normalized). Keep the surface index so we can
  // require the two ends to be different surfaces with different roles.
  type Mapped = { token: string; surfIdx: number };
  const mapped: Mapped[] = [];
  for (const tok of tokens) {
    const idx = surfaceIndexForToken(tok, surfaces);
    if (idx !== -1) mapped.push({ token: tok, surfIdx: idx });
  }

  const isDestRole = (role: string): boolean => WIRING_DEST_ROLE_RANK[role] !== undefined;

  // ROLE-AWARE pairing (2026-07-11b): the old "source = first-mapped token"
  // rule made the pair DIRECTION depend on query word order — a query that
  // mentions the integration layer first ("… uplink … PLL …") put the
  // api-side token in the source slot, leaving only domain/contract tokens
  // as destinations, i.e. exactly the inverted born-dead shape ("… must
  // reach the filter in its own header") the destination gate exists to prevent. Wiring
  // has an inherent direction: a signal flows FROM domain/contract code
  // INTO the executable integration layer. So the source is the
  // strongest mapped token on a NON-{api,ui} surface: caller-spelled
  // identifiers first, then domain/contract roles, with query order as the
  // deterministic tie-break.
  //
  // 2026-07-12b: when NO mapped token qualifies as that source — `mapped` is
  // empty, or every mapped token is itself dest-role — the OLD code fell
  // back to `mapped[0]` regardless of role (a dest-role token could become
  // `source`, inverting direction) or, if `mapped` was empty outright,
  // bailed before the destination-side workspace fallback below ever ran.
  // Both were the SAME root gap: a served surface was required for the
  // source end even though the destination end had had a workspace fallback
  // since 2026-07-10b. The mapped-source fast path (this branch) is
  // untouched; the two-sided fallback composition lives in the else branch.
  const concreteTokenSet = new Set([
    ...concreteIdentifierTokens(query).map(normIdent),
    ...tokens.filter(isWiringIdentifierShape).map(normIdent),
  ]);
  const hasUnmappedConcrete = tokens.some((token) =>
    concreteTokenSet.has(normIdent(token)) && surfaceIndexForToken(token, surfaces) === -1
  );
  const sourceRoleRank = (role: string): number => {
    if (role === "domain") return 0;
    if (role === "contract" || role === "data") return 1;
    if (role === "config") return 2;
    return 3;
  };
  const callableTokenRank = (entry: Mapped): number => {
    const escaped = entry.token.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
    const body = surfaces[entry.surfIdx]!.code ?? "";
    return new RegExp(`\\b${escaped}\\s*\\(`).test(body) ? 0 : 1;
  };
  const sourceTokenRank = (entry: Mapped): number =>
    // A short acronym commonly names the owning type/subsystem while the
    // adjacent callable is the actual producer endpoint (QKF isHealthy).
    // Longer identifiers remain in query order, preserving explicit class
    // endpoints such as EstimatorCore.
    /^[A-Z][A-Z0-9_]{2,3}$/.test(entry.token) ? 1 : 0;
  const definitionBackedSource = (entry: Mapped): boolean => {
    const surface = surfaces[entry.surfIdx]!;
    const token = normIdent(entry.token);
    const symbol = normIdent(surface.symbol ?? "");
    const basename = normIdent(path.basename(surface.path, path.extname(surface.path)));
    return (symbol.length > 0 && (symbol === token || symbol.includes(token)))
      || (basename.length >= 4 && (basename === token || basename.includes(token)))
      || definedCallableNames(servedSurfaceText(surface))
        .some((name) => normIdent(name) === token);
  };
  const mappedSourceEntry = mapped
    .map((entry, index) => ({ entry, index }))
    .filter(({ entry }) =>
      isEligibleWiringSourceRole(surfaces[entry.surfIdx]!.role)
      // Body co-occurrence alone is not a producer definition. In particular,
      // local variables such as `normalSide` must not become cross-file
      // wiring sources merely because the task query and edit body mention
      // them. Fall through to the workspace definition lookup instead.
      && definitionBackedSource(entry)
      && (!hasUnmappedConcrete || concreteTokenSet.has(normIdent(entry.token)))
    )
    .sort((a, b) => {
      const aConcrete = concreteTokenSet.has(normIdent(a.entry.token)) ? 0 : 1;
      const bConcrete = concreteTokenSet.has(normIdent(b.entry.token)) ? 0 : 1;
      return aConcrete - bConcrete
        || sourceTokenRank(a.entry) - sourceTokenRank(b.entry)
        || sourceRoleRank(structuralWiringSourceRole(
          surfaces[a.entry.surfIdx]!.path,
          asImpactSurface(surfaces[a.entry.surfIdx]!.role),
          walked,
        )) - sourceRoleRank(structuralWiringSourceRole(
          surfaces[b.entry.surfIdx]!.path,
          asImpactSurface(surfaces[b.entry.surfIdx]!.role),
          walked,
        ))
        || a.index - b.index;
    })[0]?.entry;

  // Compact, human-readable form of the wiring source's directory for the
  // check's `desc` string: at most the last 2 path segments, so `desc` stays
  // within closureTracking.ts's MAX_DESC_CHARS budget even for deeply-nested
  // source paths. "." (a workspace-root source file) reads as "the workspace
  // root" rather than a bare, easy-to-miss dot. Display-only — satisfaction
  // (closureTracking.ts's isUnderSourceDir) always compares the FULL
  // `sourceDir` field, never this shortened form.
  const shortSourceDirFor = (dir: string): string => {
    if (dir === "" || dir === ".") return "the workspace root";
    const parts = dir.replace(/\\/g, "/").split("/").filter(Boolean);
    return parts.length <= 2 ? parts.join("/") : parts.slice(-2).join("/");
  };

  const earlyFamilyScope = roleFillScope(surfaces);
  const earlyInFamily = (candidatePath: string): boolean =>
    earlyFamilyScope === "" || earlyFamilyScope === "."
      ? true
      : earlyFamilyScope !== null && isUnderAncestor(candidatePath, earlyFamilyScope);
  const workspaceSource = earlyFamilyScope === null
    ? undefined
    : findWiringSourceFallback(tokens, surfaces, walked, earlyInFamily, workspace);
  const mappedSourceQueryIndex = mappedSourceEntry === undefined
    ? Number.MAX_SAFE_INTEGER
    : tokens.findIndex((token) => normIdent(token) === normIdent(mappedSourceEntry.token));
  const workspaceSourceQueryIndex = workspaceSource === undefined
    ? Number.MAX_SAFE_INTEGER
    : tokens.findIndex((token) => normIdent(token) === normIdent(workspaceSource.token));
  const mappedSourceDefinesToken = mappedSourceEntry !== undefined
    && definedCallableNames(servedSurfaceText(surfaces[mappedSourceEntry.surfIdx]!))
      .some((name) => normIdent(name) === normIdent(mappedSourceEntry.token));
  const workspaceSourceDefinesToken = workspaceSource !== undefined
    && definedCallableNames(readCached(workspace, workspaceSource.path) ?? "")
      .some((name) => normIdent(name) === normIdent(workspaceSource.token));
  if (
    workspaceSource !== undefined
    && (
      (workspaceSourceDefinesToken && !mappedSourceDefinesToken)
      || (
        workspaceSourceDefinesToken === mappedSourceDefinesToken
        && (
          workspaceSourceQueryIndex < mappedSourceQueryIndex
          || (
            workspaceSourceQueryIndex === mappedSourceQueryIndex
            && !mappedSourceDefinesToken
          )
        )
      )
    )
  ) {
    const destination = bestMappedWiringDest(workspaceSource, mapped, surfaces)
      ?? findWiringDestFallback(workspaceSource, tokens, surfaces, walked, earlyInFamily);
    if (destination === undefined || normIdent(workspaceSource.token) === normIdent(destination.token)) {
      return undefined;
    }
    if (wiringSourceAlreadyResolved(workspaceSource.token, workspaceSource.path, workspace, undefined, destination.path)) {
      return undefined;
    }
    return {
      desc: `wiring: ${workspaceSource.token} must be consumed in non-comment code outside ${shortSourceDirFor(path.dirname(workspaceSource.path))} — e.g. in ${path.basename(destination.path)}`,
      tokens: [workspaceSource.token, destination.token],
      proximity: WIRING_PROXIMITY_LINES,
      glob: destination.path,
      role: destination.role,
      sourceDir: path.dirname(workspaceSource.path),
    };
  }

  if (mappedSourceEntry !== undefined) {
    const source = mappedSourceEntry;
    const destCandidates = mapped
      .map((cand, i) => ({
        cand,
        i,
        role: structuralWiringDestinationRole(
          surfaces[cand.surfIdx]!.path,
          asImpactSurface(surfaces[cand.surfIdx]!.role),
        ),
      }))
      .filter(({ cand, role }) =>
        cand.surfIdx !== source.surfIdx &&
        !isUnderAncestor(
          surfaces[cand.surfIdx]!.path,
          path.dirname(surfaces[source.surfIdx]!.path),
        ) &&
        role !== undefined &&
        role !== surfaces[source.surfIdx]!.role &&
        // Hard destination-role gate — see WIRING_DEST_ROLE_RANK.
        isDestRole(role),
      );
    destCandidates.sort((a, b) => {
      const ra = WIRING_DEST_ROLE_RANK[a.role!] ?? 9;
      const rb = WIRING_DEST_ROLE_RANK[b.role!] ?? 9;
      if (ra !== rb) return ra - rb;
      const callableDelta = callableTokenRank(a.cand) - callableTokenRank(b.cand);
      if (callableDelta !== 0) return callableDelta;
      return b.i - a.i;
    });
    const dest: Mapped | undefined = destCandidates[0]?.cand;
    if (dest !== undefined && normIdent(source.token) !== normIdent(dest.token)) {
      const destPath = surfaces[dest.surfIdx]!.path;
      if (wiringSourceAlreadyResolved(source.token, surfaces[source.surfIdx]!.path, workspace, undefined, destPath)) {
        return undefined;
      }
      return {
        desc: `wiring: ${source.token} must be consumed in non-comment code outside ${shortSourceDirFor(path.dirname(surfaces[source.surfIdx]!.path))} — e.g. in ${path.basename(destPath)}`,
        tokens: [source.token, dest.token],
        proximity: WIRING_PROXIMITY_LINES,
        glob: destPath,
        role: structuralWiringDestinationRole(
          destPath,
          asImpactSurface(surfaces[dest.surfIdx]!.role),
        ),
        sourceDir: path.dirname(surfaces[source.surfIdx]!.path),
      };
    }

    // 2026-07-11a: the fallback's destination walk was WORKSPACE-WIDE, so
    // registered checks pointed at the bench harness's own code, mcp-server's
    // test fixtures, and sibling projects (live: 5/5 registrations outside
    // the fixture, all born-dead). A wiring destination must live in the SAME
    // project family as the located surfaces: single-root packs scope to that
    // root, multi-root packs to their common ancestor (roleFillScope — the
    // exact discipline the required-role fill already uses), and with no
    // family anchor at all the fallback stays silent rather than guessing
    // across the workspace. "" (single-project workspace) keeps the walk
    // unscoped, which is then genuinely in-family.
    const familyScope = roleFillScope(surfaces);
    if (familyScope === null) return undefined;
    const inFamily = (p: string): boolean =>
      familyScope === "" || familyScope === "." ? true : isUnderAncestor(p, familyScope);
    const srcEndpoint: WiringEndpoint = {
      token: source.token,
      path: surfaces[source.surfIdx]!.path,
      role: surfaces[source.surfIdx]!.role as ImpactSurface,
    };
    const best = findWiringDestFallback(srcEndpoint, tokens, surfaces, walked, inFamily);
    if (best !== undefined && !wiringSourceAlreadyResolved(srcEndpoint.token, srcEndpoint.path, workspace, undefined, best.path)) {
      return {
        desc: `wiring: ${srcEndpoint.token} must be consumed in non-comment code outside ${shortSourceDirFor(path.dirname(srcEndpoint.path))} — e.g. in ${path.basename(best.path)}`,
        tokens: [srcEndpoint.token, best.token],
        proximity: WIRING_PROXIMITY_LINES,
        glob: best.path,
        role: best.role,
        sourceDir: path.dirname(srcEndpoint.path),
      };
    }
    return undefined;
  }

  // 2026-07-12b: neither endpoint is served — or the ONLY served endpoint is
  // itself dest-role (the live dir-seeded shape: the locator's surfaces missed
  // both the domain source and the consumer destination; see
  // findWiringSourceFallback's doc comment for the concrete repro). Resolve
  // SOURCE against the workspace too, under the identical conservatisms as
  // the destination fallback, then feed it into the SAME destination
  // resolution the mapped-source branch above uses (a mapped dest-role
  // surface first, else the workspace) — composing the two fallback
  // directions instead of re-deriving the pairing rules a third time.
  const familyScope = roleFillScope(surfaces);
  if (familyScope === null) return undefined;
  const inFamily = (p: string): boolean =>
    familyScope === "" || familyScope === "." ? true : isUnderAncestor(p, familyScope);

  const source = findWiringSourceFallback(tokens, surfaces, walked, inFamily, workspace);
  if (source === undefined) return undefined;

  const dest = bestMappedWiringDest(source, mapped, surfaces)
    ?? findWiringDestFallback(source, tokens, surfaces, walked, inFamily);
  if (dest === undefined) return undefined;
  if (normIdent(source.token) === normIdent(dest.token)) return undefined;
  if (wiringSourceAlreadyResolved(source.token, source.path, workspace, undefined, dest.path)) return undefined;

  return {
    desc: `wiring: ${source.token} must be consumed in non-comment code outside ${shortSourceDirFor(path.dirname(source.path))} — e.g. in ${path.basename(dest.path)}`,
    tokens: [source.token, dest.token],
    proximity: WIRING_PROXIMITY_LINES,
    glob: dest.path,
    role: dest.role,
    sourceDir: path.dirname(source.path),
  };
}

const IMPACT_SURFACE_ROLES = new Set<string>([
  "contract", "api", "domain", "data", "ui", "style", "test", "config", "doc", "unknown",
]);

function asImpactSurface(role: string): ImpactSurface {
  return IMPACT_SURFACE_ROLES.has(role) ? role as ImpactSurface : "unknown";
}

function hasServedCode(surface: TaskPackSurface): boolean {
  return (surface.code?.length ?? 0) > 0 || (surface.code_unchanged?.length ?? 0) > 0;
}

function hasSufficientEditContent(surface: TaskPackSurface): boolean {
  // anchors_served (2026-07-25 honest-hub fix): a partial hub whose decision
  // anchors are all inside the served ranges is edit-sufficient — without this
  // exemption the honest "partial" label re-created the receiver-construction
  // discovery-stuck regression the Wave-4 label suppression papered over.
  if (
    !hasServedCode(surface)
    || (surface.content_completeness === "partial" && surface.anchors_served !== true)
  ) return false;
  const inspectConfidence = surface.likely_edits
    ?.find((hint) => hint.kind === "inspect-slice")
    ?.confidence;
  return !(
    (surface.outline?.length ?? 0) > 0
    && inspectConfidence !== undefined
    && inspectConfidence < HIGH_CONFIDENCE_THRESHOLD
  );
}

function endpointFromSurface(
  token: string,
  surface: TaskPackSurface,
  evidence: TaskWiringEvidence[],
): TaskWiringEndpoint {
  return {
    token,
    handle: surface.handle,
    path: surface.path,
    range: surface.range,
    role: asImpactSurface(surface.role),
    evidence: [...new Set(evidence)],
  };
}

function firstNonCommentTokenLine(text: string, token: string): number {
  const normalizedToken = normIdent(token);
  const lines = text.split(/\r?\n/);
  for (let index = 0; index < lines.length; index++) {
    const trimmed = lines[index]!.trim();
    if (
      trimmed.startsWith("//")
      || trimmed.startsWith("/*")
      || trimmed.startsWith("*")
      || trimmed.startsWith("#")
    ) continue;
    if (normIdent(trimmed).includes(normalizedToken)) return index + 1;
  }
  return 1;
}

/**
 * Ensures a wiring endpoint is represented by an embedded, reusable handle.
 * The locator/check machinery decides the path and role; this helper only
 * supplies a bounded edit anchor and never invents implementation code.
 */
function ensureWiringEndpointSurface(
  surfaces: TaskPackSurface[],
  endpoint: WiringEndpoint,
  workspace: string,
  cache: FileReadCache | undefined,
  required: boolean,
  maxSurfaces = MAX_SURFACES_DISTINCT,
  maxSurfaceBytes = MAX_SURFACE_CODE_BYTES,
  /**
   * B2a (2026-08-01 serving-completeness): the file-size bound under which this
   * surface is served WHOLE (range 1-EOF) instead of a ±20/+40 window around the
   * token match. Defaults to the tiny-file thresholds, i.e. today's behavior;
   * the construct-receiver frontier raises it so a wiring edit site never
   * arrives as a teaser the solver has to full-read.
   */
  completeUnderBytes = TINY_FILE_MAX_BYTES,
  completeUnderLines = TINY_FILE_MAX_LINES,
): { surface: TaskPackSurface; discovered: boolean } | undefined {
  let surface = surfaces.find((candidate) => candidate.path === endpoint.path);
  if (surface !== undefined) {
    if (required) surface.required = true;
    // B2a: a raised completeness bar must also WIDEN a surface some earlier
    // (smaller-budget) ensure already served partially — otherwise the first
    // caller's teaser wins and the frontier stays incomplete.
    const widenToWhole = completeUnderBytes > TINY_FILE_MAX_BYTES
      && (surface.content_completeness === "partial" || !hasServedCode(surface));
    if (widenToWhole) {
      const text = readCached(workspace, surface.path, cache);
      const lineCount = text === undefined ? 0 : Math.max(1, countLines(text));
      if (
        text !== undefined
        && Buffer.byteLength(text, "utf8") <= completeUnderBytes
        && lineCount <= completeUnderLines
      ) {
        const wholeRange = `1-${lineCount}`;
        const whole = centeredSliceForCap(workspace, surface.path, wholeRange, cache, undefined, maxSurfaceBytes);
        if (whole !== undefined && whole.remaining_ranges.length === 0) {
          surface.range = wholeRange;
          surface.handle = handleTable.upsert({
            kind: "range",
            path: surface.path,
            range: wholeRange,
            workspaceRoot: workspace,
          }).id;
          surface.code = whole.code;
          delete surface.content_completeness;
          delete surface.remaining_ranges;
          return { surface, discovered: false };
        }
      }
    }
    if (!hasServedCode(surface)) {
      const embedded = centeredSliceForCap(workspace, surface.path, surface.range, cache, undefined, maxSurfaceBytes);
      if (embedded !== undefined) {
        surface.code = embedded.code;
        if (embedded.remaining_ranges.length > 0) {
          surface.content_completeness = "partial";
          surface.remaining_ranges = embedded.remaining_ranges;
        }
      }
    }
    return { surface, discovered: false };
  }

  const text = readCached(workspace, endpoint.path, cache);
  if (text === undefined) return undefined;
  if (surfaces.length >= maxSurfaces) {
    let replaceIndex = -1;
    for (let index = surfaces.length - 1; index >= 0; index--) {
      if (surfaces[index]!.required !== true) {
        replaceIndex = index;
        break;
      }
    }
    if (replaceIndex < 0) return undefined;
    surfaces.splice(replaceIndex, 1);
  }

  const lineCount = Math.max(1, countLines(text));
  const matchLine = firstNonCommentTokenLine(text, endpoint.token);
  const wholeFileFits = Buffer.byteLength(text, "utf8") <= completeUnderBytes
    && lineCount <= completeUnderLines;
  const range = wholeFileFits
    ? "1-" + lineCount
    : Math.max(1, matchLine - 20) + "-" + Math.min(lineCount, matchLine + 40);
  const handle = handleTable.upsert({
    kind: "range",
    path: endpoint.path,
    range,
    workspaceRoot: workspace,
  });
  const embedded = centeredSliceForCap(workspace, endpoint.path, range, cache, matchLine, maxSurfaceBytes);
  const callableSymbol = inferCallableSymbol(text, matchLine);
  surface = {
    role: endpoint.role,
    handle: handle.id,
    path: endpoint.path,
    range,
    ...(callableSymbol ? { symbol: callableSymbol } : {}),
    ...(required ? { required: true } : {}),
    why: required
      ? "wiring destination for " + endpoint.token + "; use this handle as the insertion site"
      : "wiring source for " + endpoint.token + "; review before editing the consumer",
    ...(embedded ? { code: embedded.code } : {}),
    ...(embedded && embedded.remaining_ranges.length > 0
      ? { content_completeness: "partial" as const, remaining_ranges: embedded.remaining_ranges }
      : {}),
  };
  surfaces.push(surface);
  return { surface, discovered: true };
}

function stripModuleExtension(relPath: string): string {
  return relPath
    .replace(/\.d\.(?:[cm]?ts|tsx)$/i, "")
    .replace(/\.(?:c|cc|cpp|cxx|h|hh|hpp|hxx|js|jsx|mjs|mts|ts|tsx|py|rb|go|rs)$/i, "");
}

function relationCodeOnly(content: string): string {
  return content
    .replace(/\/\*[\s\S]*?\*\//g, " ")
    .replace(/(^|\s)\/\/.*$/gm, "$1")
    .replace(/^\s*#(?!\s*include\b).*$/gm, " ");
}

function definedCallableNames(content: string): string[] {
  const names: string[] = [];
  const code = relationCodeOnly(content);
  for (const match of code.matchAll(/\b(?:def|fn|func|function)\s+([A-Za-z_][A-Za-z0-9_]*)\s*\(/g)) {
    names.push(match[1]!);
  }
  for (const match of code.matchAll(/\b([A-Za-z_][A-Za-z0-9_]*(?:::[A-Za-z_][A-Za-z0-9_]*)*)\s*\([^;{}]*\)\s*(?:const\s*)?\{/g)) {
    const name = match[1]!.split("::").at(-1)!;
    if (!/^(?:if|for|while|switch|catch)$/.test(name)) names.push(name);
  }
  return [...new Set(names)].slice(0, 12);
}

function declaredCallableNames(content: string): string[] {
  const names = [...definedCallableNames(content)];
  const code = relationCodeOnly(content);
  for (const match of code.matchAll(
    /\b([A-Za-z_][A-Za-z0-9_]*)\s*\([^;{}]*\)\s*(?:const\s*)?(?:->\s*[^;]+)?;/g,
  )) {
    const name = match[1]!;
    if (!/^(?:if|for|while|switch|catch|return|sizeof)$/.test(name)) names.push(name);
  }
  return [...new Set(names)].slice(0, 16);
}

function implementsCallableToken(content: string, token: string): boolean {
  if (definedCallableNames(content).some((name) => normIdent(name) === normIdent(token))) return true;
  if (token.length === 0) return false;
  const escaped = token.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
  return new RegExp(`\\b${escaped}::[A-Za-z_][A-Za-z0-9_]*\\s*\\(`)
    .test(relationCodeOnly(content));
}

function hasEditGradeCallArgument(
  query: string,
  destinationCode: string,
  supportingSurfaces: readonly TaskPackSurface[],
): boolean {
  const requiresLiteral = /\b(?:hard[- ]?coded|literal|replace)\b/i.test(query);
  for (const surface of supportingSurfaces) {
    const names = [surface.symbol, ...declaredCallableNames(servedSurfaceText(surface))]
      .filter((name): name is string => Boolean(name));
    for (const name of names) {
      const escaped = name.replace(/[.*+?^$(){}|[\]\\]/g, "\\$&");
      const call = new RegExp("\\b" + escaped + "\\s*\\(([^)]{1,1024})\\)", "m")
        .exec(destinationCode);
      if (call === null || call[1]!.trim().length === 0) continue;
      if (
        !requiresLiteral
        || /(?:\btrue\b|\bfalse\b|\bnull\b|\bundefined\b|["'][^"']*["']|\b\d+(?:\.\d+)?\b)/.test(call[1]!)
      ) {
        return true;
      }
    }
  }
  return false;
}

function wiringConsumerToken(
  query: string,
  surface: TaskPackSurface,
  fallback: string,
): string {
  const callables = definedCallableNames(servedSurfaceText(surface));
  const explicit = explicitCodeIdentifiers(query).find((identifier) =>
    callables.some((callable) => normIdent(callable) === normIdent(identifier))
  );
  if (explicit !== undefined) {
    return callables.find((callable) => normIdent(callable) === normIdent(explicit)) ?? explicit;
  }
  if (surface.symbol !== undefined) {
    const defined = callables.find((callable) => normIdent(callable) === normIdent(surface.symbol!));
    if (defined !== undefined) return defined;
  }
  return callables.length === 1 ? callables[0]! : surface.symbol ?? fallback;
}

/**
 * Return bounded implementation definitions for one callable token. Multiple
 * unrelated bodies mean a new wiring task has no structural basis for choosing
 * a producer; an import edge may still disambiguate them later.
 */
function workspaceCallableDefinitionPaths(
  token: string,
  walked: ReadonlyArray<string>,
  workspace: string,
  cache: FileReadCache | undefined,
): string[] {
  const wanted = normIdent(token);
  const matches: string[] = [];
  let filesRead = 0;
  let bytesRead = 0;
  for (const relPath of walked) {
    if (filesRead >= READINESS_PROBE_MAX_FILES || bytesRead >= READINESS_PROBE_MAX_BYTES) break;
    if (!isImplementationPath(relPath)) continue;
    const content = readCached(workspace, relPath, cache);
    if (content === undefined) continue;
    filesRead += 1;
    bytesRead += Buffer.byteLength(content, "utf8");
    if (definedCallableNames(content).some((name) => normIdent(name) === wanted)) {
      matches.push(relPath);
      if (matches.length > 3) break;
    }
  }
  return matches;
}

function workspaceCallableDeclarationPaths(
  token: string,
  walked: ReadonlyArray<string>,
  workspace: string,
  cache: FileReadCache | undefined,
): string[] {
  const wanted = normIdent(token);
  const escaped = token.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
  const declaration = new RegExp(
    "^\\s*(?:(?:export|declare|extern|static|inline|virtual)\\s+)*"
      + "(?:[A-Za-z_][A-Za-z0-9_:<>,*&]*\\s+)+"
      + escaped + "\\s*\\([^;{}]*\\)\\s*(?:const\\s*)?(?:->\\s*[^;]+)?;\\s*$",
    "m",
  );
  const matches: string[] = [];
  let filesRead = 0;
  let bytesRead = 0;
  for (const relPath of walked) {
    if (filesRead >= READINESS_PROBE_MAX_FILES || bytesRead >= READINESS_PROBE_MAX_BYTES) break;
    const content = readCached(workspace, relPath, cache);
    if (content === undefined) continue;
    filesRead += 1;
    bytesRead += Buffer.byteLength(content, "utf8");
    if (
      definedCallableNames(content).some((name) => normIdent(name) === wanted)
      || declaration.test(relationCodeOnly(content))
    ) {
      matches.push(relPath);
      if (matches.length > 3) break;
    }
  }
  return matches;
}

function calledCallableNames(content: string): string[] {
  const code = relationCodeOnly(content);
  const names: string[] = [];
  for (const match of code.matchAll(/\b([A-Za-z_][A-Za-z0-9_]*)\s*\(([^)]*)\)/g)) {
    const name = match[1]!;
    if (/^(?:if|for|while|switch|catch|return|sizeof)$/.test(name)) continue;
    if (match[2]!.trim().length === 0) continue;
    const tail = code.slice((match.index ?? 0) + match[0].length);
    if (/^\s*(?:const\s*)?\{/.test(tail)) continue;
    names.push(name);
  }
  return [...new Set(names)].slice(0, 6);
}

/** One bounded definition -> direct-call hop to a concrete consumer body. */
function workspaceCallConsumer(
  definitionSurface: TaskPackSurface | undefined,
  walked: ReadonlyArray<string>,
  workspace: string,
  cache: FileReadCache | undefined,
  familyScope: string | null,
  query: string,
): WiringEndpoint | undefined {
  if (definitionSurface === undefined || familyScope === null) return undefined;
  const definition = readCached(workspace, definitionSurface.path, cache)
    ?? definitionSurface.code
    ?? definitionSurface.code_unchanged
    ?? "";
  const callables = definedCallableNames(definition);
  if (callables.length === 0) return undefined;
  const queryTerms = tokenizeForEpoch(query).filter((term) => term.length >= 4).slice(0, 32);
  const candidates: Array<WiringEndpoint & { score: number }> = [];
  let filesRead = 0;
  let bytesRead = 0;
  for (const relPath of walked) {
    if (filesRead >= READINESS_PROBE_MAX_FILES || bytesRead >= READINESS_PROBE_MAX_BYTES) break;
    if (relPath === definitionSurface.path || !isImplementationPath(relPath)) continue;
    if (familyScope !== "" && familyScope !== "." && !isUnderAncestor(relPath, familyScope)) continue;
    const content = readCached(workspace, relPath, cache);
    if (content === undefined) continue;
    filesRead++;
    bytesRead += Buffer.byteLength(content, "utf8");
    const code = relationCodeOnly(content);
    for (const callable of callables) {
      const escaped = callable.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
      const match = new RegExp(`\\b${escaped}\\s*\\(`).exec(code);
      if (match === null) continue;
      const prefix = code.slice(0, match.index);
      const line = prefix.split(/\r?\n/).length;
      const bodyNorm = normIdent(code);
      const pathNorm = normIdent(relPath);
      const contextScore = queryTerms.filter((term) =>
        bodyNorm.includes(normIdent(term)) || pathNorm.includes(normIdent(term))
      ).length;
      candidates.push({
        token: callable,
        path: relPath,
        role: structuralWiringDestinationRole(relPath, classifySurface(relPath)) ?? "api",
        score: 10 + contextScore * 2 + (inferCallableSymbol(content, line) ? 2 : 0),
      });
    }
  }
  candidates.sort((left, right) => right.score - left.score || left.path.localeCompare(right.path));
  if (candidates.length === 0) return undefined;
  if (candidates[1] !== undefined && candidates[1]!.score === candidates[0]!.score) return undefined;
  const { score: _score, ...endpoint } = candidates[0]!;
  return endpoint;
}

/** Resolve only import/include evidence observable in the selected consumer. */
function importedWiringSource(
  consumer: TaskPackSurface | undefined,
  sourceToken: string,
  walked: ReadonlyArray<string>,
  workspace: string,
  cache: FileReadCache | undefined,
): WiringEndpoint | undefined {
  if (consumer === undefined) return undefined;
  const body = readCached(workspace, consumer.path, cache)
    ?? consumer.code
    ?? consumer.code_unchanged
    ?? "";
  const tokenNorm = normIdent(sourceToken);
  const specs: string[] = [];
  for (const line of body.split(/\r?\n/).slice(0, 240)) {
    if (!/^\s*(?:import\b|#\s*include\b|use\b|from\b|.*\brequire\s*\()/.test(line)) continue;
    if (!normIdent(line).includes(tokenNorm)) continue;
    for (const match of line.matchAll(/(?:from\s*|require\s*\(\s*|#\s*include\s*)["'<]([^"'>)]+)["'>)]/g)) {
      specs.push(match[1]!);
    }
  }
  for (const specifier of specs.slice(0, 8)) {
    const normalized = specifier.replace(/\\/g, "/");
    const targetStem = normalized.startsWith(".")
      ? stripModuleExtension(path.posix.normalize(path.posix.join(path.posix.dirname(consumer.path), normalized)))
      : stripModuleExtension(normalized);
    const candidates = walked.filter((candidate) => {
      const stem = stripModuleExtension(candidate);
      return normalized.startsWith(".") ? stem === targetStem : stem.endsWith("/" + targetStem) || stem === targetStem;
    });
    candidates.sort((left, right) => {
      const implementation = Number(isImplementationPath(right)) - Number(isImplementationPath(left));
      return implementation || left.localeCompare(right);
    });
    const selected = candidates[0];
    if (selected !== undefined) {
      return { token: sourceToken, path: selected, role: asImpactSurface(classifySurface(selected)) };
    }
  }
  return undefined;
}

function importLineTargetsPath(
  consumerPath: string,
  line: string,
  targetPath: string,
): boolean {
  if (!/^\s*(?:import\b|#\s*include\b|use\b|from\b|.*\brequire\s*\()/.test(line)) return false;
  const specifiers = [...line.matchAll(
    /(?:\bfrom\s*|require\s*\(\s*|#\s*include\s*)["'<]([^"'>)]+)["'>)]/g,
  )].map((match) => match[1]!);
  const pythonFrom = /^\s*from\s+([A-Za-z_][A-Za-z0-9_.]*)\s+import\b/.exec(line)?.[1];
  if (pythonFrom !== undefined) specifiers.push(pythonFrom.replace(/\./g, "/"));

  const targetStem = stripModuleExtension(targetPath);
  return specifiers.some((specifier) => {
    const normalized = specifier.replace(/\\/g, "/");
    const specifierStem = stripModuleExtension(normalized);
    if (normalized.startsWith(".")) {
      return stripModuleExtension(path.posix.normalize(
        path.posix.join(path.posix.dirname(consumerPath), normalized),
      )) === targetStem;
    }
    return targetStem === specifierStem || targetStem.endsWith("/" + specifierStem);
  });
}

function evidenceNodeId(kind: "file" | "symbol", pathValue: string, symbol = ""): string {
  return `${kind}:${shaOfText(`${pathValue}\u0000${symbol}`).slice(0, 12)}`;
}

function evidenceRelationId(kind: TaskEvidenceRelationKind, from: string, to: string): string {
  return `relation:${kind}:${shaOfText(`${from}\u0000${to}`).slice(0, 12)}`;
}

function buildWiringEvidenceGraph(
  source: TaskWiringEndpoint | undefined,
  destination: TaskWiringEndpoint | undefined,
  surfaces: ReadonlyArray<TaskPackSurface>,
  workspace: string,
  cache: FileReadCache | undefined,
): TaskEvidenceGraph | undefined {
  // D10 (2026-08-14): `TL_EVIDENCE_RELATIONS` is deleted.
  if (source === undefined || destination === undefined) return undefined;
  const sourceSurface = surfaces.find((surface) => surface.handle === source.handle);
  const destinationSurface = surfaces.find((surface) => surface.handle === destination.handle);
  if (sourceSurface === undefined || destinationSurface === undefined) return undefined;

  const sourceSymbol = sourceSurface.symbol ?? source.token;
  const destinationSymbol = destinationSurface.symbol ?? destination.token;
  const sourceFileId = evidenceNodeId("file", source.path);
  const sourceSymbolId = evidenceNodeId("symbol", source.path, sourceSymbol);
  const destinationFileId = evidenceNodeId("file", destination.path);
  const destinationSymbolId = evidenceNodeId("symbol", destination.path, destinationSymbol);
  const nodes: TaskEvidenceNode[] = [
    { id: sourceFileId, kind: "file", path: source.path, handle: source.handle, range: source.range, roles: ["producer"] },
    { id: sourceSymbolId, kind: "symbol", path: source.path, symbol: sourceSymbol, handle: source.handle, range: source.range, roles: ["producer"] },
    { id: destinationFileId, kind: "file", path: destination.path, handle: destination.handle, range: destination.range, roles: ["consumer"] },
    { id: destinationSymbolId, kind: "symbol", path: destination.path, symbol: destinationSymbol, handle: destination.handle, range: destination.range, roles: ["consumer"] },
  ];
  const relations: TaskEvidenceRelation[] = [];
  const addRelation = (
    kind: TaskEvidenceRelationKind,
    from: string,
    to: string,
    provenance: TaskEvidenceRelation["provenance"],
    confidence: number,
  ): void => {
    const id = evidenceRelationId(kind, from, to);
    if (relations.some((relation) => relation.id === id)) return;
    relations.push({ id, kind, from, to, provenance, confidence });
  };
  if (
    isImplementationPath(source.path)
    && implementsCallableToken(servedSurfaceText(sourceSurface), source.token)
  ) {
    addRelation("defines", sourceFileId, sourceSymbolId, "index", sourceSurface.symbol ? 0.98 : 0.84);
  }
  if (
    isImplementationPath(destination.path)
    && implementsCallableToken(servedSurfaceText(destinationSurface), destination.token)
  ) {
    addRelation("defines", destinationFileId, destinationSymbolId, "index", destinationSurface.symbol ? 0.98 : 0.84);
  }

  const destinationBodyRaw = (readCached(workspace, destination.path, cache)
    ?? destinationSurface.code
    ?? destinationSurface.code_unchanged
    ?? "").slice(0, 32768);
  const destinationBody = relationCodeOnly(destinationBodyRaw);
  const escapedSource = source.token.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
  const importMentionsSource = destinationBodyRaw.split(/\r?\n/).some((line) =>
    normIdent(line).includes(normIdent(source.token))
    && importLineTargetsPath(destination.path, line, source.path)
  );
  if (importMentionsSource) addRelation("imports", destinationFileId, sourceFileId, "lexical", 0.9);
  if (new RegExp(`\\b${escapedSource}\\b`, "i").test(destinationBody)) {
    addRelation("references", destinationSymbolId, sourceSymbolId, "lexical", 0.82);
  }
  if (new RegExp(`\\b${escapedSource}\\s*\\(`).test(destinationBody)) {
    addRelation("direct_calls", destinationSymbolId, sourceSymbolId, "lexical", 0.94);
  }
  for (const supportSurface of surfaces) {
    if (
      supportSurface.handle === source.handle
      || supportSurface.handle === destination.handle
      || supportSurface.path === source.path
      || supportSurface.path === destination.path
    ) continue;
    const supportDefinitions = definedCallableNames(servedSurfaceText(supportSurface));
    const supportNames = [
      supportSurface.symbol,
      ...declaredCallableNames(servedSurfaceText(supportSurface)),
    ].filter((name): name is string => Boolean(name));
    const imported = destinationBodyRaw.split(/\r?\n/).some((line) => {
      if (!importLineTargetsPath(destination.path, line, supportSurface.path)) return false;
      const lineNorm = normIdent(line);
      return /^\s*#\s*include\b/.test(line)
        || supportNames.some((name) => lineNorm.includes(normIdent(name)));
    });
    const calledName = supportNames.find((name) => {
      const escaped = name.replace(/[.*+?^$(){}|[\]\\]/g, "\\$&");
      if (!new RegExp("\\b" + escaped + "\\s*\\(").test(destinationBody)) return false;
      if (imported) return true;
      const definingPaths = new Set(
        surfaces
          .filter((candidate) => candidate.path !== destination.path)
          .filter((candidate) => [
            candidate.symbol,
            ...declaredCallableNames(servedSurfaceText(candidate)),
          ].some((candidateName) =>
            candidateName !== undefined && normIdent(candidateName) === normIdent(name)))
          .map((candidate) => candidate.path),
      );
      return definingPaths.size === 1;
    });
    if (!imported && calledName === undefined) continue;

    const supportSymbol = calledName ?? supportNames[0];
    if (supportSymbol === undefined) continue;
    const supportFileId = evidenceNodeId("file", supportSurface.path);
    const supportSymbolId = evidenceNodeId("symbol", supportSurface.path, supportSymbol);
    nodes.push(
      { id: supportFileId, kind: "file", path: supportSurface.path, handle: supportSurface.handle, range: supportSurface.range, roles: ["adapter"] },
      { id: supportSymbolId, kind: "symbol", path: supportSurface.path, symbol: supportSymbol, handle: supportSurface.handle, range: supportSurface.range, roles: ["adapter"] },
    );
    if (
      isImplementationPath(supportSurface.path)
      && supportDefinitions.some((name) => normIdent(name) === normIdent(supportSymbol))
    ) {
      addRelation("defines", supportFileId, supportSymbolId, "index", supportSurface.symbol ? 0.98 : 0.9);
    }
    if (imported) addRelation("imports", destinationFileId, supportFileId, "lexical", 0.94);
    if (calledName !== undefined) {
      addRelation("references", destinationSymbolId, supportSymbolId, "lexical", 0.9);
      addRelation("direct_calls", destinationSymbolId, supportSymbolId, "lexical", 0.98);
    }
  }
  return { version: 1, nodes, relations };
}

/**
 * Prefer a callable consumer implementation over a declaration-only endpoint.
 * Wiring discovery often grounds a destination token in an API/header first
 * (for example MAVLink -> mavlink.hpp), even though the safe insertion site is
 * a task/controller that consumes that API.  Only promote already-served,
 * body-scoped references; this never guesses an unobserved edit target.
 */
function bestServedWiringConsumer(
  query: string,
  destinationToken: string,
  surfaces: readonly TaskPackSurface[],
): TaskPackSurface | undefined {
  const destinationNorm = normIdent(destinationToken);
  if (destinationNorm.length === 0) return undefined;
  const queryTerms = tokenizeForEpoch(query)
    .filter((term) => term.length >= 4 && normIdent(term) !== destinationNorm)
    .slice(0, 32);
  const candidates = surfaces
    .filter((surface) =>
      isImplementationPath(surface.path)
      && hasCallableBody(surface)
      && surfaceBodyContainsToken(surface, destinationToken)
    )
    .map((surface) => {
      const body = normIdent(surface.code ?? surface.code_unchanged ?? "");
      const pathText = normIdent(surface.path);
      const basename = normIdent(path.basename(surface.path, path.extname(surface.path)));
      const bodyHits = queryTerms.filter((term) => body.includes(normIdent(term))).length;
      const pathHits = queryTerms.filter((term) => pathText.includes(normIdent(term))).length;
      // A same-named implementation is normally the endpoint definition, not
      // its consumer.  It remains eligible when no real consumer was served.
      const definitionPenalty = basename.includes(destinationNorm) ? 100 : 0;
      return { surface, score: bodyHits * 4 + pathHits * 2 - definitionPenalty };
    });
  candidates.sort((a, b) =>
    b.score - a.score
    || a.surface.path.localeCompare(b.surface.path)
    || a.surface.range.localeCompare(b.surface.range)
  );
  return candidates.find((candidate) => candidate.score > 0)?.surface;
}

function buildLegacyTaskWiringProfile(
  query: string,
  surfaces: TaskPackSurface[],
  workspace: string,
  cache?: FileReadCache,
): TaskWiringProfile {
  const walked = walkCodeFiles(workspace, {}).map((file) => file.relPath);
  const tokens = wiringCandidateTokens(query, walked, surfaces);
  const check = buildWiringCooccurrenceCheck(query, surfaces, workspace);

  if (check?.tokens === undefined || check.tokens.length < 2 || check.glob === undefined) {
    const sourceToken = tokens[0];
    const destinationToken = tokens.find((token) =>
      sourceToken !== undefined && normIdent(token) !== normIdent(sourceToken)
    );
    const sourceIndex = sourceToken === undefined ? -1 : surfaceIndexForToken(sourceToken, surfaces);
    const destinationIndex = destinationToken === undefined
      ? -1
      : surfaceIndexForToken(destinationToken, surfaces);
    const sourceSurface = sourceIndex >= 0 ? surfaces[sourceIndex] : undefined;
    const destinationSurface = destinationIndex >= 0 ? surfaces[destinationIndex] : undefined;
    const missing: Array<"source" | "destination"> = [];
    if (sourceSurface === undefined) missing.push("source");
    if (destinationSurface === undefined) missing.push("destination");
    return {
      version: 1,
      status: "needs-followup",
      connections: [{
        id: "w1",
        status: "needs-followup",
        ...(sourceToken !== undefined && sourceSurface !== undefined
          ? { source: endpointFromSurface(sourceToken, sourceSurface, ["query-token", "located-surface"]) }
          : {}),
        ...(destinationToken !== undefined && destinationSurface !== undefined
          ? { destination: endpointFromSurface(destinationToken, destinationSurface, ["query-token", "located-surface"]) }
          : {}),
        supporting_handles: [],
        confidence: 0.4,
        evidence: ["query-token"],
      }],
      missing: missing.length > 0 ? missing : ["destination"],
      edit_frontier: [],
      review_frontier: [sourceSurface?.handle, destinationSurface?.handle]
        .filter((handle): handle is string => handle !== undefined),
    };
  }

  const [sourceToken, destinationToken] = check.tokens;
  const familyScope = roleFillScope(surfaces);
  const inFamily = (candidatePath: string): boolean =>
    familyScope === null || familyScope === "" || familyScope === "."
      ? familyScope !== null
      : isUnderAncestor(candidatePath, familyScope);
  const isDestinationRole = (role: string): boolean => WIRING_DEST_ROLE_RANK[role] !== undefined;

  const mappedSourceIndex = surfaceIndexForToken(sourceToken!, surfaces);
  const mappedSource = mappedSourceIndex >= 0
    && !isDestinationRole(surfaces[mappedSourceIndex]!.role)
    ? surfaces[mappedSourceIndex]
    : undefined;
  const directorySource = mappedSource ?? surfaces.find((candidate) =>
    !isDestinationRole(candidate.role)
    && path.dirname(candidate.path) === check.sourceDir
    && (
      normIdent(candidate.code ?? "").includes(normIdent(sourceToken!))
      || normIdent(path.basename(candidate.path)).includes(normIdent(sourceToken!))
    )
  );
  const resolvedSource = directorySource === undefined
    ? resolveWiringWorkspaceToken(
        sourceToken!,
        walked,
        "",
        inFamily,
        "",
        (role) => !isDestinationRole(role),
        (relPath) => structuralWiringSourceRole(relPath, classifySurface(relPath), walked),
      )
    : undefined;
  const sourceEndpoint: WiringEndpoint | undefined = directorySource !== undefined
    ? { token: sourceToken!, path: directorySource.path, role: asImpactSurface(directorySource.role) }
    : resolvedSource !== undefined
      ? { token: sourceToken!, path: resolvedSource.path, role: resolvedSource.role }
      : undefined;

  const declaredDestinationSurface = surfaces.find((candidate) => candidate.path === check.glob);
  const promotedDestinationSurface = bestServedWiringConsumer(query, destinationToken!, surfaces);
  if (
    promotedDestinationSurface !== undefined
    && declaredDestinationSurface !== undefined
    && promotedDestinationSurface.handle !== declaredDestinationSurface.handle
  ) {
    // The declaration/API remains useful review evidence, but it must no
    // longer compete with the proved consumer for required-surface trim space.
    declaredDestinationSurface.required = false;
  }
  const retainedConsumerSurface = surfaces.find((candidate) =>
    candidate.why?.startsWith("wiring destination for ") === true
    && hasCallableBody(candidate)
  );
  const retainedConsumerToken = retainedConsumerSurface?.why
    ?.match(/^wiring destination for (.+?);/)?.[1];
  const retainedConsumer: WiringEndpoint | undefined = retainedConsumerSurface !== undefined
    && retainedConsumerToken !== undefined
    ? {
        token: retainedConsumerToken,
        path: retainedConsumerSurface.path,
        role: structuralWiringDestinationRole(
          retainedConsumerSurface.path,
          asImpactSurface(retainedConsumerSurface.role),
        ) ?? "api",
      }
    : undefined;
  const provisionalDestinationSurface = promotedDestinationSurface ?? declaredDestinationSurface;
  const closureDefinitionSurface: TaskPackSurface | undefined = provisionalDestinationSurface ?? (() => {
    const content = readCached(workspace, check.glob!, cache);
    if (content === undefined) return undefined;
    return {
      role: asImpactSurface(check.role ?? classifySurface(check.glob!)),
      handle: "",
      path: check.glob!,
      range: `1-${Math.max(1, countLines(content))}`,
      code: content.slice(0, 32768),
    };
  })();
  const closureConsumer = retainedConsumer ?? workspaceCallConsumer(
      closureDefinitionSurface,
      walked,
      workspace,
      cache,
      familyScope,
      query,
    );
  if (closureConsumer !== undefined && provisionalDestinationSurface !== undefined) {
    provisionalDestinationSurface.required = false;
  }
  const destinationSurface = closureConsumer === undefined
    ? provisionalDestinationSurface
    : surfaces.find((candidate) => candidate.path === closureConsumer.path);
  const destinationEndpoint: WiringEndpoint = {
    token: destinationSurface === undefined
      ? closureConsumer?.token ?? destinationToken!
      : wiringConsumerToken(query, destinationSurface, closureConsumer?.token ?? destinationToken!),
    path: closureConsumer?.path ?? destinationSurface?.path ?? check.glob,
    role: destinationSurface !== undefined
      ? structuralWiringDestinationRole(destinationSurface.path, asImpactSurface(destinationSurface.role))
        ?? asImpactSurface(destinationSurface.role)
      : closureConsumer?.role ?? asImpactSurface(check.role ?? classifySurface(check.glob)),
  };

  const ensuredDestination = ensureWiringEndpointSurface(
    surfaces,
    destinationEndpoint,
    workspace,
    cache,
    true,
  );
  if (
    closureConsumer !== undefined
    && closureDefinitionSurface !== undefined
    && closureDefinitionSurface.path !== destinationEndpoint.path
  ) {
    ensureWiringEndpointSurface(
      surfaces,
      {
        token: closureConsumer.token,
        path: closureDefinitionSurface.path,
        role: structuralWiringDestinationRole(
          closureDefinitionSurface.path,
          asImpactSurface(closureDefinitionSurface.role),
        ) ?? "api",
      },
      workspace,
      cache,
      false,
    );
  }
  // When an unrelated same-name definition exists, the consumer's concrete
  // import/include edge outranks basename/query similarity for the producer.
  const importedSource = importedWiringSource(
    ensuredDestination?.surface,
    sourceToken!,
    walked,
    workspace,
    cache,
  );
  const sourceDefinitionPaths = importedSource === undefined
    ? workspaceCallableDefinitionPaths(sourceToken!, walked, workspace, cache)
    : [];
  const ambiguousUnlinkedSource = importedSource === undefined && sourceDefinitionPaths.length > 1;
  const uniqueDefinitionSource: WiringEndpoint | undefined = importedSource === undefined
    && sourceDefinitionPaths.length === 1
    ? {
        token: sourceToken!,
        path: sourceDefinitionPaths[0]!,
        role: hasCallableDeclarationSibling(
          sourceToken!,
          sourceDefinitionPaths[0]!,
          workspace,
          cache,
        )
          ? "contract"
          : structuralWiringSourceRole(
              sourceDefinitionPaths[0]!,
              classifySurface(sourceDefinitionPaths[0]!),
              walked,
            ),
      }
    : undefined;
  const selectedSourceEndpoint = importedSource
    ?? uniqueDefinitionSource
    ?? (ambiguousUnlinkedSource ? undefined : sourceEndpoint);
  const ensuredSource = selectedSourceEndpoint === undefined
    ? undefined
    : ensureWiringEndpointSurface(surfaces, selectedSourceEndpoint, workspace, cache, false);
  const missing: Array<"source" | "destination" | "insertion"> = [];
  if (ensuredSource === undefined) missing.push("source");
  if (ensuredDestination === undefined) missing.push("destination");
  const endpointsReady = missing.length === 0;

  const source = ensuredSource === undefined
    ? undefined
    : endpointFromSurface(
        sourceToken!,
        ensuredSource.surface,
        [
          "query-token",
          uniqueDefinitionSource !== undefined || ensuredSource.discovered
            ? "workspace-definition"
            : "located-surface",
        ],
      );
  const destination = ensuredDestination === undefined
    ? undefined
    : endpointFromSurface(
        wiringConsumerToken(query, ensuredDestination.surface, destinationEndpoint.token),
        ensuredDestination.surface,
        ["query-token", ensuredDestination.discovered ? "workspace-definition" : "located-surface"],
      );
  if (ensuredDestination !== undefined) {
    for (const token of tokens) {
      if (
        normIdent(token) === normIdent(sourceToken!)
        || normIdent(token) === normIdent(destinationToken!)
      ) {
        continue;
      }
      const importedSupport = importedWiringSource(
        ensuredDestination.surface,
        token,
        walked,
        workspace,
        cache,
      );
      if (
        importedSupport !== undefined
        && importedSupport.path !== source?.path
        && importedSupport.path !== destination?.path
      ) {
        ensureWiringEndpointSurface(
          surfaces,
          importedSupport,
          workspace,
          cache,
          false,
        );
      }
    }
  }
  if (ensuredDestination !== undefined) {
    const consumerBody = readCached(workspace, ensuredDestination.surface.path, cache)
      ?? ensuredDestination.surface.code
      ?? ensuredDestination.surface.code_unchanged
      ?? "";
    for (const calledName of calledCallableNames(consumerBody)) {
      if (normIdent(calledName) === normIdent(destinationEndpoint.token)) continue;
      const definitions = workspaceCallableDefinitionPaths(calledName, walked, workspace, cache);
      const candidates = definitions.length > 0
        ? definitions
        : workspaceCallableDeclarationPaths(calledName, walked, workspace, cache);
      if (candidates.length !== 1 || candidates[0] === ensuredDestination.surface.path) continue;
      const candidatePath = candidates[0]!;
      ensureWiringEndpointSurface(
        surfaces,
        {
          token: calledName,
          path: candidatePath,
          role: asImpactSurface(classifySurface(candidatePath)),
        },
        workspace,
        cache,
        false,
      );
    }
  }
  const endpointHandles = new Set([
    source?.handle,
    destination?.handle,
  ].filter((handle): handle is string => handle !== undefined));
  const endpointTokens = new Set([
    source?.token,
    destination?.token,
  ].filter((token): token is string => token !== undefined).map(normIdent));
  const destinationCode = relationCodeOnly(
    destination === undefined
      ? ""
      : readCached(workspace, destination.path, cache)
        ?? ensuredDestination?.surface.code
        ?? ensuredDestination?.surface.code_unchanged
        ?? "",
  );
  const supportingHandles = surfaces
    .filter((candidate) =>
      !endpointHandles.has(candidate.handle)
      && candidate.path !== source?.path
      && candidate.path !== destination?.path
    )
    .map((candidate) => {
      const names = [candidate.symbol, ...declaredCallableNames(servedSurfaceText(candidate))]
        .filter((name): name is string => Boolean(name))
        .filter((name) => !endpointTokens.has(normIdent(name)));
      const directlyCalled = names.some((name) => {
        const escaped = name.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
        const call = new RegExp(`\\b${escaped}\\s*\\(([^)]*)\\)`).exec(destinationCode);
        return call !== null && call[1]!.trim().length > 0;
      });
      return { candidate, directlyCalled };
    })
    .filter(({ directlyCalled }) => directlyCalled)
    .sort((left, right) => left.candidate.path.localeCompare(right.candidate.path))
    // Recursive closure can serve a tighter slice of an adapter that is
    // already present. Semantic wiring support is file-based, so keep one
    // stable handle per implementation path instead of duplicating review
    // frontier entries for equivalent ranges.
    .filter(({ candidate }, index, all) =>
      all.findIndex(({ candidate: other }) => other.path === candidate.path) === index
    )
    .map(({ candidate }) => candidate.handle)
    .slice(0, 3);
  const supportingSurfaces = surfaces.filter((surface) =>
    supportingHandles.includes(surface.handle)
  );
  const sourceCalledDirectly = source !== undefined && (() => {
    const escaped = source.token.replace(/[.*+?^$(){}|[\]\\]/g, "\\$&");
    return new RegExp("\\b" + escaped + "\\s*\\(").test(destinationCode);
  })();
  const adapterInsertion = supportingHandles.length > 0
    && hasEditGradeCallArgument(query, destinationCode, supportingSurfaces);
  const hasCallableInsertionSite = endpointsReady
    && destination !== undefined
    && ensuredDestination !== undefined
    && hasCallableBody(ensuredDestination.surface)
    && (sourceCalledDirectly || adapterInsertion);
  if (endpointsReady && !hasCallableInsertionSite) missing.push("insertion");
  const ready = missing.length === 0;
  const evidenceGraph = buildWiringEvidenceGraph(source, destination, surfaces, workspace, cache);
  const evidenceIds = evidenceGraph?.relations.map((relation) => relation.id) ?? [];

  return {
    version: 1,
    status: ready ? "ready" : "needs-followup",
    connections: [{
      id: "w1",
      status: ready ? "ready" : "needs-followup",
      ...(source ? { source } : {}),
      ...(destination ? { destination } : {}),
      ...(hasCallableInsertionSite && destination ? { insertion_handle: destination.handle } : {}),
      supporting_handles: supportingHandles,
      confidence: ready ? 0.9 : 0.55,
      evidence: [
        "query-token",
        "closure-check",
        "project-family",
        ...(hasCallableInsertionSite ? ["callable-insertion-site" as const] : []),
      ],
      ...(evidenceIds.length > 0 ? { evidence_ids: evidenceIds } : {}),
    }],
    missing,
    edit_frontier: hasCallableInsertionSite && destination ? [destination.handle] : [],
    review_frontier: [source?.handle, ...supportingHandles]
      .filter((handle): handle is string => handle !== undefined),
    ...(evidenceGraph !== undefined ? { evidence_graph: evidenceGraph } : {}),
  };
}

function unqualifiedCallable(symbol: string): string {
  return symbol.split("::").at(-1) ?? symbol;
}

function semanticTokenForPath(resolution: SemanticWiringResolution, relPath: string): string {
  const stem = (candidate: string): string => path.basename(candidate).replace(/\.[^.]+$/, "").toLowerCase();
  if (relPath === resolution.producer.path) return unqualifiedCallable(resolution.producer.symbol);
  if (stem(relPath) === stem(resolution.producer.path)) return resolution.producer.symbol.split("::")[0]!;
  if (relPath === resolution.adapter.path || stem(relPath) === stem(resolution.adapter.path)) {
    return unqualifiedCallable(resolution.adapter.symbol);
  }
  if (relPath === resolution.host.path) return resolution.host.callSymbol;
  if (stem(relPath) === stem(resolution.host.path)) return resolution.host.type;
  if (stem(relPath) === stem(resolution.carrier.path)) return resolution.carrier.type;
  if (relPath === resolution.consumer.path) return unqualifiedCallable(resolution.consumer.symbol);
  if (stem(relPath) === stem(resolution.consumer.path)) return resolution.consumer.owner;
  if (relPath === resolution.insertion.path) return unqualifiedCallable(resolution.insertion.symbol);
  return path.basename(relPath).replace(/\.[^.]+$/, "");
}

/** A 1-based inclusive line window inside a construction hub. */
interface HubLineRange {
  start: number;
  end: number;
}

/** Sort + coalesce touching/overlapping windows into ascending disjoint ranges. */
function mergeHubLineRanges(ranges: readonly HubLineRange[]): HubLineRange[] {
  const merged: HubLineRange[] = [];
  for (const range of [...ranges].sort((a, b) => a.start - b.start)) {
    const previous = merged.at(-1);
    if (previous && range.start <= previous.end + 1) {
      previous.end = Math.max(previous.end, range.end);
    } else {
      merged.push({ ...range });
    }
  }
  return merged;
}

/**
 * A2: the bounded LANDING REGION of a construct-receiver hub — one ±radius
 * window per WIRING ANCHOR (the hub lines `connections[0].required_action`'s
 * replacement has to land on: the producer entry, its publish site, and the
 * insertion site), merged, with the radius shrunk until the windows' VERBATIM
 * text fits HUB_EDIT_HUNK_MAX_BYTES.
 *
 * Every input is a signal the pack already resolved (`resolution.insertion.line`
 * / `resolution.host.producerEntryLine` / `producerPublishLine`), so "cheaply
 * locatable" is a property of the resolution, not of a search. Returns undefined
 * — meaning "keep today's behavior" — when no anchor is a real line of this file
 * or even the tightest radius does not fit. Nothing is ever inferred, widened on
 * a guess, or synthesized: the window is only ever real file lines.
 */
function hubEditHunkRanges(
  lines: readonly string[],
  anchorLines: readonly number[],
): HubLineRange[] | undefined {
  const anchors = anchorLines.filter((line, index, all) =>
    Number.isInteger(line) && line >= 1 && line <= lines.length && all.indexOf(line) === index
  );
  if (anchors.length === 0) return undefined;
  for (const radius of HUB_EDIT_HUNK_RADII) {
    const merged = mergeHubLineRanges(anchors.map((anchor) => ({
      start: Math.max(1, anchor - radius),
      end: Math.min(lines.length, anchor + radius),
    })));
    const bytes = merged.reduce(
      (total, range) => total + Buffer.byteLength(lines.slice(range.start - 1, range.end).join("\n"), "utf8"),
      0,
    );
    if (bytes <= HUB_EDIT_HUNK_MAX_BYTES) return merged;
  }
  return undefined;
}

/**
 * A2: does comment elision MUTILATE the landing region? True means the bytes a
 * solver would copy out of the served body are a `/* doc elided L<a>-<b> *\/`
 * placeholder rather than file text — which `edit_file`'s write guard refuses as
 * `elided-content`, forcing the same-handle re-slice measured 3× on T13.
 */
function hubLandingRegionIsElided(
  lines: readonly string[],
  lang: string | undefined,
  hunks: readonly HubLineRange[],
): boolean {
  return hunks.some((range) => {
    const text = lines.slice(range.start - 1, range.end).join("\n");
    return elideDocComments(text, lang, range.start) !== text;
  });
}

/**
 * A2: render ONE served window with its landing-region sub-spans kept verbatim
 * and every other sub-span elided exactly as before.
 *
 * The single renderer both hub branches share, so "what is served verbatim" has
 * one definition. The verbatim span is the HUNK, never the whole window that
 * carries it: a window that merged with an anchored excerpt window still elides
 * its own doc comments outside the landing region.
 *
 * Splitting an elided segment at a hunk boundary is safe by construction —
 * formatCompress leaves an unclosed comment region "exactly as found", so a
 * doc comment straddling the boundary degrades to NOT being elided, never to
 * corrupted text.
 */
function renderHubWindow(
  lines: readonly string[],
  lang: string | undefined,
  window: HubLineRange,
  hunks: readonly HubLineRange[] | undefined,
): string {
  const textOf = (start: number, end: number): string => lines.slice(start - 1, end).join("\n");
  const elided = (start: number, end: number): string =>
    start > end ? "" : elideDocComments(textOf(start, end), lang, start);
  const covering = (hunks ?? [])
    .filter((hunk) => hunk.start <= window.end && hunk.end >= window.start)
    .map((hunk) => ({ start: Math.max(hunk.start, window.start), end: Math.min(hunk.end, window.end) }))
    .sort((left, right) => left.start - right.start);
  if (covering.length === 0) return elided(window.start, window.end);
  const parts: string[] = [];
  let cursor = window.start;
  for (const hunk of covering) {
    if (hunk.start > cursor) parts.push(elided(cursor, hunk.start - 1));
    parts.push(textOf(Math.max(hunk.start, cursor), hunk.end));
    cursor = hunk.end + 1;
  }
  if (cursor <= window.end) parts.push(elided(cursor, window.end));
  return parts.filter((part) => part.length > 0).join("\n");
}

/**
 * A2: re-emit a WHOLE-file construct-receiver hub body with the landing region
 * kept verbatim and every other segment elided exactly as before.
 *
 * The sibling of the excerpt path: a hub small enough for the completeness bar
 * is served whole, but `centeredSliceForCap` still elides its doc comments, so
 * an anchor over the wiring site is the same unusable placeholder. Same rule,
 * same renderer, same cap; one contiguous window instead of anchored ones.
 *
 * Returns undefined when nothing would change or the rebuild costs more than
 * HUB_EDIT_HUNK_MAX_BYTES over the elided body — the caller then keeps today's
 * body untouched.
 */
function wholeHubWithVerbatimLanding(
  text: string,
  relPath: string,
  elidedCode: string,
  hunks: readonly HubLineRange[],
): string | undefined {
  const lines = text.split(/\r?\n/);
  const lang = languageForPath(relPath);
  if (!hubLandingRegionIsElided(lines, lang, hunks)) return undefined;
  const rebuilt = renderHubWindow(lines, lang, { start: 1, end: lines.length }, mergeHubLineRanges(hunks));
  if (rebuilt.length === 0 || rebuilt === elidedCode) return undefined;
  const delta = Buffer.byteLength(rebuilt, "utf8") - Buffer.byteLength(elidedCode, "utf8");
  if (delta > HUB_EDIT_HUNK_MAX_BYTES) return undefined;
  return rebuilt;
}

/**
 * Build a construction-hub excerpt.
 *
 * A2 (v0.12): `edit_hunk_ranges` is present ONLY when the landing-region hunk
 * fired. It is SERVER-INTERNAL — the caller reads it to emit one trace line and
 * never copies it onto a surface, so it costs zero wire bytes. Its absence means
 * the excerpt is byte-for-byte the pre-A2 build.
 *
 * Exported for editHunkFrontier.spec.ts, which pins the A2 trigger and its
 * OFF-parity property (a non-matching hub keeps the pre-A2 body) without having
 * to reconstruct a whole semantic workspace for every cell.
 */
export function semanticConstructionHubExcerpt(
  source: string,
  relPath: string,
  anchorLines: readonly number[],
): { code: string; remaining_ranges: string[]; edit_hunk_ranges?: string[] } | undefined {
  const lines = source.split(/\r?\n/);
  if (lines.length === 0) return undefined;
  // Wave 4 (2026-07-24 T13 forensics): the globals center matched the FIRST
  // /global instances/i line — in real firmware hubs that is a mention inside
  // the file's top doc comment, so the excerpt never contained the actual
  // global-definition section and the model full-read the file natively.
  // Prefer a match that is immediately followed by definition-shaped code
  // (a section banner right above the instances), falling back to the first.
  const globalMatches = lines
    .map((line, index) => (/global instances/i.test(line) ? index : -1))
    .filter((index) => index >= 0);
  const definitionFollows = (index: number): boolean =>
    lines.slice(index + 1, index + 6).some((line) => {
      const trimmed = line.trim();
      return trimmed.length > 0
        && !trimmed.startsWith("//")
        && !trimmed.startsWith("/*")
        && !trimmed.startsWith("*")
        && !trimmed.startsWith("#")
        && /[;={(]/.test(trimmed);
    });
  const globals = globalMatches.find(definitionFollows) ?? globalMatches[0] ?? -1;
  const centers = [
    ...(globals >= 0 ? [globals + 1] : []),
    ...anchorLines,
  ].filter((line, index, all) =>
    Number.isInteger(line) && line >= 1 && line <= lines.length && all.indexOf(line) === index
  );
  if (centers.length === 0) return undefined;

  const lang = languageForPath(relPath);
  // A2: the landing region, and whether comment elision would MUTILATE it. A
  // solver anchors its `search` on the bytes we served; when those bytes carry a
  // `/* doc elided L<a>-<b> */` marker the resulting edit is refused
  // `elided-content` and the only recovery is a same-handle re-slice (measured
  // 3× on T13, 2026-08-26). Serving the landing region VERBATIM removes the
  // refusal at its source instead of paying for the recovery.
  const editHunks = hubEditHunkRanges(lines, anchorLines);
  const hunkElided = editHunks !== undefined && hubLandingRegionIsElided(lines, lang, editHunks);

  const build = (
    radius: number,
    hunks?: readonly HubLineRange[],
  ): { code: string; merged: HubLineRange[] } => {
    const merged = mergeHubLineRanges([
      ...centers.map((center) => ({
        start: Math.max(1, center - radius),
        end: Math.min(lines.length, center + radius),
      })),
      ...(hunks ?? []),
    ]);
    const code = merged.map((range, index) => {
      const prefix = index === 0
        ? "// TL hub excerpt; omitted ranges listed in remaining_ranges (zoom: ONE read_file mode=full of this handle beats repeated re-slices when you need most of the file)\n"
        : `// TL omitted lines before L${range.start}\n`;
      // A2: only the LANDING-REGION sub-span of a window is served verbatim —
      // the rest of that window keeps the elided representation it always had.
      return prefix + renderHubWindow(lines, lang, range, hunks);
    }).join("\n");
    return { code, merged };
  };

  // Budget 6KB (was 12KB): the definition-anchored globals center (above) adds
  // ~40 real lines over the old doc-comment center; without tightening, the
  // hub excerpt pushed the whole pack over the wire cap and the trim ladder
  // either collapsed it to a single surface or dropped review surfaces
  // (ekf.hpp) that carry unique files. With remaining_ranges now disclosed and
  // re-slice sanctioned, a leaner anchored excerpt plus zoom-on-demand beats a
  // fatter excerpt that evicts other evidence. Radius shrinks until it fits.
  // Wave 4 (2026-07-24 T13 forensics): the excerpt used to claim
  // remaining_ranges:[] ("graph-proved anchors, must not reopen"), hiding
  // the truncation — the model's actual edit landed OUTSIDE the excerpt
  // windows and it escaped to a native full-file Read to recover. Disclose
  // the true complement instead; a re-slice of this same handle is the
  // sanctioned (and far cheaper) recovery path.
  const finish = (
    built: { code: string; merged: HubLineRange[] },
    hunks?: readonly HubLineRange[],
  ): { code: string; remaining_ranges: string[]; edit_hunk_ranges?: string[] } => {
    const remaining: string[] = [];
    let cursor = 1;
    for (const range of built.merged) {
      if (range.start > cursor) remaining.push(`${cursor}-${range.start - 1}`);
      cursor = range.end + 1;
    }
    if (cursor <= lines.length) remaining.push(`${cursor}-${lines.length}`);
    return {
      code: built.code,
      remaining_ranges: remaining,
      ...(hunks !== undefined ? { edit_hunk_ranges: hunks.map((hunk) => `${hunk.start}-${hunk.end}`) } : {}),
    };
  };

  for (const radius of [32, 24, 18]) {
    const plain = build(radius);
    const plainBytes = Buffer.byteLength(plain.code, "utf8");
    // The radius ladder is UNCHANGED by A2: a radius whose plain build does not
    // fit is still skipped, so A2 never trades a tighter radius for a hunk.
    if (plainBytes > HUB_EXCERPT_BUDGET_BYTES) continue;
    // A2 TRIGGER (strict). The served evidence fails to cover the landing region
    // when the anchored windows do not CONTAIN it, or when what they contain is
    // not the file's own bytes (comment elision). Neither true ⇒ the pre-A2
    // build is returned unchanged, byte for byte.
    const uncovered = editHunks !== undefined
      && !editHunks.every((hunk) => plain.merged.some((range) => range.start <= hunk.start && range.end >= hunk.end));
    if (editHunks === undefined || (!hunkElided && !uncovered)) return finish(plain);
    const withHunk = build(radius, editHunks);
    const withHunkBytes = Buffer.byteLength(withHunk.code, "utf8");
    if (
      withHunkBytes - plainBytes <= HUB_EDIT_HUNK_MAX_BYTES
      && withHunkBytes <= HUB_EXCERPT_BUDGET_BYTES + HUB_EDIT_HUNK_MAX_BYTES
    ) {
      return finish(withHunk, editHunks);
    }
    // The landing region does not fit its cap here: keep the pre-A2 body rather
    // than widen the pack. The disclosed remaining_ranges + same-handle re-slice
    // stay the recovery path they have always been.
    return finish(plain);
  }
  return undefined;
}

function buildSemanticEvidenceGraph(
  resolution: SemanticWiringResolution,
  source: TaskWiringEndpoint,
  destination: TaskWiringEndpoint,
  insertion: TaskPackSurface,
  adapter: TaskPackSurface,
  host: TaskPackSurface,
  carrier: TaskPackSurface,
): TaskEvidenceGraph {
  const sourceFileId = evidenceNodeId("file", source.path);
  const sourceSymbolId = evidenceNodeId("symbol", source.path, source.token);
  const consumerFileId = evidenceNodeId("file", destination.path);
  const consumerSymbolId = evidenceNodeId("symbol", destination.path, destination.token);
  const insertionFileId = evidenceNodeId("file", insertion.path);
  const insertionSymbol = unqualifiedCallable(resolution.insertion.symbol);
  const insertionSymbolId = evidenceNodeId("symbol", insertion.path, insertionSymbol);
  const producerEntryId = evidenceNodeId("symbol", insertion.path, resolution.host.producerEntry);
  const adapterFileId = evidenceNodeId("file", adapter.path);
  const adapterTargetId = evidenceNodeId("symbol", adapter.path, unqualifiedCallable(resolution.adapter.symbol));
  const hostFileId = evidenceNodeId("file", host.path);
  const hostSymbolId = evidenceNodeId("symbol", host.path, resolution.host.callSymbol);
  const carrierFileId = evidenceNodeId("file", carrier.path);
  const nodes: TaskEvidenceNode[] = [
    { id: sourceFileId, kind: "file", path: source.path, handle: source.handle, range: source.range, roles: ["producer"] },
    { id: sourceSymbolId, kind: "symbol", path: source.path, symbol: source.token, handle: source.handle, range: source.range, roles: ["producer"] },
    { id: consumerFileId, kind: "file", path: destination.path, handle: destination.handle, range: destination.range, roles: ["consumer"] },
    { id: consumerSymbolId, kind: "symbol", path: destination.path, symbol: destination.token, handle: destination.handle, range: destination.range, roles: ["consumer"] },
    { id: insertionFileId, kind: "file", path: insertion.path, handle: insertion.handle, range: insertion.range, roles: ["insertion"] },
    { id: insertionSymbolId, kind: "symbol", path: insertion.path, symbol: insertionSymbol, handle: insertion.handle, range: insertion.range, roles: ["insertion"] },
    { id: producerEntryId, kind: "symbol", path: insertion.path, symbol: resolution.host.producerEntry, handle: insertion.handle, range: insertion.range, roles: ["host"] },
    { id: adapterFileId, kind: "file", path: adapter.path, handle: adapter.handle, range: adapter.range, roles: ["adapter"] },
    { id: adapterTargetId, kind: "symbol", path: adapter.path, symbol: unqualifiedCallable(resolution.adapter.symbol), handle: adapter.handle, range: adapter.range, roles: ["adapter"] },
    { id: hostFileId, kind: "file", path: host.path, handle: host.handle, range: host.range, roles: ["host"] },
    { id: hostSymbolId, kind: "symbol", path: host.path, symbol: resolution.host.callSymbol, handle: host.handle, range: host.range, roles: ["host"] },
    { id: carrierFileId, kind: "file", path: carrier.path, handle: carrier.handle, range: carrier.range, roles: ["carrier"] },
  ];
  const relations: TaskEvidenceRelation[] = [];
  const add = (kind: TaskEvidenceRelationKind, from: string, to: string, confidence: number): void => {
    const id = evidenceRelationId(kind, from, to);
    if (!relations.some((relation) => relation.id === id)) {
      relations.push({ id, kind, from, to, provenance: "lexical", confidence });
    }
  };
  add("defines", sourceFileId, sourceSymbolId, 0.98);
  add("defines", consumerFileId, consumerSymbolId, 0.94);
  add("defines", insertionFileId, insertionSymbolId, 0.96);
  add("defines", insertionFileId, producerEntryId, 0.96);
  add("defines", adapterFileId, adapterTargetId, 0.98);
  add("defines", hostFileId, hostSymbolId, 0.94);
  add("imports", consumerFileId, adapterFileId, 0.98);
  if (resolution.consumer.adapterCallMode === "existing") {
    add("direct_calls", consumerSymbolId, adapterTargetId, 0.98);
  }
  add("direct_calls", insertionSymbolId, consumerSymbolId, 0.98);
  add("direct_calls", producerEntryId, hostSymbolId, 0.96);
  add("references", insertionSymbolId, carrierFileId, 0.9);
  add("references", consumerFileId, carrierFileId, 0.88);
  return { version: 1, nodes, relations };
}

/** Cap on compile_targets carried by the minimal harness-entry recipe (pack-time bytes). */
const MAX_HARNESS_ENTRY_TARGETS = 4;

/**
 * The minimal `completion_proof.verification` a construct-receiver wiring pack
 * can honestly carry when full recipe enrichment is off: a runnable entry the
 * manifest already proved (a test's own Build/Compile marker, else a workspace
 * test entry) plus the targets it has to compile. Never a build-system FILE
 * path — required_action asks clients to EXECUTE this, so a non-command entry
 * would be a lie. Stays undefined unless a referencing test made the strategy
 * "harness".
 */
function harnessEntryRecipe(
  manifest: VerificationManifest | undefined,
  editPaths: readonly string[],
): TaskVerificationRecipe | undefined {
  if (manifest === undefined || manifest.verify_strategy !== "harness") return undefined;
  // A SYNTHESIZED harness command carries <your_harness.*> placeholders — it
  // is authoring guidance, not an executable entry; advertising it here would
  // ask clients to EXECUTE a template (and its `source` is not a real path).
  const buildCommand = manifest.harness?.build_command_synthesized === true
    ? undefined
    : manifest.harness?.build_command;
  const entry = buildCommand?.text ?? manifest.toolchain?.test_entry;
  if (entry === undefined) return undefined;
  const compileTargets = [...new Set([
    ...editPaths,
    ...manifest.surfaces.filter((surface) => surface.role === "test").map((surface) => surface.path),
  ])].slice(0, MAX_HARNESS_ENTRY_TARGETS);
  // Same disclosure the full recipe makes: a mined command written for ITS own
  // test usually does not compile the files this task edits, so "it ran green"
  // is not proof of the wiring. Only checkable against a real command's text.
  const incomplete = buildCommand !== undefined
    && editPaths.some((rel) => !buildCommand.text.includes(path.posix.basename(rel)));
  return {
    compile_targets: compileTargets,
    ...(buildCommand !== undefined ? { cwd: path.posix.dirname(buildCommand.source) } : {}),
    entry,
    ...(incomplete ? { gaps: ["executable entry does not compile every edited implementation target"] } : {}),
  };
}

function buildSemanticTaskWiringProfile(
  query: string,
  surfaces: TaskPackSurface[],
  workspace: string,
  cache: FileReadCache | undefined,
): TaskWiringProfile | undefined {
  const walked = walkCodeFiles(workspace, {}).map((file) => file.relPath);
  const scope = roleFillScope(surfaces) ?? ".";
  const resolution = resolveSemanticWiring(workspace, query, walked, scope || ".");
  if (resolution === undefined) return undefined;

  // Locator slices are optimized for the first lexical anchor and can point at
  // a declaration or an unrelated method in a multi-callable implementation.
  // Rebuild only the bounded semantic frontier so every role is sliced around
  // the implementation symbol that actually proves it.
  // A complete semantic graph supersedes the locator's provisional surfaces.
  // Keeping unrelated candidates (especially artifacts) consumes the bounded
  // surface budget and can turn a proven graph back into awaiting-input.
  surfaces.splice(0, surfaces.length);

  const ensure = (
    relPath: string,
    token: string,
    required: boolean,
    maxSurfaceBytes = 8192,
    completeUnderBytes = TINY_FILE_MAX_BYTES,
  ): TaskPackSurface | undefined => {
    const served = ensureWiringEndpointSurface(
      surfaces,
      { token, path: relPath, role: asImpactSurface(classifySurface(relPath)) },
      workspace,
      cache,
      required,
      MAX_SEMANTIC_WIRING_SURFACES,
      maxSurfaceBytes,
      completeUnderBytes,
      completeUnderBytes > TINY_FILE_MAX_BYTES ? WIRING_COMPLETE_SURFACE_LINES : TINY_FILE_MAX_LINES,
    )?.surface;
    if (served !== undefined) {
      served.required = required;
      served.why = required
        ? `semantic wiring edit frontier for ${token}`
        : `semantic wiring review evidence for ${token}`;
    }
    return served;
  };

  const isConstructReceiver = resolution.connectionMode === "construct-receiver";
  // B2a (2026-08-01 serving-completeness): a construct-receiver pack asks the
  // solver to CREATE the receiver — its three biggest REQUIRED surfaces were
  // exactly the ones it served as teasers. Live (2026-07-31-semantic-signal5-2,
  // T13): the wiring hub arrived as 4.7KB of 17.7KB, the produced class's header
  // as a 122-byte stub and the second edit site as 61 lines; all three solver
  // reps' FIRST post-pack act was `mode=full` of the hub, and two of three then
  // ran a 10-call / 72-77KB full-read cascade. Completeness is bought with a
  // bounded body budget — spending it here is strictly cheaper than the cascade.
  const completeBodyBudget = { left: isConstructReceiver ? CONSTRUCT_COMPLETE_EXTRA_BUDGET : 0 };
  // `reserveOnly` runs the budget draw WITHOUT returning a bar, so the hub can
  // claim its share before the frontier is walked in its own (carrier-first)
  // order; the second call for the same path then sees the reservation.
  const reserved = new Set<string>();
  const completeBarFor = (relPath: string, priority: boolean, reserveOnly = false): number => {
    if (!isConstructReceiver) return TINY_FILE_MAX_BYTES;
    const text = readCached(workspace, relPath, cache);
    if (text === undefined) return TINY_FILE_MAX_BYTES;
    const bytes = Buffer.byteLength(text, "utf8");
    if (bytes > WIRING_COMPLETE_SURFACE_BYTES) return TINY_FILE_MAX_BYTES;
    // A file the default tiny-file rule already serves whole is not an upgrade
    // — charging the budget for it would starve the files that ARE.
    if (bytes <= TINY_FILE_MAX_BYTES && Math.max(1, countLines(text)) <= TINY_FILE_MAX_LINES) {
      return TINY_FILE_MAX_BYTES;
    }
    if (reserved.has(relPath)) return reserveOnly ? TINY_FILE_MAX_BYTES : WIRING_COMPLETE_SURFACE_BYTES;
    // The produced class's declaration is the one never-refused draw: it is
    // small by nature (a header) and was arriving as a ~122-byte stub. Every
    // other surface — the construction hub included — draws on the bounded
    // extra budget, which is what keeps the pack under its transport ceiling.
    if (priority) {
      reserved.add(relPath);
      return WIRING_COMPLETE_SURFACE_BYTES;
    }
    if (bytes > completeBodyBudget.left) return TINY_FILE_MAX_BYTES;
    completeBodyBudget.left -= bytes;
    reserved.add(relPath);
    return reserveOnly ? TINY_FILE_MAX_BYTES : WIRING_COMPLETE_SURFACE_BYTES;
  };
  const reviewEvidenceBytes = isConstructReceiver ? 4096 : 8192;
  const sourceSurface = ensure(
    resolution.producer.path,
    unqualifiedCallable(resolution.producer.symbol),
    false,
    reviewEvidenceBytes,
  );
  const adapterSurface = ensure(
    resolution.adapter.path,
    unqualifiedCallable(resolution.adapter.symbol),
    false,
    reviewEvidenceBytes,
  );
  // B2a: the construction hub draws FIRST from the bounded extra budget (it is
  // the construction site and the measured full-read trigger), then every other
  // edit-frontier surface in frontier order.
  const editOrder = [...resolution.editPaths].sort((left, right) =>
    (left === resolution.insertion.path ? 0 : 1) - (right === resolution.insertion.path ? 0 : 1)
  );
  for (const relPath of editOrder) completeBarFor(relPath, false, true);
  const editSurfaces = resolution.editPaths.map((relPath) => {
    const bar = completeBarFor(relPath, false);
    return ensure(
      relPath,
      semanticTokenForPath(resolution, relPath),
      true,
      bar > TINY_FILE_MAX_BYTES ? bar : MAX_SURFACE_CODE_BYTES,
      bar,
    );
  });
  // Review-only declarations still need a concrete body: a handle-only entry
  // makes a prepared pack claim more evidence than it actually delivered and
  // forces the agent to reopen discovery. Keep these slices deliberately
  // small so the complete semantic graph remains below the transport ceiling
  // without trimToCap stripping the bodies again — EXCEPT (B2a) the produced
  // class's own declaration, which a construct-receiver task must read in full
  // to construct and drive an instance. That one was the 122-byte stub.
  const reviewSurfaces = resolution.reviewPaths.map((relPath) =>
    ensure(relPath, semanticTokenForPath(resolution, relPath), false, 128)
  );
  if (
    sourceSurface === undefined
    || adapterSurface === undefined
    || editSurfaces.some((surface) => surface === undefined)
    || reviewSurfaces.some((surface) => surface === undefined)
  ) {
    return undefined;
  }
  sourceSurface.required = true;
  adapterSurface.required = true;
  const consumerSurface = surfaces.find((surface) => surface.path === resolution.consumer.path);
  const insertionSurface = surfaces.find((surface) => surface.path === resolution.insertion.path);
  const hostSurface = surfaces.find((surface) => surface.path === resolution.host.path);
  const carrierSurface = surfaces.find((surface) => surface.path === resolution.carrier.path);
  if (consumerSurface === undefined || insertionSurface === undefined || hostSurface === undefined || carrierSurface === undefined) {
    return undefined;
  }
  const insertionText = readCached(workspace, insertionSurface.path, cache);
  if (insertionText === undefined) return undefined;
  const insertionLines = [
    resolution.host.producerEntryLine,
    ...(resolution.host.producerPublishLine !== undefined ? [resolution.host.producerPublishLine] : []),
    resolution.insertion.line,
  ];
  const insertionLineCount = Math.max(1, countLines(insertionText));
  const insertionRange = resolution.connectionMode === "construct-receiver"
    ? `1-${insertionLineCount}`
    : `${Math.max(1, Math.min(...insertionLines) - 20)}-${Math.min(insertionLineCount, Math.max(...insertionLines) + 40)}`;
  // B2a: a construct-receiver hub that fits the completeness bar is served
  // WHOLE. The anchored excerpt (6KB of a 17.7KB hub, with honest
  // remaining_ranges) was still the single strongest trigger for a native
  // full-read: all three T13 reps opened with `mode=full` of this exact file.
  // Only a hub too big for the bar keeps the excerpt.
  const insertionBytes = Buffer.byteLength(insertionText, "utf8");
  const insertionWhole = isConstructReceiver
    && insertionBytes <= WIRING_COMPLETE_SURFACE_BYTES
    && (insertionBytes <= TINY_FILE_MAX_BYTES || reserved.has(resolution.insertion.path))
    ? centeredSliceForCap(
        workspace,
        insertionSurface.path,
        `1-${insertionLineCount}`,
        cache,
        undefined,
        WIRING_COMPLETE_SURFACE_BYTES,
      )
    : undefined;
  const insertionCode: { code: string; remaining_ranges: string[]; edit_hunk_ranges?: string[] } | undefined
    = insertionWhole !== undefined && insertionWhole.remaining_ranges.length === 0
    ? insertionWhole
    : resolution.connectionMode === "construct-receiver"
    ? semanticConstructionHubExcerpt(
        insertionText,
        insertionSurface.path,
        insertionLines,
      )
    : centeredSliceForCap(
        workspace,
        insertionSurface.path,
        insertionRange,
        cache,
        Math.round((Math.min(...insertionLines) + Math.max(...insertionLines)) / 2),
        8192,
      );
  // Wave 4: a construct-receiver hub excerpt with disclosed remaining_ranges
  // is served (content_completeness:"partial" below), not vetoed — the old
  // bail was dead code while the excerpt hard-claimed remaining_ranges:[] and
  // would otherwise discard the entire wiring pack over honest disclosure.
  if (insertionCode === undefined) return undefined;
  // A2 (v0.12): the whole-file sibling of the excerpt path. A hub inside the
  // completeness bar is served entire, but still comment-elided — so the exact
  // anchor over the wiring site can be the same `elided-content` placeholder.
  // Re-emit that one bounded landing window verbatim; everything outside it,
  // and every non-construct-receiver hub, is untouched.
  if (
    isConstructReceiver
    && insertionWhole !== undefined
    && insertionCode === insertionWhole
  ) {
    const wholeHunks = hubEditHunkRanges(insertionText.split(/\r?\n/), insertionLines);
    const rebuilt = wholeHunks === undefined
      ? undefined
      : wholeHubWithVerbatimLanding(insertionText, insertionSurface.path, insertionCode.code, wholeHunks);
    if (rebuilt !== undefined && wholeHunks !== undefined) {
      insertionCode.code = rebuilt;
      insertionCode.edit_hunk_ranges = wholeHunks.map((hunk) => `${hunk.start}-${hunk.end}`);
    }
  }
  insertionSurface.range = insertionRange;
  insertionSurface.handle = handleTable.upsert({
    kind: "range",
    path: insertionSurface.path,
    range: insertionRange,
    workspaceRoot: workspace,
  }).id;
  insertionSurface.code = insertionCode.code;
  insertionSurface.symbol = unqualifiedCallable(resolution.insertion.symbol);
  if (resolution.connectionMode === "construct-receiver") {
    insertionSurface.why = "semantic wiring receiver-construction hub; obey wiring.connections[0].required_action";
  }
  // A2 live engagement counter (trace-only, zero wire bytes). One line per FIRE,
  // so a probe can preregister `edit_hunk_frontier` occurrences per cell as the
  // discriminating signature instead of inferring engagement from cost.
  if (insertionCode.edit_hunk_ranges !== undefined && insertionCode.edit_hunk_ranges.length > 0) {
    trace(
      "edit_hunk_frontier",
      {
        path: insertionSurface.path,
        mode: resolution.connectionMode,
        hunks: insertionCode.edit_hunk_ranges.length,
        hunk_ranges: insertionCode.edit_hunk_ranges,
        anchor_lines: insertionLines,
        hub_bytes: insertionBytes,
        served_bytes: Buffer.byteLength(insertionCode.code, "utf8"),
      },
      workspace,
    );
  }
  if (insertionCode.remaining_ranges.length > 0) {
    if (resolution.connectionMode === "construct-receiver") {
      // 2026-07-25 honest-hub fix (supersedes the Wave-4 label suppression):
      // the excerpt IS partial content and now says so. What protects the
      // receiver-construction certificate is anchors_served — the evidence
      // graph proves every anchor line sits inside the served ranges, so the
      // readiness risk model exempts this surface from the
      // partial-surface-content factor instead of the label lying (which
      // trained solvers to distrust coverage and re-probe natively).
      insertionSurface.content_completeness = "partial";
      insertionSurface.anchors_served = true;
      insertionSurface.remaining_ranges = insertionCode.remaining_ranges;
    } else {
      insertionSurface.content_completeness = "partial";
      insertionSurface.remaining_ranges = insertionCode.remaining_ranges;
    }
  } else {
    delete insertionSurface.content_completeness;
    delete insertionSurface.remaining_ranges;
  }
  const source = endpointFromSurface(
    unqualifiedCallable(resolution.producer.symbol),
    sourceSurface,
    ["query-token", "workspace-definition", "project-family"],
  );
  const destination = endpointFromSurface(
    unqualifiedCallable(resolution.consumer.symbol),
    consumerSurface,
    ["query-token", "workspace-definition", "project-family"],
  );
  const graph = buildSemanticEvidenceGraph(
    resolution,
    source,
    destination,
    insertionSurface,
    adapterSurface,
    hostSurface,
    carrierSurface,
  );
  const editFrontier = resolution.editPaths
    .map((relPath) => surfaces.find((surface) => surface.path === relPath)?.handle)
    .filter((handle): handle is string => handle !== undefined);
  const reviewFrontier = reviewSurfaces
    .filter((surface): surface is TaskPackSurface => surface !== undefined)
    .map((surface) => surface.handle);
  // R5 (2026-08-01, wont-fix): the K2 relevance gate's precise-diff
  // `changedIdentifiers` threading (server.ts buildVerificationBody) is
  // deliberately NOT wired here — no edit args exist at pack time, and this
  // call already anchors the kit on the producer/adapter symbols via
  // behaviorAnchors, which is the same signal with better provenance. The
  // edited-file-content proxy inside collectChangedIdentifiers covers the
  // rest; threading a second identifier list would add plumbing, not
  // precision.
  // A semantic wiring frontier is actionable only when every edit site has
  // been resolved, regardless of whether its receiver already exists or must
  // be constructed. This compact, server-internal completion contract needs
  // recipe discovery even when the experimental response-wide recipe flag is
  // off; only a recipe with a workspace-proven entry reaches the wire below.
  const completionManifest = buildVerificationManifest(workspace, resolution.editPaths, {
    forceRecipe: true,
    behaviorAnchors: [
      unqualifiedCallable(resolution.producer.symbol),
      unqualifiedCallable(resolution.adapter.symbol),
    ],
  });
  const completionRecipeRaw = completionManifest?.recipe;
  const completionRecipe = completionRecipeRaw?.entry === undefined
    ? undefined
    : {
      ...completionRecipeRaw,
      compile_targets: completionRecipeRaw.compile_targets
        .filter((target) =>
          resolution.editPaths.includes(target)
          || /(^|\/)(?:test|tests|__tests__)(\/|$)/.test(target)
        )
        .slice(0, 8),
      ...(completionRecipeRaw.assertion_refs !== undefined
        ? { assertion_refs: completionRecipeRaw.assertion_refs.slice(0, 2) }
        : {}),
      ...(completionRecipeRaw.contract_refs !== undefined
        ? { contract_refs: completionRecipeRaw.contract_refs.slice(0, 2) }
        : {}),
      ...(completionRecipeRaw.mock_controls !== undefined
        ? {
          mock_controls: completionRecipeRaw.mock_controls
            .filter((ref) => !/#.+_(?:H|HPP)$/.test(ref))
            .slice(0, 3),
        }
        : {}),
    };
  // required_action tells clients to execute completion_proof.verification.entry;
  // only a manifest with a workspace-proven runnable entry can satisfy that
  // demand, so a recipe containing only gaps never reaches this contract.
  const completionVerification = completionRecipe ?? harnessEntryRecipe(completionManifest, resolution.editPaths);
  const structuralChecks = resolution.structuralChecks?.map((check) => ({
    id: check.id,
    description: check.description,
    handle: surfaces.find((surface) => surface.path === check.path)!.handle,
    tokens: check.tokens,
  }));
  return {
    version: 1,
    strategy: "semantic-multihop",
    status: "ready",
    connections: [{
      id: "w1",
      status: "ready",
      mode: resolution.connectionMode,
      ...(resolution.requiredAction !== undefined ? { required_action: resolution.requiredAction } : {}),
      ...(resolution.receiverSearch !== undefined ? {
        receiver_search: {
          scope: resolution.receiverSearch.scope,
          files_scanned: resolution.receiverSearch.filesScanned,
          producer_type: resolution.receiverSearch.producerType,
          scope_complete: resolution.receiverSearch.scopeComplete,
          existing_receiver_found: resolution.receiverSearch.existingReceiverFound,
        },
      } : {}),
      ...(resolution.lifecycleSymbols !== undefined ? { lifecycle_symbols: resolution.lifecycleSymbols } : {}),
      ...(resolution.consumer.adapterCallMode === "construct" ? {
        consumer_call: {
          mode: "construct" as const,
          adapter_symbol: resolution.adapter.symbol,
          insertion_anchor: resolution.consumer.symbol,
          symbol: `${resolution.consumer.owner}::${resolution.consumer.constructionSymbol!}`,
        },
      } : {}),
      // Existing-receiver routes need not add receiver-construction checks,
      // but an independently discovered executable verification recipe is
      // still an actionable completion contract. Keep the required structural
      // checks carrier explicit (empty when none applies).
      ...(structuralChecks !== undefined || completionVerification !== undefined ? {
        completion_proof: {
          structural_checks: structuralChecks ?? [],
          ...(completionVerification !== undefined ? { verification: completionVerification } : {}),
        },
      } : {}),
      source,
      destination,
      insertion_handle: insertionSurface.handle,
      supporting_handles: reviewFrontier.filter((handle) => handle !== source.handle),
      confidence: 0.92,
      evidence: [
        "query-token",
        "workspace-definition",
        "project-family",
        "closure-check",
        "callable-insertion-site",
        ...(resolution.connectionMode === "construct-receiver" ? ["receiver-construction-site" as const] : []),
      ],
      evidence_ids: graph.relations.map((relation) => relation.id),
    }],
    missing: [],
    edit_frontier: editFrontier,
    review_frontier: reviewFrontier,
    evidence_graph: graph,
  };
}

function buildTaskWiringProfile(
  query: string,
  surfaces: TaskPackSurface[],
  workspace: string,
  cache?: FileReadCache,
): TaskWiringProfile {
  // D10 (2026-08-14): the semantic resolver always runs first — `TL_SEMANTIC_WIRING`
  // is deleted. The legacy builder remains the fail-closed fallback for the case
  // where the resolver cannot certify a connection, not a flag-selected arm.
  const semantic = buildSemanticTaskWiringProfile(query, surfaces, workspace, cache);
  if (semantic !== undefined) return semantic;
  return buildLegacyTaskWiringProfile(query, surfaces, workspace, cache);
}

function semanticCompletionCheckRecords(
  wiring: TaskWiringProfile | undefined,
  surfaces: readonly TaskPackResultSurface[],
): PackCheckRecord[] {
  const checks = wiring?.strategy === "semantic-multihop"
    ? wiring.connections[0]?.completion_proof?.structural_checks ?? []
    : [];
  return checks.map((check) => {
    const path = surfaces.find((surface) => surface.handle === check.handle)?.path;
    const draft: Omit<PackCheckRecord, "id"> = {
      desc: check.description,
      ...(path !== undefined ? { glob: path } : {}),
      allTokens: check.tokens,
      role: "wiring",
    };
    return { id: deriveCheckId(draft), ...draft };
  });
}

function pruneIrrelevantWiringImplementationSurfaces(result: TaskPackResult): void {
  if (result.wiring?.status !== "ready") return;
  const frontier = new Set([
    ...result.wiring.edit_frontier,
    ...result.wiring.review_frontier,
  ]);
  result.surfaces = result.surfaces.filter((surface) =>
    isArtifactTaskPackSurface(surface)
    || frontier.has(surface.handle)
    || !isImplementationPath(surface.path)
  );
}

function obligationKindForRole(
  role: string,
  sourceHandle: string | undefined,
  destinationHandle: string | undefined,
  handle: string,
): TaskChangeObligationKind {
  if (handle === sourceHandle) return "source";
  if (handle === destinationHandle) return "integration";
  switch (role) {
    case "contract": return "definition";
    case "api": return "integration";
    case "domain": return "implementation";
    case "data": return "persistence";
    case "ui": return "presentation";
    case "style": return "style";
    case "config": return "configuration";
    case "doc": return "documentation";
    case "test": return "verification";
    default: return "implementation";
  }
}

function obligationStage(kind: TaskChangeObligationKind): number {
  switch (kind) {
    case "definition":
    case "configuration":
    case "source":
      return 0;
    case "implementation":
    case "persistence":
      return 1;
    case "integration":
      return 2;
    case "presentation":
      return 3;
    case "style":
      return 4;
    case "documentation":
    case "verification":
      return 5;
  }
}

function taskChangeContractReason(
  surface: TaskPackSurface,
  kind: TaskChangeObligationKind,
  action: "edit" | "review",
): string {
  if (kind === "source") return "review the producer contract and emitted value before wiring its consumer";
  if (action === "review") return surface.why ?? "review this related surface; edit only if its behavior must change";
  return surface.edit_intent
    ?? surface.why
    ?? "update the " + surface.role + " surface and preserve its existing contract";
}

function shouldBuildTaskChangeContract(
  query: string,
  result: TaskPackResult,
  profile: TaskProfile,
): boolean {
  if (profile === "answer" || result.route?.action !== "edit_from_handles") return false;
  // A wiring profile still hunting an endpoint is the authority for that
  // work — a parallel change_contract would double-report the same missing
  // endpoint (taskContracts.spec pins its absence).
  if (result.wiring !== undefined && result.wiring.missing.length > 0) return false;
  const editIntent = hasRequestedMutationIntent(query)
    || profile !== "generic";
  if (!editIntent) return false;
  const surfaces = codeTaskPackSurfaces(result.surfaces);
  if (surfaces.length === 0) return false;
  return profile !== "generic"
    || isEnumLikeQueryShaped(query)
    || (result.required_surfaces?.length ?? 0) > 1
    || surfaces.length > 1;
}

/**
 * Issue #4 (2026-08-21): a "filename-match" surface whose only reason for
 * being in the pack is a query-identifier FRAGMENT (a compound identifier
 * merely containing this file's basename word as a substring — e.g. query
 * "buildInitializeInstructions" against packages/.../serverInstructions.ts)
 * or a lone generic-noun coincidence (query "issue" against a file merely
 * named "cumulativeReissueReceipt") is not real evidence once a DIFFERENT
 * surface in the SAME pack already carries the exact, workspace-unique
 * identifier the query actually names.
 *
 * Re-derives that admission with matchBasenameTokens' `strict` mode (see
 * its own history comment) rather than re-implementing the rule: this is a
 * POST-HOC surface filter, not a change to the locator's live candidate
 * pipeline. matchBasenameTokens' `resolvedSymbolFiles` fragment-suppression
 * was tried as a live Layer 5a input first and reverted — it fixed this bug
 * correctly in isolation, but silently starved an UNRELATED locator
 * mechanism (locateTaskContext's two-domain-wiring coverage/margin gate) of
 * matched-token score mass it happened to depend on. Filtering the FINAL
 * surface list here instead touches only change_contract's own obligation
 * formation — nothing upstream of it changes, so no other locate-level
 * heuristic can be starved of anything it was reading.
 *
 * Deliberately narrow: only ever REMOVES a surface whose `why` is exactly
 * "filename-match" (never a symbol/structural/anchor-focus/exact-text hit),
 * and only when the pack carries at least one workspace-unique resolved
 * identifier surface to check fragments against — a pack with no such
 * anchor is a no-op (nothing to fragment FROM), and every removal is
 * re-provable on demand via the same exported matchBasenameTokens a unit
 * test can call directly.
 */
function suppressFragmentOnlyFilenameMatches(
  workspace: string,
  query: string,
  surfaces: TaskPackResultSurface[],
): void {
  const filenameMatchSurfaces = codeTaskPackSurfaces(surfaces).filter((s) => s.why === "filename-match");
  if (filenameMatchSurfaces.length === 0) return;

  const pathTokens = extractIdentifiers(query).map((t) => t.toLowerCase());
  if (pathTokens.length === 0) return;

  // Anchors: a surface whose symbol IS an explicit query identifier — an
  // anchor-focus resolution (server.ts @ buildInitializeInstructions) or a
  // plain exact-symbol/query-symbol surface. Workspace-UNIQUE only: a
  // symbol name that resolves to >= 2 different files in this pack is
  // itself ambiguous and not a trustworthy anchor to fragment-suppress
  // siblings against.
  const resolvedSymbolFiles = new Map<string, Set<string>>();
  for (const s of codeTaskPackSurfaces(surfaces)) {
    if (!s.symbol) continue;
    const isAnchorWhy = s.why === "exact-symbol"
      || s.why === "query-symbol"
      || (typeof s.why === "string" && s.why.startsWith("anchor-focus: explicit query identifier "));
    if (!isAnchorWhy) continue;
    const key = s.symbol.toLowerCase();
    if (!pathTokens.includes(key)) continue; // only identifiers the query itself names
    let set = resolvedSymbolFiles.get(key);
    if (!set) { set = new Set(); resolvedSymbolFiles.set(key, set); }
    set.add(s.path);
  }
  for (const [key, files] of resolvedSymbolFiles) {
    if (files.size !== 1) resolvedSymbolFiles.delete(key); // not workspace-unique
  }
  if (resolvedSymbolFiles.size === 0) return;

  const frequency = buildBasenameFrequency(walkCodeFiles(workspace, {}));
  const toRemove = new Set<TaskPackResultSurface>();
  for (const s of filenameMatchSurfaces) {
    if (matchBasenameTokens(s.path, pathTokens, frequency, resolvedSymbolFiles) === null) {
      toRemove.add(s);
    }
  }
  if (toRemove.size === 0) return;
  const kept = surfaces.filter((s) => !toRemove.has(s));
  surfaces.length = 0;
  surfaces.push(...kept);
}

function buildTaskChangeContract(
  query: string,
  result: TaskPackResult,
  workspace: string,
): TaskChangeContract {
  suppressFragmentOnlyFilenameMatches(workspace, query, result.surfaces as TaskPackResultSurface[]);
  const allSurfaces = codeTaskPackSurfaces(result.surfaces);
  const wiringFrontier = result.wiring?.status === "ready"
    ? new Set([...result.wiring.edit_frontier, ...result.wiring.review_frontier])
    : undefined;
  const surfaces = wiringFrontier === undefined
    ? allSurfaces
    : allSurfaces.filter((surface) => wiringFrontier.has(surface.handle));
  const primaryHandle = surfaces[0]?.handle;
  const concernHandles = new Set((result.concerns ?? []).flatMap((concern) => concern.handles));
  const requiredRoles = new Set(result.required_surfaces ?? []);
  const connection = result.wiring?.connections[0];
  const sourceHandle = connection?.source?.handle;
  const destinationHandle = connection?.destination?.handle;
  const adapterHandles = new Set([
    ...(connection?.supporting_handles ?? []),
    ...(result.wiring?.evidence_graph?.nodes
      .filter((node) => node.roles.includes("adapter"))
      .map((node) => node.handle)
      .filter((handle): handle is string => handle !== undefined) ?? []),
  ]);
  const normalizedQuery = normIdent(query);

  const obligations: TaskChangeObligation[] = surfaces.map((surface, index) => {
    const basename = path.basename(surface.path).replace(/\.[^.]+$/, "");
    const namedToken = surface.symbol ?? basename;
    const explicitlyNamed = namedToken.length >= 3 && normalizedQuery.includes(normIdent(namedToken));
    const wiringSource = surface.handle === sourceHandle;
    const wiringDestination = surface.handle === destinationHandle;
    const editRequired = connection !== undefined
      ? wiringDestination
      : concernHandles.has(surface.handle)
        || surface.required === true
        || requiredRoles.has(surface.role)
        // Issue #4 (2026-08-21): a "doc" surface (CHANGELOG/README/contract
        // prose) must not inherit required-edit status purely from landing
        // at array position 0 — that is exactly what happens when an
        // EARLIER, stronger candidate is removed (e.g. by
        // suppressFragmentOnlyFilenameMatches above) and a documentation
        // surface that merely discusses the same topic is promoted into the
        // "primary" slot by accident. A doc surface still becomes required
        // through any of the OTHER, EXPLICIT signals above/below
        // (concernHandles, surface.required, requiredRoles, or the query
        // literally naming it) — this only removes the position-only
        // fallback for that one role.
        || (surface.handle === primaryHandle && !wiringSource && surface.role !== "doc")
        || explicitlyNamed;
    const action: "edit" | "review" = wiringSource ? "review" : editRequired ? "edit" : "review";
    const required = editRequired || wiringSource || (connection !== undefined && surface.required === true);
    // Wiring graph roles are semantic and survive project/file/symbol
    // renames; generic surface roles are filename heuristics. An adapter in
    // the proved graph therefore remains an integration review obligation
    // even when its renamed path classifies as unknown/domain.
    const kind = adapterHandles.has(surface.handle)
      ? "integration"
      : obligationKindForRole(surface.role, sourceHandle, destinationHandle, surface.handle);
    const confidence = surface.likely_edits
      ?.map((hint) => hint.confidence ?? 0)
      .reduce((best, value) => Math.max(best, value), 0);
    return {
      id: "o" + (index + 1),
      kind,
      action,
      status: hasSufficientEditContent(surface) ? "ready" : "needs-context",
      required,
      handle: surface.handle,
      path: surface.path,
      range: surface.range,
      role: asImpactSurface(surface.role),
      reason: taskChangeContractReason(surface, kind, action).slice(0, 180),
      confidence: Math.round(((confidence && confidence > 0) ? confidence : required ? 0.85 : 0.65) * 100) / 100,
      depends_on: [],
    };
  });

  const definitions = obligations.filter((item) => item.kind === "definition").map((item) => item.id);
  const integrations = obligations
    .filter((item) => ["implementation", "integration", "persistence"].includes(item.kind))
    .map((item) => item.id);
  const presentations = obligations.filter((item) => item.kind === "presentation").map((item) => item.id);
  const editIds = obligations.filter((item) => item.action === "edit").map((item) => item.id);
  for (const obligation of obligations) {
    if (obligation.kind === "integration" && sourceHandle !== undefined) {
      const source = obligations.find((item) => item.handle === sourceHandle);
      obligation.depends_on = source ? [source.id] : [];
    } else if (["implementation", "integration", "persistence"].includes(obligation.kind)) {
      obligation.depends_on = definitions.filter((id) => id !== obligation.id);
    } else if (obligation.kind === "presentation") {
      obligation.depends_on = integrations.length > 0 ? integrations : definitions;
    } else if (obligation.kind === "style") {
      obligation.depends_on = presentations.length > 0 ? presentations : integrations;
    } else if (obligation.kind === "documentation" || obligation.kind === "verification") {
      obligation.depends_on = editIds.filter((id) => id !== obligation.id);
    }
  }

  const stageNumbers = [...new Set(
    obligations.filter((item) => item.action === "edit").map((item) => obligationStage(item.kind)),
  )].sort((a, b) => a - b);
  const stages = stageNumbers.map((stage) => obligations
    .filter((item) => item.action === "edit" && obligationStage(item.kind) === stage)
    .map((item) => item.id));
  const missing = [
    ...result.missing,
    ...(result.wiring?.missing ?? []).map((item) => "wiring:" + item),
    ...obligations
      .filter((item) => item.required && item.status === "needs-context")
      .map((item) => "context:" + item.id),
  ];
  if (result.route && result.route.action !== "edit_from_handles" && missing.length === 0) {
    missing.push("route:" + result.route.action);
  }
  const uniqueMissing = [...new Set(missing)];
  const ready = uniqueMissing.length === 0
    && result.route?.action === "edit_from_handles"
    && obligations.some((item) => item.action === "edit");
  return {
    version: 1,
    status: ready ? "ready" : "needs-followup",
    discovery_complete: ready,
    obligations,
    stages,
    missing: uniqueMissing,
    max_additional_tl_calls: ready
      ? 0
      : Math.max(1, result.route?.max_additional_tl_calls ?? 1),
  };
}
const READINESS_FALSE_READY_LIMIT = 0.05;
const READINESS_NORMALIZED_TURN_COST = 0.18;
const READINESS_PROBE_MAX_FILES = 120;
const READINESS_PROBE_MAX_BYTES = 512 * 1024;
const READINESS_PROBE_MAX_SURFACES = 2;
const READINESS_PROBE_GENERIC_TOKENS = new Set([
  "answer", "behavior", "build", "contract", "exactly", "execution", "explain", "followup",
  "implementation", "needs", "pack", "read", "ready", "returns", "structured", "task", "when",
]);

/**
 * W5-B (2026-08-21, falsification-scope-and-text-floor forensics): roles
 * that must never, by themselves, widen the falsification probe's fallback
 * scope (runInternalReadinessFalsification's pathless-query default). A
 * "doc" anchor commonly sits DIRECTLY at the workspace root (a top-level
 * design doc), and an "unknown"-role anchor already survived only on
 * generic lexical overlap — neither marks the task's own project root the
 * way a code-bearing surface (api/domain/contract/test/ui/style) does.
 * Left unfiltered, a mixed anchor set (one nested-package code surface +
 * one root-level doc surface) makes roleSearchScopePrefixes' shared-
 * ancestor branch degrade to a bare "" prefix — ROOT-MEMBERSHIP semantics
 * matching every file the workspace root does not delegate to a nested
 * package (bench/, scripts/, any other top-level tree) — instead of the
 * doc's own narrow location. See runInternalReadinessFalsification's
 * dominantAnchorPaths.
 */
const FALSIFICATION_SCOPE_SUPPORT_ROLES: ReadonlySet<string> = new Set(["doc", "unknown"]);

/**
 * W5-B: a falsification counterexample is SUPPORTING negative evidence, not
 * the pack's own primary content — it must stay far under the general
 * per-surface cap (MAX_SURFACE_CODE_BYTES). The pre-fix probe window
 * (selectQueryEvidence's +-18 LINE window, uncapped in BYTES) served a
 * 9.4KB body from a file whose matched lines happened to be very long.
 * Reuses centeredSliceForCap — the same centered/graceful-truncation
 * mechanism every other oversize-surface path already uses — with this
 * smaller cap instead of inventing a second trimming mechanism.
 */
const READINESS_PROBE_COUNTEREXAMPLE_MAX_BYTES = 2048;

const IMPLEMENTATION_EXTS = new Set([
  ".c", ".cc", ".cpp", ".cxx", ".go", ".java", ".js", ".jsx", ".kt", ".kts",
  ".m", ".mm", ".php", ".py", ".rb", ".rs", ".swift", ".ts", ".tsx",
]);

const BEHAVIOR_QUERY_RE = /\b(?:behavior|conditions?|flow|lifecycle|sequence|state\s*machine|transition|when|how)\b|挙動|条件|流れ|順番|ライフサイクル|遷移|ステートマシン/i;

export function servedSurfaceText(surface: TaskPackSurface): string {
  // W4: recover a deduped surface's REAL body (see dedupedSurfaceBody) for
  // server-side reasoning so proof/readiness verdicts are invariant to the
  // cross-call `code`→pointer swap; falls back to the pointer only if no real
  // body was recorded (e.g. a surface that arrived code_unchanged).
  return surface.code ?? dedupedSurfaceBody.get(surface) ?? surface.code_unchanged ?? "";
}

function readinessEvidence(surface: TaskPackSurface): TaskReadinessEvidence {
  return {
    handle: surface.handle,
    path: surface.path,
    ...(surface.range ? { range: surface.range } : {}),
    ...(surface.symbol ? { symbol: surface.symbol } : {}),
  };
}

function isImplementationPath(filePath: string): boolean {
  return IMPLEMENTATION_EXTS.has(path.extname(filePath).toLowerCase())
    && !/\.d\.(?:[cm]?ts|tsx)$/i.test(filePath)
    // Benchmark/sample repositories often live below a host repo's `bench/`
    // or `fixtures/` directory while still containing the real implementation
    // the agent is assigned to edit. Excluding those container segments made
    // every artifact_build/wiring proof in Signal5 treat production bodies as
    // non-implementation evidence. Only explicit test/spec subtrees are
    // non-implementation by path; fixture/bench-ness is not semantic evidence.
    && !/(?:^|\/)(?:test|tests|spec|specs)(?:\/|$)/i.test(filePath)
    // Issue #4 (2026-08-21): the directory check above only matched a path
    // SEGMENT spelled exactly "test(s)"/"spec(s)" — it never matched this
    // very workspace's own dominant Vitest convention, `__tests__/*.spec.ts`
    // (nor the sibling `*.test.ts`), because "__tests__" (underscore-wrapped)
    // and a ".spec."/".test." FILENAME suffix are both different strings
    // from a bare "test"/"spec" path segment. That left every spec file in
    // this workspace classified as "implementation" — eligible to win a
    // same-topic ranking tie-break against the production file it tests, be
    // promoted to an "api" role, or (readiness-falsification's actual
    // symptom) get force-added as a REQUIRED edit "competing implementation
    // counterexample" for a request that never touched behavior at all.
    && !/(?:^|\/)__tests?__(?:\/|$)/i.test(filePath)
    && !/\.(?:spec|test)\.[^./]+$/i.test(filePath);
}

function hasCallableBody(surface: TaskPackSurface): boolean {
  if (!isImplementationPath(surface.path)) return false;
  const symbol = surface.symbol ?? "";
  if (symbol && (/::|\./.test(symbol) || /^[a-z_]/.test(symbol))) return true;
  const text = servedSurfaceText(surface);
  return /\b(?:def|fn|func|function)\s+[A-Za-z_][A-Za-z0-9_]*\s*\(/.test(text)
    || /\b(?:public|private|protected|static|async|virtual|inline|constexpr)\b[^;{}\n]*\b[A-Za-z_][A-Za-z0-9_]*(?:::[A-Za-z_][A-Za-z0-9_]*)?\s*\([^;{}]*\)\s*(?:const\s*)?\{/.test(text)
    || /\b[A-Za-z_][A-Za-z0-9_]*(?:::[A-Za-z_][A-Za-z0-9_]*)+\s*\([^;{}]*\)\s*(?:const\s*)?\{/.test(text)
    || /\b(?!if\b|for\b|while\b|switch\b|catch\b)[A-Za-z_][A-Za-z0-9_]*\s*\([^;{}]*\)\s*\{/.test(text);
}

function surfaceContainsToken(surface: TaskPackSurface, token: string | undefined): boolean {
  if (!token) return false;
  const haystack = `${surface.path}\n${surface.symbol ?? ""}\n${servedSurfaceText(surface)}`.toLowerCase();
  return haystack.includes(token.toLowerCase());
}

// Body/symbol-scoped variant for PROOF obligations (2026-07-16a review round
// 2): a wiring producer/consumer is proved only by served CODE (or the
// symbol's own identity) containing the endpoint token — never by the file
// PATH alone; two token-free bodies inside token-named files certified
// edit-ready under the path-inclusive check. surfaceContainsToken (above)
// stays for facet COVERAGE checks, where a file dedicated to the facet is
// weak-but-acceptable evidence.
function surfaceBodyContainsToken(surface: TaskPackSurface, token: string | undefined): boolean {
  if (!token) return false;
  const haystack = `${surface.symbol ?? ""}\n${servedSurfaceText(surface)}`.toLowerCase();
  return haystack.includes(token.toLowerCase());
}

function exactIdentifierEvidence(
  surfaces: readonly TaskPackSurface[],
  identifier: string,
): TaskReadinessEvidence[] {
  const needle = identifier.toLowerCase();
  return surfaces
    .filter((surface) =>
      surface.symbol?.toLowerCase() === needle
      || new RegExp(`\\b${identifier.replace(/[.*+?^${}()|[\]\\]/g, "\\$&")}\\b`).test(servedSurfaceText(surface))
    )
    .map(readinessEvidence);
}

/**
 * B1d (2026-08-01 retrieval-scope): does this line CALL a collaborator named
 * for `facet`?
 *
 * The two shapes that matter, language-agnostically:
 *   - the facet names the RECEIVER: `shippingService.requestLabel(...)`,
 *     `shipping_client->send(...)`, `Shipping::quote(...)`;
 *   - the facet names the CALLED MEMBER: `carrier.shippingQuote(...)`.
 *
 * Both are behavior; a bare mention (`total += shippingCost;`, a field
 * declaration, a comment) is not. Deliberately a line-shape test, never a
 * project vocabulary list.
 */
function isFacetCallSiteLine(source: string, facet: string): boolean {
  const escaped = facet.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
  const ident = "[A-Za-z0-9_$]*";
  const receiver = new RegExp(`${ident}${escaped}${ident}\\s*(?:\\.|->|::)\\s*[A-Za-z_$][A-Za-z0-9_$]*\\s*\\(`, "i");
  const member = new RegExp(`(?:\\.|->|::)\\s*${ident}${escaped}${ident}\\s*\\(`, "i");
  return receiver.test(source) || member.test(source);
}

/**
 * W9 (2026-08-22): `query` is scrubbed of generic path spans FIRST. Without
 * it, `slashGroups` below — a `/`-joined 2-3-segment scan meant for a real
 * collaborator reference like "wire up shipping/rate-quote" — matched EVERY
 * consecutive segment run of an absolute path mentioned in prose (confirmed
 * live: "...in /Users/…/TokenLighten/packages/mcp-server/src how does X
 * work" produced facets `users,takayuki,git,tokenlighten,packages,mcp-server`).
 * Each returned facet becomes a REQUIRED query facet for the `behavior-body`
 * obligation (buildReadinessObligations' BEHAVIOR_QUERY_RE branch) —
 * directory names can never appear as a call-site token in any real source
 * file, so an unrelated path mention permanently blocked `behavior-body`
 * from ever proving, even when the actual behavior WAS fully served. A short
 * bare reference ("shipping/rate-quote", "@scope/pkg" — one separator) is
 * unaffected; only an anchored or >=3-segment span is stripped.
 */
function requiredQueryFacets(query: string): string[] {
  const cleaned = stripPathSpans("", query);
  const slashGroups = [...cleaned.matchAll(/([A-Za-z][A-Za-z0-9_-]{2,})\s*\/\s*([A-Za-z][A-Za-z0-9_-]{2,})(?:\s*\/\s*([A-Za-z][A-Za-z0-9_-]{2,}))?/g)]
    .flatMap((match) => match.slice(1).filter((value): value is string => Boolean(value)));
  const quoted = [...cleaned.matchAll(/[`"']([A-Za-z_][A-Za-z0-9_.:-]{2,})[`"']/g)].map((match) => match[1]!);
  return [...new Set([...slashGroups, ...quoted].map((value) => value.toLowerCase()))].slice(0, 6);
}

/**
 * F-V13-6 (2026-08-30, v0.13.0 external-verification P0-B): THE FINITE
 * PLAIN-PROSE CHECKLIST.
 *
 * A query can name several things that must each be accounted for without
 * using any of the four vocabularies the proof model already recognizes:
 * `requiredQueryFacets` extracts only quoted/backticked/slash-joined tokens,
 * `explicitCodeIdentifiers` only code-SHAPED ones, `BEHAVIOR_QUERY_RE` only
 * behavior wording, and v0.13's obligation ledger only fires behind
 * `hasOpenUniverseIntent` (quantifiers: "every caller", "すべての", "列挙").
 * "…what it does with clipboard, autosave, telemetry and shortcuts" is none of
 * those, so nothing in the proof model represented it and the certificate was
 * vacuously complete — the reported defect (an `act.answer` over a head slice
 * of a long file, reproduced here in its essential form: served bytes that
 * cover none of the enumerated items).
 *
 * WHAT COUNTS AS AN ENUMERATION, and why each rule is here. Only ONE new
 * judgment is added: is this prose a LIST of short noun phrases?
 *
 *  1. SEPARATORS are commas (ASCII and CJK), semicolons, the ideographic
 *     middle dot, EN "and"/"or", JA "や", and line-leading bullets/numbering.
 *     NOT "/" — slash groups are `requiredQueryFacets`' own axis, and treating
 *     a path-shaped span as a three-item list is exactly the W9 regression
 *     that once poisoned every facet on this file.
 *  2. AN ITEM IS A SHORT NOUN PHRASE: at most two words (after a leading
 *     determiner) and 32 characters. This is the rule that separates a list
 *     from ordinary clause prose — "when the panel loads, the store is read
 *     and the bus is bound" splits into three items, and every one of them is
 *     a clause with a verb, so none qualifies and nothing is minted.
 *  3. THE LEADING ITEM IS EXEMPT FROM (2) VIA ITS TAIL, because it carries the
 *     sentence's whole preamble ("explain what the settings panel does with
 *     clipboard"). Its last two words are re-tried as the noun phrase, which
 *     is where the first enumerated head actually sits — in both languages
 *     tested. The exemption is deliberately NOT extended to the other items:
 *     that is what keeps rule (2) load-bearing against clause prose.
 *  4. AT LEAST TWO NON-LEADING items must qualify, and at least three facets
 *     must survive in total. Two items are never an enumeration; three are.
 *  5. AN ITEM'S FACET IS ONE content-bearing LATIN token of it — the longest
 *     for a genuine list item, the LAST one for the leading segment's re-tried
 *     tail (see `enumerationItemFacet`). A facet must be provable against
 *     served text, and a purely ideographic item ("環境変数") can never appear
 *     literally in code — minting it would create a permanently unprovable
 *     obligation, which is over-strictness, not rigor. Japanese enumerations
 *     therefore contribute exactly the identifier-shaped items they name.
 *  6. ANYTHING THE EXISTING ROUTES ALREADY OWN IS DROPPED — a backticked or
 *     code-shaped item already has its own obligation, and two obligations
 *     over one requirement is double-counting, not extra proof.
 */
const ENUMERATED_ITEM_OBLIGATION_PREFIX = "enumerated-item:";
/** Distinct facets required before a prose list is treated as an enumeration. */
const ENUMERATED_QUERY_MIN_FACETS = 3;
/** Upper bound on minted enumerated obligations (cf. requiredQueryFacets' own cap). */
const ENUMERATED_QUERY_MAX_FACETS = 8;
/** Shortest token carrying enough signal to look for in served code. */
const ENUMERATED_FACET_MIN_CHARS = 3;
/** Words / characters an item may have and still read as a noun phrase, not a clause. */
const ENUMERATED_ITEM_MAX_WORDS = 2;
const ENUMERATED_ITEM_MAX_CHARS = 32;
/** Function words that carry no retrieval signal, so never a facet on their own. */
const ENUMERATED_ITEM_STOPWORDS: ReadonlySet<string> = new Set([
  "the", "and", "but", "for", "its", "this", "that", "these", "those", "with", "from", "into",
  "than", "then", "what", "which", "who", "why", "does", "did", "are", "was", "were", "will",
  "would", "should", "could", "must", "may", "can", "not", "use", "uses", "used", "using", "via",
  "per", "any", "some", "other", "others", "same", "new", "old", "only", "just", "also", "etc",
  "more", "most", "each", "every", "all", "one", "two", "three", "four", "five", "six",
]);
/**
 * NUL, deliberately: a word-shaped sentinel (" enum ") would itself be split
 * by a query that happens to contain that word. NUL cannot occur in a request
 * the transport delivers, and it is already this file's join separator
 * elsewhere. Written as an escape so the source stays NUL-free.
 */
const ENUMERATION_SEPARATOR = "\u0000";
const ENUMERATION_LIST_MARKER_RE = /(?:^|\n)[ \t]*(?:[-*•]|\(?\d{1,2}[.)])[ \t]+/g;
const ENUMERATION_SEPARATOR_RE = /[,;、，・]|\band\b|\bor\b|や/gi;
const ENUMERATION_LEADING_DETERMINER_RE = /^(?:the|a|an|its|their|this|that|these|those)\s+/i;

/**
 * The facet an item contributes, or undefined when it is not a short noun
 * phrase.
 *
 * `pick` is the difference between the two callers, and it is not cosmetic. A
 * genuine list item is a noun phrase whose most distinctive word carries the
 * requirement ("autosave timer" -> autosave), so it takes the LONGEST token.
 * The leading segment's re-tried tail is the end of a SENTENCE ("…decides
 * allow"), where the enumerated head is the last word and everything before it
 * is the verb — taking the longest there mints the verb as a requirement,
 * observed live against replayCorpus's `resolveAccessPolicy decides allow,
 * deny, or needs-review` entry, which then could never prove.
 */
function enumerationItemFacet(item: string, pick: "longest" | "last" = "longest"): string | undefined {
  const phrase = item.trim().replace(ENUMERATION_LEADING_DETERMINER_RE, "");
  if (phrase.length === 0 || phrase.length > ENUMERATED_ITEM_MAX_CHARS) return undefined;
  if (phrase.split(/\s+/).length > ENUMERATED_ITEM_MAX_WORDS) return undefined;
  // Internal hyphens are part of the token: "needs-review" is one enumerated
  // item, and splitting it into "needs"/"review" both loses the requirement and
  // weakens the substring match that proves it.
  const tokens = (phrase.match(/[A-Za-z_$][A-Za-z0-9_$]*(?:-[A-Za-z0-9_$]+)*/g) ?? [])
    .map((token) => token.toLowerCase())
    .filter((token) =>
      token.length >= ENUMERATED_FACET_MIN_CHARS
      && !ENUMERATED_ITEM_STOPWORDS.has(token)
      && !READINESS_PROBE_GENERIC_TOKENS.has(token));
  if (tokens.length === 0) return undefined;
  return pick === "last"
    ? tokens[tokens.length - 1]!
    : tokens.reduce((best, token) => (token.length > best.length ? token : best));
}

/**
 * Exported for its own regression pin (enumeratedItemObligation.spec.ts),
 * matching the precedent set by `advanceExecutedLocateNextCall` and
 * `repairSuppressedNextCall`: a shape-classification heuristic whose
 * false-positive boundary is the whole risk is worth testing directly, not
 * only through six layers of pack assembly.
 */
export function enumeratedQueryFacets(query: string): string[] {
  const items = stripPathSpans("", query)
    .replace(ENUMERATION_LIST_MARKER_RE, ENUMERATION_SEPARATOR)
    .replace(ENUMERATION_SEPARATOR_RE, ENUMERATION_SEPARATOR)
    .split(ENUMERATION_SEPARATOR)
    .map((item) => item.trim())
    .filter((item) => item.length > 0);
  if (items.length < ENUMERATED_QUERY_MIN_FACETS) return [];
  const trailing = items.slice(1)
    .map((item) => enumerationItemFacet(item))
    .filter((facet): facet is string => facet !== undefined);
  // Rule 4's floor, applied to the items that had to earn it the hard way.
  if (trailing.length < ENUMERATED_QUERY_MIN_FACETS - 1) return [];
  const leadWords = items[0]!.trim().split(/\s+/);
  const lead = enumerationItemFacet(items[0]!)
    ?? enumerationItemFacet(leadWords.slice(-ENUMERATED_ITEM_MAX_WORDS).join(" "), "last");
  const owned = new Set([
    ...requiredQueryFacets(query),
    ...explicitCodeIdentifiers(query).map((identifier) => identifier.toLowerCase()),
  ]);
  const facets: string[] = [];
  const seen = new Set<string>();
  for (const facet of lead !== undefined ? [lead, ...trailing] : trailing) {
    if (owned.has(facet) || seen.has(facet)) continue;
    seen.add(facet);
    facets.push(facet);
  }
  return facets.length >= ENUMERATED_QUERY_MIN_FACETS
    ? facets.slice(0, ENUMERATED_QUERY_MAX_FACETS)
    : [];
}

function probeFileText(workspace: string, filePath: string): string | undefined {
  try {
    const rootReal = fs.realpathSync(workspace);
    const candidate = path.resolve(rootReal, filePath);
    const real = fs.realpathSync(candidate);
    const rel = path.relative(rootReal, real);
    if (rel === "" || rel.startsWith("..") || path.isAbsolute(rel)) return undefined;
    const stat = fs.statSync(real);
    if (!stat.isFile() || stat.size > READINESS_PROBE_MAX_BYTES) return undefined;
    // P3 (2026-08-27): decodeTextBuffer, not a raw utf8 read — undecodable
    // content probes as unavailable, same as any other probe failure here.
    return decodeTextBuffer(fs.readFileSync(real)) ?? undefined;
  } catch {
    return undefined;
  }
}

function maskServedRanges(
  content: string,
  filePath: string,
  surfaces: readonly TaskPackSurface[],
): string {
  const lines = content.replace(/\r\n/g, "\n").replace(/\r/g, "\n").split("\n");
  for (const surface of surfaces) {
    if (surface.path !== filePath) continue;
    const match = /^(\d+)-(\d+)$/.exec(surface.range);
    if (!match) continue;
    const start = Math.max(1, Number(match[1]));
    const end = Math.min(lines.length, Number(match[2]));
    for (let line = start; line <= end; line++) lines[line - 1] = "";
  }
  return lines.join("\n");
}

function inferCallableSymbol(content: string, line: number): string | undefined {
  const lines = content.replace(/\r\n/g, "\n").replace(/\r/g, "\n").split("\n");
  const start = Math.max(0, line - 14);
  const end = Math.min(lines.length, line + 3);
  let found: string | undefined;
  for (let index = start; index < end; index++) {
    const candidate = lines[index]!;
    // C/C++ projects commonly put the opening brace on the following line.
    // Join one lookahead line for signature recognition without widening the
    // evidence window or treating declarations ending in ';' as callables.
    const signature = `${candidate} ${lines[index + 1] ?? ""}`;
    const explicit = /\b(?:def|fn|func|function)\s+([A-Za-z_][A-Za-z0-9_]*)\s*\(/.exec(candidate)?.[1];
    const method = /\b([A-Za-z_][A-Za-z0-9_]*(?:::[A-Za-z_][A-Za-z0-9_]*)*)\s*\([^;{}]*\)\s*(?:const\s*)?\{/.exec(signature)?.[1];
    const name = explicit ?? method;
    if (name && !/^(?:if|for|while|switch|catch)$/.test(name)) found = name;
  }
  return found;
}

function selectCallableClosureEvidence(
  content: string,
  pathName: string,
  endpointTerms: readonly string[],
  contextTerms: readonly string[],
  compoundTerms: readonly string[],
): { range: string; content: string; line: number; score: number } | undefined {
  const lines = content.replace(/\r\n/g, "\n").replace(/\r/g, "\n").split("\n");
  let best: { range: string; content: string; line: number; score: number } | undefined;
  for (let index = 0; index < lines.length; index++) {
    const rawLine = lines[index]!;
    const trimmedLine = rawLine.trim();
    if (
      trimmedLine.startsWith("//")
      || trimmedLine.startsWith("/*")
      || trimmedLine.startsWith("*")
      || trimmedLine.startsWith("#")
    ) continue;
    const lineText = rawLine.toLowerCase();
    // A consumer window must contain an endpoint or task facet inside a
    // callable body. Hits confined to includes/comments are supporting
    // context, not insertion-site proof.
    if (!endpointTerms.some((term) => lineText.includes(term.toLowerCase()))) continue;
    const line = index + 1;
    if (inferCallableSymbol(content, line) === undefined) continue;
    const start = Math.max(1, line - 18);
    const end = Math.min(lines.length, line + 18);
    const selected = lines.slice(start - 1, end).join("\n");
    const scoringText = selected.split("\n")
      .map((candidate) => candidate.replace(/\/\/.*$/, ""))
      .filter((candidate) => {
        const trimmed = candidate.trim();
        return !trimmed.startsWith("/*")
          && !trimmed.startsWith("*")
          && !trimmed.startsWith("#");
      })
      .join("\n");
    const lower = scoringText.toLowerCase();
    const endpointHits = endpointTerms.filter((term) => lower.includes(term.toLowerCase())).length;
    const contextHits = contextTerms.filter((term) => lower.includes(term.toLowerCase())).length;
    const pathHits = contextTerms.filter((term) => pathName.toLowerCase().includes(term.toLowerCase())).length;
    const normalizedSelected = scoringText.toLowerCase().replace(/[-_]/g, "");
    const compoundHits = compoundTerms.filter((term) => normalizedSelected.includes(term)).length;
    const candidate = {
      range: `${start}-${end}`,
      content: selected,
      line,
      score: endpointHits * 8 + contextHits * 3 + pathHits * 2 + compoundHits * 20 + 8,
    };
    if (best === undefined || candidate.score > best.score) best = candidate;
  }
  return best;
}

/**
 * Drop comment-only lines — the same leading `//`, `/*`, `*`, `#` shapes
 * selectCallableClosureEvidence above already treats as non-anchoring —
 * before a file is scored as a readiness-falsification counterexample.
 *
 * Issue #4 (2026-08-21): a file's own PROSE mentioning an identifier (a
 * doc comment, a changelog-style "why" note, a residual-bug writeup) is not
 * competing CODE. Once isImplementationPath's `__tests__`/`.spec.`/`.test.`
 * gap was fixed so a spec file could no longer win this probe on a comment
 * match, the SAME raw comment-inclusive scan immediately promoted a
 * DIFFERENT file instead: this module's own `// R0 ...` note discussing a
 * past attempt to fix the exact "buildInitializeInstructions" bug this
 * negative probe was being run for, in prose, matched the query's own
 * explicit identifier and got force-added as a REQUIRED competing
 * implementation. This is a best-effort line-level filter (it does not
 * track string/template state), which is the safe direction here: it can
 * only make the probe LESS likely to admit a file, never invent a match
 * that is not really in the code.
 */
function stripCommentOnlyLines(content: string): string {
  return content
    .split(/\r?\n/)
    .filter((rawLine) => {
      const trimmed = rawLine.trim();
      return !(
        trimmed.startsWith("//")
        || trimmed.startsWith("/*")
        || trimmed.startsWith("*")
        || trimmed.startsWith("#")
      );
    })
    .join("\n");
}

/**
 * Negative retrieval pass. It searches outside the already-served ranges for
 * evidence that would falsify a provisional ready decision and inlines at
 * most two counterexamples into the SAME task_pack response.
 */
function runInternalReadinessFalsification(
  result: TaskPackResult,
  profile: TaskProfile,
  query: string,
  workspace: string,
  scopePaths?: readonly string[],
): number {
  const provisionalReady = result.route?.action === "answer_from_handles"
    || (result.route?.action === "edit_from_handles" && (result.route.max_additional_tl_calls ?? 0) === 0);
  if (!provisionalReady || query.trim().length === 0) return 0;

  const initialObligations = buildReadinessObligations(result, profile, query);
  const unresolved = initialObligations.filter((obligation) =>
    obligation.status === "uncovered"
    && ["surface-content", "behavior-body", "artifact-source", "wiring-source", "wiring-consumer", "wiring-insertion"].includes(obligation.kind)
  );
  // Even when the provisional pack appears fully proved, spend one bounded
  // server-side probe looking for competing implementation evidence. This is
  // the rejection test that makes ready a falsifiable prediction rather than
  // a restatement of the route heuristic. Uncovered obligations may justify a
  // second probe, but never an agent round-trip.
  const probeLimit = unresolved.length > 0
    ? Math.min(READINESS_PROBE_MAX_SURFACES, unresolved.length)
    : 1;

  const existing = codeTaskPackSurfaces(result.surfaces);
  const existingKeys = new Set(existing.map((surface) => `${surface.path}:${surface.range}`));
  const queryTokens = tokenizeForEpoch(query)
    .filter((token) => token.length >= 4 && !READINESS_PROBE_GENERIC_TOKENS.has(token))
    .slice(0, 10);
  const explicitIdentifiers = explicitCodeIdentifiers(query).map((identifier) => identifier.toLowerCase());
  const connection = result.wiring?.connections[0];
  const endpointTokens = [connection?.source?.token, connection?.destination?.token]
    .filter((token): token is string => typeof token === "string" && token.length > 0);
  const preferredPaths = existing.map((surface) => surface.path);
  const walked = walkCodeFiles(workspace, {}).map((file) => file.relPath);
  const normalizedScopes = (scopePaths ?? [])
    .map((scope) => path.posix.normalize(scope.replace(/\\/g, "/")).replace(/^\.\//, ""))
    .filter((scope) => scope.length > 0 && scope !== ".");
  // R0 (2026-08-21, W4-A pack-augmentation-pollution forensics): a PATHLESS
  // query supplies no explicit `scopePaths` at all, which left this
  // negative-retrieval pass scanning the WHOLE workspace for a
  // "counterexample" — including an unrelated project's own tree, admitted
  // as a REQUIRED surface on nothing stronger than incidental lexical
  // overlap with the query's own prose (live case: the query's own quoted
  // phrase, planted verbatim in an unrelated fixture file's comment, was
  // "found" as a competing implementation and force-added). Fall back to
  // this task's own TRUSTWORTHY surfaces' project root(s) — the same
  // discipline selectClosureCandidates/augmentNativeSurfaceClosure now
  // apply — when the caller supplied no explicit scope; an explicit
  // scopePaths stays authoritative exactly as before (never narrowed).
  // W5-B: prefer the DOMINANT (code-bearing) trustworthy anchors when
  // computing the fallback scope — see FALSIFICATION_SCOPE_SUPPORT_ROLES.
  // Falls back to the full trustworthy set only when nothing code-bearing
  // survives (a genuinely doc-only pack keeps the prior, unnarrowed scope
  // unchanged — no regression for that case).
  const dominantRoleExisting = existing.filter((s) => !FALSIFICATION_SCOPE_SUPPORT_ROLES.has(s.role));
  const dominantAnchorPaths = trustworthySurfaceAnchorPaths(
    dominantRoleExisting.length > 0 ? dominantRoleExisting : existing,
  );
  const fallbackScopePrefixes = normalizedScopes.length === 0
    ? roleSearchScopePrefixes(dominantAnchorPaths)
    : null;
  const candidatePaths = [...new Set([...preferredPaths, ...walked])]
    .filter(isImplementationPath)
    .filter((candidate) => normalizedScopes.length === 0
      ? isWithinRoleSearchScope(candidate, fallbackScopePrefixes)
      : normalizedScopes.some((scope) =>
          candidate === scope || candidate.startsWith(`${scope.replace(/\/$/, "")}/`)
        ))
    .sort((a, b) => {
      const aPreferred = preferredPaths.includes(a) ? 1 : 0;
      const bPreferred = preferredPaths.includes(b) ? 1 : 0;
      const aPathHits = queryTokens.filter((token) => a.toLowerCase().includes(token)).length;
      const bPathHits = queryTokens.filter((token) => b.toLowerCase().includes(token)).length;
      return bPreferred - aPreferred || bPathHits - aPathHits || a.localeCompare(b);
    })
    .slice(0, READINESS_PROBE_MAX_FILES);

  const ranked: Array<{
    path: string;
    range: string;
    line: number;
    content: string;
    maskedContent: string;
    symbol?: string;
    score: number;
    role: string;
    strong: boolean;
  }> = [];
  for (const filePath of candidatePaths) {
    const content = probeFileText(workspace, filePath);
    if (content === undefined) continue;
    const masked = maskServedRanges(content, filePath, existing);
    // Admission is decided on CODE only (see stripCommentOnlyLines) — a
    // comment mentioning the query's identifier is not a competing
    // implementation. `masked` (comment-inclusive) is still what
    // selectQueryEvidence windows below, so a file that DOES admit still
    // gets its normal surrounding-context window.
    const lower = stripCommentOnlyLines(masked).toLowerCase();
    const queryTokenHits = queryTokens.filter((token) => lower.includes(token)).length;
    const matchedIdentifiers = explicitIdentifiers.filter((identifier) => lower.includes(identifier));
    const identifierHit = matchedIdentifiers.length > 0;
    const endpointHit = endpointTokens.some((token) => lower.includes(token.toLowerCase()));
    // Once a file already contributes an action surface, generic lexical
    // co-occurrence in another range of that same file is not a competing
    // implementation. Require an exact named identifier or wiring endpoint
    // before paying to inline a second slice from it.
    if (preferredPaths.includes(filePath) && !identifierHit && !endpointHit) continue;
    // A single generic word is not a counterexample. Require an exact named
    // identifier, a wiring endpoint, or two independent query facets.
    //
    // For an already-covered multi-concern pack, broad lexical overlap in an
    // orchestrator/bootstrap file is not evidence of a new edit obligation:
    // words from several independent concerns naturally co-occur there. The
    // per-concern locator has already done the bounded implementation search,
    // so a negative probe must carry an exact identifier or graph endpoint.
    const allMultiConcernsCovered = profile === "multi_concern"
      && result.concerns?.length
      && result.concerns.every((concern) => concern.status === "covered");
    if (
      (allMultiConcernsCovered && !endpointHit)
      || (!identifierHit && !endpointHit && queryTokenHits < 2)
    ) {
      continue;
    }
    const probeQuery = preferredPaths.includes(filePath) && matchedIdentifiers.length > 0
      ? matchedIdentifiers.join(" ")
      : query;
    const evidence = selectQueryEvidence(masked, probeQuery, { path: filePath, windowLines: 18 });
    if (evidence === undefined) continue;
    const key = `${filePath}:${evidence.range}`;
    if (existingKeys.has(key)) continue;
    const endpointScore = endpointTokens.reduce((score, token) =>
      score + (lower.includes(token.toLowerCase()) ? 8 : 0), 0);
    const sameFileScore = preferredPaths.includes(filePath) ? 12 : 0;
    const role = classifySurface(filePath);
    // W5-B (item 1b): a same-root test/spec surface is a more CREDIBLE
    // falsification counterexample than an arbitrary same-root file with
    // matching lexical score — prefer it.
    const roleScore = role === "test" ? 40 : 0;
    ranked.push({
      path: filePath,
      range: evidence.range,
      line: evidence.line,
      content: evidence.content,
      maskedContent: masked,
      symbol: inferCallableSymbol(content, evidence.line),
      score: evidence.score + endpointScore + sameFileScore + roleScore,
      role,
      // Issue #4 (2026-08-21): identifierHit/endpointHit name an EXACT code
      // identifier or wiring endpoint — real evidence a competing
      // implementation exists. The queryTokenHits-only fallback (2+ ordinary
      // words appearing ANYWHERE in the file, admitted so a borderline case
      // with no strong identifier is not silently dropped) is corpus-wide
      // noise on any large, richly-commented/self-documenting file: this
      // repo's own generic-noun/stop-word arrays (e.g. locateTaskContext.ts
      // literally containing the string literals "server" and "issue" as
      // DATA in a stop-word Set) satisfy it for nearly any two-word query
      // without the file having anything to do with the request. A match
      // that strong evidence didn't corroborate must not be able to force a
      // REQUIRED obligation — see the `required: candidate.strong` write
      // below; it still surfaces as review-only supporting context.
      strong: identifierHit || endpointHit,
    });
  }

  ranked.sort((a, b) => b.score - a.score || a.path.localeCompare(b.path) || a.line - b.line);
  // W5-B (item 1d): once a same-root test/spec candidate exists at all, a
  // role-unknown candidate (generic lexical overlap, no recognized part to
  // play) never gets to stand in as the falsification counterexample.
  const sameRootTestExists = ranked.some((candidate) => candidate.role === "test");
  let added = 0;
  const usedPaths = new Set<string>();
  // Wire-baseline drift fix (2026-08-21, W7 integration): centeredSliceForCap
  // reads the file fresh from `workspace` (via `readCached`), bypassing the
  // `maskServedRanges` blanking this pass already applied to select
  // `candidate.range`/`candidate.content` above. Unseeded, that reintroduced
  // an already-served range's raw text into the counterexample body — e.g.
  // duplicating a symbol already served as a separate evidence item onto
  // this one, wasting bytes on content the caller already has (exactly what
  // masking exists to prevent). Seed a per-candidate cache with the SAME
  // masked text used for range/window selection so the byte-capped body
  // stays consistent with it.
  const counterexampleCache = new FileReadCache();
  for (const candidate of ranked) {
    if (added >= probeLimit) break;
    if (usedPaths.has(candidate.path) && profile !== "answer") continue;
    if (candidate.role === "unknown" && sameRootTestExists) continue;
    // W5-B (item 1c): a counterexample is supporting negative evidence, not
    // primary content — cap it far under MAX_SURFACE_CODE_BYTES using the
    // same centered/graceful-truncation mechanism every other oversize
    // surface already uses, instead of the raw (byte-uncapped) probe window
    // (root cause of the pre-fix 9.4KB body: selectQueryEvidence's +-18
    // LINE window has no BYTE ceiling, and this file's matched lines were
    // very long).
    counterexampleCache.seed(candidate.path, candidate.maskedContent);
    const capped = centeredSliceForCap(
      workspace,
      candidate.path,
      candidate.range,
      counterexampleCache,
      candidate.line,
      READINESS_PROBE_COUNTEREXAMPLE_MAX_BYTES,
    );
    if (capped === undefined) continue;
    const handle = handleTable.upsert({
      kind: "range",
      path: candidate.path,
      range: candidate.range,
      ...(candidate.symbol ? { symbol: candidate.symbol } : {}),
      workspaceRoot: workspace,
      sha: shaOfText(candidate.content),
    });
    (result.surfaces as TaskPackResultSurface[]).push({
      role: candidate.role,
      handle: handle.id,
      path: candidate.path,
      range: candidate.range,
      ...(candidate.symbol ? { symbol: candidate.symbol } : {}),
      // Issue #4 (2026-08-21): only a strong (identifier/endpoint) match
      // forces a REQUIRED obligation; a weak (generic-word-only) match still
      // surfaces so the agent can review it, but must not by itself flip a
      // provisionally ready pack into a blocked one. See the `strong` field
      // built above.
      required: candidate.strong,
      why: "readiness-falsification-counterexample",
      code: capped.code,
      ...(capped.remaining_ranges.length > 0
        ? { content_completeness: "partial" as const, remaining_ranges: capped.remaining_ranges }
        : {}),
    });
    existingKeys.add(`${candidate.path}:${candidate.range}`);
    usedPaths.add(candidate.path);
    added++;
  }
  return added;
}

const RECURSIVE_READ_CLOSURE_MAX_DEPTH = 3;
const RECURSIVE_READ_CLOSURE_MAX_BRANCHES = 3;
const RECURSIVE_READ_CLOSURE_MAX_OPERATIONS = 6;
const RECURSIVE_READ_CLOSURE_MAX_MERGED_BYTES = 24 * 1024;
/**
 * Profiles whose discovery is structurally multi-site — the only shapes where
 * the DEEP recursive closure loop has paid for itself (2026-07-19a forensics:
 * on lean single-area tasks the loop inflated pack bulk and downstream output
 * tokens with both arms worse; the wins concentrate in cross-subsystem
 * wiring/multi-concern packs). An explicit caller-pinned taskProfile
 * (profile_binding.source === "explicit") opts in regardless, so mechanism
 * runs can exercise the loop on any profile.
 */
const RECURSIVE_CLOSURE_DEEP_PROFILES: ReadonlySet<TaskProfile> = new Set([
  "wiring",
  "multi_concern",
  // Propagation tasks are structurally multi-site too: a definition change is
  // incomplete until its bounded consumer/config surfaces are prepared. Use
  // the existing fail-closed depth/branch/operation/byte caps rather than a
  // task- or repository-specific expansion rule.
  "change_propagation",
]);

function canonicalClosureValue(value: unknown): unknown {
  if (Array.isArray(value)) return value.map(canonicalClosureValue);
  if (value !== null && typeof value === "object") {
    return Object.fromEntries(
      Object.entries(value as Record<string, unknown>)
        .sort(([a], [b]) => a.localeCompare(b))
        .map(([key, item]) => [key, canonicalClosureValue(item)]),
    );
  }
  return value;
}

function closureCallSignature(call: ContinuationCall): string {
  const args = call.arguments;
  const handleId = typeof args["handle"] === "string" ? args["handle"] : undefined;
  const handleSha = handleId ? handleTable.get(handleId)?.sha ?? "" : "";
  return `${call.tool}\u0000${JSON.stringify(canonicalClosureValue(args))}\u0000${handleSha}`;
}

function closureSlice(
  content: string,
  requestedRange: string | undefined,
): { range: string; code: string } | undefined {
  const lines = content.replace(/\r\n/g, "\n").replace(/\r/g, "\n").split("\n");
  if (lines.length === 0) return undefined;
  const match = requestedRange ? /^(\d+)-(\d+)$/.exec(requestedRange) : undefined;
  let start = match ? Math.max(1, Number(match[1])) : 1;
  let end = match ? Math.min(lines.length, Number(match[2])) : lines.length;
  if (!Number.isFinite(start) || !Number.isFinite(end) || start > end || start > lines.length) return undefined;
  let code = lines.slice(start - 1, end).join("\n");
  while (Buffer.byteLength(code, "utf8") > MAX_SURFACE_CODE_BYTES && end > start) {
    end -= 1;
    code = lines.slice(start - 1, end).join("\n");
  }
  if (Buffer.byteLength(code, "utf8") > MAX_SURFACE_CODE_BYTES) {
    code = Buffer.from(code, "utf8").subarray(0, MAX_SURFACE_CODE_BYTES).toString("utf8");
  }
  return { range: `${start}-${end}`, code };
}

function closureUnionRange(a: string, b: string): string | undefined {
  const ma = /^(\d+)-(\d+)$/.exec(a);
  const mb = /^(\d+)-(\d+)$/.exec(b);
  if (!ma || !mb) return undefined;
  const aStart = Number(ma[1]);
  const aEnd = Number(ma[2]);
  const bStart = Number(mb[1]);
  const bEnd = Number(mb[2]);
  if (aEnd + 1 < bStart || bEnd + 1 < aStart) return undefined;
  return `${Math.min(aStart, bStart)}-${Math.max(aEnd, bEnd)}`;
}

function closureCanMerge(result: TaskPackResult, extraCode: string): boolean {
  return Buffer.byteLength(JSON.stringify(result), "utf8") + Buffer.byteLength(extraCode, "utf8")
    <= RECURSIVE_READ_CLOSURE_MAX_MERGED_BYTES;
}

function executeClosureRead(
  result: TaskPackResult,
  call: ContinuationCall,
  workspace: string,
): { evidence: number; handle?: string } {
  const args = call.arguments;
  const requestedRange = typeof args["range"] === "string" ? args["range"] : undefined;
  const targets: Array<{ path: string; range?: string; handle?: string }> = [];
  const pushHandle = (id: string): void => {
    const entry = handleTable.get(id);
    if (!entry?.path || resolveReal(entry.workspaceRoot) !== resolveReal(workspace)) return;
    targets.push({ path: entry.path, range: requestedRange ?? entry.range, handle: id });
  };
  if (typeof args["handle"] === "string") pushHandle(args["handle"]);
  if (Array.isArray(args["handles"])) {
    for (const id of args["handles"]) if (typeof id === "string") pushHandle(id);
  }
  if (typeof args["path"] === "string") {
    targets.push({ path: args["path"], range: requestedRange });
  }
  if (Array.isArray(args["paths"])) {
    for (const item of args["paths"]) {
      if (typeof item === "string") targets.push({ path: item });
      else if (item && typeof item === "object" && typeof (item as Record<string, unknown>)["path"] === "string") {
        const obj = item as Record<string, unknown>;
        targets.push({
          path: obj["path"] as string,
          range: typeof obj["range"] === "string" ? obj["range"] : undefined,
        });
      }
    }
  }

  let evidence = 0;
  let receiptHandle: string | undefined;
  for (const target of targets.slice(0, RECURSIVE_READ_CLOSURE_MAX_BRANCHES)) {
    const content = probeFileText(workspace, target.path);
    if (content === undefined) continue;
    const served = closureSlice(content, target.range);
    if (!served || !closureCanMerge(result, served.code)) continue;
    const existing = codeTaskPackSurfaces(result.surfaces).find((surface) =>
      surface.handle === target.handle || (
        surface.path === target.path
        && surface.range === served.range
      )
    );
    if (existing) {
      if (existing.code === undefined) {
        existing.code = elideDocComments(served.code, languageForPath(target.path));
        existing.range = served.range;
      } else if (target.range && existing.remaining_ranges?.includes(target.range)) {
        const union = closureUnionRange(existing.range, target.range);
        const merged = union ? closureSlice(content, union) : undefined;
        if (!merged || merged.range !== union || !closureCanMerge(result, merged.code)) continue;
        existing.range = merged.range;
        existing.code = elideDocComments(merged.code, languageForPath(target.path));
        existing.remaining_ranges = existing.remaining_ranges.filter((range) => range !== target.range);
        if (existing.remaining_ranges.length === 0) {
          delete existing.remaining_ranges;
          delete existing.content_completeness;
        }
        const refreshed = handleTable.upsert({
          kind: existing.symbol ? "symbol" : "range",
          path: existing.path,
          range: existing.range,
          ...(existing.symbol ? { symbol: existing.symbol } : {}),
          workspaceRoot: workspace,
          sha: shaOfText(merged.code),
        });
        existing.handle = refreshed.id;
        receiptHandle = refreshed.id;
      } else {
        continue;
      }
      receiptHandle ??= existing.handle;
    } else {
      const handle = handleTable.upsert({
        kind: "range",
        path: target.path,
        range: served.range,
        workspaceRoot: workspace,
        sha: shaOfText(served.code),
      });
      (result.surfaces as TaskPackResultSurface[]).push({
        role: classifySurface(target.path),
        handle: handle.id,
        path: target.path,
        range: served.range,
        required: true,
        why: "recursive-read-closure",
        code: elideDocComments(served.code, languageForPath(target.path)),
      });
      receiptHandle = handle.id;
    }
    evidence += 1;
  }
  return { evidence, ...(receiptHandle ? { handle: receiptHandle } : {}) };
}

function executeClosureSearch(
  result: TaskPackResult,
  call: ContinuationCall,
  workspace: string,
  originalQuery: string,
  scopePaths?: readonly string[],
): number {
  const args = call.arguments;
  const seed = Array.isArray(args["queries"])
    ? args["queries"].filter((item): item is string => typeof item === "string").join(" ")
    : typeof args["query"] === "string"
      ? args["query"]
      : originalQuery;
  const terms = repairedNextCallTerms(seed);
  if (terms.length === 0) return 0;
  const wiringSemanticSearch = result.task_profile === "wiring";
  const wiringReferenceSearch = wiringSemanticSearch && args["action"] === "references";
  const evidenceQuery = wiringReferenceSearch ? originalQuery : seed;
  const contextTerms = wiringReferenceSearch
    ? tokenizeForEpoch(originalQuery)
        .filter((term) => term.length >= 4 && !NEXT_CALL_GENERIC_QUERY_TERMS.has(term))
        .slice(0, 32)
    : [];
  const compoundTerms = wiringReferenceSearch
    ? (() => {
        const words = tokenizeForEpoch(originalQuery).filter((term) => term.length >= 3);
        const compounds = new Set<string>();
        for (let index = 0; index + 1 < words.length; index++) {
          const left = words[index]!.toLowerCase().replace(/[-_]/g, "");
          const right = words[index + 1]!.toLowerCase().replace(/[-_]/g, "");
          compounds.add(left + right);
          compounds.add((left === "system" ? "sys" : left) + right);
        }
        return [...compounds].filter((term) => term.length >= 6).slice(0, 64);
      })()
    : [];
  const scopes = (typeof args["path"] === "string" ? [args["path"]] : scopePaths ?? [])
    .map((entry) => path.posix.normalize(entry.replace(/\\/g, "/")).replace(/^\.\//, ""))
    .filter((entry) => entry.length > 0);
  const existingKeys = new Set(
    codeTaskPackSurfaces(result.surfaces).map((surface) => `${surface.path}:${surface.range}`),
  );
  const ranked: Array<{
    path: string;
    range: string;
    line: number;
    code: string;
    score: number;
    symbol?: string;
  }> = [];

  const pathRankTerms = [...new Set([...terms, ...contextTerms])];
  const closureFiles = walkCodeFiles(workspace, {})
    .filter((file) =>
      scopes.length === 0
      || scopes.some((scope) =>
        file.relPath === scope || file.relPath.startsWith(`${scope.replace(/\/$/, "")}/`)
      )
    )
    .sort((a, b) => {
      const aPath = a.relPath.toLowerCase();
      const bPath = b.relPath.toLowerCase();
      const aHits = pathRankTerms.filter((term) => aPath.includes(term.toLowerCase())).length;
      const bHits = pathRankTerms.filter((term) => bPath.includes(term.toLowerCase())).length;
      return bHits - aHits || a.relPath.localeCompare(b.relPath);
    })
    .slice(0, READINESS_PROBE_MAX_FILES);

  for (const file of closureFiles) {
    const content = probeFileText(workspace, file.relPath);
    if (content === undefined) continue;
    // Wiring closure is proof construction, not a raw text search. Strip
    // comments before hit counting and evidence selection so prose-only
    // decoys cannot create semantic surfaces or consume the closure budget.
    const searchableContent = wiringSemanticSearch ? relationCodeOnly(content) : content;
    const lower = searchableContent.toLowerCase();
    const hitCount = terms.filter((term) => lower.includes(term.toLowerCase())).length;
    if (hitCount === 0) continue;
    const selected = wiringReferenceSearch
      ? selectCallableClosureEvidence(
          searchableContent,
          file.relPath,
          [...new Set([...terms, ...contextTerms])],
          contextTerms,
          compoundTerms,
        )
        ?? selectQueryEvidence(searchableContent, evidenceQuery, { path: file.relPath, windowLines: 18 })
      : selectQueryEvidence(searchableContent, evidenceQuery, { path: file.relPath, windowLines: 18 });
    let range: string;
    let code: string;
    let line: number;
    let score: number;
    if (selected) {
      ({ range, content: code, line, score } = selected);
    } else {
      const lines = searchableContent.replace(/\r\n/g, "\n").replace(/\r/g, "\n").split("\n");
      const index = lines.findIndex((candidate) =>
        terms.some((term) => candidate.toLowerCase().includes(term.toLowerCase()))
      );
      if (index < 0) continue;
      const start = Math.max(1, index - 5 + 1);
      const end = Math.min(lines.length, index + 5 + 1);
      range = `${start}-${end}`;
      code = lines.slice(start - 1, end).join("\n");
      line = index + 1;
      score = hitCount;
    }
    if (existingKeys.has(`${file.relPath}:${range}`)) continue;
    const contextHits = contextTerms.filter((term) => lower.includes(term.toLowerCase())).length;
    const basename = normIdent(path.basename(file.relPath, path.extname(file.relPath)));
    const endpointDefinitionPenalty = wiringReferenceSearch
      && terms.some((term) => basename === normIdent(term))
      ? 40
      : 0;
    const implementationScore = wiringReferenceSearch
      ? isImplementationPath(file.relPath) ? 8 : -8
      : 0;
    ranked.push({
      path: file.relPath,
      range,
      line,
      code,
      score: score + hitCount * 4 + contextHits * 3 + implementationScore - endpointDefinitionPenalty,
      symbol: inferCallableSymbol(content, line),
    });
  }

  ranked.sort((a, b) => b.score - a.score || a.path.localeCompare(b.path) || a.line - b.line);
  let added = 0;
  for (const candidate of ranked) {
    if (added >= RECURSIVE_READ_CLOSURE_MAX_BRANCHES) break;
    const code = elideDocComments(candidate.code, languageForPath(candidate.path));
    if (!closureCanMerge(result, code)) break;
    const handle = handleTable.upsert({
      kind: "range",
      path: candidate.path,
      range: candidate.range,
      ...(candidate.symbol ? { symbol: candidate.symbol } : {}),
      workspaceRoot: workspace,
      sha: shaOfText(candidate.code),
    });
    (result.surfaces as TaskPackResultSurface[]).push({
      role: classifySurface(candidate.path),
      handle: handle.id,
      path: candidate.path,
      range: candidate.range,
      ...(candidate.symbol ? { symbol: candidate.symbol } : {}),
      required: true,
      why: "recursive-read-closure",
      code,
    });
    existingKeys.add(`${candidate.path}:${candidate.range}`);
    added += 1;
  }
  return added;
}

function refreshAfterRecursiveClosure(
  result: TaskPackResult,
  profile: TaskProfile,
  query: string,
  workspace: string,
  cache?: FileReadCache,
): void {
  const surfaces = codeTaskPackSurfaces(result.surfaces);
  const roles = new Set(surfaces.map((surface) => surface.role));
  result.missing = result.missing.filter((item) => !roles.has(item));
  if (result.missing_required_surfaces) {
    result.missing_required_surfaces = result.missing_required_surfaces.filter((item) => !roles.has(item));
  }
  let unmatched = unmatchedConcernTokens(query, surfaces);
  let unresolvedMultiConcerns = false;
  if (profile === "multi_concern" && result.concerns) {
    const groups = concernGroupsForQuery(query);
    for (const [index, concern] of result.concerns.entries()) {
      if (concern.status === "covered") continue;
      if (result.concern_ambiguities?.some((ambiguity) => ambiguity.id === concern.id)) continue;
      const group = groups[index];
      if (!group) continue;
      const evidenceTokens = group.tokens.filter((token) =>
        // R0 (2026-08-21, W4-A): compare case-insensitively — group.tokens
        // (concernAnchorTokens) may now carry a case-preserved identifier
        // (e.g. "buildInitializeInstructions") since orderedUniqueTokens
        // stopped silently lowercasing its output; META_CONCERN_EVIDENCE_TOKENS
        // is lowercase-only.
        !META_CONCERN_EVIDENCE_TOKENS.has(token.toLowerCase())
      );
      const matches = surfaces.filter((surface) =>
        evidenceTokens.some((token) => concernTokenMatchesSurface(token, surface))
      );
      if (matches.length > 0) {
        concern.status = "covered";
        concern.handles = [...new Set(matches.map((surface) => surface.handle))];
      }
    }
    unresolvedMultiConcerns = result.concerns.some((concern) => concern.status === "needs-followup");
    if (unresolvedMultiConcerns) {
      result.coverage = "partial";
      result.coverage_reason = "concerns-uncovered";
    }
  }
  if (
    !unresolvedMultiConcerns
    && result.missing.length === 0
    && unmatched.length === 0
    && (result.coverage_reason === "missing-roles" || result.coverage_reason === "concerns-uncovered")
  ) {
    result.coverage = "focused";
    delete result.coverage_reason;
    delete result.blocking_next_steps;
  } else if (
    !unresolvedMultiConcerns
    && result.coverage_reason === "missing-roles"
    && result.missing.length === 0
    && (result.missing_required_surfaces?.length ?? 0) === 0
  ) {
    // B1b (2026-08-01 retrieval-scope): the closure covered every role the
    // filters above could name, but an unmatched CONCERN token still holds the
    // pack partial — so the "missing-roles" label now names nothing. That exact
    // combination shipped `partial + missing-roles + missing:[]` live (run
    // 2026-07-31-semantic-signal5-2, T09 rep1/rep2). Report the reason that is
    // actually true; reconcileMissingRolesContract enforces the same invariant
    // at the shared exit for every other producer.
    result.coverage_reason = "concerns-uncovered";
  }

  if (profile === "wiring") {
    result.wiring = buildTaskWiringProfile(query, result.surfaces, workspace, cache);
    pruneIrrelevantWiringImplementationSurfaces(result);
    surfaces.splice(0, surfaces.length, ...codeTaskPackSurfaces(result.surfaces));
    unmatched = unmatchedConcernTokens(query, surfaces);
    if (result.wiring.status === "ready" && result.wiring.evidence_graph !== undefined) {
      // Recursive closure recomputes the generic role inventory. Neutral
      // directory names can reintroduce contract/api gaps even though the
      // wiring graph still proves the producer, consumer, adapter, and
      // callable insertion site. The graph is the authoritative proof for a
      // wiring task, so do not send the agent back through filename-driven
      // discovery after that proof is complete.
      result.missing = [];
      delete result.missing_required_surfaces;
      if (
        result.coverage_reason === "candidate-list"
        || result.coverage_reason === "missing-roles"
      ) {
        result.coverage = "focused";
        delete result.coverage_reason;
        delete result.blocking_next_steps;
      }
    }
  }
  reconcileContentSufficiency(result);
  // 2026-07-31: `missing_required_surfaces` is omitted from the wire whenever
  // missingRequired is EMPTY, so an ABSENT field means "nothing is required" —
  // it must not fall back to the union. `result.missing` mixes surface ROLES
  // with unresolvable caller inputs (nonexistent paths, unexpanded directory
  // notes); feeding those here produced `missing required surface(s): <a path>`
  // and, 30 lines below, a `surfaceRoles=[<a path>]` next_call that no pack can
  // ever satisfy — the caller re-ran it forever. Elsewhere in this file the same
  // expression is already spelled `?? []`; this call site was the lone exception.
  result.route = buildRoute(
    surfaces,
    result.missing_required_surfaces ?? [],
    result.coverage,
    result.missing,
    query,
    result.coverage_reason,
  );
  if (unresolvedMultiConcerns) {
    const unresolvedIds = result.concerns
      ?.filter((concern) => concern.status === "needs-followup")
      .map((concern) => concern.id) ?? [];
    result.route = {
      action: "locate_missing_surfaces",
      reason: `unresolved concern(s): ${unresolvedIds.join(",")}`,
      max_additional_tl_calls: Math.ceil(unresolvedIds.length / MAX_STAGE_CALLS),
    };
  }
  if (profile === "answer" && result.route.action === "edit_from_handles") {
    result.route = {
      action: "answer_from_handles",
      reason: "recursive read-only closure bundled the answer evidence",
      max_additional_tl_calls: 0,
    };
  }
  // Build the change contract after the refreshed route. A wiring task can
  // become edit-ready only during recursive closure; building against the
  // preceding locate route would omit the otherwise-ready obligations.
  if (query.length > 0 && shouldBuildTaskChangeContract(query, result, profile)) {
    result.change_contract = buildTaskChangeContract(query, result, workspace);
  }
  reconcileTaskChangeContract(result);
  const next = nextHintForCoverage(
    surfaces,
    result.coverage,
    result.coverage_reason,
    query,
    // Roles only: this parameter is what the missing-roles branch turns into
    // `surfaceRoles=[...]`. See the buildRoute call above for why
    // `result.missing` is the wrong array to hand it.
    result.missing_required_surfaces ?? [],
    unmatched,
  );
  if (next) result.next = next;
  else delete result.next;
}

/**
 * Bounded candidate-list expansion (2026-07-19a T13 rep0): a candidate-list
 * pack used to break out of closure with CODELESS candidates and no stop
 * condition — the most ambiguous shape got the least server help, and the
 * model thrashed full/slice/search for 109 turns ($6.15 vs $2.21 paired).
 * One pass inlines the ranked candidates' own bodies
 * (<= RECURSIVE_READ_CLOSURE_MAX_BRANCHES, under the closureCanMerge byte
 * cap) into THIS response so "here are N names" becomes "here are N bodies —
 * choose". Never recurses: after the pass the pack stops explicitly (see
 * buildTaskExecutionContract's choose-candidate close).
 */
function runCandidateInlinePass(
  result: TaskPackResult,
  profile: TaskProfile,
  query: string,
  workspace: string,
  receipt: NonNullable<TaskPackResult["internalized"]>,
  cache?: FileReadCache,
): number {
  const codeless = [...new Set(
    codeTaskPackSurfaces(result.surfaces)
      .filter((surface) => !hasServedCode(surface))
      .map((surface) => surface.handle),
  )].slice(0, RECURSIVE_READ_CLOSURE_MAX_BRANCHES);
  if (codeless.length === 0) return 0;
  const used = executeClosureRead(
    result,
    { tool: "read_file", arguments: { handles: codeless } },
    workspace,
  );
  if (used.evidence > 0) {
    receipt.push({
      op: "read",
      status: "used",
      evidence: used.evidence,
      ...(used.handle ? { handle: used.handle } : {}),
    });
    refreshAfterRecursiveClosure(result, profile, query, workspace, cache);
  }
  return 1;
}

function isCandidateListPack(result: TaskPackResult): boolean {
  return result.route?.action === "confirm_candidates" || result.coverage_reason === "candidate-list";
}

const DUPLICATE_SYMPTOM_RE = /\b(?:double|duplicate|duplicated|twice)\b|重複|二重|[2２]\s*件/iu;

/**
 * A duplicate-event report can disambiguate itself: when one served body is
 * the only candidate that repeats the caller's all-caps event identifier, it
 * is the defect site rather than a definition or display consumer.
 */
function resolveServedDuplicateCandidate(
  result: TaskPackResult,
  profile: TaskProfile,
  query: string,
): boolean {
  if (profile === "answer" || !isCandidateListPack(result) || !DUPLICATE_SYMPTOM_RE.test(query)) {
    return false;
  }
  const identifiers = explicitCodeIdentifiers(query)
    .filter((identifier) => /^[A-Z][A-Z0-9_]{3,}$/.test(identifier));
  if (identifiers.length === 0) return false;
  const candidates = codeTaskPackSurfaces(result.surfaces).filter(hasServedCode);
  const scored = candidates.map((surface) => {
    const body = surface.code ?? surface.code_unchanged ?? "";
    const repeats = Math.max(0, ...identifiers.map((identifier) =>
      body.split(identifier).length - 1
    ));
    return { surface, repeats };
  });
  const maxRepeats = Math.max(0, ...scored.map(({ repeats }) => repeats));
  const winners = scored.filter(({ repeats }) => repeats === maxRepeats);
  if (maxRepeats < 2 || winners.length !== 1) return false;

  const winner = winners[0]!.surface;
  winner.required = true;
  result.surfaces = result.surfaces.filter((surface) =>
    isArtifactTaskPackSurface(surface) || surface.handle === winner.handle
  );
  result.coverage = "focused";
  delete result.coverage_reason;
  result.missing = [];
  delete result.missing_required_surfaces;
  delete result.blocking_next_steps;
  delete result.concern_ambiguities;
  delete result.next;
  delete result.continuation;
  result.checks = [];
  return true;
}

export function runRecursiveReadOnlyClosure(
  result: TaskPackResult,
  profile: TaskProfile,
  query: string,
  workspace: string,
  cache?: FileReadCache,
  scopePaths?: readonly string[],
): number {
  // D10 (2026-08-14): `TL_RECURSIVE_READ_CLOSURE` is deleted.
  if (query.trim().length === 0) return 0;
  if ((result.concern_ambiguities?.length ?? 0) > 0) return 0;
  const seen = new Set<string>();
  const receipt: NonNullable<TaskPackResult["internalized"]> = [];
  let operations = 0;

  // Candidate-list packs get exactly one bounded inline pass and an explicit
  // stop — never the open-ended discovery loop (whose next_call re-derivation
  // is what the 2026-07-19a thrash fed on). Runs for EVERY profile: ambiguity
  // is where the anti-thrash help matters most.
  if (isCandidateListPack(result)) {
    operations += runCandidateInlinePass(result, profile, query, workspace, receipt, cache);
    if (resolveServedDuplicateCandidate(result, profile, query)) {
      refreshAfterRecursiveClosure(result, profile, query, workspace, cache);
    }
    if (receipt.length > 0) result.internalized = receipt.slice(0, RECURSIVE_READ_CLOSURE_MAX_OPERATIONS);
    return operations;
  }

  // Deep recursive discovery is limited to structurally multi-site profiles
  // (or an explicit caller-pinned taskProfile) — see
  // RECURSIVE_CLOSURE_DEEP_PROFILES.
  if (
    result.profile_binding?.source !== "explicit"
    && !RECURSIVE_CLOSURE_DEEP_PROFILES.has(profile)
  ) {
    return operations;
  }

  for (let depth = 0; depth < RECURSIVE_READ_CLOSURE_MAX_DEPTH; depth++) {
    if (isCandidateListPack(result)) {
      // A refresh flipped the pack INTO a candidate list mid-loop: same
      // bounded inline + explicit stop as the entry path.
      operations += runCandidateInlinePass(result, profile, query, workspace, receipt, cache);
      break;
    }
    const contract = buildTaskExecutionContract(
      result, profile, query, buildTaskWorkspaceState(result), workspace,
    );
    if (
      contract.typestate.phase !== "discovery"
      || contract.call_budget?.discovery_allowed !== true
      || contract.next_call === undefined
    ) {
      break;
    }
    const candidates: ContinuationCall[] = [
      contract.next_call as ContinuationCall,
      ...(result.continuation?.stages[0]?.calls ?? []),
    ];
    const admitted: ContinuationCall[] = [];
    for (const candidate of candidates) {
      const call = admitReadOnlyNextCall(candidate, query);
      if (!call) continue;
      const signature = closureCallSignature(call);
      if (seen.has(signature)) continue;
      seen.add(signature);
      admitted.push(call);
      if (admitted.length >= RECURSIVE_READ_CLOSURE_MAX_BRANCHES) break;
    }
    if (admitted.length === 0) break;

    let stageEvidence = 0;
    for (const call of admitted) {
      if (operations >= RECURSIVE_READ_CLOSURE_MAX_OPERATIONS) break;
      operations += 1;
      if (call.tool === "read_file") {
        const used = executeClosureRead(result, call, workspace);
        if (used.evidence > 0) {
          receipt.push({
            op: "read",
            status: "used",
            evidence: used.evidence,
            ...(used.handle ? { handle: used.handle } : {}),
          });
          stageEvidence += used.evidence;
        }
      } else if (call.tool === "search_files") {
        const evidence = executeClosureSearch(result, call, workspace, query, scopePaths);
        if (evidence > 0) {
          receipt.push({ op: "find", status: "used", evidence });
          stageEvidence += evidence;
        }
      }
    }
    if (stageEvidence === 0) break;
    refreshAfterRecursiveClosure(result, profile, query, workspace, cache);
    if (operations >= RECURSIVE_READ_CLOSURE_MAX_OPERATIONS) break;
  }

  if (receipt.length > 0) result.internalized = receipt.slice(0, RECURSIVE_READ_CLOSURE_MAX_OPERATIONS);
  return operations;
}

/**
 * B1d (2026-08-01 retrieval-scope): is a facet's evidence STILL unserved?
 *
 * True when the surface is disclosed-partial AND the file holds a collaborator
 * CALL SITE for `facet` that this surface's served text does not contain. Live
 * forensics (run 2026-07-31-semantic-signal5-2, T03): the "shipping" facet
 * matched a shipping-COST line inside the already-served head, so the pack
 * claimed the facets were bundled and dropped `next`/`continuation` — while the
 * `shippingService.*` calls the question was actually about sat entirely in
 * remaining_ranges. A lexical hit inside what is already served proves nothing
 * new when the behavior is provably elsewhere.
 *
 * Needs the workspace to read the file; without one the check is skipped
 * (pre-B1d behavior), so no caller is forced to thread it through.
 */
function facetEvidenceStillUnserved(
  surface: TaskPackSurface,
  facet: string,
  workspace: string,
): boolean {
  const partial = surface.content_completeness === "partial"
    || (surface.remaining_ranges?.length ?? 0) > 0;
  if (!partial) return false;
  const isCallSite = (line: string): boolean =>
    !/^\s*(?:\/\/|\/\*|\*|#)/.test(line)
    && hasIdentifierSegment(line, facet)
    && isFacetCallSiteLine(line, facet);
  // The facet's behavior IS demonstrated when the SERVED text itself carries a
  // call site for it. Further call sites elsewhere in a large class are more of
  // the same evidence, not a gap — this is the line between "answer the
  // question" and "dump the class", and crossing it would reopen discovery on
  // every partially-served answer pack.
  const servedText = servedSurfaceText(surface);
  if (servedText.split("\n").some(isCallSite)) return false;
  // Served evidence is a bare lexical mention (T03: a shipping-COST field).
  // A call site the pack did NOT serve is then the actual evidence gap.
  const content = probeFileText(workspace, surface.path);
  if (content === undefined) return false;
  for (const line of content.replace(/\r\n/g, "\n").split("\n")) {
    if (!isCallSite(line)) continue;
    if (!servedText.includes(line.trim())) return true;
  }
  return false;
}

function hasExactNamedAnswerFacetEvidence(
  result: TaskPackResult,
  query: string,
  workspace?: string,
): boolean {
  const facets = requiredQueryFacets(query);
  if (facets.length === 0) return false;
  const targetNames = new Set(explicitCodeIdentifiers(query).map(normIdent));
  return codeTaskPackSurfaces(result.surfaces).some((surface) =>
    targetNames.has(normIdent(path.basename(surface.path, path.extname(surface.path))))
    && hasCallableBody(surface)
    && facets.every((facet) =>
      surfaceContainsToken(surface, facet)
      // B1d: a facet whose call sites are still unserved is NOT bundled, no
      // matter how many times the word appears in what WAS served.
      && !(workspace !== undefined && facetEvidenceStillUnserved(surface, facet, workspace))
    )
  );
}

/**
 * Broad analysis/review wording is intentionally read-only, but it is also
 * easy to satisfy accidentally with a same-word source file or documentation
 * decoy.  Only caller-scoped evidence, an extracted artifact, or an explicit
 * code identifier can close analysis discovery.  Unknown-scope analysis stays
 * in discovery and receives one bounded locate call instead of a false ready
 * certificate over arbitrary token matches.
 */
function groundedAnalysisEvidence(
  result: TaskPackResult,
  query: string,
  served: readonly TaskPackSurface[],
): TaskReadinessEvidence[] {
  const evidence: TaskReadinessEvidence[] = [];
  for (const surface of served) {
    if (/(?:^|; )caller-supplied(?:-dir)?$/.test(surface.why ?? "")) {
      evidence.push(readinessEvidence(surface));
    }
  }
  for (const surface of result.surfaces.filter(isArtifactTaskPackSurface)) {
    evidence.push({ handle: surface.handle, path: surface.path });
  }
  for (const identifier of explicitCodeIdentifiers(query).slice(0, 6)) {
    evidence.push(...exactIdentifierEvidence(served, identifier));
  }
  const seen = new Set<string>();
  return evidence.filter((item) => {
    const key = `${item.handle}\u0000${item.path}\u0000${item.range ?? ""}`;
    if (seen.has(key)) return false;
    seen.add(key);
    return true;
  }).slice(0, 6);
}

function buildReadinessObligations(
  result: TaskPackResult,
  profile: TaskProfile,
  query: string,
  openUniverseDischarged = false,
  /** I-7: workspace for the trace-only enumerated_obligation_summary emission below; omitted by the readiness specs that build synthetic results with no real filesystem — same optionality convention buildTaskExecutionContract's own `workspace` param already uses. */
  workspace?: string,
): TaskReadinessObligation[] {
  const surfaces = codeTaskPackSurfaces(result.surfaces);
  const served = surfaces.filter(hasServedCode);
  const obligations: TaskReadinessObligation[] = [];
  const focusedAnswerEvidence = profile === "answer"
    && (result.answer_resolution !== undefined || hasExactNamedAnswerFacetEvidence(result, query));
  const provedArtifactCreate = profile === "artifact_build"
    && result.create_target !== undefined
    && hasRequiredArtifactSections(result, query);
  // W3 (turn-economy wave 2): a resolved create_target on a NON-artifact pack is
  // itself the proved frontier — the new file's path came from the caller's
  // explicit request and its parent directory exists, so nothing more needs to
  // be discovered before creating it (any served surfaces are templates, not
  // required edit content). Without this, a create-intent pack's related-context
  // surfaces are judged "codeless/uncovered", the contract falls to
  // needs-followup, and reconcileTaskPackExecutionContract demotes the create
  // route back to "go search for the file that doesn't exist yet". Scoped to
  // non-artifact profiles so artifact_build keeps its stricter section proof.
  const provedCreate = profile !== "artifact_build" && result.create_target !== undefined;
  // A1 (2026-07-30): a pack whose action-bearing evidence IS artifact content —
  // no code surface was identified at all, nothing required is missing, and every
  // caller-named artifact carries a MEANINGFUL inlined section — has proved
  // surface-content. Same principle as provedArtifactCreate above (extracted
  // artifact sections are content-bearing proof even though they are not code),
  // generalized to the read-only audit shape that has no create target. Without
  // it the T14 two-deck audit certified `awaiting-input` / "request user input"
  // while holding both decks' text, which is strictly worse than the round trip
  // inlining saves. Gated on `surfaces.length === 0` so it can never mask a
  // codeless CODE surface — the case this obligation exists to catch.
  const provedArtifactContent = surfaces.length === 0
    && result.missing.length === 0
    && (result.artifact_sections?.length ?? 0) > 0
    && hasRequiredArtifactSections(result, query);
  // 2026-07-16a: optional enrichment surfaces (required:false — e.g. the C/C++
  // header-source pair sibling, why="header-source-pair") must not veto
  // readiness: a codeless OPTIONAL surface forced risk=1 on every C++ pack.
  // `result.missing` stays unfiltered — it is required-role-scoped by
  // construction (coverage_reason "missing-roles" docs).
  const contentComplete = provedArtifactCreate || provedCreate || provedArtifactContent || (
    served.length > 0
    && result.missing.every((entry) => entry.startsWith("explicit-gap:"))
    && surfaces.filter((surface) => surface.required !== false).every(hasServedCode)
    && (result.content_sufficiency !== "needs-followup" || focusedAnswerEvidence)
  );
  obligations.push({
    id: "surface-content",
    kind: "surface-content",
    status: contentComplete ? "proved" : "uncovered",
    required: true,
    evidence: provedArtifactCreate || provedArtifactContent
      ? result.surfaces.filter(isArtifactTaskPackSurface).map((surface) => ({
          handle: surface.handle,
          path: surface.path,
        })).slice(0, 6)
      : served.map(readinessEvidence).slice(0, 6),
    reason: contentComplete
      ? provedCreate
        ? `new-file create target ${result.create_target!.path} resolved from the request; served surfaces are templates`
        : provedArtifactContent
          ? "every caller-named artifact carries an inlined content-bearing section"
          : "all action-bearing surfaces carry served content"
      : "one or more required surfaces are missing or codeless",
  });

  // W3: a resolved (non-artifact) create_target is a self-complete frontier —
  // the identifiers/behavior/change obligations below describe content the NEW
  // file will contain, not pre-existing evidence that must be served first.
  // Requiring them would keep the contract needs-followup and demote the create
  // route to "search for the file that doesn't exist yet". The single proved
  // surface-content obligation above IS the create task's proof.
  if (provedCreate) return obligations;

  if (profile === "answer") {
    if (hasAnalysisIntentMarker(query)) {
      const evidence = groundedAnalysisEvidence(result, query, served);
      obligations.push({
        id: "analysis-source",
        kind: "surface-content",
        status: evidence.length > 0 ? "proved" : "uncovered",
        required: true,
        evidence,
        reason: evidence.length > 0
          ? "analysis is grounded in caller-scoped, artifact, or explicit-identifier evidence"
          : "analysis has only unscoped token matches; locate the requested evidence before answering",
      });
    }
    for (const identifier of explicitCodeIdentifiers(query).slice(0, 6)) {
      const exactEvidence = exactIdentifierEvidence(served, identifier);
      const semanticEvidence = result.answer_resolution
        && normIdent(result.answer_resolution.requested_identifier) === normIdent(identifier)
        ? served
            .filter((surface) => surface.handle === result.answer_resolution!.resolved_handle)
            .map(readinessEvidence)
        : [];
      const evidence = exactEvidence.length > 0 ? exactEvidence : semanticEvidence;
      obligations.push({
        id: `identifier:${identifier}`,
        kind: "explicit-identifier",
        status: evidence.length > 0 || verifiedAbsentIdentifiersFor(result).includes(identifier) ? "proved" : "uncovered",
        required: true,
        evidence,
        reason: evidence.length > 0
          ? exactEvidence.length > 0
            ? `explicit identifier ${identifier} is present in served evidence`
            : `requested identifier ${identifier} resolves to the served multi-facet responsibility owner`
          : verifiedAbsentIdentifiersFor(result).includes(identifier)
            ? `explicit identifier ${identifier} has zero literal occurrences in the workspace (verified absent — answer from that fact or correct the name)`
            : `explicit identifier ${identifier} has no served evidence`,
      });
    }
    if (BEHAVIOR_QUERY_RE.test(query)) {
      const callable = served.filter(hasCallableBody);
      const facets = requiredQueryFacets(query);
      const missingFacets = facets.filter((facet) => !callable.some((surface) => surfaceContainsToken(surface, facet)));
      const proved = callable.length > 0 && missingFacets.length === 0;
      obligations.push({
        id: "behavior-body",
        kind: "behavior-body",
        status: proved ? "proved" : "uncovered",
        required: true,
        evidence: callable.map(readinessEvidence).slice(0, 6),
        reason: proved
          ? "callable implementation evidence covers the requested behavior"
          : missingFacets.length > 0
            ? `callable evidence is missing query facets: ${missingFacets.join(",")}`
            : "behavioral question has no callable implementation body",
      });
    }
    // F-V13-6: one obligation per enumerated item, minted independently of the
    // behavior-body branch above so a checklist with no behavior wording is
    // still represented. `kind:"concern"` is the existing wire vocabulary for
    // "a requirement the query raised that needs its own served evidence"; the
    // id prefix keeps the items individually addressable, which is what puts
    // each unproved one on the wire by NAME (falsification.unresolved and the
    // capability gap's obligation_ids).
    //
    // BODY-SCOPED PROOF (surfaceBodyContainsToken, not surfaceContainsToken):
    // this obligation exists because a served RANGE was treated as evidence for
    // a question it never covered, so proving it from the file PATH would
    // recreate the same vacuity one level down — the exact reason
    // surfaceBodyContainsToken was split out for proof obligations in the first
    // place.
    const enumeratedFacets = enumeratedQueryFacets(query);
    if (enumeratedFacets.length > 0) {
      // DISCHARGE BY NAMED AUTHORITY — the line between "no evidence for what
      // was asked" and "proof of a negative".
      //
      // When the request NAMES an implementation and the pack served that
      // body in full, an item the body does not mention is already answered by
      // the served bytes ("it never returns choose-candidate"). Requiring a
      // literal occurrence there would demand the server prove a negative and
      // would reopen discovery on a complete, correctly-targeted pack —
      // observed against replayCorpus's `buildTaskExecutionContract returns
      // answer-ready, edit-ready, choose-candidate, or needs-followup` entry,
      // where two of the four states genuinely do not exist in the function.
      // The defect this obligation exists for has no such anchor: nothing the
      // request enumerated appears anywhere in the served bytes, and no
      // identifier it named was served whole.
      const queryIdentifiers = explicitCodeIdentifiers(query);
      const namedAuthorityServed = queryIdentifiers.length > 0
        && served.some((surface) =>
          surface.content_completeness !== "partial"
          && queryIdentifiers.some((identifier) => surfaceBodyContainsToken(surface, identifier)));
      // I-7 (2026-08-30 forensics attribution wave): the span of `obligations`
      // this block is about to mint, so the trace emission below can report on
      // exactly those entries without touching the loop that mints them.
      const enumeratedObligationsStart = obligations.length;
      for (const facet of enumeratedFacets) {
        const evidence = served
          .filter((surface) => surfaceBodyContainsToken(surface, facet))
          .map(readinessEvidence);
        obligations.push({
          id: `${ENUMERATED_ITEM_OBLIGATION_PREFIX}${facet}`,
          kind: "concern",
          status: evidence.length > 0 || namedAuthorityServed ? "proved" : "uncovered",
          required: true,
          evidence: evidence.slice(0, 3),
          reason: evidence.length > 0
            ? `enumerated item ${facet} appears in served evidence`
            : namedAuthorityServed
              ? `enumerated item ${facet} is answerable from the fully served implementation the request named`
              : `enumerated item ${facet} was named by the request but has no served evidence`,
        });
      }
      // I-7: observe the minting itself (facet count / unproved count / the
      // named-authority discharge route), not the eventual act/gap decision --
      // T03 forensics needed exactly this and had to hand-reconstruct it from
      // raw transcripts (bench/workflows/experiments/2026-08-30-v0131-forensics/
      // REPORT.md's I-1 section). Dedup to one emission per pack via
      // enumeratedObligationSummaryCountedResults — see its own doc comment.
      // `workspace` is omitted by readiness specs that build synthetic
      // results with no real filesystem, which this silently skips (never
      // throws) rather than trace against a workspace that doesn't exist.
      //
      // PRIVACY: facets are QUERY-DERIVED tokens the caller typed, unlike the
      // workspace-relative PATHS several existing events already emit
      // (edit_hunk_frontier's `path`, repeated_range's `path`,
      // post_edit_readback's `path`) — no existing trace event emits a raw
      // query substring (grepped: only `query_chars`/length and `task_ref`/
      // hash exist), so this follows that precedent rather than the path one:
      // a short content hash (reusing shaOfText, the same primitive
      // handle-content-addressing already uses) plus the char length, never
      // the literal word.
      if (
        isTraceEnabled()
        && workspace !== undefined
        && !enumeratedObligationSummaryCountedResults.has(result)
      ) {
        enumeratedObligationSummaryCountedResults.add(result);
        const minted = obligations.slice(enumeratedObligationsStart);
        trace(
          "enumerated_obligation_summary",
          {
            facet_count: enumeratedFacets.length,
            unproved_count: minted.filter((obligation) => obligation.status === "uncovered").length,
            named_authority_discharge: namedAuthorityServed,
            facets: enumeratedFacets.map((facet) => ({
              hash: shaOfText(facet).slice(7, 15),
              chars: facet.length,
            })),
          },
          workspace,
        );
      }
    }
  }

  // A quantifier names an open universe, not a single identifier fact.  A-5
  // expands it into concrete direct dependencies; until then it is deliberately
  // undischargeable so an exact-location answer cannot falsely close it.
  if (hasOpenUniverseIntent(query) && !isAdditiveEnumIntent(query)) {
    const kind = /enum|列挙/.test(query) ? "enum-variants"
      : /callers|呼び出し元/.test(query) ? "all-callers"
      : /references|参照/.test(query) ? "all-references"
      : "dependency-definitions";
    obligations.push({
      id: `open-universe:${kind}`,
      kind: "surface-content",
      status: openUniverseDischarged ? "proved" : "uncovered",
      required: true,
      evidence: [],
      reason: openUniverseDischarged
        ? `${kind} was discharged by an explicit evidence-expansion capability gap`
        : `${kind} is an exhaustive open-universe requirement awaiting evidence expansion`,
    });
  }

  if (profile === "change_propagation" || profile === "generic") {
    for (const obligation of result.change_contract?.obligations.filter((item) => item.required) ?? []) {
      const evidence = obligation.handle
        ? served.filter((surface) => surface.handle === obligation.handle).map(readinessEvidence)
        : [];
      const proved = obligation.status === "ready" && evidence.length > 0;
      obligations.push({
        id: `change:${obligation.id}`,
        kind: "change-obligation",
        status: proved ? "proved" : "uncovered",
        required: true,
        evidence,
        reason: proved ? (obligation.reason ?? `change obligation ${obligation.id} is ready`) : `change obligation ${obligation.id} lacks edit-grade evidence`,
      });
    }
  }

  if (profile === "multi_concern") {
    const concerns = result.concerns ?? [];
    const uniqueFrontiers = new Set(concerns.map((concern) => [...concern.handles].sort().join(",")));
    for (const concern of concerns) {
      const evidence = served.filter((surface) => concern.handles.includes(surface.handle)).map(readinessEvidence);
      const independent = concerns.length <= 1 || uniqueFrontiers.size > 1;
      const proved = concern.status === "covered" && evidence.length > 0 && independent;
      obligations.push({
        id: `concern:${concern.id}`,
        kind: "concern",
        status: proved ? "proved" : "uncovered",
        required: true,
        evidence,
        reason: proved
          ? "concern has served, handle-backed evidence"
          : independent
            ? "concern lacks served evidence"
            : "multiple concerns share one indistinguishable evidence frontier",
      });
    }
  }

  if (profile === "artifact_build") {
    const artifactEvidence = result.surfaces
      .filter(isArtifactTaskPackSurface)
      .map((surface) => ({ handle: surface.handle, path: surface.path }));
    const requiredArtifactSections = hasRequiredArtifactSections(result, query);
    obligations.push({
      id: "artifact-input",
      kind: "artifact-source",
      status: requiredArtifactSections && artifactEvidence.length > 0 ? "proved" : "uncovered",
      required: true,
      evidence: artifactEvidence,
      reason: requiredArtifactSections && artifactEvidence.length > 0
        ? "all requested artifact sections and source handles are bundled"
        : "one or more requested artifact source sections are not bundled",
    });
    const explicitTargetTokens = moduleShapedQueryTokens(query).map((token) => normIdent(token));
    const implementation = served.filter((surface) =>
      isImplementationPath(surface.path)
      && (
        /query-/.test(surface.why ?? "")
        || hasCallableBody(surface)
        || explicitTargetTokens.some((token) =>
          normIdent(path.basename(surface.path, path.extname(surface.path))) === token
        )
      )
    );
    const createEvidence = result.create_target
      ? artifactEvidence.filter((evidence) => bundledArtifactPaths(result).includes(evidence.path)).slice(0, 4)
      : [];
    obligations.push({
      id: "artifact-implementation",
      kind: "artifact-source",
      status: implementation.length > 0 || createEvidence.length > 0 ? "proved" : "uncovered",
      required: true,
      evidence: implementation.length > 0
        ? implementation.map(readinessEvidence).slice(0, 4)
        : createEvidence,
      reason: implementation.length > 0
        ? "query-grounded implementation target is bundled with the artifact"
        : createEvidence.length > 0
          ? `new-file frontier ${result.create_target!.path} is proved by a unique existing package directory`
        : "artifact is present but its implementation target is not proved",
    });
  }

  if (profile === "wiring") {
    const connection = result.wiring?.connections[0];
    const evidenceGraph = result.wiring?.evidence_graph;
    const sourceToken = connection?.source?.token;
    const destinationToken = connection?.destination?.token;
    const sourceImpl = served.filter((surface) => {
      if (surface.handle !== connection?.source?.handle || !isImplementationPath(surface.path)) return false;
      const token = sourceToken ?? "";
      return implementsCallableToken(servedSurfaceText(surface), token);
    });
    const consumerImpl = served.filter((surface) =>
      surface.handle === connection?.destination?.handle
      && isImplementationPath(surface.path)
      && definedCallableNames(servedSurfaceText(surface))
        .some((name) => normIdent(name) === normIdent(destinationToken ?? ""))
    );
    const insertionImpl = served.filter((surface) =>
      surface.handle === connection?.insertion_handle
      && isImplementationPath(surface.path)
      && hasCallableBody(surface)
      && connection?.evidence.includes("callable-insertion-site") === true
    );
    const sourceNodeIds = new Set(evidenceGraph?.nodes
      .filter((node) => node.handle === connection?.source?.handle)
      .map((node) => node.id) ?? []);
    const consumerNodeIds = new Set(evidenceGraph?.nodes
      .filter((node) => node.handle === connection?.destination?.handle)
      .map((node) => node.id) ?? []);
    const insertionNodeIds = new Set(evidenceGraph?.nodes
      .filter((node) =>
        node.handle === connection?.insertion_handle
        && (node.roles.includes("insertion") || node.roles.includes("consumer"))
      )
      .map((node) => node.id) ?? []);
    const sourceDefineIds = evidenceGraph?.relations
      .filter((relation) => relation.kind === "defines" && sourceNodeIds.has(relation.to))
      .map((relation) => relation.id) ?? [];
    const consumerDefineIds = evidenceGraph?.relations
      .filter((relation) => relation.kind === "defines" && consumerNodeIds.has(relation.to))
      .map((relation) => relation.id) ?? [];
    const insertionDefineIds = evidenceGraph?.relations
      .filter((relation) => relation.kind === "defines" && insertionNodeIds.has(relation.to))
      .map((relation) => relation.id) ?? [];
    const adapterNodeIds = new Set(evidenceGraph?.nodes
      .filter((node) => node.roles.includes("adapter"))
      .map((node) => node.id) ?? []);
    const consumerAdapterRelationIds = evidenceGraph?.relations
      .filter((relation) =>
        (relation.kind === "imports" || relation.kind === "direct_calls")
        && consumerNodeIds.has(relation.from)
        && adapterNodeIds.has(relation.to)
      )
      .map((relation) => relation.id) ?? [];
    const consumerSourceRelationIds = evidenceGraph?.relations
      .filter((relation) =>
        (relation.kind === "imports" || relation.kind === "direct_calls")
        && consumerNodeIds.has(relation.from)
        && sourceNodeIds.has(relation.to)
      )
      .map((relation) => relation.id) ?? [];
    const insertionConsumerRelationIds = evidenceGraph?.relations
      .filter((relation) =>
        relation.kind === "direct_calls"
        && insertionNodeIds.has(relation.from)
        && consumerNodeIds.has(relation.to)
      )
      .map((relation) => relation.id) ?? [];
    const linkRelationIds = [...new Set([
      ...consumerAdapterRelationIds,
      ...consumerSourceRelationIds,
      ...insertionConsumerRelationIds,
    ])];
    const directCallIds = evidenceGraph?.relations
      .filter((relation) =>
        relation.kind === "direct_calls"
        && (
          (consumerNodeIds.has(relation.from)
            && (adapterNodeIds.has(relation.to) || sourceNodeIds.has(relation.to)))
          || (insertionNodeIds.has(relation.from) && consumerNodeIds.has(relation.to))
        )
      )
      .map((relation) => relation.id) ?? [];
    const relationBacked = evidenceGraph !== undefined;
    const sourceProved = sourceImpl.length > 0 && (!relationBacked || sourceDefineIds.length > 0);
    const consumerProved = consumerImpl.length > 0 && (!relationBacked || consumerDefineIds.length > 0);
    const semanticInsertion = result.wiring?.strategy === "semantic-multihop";
    const insertionProved = insertionImpl.length > 0
      && directCallIds.length > 0
      && (!semanticInsertion || (insertionDefineIds.length > 0 && insertionConsumerRelationIds.length > 0));
    obligations.push({
      id: "wiring-source",
      kind: "wiring-source",
      status: sourceProved ? "proved" : "uncovered",
      required: true,
      evidence: sourceImpl.map(readinessEvidence).slice(0, 4),
      ...(sourceDefineIds.length > 0 ? { evidence_ids: sourceDefineIds } : {}),
      reason: sourceProved ? "producer implementation is served and definition-backed" : "producer declaration lacks an implementation body or defines edge",
    });
    obligations.push({
      id: "wiring-consumer",
      kind: "wiring-consumer",
      status: consumerProved ? "proved" : "uncovered",
      required: true,
      evidence: consumerImpl.map(readinessEvidence).slice(0, 4),
      ...(consumerDefineIds.length > 0 ? { evidence_ids: consumerDefineIds } : {}),
      reason: consumerProved ? "consumer implementation is served and definition-backed" : "consumer implementation/call site or defines edge is missing",
    });
    if (relationBacked) {
      obligations.push({
        id: "wiring-link",
        kind: "wiring-link",
        status: linkRelationIds.length > 0 && insertionProved ? "proved" : "uncovered",
        required: true,
        evidence: [...sourceImpl, ...insertionImpl].map(readinessEvidence).slice(0, 4),
        evidence_ids: linkRelationIds,
        reason: linkRelationIds.length > 0 && insertionProved
          ? "consumer call path reaches the producer or review-only adapter at the insertion site"
          : "consumer import/call relation or callable insertion proof is missing",
      });
    }
    obligations.push({
      id: "wiring-insertion",
      kind: "wiring-insertion",
      status: insertionProved ? "proved" : "uncovered",
      required: true,
      evidence: insertionImpl.map(readinessEvidence).slice(0, 4),
      ...(directCallIds.length > 0 ? { evidence_ids: directCallIds } : {}),
      reason: insertionProved
        ? semanticInsertion
          ? "callable hub insertion site is definition-backed and directly calls the executable consumer"
          : "callable consumer insertion site is backed by a direct producer or adapter call"
        : "edit-grade consumer or hub insertion call is not proved",
    });
  }

  return obligations.slice(0, 12);
}

/**
 * P0 defect 3: same-epoch evidence the caller ALREADY holds, as certificate
 * frontier entries — served-surface handles plus the still-open edit
 * obligations an earlier pack of this task recorded.
 *
 * Not a widening of write permission: `recordServedEditAdmissibility` already
 * admits every content-bearing serve for the rest of the epoch, and
 * priorPackStore's entries are obligations this server itself published. What
 * this closes is the DISAGREEMENT — the primary certificate named only the
 * current call's frontier, so the three existing nets (session's admissible
 * union via the mid-fence receipt, the qref-replay carry-forward, F-B3) were
 * the only things that remembered pack 1, and none of them reaches this path.
 *
 * Bounded at FRONTIER_HANDLE_CAP parity (32) before the certificate applies
 * its own 12-entry cap; current-call entries always sort first, so prior
 * evidence can only fill slots the current call left empty.
 */
const PRIOR_EPOCH_FRONTIER_CAP = 32;

function priorEpochActionFrontier(workspace: string, query: string): string[] {
  const epochTokens = tokenizeForEpoch(query);
  if (epochTokens.length === 0) return [];
  const handles = queryServedSurfaces(workspace, workspace, { epochTokens })
    .map((entry) => entry.handle)
    .filter((handle): handle is string => typeof handle === "string" && handle.length > 0);
  const obligationPaths = queryPriorPackObligations(workspace, epochTokens)
    .filter((obligation) => obligation.open && obligation.action === "edit")
    .map((obligation) => obligation.path);
  return [...new Set([...handles, ...obligationPaths])].slice(0, PRIOR_EPOCH_FRONTIER_CAP);
}

function actionFrontierForCertificate(
  result: TaskPackResult,
  profile: TaskProfile,
  obligations: readonly TaskReadinessObligation[],
  priorEpoch: readonly string[] = [],
): string[] {
  if (profile === "answer") return [];
  if (profile === "wiring") {
    // A wiring certificate's writable set is its edit_frontier ALONE (the
    // wiring write protocol), so prior-epoch evidence is deliberately excluded.
    return [...new Set(result.wiring?.edit_frontier ?? [])].slice(0, 12);
  }
  const fromWiring = result.wiring?.edit_frontier ?? [];
  const fromChange = result.change_contract?.obligations
    .filter((obligation) => obligation.action === "edit" && obligation.status === "ready" && obligation.handle)
    .map((obligation) => obligation.handle!) ?? [];
  const provedImplementation = obligations
    .filter((obligation) => obligation.status === "proved" && obligation.kind !== "explicit-identifier")
    .flatMap((obligation) => (obligation.evidence ?? []).map((evidence) => evidence.handle));
  const createTarget = result.create_target?.path ? [result.create_target.path] : [];
  // Create-only context surfaces are read-only imitation evidence, not edit targets.
  // Mixed create+edit requests retain independent wiring/change obligations.
  //
  // THE EMPTY RETURN IS LOAD-BEARING — do not "fix" it (2026-08-30, F-V13-3
  // wave; measured, not reasoned). Returning `[...createTarget]` here looks
  // like the obvious repair for "a create pack does not authorize its own
  // create target", and it is wrong twice over:
  //
  //  (a) It is unnecessary. `recordExecutionContract` (state/session.ts, the
  //      2026-07-25 T13 refuse-only-fence guard) CLEARS the fence outright for
  //      an edit-terminal certificate with no handles, no action paths and no
  //      evidence — which is exactly a create-only pack — so the create is
  //      ungated. Verified end to end through the real dispatch, including the
  //      case where the same lane already holds admissible material (so the
  //      guard does NOT fire and the fence installs with an empty frontier):
  //      the `edits[]` create still lands, authorized by server.ts's own
  //      create pin (`create_target.authorized_by:"explicit-cwd"`).
  //  (b) It closes discovery. A non-empty frontier makes this a real prepared
  //      fence, so the NEXT task_pack of an unrelated task in the same session
  //      is refused `prepared-discovery-closed` — a single-lane wire change,
  //      caught by replayCorpus's tew3_2/tew3_3 (a create pack followed by an
  //      enum-addition pack).
  //
  // The create refusal observed in the field (F-V13-3, v0.13.0 decision run)
  // was NOT this: the lane's fence held a PEER lane's certificate. That is
  // fixed in the five lane-scoped stores, not here.
  if (createTarget.length > 0 && fromWiring.length === 0 && fromChange.length === 0) return [];
  return [...new Set([
    ...createTarget, ...fromWiring, ...fromChange, ...provedImplementation,
    // Last, so the cap can only ever evict prior-epoch entries.
    ...priorEpoch,
  ])].slice(0, 12);
}

const MAX_WORKSPACE_STATE_INVENTORY_FILES = 10_000;
const MAX_WORKSPACE_STATE_HASH_BYTES = 4 * 1024 * 1024;

interface WorkspaceInventoryState {
  fingerprint: string;
  files: number;
  complete: boolean;
}

/**
 * Cheap invalidation fingerprint over the walked source inventory. Content
 * hashes for action-bearing evidence files are added separately below. If the
 * inventory cannot be proved complete, exact-call caching fails closed.
 *
 * turn-economy wave 3 (V2): keyed on (relPath, size) — NOT mtime. E2 forensics
 * (run signal5-3): a byte-identical task_pack re-call "in a row" re-served the
 * full ~12-14KB pack instead of the sub-1KB `pack_unchanged` acknowledgement,
 * because `mtimeMs` folded into this fingerprint (and thus into
 * workspace_state.fingerprint) flipped on ANY file's mtime change between the
 * two calls — even an UNRELATED file, even a no-op rewrite with byte-identical
 * content (a formatter pass, an edit-then-revert, a checkout). tryServeCachedPack
 * then saw a "changed" workspace and recomputed. mtime is a false-positive-prone
 * proxy for change; (relPath, size) is content-stable across those no-op touches
 * while still catching add/remove (the sorted relPath set) and any size-changing
 * edit. A same-size in-place edit to an UNSERVED file is the only change this no
 * longer flags here — an accepted, negligible narrowing, since every SERVED
 * surface is content-sha revalidated separately (surfaceFileSha, evidence hashes)
 * and the dedup only fires on a byte-identical re-request.
 */
function computeWorkspaceInventoryState(workspace: string): WorkspaceInventoryState {
  const files = walkCodeFiles(workspace, {})
    .map((file) => file.relPath)
    .sort();
  const complete = files.length <= MAX_WORKSPACE_STATE_INVENTORY_FILES;
  const inventory = complete ? files : files.slice(0, MAX_WORKSPACE_STATE_INVENTORY_FILES);
  const records: Array<[string, number]> = [];
  for (const relPath of inventory) {
    const abs = safeResolve(relPath, workspace);
    if (abs === undefined) continue;
    try {
      const real = fs.realpathSync(abs);
      if (!isWithin(real, resolveReal(workspace))) continue;
      const stat = fs.statSync(real);
      records.push([relPath, stat.size]);
    } catch {
      return {
        fingerprint: shaOfText(JSON.stringify(records)),
        files: files.length,
        complete: false,
      };
    }
  }
  return {
    fingerprint: shaOfText(JSON.stringify(records)),
    files: files.length,
    complete,
  };
}

function evidenceFileState(workspace: string, relPath: string): string | undefined {
  const abs = safeResolve(relPath, workspace);
  if (abs === undefined) return undefined;
  try {
    const real = fs.realpathSync(abs);
    if (!isWithin(real, resolveReal(workspace))) return undefined;
    const stat = fs.statSync(real);
    if (!stat.isFile()) return undefined;
    if (stat.size > MAX_WORKSPACE_STATE_HASH_BYTES) {
      return `large:${stat.size}:${Math.trunc(stat.mtimeMs)}`;
    }
    return shaOfBytes(fs.readFileSync(real));
  } catch {
    return undefined;
  }
}

function buildTaskWorkspaceState(
  result: TaskPackResult,
  workspace?: string,
  inventory?: WorkspaceInventoryState,
): TaskWorkspaceState {
  const evidence = [...new Map(
    (result.surfaces as TaskPackResultSurface[]).map((surface) => [surface.path, surface] as const),
  ).values()]
    .sort((left, right) => left.path.localeCompare(right.path))
    .map((surface) => {
      const diskState = workspace ? evidenceFileState(workspace, surface.path) : undefined;
      const servedState = isArtifactTaskPackSurface(surface)
        ? `${surface.handle}:${surface.size}`
        : shaOfText(surface.code ?? surface.code_unchanged ?? `${surface.handle}:${surface.range}`);
      return [surface.path, diskState ?? servedState];
    });
  const inventoryState = workspace
    ? inventory ?? computeWorkspaceInventoryState(workspace)
    : { fingerprint: "no-inventory", files: 0, complete: false };
  const scope = inventoryState.complete ? "evidence-plus-inventory" : "served-evidence";
  return {
    version: 1,
    fingerprint: shaOfText(JSON.stringify({
      evidence,
      inventory: inventoryState.complete ? inventoryState.fingerprint : undefined,
    })),
    scope,
    evidence_files: evidence.length,
    inventory_files: inventoryState.files,
    inventory_complete: inventoryState.complete,
  };
}

function buildDecisionEvidenceModel(
  terminalAction: "answer" | "edit",
  obligations: readonly TaskReadinessObligation[],
  falsification: TaskFalsificationReport,
  risk: TaskReadinessRisk,
): TaskDecisionEvidenceModel {
  const confidence = Math.max(0, Math.min(1, 1 - risk.estimated_false_ready_risk));
  return {
    version: 1,
    decision: terminalAction,
    claims: obligations.map((obligation) => ({
      id: obligation.id,
      kind: obligation.kind,
      status: obligation.status === "proved" ? "supported" : "unresolved",
      reason: (obligation.reason ?? obligation.status).slice(0, 180),
      evidence_handles: [...new Set((obligation.evidence ?? []).map((item) => item.handle))].slice(0, 6),
      ...(obligation.evidence_ids?.length ? { evidence_ids: obligation.evidence_ids.slice(0, 8) } : {}),
      confidence: obligation.status === "proved" ? confidence : 0,
    })),
    counterexamples: falsification.counterexamples.slice(0, 2),
    unresolved: [...new Set([
      ...obligations.filter((obligation) => obligation.status !== "proved").map((obligation) => obligation.id),
      ...falsification.unresolved,
    ])].slice(0, 12),
  };
}

/** `queries` is OR-matched and capped at 5 per call (server.ts find dispatch). */
const UNCOVERED_IDENTIFIER_FIND_MAX = 5;

/**
 * W9 (2026-08-22) — THE ONE BATCHED FIND THAT CLOSES AN UNCOVERED-IDENTIFIER GAP.
 *
 * A pack whose `answer-explicit-symbol-focus` shortcut resolved ONE query
 * identifier lands `coverage:"focused"` on that one symbol, while the
 * obligation pass leaves `identifier:<other>` uncovered. The gap built for it
 * (`ambiguous-target`, below) carried NO call, so the projector fell through to
 * `servedEvidenceZoom` — a same-handle remaining-range zoom, which for the live
 * repro was the served file's 519-byte `using` header. That zoom is a dead end
 * FOR THE GAP IT WAS ISSUED FOR by construction: the identifier is provably not
 * in this file's body, so widening this file's window cannot produce it. The
 * caller then paid its own find+read fan-out for the rest of the path.
 *
 * The recovery is the call the gap actually needs, batched into ONE search:
 * `search_files action=find queries=[<every still-uncovered identifier>]`.
 *
 * FOUR STRICTNESS RULES, so this can never widen into a speculative find:
 *  0. `coverage:"complete"` is EXCLUDED. That value is model.ts's promise that
 *     every required role is covered and every query concern is addressed —
 *     "trust it" — so a find issued against it would contradict the pack's own
 *     claim, exactly the contradiction `complete-coverage-forbids-discover-gaps`
 *     encodes and `discoveryBundleNext` opens with. Caught live by replay case
 *     ds8 (colloquial `QuoteOrchestrator` resolved semantically on a complete
 *     pack): re-siting its "act on the served evidence" grant onto a find for a
 *     name the pack had ALREADY resolved would have bought a round trip and
 *     lost a served answer. `focused` and `partial` are the honest targets —
 *     `focused` in particular claims "one confident site, no fan-out", which an
 *     uncovered query identifier directly contradicts.
 *  1. ONLY `explicit-identifier` obligations — identities the QUERY named
 *     literally (`explicitCodeIdentifiers`), never an inferred facet or token.
 *  2. The absence test is `exactIdentifierEvidence` — the SAME word-boundary
 *     matcher that marked the obligation uncovered, over served body + symbol
 *     only. Never a substring test, so `Mount` inside `DriveMounter` is not
 *     "found" and, conversely, a token this pack really did serve never earns
 *     a find.
 *  3. A VERIFIED-ABSENT identifier is dropped: its zero-occurrence fact is
 *     already decision-grade (the guide's "absence is meaning" contract), so
 *     re-searching it is the re-grep the protocol forbids.
 *
 * Returns `undefined` when nothing qualifies, which leaves every existing
 * catch-all — including the served-evidence zoom for packs with NO uncovered
 * identifiers — exactly as it was.
 */
function uncoveredIdentifierFindCall(
  result: TaskPackResult,
  obligations: readonly TaskReadinessObligation[],
): ContinuationCall | undefined {
  if (result.coverage === "complete") return undefined;
  const served = codeTaskPackSurfaces(result.surfaces).filter(hasServedCode);
  const verifiedAbsent = new Set(verifiedAbsentIdentifiersFor(result).map(normIdent));
  const seen = new Set<string>();
  const tokens: string[] = [];
  for (const obligation of obligations) {
    if (obligation.status === "proved") continue;
    if (obligation.kind !== "explicit-identifier") continue;
    if (!obligation.id.startsWith("identifier:")) continue;
    const identifier = obligation.id.slice("identifier:".length);
    if (!/^[A-Za-z_$][A-Za-z0-9_$]*$/u.test(identifier)) continue;
    const key = normIdent(identifier);
    if (seen.has(key) || verifiedAbsent.has(key)) continue;
    if (exactIdentifierEvidence(served, identifier).length > 0) continue;
    seen.add(key);
    tokens.push(identifier);
    if (tokens.length >= UNCOVERED_IDENTIFIER_FIND_MAX) break;
  }
  if (tokens.length === 0) return undefined;
  return { tool: "search_files", arguments: { action: "find", queries: tokens } };
}

function buildCapabilityGaps(
  result: TaskPackResult,
  obligations: readonly TaskReadinessObligation[],
  nextCall: ContinuationCall | undefined,
  awaitingUserInput: boolean,
): TaskCapabilityGap[] | undefined {
  const uncovered = obligations.filter((obligation) => obligation.status !== "proved").map((obligation) => obligation.id);
  const gaps: TaskCapabilityGap[] = [];
  if (result.missing.some((item) => item.startsWith("(invalid paths[] entry"))) {
    gaps.push({
      kind: "invalid-request",
      recoverable: true,
      reason: "one or more caller-supplied paths entries are malformed",
    });
  }
  if (result.route?.action === "fallback_native") {
    gaps.push({
      kind: "unsupported-operation",
      recoverable: false,
      reason: result.route.reason,
    });
  } else if (awaitingUserInput) {
    // W9: the gap NAMES its own recovery when one exists. `TaskCapabilityGap`
    // has always had `next_call` (the missing-evidence branch below uses it);
    // this arm simply stopped being the one gap kind that could never carry a
    // call. The wire projection of a gap is `code` + `refs` (§4.4), so this
    // costs no response bytes — the projector reads it to replace the dead
    // same-handle zoom it would otherwise emit as `decision.discover.next`.
    const identifierFind = uncoveredIdentifierFindCall(result, obligations);
    gaps.push({
      kind: "ambiguous-target",
      recoverable: true,
      reason: result.route?.reason ?? "repository evidence does not determine one safe target",
      ...(uncovered.length > 0 ? { obligation_ids: uncovered.slice(0, 8) } : {}),
      ...(identifierFind ? { next_call: identifierFind } : {}),
    });
  } else if (uncovered.length > 0 || result.missing.length > 0) {
    gaps.push({
      kind: "missing-evidence",
      recoverable: nextCall !== undefined,
      reason: nextCall
        ? "a bounded evidence call can resolve the remaining semantic obligation"
        : "required semantic evidence is not present in the served pack",
      ...(uncovered.length > 0 ? { obligation_ids: uncovered.slice(0, 8) } : {}),
      ...(nextCall ? { next_call: nextCall } : {}),
    });
  }
  return gaps.length > 0 ? gaps.slice(0, 3) : undefined;
}

function buildFalsificationReport(
  result: TaskPackResult,
  profile: TaskProfile,
  obligations: readonly TaskReadinessObligation[],
): TaskFalsificationReport {
  const checked = ["required-surface-content", "uncovered-obligations"];
  if (result.route?.action === "answer_from_handles" || result.route?.action === "edit_from_handles") {
    checked.push("bounded-counterexample-probe");
  }
  if (profile === "answer") checked.push("callable-behavior-evidence");
  if (profile === "wiring") checked.push("producer-consumer-implementation", "callable-insertion-site");
  if (profile === "artifact_build") checked.push("artifact-to-implementation-target");
  if (profile === "multi_concern") checked.push("independent-concern-frontiers");
  const counterexamples = codeTaskPackSurfaces(result.surfaces)
    .filter((surface) => surface.why === "readiness-falsification-counterexample")
    .map((surface) => `${surface.path}:${surface.range}`)
    .slice(0, READINESS_PROBE_MAX_SURFACES);
  return {
    version: 1,
    checked,
    counterexamples,
    unresolved: obligations.filter((obligation) => obligation.status === "uncovered").map((obligation) => obligation.id),
  };
}

function estimateReadinessRisk(
  result: TaskPackResult,
  profile: TaskProfile,
  query: string,
  obligations: readonly TaskReadinessObligation[],
  falsification: TaskFalsificationReport,
): TaskReadinessRisk {
  const factors: string[] = [];
  let risk = 0.01;
  if (obligations.some((obligation) => obligation.status === "uncovered")) {
    risk = 1;
    factors.push("uncovered-required-obligation");
  } else {
    if (result.coverage === "focused") { risk += 0.01; factors.push("focused-not-complete"); }
    if (result.coverage === "partial") {
      const terminalRoute = result.route?.action === "answer_from_handles" || result.route?.action === "edit_from_handles";
      if (terminalRoute) {
        risk += 0.01;
        factors.push("partial-coverage-with-terminal-route");
      } else {
        risk += 0.5;
        factors.push("partial-coverage");
      }
    }
    // 2026-07-16a: an INLINED counterexample is served evidence the agent sees;
    // enrichment must not tip an otherwise-proved pack over the reject line.
    // Zero-risk factor kept for observability; competing evidence that could
    // NOT be incorporated still lands in the uncovered branch above (risk=1).
    if (falsification.counterexamples.length > 0) { factors.push("counterexample-incorporated"); }
    if (
      !(profile === "answer" && (
        result.answer_resolution !== undefined
        || hasExactNamedAnswerFacetEvidence(result, query)
      ))
      && codeTaskPackSurfaces(result.surfaces).some((surface) =>
        surface.required !== false
        && surface.content_completeness === "partial"
        && surface.anchors_served !== true
      )
    ) {
      risk += 0.2;
      factors.push("partial-surface-content");
    }
    const roots = new Set(codeTaskPackSurfaces(result.surfaces).map((surface) => projectRootOf(surface.path) ?? surface.path.split("/", 1)[0]));
    // 2026-07-16a: wiring/change_propagation/multi_concern are cross-root BY
    // TASK SHAPE (producer→consumer hookup, enum/schema fan-out) — spanning
    // roots is their completeness evidence, not a false-ready signal.
    const crossRootByShape = profile === "wiring"
      || profile === "change_propagation"
      || profile === "multi_concern"
      || profile === "artifact_build";
    if (roots.size > 1 && !crossRootByShape) { risk += 0.04; factors.push("multi-root-evidence"); }
    if (profile === "answer" && explicitCodeIdentifiers(result.profile_binding?.reason ?? "").length === 0) {
      // No extra penalty here: explicit query obligations are already checked.
    }
    if (profile === "wiring") {
      const implementations = new Set(codeTaskPackSurfaces(result.surfaces).filter((surface) => isImplementationPath(surface.path)).map((surface) => surface.path));
      if (implementations.size < 3) { risk += 0.03; factors.push("thin-wiring-path"); }
      else { risk += 0.02; factors.push("wiring-path"); }
    }
  }
  const estimated = Number(Math.min(1, risk).toFixed(3));
  return {
    policy: "selective-reject",
    estimated_false_ready_risk: estimated,
    max_false_ready_risk: READINESS_FALSE_READY_LIMIT,
    decision: estimated <= READINESS_FALSE_READY_LIMIT ? "accept" : "reject",
    factors,
  };
}

const NEXT_CALL_SEARCH_ACTIONS = new Set(["find", "symbols", "references", "diff", "tree", "locate"]);
const NEXT_CALL_GENERIC_QUERY_TERMS = new Set([
  "what", "should", "could", "would", "explain", "describe", "behavior", "feature", "issue", "system",
]);

/** A token made only of hiragana (plus the長音 mark) — see repairedNextCallTerms. */
const HIRAGANA_ONLY_TERM_RE = /^[\p{Script=Hiragana}ー]+$/u;

function repairedNextCallTerms(query: string): string[] {
  const terms = tokenizeForEpoch(query)
    .filter((term) => !NEXT_CALL_GENERIC_QUERY_TERMS.has(term));
  // 2026-07-31: this list is consumed POSITIONALLY — terms[0] becomes the whole
  // query of the emitted next_call. tokenizeForEpoch splits CJK on script
  // boundaries, so a Japanese request's leading run is very often a
  // demonstrative or other function word carrying no search signal; a live pack
  // emitted `search_files action=locate query="こんな"` from exactly that.
  // Han / Katakana / ASCII runs are the content-bearing ones, so rank the
  // hiragana-only runs last. Stable within each group, and a no-op for queries
  // with no hiragana at all.
  const contentful = terms.filter((term) => !HIRAGANA_ONLY_TERM_RE.test(term));
  const functional = terms.filter((term) => HIRAGANA_ONLY_TERM_RE.test(term));
  return [...contentful, ...functional].slice(0, 5);
}

/**
 * Validate and deterministically repair a discovery continuation before it is
 * exposed. This mirrors the advertised dispatcher shape closely enough to
 * reject calls that are guaranteed to fail without performing repo I/O.
 */
function admitReadOnlyNextCall(call: ContinuationCall, originalQuery: string): ContinuationCall | undefined {
  if (call.tool === "edit_file") return undefined;
  const args = { ...call.arguments };

  if (call.tool === "read_file") {
    const hasBoundedTarget =
      (typeof args["handle"] === "string" && args["handle"].length > 0)
      || (Array.isArray(args["handles"]) && args["handles"].length > 0)
      || (typeof args["path"] === "string" && args["path"].length > 0)
      || (Array.isArray(args["paths"]) && args["paths"].length > 0)
      || (typeof args["symbol"] === "string" && args["symbol"].length > 0);
    if (hasBoundedTarget) return { ...call, arguments: args };
    const readQuery = typeof args["query"] === "string" ? args["query"] : "";
    return repairedNextCallTerms(readQuery).length > 0
      ? { ...call, arguments: args }
      : undefined;
  }

  const action = typeof args["action"] === "string" ? args["action"] : "";
  if (!NEXT_CALL_SEARCH_ACTIONS.has(action)) return undefined;
  if (action === "tree" || action === "diff") return { ...call, arguments: args };
  if (action === "locate") {
    // `locate` is the semantic action: nextCallForUnresolved hands it the FULL
    // query (with includeClosure) deliberately, because the locator ranks over
    // the whole request rather than one token. Collapsing it to terms[0] threw
    // that intent away — and on a CJK query it also picked a function word.
    // Keep the producer's query; only reject an empty one.
    const locateQuery = typeof args["query"] === "string" ? args["query"].trim() : "";
    return locateQuery.length > 0 ? { ...call, arguments: args } : undefined;
  }
  if (action === "references") {
    // A reference lookup is symbol-sensitive: splitting camelCase would turn
    // telemetrySender into a broader, semantically different telemetry query.
    const reference = typeof args["query"] === "string"
      ? args["query"].trim()
      : typeof args["symbol"] === "string"
        ? args["symbol"].trim()
        : "";
    return reference.length > 0 ? { ...call, arguments: args } : undefined;
  }

  const suppliedQueries = Array.isArray(args["queries"])
    ? args["queries"].filter((item): item is string => typeof item === "string")
    : [];
  const seed = suppliedQueries.length > 0
    ? suppliedQueries.join(" ")
    : typeof args["query"] === "string"
      ? args["query"]
      : originalQuery;
  const terms = repairedNextCallTerms(seed);
  if (terms.length === 0) return undefined;

  delete args["queries"];
  // 2026-08-21 (W7 integration fix): a single ASCII-identifier-shaped seed —
  // no caller-supplied queries[] array, and the raw text is ONE bare
  // camelCase/snake_case/PascalCase token (e.g. "lunarBeacon") — must stay
  // ONE find query. repairedNextCallTerms splits on camelCase/script
  // boundaries for EPOCH-comparison purposes and would otherwise decompose
  // it into independent lowercase words ("lunar", "beacon"), discarding the
  // compound identifier the caller actually named in favor of two generic
  // words — worse recall, not better (see the `references` branch above,
  // which already protects the identical shape for the same reason:
  // "splitting camelCase would turn telemetrySender into a broader,
  // semantically different telemetry query"). Deliberately an ASCII
  // identifier-grammar test, not a bare "no whitespace" test: Japanese (and
  // other CJK) prose carries no space between words at all — a whitespace-
  // free JA sentence like "認証フローを確認してください" is many words, not
  // one identifier, and readinessSemantics.spec.ts pins its script-boundary
  // decomposition into queries:["認証","フロー","確認"] as intended recall
  // behavior, unaffected here. A genuinely multi-word original seed
  // ("payment gateway retry") is likewise unaffected and still becomes
  // queries[] below.
  const seedIsSingleToken = action === "find"
    && suppliedQueries.length === 0
    && /^[A-Za-z_$][A-Za-z0-9_$]*$/.test(seed.trim());
  if (seedIsSingleToken) {
    args["query"] = seed.trim();
  } else if (action === "find" && terms.length > 1) {
    delete args["query"];
    args["queries"] = terms;
  } else {
    args["query"] = terms[0];
  }
  return { ...call, arguments: args };
}

/**
 * F-V13-6, THE TERMINATION RULE FOR ENUMERATED ITEMS.
 *
 * A stricter obligation must not buy itself an escape hatch: the only thing
 * this adds is ONE bounded `find` per item, and only while that find is still
 * unspent. Once the session has run it — whatever it returned — the item is
 * NOT promoted to proved (a search result the pack never served is not served
 * evidence) and it is NOT proposed again. The obligation then simply stays
 * uncovered and travels as an honest disclosure, which is the convergent state
 * for a requirement the repository cannot satisfy.
 *
 * `workspace` is optional so the readiness specs' synthetic, workspace-free
 * contracts keep their existing call shape; without it the first unproved item
 * is proposed exactly once per pack, as any other proof next would be.
 */
function enumeratedItemFindCall(
  obligations: readonly TaskReadinessObligation[],
  workspace: string | undefined,
): ContinuationCall | undefined {
  for (const obligation of obligations) {
    if (obligation.status === "proved") continue;
    if (!obligation.id.startsWith(ENUMERATED_ITEM_OBLIGATION_PREFIX)) continue;
    const facet = obligation.id.slice(ENUMERATED_ITEM_OBLIGATION_PREFIX.length);
    if (facet.length === 0) continue;
    if (workspace !== undefined && consultExecutedSearch(workspace, "find", facet) !== undefined) continue;
    return { tool: "search_files", arguments: { action: "find", query: facet } };
  }
  return undefined;
}

function nextCallForUnresolved(
  result: TaskPackResult,
  obligations: readonly TaskReadinessObligation[],
  query: string,
  workspace?: string,
): ContinuationCall | undefined {
  const unresolved = obligations.find((obligation) => obligation.status === "uncovered");
  if (!unresolved) return undefined;
  // Ahead of the generic branches below: an enumerated item names its own
  // search term, so the shared "first uncovered obligation" fallbacks would
  // otherwise emit a symbols/references sweep over an unrelated token.
  if (unresolved.id.startsWith(ENUMERATED_ITEM_OBLIGATION_PREFIX)) {
    return enumeratedItemFindCall(obligations, workspace);
  }
  const identifier = unresolved.id.startsWith("identifier:") ? unresolved.id.slice("identifier:".length) : undefined;
  if (identifier) return { tool: "search_files", arguments: { action: "symbols", query: identifier } };
  const connection = result.wiring?.connections[0];
  // Raw-query fallbacks manufactured junk sweeps (references on the whole
  // sentence / a stopword — 2026-07-31 T13 rep0 forensics); endpoint recovery
  // calls run on identifier-shaped tokens only, or not at all (the
  // no-dead-end guard still keeps one bounded call open downstream).
  // W9 (2026-08-22): scrub generic path spans from `query` FIRST — this is a
  // raw `.match()` extraction, not a call through one of the shared,
  // already-scrubbing token helpers, so a path segment mentioned in prose
  // (e.g. a PascalCase repo folder name) would otherwise pass
  // isWiringIdentifierShape and become a wiring `find`/`references` `next`.
  const cleanedQuery = stripPathSpans("", query);
  const queryIdentifiers = (cleanedQuery.match(/\b[A-Za-z_$][A-Za-z0-9_$-]*\b/g) ?? []).filter(isWiringIdentifierShape);
  if (unresolved.kind === "wiring-source") {
    const token = connection?.source?.token ?? queryIdentifiers[0];
    return token === undefined
      ? undefined
      : { tool: "search_files", arguments: { action: "find", query: token } };
  }
  if (unresolved.kind === "wiring-consumer" || unresolved.kind === "wiring-insertion") {
    const token = connection?.destination?.token
      ?? (queryIdentifiers.length > 1 ? queryIdentifiers.at(-1) : queryIdentifiers[0]);
    return token === undefined
      ? undefined
      : { tool: "search_files", arguments: { action: "references", query: token } };
  }
  if (unresolved.kind === "behavior-body") {
    const explicit = explicitCodeIdentifiers(query)[0];
    return { tool: "search_files", arguments: { action: explicit ? "references" : "symbols", query: explicit ?? tokenizeForEpoch(cleanedQuery)[0] ?? query } };
  }
  if (unresolved.id === "analysis-source") {
    return { tool: "search_files", arguments: { action: "locate", query, includeClosure: true } };
  }
  if (unresolved.id === "artifact-input") {
    const artifact = result.surfaces.find(isArtifactTaskPackSurface);
    if (artifact) {
      return { tool: "read_file", arguments: { mode: "artifact", path: artifact.path } };
    }
  }
  if (unresolved.id === "artifact-implementation") {
    return {
      tool: "search_files",
      arguments: {
        action: "locate",
        query: moduleShapedQueryTokens(query)[0] ?? query,
        includeClosure: true,
      },
    };
  }
  if (unresolved.id.startsWith("open-universe:")) {
    // Additive propagation queries still need one authoritative existence
    // probe for the requested value before the caller continues. Prefer an
    // identifier-shaped all-caps token (e.g. REFUNDED) over the enum name.
    const requestedValue = query.match(/\b[A-Z][A-Z0-9_]{2,}\b/)?.[0];
    if (requestedValue !== undefined) {
      return { tool: "search_files", arguments: { action: "find", query: requestedValue } };
    }
  }
  return undefined;
}

function deterministicCertificate(
  query: string,
  profile: TaskProfile,
  obligations: TaskReadinessObligation[],
  falsification: TaskFalsificationReport,
  risk: TaskReadinessRisk,
  actionFrontier: string[],
  workspaceState: TaskWorkspaceState,
  ledgerDigest?: string,
): TaskReadinessCertificate {
  const taskHash = shaOfText(`${profile}\u0000${query}`).replace(/^sha256:/, "").slice(0, 16);
  const proofHash = shaOfText(JSON.stringify({
    taskHash,
    obligations: obligations.map((obligation) => [obligation.id, obligation.status, (obligation.evidence ?? []).map((item) => item.handle)]),
    actionFrontier,
    workspaceState: workspaceState.fingerprint,
    ledgerDigest,
  })).replace(/^sha256:/, "").slice(0, 16);
  return {
    version: 1,
    id: ledgerDigest === undefined ? `ready-${proofHash}` : `ready-${ledgerDigest.slice(0, 16)}-${proofHash}`,
    task_fingerprint: `task-${taskHash}`,
    workspace_state_fingerprint: workspaceState.fingerprint,
    profile,
    obligations,
    evidence_handles: [...new Set(obligations.flatMap((obligation) => (obligation.evidence ?? []).map((item) => item.handle)))].slice(0, 16),
    action_frontier: actionFrontier,
    falsification,
    risk,
  };
}

function valueCallBudget(
  terminalAction: "answer" | "edit",
  baseReady: boolean,
  accepted: boolean,
  unresolvedCount: number,
  risk: TaskReadinessRisk,
  candidateCall: ContinuationCall | undefined,
): TaskCallBudget {
  const expectedDecisionChange = accepted
    ? Math.min(0.1, risk.estimated_false_ready_risk)
    : Math.min(0.98, Math.max(baseReady ? 0.72 : 0.58, unresolvedCount * 0.22));
  const falseReadyWeight = terminalAction === "edit" ? 1.35 : 1.1;
  const expectedValue = Number((expectedDecisionChange * falseReadyWeight).toFixed(3));
  const discoveryAllowed = !accepted && candidateCall !== undefined && expectedValue > READINESS_NORMALIZED_TURN_COST;
  return {
    version: 2,
    policy: "expected-decision-change",
    normalized_turn_cost: READINESS_NORMALIZED_TURN_COST,
    expected_decision_change: Number(expectedDecisionChange.toFixed(3)),
    expected_value: expectedValue,
    decision_threshold: READINESS_NORMALIZED_TURN_COST,
    discovery_allowed: discoveryAllowed,
    terminal_action: terminalAction,
    ...(candidateCall ? { candidate_call: candidateCall } : {}),
    reason: discoveryAllowed
      ? "candidate evidence can change the terminal action and exceeds one-turn cost"
      : accepted
        ? "certificate risk is below threshold; further discovery has lower value than one turn"
        : "no bounded candidate call has positive decision value",
  };
}

export function answerArtifactDiscoveryCall(
  result: TaskPackResult,
  query: string,
): ContinuationCall | undefined {
  const artifacts = result.surfaces
    .filter(isArtifactTaskPackSurface);
  if (artifacts.length === 0) return undefined;
  const evidenceText = [
    query,
    ...codeTaskPackSurfaces(result.surfaces).map((surface) =>
      surface.code ?? surface.code_unchanged ?? ""
    ),
  ].join("\n").toLowerCase();
  const ranked = artifacts.map((surface) => {
    const basename = path.basename(surface.path).toLowerCase();
    const stem = path.basename(surface.path, path.extname(surface.path)).toLowerCase();
    const tokens = stem.split(/[^a-z0-9]+/u).filter((token) => token.length >= 4);
    const basenameMentions = evidenceText.split(basename).length - 1;
    const stemMentions = evidenceText.split(stem).length - 1;
    const tokenMentions = tokens.reduce(
      (total, token) => total + Math.min(3, evidenceText.split(token).length - 1),
      0,
    );
    // A literal artifact citation is stronger evidence than an arbitrary
    // number of generic stem-token repetitions in a large source/doc slice.
    // Cap token frequency so long files cannot win merely by saying "claim"
    // many times while explicitly naming a different authority.
    return {
      surface,
      score: basenameMentions * 64 + stemMentions * 32 + tokenMentions,
    };
  }).sort((a, b) => b.score - a.score || a.surface.path.localeCompare(b.surface.path));
  const selected = ranked[0]?.surface;
  return selected
    ? { tool: "read_file", arguments: { mode: "artifact", path: selected.path } }
    : undefined;
}

// Exported for readinessSemantics.spec.ts — synthetic TaskPackResult inputs are
// the only practical way to pin the per-profile risk/fallback branches.
export function buildTaskExecutionContract(
  result: TaskPackResult,
  profile: TaskProfile,
  query: string,
  workspaceState: TaskWorkspaceState = buildTaskWorkspaceState(result),
  /** P0 defect 3: the workspace root, so the certificate can name same-epoch evidence the caller already holds. Optional — the readiness specs build contracts from synthetic results with no workspace, and omitting it is exactly the pre-defect-3 frontier. */
  workspace?: string,
  /** A verified expansion gap from this response, before its durable replay projection is consulted. */
  currentOpenUniverseExplicitGap = false,
  servedZoomScope?: ServedWindowScope,
): TaskExecutionContract {
  const proofCompletion = proofCompletionEnabled();
  // P2(b) (2026-08-28 review-fix wave): this builder runs at least twice per
  // pack (this doc comment's own next line: "before and after final
  // same-epoch reconciliation"), and up to four call sites in this module can
  // each invoke it for the SAME pack. The engagement counter exists to
  // measure live PACK decisions, not internal re-evaluations of one —
  // dedupe on the `result` object identity (the same TaskPackResult is
  // threaded through every re-evaluation within one pack build) so a pack
  // that gets rebuilt three times still counts once.
  if (proofCompletion && !proofCompletionCountedResults.has(result)) {
    proofCompletionCountedResults.add(result);
    noteProofCompletionPack();
  }
  // This builder runs before and after final same-epoch reconciliation. Its
  // ledger rows are derived projection, so do not carry a discharged row from
  // the preliminary evaluation into the final one.
  result.missing = result.missing.filter((entry) => !entry.startsWith("unresolved-ledger:"));
  const ledgerEpochTokens = tokenizeForEpoch(query);
  const ledger = !proofCompletion || workspace === undefined
    ? { open: [] as string[], explicitGaps: [] as string[], resolved: [] as string[] }
    : taskContractGapProjection(workspace, ledgerEpochTokens);
  if (ledger.open.length > 0 || ledger.explicitGaps.length > 0) {
    result.missing = [...new Set([
      ...result.missing,
      ...ledger.open.map((entry) => `unresolved-ledger:${entry}`),
      ...ledger.explicitGaps.map((entry) => `explicit-gap:${entry}`),
    ])];
    if (ledger.open.length > 0) {
      result.coverage = "partial";
      result.coverage_reason ??= "concerns-uncovered";
    }
  }
  const ledgerCertificate = !proofCompletion || workspace === undefined
    ? undefined
    : taskContractDischargeCertificate(workspace, ledgerEpochTokens);
  // -------------------------------------------------------------------------
  // R2 (2026-08-28): THE EXPANSION-SUCCEEDED ARM.
  //
  // Until this, `openUniverseDischarged` had exactly one route — an explicit
  // capability gap — so the open universe could be closed only by FAILING to
  // expand it. A pack whose expansion fully SUCCEEDED had no discharge route at
  // all: `recordEvidenceExpansion` proves each served target `{type:"served"}`,
  // but the two-column gap projection dropped those proofs, so the
  // `open-universe:*` obligation stayed "uncovered" with `missing:[]`,
  // `coverage:"complete"` and zero gaps on the wire, and the certificate that
  // would authorise `act.answer` was never minted. Reproduced on the TS
  // 4-definition fixture in `ledgerDecisionIntegration.spec.ts`, where all four
  // direct-callee bodies are served.
  //
  // THE CONDITION IS DELIBERATELY THE STRONGEST ONE THAT STILL FIRES, because a
  // weaker one reopens P0-1 (an `act.answer` over an unresolved callee):
  //
  //   1. `ledger.open` is EMPTY — not "no open dependency-definitions", but
  //      nothing open at all. With 2 of 4 targets served and 2 still open, this
  //      is false and no discharge happens. That is the negative example, and
  //      it is the whole safety argument: an obligation with no proof is
  //      exactly what `open` means.
  //   2. At least one `dependency-definitions:` obligation is actually PROVED
  //      (served, or explicit-gap). An epoch that recorded no expansion at all
  //      also has an empty `open`, and it must not discharge on that emptiness —
  //      silence is not proof. This is what keeps the provisional contract
  //      rebuilds (`rebuildFinalExecutionContract`, which can run BEFORE this
  //      pack's own facts are persisted) fail-closed: they see an empty
  //      projection, so this arm cannot fire on a not-yet-recorded expansion.
  // -------------------------------------------------------------------------
  const dependencyDefinitionsSettled = ledger.open.length === 0
    && [...ledger.resolved, ...ledger.explicitGaps]
      .some((entry) => entry.startsWith("dependency-definitions:"));
  const openUniverseDischarged = hasOpenUniverseIntent(query)
    // An explicit capability gap discharges only the open-universe axis. Other
    // ledger obligations can still keep the terminal act locked, but the
    // prescribed absence probe must not be re-issued while those unrelated
    // obligations remain unresolved.
    && (
      currentOpenUniverseExplicitGap
      || ledger.explicitGaps.some((entry) => entry.startsWith("dependency-definitions:open-universe "))
      || dependencyDefinitionsSettled
    );
  // I-7: forwards this builder's OWN `workspace` param (undefined for the
  // synthetic/unit-built contracts several readiness specs construct — same
  // optionality this whole function already has) so buildReadinessObligations
  // can trace-summarize an enumerated-item mint. NOT threaded into
  // runInternalReadinessFalsification's OWN preview call, above this
  // function's definition — that call's `initialObligations` is a throwaway
  // internal check (never assigned to result.execution_contract, never
  // returned to a caller), so it must never win the one-emission-per-pack
  // dedup below. This function itself runs multiple times per pack (its own
  // P2(b) comment above, and runRecursiveReadOnlyClosure's discovery-loop
  // callers re-invoke it once per depth as evidence grows), so the dedup can
  // land on an early, not-yet-fully-settled evaluation for a pack that engages
  // that loop — an accepted, disclosed imprecision for a trace-only summary,
  // never a wire effect. See enumeratedObligationSummaryCountedResults' doc
  // comment.
  const obligations = buildReadinessObligations(result, profile, query, openUniverseDischarged, workspace);
  const falsification = buildFalsificationReport(result, profile, obligations);
  const risk = estimateReadinessRisk(result, profile, query, obligations, falsification);
  const focusedAnswerCandidate = profile === "answer"
    && result.route?.action === "confirm_candidates"
    && queryRequestsTestEvidence(query)
    && codeTaskPackSurfaces(result.surfaces).some((surface) => surface.role === "test" && hasServedCode(surface))
    && codeTaskPackSurfaces(result.surfaces).some((surface) => isImplementationPath(surface.path) && hasServedCode(surface))
    && codeTaskPackSurfaces(result.surfaces).every(hasServedCode);
  // A route may retain `needs-followup` after the router has already served
  // every required primary body.  Treating that conservative routing hint as
  // stronger than the completed, falsifiable readiness obligations strands a
  // caller at `await_input:act-on-served-evidence`: there is no executable
  // discovery call, but the missing certificate also prevents the decision
  // projection from emitting the answer.  A fully served, gap-free answer
  // route is instead a legitimate *candidate*; the proof/ledger gates below
  // still decide whether it receives an act certificate.
  const fullyServedAnswerRoute = result.route?.action === "answer_from_handles"
    && result.missing.every((entry) => entry.startsWith("explicit-gap:"))
    && codeTaskPackSurfaces(result.surfaces)
      .filter((surface) => surface.required !== false)
      .every(hasServedCode);
  const answerCandidate = profile === "answer"
    && (
      (result.route?.action === "answer_from_handles"
        && (result.content_sufficiency !== "needs-followup" || fullyServedAnswerRoute))
      || focusedAnswerCandidate
    );
  // 2026-08-01: answer_from_handles is the same "working set complete" verdict
  // as edit_from_handles with an answer act — excluding it made answer-shaped
  // packs structurally unable to reach prepared, so a fully-served explain
  // query fell to awaiting-input with allowed:["edit","request-user-input"]
  // (an edit grant and no answer affordance, on a question).
  // D1 (2026-08-07): two shipped rules disagreed about what
  // `route.max_additional_tl_calls` MEANS, and nothing reconciled them.
  // buildRoute's Wave-4 contract (2026-07-24) keeps it as "calls needed to
  // CLOSE the task", explicitly noting that "certification acceptance
  // requires 0 — a zoom affordance is not missing evidence". A5 (2026-08-01
  // signal5-2 T03) then started SPENDING that field to advertise the one
  // sanctioned same-handle zoom over a disclosed-partial primary. The result
  // was a pack that could never be both: routeCandidate demanded 0, the
  // answer route carried 1, so every partial-primary answer pack was
  // structurally barred from `prepared` and fell to discovery /
  // awaiting-input while its own route said "answer from handles" — the
  // self-contradiction observed live (route budget + prose granting a widen,
  // contract phase refusing to authorize the answer).
  //
  // AGENTS.md states the intended joint shape directly: "prepared+partial
  // primary grants `route.max_additional_tl_calls=1` — spend it on the served
  // zoom". So the zoom grant is an AFFORDANCE, and `prepared` must survive
  // it. Scoped to the answer route because editFromHandlesRoute only mints
  // the grant there; on an edit route a nonzero budget still means evidence
  // the pack owes, and reconcileTaskChangeContract sets exactly that.
  // Shared with canonicalDecision.ts's deriveCanonicalTaskDecision and this
  // function's own reconcileContentSufficiency (§3.4.1 / D1): ONE predicate
  // for "does this pack carry the sanctioned served-zoom affordance", so the
  // three sites cannot disagree about the shape again.
  const servedZoomAffordance = hasServedZoomAffordance(result);
  // L1 (2026-08-08): the doc-authority sliver grant, same shape as
  // servedZoomAffordance but reachable on an EDIT route. One sanctioned section
  // zoom over an already-complete CODE working set is an affordance, not missing
  // evidence, so `prepared` must survive it — exactly the D1 reconciliation.
  // (In the main path the grant is applied after this contract is built; this
  // keeps the invariant true in either order.)
  const servedDocSliverAffordance = (result.route?.max_additional_tl_calls ?? 0) === 1
    && (result.route?.reason?.includes(DOC_SLIVER_ROUTE_MARKER) ?? false);
  const routeCandidate = (result.route?.action === "edit_from_handles"
      || result.route?.action === "answer_from_handles")
    && result.missing.every((entry) => entry.startsWith("explicit-gap:"))
    && result.content_sufficiency !== "needs-followup"
    && ((result.route.max_additional_tl_calls ?? 0) === 0 || servedZoomAffordance || servedDocSliverAffordance);
  // A resolved non-artifact create target is its own terminal frontier. The
  // fenced content can make profile inference look multi-concern, but those
  // content tokens describe the new file; they are not pre-existing concerns
  // that must independently resolve before create:true is authorized.
  const resolvedCreateCandidate = profile !== "artifact_build"
    && result.create_target !== undefined;
  const profileCandidate = resolvedCreateCandidate
    ? true
    : profile === "wiring"
      ? result.wiring?.status === "ready"
    : profile === "artifact_build"
      ? hasRequiredArtifactSections(result, query)
      : profile === "change_propagation"
        ? result.change_contract?.status === "ready"
        : profile === "multi_concern"
          ? (result.concerns?.length ?? 0) > 0 && result.concerns?.every((concern) => concern.status === "covered") === true
          : true;
  const baseReady = answerCandidate || (routeCandidate && profileCandidate);
  const proofComplete = obligations.every((obligation) => obligation.status === "proved");
  // D1's own comment says the quiet part: "a zoom affordance is not missing
  // evidence". The falsification/risk engine still (correctly) flags the
  // affordance surface's own undisclosed remainder as unresolved — that is
  // exactly what the affordance's one sanctioned call is FOR — so that
  // unresolved obligation alone must not reject a served-zoom-affordance
  // pack. Scoped tightly: every OTHER required surface must already carry
  // served code, or a genuinely starved surface the affordance does not name
  // still blocks acceptance exactly as before.
  const hasUnservedRequiredSurface = codeTaskPackSurfaces(result.surfaces)
    .some((surface) => surface.required !== false && !hasServedCode(surface));
  // A digest alone is not evidence that THIS epoch has an obligation to
  // discharge: lane state is created before the first obligation is added and
  // may also contain an unrelated, non-matching epoch after a restart. A
  // matching ledger with obligations still requires its branded discharge
  // certificate; only an empty projection may proceed without one.
  //
  // A-F3 (2026-08-28): THAT RULE WAS FAIL-OPEN ON ITS OWN TARGET CLASS.
  // `taskContractDigest(workspace) === undefined` — no ledger at all — read as
  // ESTABLISHED, so the one state in which nothing has been proved was the
  // easiest way to satisfy the theorem. Combined with the vacuous certificate
  // an empty ledger used to mint, an exhaustive request could reach `accepted`
  // having recorded no requirement whatsoever. An open-universe query is
  // exactly the class A-4..A-7 exist for: for it, no ledger means NOT
  // established, and only the branded discharge certificate over a non-empty
  // ledger admits the act. `currentOpenUniverseExplicitGap` is deliberately not
  // an escape here — this builder's final invocation runs after
  // `recordEpochTaskContract` has persisted that same gap, so the durable proof
  // is available by the time the decision ships.
  //
  // Non-exhaustive tasks keep the v0.12-compatible empty/absent-ledger accept.
  // What they may NOT do is claim a ledger discharge while doing it, and
  // `dischargeCertificate` now enforces that half: with no obligations there is
  // no certificate, so the id stays in the legacy single-segment form.
  const ledgerEstablished = hasOpenUniverseIntent(query)
    ? ledgerCertificate !== undefined
    : workspace === undefined
      || taskContractDigest(workspace) === undefined
      || ledgerCertificate !== undefined
      // The current response's expansion limitation is already verified; the
      // durable ledger is its continuation/replay record, not a prerequisite
      // for recognizing this pack's own explicit capability gap.
      || currentOpenUniverseExplicitGap
      || (ledger.open.length === 0 && ledger.explicitGaps.length === 0);
  // -------------------------------------------------------------------------
  // A-F6 (2026-08-28): THE ACCEPTANCE LAYERS, and the OFF arm that is exactly
  // v0.12.
  //
  // The shipped expression read `(!proofCompletion || proofComplete) &&
  // (!proofCompletion || ledgerEstablished)`, which collapses to `true` when
  // the switch is OFF — so OFF was neither v0.13 nor v0.12 but a THIRD, LOOSER
  // behavior in which `baseReady` alone certified. The compatibility switch has
  // to mean "v0.12 semantics", or the parity spec measures nothing and the
  // escape hatch is more permissive than either release.
  //
  // LAYER 1, the theorem (A-6(1)): every readiness obligation carries a proof,
  // and — under proof completion — the durable ledger backing them is
  // established and discharged. Coverage classification and the risk score are
  // NOT premises of it.
  //
  // LAYER 2, risk, as SUPPRESSION ONLY: it can withhold a theorem that holds,
  // never admit one that does not. That keeps ON a restriction of OFF rather
  // than a different rule, which is what makes the two comparable in a decision
  // run.
  const theoremHolds = proofComplete && (!proofCompletion || ledgerEstablished);
  const riskSuppresses = risk.decision === "reject";
  // E-1: every terminal act remains theorem-backed. A served-zoom
  // affordance authorizes one bounded follow-up, but it is not a proof of
  // unrelated answer obligations or of the task ledger's discharge. The
  // affordance may absorb only the expected residual on the selected evidence
  // surface (including its behavior body); an open-universe, identifier,
  // concern, or ledger obligation still blocks the act.
  const affordanceResidual = obligations.some((obligation) =>
    obligation.status !== "proved"
    && obligation.id !== "surface-content"
    && obligation.id !== "behavior-body"
  );
  const accepted = baseReady
    && (
      (theoremHolds && !riskSuppresses)
      || (servedZoomAffordance && !hasUnservedRequiredSurface && !affordanceResidual)
    );
  // The route relabel (answer_from_handles) is query-shape truth even when §14
  // serves the pack as generic; the terminal act must agree with it, or the
  // contract instructs an edit on a question.
  const answerShapedRoute = result.route?.action === "answer_from_handles";
  const terminalAction: "answer" | "edit" = profile === "answer" || answerShapedRoute ? "answer" : "edit";
  const continuationCall = result.continuation?.stages[0]?.calls[0];
  const parsedNextCandidate = result.next ? nextStringToCall(result.next) : undefined;
  const currentPackSpans = packServedSpans(result);
  // The route builder can publish a zoom before same-pack sibling evidence is
  // folded into the served-window ledger. Treat that parsed next as consumed
  // immediately when the current response already covers its exact range; the
  // later persisted ledger cannot repair a stale candidate that wins here.
  const parsedNext = parsedNextCandidate !== undefined
    && nextCallCoveredBySpans(result, parsedNextCandidate, currentPackSpans)
    ? undefined
    : parsedNextCandidate;
  const proofNext = nextCallForUnresolved(result, obligations, query, workspace);
  // A pre-contract next can be stale after an authoritative absence has
  // discharged the open-universe axis. Keep it only when the current proof
  // model still has a matching unresolved call; otherwise the same search
  // would be emitted again despite the durable explicit-gap witness.
  const parsedNextAfterProof = openUniverseDischarged && proofNext === undefined
    ? undefined
    : parsedNext;
  // No unconditional tree fallback: every fallback must name evidence that
  // can resolve the current proof gap. Codeless surfaces re-serve their handle;
  // partial surfaces request the first explicitly omitted range.
  const requiredSurfaces = codeTaskPackSurfaces(result.surfaces)
    .filter((surface) => surface.required !== false);
  const codelessRequired = requiredSurfaces.find((surface) => !hasServedCode(surface));
  const gapFallback: ContinuationCall | undefined = codelessRequired
    ? { tool: "read_file", arguments: { handle: codelessRequired.handle } }
    : undefined;
  const partialRequired = requiredSurfaces
    .map((surface) => ({ surface, range: firstUnservedPartialRange(workspace, surface, servedZoomScope, packServedSpans(result)) }))
    .find((entry): entry is { surface: TaskPackSurface; range: string } => entry.range !== undefined);
  const partialFallback: ContinuationCall | undefined = partialRequired
    ? {
        tool: "read_file",
        arguments: { handle: partialRequired.surface.handle, range: partialRequired.range },
      }
    : undefined;
  // Serve-honesty (2026-08-01): a disclosed-but-unserved file — byte-budget
  // shed with an embedded re-request handle, or a caller/query-named path that
  // never served — is a concrete read affordance. Without it, a pack whose
  // only gap sits in missing[] falls to awaiting-input and the observed caller
  // fallback is native IO on exactly that path.
  const missingAffordance: ContinuationCall | undefined = (() => {
    for (const item of result.missing) {
      const handleMatch = /read_file handle=(h[0-9a-z]+)/.exec(item);
      if (handleMatch) return { tool: "read_file", arguments: { handle: handleMatch[1]! } };
      const namedMatch = /^(.+?) \((?:named but unserved|query-named unserved)\)$/.exec(item);
      if (namedMatch) return { tool: "read_file", arguments: { path: namedMatch[1]! } };
    }
    return undefined;
  })();
  const artifactFallback = profile === "answer" && isCandidateListPack(result)
    ? answerArtifactDiscoveryCall(result, query)
    : undefined;
  // choose-candidate close (2026-07-19a): once every candidate body is served
  // inline, no read-only follow-up can disambiguate further — the choice is
  // the caller's. Suppressing the bounded-call fallbacks here is what lands
  // the typestate on awaiting-input instead of a discovery next_call that
  // re-serves already-served handles (the T13 rep0 thrash fuel).
  const contractSurfaces = codeTaskPackSurfaces(result.surfaces);
  const candidateChoicePending = !accepted
    && (result.route?.action === "confirm_candidates" || result.coverage_reason === "candidate-list")
    && contractSurfaces.length > 1
    && contractSurfaces.every(hasServedCode)
    && artifactFallback === undefined;
  const concernChoicePending = !accepted
    && result.route?.action === "confirm_candidates"
    && (result.concern_ambiguities?.length ?? 0) > 0;
  const humanChoicePending = candidateChoicePending || concernChoicePending;
  // 2026-07-31: `answer_from_handles` is the pack's OWN verdict that discovery
  // is closed and the bundled evidence is the answer. recomputeRouteAndNextAfterTrim
  // already strips `next`/`continuation` on that route — but this contract is
  // assembled independently of the route, so it re-minted the very call the
  // route had just dropped. Observed live: route.action=answer_from_handles with
  // max_additional_tl_calls:0 served in the same envelope as phase:"discovery"
  // plus a search_files next_call. Deliberately scoped to the answer route: the
  // edit side keeps its no-dead-end override below, which exists for starved
  // required surfaces.
  // F-V13-6: "the pack's OWN verdict that discovery is closed" is exactly what
  // an unproved ENUMERATED item contradicts — the same proof model, saying the
  // request named something the served bytes do not cover. Without this the
  // stricter obligation would land the caller on a bare
  // `await_input:"act-on-served-evidence"`, whose frozen wire member carries no
  // `gaps` key, so the open items could not be NAMED and the honest disclosure
  // would be silent. Scoped to the new obligation class: a pack that could not
  // previously mint one is byte-identical to before. The escape is bounded by
  // `enumeratedItemFindCall`, which proposes each item's find at most once per
  // session and nothing at all once they are spent.
  const enumeratedItemOpen = obligations.some((obligation) =>
    obligation.status !== "proved"
    && obligation.id.startsWith(ENUMERATED_ITEM_OBLIGATION_PREFIX));
  const routeClosedAnswer = result.route?.action === "answer_from_handles"
    && (result.route.max_additional_tl_calls ?? 0) === 0
    && !enumeratedItemOpen;
  const candidateCall = accepted || humanChoicePending || routeClosedAnswer
    ? undefined
    : [artifactFallback, continuationCall, proofNext, parsedNextAfterProof, gapFallback, partialFallback, missingAffordance]
        .filter((call): call is ContinuationCall => call !== undefined)
        .map((call) => admitReadOnlyNextCall(call, query))
        .find((call): call is ContinuationCall => call !== undefined);
  const callBudget = valueCallBudget(
    terminalAction,
    baseReady,
    accepted,
    falsification.unresolved.length,
    risk,
    candidateCall,
  );
  let nextCall = callBudget.discovery_allowed ? candidateCall : undefined;
  // No-dead-end rule (2026-07-30): while required surfaces are still
  // code-starved, one bounded evidence-grounded call always beats an
  // awaiting-input verdict an autonomous caller cannot act on — the
  // value-of-information budget does not price in the cost of a contract
  // whose only remaining action is request-user-input.
  // Artifact surfaces carry their content in artifact_sections, not in
  // surface.code — a fully-inlined deck/sheet must count as served evidence
  // for the terminal grant (observed: a named-deck audit pack served both
  // decks inline yet still verdicted awaiting-input/"request user input").
  // Deliberately NOT folded into hasServedCode: slide text is answer
  // evidence, never editable code, and the edit-side sufficiency checks
  // must keep rejecting it.
  const artifactContentByPath = new Map<string, boolean>();
  for (const entry of result.artifact_sections ?? []) {
    if (entry === null || typeof entry !== "object") continue;
    const path = (entry as { path?: unknown }).path;
    const section = (entry as { section?: unknown }).section;
    if (typeof path !== "string" || section === undefined) continue;
    const bytes = Buffer.byteLength(JSON.stringify(section), "utf8");
    if (bytes > 200) artifactContentByPath.set(path, true);
  }
  const servedForGrant = (surface: TaskPackSurface): boolean =>
    hasServedCode(surface) || artifactContentByPath.get(surface.path) === true;
  // codeTaskPackSurfaces() excludes kind:"artifact" entries, so an all-artifact
  // pack would leave requiredSurfaces empty and the grant permanently off —
  // judge the grant over the FULL surface list (code + artifact), same
  // required!==false convention.
  const grantSurfaces = result.surfaces.filter(
    (surface): surface is TaskPackSurface =>
      typeof (surface as { path?: unknown }).path === "string"
      && (surface as { required?: boolean }).required !== false,
  );
  const requiredServed = grantSurfaces.length > 0 && grantSurfaces.every(servedForGrant);
  const tiedConcernAmbiguity = (result.concern_ambiguities?.length ?? 0) > 0;
  if (!accepted && nextCall === undefined && candidateCall !== undefined && !requiredServed && !tiedConcernAmbiguity) {
    // Tied concern candidates are excluded: repository evidence cannot break
    // a tie, so expanding it is bounded-waste by design (multiConcern spec) —
    // that verdict stays with the user. Plain starvation is different: the
    // named call fetches evidence that does not exist in context yet.
    nextCall = candidateCall;
  }
  const effectiveBudget = nextCall !== undefined && !callBudget.discovery_allowed
    ? {
        ...callBudget,
        discovery_allowed: true,
        reason: `${callBudget.reason}; overridden: starved required surfaces make one bounded call cheaper than a dead end`,
      }
    : callBudget;
  // A rejected ready candidate with no bounded discovery call must not fall
  // through to the eventual terminal action. There is no certificate, so
  // answer/edit would bypass the readiness gate; ask the user to resolve the
  // ambiguity instead.
  const awaitingUserInput = !accepted && nextCall === undefined;
  const readiness: TaskExecutionContract["readiness"] = accepted
    ? terminalAction === "answer" ? "answer-ready" : "edit-ready"
    : result.route?.action === "confirm_candidates" || result.coverage_reason === "candidate-list"
      ? "choose-candidate"
      : "needs-followup";
  // Grant the terminal action on a served-but-proof-rejected pack ONLY for the
  // needs-followup family (e.g. concern proof rejected a fully-served frontier).
  // choose-candidate keeps the 2026-07-16a fence: a single served candidate is
  // not authorization — its route already sanctions a bounded follow-up, so it
  // is not a dead end.
  const grantServedTerminal = requiredServed && readiness === "needs-followup" && !humanChoicePending;
  const actionFrontier = actionFrontierForCertificate(
    result,
    profile,
    obligations,
    workspace !== undefined ? priorEpochActionFrontier(workspace, query) : [],
  );
  const certificate = accepted
    ? deterministicCertificate(query, profile, obligations, falsification, risk, actionFrontier, workspaceState, ledgerCertificate?.digest)
    : undefined;
  const reason = accepted
    ? terminalAction === "answer"
      ? "proof-carrying answer evidence passed falsification and risk gates"
      : "proof-carrying edit frontier passed falsification and risk gates"
    : falsification.unresolved.length > 0
      ? `ready rejected: unresolved proof ${falsification.unresolved[0]}`
      : risk.decision === "reject"
        ? `ready rejected: false-ready risk ${risk.estimated_false_ready_risk} exceeds ${risk.max_false_ready_risk}`
        : result.route?.reason ?? "task surfaces still need bounded follow-up";
  const chooseCandidateHandles = candidateChoicePending
    ? [...new Set(contractSurfaces.map((surface) => surface.handle))].slice(0, MAX_BATCH_NEXT_HANDLES)
    : [];
  const reasonFinal = awaitingUserInput
    ? candidateChoicePending
      ? `choose-candidate: every candidate body is served inline (handles ${chooseCandidateHandles.join(",")}); pick the surface matching the task and act on it — bounded expansion is closed`
      : concernChoicePending
        ? `choose-candidate: repository evidence leaves concern candidates tied (${result.concern_ambiguities?.map(({ id }) => id).join(",")}); request the intended target before acting`
        : grantServedTerminal
          ? `${reason}; the selected windows of every required surface are served — act on the served evidence (${terminalAction} against the served handles), widening a window via its handle first if the target lies outside what was served; evidence_model.unresolved lists the open concerns`
          : `${reason}; no evidence-grounded read/search call remains, so request user input before acting`
    : reason;
  // protocol v1, A.7.2 / A.9.2 row 21: emit the awaiting-input CODE from the
  // branch that took the decision. The four arms below are the complete arm set
  // of the ternary above (one conditional under one guard,
  // `awaitingUserInput = !accepted && nextCall === undefined`), so the mapping
  // is exhaustive here rather than a sample — and it is derived from the same
  // booleans the prose is, never parsed back out of the prose.
  //
  // ARM 3 IS RE-SITED ON THE WIRE — [R5-30] / ruling 6, 2026-08-14. This file
  // still MARKS the branch with `"act-on-served-evidence"`, because the token
  // names which branch took the decision and that is exactly what it is for;
  // what changed is that the token is no longer the last word on the DECISION
  // KIND. `protocol/decisionWire.ts`'s `projectTaskDecision` reads it and
  // re-sites the branch onto `act.answer`/`act.edit` when §2.1.1's floor
  // genuinely holds, onto `discover` with a concrete restoring call when it
  // does not, and leaves it here only when neither is true. The contract's
  // in-process `next_action`/`allowed_actions` grant below is deliberately NOT
  // touched: [R5-12] ratified it as alive in-process, and ruling 9 schedules
  // the three "gate open in-process, decision not `act.*`" branches — this is
  // the third — for a post-P3a audit of the GRANT. Ruling 6 does the decision
  // half only.
  const awaitInputCode: TaskExecutionContract["await_input_code"] = awaitingUserInput
    ? candidateChoicePending
      ? "choose-candidate"
      : concernChoicePending
        ? "name-intended-target"
        : grantServedTerminal
          ? "act-on-served-evidence"
          : "no-grounded-call-remains"
    : undefined;
  const evidenceModel = buildDecisionEvidenceModel(terminalAction, obligations, falsification, risk);
  const capabilityGaps = buildCapabilityGaps(result, obligations, nextCall, awaitingUserInput);
  const semanticClosure: TaskSemanticClosure = {
    version: 1,
    state: accepted ? "closed" : awaitingUserInput ? "awaiting-input" : "open",
    closure_id: certificate?.id ?? `open-${shaOfText(JSON.stringify({
      task: `${profile}\u0000${query}`,
      workspace: workspaceState.fingerprint,
      unresolved: evidenceModel.unresolved,
    })).replace(/^sha256:/, "").slice(0, 16)}`,
    obligations_total: obligations.length,
    obligations_proved: obligations.filter((obligation) => obligation.status === "proved").length,
    unresolved: evidenceModel.unresolved,
  };
  return {
    version: 1,
    state: accepted ? "ready" : "needs-followup",
    readiness,
    discovery_complete: accepted,
    next_action: accepted
      ? terminalAction
      : awaitingUserInput
        ? grantServedTerminal ? terminalAction : "request-user-input"
        : "followup",
    max_additional_discovery_calls: nextCall ? 1 : 0,
    reason: reasonFinal,
    ...(awaitInputCode !== undefined ? { await_input_code: awaitInputCode } : {}),
    workspace_state: workspaceState,
    evidence_model: evidenceModel,
    semantic_closure: semanticClosure,
    ...(capabilityGaps ? { capability_gaps: capabilityGaps } : {}),
    ...(certificate ? { readiness_certificate: certificate } : { falsification, readiness_risk: risk }),
    typestate: accepted
      ? {
          phase: "prepared",
          certificate_id: certificate!.id,
          // Declared answer keeps the strict read-only grant. An INFERRED
          // answer shape (§14 serves the pack as generic) stays permissive:
          // answer leads, edit remains reachable without a challenge turn —
          // the 2026-07-25 misfire measurement showed answer-shaped queries
          // are often edit tasks, so blocking edit here would re-create that
          // regression in the other direction.
          allowed_actions: terminalAction === "answer"
            ? profile === "answer" ? ["answer", "challenge"] : ["answer", "edit", "challenge"]
            : ["edit", "challenge"],
          challenge_required_for: terminalAction === "answer" && profile === "answer"
            ? ["read", "search", "edit"]
            : ["read", "search"],
        }
      : awaitingUserInput
        ? {
            phase: "awaiting-input",
            // choose-candidate: the pending input IS the choice, and acting on
            // a served candidate is how the caller supplies it — edit stays
            // allowed so closing discovery never costs an extra ask turn. No
            // fence is installed for a non-ready contract, so this is wire
            // guidance, not a guard change.
            allowed_actions: candidateChoicePending
              ? terminalAction === "answer"
                ? ["answer", "edit", "request-user-input"]
                : ["edit", "request-user-input"]
              : grantServedTerminal
                // Same rationale as choose-candidate above: every required
                // body is already in context, so acting on it is the one
                // move that closes the task without an ask turn. No fence is
                // installed for a non-ready contract — wire guidance only.
                ? [terminalAction, "request-user-input"]
                : ["request-user-input"],
            challenge_required_for: [],
          }
        : {
            phase: "discovery",
            allowed_actions: ["read", "search"],
            challenge_required_for: [],
          },
    call_budget: effectiveBudget,
    ...(nextCall ? { next_call: nextCall } : {}),
  };
}

/**
 * Keep proof payloads from competing with evidence surfaces during trim.
 *
 * Root cause (2026-08-21 smoke-gate T05c dead-end forensics): this allowlist
 * used to omit `workspace_state` — a small fixed-size marker (a handful of
 * numbers, two short strings, one sha256 hex string; nowhere near the
 * kilobyte-scale evidence bodies this function actually exists to shed). For
 * a pack that fits comfortably under its byte cap, that omission was masked:
 * a LATER, un-trimmed `buildTaskExecutionContract(...)` rebuild (the
 * "post-trim route recomputation" pass, further down this same pipeline)
 * overwrites `execution_contract` wholesale and restores it. But a pack that
 * is STILL over cap after that rebuild runs this function a second time (the
 * `!fitsInCap(trimmed)` compaction step) with nothing left to restore it
 * afterward. decisionWire.ts's projectCertificate requires workspace_state
 * (or a top-level result.workspace_state that production code never actually
 * populates) to construct the wire certificate — without it, an otherwise
 * fully-proved "prepared/ready/edit" contract silently degrades to
 * `await_input:"no-grounded-call-remains"` with zero candidates and zero
 * next, because a "prepared" phase contract carries no next_call to fall
 * back to either. Observed live: an 8-surface, fully-covered multi_concern
 * aeroctl fix pack (all three reported bugs located) discarded outright.
 */
function contractForTrim(contract: TaskExecutionContract): TaskExecutionContract {
  const certificate = contract.readiness_certificate;
  return {
    version: contract.version,
    state: contract.state,
    readiness: contract.readiness,
    discovery_complete: contract.discovery_complete,
    next_action: contract.next_action,
    max_additional_discovery_calls: contract.max_additional_discovery_calls,
    reason: contract.reason,
    typestate: contract.typestate,
    ...(contract.workspace_state ? { workspace_state: contract.workspace_state } : {}),
    ...(certificate ? {
      readiness_certificate: {
        ...certificate,
        obligations: certificate.obligations.map((obligation) => ({
          id: obligation.id,
          kind: obligation.kind,
          status: obligation.status,
          required: true as const,
          evidence: [],
          ...(obligation.evidence_ids ? { evidence_ids: obligation.evidence_ids } : {}),
          reason: obligation.status === "proved"
            ? "proved by referenced evidence"
            : obligation.reason ?? obligation.status,
        })),
        ...(certificate.falsification !== undefined ? {
          falsification: {
            ...certificate.falsification,
            checked: certificate.falsification.checked.slice(0, 3),
            counterexamples: certificate.falsification.counterexamples.slice(0, 2),
            unresolved: certificate.falsification.unresolved.slice(0, 2),
          },
        } : {}),
      },
    } : {}),
    ...(contract.call_budget ? { call_budget: contract.call_budget } : {}),
    ...(contract.next_call ? { next_call: contract.next_call } : {}),
  };
}

/**
 * Last proof-only compaction before any source evidence is discarded.
 * The certificate id remains bound to the full proof and workspace state;
 * frontiers and evidence stay available on the task pack itself.
 *
 * 2026-08-21 (T05c byte-cap regression, follow-up to the workspace_state
 * fix above): this used to hand-roll a certificate that still carried a
 * truncated `falsification` report, a `risk` model minus `factors`, and a
 * per-obligation `evidence: []` + `reason: status` pair — bytes
 * `certificateBindingFor`'s own doc comment already named as wasted on
 * exactly this tier ("~256 bytes of proof on a receipt whose whole claim is
 * 'you already hold this'"), and the TaskReadinessObligation/
 * TaskReadinessCertificate fields those bytes fill (`evidence`, `reason`,
 * `falsification`, `risk`) are documented OPTIONAL for precisely this
 * binding-not-proof reason. No production reader consults them once a
 * certificate reaches this stage: decisionWire.ts's projectCertificate reads
 * only id/obligation-ids/workspace; leanExecutionContract.ts's `prepared`
 * projection reads only id/obligation-ids/allowed/frontier; contractForTrim
 * (above) has already zeroed every obligation's `evidence` and dropped the
 * contract-level `evidence_model` regardless. Delegating to
 * certificateBindingFor — written for the same "carries identity, not
 * prose" tier on the pack-unchanged re-serve path — removes that dead
 * weight instead of re-deriving it, so retaining `workspace_state` (the
 * fix above) no longer has to cost an otherwise-fitting multi_concern pack
 * a required-adjacent surface (observed: T05c's paired mixer.hpp) to the
 * optional-surface eviction ladder below.
 */
export function contractForEvidenceRetention(contract: TaskExecutionContract): TaskExecutionContract {
  const compact = contractForTrim(contract);
  const certificate = compact.readiness_certificate;
  if (!certificate) return compact;
  return {
    ...compact,
    // 2026-07-25 T13 forensics: evidence_handles/action_frontier are short
    // handle-id arrays (≤16 entries), not evidence bytes — stripping them
    // produced a state:"ready" contract whose certificate had frontier:[]
    // (the fence armed from it could only refuse). certificateBindingFor
    // keeps both through every retention trim.
    readiness_certificate: certificateBindingFor(certificate),
  };
}

/**
 * The certificate BINDING a `pack-unchanged` re-serve carries — not the proof.
 *
 * §2.1.1 (C2-3) put a certificate on the compact re-serve because the wire
 * `decision` needs one: `projectTaskDecision` degrades an `act` to
 * `await_input:"no-grounded-call-remains"` unless it can build a `CertificateRef`
 * (id + NON-EMPTY obligation ids + workspace marker), and A.4's `pack-unchanged`
 * gate rejects a body whose decision moved. What it did NOT need was the PROOF:
 * `contractForEvidenceRetention`'s output still carried the falsification report,
 * the selective-prediction risk model, and a per-obligation `evidence: []` +
 * `reason:"proved"` pair that only restated `status` — ~256 bytes of proof on a
 * receipt whose whole claim is "you already hold this".
 *
 * What a re-serve genuinely re-binds, and therefore all this keeps:
 *   - `id` + `task_fingerprint`  the fence's install key (`state/session.ts`'s
 *     `certificateId`/`taskFingerprint`) and `TaskRef.id` on the wire;
 *   - `obligations[].id` + `status`  `CertificateRef.obligations` (non-empty by
 *     type) and the fence's `obligationIds`;
 *   - `evidence_handles` + `action_frontier`  the fence's admissible set and
 *     `FrontierEntry[]`. The 2026-07-25 T13 forensics rule stands: stripping
 *     these two arms a fence that can only refuse.
 * The workspace marker rides `TaskExecutionContract.workspace_state` / the
 * pack's own top-level copy, which `projectCertificate` already reads.
 */
export function certificateBindingFor(
  certificate: TaskReadinessCertificate,
): TaskReadinessCertificate {
  return {
    version: certificate.version,
    id: certificate.id,
    task_fingerprint: certificate.task_fingerprint,
    // `workspace_state_fingerprint` is NOT re-bound here: it has no reader
    // anywhere in the tree (it is written at mint and never read back), and the
    // marker the wire actually uses — `CertificateRef.workspace` — is projected
    // from `TaskExecutionContract.workspace_state` or from the compact record's
    // own top-level `workspace_state`, both of which this response carries.
    profile: certificate.profile,
    obligations: certificate.obligations.map((obligation) => ({
      id: obligation.id,
      kind: obligation.kind,
      status: obligation.status,
      required: true as const,
    })),
    evidence_handles: certificate.evidence_handles,
    action_frontier: certificate.action_frontier,
  };
}
function reconcileTaskChangeContract(result: TaskPackResult): void {
  const contract = result.change_contract;
  if (contract === undefined) return;
  const servedByHandle = new Map(codeTaskPackSurfaces(result.surfaces).map((surface) => [surface.handle, surface]));
  for (const obligation of contract.obligations) {
    const surface = servedByHandle.get(obligation.handle);
    obligation.status = surface !== undefined && hasSufficientEditContent(surface) ? "ready" : "needs-context";
  }
  const nonContextMissing = contract.missing.filter((item) => !item.startsWith("context:"));
  const contextMissing = contract.obligations
    .filter((item) => item.required && item.status === "needs-context")
    .map((item) => "context:" + item.id);
  contract.missing = [...new Set([...nonContextMissing, ...contextMissing])];
  contract.status = contract.missing.length === 0 ? "ready" : "needs-followup";
  contract.discovery_complete = contract.status === "ready";
  contract.max_additional_tl_calls = contract.status === "ready"
    ? 0
    : Math.max(1, contract.max_additional_tl_calls);
  if (contract.status === "needs-followup" && result.route?.action === "edit_from_handles") {
    result.route.reason = "read the contract's needs-context handles once, then batch the edits";
    result.route.max_additional_tl_calls = 1;
  }
}

/**
 * Keep the advisory route/profile summaries subordinate to the authoritative
 * execution contract. A provisional edit route can be rejected by the later
 * falsification/readiness pass; leaving coverage=complete, wiring=ready, and
 * change_contract=ready beside phase=discovery made agents trust the wrong
 * half of the response and is exactly the T13 false-complete shape.
 */
export function reconcileTaskPackExecutionContract(result: TaskPackResult): void {
  const contract = result.execution_contract;
  if (!contract || contract.state === "ready") return;

  const hasTerminalContradiction = result.route?.action === "edit_from_handles"
    || result.next?.startsWith("edit_file ") === true
    || result.wiring?.status === "ready"
    || result.change_contract?.status === "ready";
  if (!hasTerminalContradiction) return;

  const unresolved = contract.falsification?.unresolved ?? [];
  if (result.wiring !== undefined || result.change_contract !== undefined) {
    // Role/surface coverage and action readiness are distinct for a generic
    // candidate pack. Only structured change/wiring profiles claim complete
    // edit closure and therefore need a coverage demotion when proof rejects.
    result.coverage = "partial";
    result.coverage_reason = "concerns-uncovered";
  }
  const preservedDiscoveryRoute = result.route !== undefined
    && result.route.action !== "edit_from_handles"
    && (result.route.max_additional_tl_calls ?? 0) > 0
    ? result.route
    : undefined;
  result.route = {
    action: "locate_missing_surfaces",
    reason: contract.reason,
    max_additional_tl_calls: contract.next_call ? 1 : 0,
  };

  if (contract.next_call) {
    const plan = enforceContinuationBudget({
      version: 1,
      stages: [{ execution: "sequential", calls: [contract.next_call] }],
    });
    if (plan) {
      result.continuation = plan;
      const next = deriveNextFromPlan(plan);
      if (next) result.next = next;
      else delete result.next;
    }
  } else if (preservedDiscoveryRoute !== undefined) {
    // Proof rejection demotes the pack's EDIT claim, not its sanctioned
    // discovery. A pre-existing bounded non-terminal route (e.g. the
    // unresolved-concern synthesis) stays legal — overwriting it with a
    // zero-call locate route strips every machine-executable action from the
    // response (observed live: T05c 2026-07-30 rep2, 16-escape desertion).
    result.route = preservedDiscoveryRoute;
    if (result.next !== undefined && result.next.startsWith("edit_file")) delete result.next;
    if (result.continuation?.stages[0]?.calls[0]?.tool === "edit_file") delete result.continuation;
  } else {
    // Strip only edit-flavored guidance — the contradiction being repaired is
    // the edit claim; a read/search-shaped next stays legal and keeps one
    // bounded call open.
    if (result.next === undefined || result.next.startsWith("edit_file")) {
      delete result.next;
      delete result.continuation;
    } else {
      result.route.max_additional_tl_calls = 1;
    }
  }

  if (result.change_contract?.status === "ready") {
    result.change_contract.status = "needs-followup";
    result.change_contract.discovery_complete = false;
    result.change_contract.max_additional_tl_calls = contract.next_call ? 1 : 0;
    result.change_contract.missing = [
      ...new Set([
        ...result.change_contract.missing,
        ...unresolved.map((id) => `execution:${id}`),
      ]),
    ];
  }

  if (result.wiring?.status === "ready") {
    const missing = new Set(result.wiring.missing);
    if (unresolved.includes("wiring-source")) missing.add("source");
    if (unresolved.includes("wiring-consumer") || unresolved.includes("wiring-insertion")) {
      missing.add("destination");
    }
    result.wiring.status = "needs-followup";
    result.wiring.missing = [...missing];
    result.wiring.edit_frontier = [];
    for (const connection of result.wiring.connections) connection.status = "needs-followup";
  }
}

/**
 * I-6 fix (v0.13.1 forensics, DESIGN-v0.13-plan.md §6 2026-08-30 entry;
 * bench/workflows/experiments/2026-08-30-v0131-forensics/{REPORT.md,root/
 * i6_fallback_probe.mjs,root/i6-fallback-result.json}): whether ONE of
 * `enforceNoDeadEndContract`'s own last-resort candidates has already run.
 *
 * `hasExecutedNext` fingerprints the WHOLE call — tool, action, AND every
 * task/budget/scope field the real dispatch carried (packServeLog.ts's
 * `nextFingerprint`/`semanticTask`). A real search_files dispatch almost
 * always carries SOME task envelope (`taskEpoch`/`taskProfile`/...), while
 * this guard's own bare `{action:"tree"}` candidate carries none — so their
 * fingerprints never match, and `hasExecutedNext` alone can never see that
 * the candidate already ran (measured: recording a real dispatch's args
 * `{cwd,lane,action:"tree",taskEpoch:"new",taskProfile:"answer"}` and then
 * checking the bare candidate against it returns `false` — the two
 * fingerprints differ only in the task field the candidate never carries).
 * Root's unit probe reproduced the resulting loop end-to-end: the identical
 * pathless-tree next came back after the SAME workspace/lane had already
 * executed it (`i6-fallback-result.json`: `repeated_identically:true`).
 *
 * `consultExecutedSearch`'s coarser `${action}::${query-or-path}` ledger is
 * immune to that task/epoch noise — the same two-ledger reasoning
 * `receiptContractWithoutConsumedSearch` below already documents for the
 * receipt path — so this checks it too, scoped to the one candidate shape
 * that is actually pathless: a bare `search_files` tree. server.ts's
 * `recordExecutedSearch` call now keys a pathless tree under the workspace
 * root (`treePath ?? workspace`), matched here with the identical fallback,
 * so a bare-tree candidate consulted here and a bare-tree call recorded
 * there always agree on one key.
 */
function noDeadEndCandidateAlreadyExecuted(
  ledger: { workspace: string; lane: string },
  candidate: ContinuationCall,
): boolean {
  const candidateArgs = (candidate.arguments ?? {}) as Record<string, unknown>;
  if (hasExecutedNext(ledger.workspace, ledger.lane, candidate.tool, candidateArgs)) return true;
  if (candidate.tool === "search_files" && candidateArgs["action"] === "tree") {
    const treePath = typeof candidateArgs["path"] === "string" ? candidateArgs["path"] : undefined;
    return consultExecutedSearch(ledger.workspace, "tree", treePath ?? ledger.workspace) !== undefined;
  }
  return false;
}

/**
 * Final no-dead-end invariant (2026-07-30): a non-prepared pack must always
 * carry at least one machine-executable action — a bounded next/continuation,
 * a route with sanctioned calls, or a granted terminal action. A response
 * whose only allowed action is request-user-input is unactionable for an
 * autonomous caller, and the live consequence is native-tool desertion
 * (T05c rep2 2026-07-30: 16 native escapes immediately after exactly this
 * shape). Belt-and-suspenders behind the builder/reconciler fixes; counts
 * via checks[] whenever it has to rewrite so the rate is observable.
 */
export function enforceNoDeadEndContract(
  result: TaskPackResult,
  query: string,
  /**
   * A-F2: the executed-next ledger this pack is being built against. Without
   * it the guard is free to re-open a call whose result the session already
   * consumed — which is why it had to run BEFORE suppression, and why the
   * suppression that ran after it could leave a dead end nothing repaired.
   * Optional so the readiness specs' synthetic, workspace-free contracts keep
   * their pre-A-F2 call shape.
   */
  ledger?: { workspace: string; lane: string },
): void {
  const contract = result.execution_contract;
  if (contract === undefined) return;
  const phase = contract.typestate.phase;
  if (phase === "prepared" || phase === "acting" || phase === "verifying" || phase === "done") return;
  if (result.route !== undefined && (result.route.reason === undefined || result.route.reason === "")) {
    result.route.reason = contract.reason;
  }
  const grantsTerminal = contract.typestate.allowed_actions.includes("edit")
    || contract.typestate.allowed_actions.includes("answer");
  const hasBoundedCall = contract.next_call !== undefined
    || result.next !== undefined
    || result.continuation !== undefined
    || (result.route?.max_additional_tl_calls ?? 0) > 0;
  // Tied concern candidates stay a user decision by design (multiConcern
  // spec): the ambiguity inventory IS the actionable content, and repository
  // evidence cannot break a tie — synthesizing a find would just expand it.
  if ((result.concern_ambiguities?.length ?? 0) > 0) return;
  if (grantsTerminal || hasBoundedCall) return;
  const surfaces = codeTaskPackSurfaces(result.surfaces);
  const codeless = surfaces.find((surface) => !hasServedCode(surface));
  const unmatched = unmatchedConcernTokens(query, surfaces)[0];
  const candidates: ContinuationCall[] = [
    ...(codeless !== undefined ? [{ tool: "read_file", arguments: { handle: codeless.handle } } as ContinuationCall] : []),
    ...(unmatched !== undefined ? [{ tool: "search_files", arguments: { action: "find", query: unmatched } } as ContinuationCall] : []),
    { tool: "search_files", arguments: { action: "tree" } },
  ];
  // A-F2: re-opening a call whose result is already consumed is the same
  // non-progress this guard exists to prevent, one indirection removed. With a
  // ledger in hand the guard picks the first UNEXECUTED candidate; with every
  // candidate consumed it leaves the contract alone rather than manufacturing
  // a loop, and the disclosure written by repairSuppressedNextCall stands.
  // I-6 fix: "unexecuted" is decided by noDeadEndCandidateAlreadyExecuted
  // (hasExecutedNext PLUS a pathless-tree-aware consultExecutedSearch check —
  // see that function's own doc comment), not by hasExecutedNext alone.
  const call = ledger === undefined
    ? candidates[0]!
    : candidates.find((candidate) => !noDeadEndCandidateAlreadyExecuted(ledger, candidate));
  if (call === undefined) return;
  const plan = enforceContinuationBudget({
    version: 1,
    stages: [{ execution: "sequential", calls: [call] }],
  });
  if (plan) {
    result.continuation = plan;
    const next = deriveNextFromPlan(plan);
    if (next !== undefined) result.next = next;
  }
  result.route = {
    action: "locate_missing_surfaces",
    reason: "no-dead-end guard: re-opened one bounded evidence call after the contract stripped every machine-executable action",
    max_additional_tl_calls: 1,
  };
  contract.next_call = call;
  contract.max_additional_discovery_calls = 1;
  contract.next_action = "followup";
  if (contract.typestate.phase === "awaiting-input") {
    contract.typestate = { phase: "discovery", allowed_actions: ["read", "search"], challenge_required_for: [] };
  }
  if (contract.semantic_closure !== undefined && contract.semantic_closure.state === "awaiting-input") {
    contract.semantic_closure.state = "open";
  }
  if (contract.call_budget !== undefined) {
    contract.call_budget = {
      ...contract.call_budget,
      discovery_allowed: true,
      reason: `${contract.call_budget.reason}; no-dead-end guard re-opened one bounded call`,
    };
  }
  result.checks = [...(result.checks ?? []), "no-dead-end-guard: rewrote a dead-end contract (telemetry)"];
}

/** Cap on the number of per-site sibling checks emitted (item 2). */
const MAX_PER_SITE_CHECKS = 8;

/** ALL-CAPS-constant / enum-member token shape (mirrors the locator's member key regex). */
const ENUM_MEMBER_TOKEN_RE = /\b[A-Z][A-Z0-9_]{1,}\b/g;

/**
 * Item 2 (per-site sibling checks): the enum's EXISTING sibling members, read
 * from the located contract surface(s)' own content — the same ALL-CAPS
 * key-shape the locator's extractMemberIdentifiers uses. Used to decide which
 * query token is a NOVEL member (a value being ADDED, e.g. CRITICAL/BLOCKED)
 * versus an existing one that is already present everywhere. Normalized for
 * comparison.
 */
function existingSiblingMembers(surfaces: TaskPackSurface[], workspace: string, cache?: FileReadCache): Set<string> {
  const members = new Set<string>();
  for (const s of surfaces) {
    if (s.role !== "contract") continue;
    const raw = readCached(workspace, s.path, cache);
    if (raw === undefined) continue;
    for (const m of raw.match(ENUM_MEMBER_TOKEN_RE) ?? []) members.add(normIdent(m));
  }
  return members;
}

/**
 * Item 2: a surface that is a per-file enumeration SITE of the family — an
 * initializer / aggregation map (sibling-enumeration / exhaustive-initializer),
 * a presentation/style file, or the contract definition itself. These are the
 * files whose per-site obligation (the new member must be present in THIS file)
 * is machine-verifiable, so each gets its own single-token check scoped to its
 * exact path. Detected by role plus the summarized `why` markers those sites
 * carry (whySummary transforms the raw `why`, so match its output forms).
 */
function isPerSiteCheckSurface(s: TaskPackSurface): boolean {
  if (s.role === "style" || s.role === "contract") return true;
  // A propagation-discovered seed is intentionally re-routed through the
  // caller-supplied seeded builder, whose generic `why` no longer says
  // "initializer". Required API/data/config surfaces are nevertheless exact
  // propagation obligations and need the same per-file novel-member check.
  if (s.required && ["api", "data", "config"].includes(s.role)) return true;
  const why = (s.why ?? "").toLowerCase();
  return /sibling|initializer|aggregation|enumerat|family/.test(why);
}

/** Escape a literal for embedding in a RegExp (matches the file's inline convention). */
function escapeForRegExp(s: string): string {
  return s.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
}

/**
 * IMPROVEMENT C: candidate enum/union TYPE symbol names for the dynamic-consumer
 * check — PascalCase identifiers named in the query (TicketPriority, Status,
 * OrderState, ...) plus the symbol of any contract-role surface. These are the
 * symbols a dynamic consumer derives from (Object.values(TicketPriority),
 * z.nativeEnum(TicketPriority), Record<TicketPriority, ...>). Empty when the
 * type cannot be named — the per-site check is then always kept (conservative).
 */
export function enumSubjectSymbols(query: string, surfaces: TaskPackSurface[]): string[] {
  const out = new Set<string>();
  // PascalCase-ish: starts uppercase AND contains a lowercase (so an ALL-CAPS
  // member token like ONHOLD is NOT mistaken for the enum TYPE).
  for (const m of query.match(/\b[A-Z][A-Za-z0-9]*[a-z][A-Za-z0-9]*\b/g) ?? []) out.add(m);
  for (const s of surfaces) {
    if (s.role === "contract" && s.symbol) out.add(s.symbol);
  }
  return [...out];
}

/** IMPROVEMENT C: true when `body` derives from enum `sym` via a DYNAMIC form. */
export function dynamicDerivationOf(body: string, sym: string): boolean {
  const S = escapeForRegExp(sym);
  const patterns: RegExp[] = [
    // TS/JS
    new RegExp(`Object\\.(?:values|keys|entries)\\s*\\(\\s*${S}\\b`),
    new RegExp(`\\bnativeEnum\\s*\\(\\s*${S}\\b`),          // z.nativeEnum(S)
    new RegExp(`\\bRecord\\s*<\\s*${S}\\b`),                 // Record<S, ...>
    new RegExp(`\\bkeyof\\s+typeof\\s+${S}\\b`),             // keyof typeof S
    new RegExp(`\\b${S}\\s*\\[\\s*[^\\]\\s]`),               // indexed access S[expr] (not S[] array type)
    // Python
    new RegExp(`\\bfor\\s+\\w+\\s+in\\s+${S}\\b`),           // for x in S
    new RegExp(`\\bin\\s+${S}\\s*:`),                        // comprehension `... in S:`
    new RegExp(`\\b${S}\\.__members__\\b`),                  // S.__members__
    new RegExp(`\\blen\\s*\\(\\s*${S}\\s*\\)`),              // len(S)
  ];
  return patterns.some((re) => re.test(body));
}

/**
 * IMPROVEMENT C: decide whether a per-site "member present in <file>" obligation
 * should be SUPPRESSED for a file that consumes the enum ONLY via dynamic
 * derivation (no literal member edit needed).
 *
 *  - dynamic evidence: `body` references some subject symbol via a derivation
 *    form (see dynamicDerivationOf).
 *  - static-enumeration evidence (BLOCKS suppression): `body` contains >=2 OTHER
 *    member literals of the same enum — a positive sign it enumerates members
 *    statically (switch cases / literal maps), so the new member DOES belong here.
 *
 * Suppress ONLY on positive dynamic evidence AND absence of static evidence.
 * Returns the matched subject symbol so the informational entry can name it.
 */
export function dynamicEnumConsumerVerdict(
  body: string,
  subjectSymbols: readonly string[],
  siblingMembers: ReadonlySet<string>,
): { suppress: boolean; symbol?: string } {
  if (subjectSymbols.length === 0) return { suppress: false };
  // Static evidence: how many DISTINCT existing sibling members appear as
  // literal ALL-CAPS member tokens in the body (same extraction the contract
  // uses, normalized for comparison).
  const bodyMembers = new Set((body.match(ENUM_MEMBER_TOKEN_RE) ?? []).map(normIdent));
  let staticHits = 0;
  for (const m of siblingMembers) if (bodyMembers.has(m)) staticHits++;
  const hasStatic = staticHits >= 2;
  let matched: string | undefined;
  for (const sym of subjectSymbols) {
    if (dynamicDerivationOf(body, sym)) { matched = sym; break; }
  }
  if (matched === undefined) return { suppress: false };
  return { suppress: !hasStatic, symbol: matched };
}

/**
 * DESIGN-v0.8 coverage-honesty (Part 5): structured, closure-trackable check
 * records. A record with a `token`+`glob` is machine-verifiable by edit_code
 * closure tracking (satisfied when `token` appears in a file matching `glob`);
 * a record with `desc` only is advisory. Emitted per role that STRUCTURALLY
 * exists in the located surfaces' dominant root (never a phantom role).
 *
 * Ids are CONTENT-derived (deriveCheckId: role + glob + token set; role +
 * desc for advisory records) so the same logical check keeps ONE id across
 * repeat same-task packs and different checks never collide. The positional
 * `chk-<n>` scheme this replaces was NOT stable across packs: emission order
 * shifts with surface inventory, so an epoch merge could overwrite an OPEN
 * check with an unrelated same-index one (closure then verified the wrong
 * token) or accumulate duplicates of a re-emitted check under new indexes.
 */
function buildCheckRecords(
  query: string,
  surfaces: TaskPackSurface[],
  workspace: string,
): PackCheckRecord[] {
  const records: PackCheckRecord[] = [];
  const push = (r: Omit<PackCheckRecord, "id">): void => {
    const id = deriveCheckId(r);
    // Identical identity = the same logical check; keep the first emission
    // (required-surface per-site checks are pushed before optional ones).
    if (records.some((x) => x.id === id)) return;
    records.push({ id, ...r });
  };

  // Item 5: camelCase-robust enum gate so a camelCase-only query still emits
  // the machine-verifiable style/api check records (and, below, the per-site
  // sibling checks from item 2).
  if (isEnumLikeQueryShaped(query)) {
    // Roles that structurally exist in the located surfaces' own dominant
    // root — never emit a style record for a root that has no stylesheets.
    const root = surfaces.length > 0
      ? dominantRoot(surfaces.map((s) => s.path), (p) => projectRootOf(p))
      : null;
    const inventory = roleInventoryFor(workspace, root);
    const tokens = extractEnumValueTokens(query);
    const tokenLabel = tokens[0] ?? "the new value";

    // contract/api: advisory (desc only) — no literal token to grep, the edit
    // is structural (add to the family/initializers).
    if (inventory.has("contract")) {
      push({ desc: `contract: add ${tokenLabel} to the family definition and exhaustive initializers`, role: "contract" });
    }
    if (inventory.has("api")) {
      push({ desc: `api: handle ${tokenLabel} in validation, transitions, and aggregation enumerations`, role: "api" });
    }

    // style: machine-verifiable — the `--<kebab>` custom property must exist
    // in a stylesheet. Only when the root actually HAS a style role.
    if (inventory.has("style") && tokens.length > 0) {
      const kebab = kebabTokenVariant(tokens[0]!);
      const matcher = kebabCheckToken(tokens[0]!);
      if (kebab && matcher) {
        push({
          desc: `style: token ${kebab} (or a namespaced --*${matcher}) present in stylesheets`,
          token: matcher,
          glob: "*.css",
          role: "style",
        });
      }
    }

    // Item 2 (per-site sibling checks): each enumeration SITE (initializer /
    // aggregation map / style / contract surface) gets its OWN single-token
    // check scoped to that exact file's rel path, so closure can tell the agent
    // WHICH site still lacks the new member (the enum-extension forensic: stats
    // map, board columns, badge config across ~8 files had no per-file check, so
    // closure could not say which remained). The token is the NOVEL member (a
    // value being ADDED, not already a sibling); for a style site the token is
    // its CSS custom-property form (--kebab) since the raw ALL-CAPS constant
    // does not appear verbatim in a stylesheet. Capped at MAX_PER_SITE_CHECKS,
    // required surfaces first.
    const existing = existingSiblingMembers(surfaces, workspace);
    const novelMembers = tokens.filter((t) => !existing.has(normIdent(t)));
    if (novelMembers.length > 0) {
      const siteSurfaces = surfaces
        .filter(isPerSiteCheckSurface)
        .sort((a, b) => (a.required === b.required ? 0 : a.required ? -1 : 1)); // required first
      // IMPROVEMENT C: memoize the dynamic-consumer verdict per site file so a
      // file that consumes the enum ONLY via dynamic derivation gets an
      // informational entry instead of a false "member present" gate (which
      // sent agents into npm-test rabbit holes). `existing` (the contract's
      // sibling members) is the static-enumeration evidence source.
      const subjectSymbols = enumSubjectSymbols(query, surfaces);
      const verdictCache = new Map<string, { suppress: boolean; symbol?: string }>();
      const verdictFor = (s: TaskPackSurface): { suppress: boolean; symbol?: string } => {
        let v = verdictCache.get(s.path);
        if (v === undefined) {
          const body = readCached(workspace, s.path);
          v = body === undefined
            ? { suppress: false } // body unavailable -> keep the check (conservative)
            : dynamicEnumConsumerVerdict(body, subjectSymbols, existing);
          verdictCache.set(s.path, v);
        }
        return v;
      };
      let perSiteEmitted = 0;
      const informedPaths = new Set<string>();
      for (const member of novelMembers) {
        const kebab = kebabCheckToken(member);
        for (const s of siteSurfaces) {
          if (perSiteEmitted >= MAX_PER_SITE_CHECKS) break;
          // IMPROVEMENT C: dynamic consumer (Object.values(E)/nativeEnum(E)/
          // Record<E,...>/keyof typeof E) with no static enumeration evidence —
          // no literal edit is required here. Emit one informational (advisory,
          // token-less, never reported open) entry per such file and skip the
          // literal obligation for every novel member.
          const verdict = verdictFor(s);
          if (verdict.suppress) {
            if (!informedPaths.has(s.path)) {
              informedPaths.add(s.path);
              push({
                desc: `${path.basename(s.path)}: derives from ${verdict.symbol} dynamically — no literal edit required`,
                role: s.role,
              });
              perSiteEmitted++;
            }
            continue;
          }
          // A stylesheet stores the member as its CSS custom-property form,
          // not the raw ALL-CAPS constant — match the single-dash suffix form
          // there (see kebabCheckToken: namespaced --color-priority-<kebab>
          // must satisfy too).
          const token = s.role === "style" ? kebab : member;
          if (token === undefined) continue;
          push({
            token,
            glob: s.path,
            role: s.role,
            desc: `${s.role}: ${member} present in ${path.basename(s.path)}`,
          });
          perSiteEmitted++;
        }
        if (perSiteEmitted >= MAX_PER_SITE_CHECKS) break;
      }
    }

    // search/filter/lookup/autocomplete: the value's user-visible text must
    // match. Scope the glob to the api surface's directory when identifiable;
    // otherwise leave advisory (desc only, no token — an unscoped literal
    // search over the whole repo is too loose to be a useful closure check).
    if (/\b(search|filter|lookup|autocomplete)\b/i.test(query) && tokens.length > 0) {
      const dir = apiSurfaceDir(surfaces);
      if (dir) {
        push({
          desc: `search/filter: user-visible text for ${tokens[0]} must match`,
          token: tokens[0]!.toLowerCase(),
          glob: dir,
          role: "api",
        });
      } else {
        push({ desc: `search/filter: user-visible text for ${tokens[0]} must match`, role: "api" });
      }
    }
  }

  // rename/bug/spec queries: today's advisory single line, as a record with no
  // token (advisory-only — closure tracking never reports it open).
  if (hasRenameRequestMarker(query)) {
    push({ desc: "rename: update imports, exports, tests, docs, and string/config references that intentionally name the symbol" });
  }

  // Closure-hardening: a cross-surface WIRING check (source→destination
  // co-occurrence) when the query connects two distinct-role surfaces. Emitted
  // for any query shape (not just enum-like) since wiring is orthogonal to the
  // enum/rename shapes above. A plain enum/value propagation is deliberately
  // excluded: filename-grounded words such as "enum" are not runtime signals,
  // and turning them into a co-occurrence check creates a permanently-open
  // false obligation. An explicit wiring verb still opts an enum task in.
  if (!isEnumLikeQuery(query) || hasWiringIntentMarker(query)) {
    const wiring = buildWiringCooccurrenceCheck(query, surfaces, workspace);
    if (wiring) push(wiring);
  }

  return records;
}

/**
 * Build the pack's public `checks` string[] AND the structured records the
 * session's closure tracker consumes. The public field stays string[] (Part 5:
 * "the pack's public `checks` field STAYS string[]"); `records` is recorded
 * separately via recordPackChecks in dedupeTrimAndPersist. Lines are derived
 * from the same shape as before, capped at MAX_COMPLETION_CHECKS and
 * MAX_CHECK_LINE_CHARS.
 *
 * FIX-A (2026-07-09d forensics): `unmatchedConcern` — the SAME gated token
 * list route.reason already names ("concern(s) not covered: telemetry,
 * sensor") — takes priority over the generic enum contract/api/ui template
 * below when non-empty: the template was "the same generic 3-line ...
 * template on every pack regardless of task", while naming the actual
 * uncovered concern(s) is directly actionable. Callers with no meaningful
 * concept of an unmatched concern for this pack (the no-candidate abstain,
 * the seeded paths[] flow — see their own call sites' comments) omit it,
 * which keeps their existing (unconditional enum-template) behavior.
 */
function buildCompletionChecks(
  query: string,
  surfaces: TaskPackSurface[],
  missing: string[],
  workspace: string,
  unmatchedConcern: string[] = [],
): { lines: string[]; records: PackCheckRecord[] } {
  const present = new Set(surfaces.map((s) => s.role));
  const checks: string[] = [];

  if (unmatchedConcern.length > 0) {
    // Name the specific gap instead of the boilerplate template — matches
    // route.reason's own "concern(s) not covered: ..." phrasing so the two
    // fields never disagree about what is actually missing.
    checks.push(`concern(s) not covered: ${unmatchedConcern.join(",")} — locate/confirm before declaring done`);
  } else if (isEnumLikeQueryShaped(query)) {
    // Item 5: camelCase-robust enum gate (matches the record-derivation gate).
    checks.push("contract: update exported enum/schema/type definitions and any Record/switch initializers");
    checks.push("api/domain: update validation, transition, repository/search, and aggregation paths that enumerate the value");
    if (surfaces.some((s) => s.role === "ui" || s.role === "style")) {
      checks.push("ui/style: update badges/forms/filters plus design tokens or class names for the new value");
    }
  }

  if (hasRenameRequestMarker(query)) {
    checks.push("rename: update imports, exports, tests, docs, and string/config references that intentionally name the symbol");
  }

  // A "<role>: not covered" hint is only actionable when that role actually
  // EXISTS somewhere in this workspace but this pack didn't reach it. In a
  // repo with no presentation layer at all (e.g. C++ firmware), emitting
  // "ui: not covered" / "style: not covered" is phantom guidance that sends
  // the agent to locate a surface that cannot exist — mirror the
  // workspaceHasRole() guard that requiredSurfacesForTask already applies.
  for (const role of ["contract", "api", "domain", "ui", "style", "test"]) {
    if (checks.length >= MAX_COMPLETION_CHECKS) break;
    const eligible =
      missing.includes(role) ||
      (!present.has(role) && checks.length > 0 && ["contract", "api", "ui", "style"].includes(role));
    if (eligible && workspaceHasRole(workspace, role as ImpactSurface)) {
      checks.push(`${role}: not covered by this pack; run targeted locate/search before declaring done`);
    }
  }

  const lines = [...new Set(checks)]
    .slice(0, MAX_COMPLETION_CHECKS)
    .map((l) => (l.length > MAX_CHECK_LINE_CHARS ? l.slice(0, MAX_CHECK_LINE_CHARS) : l));
  return { lines, records: buildCheckRecords(query, surfaces, workspace) };
}

/**
 * Names that mark a directory as a self-contained project/toolchain root.
 * Used to stop the upward package.json walk at the surface's OWN project
 * boundary instead of crediting a distant ancestor's package.json (e.g. a
 * monorepo host root, or — inside a benchmark fixture tree — the outer
 * repo that happens to have its own unrelated `npm test`).
 */
const PROJECT_BOUNDARY_MARKERS = ["Makefile", "makefile", "CMakeLists.txt", "Cargo.toml", "go.mod", "pom.xml", "build.gradle", "pyproject.toml", "setup.py"];

function hasProjectBoundaryMarker(workspace: string, dir: string): boolean {
  return PROJECT_BOUNDARY_MARKERS.some((name) => fs.existsSync(path.join(workspace, dir, name)));
}

/**
 * Walk upward from `rel`'s directory looking for the NEAREST package.json.
 * Stops (without crediting a test/typecheck script) as soon as a non-npm
 * project-boundary marker (Makefile, CMakeLists.txt, Cargo.toml, ...) is
 * found first — that marker means this subtree owns its own toolchain and
 * an ancestor's package.json belongs to an unrelated host project, not this
 * surface's own build.
 */
function findNearestNpmScripts(workspace: string, rel: string): { dir: string; pkg: { scripts?: Record<string, string> } } | null {
  const parts = rel.split("/");
  for (let i = parts.length - 1; i >= 0; i--) {
    const dir = parts.slice(0, i).join("/");
    if (hasProjectBoundaryMarker(workspace, dir)) return null;
    const pkgPath = path.join(workspace, dir, "package.json");
    try {
      // P3 (2026-08-27): decodeTextBuffer, not a raw utf8 read; an
      // undecodable package.json is treated the same as an absent/unparsable
      // one — keep walking upward, never crash and never JSON.parse garbage.
      const decoded = decodeTextBuffer(fs.readFileSync(pkgPath));
      if (decoded === null) continue;
      const pkg = JSON.parse(decoded) as { scripts?: Record<string, string> };
      return { dir, pkg };
    } catch {
      // keep walking upward
    }
  }
  return null;
}

/**
 * DESIGN-v0.8 §C2 item 1: the project root that OWNS most of the selected
 * surfaces — reuses locateTaskContext's own `projectRootOf` (the
 * `bench/fixtures/<name>` vs host-repo boundary A2 already established)
 * rather than a second, potentially-drifting definition of the same
 * boundary. Majority vote over the surfaces' own roots (count weight, via
 * the shared `dominantRoot` helper in util/dominantRoot.ts — see its doc
 * comment for the general shape locateTaskContext.ts's `computeDominantRoot`
 * also builds on), not just surfaces[0]'s root: a pack whose primary
 * surface happens to be the one contaminating outlier must still resolve to
 * the root the REST of the pack actually lives under. Ties break toward
 * whichever root is encountered first (stable, deterministic — surfaces are
 * already in a meaningful priority order by the time this runs). Adapts
 * `dominantRoot`'s `null`-on-empty to this call site's original `""`-on-
 * empty contract, so every existing caller is unaffected.
 */
function dominantRootOf(surfaces: TaskPackSurface[]): string {
  return dominantRoot(surfaces, (s) => projectRootOf(s.path)) ?? "";
}

/**
 * DESIGN-v0.8 §A7: byte cap for a partial pack's embedded `tree` field. Kept
 * well under the pack's own byte cap (the tree is a navigation aid, not the
 * payload) and independently of exploreTree's own TREE_CAP_BYTES so a full
 * `explore action=tree` call can return a larger inventory than the inline
 * partial-pack teaser.
 */
const PARTIAL_TREE_CAP_BYTES = 512;

/**
 * The workspace-relative common-ancestor directory of `surfaces`' paths that
 * are under the dominant root — the subtree an agent following THIS pack would
 * most plausibly want a wider inventory of. Returns "" (whole-root) when the
 * cluster shares no directory below the root itself.
 */
function partialTreeSubPath(surfaces: TaskPackSurface[]): string {
  const dominantRoot = dominantRootOf(surfaces);
  const clusterPaths = surfaces.map((s) => s.path).filter((p) => projectRootOf(p) === dominantRoot);
  return commonAncestorDir(clusterPaths.length > 0 ? clusterPaths : surfaces.map((s) => s.path));
}

/**
 * DESIGN-v0.8 §A7: on a PARTIAL pack with at least one surface, attach a
 * compact one-line-per-dir tree of the surfaces' dominant-root cluster as
 * `result.tree`, and mention `explore action=tree` in `next` so the agent
 * reaches for the (larger, byte-capped) explore inventory instead of a native
 * `find`. No-op on complete packs, empty-surface packs, or an unavailable
 * tree. Runs BEFORE trimToCap so its bytes count toward the budget and the
 * trim ladder can drop it first when the pack is over cap.
 */
function attachPartialTree(result: TaskPackResult, workspace: string): void {
  if (result.coverage !== "partial" || result.surfaces.length === 0) return;
  const subPath = partialTreeSubPath(result.surfaces);
  const compact = buildCompactTree(workspace, subPath || undefined, undefined, PARTIAL_TREE_CAP_BYTES);
  if (compact.tree.length === 0) return;
  result.tree = compact.tree;
  // Point the agent at the fuller inventory rather than a native `find`, but
  // never concatenate two commands into legacy `next`: that string must stay
  // exactly parseable into one schema-valid execution_contract.next_call.
  // The compact tree is already present inline, so an existing primary next is
  // sufficient. Use tree only when no more specific follow-up exists.
  const treeHint = `search_files action=tree${subPath ? ` path=${subPath}` : ""}`;
  if (result.next === undefined) result.next = treeHint;
}

/** Cap on the number of `verify` command hints returned (DESIGN-v0.8 §C1 item 4: lowered from 5 to 2). */
const MAX_VERIFY_HINTS = 2;

function discoverVerificationHints(workspace: string, query: string, surfaces: TaskPackSurface[]): string[] {
  void query;
  const hints = new Set<string>();
  // DESIGN-v0.8 §C2 item 1: scope every hint below to paths under the
  // SELECTED surfaces' own dominant root — a host-repo surface that leaked
  // into an otherwise fixture-scoped pack (or vice versa) must not seed a
  // `cd packages/vscode-extension && npm test` / `cd bench/fixtures/x && ...`
  // hint the rest of the pack has nothing to do with. findNearestNpmScripts'
  // own PROJECT_BOUNDARY_MARKERS guard already stops a single path from
  // walking PAST its own project boundary; this is the complementary guard
  // against a MIXED-root surface set feeding the wrong path in at all.
  const dominantRoot = dominantRootOf(surfaces);
  const paths = surfaces.map((s) => s.path).filter((p) => projectRootOf(p) === dominantRoot);

  for (const rel of paths) {
    const found = findNearestNpmScripts(workspace, rel);
    if (!found) continue;
    const { dir, pkg } = found;
    if (pkg.scripts?.test) {
      hints.add(dir ? `cd ${dir} && npm test` : "npm test");
    }
    if (pkg.scripts?.typecheck) {
      hints.add(dir ? `cd ${dir} && npm run typecheck` : "npm run typecheck");
    }
  }

  if (paths.some((p) => p.endsWith(".go"))) hints.add("go test ./...");
  if (paths.some((p) => p.endsWith(".rs")) || fs.existsSync(path.join(workspace, "Cargo.toml"))) hints.add("cargo test");
  if (paths.some((p) => p.endsWith(".py"))) hints.add("pytest");
  if (paths.some((p) => p.endsWith(".java"))) {
    if (fs.existsSync(path.join(workspace, "pom.xml"))) hints.add("mvn test");
    if (fs.existsSync(path.join(workspace, "gradlew"))) hints.add("./gradlew test");
  }

  // C/C++ awareness (item 7): ungated on query keywords — any pack whose
  // surfaces are C/C++ needs a verify hint regardless of what words the
  // query used. Prefer a real build-system command when one exists in the
  // surface's own subtree; otherwise say plainly that there is no
  // discoverable toolchain, rather than staying silent or emitting an
  // unrelated `npm test` from a distant ancestor.
  const nativePaths = paths.filter((p) => /\.(c|cc|cpp|cxx|h|hpp|hh|hxx)$/.test(p));
  if (nativePaths.length > 0) {
    let buildCmd: string | null = null;
    for (const rel of nativePaths) {
      const parts = rel.split("/");
      for (let i = parts.length - 1; i >= 0; i--) {
        const dir = parts.slice(0, i).join("/");
        if (fs.existsSync(path.join(workspace, dir, "CMakeLists.txt"))) {
          buildCmd = dir ? `cmake --build ${dir}` : "cmake --build .";
          break;
        }
        if (fs.existsSync(path.join(workspace, dir, "Makefile")) || fs.existsSync(path.join(workspace, dir, "makefile"))) {
          buildCmd = dir ? `make -C ${dir}` : "make";
          break;
        }
      }
      if (buildCmd) break;
    }
    hints.add(buildCmd ?? "no build toolchain detected for this native module: verify by tracing the code path against the reported symptom");
  }

  return [...hints].slice(0, MAX_VERIFY_HINTS);
}

// ---------------------------------------------------------------------------
// Convert an ImpactCandidate to a TaskPackSurface (minting handles).
// ---------------------------------------------------------------------------

/**
 * Resolve a candidate's named symbol to the parser's real body range.
 *
 * Locator candidates can carry an enclosing symbol name together with a
 * keyword-centered range. Treating that pair as a symbol handle is misleading:
 * the handle says "class/function" while covering only a few lines around the
 * hit. Prefer the exact parsed range when available; keep the locator window on
 * unsupported languages or parse failure.
 */
async function parsedCandidateSymbolRange(
  candidate: ImpactCandidate,
  workspace: string,
  cache?: FileReadCache,
): Promise<string | undefined> {
  if (!candidate.symbol) return undefined;
  const content = readCached(workspace, candidate.path, cache);
  const lang = languageForPath(candidate.path);
  if (content === undefined || lang === undefined) return undefined;
  const parsed = cache
    ? await cache.parsedSymbols(content, lang, candidate.path)
    : await collectSymbols(content, lang, {}).catch(() => null);
  if (!parsed || parsed.length === 0) return undefined;

  const normalizedTarget = candidate.symbol.toLowerCase().replace(/[^a-z0-9]/g, "");
  if (normalizedTarget.length === 0) return undefined;
  const named = parsed.filter((symbol) => {
    const normalizedName = symbol.name.toLowerCase().replace(/[^a-z0-9]/g, "");
    return normalizedName === normalizedTarget
      || normalizedTarget.endsWith(normalizedName)
      || normalizedName.endsWith(normalizedTarget);
  });
  if (named.length === 0) return undefined;
  const containing = named.filter((symbol) =>
    symbol.startLine <= candidate.line && symbol.endLine >= candidate.line
  );
  const chosen = (containing.length > 0 ? containing : named)
    .sort((a, b) => (a.endLine - a.startLine) - (b.endLine - b.startLine))[0];
  return chosen ? `${chosen.startLine}-${chosen.endLine}` : undefined;
}

/** Cap on remaining-range disclosure entries per surface (wire weight). */
const MAX_REMAINING_RANGE_ENTRIES = 3;

/**
 * Serve-honesty (2026-08-01): bounded remaining-span disclosure for a served
 * surface — embed-trim spans first (they are the precise zoom targets), then
 * the served range's complement within the WHOLE file. Empty when the file is
 * fully covered. Completeness stamped relative to the served range only
 * (the pre-fix §4.1 behavior) let a surface that embedded its whole range
 * while covering part of the file read as complete (T05c rep2:
 * mode_manager.cpp served 136-309 of 309 with no stamp, no remaining_ranges),
 * so the guide's zoom affordance never fired and the caller's only visible
 * continuation was native IO.
 */
function partialityStamp(
  workspace: string,
  relPath: string,
  servedRange: string,
  embedRemaining: string[],
  cache?: FileReadCache,
  callerWindow = false,
): { content_completeness?: "partial"; remaining_ranges?: string[] } {
  const remaining = [...embedRemaining];
  // A caller-EXPLICIT range is a hard window (11a): its remainder protocol is
  // "advance INSIDE the window" — embed-trim spans only. Appending the file
  // complement would point the zoom OUTSIDE the window the caller pinned and
  // break remainder progress (every entry must start past the window head).
  const match = callerWindow ? null : /^(\d+)-(\d+)$/.exec(servedRange);
  const content = match ? readCached(workspace, relPath, cache) : undefined;
  if (match && content !== undefined) {
    const start = Number(match[1]);
    const end = Number(match[2]);
    const total = countLines(content);
    if (start > 1) remaining.push(`1-${start - 1}`);
    if (end < total) remaining.push(`${end + 1}-${total}`);
  }
  const deduped = [...new Set(remaining)].slice(0, MAX_REMAINING_RANGE_ENTRIES);
  if (deduped.length === 0) return {};
  // The "partial" LABEL stays scoped to an embed-level trim — the served
  // slice itself was cut (§4.1), which is what the readiness risk model's
  // partial-surface-content factor prices. A COMPLETE embed whose file merely
  // has more content discloses the remainder as zoomable remaining_ranges
  // without the label: it is a normal serving shape, not false-ready risk,
  // and labeling it demoted every windowed-but-whole serve to discovery
  // (4 replay-corpus prepared certificates regressed before this split).
  return embedRemaining.length > 0
    ? { content_completeness: "partial", remaining_ranges: deduped }
    : { remaining_ranges: deduped };
}

export async function candidateToSurface(
  candidate: ImpactCandidate,
  workspace: string,
  requiredRoles: string[],
  query: string,
  cache?: FileReadCache,
  options?: { answerProfile?: boolean; causalMultiMethod?: boolean },
): Promise<TaskPackSurface> {
  const callerScoped = candidate.why === "caller-supplied";
  candidate = refocusCandidateForQuery(candidate, workspace, query, cache);
  const role = candidate.surface as string;
  const rawRange = candidate.range ?? `${candidate.line}-${candidate.line}`;
  const parsedSymbolRange = await parsedCandidateSymbolRange(candidate, workspace, cache);
  // Item 11: a large markdown contract/spec candidate gets its range
  // widened to the whole enclosing section instead of the locator's fixed
  // keyword-centered window, so the pack can hand over the relevant
  // section as a real slice instead of leaving the agent to Read the
  // whole file natively. DESIGN-v0.8 §A5 deliverable 2: a C/C++ candidate
  // gets the same treatment, widened to its enclosing symbol instead of a
  // section — see widenNativeSymbolRange's doc comment.
  // A caller-EXPLICIT range is a hard window (11a parity for the pack path,
  // 2026-07-30): never widen it back to the enclosing symbol/section. A
  // remaining_ranges zoom that gets re-widened re-serves already-held bytes
  // and its remaining_ranges never advance (live T03 loop).
  const widenedRange = candidate.callerRange === true
    ? rawRange
    : candidate.symbol
      ? parsedSymbolRange ?? rawRange
      : isNativeExtPath(candidate.path)
        ? await widenNativeRangeString(workspace, candidate.path, rawRange, cache)
        : widenMarkdownSectionRange(workspace, candidate.path, rawRange, cache);
  const boundedRange = clampRangeToFile(workspace, candidate.path, widenedRange, cache);

  // B4: a range-kind candidate on a TINY file gets a WHOLE-FILE handle
  // (1-EOF). A partial-range handle on a tiny stub inevitably forces the agent
  // to re-slice for the rest of the file (the live tiny-stub failure). Symbol-kind
  // candidates keep their symbol-precise handle (a symbol handle already covers
  // the whole symbol; re-pointing it at the file would lose that precision).
  // Returns the widened range unchanged when the file is not tiny or is
  // unreadable, so non-tiny files are unaffected.
  const tinyWholeRange = candidate.symbol
    ? boundedRange
    : tinyFileWholeRange(workspace, candidate.path, boundedRange, cache);

  // Item 4 (tiny-file whole embed): for a TINY non-symbol file, DISPLAY the
  // whole-file range (and, below, slice the whole file as `code`) instead of a
  // narrow keyword window that would force a follow-up `read_code mode=slice`
  // just to see the rest of a small stub. Only when the whole-file slice
  // actually fits the per-surface byte cap (checked here so a tiny-but-dense
  // file between MAX_SURFACE_CODE_BYTES and TINY_FILE_MAX_BYTES keeps the
  // narrow window rather than losing its code entirely to sliceCode's own cap
  // rejection); trimToCap Phase 5 may still strip the whole-file code under
  // pack-level pressure (unchanged, honest). A large file keeps its windowed
  // slice — tinyWholeRange === widenedRange there, so `range` is unchanged.
  let range =
    !candidate.symbol && tinyWholeRange !== boundedRange && wholeTinyFileFitsCap(workspace, candidate.path, tinyWholeRange, cache)
      ? tinyWholeRange
      : boundedRange;

  // The handle covers the whole tiny file regardless of whether the DISPLAY
  // range was widened to it (a narrow display range still wants a whole-file
  // handle — the tiny-file fix), so mint against tinyWholeRange, not `range`.
  const handleRange = tinyWholeRange;

  // Reuse the handle already minted by locateTaskContext ONLY when the handle
  // range was not changed by widening OR the tiny-file clamp (either change
  // needs its own handle, since the original still points at the narrow
  // keyword window).
  let handleId: string;
  if (candidate.handle && handleRange === rawRange) {
    handleId = candidate.handle;
  } else {
    const kind = candidate.symbol ? "symbol" : "range";
    const entry = handleTable.upsert({
      kind,
      path: candidate.path,
      range: handleRange,
      ...(candidate.symbol ? { symbol: candidate.symbol } : {}),
      workspaceRoot: workspace,
    });
    handleId = entry.id;
  }

  // Derive a short why string from the candidate's why field.
  const summarizedWhy = candidate.why
    ? whySummary(candidate.why, candidate.symbol, role)
    : undefined;
  const why = callerScoped && summarizedWhy
    ? `${summarizedWhy}; caller-supplied`
    : summarizedWhy;

  // Build likely_edits: a single generic hint based on role (or the
  // candidate's raw `why`, when it carries more specific guidance — see
  // DESIGN-v0.8 §A6 deliverable 1 — NOT the human-summarized `why` local
  // above, which is display text, not the machine-checkable raw tag).
  const answerProfile = options?.answerProfile === true;
  const editIntent = answerProfile ? undefined : editIntentForRole(query, role, candidate.why);
  const doneCheck = answerProfile ? undefined : doneCheckForRole(query, role, candidate.why);
  const required = requiredRoles.includes(role) || candidate.required === true;
  // DESIGN-v0.8 §C1 item 1: likely_edits is boilerplate on every SECONDARY
  // surface — build it only for a required surface here; the pack-assembly
  // callers additionally run finalizePrimarySurface() so surfaces[0] keeps it
  // even when nothing in the pack is required.
  const likelyEdits = !answerProfile && required
    ? buildLikelyEdits(role, handleId, candidate.confidence)
    : [];

  // A2 item 1: embed the exact code for required/high-confidence surfaces so
  // the pack can close the task in one read instead of forcing a follow-up
  // mode=slice call per surface. Lower-confidence surfaces stay
  // handle-only (no `code`) to keep the pack compact — the agent widens via
  // `read_code mode=slice handle=<handle>` only when it actually needs that
  // content.
  // A markdown-contract-section match (item 11) always embeds its slice
  // when found at all: locating the right section IS the hard part, and a
  // handle without the section text still forces a follow-up read.
  // DESIGN-v0.8 §A5 deliverable 1: a NATIVE_EXTS (C/C++) surface also always
  // embeds, regardless of confidence — see isNativeExtPath's doc comment.
  const isHighConfidence = answerProfile || required || candidate.confidence >= HIGH_CONFIDENCE_THRESHOLD || candidate.why === "doc-contract-match" || isNativeExtPath(candidate.path);
  // DESIGN-v0.9 §4.1: centeredSliceForCap never returns undefined "solely
  // because of the cap" — an oversize surface gets a match-centered trimmed
  // body + remaining_ranges instead of the old code-less cliff. Center on the
  // candidate's own matched line when known (finer than the range midpoint).
  const centeredEmbed = isHighConfidence
    ? centeredSliceForCap(workspace, candidate.path, range, cache, candidate.line)
    : undefined;
  const fullAfterElision = centeredEmbed?.remaining_ranges.length
    ? sliceCodeAfterCommentElision(
        workspace,
        candidate.path,
        range,
        cache,
        options?.causalMultiMethod === true,
      )
    : undefined;
  let embed = fullAfterElision !== undefined
    ? { code: fullAfterElision, remaining_ranges: [] }
    : centeredEmbed;

  // F1 (2026-08-07): a complete embed (no cap-driven trim) whose range is
  // already the dominant majority of the file gets one shot at promotion to
  // the WHOLE file, shipped as a complete embed with no remaining_ranges at
  // all — this is what actually removes the deterministic follow-up
  // same-handle re-slice for the complement (measured: T05c-shaped packs,
  // 12/12 cells re-requested the exact head complement of a majority-served
  // native surface). Bounded by construction: it can never make a single
  // surface bigger than MAX_SURFACE_CODE_BYTES already allowed it to be
  // (centeredSliceForCap enforces that below, and a non-fit aborts the
  // promotion), and it only fires when the locate-time window was already
  // dominant, so a small match inside an unrelated small file keeps its
  // narrow, deliberately-scoped window. Symbol-kind and caller-pinned
  // ranges are hard windows (DESIGN-v0.8 §B4 / 11a) and are never widened.
  if (
    embed !== undefined
    && embed.remaining_ranges.length === 0
    && candidate.symbol === undefined
    && candidate.callerRange !== true
  ) {
    const wholeRange = wholeFileRangeIfDominant(workspace, candidate.path, range, cache);
    if (wholeRange !== undefined) {
      const wholeEmbed = centeredSliceForCap(workspace, candidate.path, wholeRange, cache, candidate.line);
      if (wholeEmbed !== undefined && wholeEmbed.remaining_ranges.length === 0) {
        range = wholeRange;
        embed = wholeEmbed;
        handleId = handleTable.upsert({
          kind: "range",
          path: candidate.path,
          range: wholeRange,
          workspaceRoot: workspace,
        }).id;
      }
    }
  }
  // A small file-complement is cheaper to inline than a guaranteed follow-up:
  // promote it only when the whole Markdown file fits the existing per-surface
  // cap. The pack-level trim still decides whether the resulting pack fits.
  if (
    !candidate.symbol
    && candidate.callerRange !== true
    && isMarkdownPath(candidate.path)
    && embed !== undefined
    && embed.remaining_ranges.length === 0
  ) {
    const wholeRange = wholeMarkdownRangeIfSmallRemainder(workspace, candidate.path, range, cache);
    if (wholeRange !== undefined && wholeRange !== range) {
      const wholeEmbed = centeredSliceForCap(workspace, candidate.path, wholeRange, cache, candidate.line);
      if (wholeEmbed !== undefined && wholeEmbed.remaining_ranges.length === 0) {
        range = wholeRange;
        embed = wholeEmbed;
        handleId = handleTable.upsert({
          kind: "range",
          path: candidate.path,
          range: wholeRange,
          workspaceRoot: workspace,
        }).id;
      }
    }
  }

  // DESIGN-v0.8 §A5 deliverable 3: a matched contract-doc surface also
  // carries the whole document's heading outline alongside its matched
  // section, so a multi-topic doc can be sliced targeted instead of
  // natively dumped when the task's OTHER topics live in a different
  // section. See buildContractDocOutline's doc comment.
  let outline = candidate.why === "doc-contract-match"
    ? buildContractDocOutline(workspace, candidate.path, cache)
    : undefined;
  // A broad answer symbol (typically a class/module) may exceed the contiguous
  // body cap. Bundle the query-ranked, independently sliceable member handles
  // so the one honest follow-up can batch them instead of paying one discovery
  // turn per method after a separate skeleton read.
  if (answerProfile && embed && embed.remaining_ranges.length > 0 && outline === undefined) {
    const content = readCached(workspace, candidate.path, cache);
    const focus = content === undefined
      ? undefined
      : await selectAnchorFocus(content, candidate.path, query, cache);
    if (focus) {
      const relevant = [focus.best, ...focus.runnerUps]
        .filter((item) =>
          item.range !== range
          && (!candidate.symbol || item.name.toLowerCase() !== candidate.symbol.toLowerCase())
        )
        .slice(0, ANCHOR_FOCUS_MAX_RUNNER_UPS + 1);
      if (relevant.length > 0) {
        outline = anchorFocusRunnerUpOutline(relevant, candidate.path, workspace);
      }
    }
  }

  return {
    role,
    handle: handleId,
    path: candidate.path,
    range,
    ...(candidate.symbol ? { symbol: candidate.symbol } : {}),
    ...(required ? { required: true } : {}),
    ...(why ? { why } : {}),
    // DESIGN-v0.8 §C1 item 2: omit when equal to the generic role fallback —
    // a role-specific string (enum-like queries) always survives since it
    // carries task information the fallback does not.
    ...(!answerProfile && editIntent !== genericEditIntent(role) ? { edit_intent: editIntent } : {}),
    ...(!answerProfile && doneCheck !== genericDoneCheck(role) ? { done_check: doneCheck } : {}),
    ...(likelyEdits.length > 0 ? { likely_edits: likelyEdits } : {}),
    ...(embed ? { code: embed.code } : {}),
    // §4.1 + serve-honesty (2026-08-01): completeness is stamped relative to
    // the FILE, not just the served range — see partialityStamp's doc comment.
    ...(embed
      ? partialityStamp(workspace, candidate.path, range, embed.remaining_ranges, cache, candidate.callerRange === true)
      : {}),
    ...(outline ? { outline } : {}),
  };
}

// ---------------------------------------------------------------------------
// Handle-grounded likely_edits: steer callers to inspect and edit the returned
// handle instead of falling back to path reads/writes. More syntax-specific
// edit intents can be added once task_pack includes exact slice text.
// ---------------------------------------------------------------------------

/**
 * Item 3: `edit_intent`/`done_check` are already carried ONCE at the
 * surface level (TaskPackSurface.edit_intent / .done_check). Earlier
 * versions repeated the identical strings a second time inside
 * likely_edits[handle-scoped-edit].intent/.done_check — pure duplicated
 * boilerplate that ate into the byte budget without adding information.
 * likely_edits now carries only the action-routing fields (kind/handle/
 * target/confidence); callers read intent/done_check from the surface.
 */
function buildLikelyEdits(
  role: string,
  handle: string,
  confidence?: number,
): Array<{ kind: string; handle?: string; target?: string; confidence?: number }> {
  const c = Math.max(0.1, Math.min(1, Math.round((confidence ?? 0.5) * 100) / 100));
  const hints: Array<{ kind: string; handle?: string; target?: string; confidence?: number }> = [
    {
      kind: "inspect-slice",
      handle,
      target: `read_file mode=slice handle=${handle}`,
      confidence: c,
    },
  ];

  if (!["data", "doc", "test"].includes(role)) {
    hints.push({
      kind: "handle-scoped-edit",
      handle,
      target: `edit_file handle=${handle} allowPathFallback=false`,
      confidence: Math.max(0.1, Math.round((c - 0.1) * 100) / 100),
    });
  }

  return hints;
}

// ---------------------------------------------------------------------------
// Derive a short "why" string from the locate candidate's raw why.
// ---------------------------------------------------------------------------

function whySummary(raw: string, symbol?: string, role?: string): string {
  if (raw === "exact-symbol" && symbol) return `defines ${symbol}`;
  if (raw === "exact-text:distinctive") return "matched distinctive identifier";
  if (raw === "query-symbol") return `symbol hit for ${role ?? "query"}`;
  if (raw === "reference") return "reference site";
  if (raw === "family-stem") return "style family member";
  if (raw === "variant-text") return "variant token match";
  if (raw === "path-token") return "path token match";
  if (raw === "enum-family") return "enumerated value family";
  if (raw === "presentation-family") return "presentation/style family";
  if (raw === "exhaustive-initializer") return "exhaustive initializer/aggregation map for this family";
  // F2: a below-coverage-bar partial member overlap. The summary deliberately
  // avoids isPerSiteCheckSurface's trigger substrings (sibling/initializer/
  // aggregation/enumerat/family) so this non-required surface spawns no per-site
  // closure obligation.
  if (raw === "related-member-hit") return "related member reference";
  if (raw === "doc-contract-match") return "matched section in a contract/spec doc";
  // Default: return the raw string truncated.
  return raw.length > 60 ? raw.slice(0, 60) : raw;
}

// ---------------------------------------------------------------------------
// Confidence-scaled byte cap (A2 item 2): a pack with no code-bearing
// surfaces stays at the base MAX_TASK_PACK_BYTES (compact, handle-first).
// A pack whose required/high-confidence surfaces carry embedded code is
// allowed to grow toward MAX_TASK_PACK_BYTES_HIGH_CONFIDENCE — just enough
// to fit that code instead of immediately discarding it in trimToCap's
// Phase 5. This keeps the LOW-confidence / discovery-only case compact
// while letting a genuinely task-closing pack actually close the task.
// ---------------------------------------------------------------------------

/**
 * DESIGN-v0.8 coverage-honesty (Part 4): the exact `why` markers
 * expandCppModuleClosure / augmentNativeSurfaceClosure stamp on a C/C++
 * header↔impl companion surface. A surface carrying one of these markers is a
 * module-closure companion of an already-selected native surface — its
 * embedded code is as load-bearing as a required surface's (live C++ cases:
 * a bug's lookup table lives in the .hpp companion of the matched .cpp), so it
 * gets high-confidence-cap treatment even though it is not itself `required`.
 */
const MODULE_CLOSURE_WHY_MARKERS: ReadonlySet<string> = new Set(["header-source-pair", "include-target"]);

/** True when a surface is a C/C++ module-closure companion (by its `why` marker). */
function isModuleClosureSurface(s: TaskPackSurface): boolean {
  return s.why !== undefined && MODULE_CLOSURE_WHY_MARKERS.has(s.why);
}

function rawCapForResult(result: TaskPackResult): number {
  if (result.wiring?.strategy === "semantic-multihop") {
    if ((result.wiring.connections[0]?.completion_proof?.structural_checks.length ?? 0) > 0) {
      return MAX_TASK_PACK_BYTES_CONSTRUCT_PROOF;
    }
    return MAX_TASK_PACK_BYTES_SEMANTIC_WIRING;
  }
  if (
    (result.artifact_requirements?.length ?? 0) > 1
    && result.artifact_sections?.length === result.artifact_requirements?.length
    && (
      result.execution_contract === undefined
      || result.execution_contract.state === "ready"
    )
  ) {
    return MAX_TASK_PACK_BYTES_ARTIFACT_BUNDLE;
  }
  if (
    result.task_profile === "artifact_build"
    && result.create_target !== undefined
    && (result.artifact_sections?.length ?? 0) > 0
  ) {
    // Extracted artifact sections are content-bearing proof even though they
    // are not code surfaces. Give a uniquely proved create frontier the same
    // bounded tier as required implementation code; otherwise 9-10KB of
    // useful XLSX/DOCX evidence collapses into the empty emergency fallback.
    return MAX_TASK_PACK_BYTES_HIGH_CONFIDENCE;
  }
  if (
    result.task_profile === "multi_concern"
    && (result.concerns?.length ?? 0) > 1
    && result.concerns?.every((concern) => concern.status === "covered") === true
    && codeTaskPackSurfaces(result.surfaces)
      .filter((surface) => surface.required !== false)
      .every(hasServedCode)
  ) {
    // Independent concerns have a real additive evidence floor. A fixed 8KB
    // cap forced a complete three-bug pack through the emergency empty-pack
    // fallback, making the cheapest run depend on agent luck. This tier is
    // available only while every concern still carries required code.
    return MAX_TASK_PACK_BYTES_MULTI_CONCERN;
  }
  // DESIGN-v0.8 §C1 item 3: needs_exact_code was always `code !== undefined`
  // at every emission site — the check now reads `code` directly instead of
  // the redundant boolean.
  // DESIGN-v0.8 coverage-honesty (Part 4): module-closure companion code
  // counts toward the high-confidence 16KB cap just like required-surface
  // code — a header+impl chain must be able to ship both bodies without
  // trimToCap stripping the companion's, which is exactly where the fix
  // lives.
  const hasHighConfCode = result.surfaces.some(
    (s) => s.code !== undefined && (s.required || isModuleClosureSurface(s)),
  );
  const hasAnyCode = result.surfaces.some((s) => s.code !== undefined);
  const base = hasHighConfCode
    ? MAX_TASK_PACK_BYTES_HIGH_CONFIDENCE
    : hasAnyCode
      ? Math.round((MAX_TASK_PACK_BYTES + MAX_TASK_PACK_BYTES_HIGH_CONFIDENCE) / 2)
      : MAX_TASK_PACK_BYTES;
  // DESIGN-v0.9 §4.8: content the server internally executed (§4.6b codeless
  // surface bodies — signalled by a non-empty `inlined[]`) may expand the
  // budget to the must-fetch tier, and ONLY behind TL_MUSTFETCH_EXPAND.
  // mustFetchPackCap returns `base` unchanged when the flag is off, so a pack
  // that never inlined, or any pack with the flag off, keeps today's caps —
  // the isolation the lean-stratum rollback criterion (§11.6) requires.
  if (result.inlined !== undefined && result.inlined.length > 0) return mustFetchPackCap(base);
  return base;
}

/**
 * T1 (2026-08-27 field-eval): the byte budget actually enforced for `result`
 * -- rawCapForResult's type-specific tier, clamped down (never up) to
 * whatever byte ceiling is active for the buildTaskPack() call in progress
 * (see packByteCeilingStorage above). Every existing caller of the old
 * capForResult name is unaffected when no ceiling is active (the ordinary
 * case: today's corpus calls pass neither maxBytes/maxTokens nor a
 * clientInfo the vscode profile matches), since clampToActiveCeiling is then
 * the identity function.
 */
function capForResult(result: TaskPackResult): number {
  return clampToActiveCeiling(rawCapForResult(result));
}

// ---------------------------------------------------------------------------
// DESIGN-v0.8 §C2 item 2: pack-level code-block dedupe.
//
// Scope note (per the design's own escape hatch — "if cross-call state is
// too invasive, implement the within-pack duplicate-block dedupe... and FLAG
// the cross-pack part as deferred"): this implements BOTH. Cross-pack state
// turned out NOT to be invasive here — a single module-level Map keyed by
// workspace, overwritten (never appended) on every call, so memory is
// bounded by the number of distinct workspaces this process has ever served
// (already the same shape handleTable/session.ts use for per-workspace
// state). Within-pack dedupe falls out of the SAME pass for free: fingerprints
// are checked against, and folded into, one running list that starts as a
// copy of the PRIOR pack's fingerprints and accumulates the CURRENT pack's
// surfaces as they are visited in order — so a second same-pack surface
// sharing handle+range+hash with an earlier one in the SAME pack matches
// exactly like a cross-pack match would, just against an entry that was
// added moments earlier instead of on a prior call.
// ---------------------------------------------------------------------------

interface PackedBlockFingerprint {
  handle: string;
  range: string;
  hash: string;
  /** True when carried over from the PRIOR call's pack; false when added by the surface currently being processed in THIS pack. Decides the pointer wording at match time (a within-pack match must not claim "prior pack"). */
  fromPriorPack: boolean;
}

/**
 * Last pack's embedded-code fingerprints, keyed by workspace. Intentionally
 * simple: ONE entry per workspace (the design's "module-level last-pack
 * cache"), overwritten wholesale at the end of every dedupe pass — no
 * eviction policy needed since old entries are never appended, only replaced.
 */
const lastPackBlocksByWorkspace = new Map<string, PackedBlockFingerprint[]>();

/**
 * W4 (turn-economy wave 2): the REAL body of a surface whose `code`
 * applyPackDedupe replaced with a compact `code_unchanged` pointer. E4
 * forensics: applyPackDedupe runs before the proof/falsification/execution-
 * contract pass, and a cross-call dedup swaps a duplicate surface's `code` for
 * a POINTER string ("handle — see prior pack"). That is content-equivalent for
 * the CALLER (it already holds the body) but NOT for server-side token
 * scanning: servedSurfaceText would then return the pointer, so a
 * readiness/falsification counterexample that demoted the FIRST call to
 * awaiting-input silently vanishes on an identical re-call — flipping the phase
 * to prepared with no new information (a manufactured turn). Keyed on the
 * ORIGINAL surface object (identity), this side channel lets servedSurfaceText
 * recover the real body for server-side reasoning while the serialized RESPONSE
 * still ships only the compact pointer — so every proof pass reaches the SAME
 * verdict on every identical call, without re-sending the body. A WeakMap so a
 * dropped surface is garbage-collected with no eviction bookkeeping.
 */
const dedupedSurfaceBody = new WeakMap<TaskPackSurface, string>();

/**
 * Test-only reset hook (mirrors handleTable.reset()/resetAllSessions()) so
 * cross-pack dedupe state does not bleed between unrelated test cases that
 * happen to reuse the same on-disk workspace path or run in the same process.
 */
export function resetPackDedupeCache(): void {
  lastPackBlocksByWorkspace.clear();
  servedPacksByWorkspace.clear();
  // C2: the retained certificate is per-workspace serve state of exactly the
  // same kind, so every suite that resets the dedupe cache gets isolation here.
  certifiedWorkingSets.clear();
  // P0 defect 2/3: so is the epoch requirement contract — and it is the one
  // piece of per-workspace state that can make a LATER, unrelated case
  // uncertifiable, so every suite already resetting here gets it for free.
  resetTaskContractStoreForTest();
}

/**
 * Explicit epoch boundary for the pack-dedupe family, mirroring
 * clearTaskQueryRef's per-workspace epoch clear in state/session.ts.
 *
 * Root cause (2026-08-21 smoke-gate forensics): lastPackBlocksByWorkspace /
 * servedPacksByWorkspace / certifiedWorkingSets are keyed ONLY by the
 * resolved workspace path, with no task-epoch or session boundary. The MCP
 * server is a long-lived process in production (not restarted per task), so
 * once ANY earlier, wholly unrelated task_pack call is served against a given
 * workspace path, its fingerprints sit in these maps indefinitely. A LATER,
 * logically independent task that happens to reuse the exact same workspace
 * path (observed live: a bench harness reseals a fixed cell directory across
 * separate smoke-gate attempts at different git heads) and touches a file
 * whose content is byte-identical to what the earlier task saw gets that
 * surface's body silently replaced with a false "<handle> — see prior pack"
 * pointer — even though THIS caller, in THIS task, never actually saw it.
 * That is exactly the class of bug taskEpoch:"new" exists to prevent
 * (AGENTS.md: "a different task re-packs with taskEpoch:'new'"), but
 * server.ts's taskEpoch:"new" handling previously called only
 * clearTaskQueryRef, never this. Workspace-scoped (not the process-wide
 * resetPackDedupeCache, which would also discard OTHER workspaces'
 * legitimate in-task history).
 */
export function clearPackDedupeForWorkspace(workspace: string, lane?: string): void {
  const key = path.resolve(workspace);
  // F-V13-3: lane-scoped, so `task.epoch:"new"` clears THIS agent's caches
  // and never a peer lane's. The key SPELLING is preserved verbatim (this
  // function has always resolved, while captureServedPack keys on the raw
  // `workspace` string its caller passes); unifying it is a separate change.
  lastPackBlocksByWorkspace.delete(laneScopedKey(key));
  servedPacksByWorkspace.delete(laneScopedKey(key));
  certifiedWorkingSets.delete(laneScopedKey(key));
  // P0 defect 2 (2026-08-27): the epoch requirement contract is the same class
  // of workspace-keyed, task-scoped state, and `taskEpoch:"new"` is exactly
  // the boundary at which it must go. Doing it HERE rather than in server.ts's
  // `taskEpoch:"new"` branch keeps that branch's call list unchanged: the
  // branch already calls this function for the identical reason (see the doc
  // comment above), so the epoch reset the guide promises stays one edit.
  // Both spellings: the store keys on the string its writers pass, and those
  // writers take `workspace` verbatim while this function's own siblings key
  // on the resolved path.
  if (lane === undefined) {
    // Legacy/internal callers do not have an authenticated lane. Preserve
    // their old in-memory clearing semantics; the public server always passes
    // its validated lane below and therefore never broad-clears peer lanes.
    clearTaskContract(workspace);
    clearTaskContract(key);
    // P1-c(i): same incident class as the epoch requirement contract above
    // (see that comment) — the executed-next ledger is the other per-lane,
    // task-scoped store that must not let a later, unrelated task inherit an
    // earlier one's "already ran this" suppression.
    clearExecutedNextForWorkspace(workspace);
    clearExecutedNextForWorkspace(key);
  } else {
    clearTaskContractsForLane(workspace, lane);
    if (key !== workspace) clearTaskContractsForLane(key, lane);
    clearExecutedNextForLane(workspace, lane);
    if (key !== workspace) clearExecutedNextForLane(key, lane);
  }
  // Same incident class, one store over (2026-08-27 field-eval follow-up).
  // `priorPackStore` shipped `clearPriorPackObligations` as its declared epoch
  // boundary and then never called it from production — so an explicit
  // `taskEpoch:"new"` left a prior task's open, required, action:"edit"
  // obligations standing on a workspace path a later task reuses. The next
  // pack of that unrelated task then either packs against them (SEAM 1's
  // `queryPriorPackObligations` read) or has its OWN certificate demoted by
  // F-B3's `unserved-obligation:` disclosure — the exact "a later, logically
  // independent task inherits the earlier one's state" failure this function's
  // doc comment above describes for the dedupe maps.
  clearPriorPackObligations(workspace);
  clearPriorPackObligations(key);
}

// ---------------------------------------------------------------------------
// Behavior 3: task_pack RE-CALL dedup — a compact, idempotent response.
//
// A solver commonly re-issues the SAME task_pack while working one task (the
// forensic: 4 calls, each re-serving a multi-KB pack — a full round trip
// costing ~$0.02-0.03 apiece). When the very same request lands again and the
// files the prior pack surfaced are unchanged, there is nothing new to say:
// return a sub-1KB acknowledgement (`pack_unchanged:true` + the surface
// handles + the still-open checks) instead of recomputing and re-serializing
// the whole pack. Anything that would change the served content (a changed
// surface file, or an "extra" arg the stored pack did not have) falls through
// to a full recompute exactly as today.
// ---------------------------------------------------------------------------

/**
 * The essentials of a served task_pack, retained per workspace so an identical
 * re-call can be answered compactly without recomputing. Deliberately small:
 * no code/skeleton/facts — only what the compact response needs plus the
 * file-fingerprint list used to prove the surfaces are unchanged.
 */
interface ServedPackRecord {
  /**
   * F-V13-3 (2026-08-30): the lane this pack was SERVED to. The window is
   * keyed per (workspace, lane), so this is defence in depth on the two
   * lookups that hand a stored `executionContract` back to a caller
   * (tryServeCachedPack / tryServeSemanticDuplicatePack) — a receipt that
   * restates a certificate is an authorization like any other, and
   * `computePackFingerprint` cannot discriminate lanes (it hashes query
   * tokens + paths + roles + workspace, nothing about the caller).
   */
  lane: string;
  /** hash of (normalized query tokens + sorted paths[] + surfaceRoles + workspace) — the dedup-eligible identity. */
  fingerprint: string;
  /** JSON of the "extra" args {path,symbol,lang,limit} the served call carried; a differing value forces recompute (those args change served content). */
  extraArgsKey: string;
  /** B2b: the REQUESTED taskProfile ("auto" when omitted) — gated separately from extraArgsKey via packProfileCompatible. */
  requestProfileKey: string;
  /**
   * Per-surface identity for the compact response (role is `string` to mirror
   * TaskPackSurface.role, not the narrower ImpactSurface).
   *
   * B5 (2026-08-21 serve-honesty): `content_completeness`/`total_lines` are
   * carried through from the served TaskPackSurface at capture time (see
   * captureServedPack), NOT projected onto any compact receipt this record
   * later builds (protocol/readFamily.ts's pack-unchanged projector has no
   * per-evidence slot for them — the frozen wire shape stays untouched). Their
   * sole purpose is so revalidateRecordToReceipt can tell whether the surface
   * THIS record represents was ever disclosed as partial, and decline the
   * compact receipt on a qref replay rather than silently dropping that fact
   * — the same structural gap tryServeSubsetReceipt's own guard closed for
   * its (unstored, ad-hoc) record.
   */
  surfaces: Array<{
    handle: string;
    path: string;
    range: string;
    role: string;
    content_completeness?: "partial";
    total_lines?: number;
  }>;
  /**
   * D2 (F-B2, 2026-08-21): the workspace_state THIS record's own `surfaces`
   * produce, computed at capture time from live disk plus the capture-time
   * inventory (see `recordWorkspaceState`).
   *
   * `revalidateRecordToReceipt` used to compare a recomputation over these
   * surfaces against `executionContract.workspace_state.fingerprint` — the
   * CERTIFICATE's fingerprint, hashed over the surface list the contract was
   * BUILT over. Those two lists diverge for every pack whose certificate
   * outlives the surfaces it was issued over: `carryForwardCertifiedWorkingSet`
   * clones an EARLIER pack's contract onto a replay that serves a narrower
   * set, and trimToCap's byte-budget shed pass removes surfaces AFTER
   * `buildTaskExecutionContract` has already hashed them. For those packs the
   * comparison could never succeed, so a cumulative / carry-forward pack was
   * permanently ineligible for the exact-reissue receipt and re-built in full
   * on every identical re-issue, however untouched the workspace was.
   *
   * Storing the record's own state removes that lossy reconstruction WITHOUT
   * weakening the proof — the other side of the equality is still recomputed
   * from live disk on every probe, so the equality remains exactly a freshness
   * test. Additive, module-private, never projected onto any wire shape (the
   * B5 precedent).
   */
  workspaceState: TaskWorkspaceState;
  coverage: TaskPackResult["coverage"];
  coverageReason: TaskPackResult["coverage_reason"] | undefined;
  missing: string[];
  next: string | undefined;
  profileBinding: TaskProfileBinding | undefined;
  taskProfile: Exclude<TaskProfile, "generic"> | undefined;
  executionContract: TaskExecutionContract | undefined;
  contentSufficiency: TaskPackResult["content_sufficiency"] | undefined;
  /** iter-2 W1: coverage_basis at serve time (so a re-serve receipt re-affirms cumulative completeness). */
  coverageBasis: TaskPackResult["coverage_basis"] | undefined;
  /** iter-2 W1/W2: the served route, echoed on the compact receipt so "working set complete — stop" survives the re-serve. */
  route: TaskPackResult["route"] | undefined;
  /** iter-2 W1: epoch tokens this pack was served under — restricts subset re-serves to the SAME task. */
  epochTokens: string[];
  /** iter-3 F2: the request's surfaceRoles[] (sorted) at serve time — a differing role set is a DIFFERENT working set (adds required roles), so it must not be treated as a semantic duplicate. */
  requestRoles: string[];
  /** iter-3 F2: the request's REQUESTED paths (normalized+sorted) at serve time — what the caller SEEDED, distinct from the served surfaces. A semantic-duplicate re-ask must re-request a SUBSET of these; a caller who deliberately seeds a DIFFERENT file (even one this pack served as supporting context) is re-focusing, not truncating, and earns a full serve. */
  requestPaths: string[];
  /** iter-2 W1: the inventory-complete workspace fingerprint at serve time, or "" when the inventory was not complete. A subset re-serve requires the CURRENT inventory to match this exactly — proof the WHOLE workspace (not just the served files) is unchanged. */
  inventoryFingerprint: string;
  /** Check ids present when this pack was served (intersected with session lastOpenIds at compact-response time). */
  checkIds: string[];
  /** Content-sha of each surface file at serve time (bounded set), keyed by workspace-relative path. Empty when a file was unreadable/oversized — then that surface can't be proven unchanged and we recompute. */
  fileShas: Array<{ path: string; sha: string }>;
}

/**
 * iter-2 W1: the last few served task_pack records per workspace (bounded FIFO),
 * NOT just the single most recent. The iter-1 verbatim-duplicate loss was a
 * VERBATIM re-call "1 call apart": an intervening pack had overwritten the old
 * single-slot cache, so the identical re-call compared against the wrong record,
 * saw a fingerprint mismatch, and re-served ~20KB in full with no receipt.
 * Retaining a small window lets a duplicate that is not the immediately-
 * preceding call still hit the compact re-serve. Each record still carries its
 * own file-sha + workspace-state proof, so staleness is verified per record.
 */
const MAX_CACHED_PACKS_PER_WORKSPACE = 8;
const servedPacksByWorkspace = new Map<string, ServedPackRecord[]>();

/**
 * iter-3 F2: fraction of the LARGER of (current query tokens, prior record
 * tokens) that must be shared for a semantic-duplicate match. 0.8 = a re-ask may
 * differ by at most ~20% of tokens (the truncated-mid-word tail) before it is
 * treated as a genuinely different request that earns a full serve.
 */
const SEMANTIC_DUP_OVERLAP_RATIO = 0.8;

/**
 * iter-3 F3: minimum body size (bytes) before a cross-pack resident surface is
 * worth replacing with a code-less marker. Tiny bodies (< this) cost more in
 * indirection/marker text than they save, so they keep their inline code.
 */
const RESIDENT_DEDUP_MIN_BYTES = 512;

/** iter-3 F4: hard cap on route.reason length on an envelope-trimmed pack. */
const ROUTE_REASON_ENVELOPE_CAP = 200;

/** iter-2 W1 (subset case): two epoch-token lists name the same task iff they share a token (both non-empty). Mirrors packServeLog's internal overlap rule. */
function epochsOverlap(a: readonly string[], b: readonly string[]): boolean {
  if (a.length === 0 || b.length === 0) return false;
  const set = new Set(a);
  return b.some((t) => set.has(t));
}

/** Bound on how many surface files the unchanged-check reads (keeps the cheap idempotency probe cheap). */
const MAX_FINGERPRINT_FILES = 12;
/** Per-file byte cap for the unchanged-check sha (skip pathological huge files rather than reading them on a mere probe). */
const MAX_FINGERPRINT_FILE_BYTES = 256 * 1024;

/**
 * The dedup-eligible identity of a task_pack request: normalized query tokens
 * (order-independent via sort), the paths[] entries (path+range+symbol, sorted),
 * surfaceRoles (sorted), and the workspace. cwd is folded in as `workspace`
 * (the caller-supplied cwd is already resolved to the workspace root before we
 * are called). Excludes path/symbol/lang/limit — those are the "extra" args
 * gated separately via extraArgsKey.
 */
function computePackFingerprint(args: TaskPackArgs, workspace: string): string {
  const queryTokens = tokenizeForEpoch(args.query ?? "").slice().sort();
  // FIX-B3: paths[] entries may still be bare strings at this point (this
  // fingerprint is computed BEFORE buildSeededTaskPack's own normalization
  // runs) — normalize per-entry here too so a bare-string call fingerprints
  // identically to the equivalent {path:...} object form instead of
  // throwing on a missing `.path`.
  const paths = (args.paths ?? [])
    .map(normalizePathEntry)
    .map((p) => [p.path, p.range ?? "", p.symbol ?? ""].join("\u0000"))
    .slice()
    .sort();
  const roles = (args.surfaceRoles ?? []).slice().sort();
  const material = JSON.stringify({ q: queryTokens, p: paths, r: roles, w: workspace });
  return shaOfText(material);
}

/** iter-3 F2: the normalized, sorted set of paths a request explicitly SEEDED (paths[] + path). */
function requestedPathSet(args: TaskPackArgs): string[] {
  const out = new Set<string>();
  for (const entry of args.paths ?? []) {
    const p = normalizePathEntry(entry).path;
    if (p.length > 0) out.add(p);
  }
  if (typeof args.path === "string" && args.path.length > 0) out.add(args.path);
  return [...out].sort();
}

/**
 * JSON key of the "extra" args that must MATCH for a dedup (differing →
 * recompute).
 *
 * B2b (2026-08-01 serving-completeness): `taskProfile` LEFT this key and moved
 * to computeRequestProfileKey. Live defect (2026-07-31-semantic-signal5-2,
 * T09): a qref-only replay that OMITTED taskProfile re-served the whole 14.7KB
 * pack, while the byte-identical call that carried taskProfile got the 1.9KB
 * `pack_unchanged` receipt — the sole difference was `"auto"` vs the declared
 * value inside this key. Absence of the arg is not a different request; it is
 * the documented "omit if unknown" form of the same one.
 */
function computeExtraArgsKey(args: TaskPackArgs): string {
  return JSON.stringify({
    path: args.path ?? null,
    symbol: args.symbol ?? null,
    lang: args.lang ?? null,
    limit: args.limit ?? null,
  });
}

/** B2b: the REQUESTED profile, normalized ("auto" when the caller omitted it). */
function computeRequestProfileKey(args: TaskPackArgs): string {
  return String(args.taskProfile ?? "auto");
}

/**
 * B2b: may a record captured under `recordProfileKey` answer a request made
 * under `requestProfileKey`?
 *
 * Asymmetric on purpose. An EXPLICIT profile is a caller declaration that
 * changes the served route/shape, so it must match exactly (unchanged
 * behavior). An ABSENT profile ("auto") is the caller saying "you decide" — the
 * record's own resolved profile is a valid answer, and the receipt echoes it
 * (`task_profile`), so the caller is never told something the pack did not say.
 * Every other staleness gate (file shas, workspace fingerprint) still runs.
 */
function packProfileCompatible(requestProfileKey: string, recordProfileKey: string): boolean {
  return requestProfileKey === recordProfileKey || requestProfileKey === "auto";
}

/** Read a workspace-relative file and return its content-sha, bounded by size; undefined when unreadable/oversized. */
function surfaceFileSha(workspace: string, relPath: string): string | undefined {
  const abs = safeResolve(relPath, workspace);
  if (abs === undefined) return undefined;
  try {
    const real = fs.realpathSync(abs);
    if (!isWithin(real, resolveReal(workspace))) return undefined;
    const size = fs.statSync(real).size;
    if (size > MAX_FINGERPRINT_FILE_BYTES) return undefined;
    return shaOfText(fs.readFileSync(real, "utf8"));
  } catch {
    return undefined;
  }
}

/**
 * Capture the served pack's essentials + surface-file shas for a possible
 * future re-call dedup. Runs AFTER the full pack is built and trimmed, so the
 * captured surfaces/coverage/missing/next reflect exactly what shipped.
 */
/** Parse a "N-M" surface/remaining range; undefined for anything else. */
function parseSurfaceSpan(range: string): { start: number; end: number } | undefined {
  const parsed = range.trim().match(/^(\d+)-(\d+)$/);
  if (parsed === null) return undefined;
  const start = parseInt(parsed[1]!, 10);
  const end = parseInt(parsed[2]!, 10);
  if (!Number.isFinite(start) || !Number.isFinite(end) || start < 1 || end < start) return undefined;
  return { start, end };
}

/**
 * B2d (2026-08-01 serving-completeness): the FILE LINE SPANS a pack actually
 * put on the wire, per workspace-relative path.
 *
 * A surface with no `code` (handle-only, or a `code_unchanged` pointer) shipped
 * no bytes and contributes nothing. A disclosed-partial surface contributes its
 * `range` MINUS its `remaining_ranges` — never the whole range, so the ledger
 * can never claim lines the excerpt omitted.
 *
 * W14 L1 (2026-08-22): the surface `range` is the exact task-pack evidence
 * window. A compact `doc elided L<a>-<b>` marker still addresses those source
 * lines inside that semantic window, so they remain prior on a later full read;
 * only explicit `remaining_ranges` stay unserved. This is deliberately scoped
 * to task_pack surface semantics — direct full/slice serves still book only the
 * physical spans their wire body carries.
 *
 * Exported for readCodeTaskPack.spec.ts, which pins the §3 residual property
 * (what dedupeTrimAndPersist CAPTURES for the ledger is a subset of what it
 * RETURNS) without having to reach into session state.
 */
export function packServedSpans(result: TaskPackResult): Map<string, Array<[number, number]>> {
  const spans = new Map<string, Array<[number, number]>>();
  for (const surface of codeTaskPackSurfaces(result.surfaces)) {
    if (surface.code === undefined || surface.code.length === 0) continue;
    const span = parseSurfaceSpan(String(surface.range ?? ""));
    if (span === undefined) continue;
    const omitted = (surface.remaining_ranges ?? [])
      .map(parseSurfaceSpan)
      .filter((entry): entry is { start: number; end: number } => entry !== undefined)
      .sort((left, right) => left.start - right.start);
    const served: Array<[number, number]> = [];
    let cursor = span.start;
    for (const gap of omitted) {
      if (gap.end < cursor) continue;
      if (gap.start > cursor) served.push([cursor, Math.min(span.end, gap.start - 1)]);
      cursor = Math.max(cursor, gap.end + 1);
      if (cursor > span.end) break;
    }
    if (cursor <= span.end) served.push([cursor, span.end]);
    // A task-pack range is its exact semantic evidence window. The body may
    // compact comments into a marker, but that marker still addresses the
    // omitted source lines; recording only marker-free tail lines made a later
    // mode=full treat the rest of the selected evidence as unserved.
    const existing = spans.get(surface.path) ?? [];
    existing.push(...served.filter(([start, end]) => end >= start));
    if (existing.length > 0) spans.set(surface.path, existing);
  }
  return spans;
}

function nextCallCoveredBySpans(
  result: TaskPackResult,
  call: ContinuationCall,
  spans: ReadonlyMap<string, ReadonlyArray<readonly [number, number]>>,
): boolean {
  if (call.tool !== "read_file") return false;
  const args = call.arguments as Record<string, unknown>;
  if (typeof args.handle !== "string") return false;
  const range = parsePackLineRange(args.range);
  if (range === undefined) return false;
  const surface = codeTaskPackSurfaces(result.surfaces)
    .find((candidate) => candidate.handle === args.handle);
  if (surface === undefined) return false;
  return spans.get(surface.path)?.some(([start, end]) =>
    start <= range[0] && end >= range[1]
  ) === true;
}

/**
 * B2d: LEDGER CONTINUITY. The pack serve path never touched the served-range
 * ledger, so a slice issued right after a pack could not see the pack's own
 * serving credit — the caller was charged twice for the same lines, and the
 * zoom-progress ordering above had no evidence to order by. Record what the
 * pack actually served (path, file sha, span) as the pack is captured.
 *
 * Bounded by MAX_FINGERPRINT_FILES like every other capture-time read, and
 * fail-safe: an unreadable file simply records nothing.
 */
function parsePackLineRange(value: unknown): [number, number] | undefined {
  if (typeof value !== "string") return undefined;
  const match = /^(\d+)-(\d+)$/.exec(value);
  if (match === null) return undefined;
  const start = Number(match[1]);
  const end = Number(match[2]);
  return Number.isInteger(start) && Number.isInteger(end) && start >= 1 && end >= start
    ? [start, end]
    : undefined;
}

export interface ServedWindowScope {
  readonly epoch: string;
  readonly lane: string;
  readonly reset?: boolean;
}

function servedZoomScopeForArgs(
  args: TaskPackArgs | undefined,
  query: string,
): ServedWindowScope | undefined {
  if (args === undefined) return undefined;
  return {
    epoch: tokenizeForEpoch(query).join("\u0000"),
    lane: normalizeContractLane(args.lane),
    ...(args.taskEpoch === "new" ? { reset: true } : {}),
  };
}

function scopedWindowHasUnservedLines(
  workspace: string | undefined,
  surface: TaskPackSurface,
  parsed: [number, number],
  scope: ServedWindowScope | undefined,
): boolean {
  if (workspace === undefined) return true;
  if (scope === undefined) {
    return servedFindWindowHasUnservedLines(workspace, surface.path, parsed[0], parsed[1]);
  }
  return servedWindowHasUnservedLines({
    workspaceRoot: workspace,
    epoch: scope.epoch,
    lane: scope.lane,
    path: surface.path,
    sha: surfaceFileSha(workspace, surface.path),
    startLine: parsed[0],
    endLine: parsed[1],
  });
}

/** Select only a disclosed window that still has bytes outside the served ledger. */
export function firstUnservedPartialRange(
  workspace: string | undefined,
  surface: TaskPackSurface,
  scope?: ServedWindowScope,
  currentPackSpans?: ReadonlyMap<string, ReadonlyArray<readonly [number, number]>>,
): string | undefined {
  for (const range of surface.remaining_ranges ?? []) {
    const parsed = parsePackLineRange(range);
    // Keep malformed producer data actionable; the normal read admission will
    // reject it if it cannot be executed, while a valid range is checked against
    // the scoped served-window ledger before it becomes a zoom.
    if (parsed === undefined) return range;
    const coveredByCurrentPack = currentPackSpans?.get(surface.path)?.some(([start, end]) =>
      start <= parsed[0] && end >= parsed[1]
    ) === true;
    if (coveredByCurrentPack) continue;
    if (scopedWindowHasUnservedLines(workspace, surface, parsed, scope)) return range;
  }
  return undefined;
}

/**
 * Mark zoom ranges already served by this workspace session before canonical
 * projection. This is an internal producer marker: protocol/readFamily.ts
 * projects only the frozen wire fields, while canonicalDecision.ts uses it to
 * avoid reviving a stale document zoom after the contract correctly declined it.
 */
export function markServedZoomCandidates(
  workspace: string,
  result: TaskPackResult,
  forceServe = false,
  scope?: ServedWindowScope,
): void {
  if (forceServe) return;
  const suppressed: string[] = [];
  const currentPackSpans = packServedSpans(result);
  for (const surface of codeTaskPackSurfaces(result.surfaces)) {
    for (const range of surface.remaining_ranges ?? []) {
      const parsed = parsePackLineRange(range);
      if (
        parsed !== undefined
        && (
          currentPackSpans.get(surface.path)?.some(([start, end]) =>
            start <= parsed[0] && end >= parsed[1]
          ) === true
          || !scopedWindowHasUnservedLines(workspace, surface, parsed, scope)
        )
      ) {
        suppressed.push(JSON.stringify([surface.handle, range]));
      }
    }
  }
  if (suppressed.length > 0) {
    (result as TaskPackResult & { served_zoom_suppressed?: string[] }).served_zoom_suppressed = suppressed;
  }

  // `result.next` and the continuation plan can be minted before the current
  // pack's served spans are booked. If that emitted zoom is already covered by
  // a sibling surface in this same response, remove it before canonical
  // projection and select the first genuinely unserved required window.
  const parsedResultNext = result.next ? nextStringToCall(result.next) : undefined;
  const staleResultNext = parsedResultNext !== undefined
    && nextCallCoveredBySpans(result, parsedResultNext, currentPackSpans);
  const contractNext = result.execution_contract?.next_call;
  const staleContractNext = contractNext !== undefined
    && nextCallCoveredBySpans(result, contractNext, currentPackSpans);
  const servedZoomExhausted = suppressed.length > 0
    && codeTaskPackSurfaces(result.surfaces)
      .filter((surface) => surface.required !== false)
      .every((surface) =>
        firstUnservedPartialRange(workspace, surface, scope, currentPackSpans) === undefined
      );
  if (
    staleResultNext
    || staleContractNext
    || (!proofCompletionEnabled() && servedZoomExhausted)
  ) {
    delete result.next;
    delete result.continuation;
    const replacement = codeTaskPackSurfaces(result.surfaces)
      .filter((surface) => surface.required !== false)
      .map((surface) => ({
        surface,
        range: firstUnservedPartialRange(workspace, surface, scope, currentPackSpans),
      }))
      .find((entry): entry is { surface: TaskPackSurface; range: string } => entry.range !== undefined);
    if (replacement !== undefined) {
      const replacementCall: ContinuationCall = {
        tool: "read_file",
        arguments: { handle: replacement.surface.handle, range: replacement.range },
      };
      result.next = `read_file mode=slice handle=${replacement.surface.handle} range=${replacement.range}`;
      if (result.execution_contract !== undefined) {
        result.execution_contract.next_call = replacementCall;
        if (result.execution_contract.call_budget !== undefined) {
          result.execution_contract.call_budget = {
            ...result.execution_contract.call_budget,
            candidate_call: replacementCall,
          };
        }
      }
    } else if (result.execution_contract !== undefined) {
      delete result.execution_contract.next_call;
      if (result.execution_contract.call_budget !== undefined) {
        delete result.execution_contract.call_budget.candidate_call;
      }
      // E-1 OFF parity: E-2 still removes a same-pack-served zoom, but the
      // proof-completion switch must not turn that removal into a terminal
      // act.* decision. Keep the legacy conservative await-input state when
      // no unserved replacement remains; ON can certify only after discharge.
      if (!proofCompletionEnabled()) {
        result.execution_contract.state = "needs-followup";
        result.execution_contract.readiness = "needs-followup";
        result.execution_contract.discovery_complete = false;
        result.execution_contract.next_action = "request-user-input";
        result.execution_contract.typestate.phase = "awaiting-input";
        result.execution_contract.typestate.allowed_actions = ["request-user-input"];
        result.execution_contract.typestate.challenge_required_for = [];
      }
    }
  }
}

function recordPackServedRanges(
  workspace: string,
  result: TaskPackResult,
  scope?: ServedWindowScope,
): void {
  if (scope?.reset) clearServedWindowsForScope(workspace, scope.epoch, scope.lane);
  const spans = packServedSpans(result);
  if (spans.size === 0) return;
  // F3: one ordinal for the whole pack — every span it books was served by the
  // same call.
  const packCall = beginServeCall(workspace);
  let files = 0;
  for (const [relPath, ranges] of spans) {
    if (files >= MAX_FINGERPRINT_FILES) break;
    const abs = safeResolve(relPath, workspace);
    if (abs === undefined) continue;
    let text: string;
    try {
      const real = fs.realpathSync(abs);
      if (!isWithin(real, resolveReal(workspace))) continue;
      if (fs.statSync(real).size > MAX_FINGERPRINT_FILE_BYTES) continue;
      text = fs.readFileSync(real, "utf8");
    } catch {
      continue;
    }
    files += 1;
    const sha = shaOfText(text);
    const totalLines = Math.max(1, countLines(text));
    for (const [start, end] of ranges) {
      if (start > totalLines) continue;
      const servedEnd = Math.min(end, totalLines);
      recordServedRange(workspace, relPath, sha, start, servedEnd, totalLines, {
        mode: "task_pack",
        range: `${start}-${servedEnd}`,
        call: packCall,
      });
      if (scope !== undefined) {
        recordServedWindow({
          workspaceRoot: workspace,
          epoch: scope.epoch,
          lane: scope.lane,
          path: relPath,
          sha,
          startLine: start,
          endLine: servedEnd,
        });
      }
    }
  }
}

/**
 * D2 (F-B2): the ONE workspace_state computation both sides of
 * `revalidateRecordToReceipt`'s freshness equality run — over a record's OWN
 * stored surfaces, never over a certificate's possibly-broader evidence list.
 * `captureServedPack` calls it once to stamp `ServedPackRecord.workspaceState`;
 * the revalidation calls it again against live disk and the current inventory.
 * Both callers hash the SAME surface identity list by construction, so the only
 * thing their comparison can detect is an actual change on disk — which is
 * precisely what the receipt's "you already hold these exact bytes" claim needs
 * proved, and all it ever proved.
 */
function recordWorkspaceState(
  workspace: string,
  record: Pick<ServedPackRecord, "surfaces" | "coverage" | "missing">,
  inventory: WorkspaceInventoryState,
): TaskWorkspaceState {
  return buildTaskWorkspaceState({
    mode: "task_pack",
    coverage: record.coverage,
    surfaces: record.surfaces as TaskPackSurface[],
    missing: record.missing,
  }, workspace, inventory);
}

function captureServedPack(
  workspace: string,
  args: TaskPackArgs,
  fingerprint: string,
  result: TaskPackResult,
  /**
   * §3 residual (2026-08-02 serve-honesty): the pack actually RETURNED on the
   * wire, when it differs from the dedup baseline `result`. dedupeTrimAndPersist
   * deliberately captures the full `trimmed` as the FINGERPRINT baseline even
   * when it returns a compact subset receipt — that asymmetry is correct for
   * dedup and WRONG for the served-range ledger, which may only book bytes that
   * went out. Closed by construction here rather than by argument: the ledger
   * always follows this parameter, which defaults to `result` for every caller
   * where baseline and wire are the same object.
   */
  served: TaskPackResult = result,
): void {
  // B2d: ledger continuity runs for EVERY captured pack, including the artifact
  // shape that returns early below — code surfaces on a mixed pack still served
  // real lines.
  recordPackServedRanges(
    workspace,
    served,
    servedZoomScopeForArgs(args, args.query ?? args.symbol ?? args.path ?? ""),
  );
  // Cached packs serialize code-surface identity (range+role). Artifact packs
  // deliberately bypass this optimization so their strict discovery shape is
  // never collapsed into, or re-served as, a code surface — so THIS pack is not
  // captured. iter-3 F1: do NOT wipe the whole per-workspace window here. The old
  // `servedPacksByWorkspace.delete(workspace)` evicted every UNRELATED code-pack
  // record too, so an artifact-bearing pack interleaved between two identical
  // code packs poisoned the first code pack's record — the identical repeat then
  // missed the cache and re-served in full (a full serve of one-call latency),
  // and only the NEXT repeat (after re-capture) received the receipt. Instead,
  // drop only any record that shares THIS request's signature (a stale code
  // record for a request that now yields an artifact pack must not be re-served),
  // leaving every other code pack's receipt eligibility intact.
  if ((result.surfaces as TaskPackResultSurface[]).some(isArtifactTaskPackSurface)) {
    const artifactSignatureKey =
      `${fingerprint} ${computeExtraArgsKey(args)} ${computeRequestProfileKey(args)}`;
    const remaining = (servedPacksByWorkspace.get(laneScopedKey(workspace)) ?? []).filter(
      (r) => `${r.fingerprint} ${r.extraArgsKey} ${r.requestProfileKey}` !== artifactSignatureKey,
    );
    if (remaining.length > 0) servedPacksByWorkspace.set(laneScopedKey(workspace), remaining);
    else servedPacksByWorkspace.delete(laneScopedKey(workspace));
    return;
  }
  const distinctPaths: string[] = [];
  const seen = new Set<string>();
  for (const s of result.surfaces) {
    if (!seen.has(s.path)) { seen.add(s.path); distinctPaths.push(s.path); }
    if (distinctPaths.length >= MAX_FINGERPRINT_FILES) break;
  }
  const fileShas: Array<{ path: string; sha: string }> = [];
  for (const p of distinctPaths) {
    const sha = surfaceFileSha(workspace, p);
    if (sha !== undefined) fileShas.push({ path: p, sha });
  }
  const inventory = computeWorkspaceInventoryState(workspace);
  // D2 (F-B2): projected ONCE and reused below, so the workspace_state stamped
  // on the record is provably computed over the very array the record carries —
  // the property that makes the revalidation equality a pure freshness test.
  const recordSurfaces: ServedPackRecord["surfaces"] = result.surfaces.map((s) => ({
    handle: s.handle,
    path: s.path,
    range: s.range,
    role: s.role,
    // B5: thread the honesty markers through — see ServedPackRecord.surfaces.
    ...(s.content_completeness !== undefined ? { content_completeness: s.content_completeness } : {}),
    ...(s.total_lines !== undefined ? { total_lines: s.total_lines } : {}),
  }));
  const record: ServedPackRecord = {
    lane: currentSessionLane(),
    fingerprint,
    extraArgsKey: computeExtraArgsKey(args),
    requestProfileKey: computeRequestProfileKey(args),
    surfaces: recordSurfaces,
    workspaceState: recordWorkspaceState(
      workspace,
      { surfaces: recordSurfaces, coverage: result.coverage, missing: result.missing },
      inventory,
    ),
    coverage: result.coverage,
    coverageReason: result.coverage_reason,
    coverageBasis: result.coverage_basis,
    route: result.route ? { ...result.route } : undefined,
    epochTokens: tokenizeForEpoch(args.query ?? args.symbol ?? args.path ?? ""),
    requestRoles: (args.surfaceRoles ?? []).slice().sort(),
    requestPaths: requestedPathSet(args),
    inventoryFingerprint: inventory.complete ? inventory.fingerprint : "",
    missing: [...result.missing],
    next: result.next,
    profileBinding: result.profile_binding,
    taskProfile: result.task_profile,
    executionContract: result.execution_contract,
    contentSufficiency: result.content_sufficiency,
    checkIds: (getPackChecks(workspace)?.checks ?? []).map((c) => c.id),
    fileShas,
  };
  // iter-2 W1: bounded multi-slot cache. Replace any prior record with the SAME
  // request signature (fingerprint+extraArgs), then append and FIFO-evict.
  // B2b (2026-08-01 serving-completeness): the requested profile stays part of
  // the SIGNATURE (so two genuinely differently-profiled calls keep their own
  // slots) even though LOOKUP matches it loosely via packProfileCompatible.
  const signatureKey = `${fingerprint}\u0000${record.extraArgsKey}\u0000${record.requestProfileKey}`;
  const kept = (servedPacksByWorkspace.get(laneScopedKey(workspace)) ?? []).filter(
    (r) => `${r.fingerprint}\u0000${r.extraArgsKey}\u0000${r.requestProfileKey}` !== signatureKey,
  );
  kept.push(record);
  while (kept.length > MAX_CACHED_PACKS_PER_WORKSPACE) kept.shift();
  servedPacksByWorkspace.set(laneScopedKey(workspace), kept);
}

/**
 * iter-2 W2: the guidance a complete/focused working set must carry — an
 * explicit STOP signal, not just an edit route. The iter-1 loss cell issued 4
 * MORE packs (post_ready_reads=5) after coverage flipped complete because a bare
 * edit route read as "you MAY edit", not "stop discovering". GUIDANCE ONLY: it
 * never adds a refusal and never raises max_additional_tl_calls above 0 (the
 * serve-with-state gate stays the sole refusal mechanism — wave-4 proved empty
 * refusals backfire).
 */
const COMPLETE_STOP_REASON =
  "working set complete — stop discovery; edit from resident handles now";
const COMPLETE_STOP_REASON_ANSWER =
  "working set complete — stop discovery; answer from resident handles now";

/**
 * iter-2 W2: enrich a complete/focused (single-call OR cumulative) pack's route
 * with the explicit stop signal. Returns a route with action edit_from_handles
 * (or answer_from_handles for the answer profile), max_additional_tl_calls:0,
 * and a reason that leads with the stop language. Idempotent (never appends the
 * phrase twice). Non-complete coverage is returned unchanged.
 */
function applyCompleteStopRoute(
  route: TaskPackResult["route"] | undefined,
  coverage: TaskPackResult["coverage"],
  isAnswer: boolean,
  partialPrimary = false,
): TaskPackResult["route"] | undefined {
  if (coverage !== "complete" && coverage !== "focused") return route;
  // A5 (2026-08-01 signal5-2 T03): an ANSWER pack whose primary surface is
  // disclosed-partial must not claim "working set complete / stop" with a zero
  // call budget — the queried behavior may sit in remaining_ranges (T03: 82% of
  // the class, including every lifecycle method the query named), and three
  // solver runs resolved that contradiction three different ways (zoom /
  // hedged answer / no answer at all). Grant ONE sanctioned same-handle zoom
  // and say so explicitly.
  const stop = isAnswer
    ? (partialPrimary
        ? "answer from resident handles; primary surface is partial — if the queried behavior lies in its remaining_ranges, ONE same-handle slice zoom is sanctioned first"
        : COMPLETE_STOP_REASON_ANSWER)
    : COMPLETE_STOP_REASON;
  const budget = isAnswer && partialPrimary ? 1 : 0;
  const action = isAnswer ? "answer_from_handles" : "edit_from_handles";
  if (route === undefined) {
    return { action, reason: stop, max_additional_tl_calls: budget };
  }
  // ONLY enrich an already-terminal handle route. A complete pack whose content
  // is insufficient (route inspect_handles) or whose primary is ambiguous (route
  // confirm_candidates) genuinely needs the follow-up — never rewrite that into
  // an edit-now stop.
  if (route.action !== "edit_from_handles" && route.action !== "answer_from_handles") {
    return route;
  }
  // L1 (2026-08-08 doc-authority serve honesty): a doc-sliver pack has already
  // REPLACED this clause with an honest one and raised the budget. Re-appending
  // "working set complete — stop discovery" on a re-serve receipt would restore
  // the exact false certificate the lever exists to remove.
  if (route.reason.includes(DOC_SLIVER_ROUTE_MARKER)) return route;
  if (route.reason.includes(stop)) {
    return { ...route, max_additional_tl_calls: budget };
  }
  // Preserve the specific already-computed reason (e.g. cumulative-coverage
  // provenance, the batch-edit nudge) by APPENDING the stop signal, so existing
  // reason-substring expectations still hold.
  return {
    ...route,
    reason: `${route.reason}; ${stop}`,
    max_additional_tl_calls: budget,
  };
}

/**
 * iter-2 W1: build the compact `pack_unchanged` receipt from a served-pack
 * record proven unchanged against `currentWorkspaceState`. Surfaces are stripped
 * to identity (handle/path/range/role + revalidation sha), NO bodies; the route
 * carries the W2 complete-stop signal; coverage_basis is preserved so a
 * cumulative-complete re-serve still reads trustworthy. Deliberately omits
 * frontier_index / served_earlier / verification (W3: never on a receipt).
 */
/**
 * D1/D3 (2026-08-01 probe sweep): a contract must never point BACKWARD.
 * (1) identity echo — next_call re-issues the very invocation that produced
 *     this pack (observed live: paths=[<dir>] packs whose remedy was the same
 *     query+dirs re-pack). Remedied with a tree inventory call on the first
 *     still-existing caller dir, else suppressed.
 * (2) executed echo — next_call names a locate this session ALREADY ran
 *     (observed live via a qref re-pack). Advanced to a batched read of the
 *     locate's own candidate handles, else suppressed.
 * Suppression stamps retry_same_call:false and says why in the reason.
 */
function suppressNonProgressingNextCall(
  workspace: string,
  result: TaskPackResult,
  args: TaskPackArgs | undefined,
  effectiveQuery: string,
  servedZoomScope?: ServedWindowScope,
): void {
  const contract = result.execution_contract;
  const nextCall = contract?.next_call;
  if (contract === undefined || nextCall === undefined) return;
  const ncArgs = (nextCall.arguments ?? {}) as Record<string, unknown>;

  // The server records every successful tool action before the next pack can
  // be built.  A result (including absence/empty) is consumed work, so never
  // put the identical action back on the wire.
  const requestedLane = args as (TaskPackArgs & { lane?: unknown }) | undefined;
  // A-F1: the SAME normalization the dispatcher writes under. This read keyed
  // on "default" while the writer keyed on "" for every lane-less call, which
  // is exactly the default single-agent path, so the gate below never fired.
  const lane = normalizeContractLane(requestedLane?.lane);
  if (hasExecutedNext(workspace, lane, nextCall.tool, ncArgs)) {
    // A-F2: suppression alone was a dead end. Try the deterministic axes first.
    repairSuppressedNextCall(workspace, lane, result, effectiveQuery, nextCall, servedZoomScope);
    return;
  }

  if (nextCall.tool === "read_file" && ncArgs["mode"] === "task_pack" && args !== undefined) {
    const callerPaths = [
      ...(typeof args.path === "string" && args.path.length > 0 ? [args.path] : []),
      ...(args.paths ?? []).map((entry) => normalizePathEntry(entry).path)
        .filter((p): p is string => typeof p === "string" && p.length > 0),
    ].map(normalizedRequestPath).sort();
    const ncPaths = Array.isArray(ncArgs["paths"])
      ? (ncArgs["paths"] as unknown[])
          .map((p) => typeof p === "object" && p !== null ? String((p as { path?: unknown }).path ?? "") : String(p))
          .filter((p) => p.length > 0)
          .map(normalizedRequestPath)
          .sort()
      : [];
    const ncQuery = typeof ncArgs["query"] === "string" ? ncArgs["query"] : "";
    // unexpandedDirNext historically sliced the query to 80 chars — a prefix
    // match therefore counts as "the same query".
    const sameQuery = ncQuery.length > 0 && effectiveQuery.startsWith(ncQuery);
    const samePaths = ncPaths.length > 0 && JSON.stringify(ncPaths) === JSON.stringify(callerPaths);
    if (sameQuery && samePaths) {
      const dir = ncPaths.find((p) => dirExistsCached(workspace, workspace, p));
      if (dir !== undefined && !hasExecutedNext(workspace, lane, "search_files", { action: "tree", path: dir })) {
        contract.next_call = { tool: "search_files", arguments: { action: "tree", path: dir } };
      } else {
        // A-F2: the dir arm's own else-branch was a second bare dead end.
        repairSuppressedNextCall(workspace, lane, result, effectiveQuery, nextCall, servedZoomScope);
      }
      return;
    }
  }

  if (advanceExecutedLocateNextCall(workspace, contract) === "suppressed") {
    // A-F2: a locate that returned nothing is consumed work, not a terminus.
    repairSuppressedNextCall(workspace, lane, result, effectiveQuery, nextCall, servedZoomScope);
  }
}

/**
 * D3 (2026-08-01, + same-day receipt-staleness fix): when a contract's
 * next_call names a locate this session ALREADY executed, advance it to a
 * batched read of that locate's own candidate handles — or suppress it (with
 * a reason) when the locate returned nothing. Shared by the fresh-build
 * choke point (suppressNonProgressingNextCall) and the receipt paths
 * (compactReceiptFromRecord serves both the subset receipt and the
 * exact-fingerprint replay), because a receipt replays the contract captured
 * BEFORE its next_call ran and would otherwise re-point at it forever.
 */
/** Actions advanceExecutedLocateNextCall's no-repeat check covers. tree keys on `path`; the rest key on `query`. */
export const GENERALIZED_NO_REPEAT_ACTIONS: ReadonlySet<string> = new Set(["locate", "find", "references", "tree"]);
/**
 * P1-b (2026-08-28): the RECEIPT call site (compactReceiptFromRecord) passes
 * this narrower set deliberately — see that call site's own comment for why
 * (no wire channel exists there for a NEW find/references/tree disclosure;
 * `read.task_pack`'s projector never ships `missing[]` and the frozen
 * `await_input` decision member carries no `gaps` key).
 */
export const LOCATE_ONLY_NO_REPEAT_ACTIONS: ReadonlySet<string> = new Set(["locate"]);

/**
 * F-V13-5 (2026-08-30, v0.13.0 external-verification P0-A): the actions whose
 * stale `next_call` a RECEIPT can carry but cannot itself repair.
 *
 * THE OBSERVED LOOP. A caller re-sent the same query seven times and got the
 * byte-identical `search_files action=tree` `next` back every time, after
 * running that tree on turn one. The fresh-build path had the right brake all
 * along — `recordExecutedSearch` -> `suppressNonProgressingNextCall` ->
 * `advanceExecutedLocateNextCall(..., GENERALIZED_NO_REPEAT_ACTIONS)` ->
 * `repairSuppressedNextCall`, which either finds an alternative progress axis
 * or discloses an honest gap. The RECEIPT paths never reach it: an identical
 * (or qref-replayed, or semantically duplicate) request short-circuits on
 * `computePackFingerprint`, which hashes the REQUEST and therefore cannot see
 * that the stored contract's own next has since been consumed.
 * `compactReceiptFromRecord` then advances only `LOCATE_ONLY_NO_REPEAT_ACTIONS`
 * — deliberately, because it has no wire channel for a NEW find/references/tree
 * disclosure (see its own comment). So for exactly these three actions a
 * receipt re-proposed consumed work forever.
 *
 * THE FIX IS NOT A REFUSAL, not a widening of the receipt's own advance, and
 * NOT a decline. `guardExecutionDiscovery` records that refusing a
 * discovery-phase caller measurably drove native-tool desertion, and the
 * receipt's locate-only scope is load-bearing (pinned in
 * advanceExecutedLocateNextCall.spec.ts).
 *
 * DECLINING WAS TRIED FIRST AND IS WRONG, for a reason worth keeping: the
 * receipt is the ONLY read-family member that ships a top-level `missing[]`
 * (`projectTaskPack`'s allowlist has no such member), so falling through to a
 * full build trades a stale next for a LOST disclosure. sequenceCorpus's I5
 * invariant — "a continuation is progress or a disclosed gap, never a bare
 * terminus" — catches exactly that: the fall-through terminated with no next
 * AND no `missing[]`. So the three entry points REPAIR the contract they are
 * about to re-serve (drop the consumed next, say why) and serve the receipt,
 * which keeps both halves honest.
 *
 * The set is derived from the two published ones rather than spelled out, so
 * an action added to the generalized set joins this one automatically.
 */
const RECEIPT_UNREPAIRABLE_STALE_ACTIONS: ReadonlySet<string> = new Set(
  [...GENERALIZED_NO_REPEAT_ACTIONS].filter((action) => !LOCATE_ONLY_NO_REPEAT_ACTIONS.has(action)),
);

/**
 * The contract a receipt may honestly re-serve: `contract` itself when its
 * `next_call` is still owed, or a COPY with a consumed find/references/tree
 * dropped and the reason stamped.
 *
 * Returns the argument BY IDENTITY when nothing is stale, so every receipt
 * that was correct before is byte-for-byte unchanged and the caller can tell
 * the two cases apart with `===`. The stored record and the live result are
 * never mutated: the repair happens on a clone.
 *
 * TWO LEDGERS, because the fresh-build gate consults two. `hasExecutedNext`
 * fingerprints the WHOLE call and is what `suppressNonProgressingNextCall`
 * tries first; `consultExecutedSearch` (via advanceExecutedLocateNextCall)
 * keys on `${action}::${query-or-path}`. Neither subsumes the other for this
 * class: a bare `{action:"tree"}` carries no path, and `hasExecutedNext`
 * fingerprints the WHOLE call — including the task/budget/scope envelope a
 * real dispatch almost always carries and a synthesized bare candidate never
 * does — so it alone can never recognize a repeat of exactly this shape
 * (I-6, 2026-08-30: measured, and reproduced end-to-end by root's unit
 * probe — `i6-fallback-result.json`'s `repeated_identically:true`). PRE-I-6,
 * a bare tree additionally had NO key in the action+term ledger at all
 * (server.ts's `recordExecutedSearch` call only fired `if (treePath !==
 * undefined)`), so neither ledger covered it. Both now key a pathless tree
 * under the workspace root (`treePath ?? workspace`, matched here by this
 * function's own `searchKey` fallback below) — the SAME fix
 * `noDeadEndCandidateAlreadyExecuted` above applies to the fresh-build
 * guard's own bare-tree candidate. The action+term ledger remains the one
 * that ALSO recognizes a call the agent executed under a different argument
 * SPELLING than the contract proposed (`references scope.symbol=X` for a
 * proposed `references query=X` — the observed loop, and the case the
 * wire's own exact-fingerprint `firstUnconsumed` filter cannot see). Only
 * "suppressed" counts from the first: an "advanced" locate is already
 * repaired in-receipt by `compactReceiptFromRecord`, and a "kept" next has
 * never run. Both arms stay inside RECEIPT_UNREPAIRABLE_STALE_ACTIONS, so a
 * read/zoom next is never touched.
 */
function receiptContractWithoutConsumedSearch(
  workspace: string,
  contract: TaskExecutionContract | undefined,
  /**
   * I-7 (2026-08-30 forensics attribution wave): the lane the STORED record
   * this contract came from was served into, defaulting to the current lane
   * for the one call site (`tryServeSubsetReceipt`'s synthetic, never-stored
   * record) that has no separate record to compare against — see
   * ServedPackRecord.lane's own F-V13-3 doc comment; `same_lane` below is a
   * regression canary for that exact cross-lane leakage class, not a
   * functional input to the repair decision itself.
   */
  recordLane: string = currentSessionLane(),
): TaskExecutionContract | undefined {
  const nextCall = contract?.next_call;
  if (contract === undefined || nextCall === undefined) return contract;
  if (nextCall.tool !== "search_files") return contract;
  const ncArgs = (nextCall.arguments ?? {}) as Record<string, unknown>;
  const action = typeof ncArgs["action"] === "string" ? ncArgs["action"] : "";
  if (!RECEIPT_UNREPAIRABLE_STALE_ACTIONS.has(action)) return contract;
  // THE WIRE ALREADY OWNS HALF OF THIS, AND MUST KEEP IT. `projectTaskDecision`
  // filters a consumed candidate out of its own precedence chain
  // (`firstUnconsumed`), so a receipt whose next this lane executed VERBATIM
  // still progresses — the chain simply falls through to the discovery bundle
  // or a gap-named call. Repairing that case here would replace a progressing
  // continuation with a bare terminus, because a contract with no `next_call`
  // canonicalizes to `await_input` and the wire's other candidates are then
  // never consulted (sequenceCorpus's I5 invariant catches exactly this).
  //
  // What the wire CANNOT see is a call executed under a different argument
  // spelling than the contract proposed, because its filter is a whole-call
  // fingerprint: `references scope.symbol=X` does not fingerprint-match a
  // proposed `references query=X`, so the identical next came back every time.
  // That — and only that — is what this repairs.
  if (hasExecutedNext(workspace, currentSessionLane(), nextCall.tool, ncArgs)) return contract;
  const repaired: TaskExecutionContract = { ...contract, next_call: structuredClone(nextCall) };
  const outcome = advanceExecutedLocateNextCall(workspace, repaired, RECEIPT_UNREPAIRABLE_STALE_ACTIONS);
  if (outcome === "kept") return contract;
  // I-7: the contract was ACTUALLY repaired — the one place shared by all
  // three receipt call sites (tryServeSubsetReceipt's own no-record call,
  // tryServeCachedPack, tryServeSemanticDuplicatePack via
  // receiptRecordWithoutConsumedSearch) where a stale next_call this
  // request's own fingerprint-match check could not see gets fixed before
  // the receipt goes out. See this function's own long doc comment above for
  // why a decline was tried first and is wrong. `outcome` here is always
  // "suppressed" today (RECEIPT_UNREPAIRABLE_STALE_ACTIONS excludes
  // "locate", the only action advanceExecutedLocateNextCall's "advanced" arm
  // handles), kept as the callee's own three-value type rather than
  // hand-narrowed so this stays correct if that ever changes.
  if (isTraceEnabled() && shouldTraceReceiptRepairOnce()) {
    trace(
      "receipt_next_repair",
      {
        action,
        outcome,
        has_path: typeof ncArgs["path"] === "string" && (ncArgs["path"] as string).length > 0,
        same_lane: recordLane === currentSessionLane(),
      },
      workspace,
    );
  }
  return repaired;
}

/** The record a receipt may honestly re-serve; identity-preserving when nothing is stale. */
function receiptRecordWithoutConsumedSearch(
  workspace: string,
  record: ServedPackRecord,
): ServedPackRecord {
  const repaired = receiptContractWithoutConsumedSearch(workspace, record.executionContract, record.lane);
  return repaired === record.executionContract ? record : { ...record, executionContract: repaired };
}

export function advanceExecutedLocateNextCall(
  workspace: string,
  contract: TaskExecutionContract,
  allowedActions: ReadonlySet<string> = GENERALIZED_NO_REPEAT_ACTIONS,
): "advanced" | "suppressed" | "kept" {
  const nextCall = contract.next_call;
  if (nextCall === undefined || nextCall.tool !== "search_files") return "kept";
  const ncArgs = (nextCall.arguments ?? {}) as Record<string, unknown>;
  const action = typeof ncArgs["action"] === "string" ? ncArgs["action"] : "";
  // P1-b (2026-08-28 review-fix wave): was `action !== "locate"` return
  // "kept" — a bare passthrough that let find/references/tree re-propose an
  // already-answered search forever, the same loop-class this locate-only
  // check already closed for locate (P0-2's own defect class). The fresh-
  // build call site (suppressNonProgressingNextCall) uses the default,
  // generalized set; the receipt call site restricts to locate-only.
  if (!allowedActions.has(action)) return "kept";
  // I-6 fix: a pathless tree keys on the workspace root, the SAME fallback
  // server.ts's recordExecutedSearch call now records it under — previously
  // `""` here, which never matched anything server.ts recorded (that call
  // used to skip a pathless tree entirely) and left a bare-tree next_call
  // permanently unrepairable by this function.
  const searchKey = action === "tree" ? String(ncArgs["path"] ?? workspace) : String(ncArgs["query"] ?? "");
  const executed = consultExecutedSearch(workspace, action, searchKey);
  if (executed === undefined) return "kept";
  // Only `locate` mints handles its candidates can be batch-read from
  // (candidateDetails[].handle). find/references/tree responses are
  // path/line-shaped with no handle affordance (findText.ts/findReferences.ts
  // never mint one), so advancing them to a `read_file handles=[...]` next
  // would invent a call shape their own recorded candidates cannot back —
  // every non-locate action here only ever SUPPRESSES a proven repeat.
  if (action === "locate" && executed.length > 0) {
    contract.next_call = {
      tool: "read_file",
      arguments: { handles: executed.slice(0, MAX_BATCH_NEXT_HANDLES) },
    };
    return "advanced";
  }
  delete contract.next_call;
  contract.reason = `${contract.reason}; next_call suppressed: that ${action} already ran this session and returned no usable candidates — widen scope (paths/cwd) or act on held evidence`;
  return "suppressed";
}

/**
 * A-F2 (2026-08-28): THE DETERMINISTIC ALTERNATIVE-AXIS RULES.
 *
 * `advanceExecutedLocateNextCall` already knew that suppressing a consumed
 * next is only half the job — its "advanced" arm turns an executed locate into
 * a batched read of that locate's own candidates, and
 * `suppressNonProgressingNextCall`'s dir arm turns a same-scope re-pack into a
 * tree of that scope. Both rules were reachable from exactly one call shape
 * each; every OTHER suppression fell straight through to a bare
 * `await_input: no-grounded-call-remains` with no next and no gap. This
 * generalizes the two rules and adds the surface-derived axes, in a fixed
 * order, and drops any candidate whose result this session has already
 * consumed.
 *
 * There is deliberately NO unconditional `tree` arm: the contract builder's
 * own rule is that "every fallback must name evidence that can resolve the
 * current proof gap", and a repo-wide inventory names none. When no rule
 * yields an unexecuted, evidence-naming call, the caller discloses an explicit
 * gap instead of inventing work.
 */
function alternativeProgressAxis(
  workspace: string,
  lane: string,
  result: TaskPackResult,
  query: string,
  blocked: ContinuationCall,
  servedZoomScope?: ServedWindowScope,
): ContinuationCall | undefined {
  const blockedArgs = (blocked.arguments ?? {}) as Record<string, unknown>;
  const candidates: ContinuationCall[] = [];

  // Rule 1 — generalizes advanceExecutedLocateNextCall's "advanced" arm: a
  // search whose candidates this session already holds becomes a batched read.
  if (blocked.tool === "search_files") {
    const executed = consultExecutedLocate(workspace, String(blockedArgs["query"] ?? ""));
    if (executed !== undefined && executed.length > 0) {
      candidates.push({ tool: "read_file", arguments: { handles: executed.slice(0, MAX_BATCH_NEXT_HANDLES) } });
    }
  }
  // Rule 2 — generalizes the same-scope re-pack arm: a re-pack of a directory
  // this caller already named becomes an inventory of that exact directory.
  if (blocked.tool === "read_file" && blockedArgs["mode"] === "task_pack") {
    const requested = Array.isArray(blockedArgs["paths"]) ? blockedArgs["paths"] as unknown[] : [];
    for (const entry of requested) {
      const candidate = typeof entry === "object" && entry !== null
        ? String((entry as { path?: unknown }).path ?? "")
        : String(entry);
      if (candidate.length > 0 && dirExistsCached(workspace, workspace, candidate)) {
        candidates.push({ tool: "search_files", arguments: { action: "tree", path: candidate } });
      }
    }
  }
  // Tolerates a loosely-typed producer-exit payload whose surfaces may already
  // have been projected away; the search axes above and below still apply.
  const surfaces = codeTaskPackSurfaces(Array.isArray(result.surfaces) ? result.surfaces : []);
  // Rule 3 — a required surface with no served code re-serves its own handle
  // (the contract builder's `gapFallback`, reachable after suppression).
  for (const surface of surfaces) {
    if (surface.required !== false && !hasServedCode(surface)) {
      candidates.push({ tool: "read_file", arguments: { handle: surface.handle } });
    }
  }
  // Rule 4 — a disclosed-partial required surface zooms its first omitted
  // window (the builder's `partialFallback`).
  for (const surface of surfaces) {
    if (surface.required !== false) {
      const range = firstUnservedPartialRange(workspace, surface, servedZoomScope, packServedSpans(result));
      if (range !== undefined) {
        candidates.push({
          tool: "read_file",
          arguments: { handle: surface.handle, range },
        });
      }
    }
  }
  // Rule 5 — a concern token the served bodies still do not match gets its own
  // find. Absence-proved tokens are already excluded by unmatchedConcernTokens,
  // and the ledger filter below catches anything it misses.
  for (const token of unmatchedConcernTokens(query, surfaces)) {
    candidates.push({ tool: "search_files", arguments: { action: "find", query: token } });
  }
  // Rule 6 (R1, 2026-08-28) — THE DISCOVERY BUNDLE IS AN AXIS, NOT A SEPARATE
  // ROUTE.
  //
  // `discoveryBundleNext` is the projector's own highest-ranked continuation,
  // and it satisfies this function's admission rule exactly — its paths are
  // "limited to files already related by served candidates or evidence edges",
  // which is what "name evidence that can resolve the current proof gap" means
  // for a re-pack. It was invisible here only because it is computed on the
  // WIRE side, so a pack whose contract next_call was suppressed declared
  // exhaustion while its own bundle route was still unexecuted — observed on
  // the qc1 replay shape, whose caller-supplied `surfaceRoles` make the
  // missing-roles hint byte-identical to the call being served.
  //
  // ORDERED LAST, deliberately: every shape rules 1–5 already repair keeps the
  // exact call it repaired to, so this rescues only the packs that would
  // otherwise have disclosed exhaustion. The ledger filter below still applies,
  // so a bundle this lane has already run is not proposed again.
  //
  // REACHABLE ONLY FROM THE PRODUCER EXIT, and that is the point: `qref` is
  // stamped in server.ts AFTER `buildTaskPack` returns, so at the in-build
  // choke `discoveryBundleNext` reads an unset `qref` and declines. The exit's
  // own call to this repair runs with the ref bound, which is where the axis
  // becomes real.
  const bundle = discoveryBundleNext(result);
  if (bundle !== undefined) candidates.push(bundle);

  const blockedFingerprint = nextFingerprint(blocked.tool, blockedArgs);
  for (const candidate of candidates) {
    const args = (candidate.arguments ?? {}) as Record<string, unknown>;
    if (nextFingerprint(candidate.tool, args) === blockedFingerprint) continue;
    // The predicate is RESULT CONSUMPTION, not byte novelty: an absent find and
    // an empty tree consumed their call just as much as a served slice did.
    if (hasExecutedNext(workspace, lane, candidate.tool, args)) continue;
    const admitted = admitReadOnlyNextCall(candidate, query);
    if (admitted !== undefined) return admitted;
  }
  return undefined;
}

/**
 * A-F2: turn ONE suppressed next into either progress or an honest disclosure —
 * never into a bare dead end, and never into a smaller request.
 *
 * Shared by this module's choke point and server.ts's producer exit, which had
 * independently grown the same "delete next_call, set request-user-input"
 * rewrite. The exit copy additionally FILTERED the suppressed call's query out
 * of `missing[]`, which shrank the disclosed requirement set at the exact
 * moment the pack lost its ability to act on it; §A-6(2) makes disclosure of an
 * undischarged obligation permanent, so that filter is gone.
 */
export function repairSuppressedNextCall(
  workspace: string,
  lane: string,
  result: TaskPackResult,
  query: string,
  blocked: ContinuationCall,
  servedZoomScope?: ServedWindowScope,
): "advanced" | "disclosed" {
  const contract = result.execution_contract;
  const alternative = alternativeProgressAxis(workspace, lane, result, query, blocked, servedZoomScope);
  (result as unknown as Record<string, unknown>)["retry_same_call"] = false;
  if (alternative !== undefined) {
    if (contract !== undefined) {
      contract.next_call = alternative;
      contract.next_action = "followup";
      delete contract.await_input_code;
      contract.max_additional_discovery_calls = Math.max(contract.max_additional_discovery_calls ?? 0, 1);
      contract.reason = `${contract.reason}; identical successful next replaced by the next unexecuted evidence axis`;
    }
    const plan = enforceContinuationBudget({
      version: 1,
      stages: [{ execution: "sequential", calls: [alternative] }],
    });
    if (plan) {
      result.continuation = plan;
      const next = deriveNextFromPlan(plan);
      if (next !== undefined) result.next = next;
    }
    return "advanced";
  }
  // Exhausted. Disclose the gap; keep every open obligation visible.
  const blockedTarget = typeof (blocked.arguments ?? {})["query"] === "string"
    ? String((blocked.arguments ?? {})["query"])
    : blocked.tool;
  if (contract !== undefined) {
    delete contract.next_call;
    contract.next_action = "request-user-input";
    contract.await_input_code = "no-grounded-call-remains";
    contract.max_additional_discovery_calls = 0;
    contract.reason = `${contract.reason}; identical successful next suppressed and every deterministic evidence axis is already consumed — the remaining gap is disclosed, not withdrawn`;
    const uncovered = (contract.evidence_model?.unresolved ?? []).slice(0, 8);
    contract.capability_gaps = [
      ...(contract.capability_gaps ?? []).filter((gap) => gap.kind !== "missing-evidence"),
      {
        kind: "missing-evidence" as const,
        recoverable: false,
        reason: `every deterministic evidence axis for ${blockedTarget} has already been executed this task`,
        ...(uncovered.length > 0 ? { obligation_ids: uncovered } : {}),
      },
    ].slice(0, 3);
  }
  delete result.next;
  delete result.continuation;
  // The producer exit calls this with a loosely-typed payload whose `missing`
  // may legitimately be absent (its previous inline copy guarded the same way).
  result.missing = [...new Set([
    ...(Array.isArray(result.missing) ? result.missing : []),
    `explicit-gap:${blockedTarget} (every deterministic evidence axis for it has already been executed)`,
  ])];
  // `missing[]` ships only on a partial pack (readFamily.ts A.8.2), and the
  // frozen `await_input` member carries no `gaps` key — so without this the
  // disclosure exists in-process and nowhere the caller can see it. A pack that
  // has run out of ways to discharge an obligation is not complete.
  if (result.coverage !== "partial") {
    result.coverage = "partial";
    result.coverage_reason ??= "concerns-uncovered";
  }
  return "disclosed";
}

/** Cap `reason` at `cap` chars on a clause boundary ("; " / " — "), never mid-word. */
function capReasonAtClause(reason: string, cap: number): string {
  if (reason.length <= cap) return reason;
  const cut = reason.slice(0, cap);
  const boundary = Math.max(cut.lastIndexOf("; "), cut.lastIndexOf(" — "));
  if (boundary > 0) return cut.slice(0, boundary);
  const wordSafe = cut.replace(/\s+\S*$/, "");
  return wordSafe.length > 0 ? wordSafe : cut;
}

function compactReceiptFromRecord(
  workspace: string,
  prev: ServedPackRecord,
  currentWorkspaceState: TaskWorkspaceState,
): TaskPackResult {
  const computed = computeClosureStateSafe(workspace);
  const storedIds = new Set(prev.checkIds);
  const openDescs = (computed?.open ?? [])
    .filter((c) => storedIds.has(c.id))
    .slice(0, 3)
    .map((c) => (c.desc.length > 140 ? c.desc.slice(0, 140) : c.desc));
  const compactExecutionContract: TaskExecutionContract | undefined = prev.executionContract
    ? {
        version: prev.executionContract.version,
        state: prev.executionContract.state,
        readiness: prev.executionContract.readiness,
        discovery_complete: prev.executionContract.discovery_complete,
        next_action: prev.executionContract.next_action,
        max_additional_discovery_calls: prev.executionContract.max_additional_discovery_calls,
        // Clause-boundary cap (same discipline as applyEnvelopeWeightTrim):
        // the hard slice shipped fragments like "… are served — act on the",
        // an instruction cut mid-directive.
        reason: capReasonAtClause(prev.executionContract.reason, 120),
        typestate: prev.executionContract.typestate,
        // protocol v1 §2.1.1 (C2-3): THE CERTIFICATE BINDING TRAVELS WITH THE
        // RE-SERVE, in its retention-trimmed form (ids, obligation statuses and
        // the action frontier — no evidence bodies, no falsification detail).
        //
        // `hasCertificateBinding` documents two legal forms, the full proof and
        // a bare `typestate.certificate_id`, and states that "both forms project
        // the SAME lean `certificate` field on the wire". That stopped being
        // true when the wire became A.3's `decision`: §2.1.1 lets an `act` ship
        // only over a `CertificateRef` (id + NON-EMPTY obligations + workspace
        // marker) and, for `act.edit`, a non-empty `FrontierEntry[]`. The
        // id-only form carries neither, so `projectTaskDecision` degraded a
        // prepared, edit-ready re-serve to `await_input:
        // "no-grounded-call-remains"` — telling an agent that already holds the
        // complete pack to go ask the user. The receipt ALSO stopped
        // classifying as A.4 `pack-unchanged`, whose honesty gate rejects a
        // body whose decision moved to `discover`/`await_input`; the decision
        // had not moved, the proof of it had merely been trimmed away.
        //
        // This costs no wire bytes. Both members that can carry this body —
        // `read.receipt` (A.5.6) and `read.task_pack` (A.5.1) — drop
        // `execution_contract` outright in `protocol/readFamily.ts`, so the
        // "paying the full certificate's bytes again on a sub-1KB receipt"
        // concern that shaped this projection is now satisfied downstream.
        //
        // P2 CORRECTION: it costs no WIRE bytes, but it cost 782 of the
        // record's own — `contractForEvidenceRetention` is the general
        // retention trim, and it still carries the falsification report, the
        // risk model and a per-obligation `evidence: []` + `reason:"proved"`
        // pair that restates `status`. None of that is read on this path, and
        // it pushed the compact revalidation receipt from ~1.9KB to 2259B
        // against its documented <2KB budget. `certificateBindingFor` emits the
        // BINDING the fence and the wire decision actually consume; the proof
        // stays on the pack that computed it.
        ...(prev.executionContract.readiness_certificate
          ? {
              readiness_certificate:
                certificateBindingFor(prev.executionContract.readiness_certificate),
            }
          : {}),
        ...(prev.executionContract.next_call ? { next_call: prev.executionContract.next_call } : {}),
      }
    : undefined;
  const shaByPath = new Map(prev.fileShas.map(({ path: p, sha }) => [p, sha]));
  // D3 receipt staleness (2026-08-01): the captured contract predates the
  // execution of its own next_call — re-validate against the session's
  // executed-locate ledger so a receipt can never re-point at a call that
  // already ran (observed live: a qref re-pack replaying the locate it had
  // just triggered, once the decoy-gate made the subset receipt eligible).
  //
  // P1-b (2026-08-28 review-fix wave): DELIBERATELY kept locate-only here
  // (the historical scope), not generalized to find/references/tree. Reason,
  // discovered empirically while extending it: `read.task_pack`'s wire
  // projector (readFamily.ts's `KEPT_ON_TASK_PACK`/`projectTaskPack`) never
  // ships a top-level `missing[]` field, and the frozen `await_input`
  // decision member carries no `gaps` key either (I5's own comment) — so a
  // NEW disclosure this compact path might synthesize for a find/references/
  // tree suppression has no wire channel to travel through without a
  // protocol-frozen field addition, out of this wave's scope. The fresh-build
  // path (`suppressNonProgressingNextCall` -> `repairSuppressedNextCall`)
  // does not have this gap — it runs before the response is wire-projected
  // and its own `missing[]` write is a genuine, already-tested mechanism —
  // so the generalization is scoped to that path only. See the design-delta
  // note in the A-REPORT for the fuller trace.
  const receiptAdvance = compactExecutionContract !== undefined
    ? advanceExecutedLocateNextCall(workspace, compactExecutionContract, LOCATE_ONLY_NO_REPEAT_ACTIONS)
    : "kept";
  // iter-2 W1/W2: the receipt's route re-affirms "working set complete — stop"
  // when the cached pack was complete/focused, so re-serves keep telling the
  // model to edit, not re-discover. A partial pack keeps its own route.
  const isAnswerReceipt = prev.taskProfile === "answer"
    || prev.route?.action === "answer_from_handles";
  const receiptRoute = applyCompleteStopRoute(prev.route, prev.coverage, isAnswerReceipt);
  const receipt: TaskPackResult = {
    mode: "task_pack",
    coverage: prev.coverage,
    ...(prev.coverageReason ? { coverage_reason: prev.coverageReason } : {}),
    ...(prev.coverageBasis ? { coverage_basis: prev.coverageBasis } : {}),
    // A.4 (C2-3): the `receipt` TAG is v1's discriminator. `pack_unchanged`
    // survives as the in-process signal `attachSupply`, the canonical-decision
    // fence and this module's own compaction guards read; `readFamily.ts`
    // deletes it from the wire.
    receipt: "pack-unchanged",
    pack_unchanged: true,
    workspace_state: currentWorkspaceState,
    surfaces: prev.surfaces.map((s) => {
      const sha = shaByPath.get(s.path);
      return {
        handle: s.handle,
        path: s.path,
        range: s.range,
        role: s.role,
        ...(sha !== undefined ? { sha } : {}),
      };
    }),
    missing: [...prev.missing],
    ...(receiptRoute ? { route: receiptRoute } : {}),
    ...(prev.taskProfile ? { task_profile: prev.taskProfile } : {}),
    ...(compactExecutionContract ? { execution_contract: compactExecutionContract } : {}),
    ...(receiptAdvance === "suppressed" ? { retry_same_call: false } : {}),
    ...(prev.contentSufficiency ? { content_sufficiency: prev.contentSufficiency } : {}),
    ...(openDescs.length > 0 ? { checks_open: openDescs } : {}),
    ...(prev.next && prev.executionContract?.next_call === undefined ? { next: prev.next } : {}),
  };
  // P0a §6.1: a receipt is an alternate WIRE shape, not alternate decision
  // semantics. The compact projection can contradict itself in exactly one
  // reachable way — `advanceExecutedLocateNextCall` suppressing the captured
  // next_call leaves a discovery-phase contract with nothing executable while
  // the carried-forward route still claims a terminal action. Fail closed onto
  // the canonical decision (insufficient evidence with no bounded call =>
  // ask, never "answer anyway"). A coherent receipt passes through untouched,
  // so the pinned prepared/discovery receipt bytes are unchanged.
  applyCanonicalTaskDecision(receipt);
  return receipt;
}

/**
 * If this call is an exact re-issue of a recently served pack for `workspace`
 * (matching fingerprint AND extra-args AND every captured surface file still
 * hashing identically), return a COMPACT idempotent response; otherwise
 * undefined (caller does the full recompute). Target: well under 1KB.
 *
 * iter-2 W1: searches the bounded multi-slot window (most-recent first), not
 * just the single latest slot, so a verbatim re-call that was NOT the
 * immediately-preceding call (an intervening pack sat between them) still hits.
 */
function tryServeCachedPack(
  workspace: string,
  args: TaskPackArgs,
  fingerprint: string,
): TaskPackResult | undefined {
  const records = servedPacksByWorkspace.get(laneScopedKey(workspace));
  if (records === undefined || records.length === 0) return undefined;
  const extraArgsKey = computeExtraArgsKey(args);
  const requestProfileKey = computeRequestProfileKey(args);
  // Most-recent first: if the same signature was served twice in the window, the
  // freshest capture carries the most up-to-date check ids / contract.
  const lane = currentSessionLane();
  const prev = [...records].reverse().find(
    (r) => r.fingerprint === fingerprint
      && r.extraArgsKey === extraArgsKey
      // F-V13-3: never re-serve another lane's record. A miss here is not a
      // refusal — it falls through to the full recompute this lane is owed.
      && r.lane === lane
      // B2b: an omitted taskProfile is the same request, not a new one.
      && packProfileCompatible(requestProfileKey, r.requestProfileKey),
  );
  if (prev === undefined) return undefined;
  // F-V13-5: the exact-fingerprint hit is the loop's own shape — the same
  // request, so the same fingerprint, so the same stored contract, so the same
  // consumed `next` — and the fingerprint cannot see the execution that
  // happened between the two calls.
  return revalidateRecordToReceipt(
    workspace,
    receiptRecordWithoutConsumedSearch(workspace, prev),
    args.taskQueryRefReplay === true,
  );
}

/**
 * Read-only preflight for the prepared-fence dispatcher exception. It proves
 * that this exact task_pack will be compacted to pack_unchanged before the
 * fence lets it reach the builder; it never authorizes a recomputed pack.
 */
export function canServeCachedTaskPackReceipt(workspace: string, args: TaskPackArgs): boolean {
  return tryServeCachedPack(workspace, args, computePackFingerprint(args, workspace)) !== undefined;
}

/**
 * iter-3 F1/F2 shared revalidation: prove `prev` is still safe to re-serve as a
 * compact receipt (every captured surface file byte-unchanged AND the whole
 * inventory-complete workspace_state fingerprint unchanged), then build the
 * receipt. Returns undefined — forcing a full recompute — when any proof fails.
 * Factored out so the EXACT-fingerprint path (tryServeCachedPack) and the
 * SEMANTIC-duplicate path (tryServeSemanticDuplicatePack) apply byte-for-byte
 * identical staleness gates; the only thing that differs between them is HOW
 * `prev` was selected.
 *
 * B5 (2026-08-21 serve-honesty, twin of the tryServeSubsetReceipt C2 fix): a
 * qref REPLAY whose matched record holds a surface still stamped
 * content_completeness:"partial" must never collapse into a compact
 * pack_unchanged receipt here either. The reasoning is identical to
 * tryServeSubsetReceipt's own guard (below) — a replay's premise is "the
 * caller is short on evidence for this exact working set and is asking to see
 * more of it" — but the STRUCTURAL gap is one layer deeper: this path answers
 * from a STORED `ServedPackRecord`, not a freshly-built `TaskPackResult`, and
 * `ServedPackRecord.surfaces` used to carry only {handle,path,range,role} — no
 * field to even ASK whether the record's own surface was ever partial. Now
 * that captureServedPack threads content_completeness/total_lines onto the
 * record (see ServedPackRecord.surfaces), this gate can see what the record
 * itself knows. A wire pack-unchanged receipt (protocol/readFamily.ts's
 * `receiptOf`, case "pack-unchanged") allowlists only handle/prior/path/range/
 * role per evidence entry — there is no slot to RESTATE a partial marker on
 * it — so, exactly as the merged fix chose, the only honest move is to
 * DECLINE the compact receipt and let the caller fall through to a fresh
 * build, whose `content_completeness`/`remaining_ranges`/`total_lines`/
 * `next_call` all travel the normal (non-receipt) wire path honestly.
 *
 * Scoped to REPLAY specifically, exactly like the merged fix: a PLAIN re-ask
 * (isQueryRefReplay:false) that happens to exact-fingerprint-match or
 * semantic-duplicate-match a record with one still-partial peripheral surface
 * is not asking to see more of that surface, so a receipt over it stays a
 * correct, load-bearing "nothing changed" answer (semanticSignalRegression's
 * T05c case, mirrored for this path in servedRecordPartialSurface.spec.ts).
 */
function revalidateRecordToReceipt(
  workspace: string,
  prev: ServedPackRecord,
  isQueryRefReplay: boolean,
): TaskPackResult | undefined {
  // The captured surfaces must actually be verifiable AND unchanged. An empty
  // capture (a no-surface pack) has nothing to re-serve compactly and nothing
  // to prove unchanged — recompute rather than emit an empty acknowledgement.
  if (prev.fileShas.length === 0) return undefined;
  if (isQueryRefReplay && prev.surfaces.some((s) => s.content_completeness === "partial")) {
    return undefined;
  }
  for (const { path: p, sha } of prev.fileShas) {
    if (surfaceFileSha(workspace, p) !== sha) return undefined; // changed → recompute
  }
  // A re-serve is idempotent only for the same workspace state, not merely when
  // the previously surfaced files stayed byte-identical. Otherwise a new/changed
  // unserved implementation could invalidate the falsification result while the
  // compact response kept claiming closure.
  //
  // D2 (F-B2, 2026-08-21): the RECORD side of this equality used to be the
  // CERTIFICATE's fingerprint (`executionContract.workspace_state`), while the
  // current side was recomputed from `prev.surfaces`. Those are hashed over
  // DIFFERENT surface lists whenever a certificate outlives the surfaces it was
  // issued over — a C2 carry-forward clones an earlier pack's contract onto a
  // narrower replay, and trimToCap's byte-budget shed removes surfaces after the
  // contract was built — so for those packs the equality could never hold and
  // the receipt was permanently unreachable (F-B2). The record now carries its
  // OWN capture-time state, so the comparison stops reconstructing the record
  // side lossily. It does NOT get weaker: `recordWorkspaceState` re-hashes every
  // surface file's bytes off live disk and re-walks the inventory on THIS call,
  // so one changed surface byte — or any add/remove/resize anywhere in the
  // workspace — still moves the current fingerprint and declines the receipt.
  // The certificate's own `inventory_complete` gate is kept as-is: a proof
  // issued under an incomplete inventory is a weaker claim and stays
  // receipt-ineligible, and so is a record captured under one.
  const priorWorkspaceState = prev.executionContract?.workspace_state;
  if (priorWorkspaceState?.inventory_complete !== true) return undefined;
  if (prev.workspaceState.inventory_complete !== true) return undefined;
  const inventory = computeWorkspaceInventoryState(workspace);
  if (!inventory.complete) return undefined;
  const currentWorkspaceState = recordWorkspaceState(workspace, prev, inventory);
  if (currentWorkspaceState.fingerprint !== prev.workspaceState.fingerprint) return undefined;
  return compactReceiptFromRecord(workspace, prev, currentWorkspaceState);
}

/**
 * iter-3 F2 (semantic-duplicate receipt): the exact-fingerprint dedup
 * (tryServeCachedPack) misses when a re-issue of the SAME request was merely
 * TRUNCATED mid-word by the client (the last epoch token differs) or otherwise
 * tokenizes slightly differently — the forensic case that re-served a full
 * ~20KB pack for zero new content. This recognizes such a near-identical re-ask
 * and serves the SAME compact receipt.
 *
 * The match is deliberately conservative — DISTINCT working-set packs (enum vs
 * backend vs frontend, different concern/paths) must NOT collapse into one
 * another. The strong discriminator is PATH-SUBSET: the new request's explicit
 * paths must be a subset of the prior record's served paths. Token overlap alone
 * is not enough (two different concerns can share a few tokens); the rule is
 * path-subset ∧ HIGH (near-equality) epoch-token overlap ∧ the whole-workspace
 * inventory fingerprint unchanged. Every staleness gate the exact path applies
 * still runs (revalidateRecordToReceipt), so a changed file forces a full serve.
 */
function tryServeSemanticDuplicatePack(
  workspace: string,
  args: TaskPackArgs,
): TaskPackResult | undefined {
  const records = servedPacksByWorkspace.get(laneScopedKey(workspace));
  if (records === undefined || records.length === 0) return undefined;
  const queryTokens = tokenizeForEpoch(args.query ?? args.symbol ?? args.path ?? "");
  if (queryTokens.length < 2) return undefined; // too little signal to match semantically
  const extraArgsKey = computeExtraArgsKey(args);
  const requestProfileKey = computeRequestProfileKey(args);
  const curPaths = requestedPathSet(args);
  const curPathsKey = curPaths.join(" ");
  const inventory = computeWorkspaceInventoryState(workspace);
  if (!inventory.complete) return undefined;
  const curTokenSet = new Set(queryTokens);
  const curRoles = (args.surfaceRoles ?? []).slice().sort().join("\u0000");
  const lane = currentSessionLane();
  // Most-recent first: prefer the freshest matching record.
  for (const prev of [...records].reverse()) {
    // F-V13-3: a semantic duplicate is duplicate only WITHIN one lane — this
    // path is strictly looser than the exact one (near-token-equality, not
    // identity), so it is the likelier of the two to collapse two agents'
    // distinct working sets into one another's receipt.
    if (prev.lane !== lane) continue;
    // Same "extra" args (path/symbol/lang/limit) — a differing value changes
    // served content, exactly as the exact path requires. B2b: the requested
    // profile is gated separately, so an omitted one still matches.
    if (prev.extraArgsKey !== extraArgsKey) continue;
    if (!packProfileCompatible(requestProfileKey, prev.requestProfileKey)) continue;
    // Same surfaceRoles set — a differing role set is a DIFFERENT working set
    // (it adds required roles / more surfaces), never a mere query truncation.
    if (prev.requestRoles.join("\u0000") !== curRoles) continue;
    // Whole-workspace-unchanged: the record's inventory-complete fingerprint must
    // equal the current one (proves no unserved file moved), same as the subset
    // receipt's guarantee.
    if (prev.inventoryFingerprint === "" || prev.inventoryFingerprint !== inventory.fingerprint) continue;
    // SAME requested (seeded) path set. Query truncation preserves paths exactly,
    // so the requested sets must be EQUAL — not merely the current being a subset
    // of what the prior pack SERVED. This keeps a deliberate re-seed of a DIFFERENT
    // file (even one the earlier pack served as supporting context), and a pathless
    // re-ask against a seeded prior, from collapsing into the wrong receipt.
    if (prev.requestPaths.join(" ") !== curPathsKey) continue;
    // HIGH (near-equality) token overlap — a truncated re-ask shares all-but-the-
    // truncated-tail token with the prior. Require the shared count to dominate
    // BOTH token lists so a small query cannot match a large unrelated record.
    const recTokens = prev.epochTokens;
    if (recTokens.length === 0) continue;
    let shared = 0;
    for (const t of recTokens) if (curTokenSet.has(t)) shared += 1;
    const denom = Math.max(queryTokens.length, recTokens.length);
    if (shared < denom * SEMANTIC_DUP_OVERLAP_RATIO) continue;
    // A pathless re-ask (no explicit paths) rides on token overlap alone, so it
    // must clear a stricter bar: all-but-one current token present in the record.
    if (curPaths.length === 0 && shared < queryTokens.length - 1) continue;
    // F-V13-5: the looser of the two receipt paths repairs its match the same
    // way the exact one does — the staleness is a property of the RECORD, not
    // of how it was selected.
    const receipt = revalidateRecordToReceipt(
      workspace,
      receiptRecordWithoutConsumedSearch(workspace, prev),
      args.taskQueryRefReplay === true,
    );
    if (receipt !== undefined) return receipt;
  }
  return undefined;
}

/**
 * iter-2 W1 (subset case): a freshly-built pack whose every code-bearing surface
 * (path+range) is already resident-and-unchanged from earlier same-epoch serves
 * in this session is re-serving bodies the caller already holds. When the
 * workspace is proven unchanged (same inventory-complete workspace_state
 * fingerprint the exact-dup path uses), return the compact receipt naming those
 * resident handles instead. Conservative: exact (path,range) membership, and
 * only when EVERY served surface is covered — a genuinely new surface falls
 * through to a normal full serve.
 */
function tryServeSubsetReceipt(
  workspace: string,
  result: TaskPackResult,
  epochTokens: readonly string[],
  isQueryRefReplay: boolean,
): TaskPackResult | undefined {
  if (result.pack_unchanged === true) return undefined;
  if ((result.surfaces as TaskPackResultSurface[]).some(isArtifactTaskPackSurface)) return undefined;
  // Only collapse a COMPLETE/focused working set. A partial pack still carries
  // load-bearing missing-role guidance (missing_required_surfaces /
  // blocking_next_steps / concerns) that a body-stripped receipt would drop —
  // and its bodies being resident does not make the pack redundant. This is
  // exactly the D2 case (re-packs AFTER coverage already flipped complete).
  if (result.coverage !== "complete" && result.coverage !== "focused") return undefined;
  // C2 byte-cap-fix follow-up (2026-08-21): a qref REPLAY (taskQueryRefReplay)
  // whose carried-forward working set still holds a surface with
  // content_completeness:"partial" — the doc-sliver affordance's own honesty
  // marker (applyDocSliverHonesty, above) — must never collapse into this
  // receipt. The replay's whole premise is "the caller is short on evidence
  // for this exact working set and is asking to see more of it" (C2's own
  // carryForwardCertifiedWorkingSet docstring), and ServedPackRecord.surfaces
  // (and this function's own ad-hoc record below) carry only
  // {handle,path,range,role}: a compact pack_unchanged receipt has no field to
  // restate the partial marker/total_lines/next_call in. Collapsing such a
  // reply here silently retracts the partial disclosure L1/C1/C2 exist to
  // make honest — the same false closure applyDocSliverHonesty's own
  // `pack_unchanged` guard refuses to create, approached from the other
  // direction (a pack that already earned the marker, reaching this
  // compaction afterward instead of before). Scoped to a REPLAY specifically
  // (not every partial-content pack): a plain re-ask of a broader multi-file
  // working set that happens to include one still-partial peripheral doc
  // — semanticSignalRegression.spec.ts's T05c case — is not asking to see
  // more of that doc, so a receipt over it stays a correct, load-bearing
  // "nothing changed" answer.
  if (
    isQueryRefReplay
    && codeTaskPackSurfaces(result.surfaces).some((s) => s.content_completeness === "partial")
  ) return undefined;
  const bodySurfaces = codeTaskPackSurfaces(result.surfaces).filter(
    (s) => s.code !== undefined || s.code_unchanged !== undefined,
  );
  if (bodySurfaces.length === 0) return undefined;
  if (result.execution_contract?.workspace_state === undefined) return undefined;
  const records = servedPacksByWorkspace.get(laneScopedKey(workspace));
  if (records === undefined || records.length === 0) return undefined;
  // Whole-workspace-unchanged proof: the CURRENT inventory-complete fingerprint
  // must equal one a prior serve captured. This rules out a new/changed UNSERVED
  // file (which per-surface shas alone cannot see) silently invalidating the
  // pack — exactly the guarantee the exact-dup path's inventory gate gives.
  const inventory = computeWorkspaceInventoryState(workspace);
  if (!inventory.complete) return undefined;
  // The union of (path,range) served under an OVERLAPPING epoch by a record whose
  // whole-workspace inventory still matches, each file proven byte-unchanged.
  const resident = new Set<string>();
  for (const rec of records) {
    if (rec.inventoryFingerprint === "" || rec.inventoryFingerprint !== inventory.fingerprint) continue;
    if (!epochsOverlap(rec.epochTokens, epochTokens)) continue;
    const shaByPath = new Map(rec.fileShas.map(({ path: p, sha }) => [p, sha]));
    for (const s of rec.surfaces) {
      const capturedSha = shaByPath.get(s.path);
      if (capturedSha === undefined) continue; // unproven file — cannot claim unchanged
      if (surfaceFileSha(workspace, s.path) !== capturedSha) continue; // changed
      resident.add(`${s.path}\u0000${s.range}`);
    }
  }
  if (resident.size === 0) return undefined;
  if (!bodySurfaces.every((s) => resident.has(`${s.path}\u0000${s.range}`))) return undefined;
  const fileShas: Array<{ path: string; sha: string }> = [];
  const seen = new Set<string>();
  for (const s of result.surfaces) {
    if (seen.has(s.path)) continue;
    seen.add(s.path);
    const sha = surfaceFileSha(workspace, s.path);
    if (sha !== undefined) fileShas.push({ path: s.path, sha });
    if (fileShas.length >= MAX_FINGERPRINT_FILES) break;
  }
  const record: ServedPackRecord = {
    // Synthetic, never stored (see the workspaceState note below), but the
    // field is required so no record-builder can forget it — and the honest
    // value is the lane this subset receipt is being served INTO.
    lane: currentSessionLane(),
    fingerprint: "",
    extraArgsKey: "",
    requestProfileKey: "auto",
    surfaces: result.surfaces.map((s) => ({
      handle: s.handle,
      path: s.path,
      range: s.range,
      role: s.role,
      // B5: keep this ad-hoc record's fields accurate too — never stored (see
      // the "never F2-matched" note below), but compactReceiptFromRecord's
      // shared surface-projection reads them uniformly regardless of which
      // caller built the record.
      ...(s.content_completeness !== undefined ? { content_completeness: s.content_completeness } : {}),
      ...(s.total_lines !== undefined ? { total_lines: s.total_lines } : {}),
    })),
    // D2 (F-B2): this record is synthetic and never stored, so it never reaches
    // revalidateRecordToReceipt's freshness equality — but the field is
    // deliberately REQUIRED on ServedPackRecord so no future record-builder can
    // forget it. The honest value here is the freshly-built pack's own
    // workspace_state, which is also what this call hands
    // compactReceiptFromRecord below (non-undefined by the guard above).
    workspaceState: result.execution_contract!.workspace_state!,
    coverage: result.coverage,
    coverageReason: result.coverage_reason,
    coverageBasis: result.coverage_basis,
    route: result.route ? { ...result.route } : undefined,
    epochTokens: [...epochTokens],
    requestRoles: [], // synthetic subset-receipt record — never stored, so never F2-matched
    requestPaths: [],
    inventoryFingerprint: inventory.fingerprint,
    missing: [...result.missing],
    next: result.next,
    profileBinding: result.profile_binding,
    taskProfile: result.task_profile,
    // F-V13-5, belt-and-braces on the third door: this path compacts a FRESHLY
    // BUILT pack, so `suppressNonProgressingNextCall` has normally repaired a
    // consumed next already — but the compaction is otherwise free to
    // re-publish whatever contract survived it, and the identity-preserving
    // repair costs nothing when there is nothing to repair.
    executionContract: receiptContractWithoutConsumedSearch(workspace, result.execution_contract),
    contentSufficiency: result.content_sufficiency,
    checkIds: (getPackChecks(workspace)?.checks ?? []).map((c) => c.id),
    fileShas,
  };
  return compactReceiptFromRecord(workspace, record, result.execution_contract!.workspace_state!);
}

// ===========================================================================
// iter-2 W2 / W4 / W5 orchestration — applied at the shared dedupeTrimAndPersist
// exit so every pack flow reports identically.
// ===========================================================================

/** Every value `classifySurface` can return — the vocabulary `missing` uses for a ROLE gap. */
const SURFACE_ROLE_NAMES: ReadonlySet<string> = new Set(Object.keys(SURFACE_ROLE_PRIORITY));

/**
 * B1b (2026-08-01 retrieval-scope): `coverage_reason:"missing-roles"` is a
 * claim ABOUT ROLES, so it must always NAME them.
 *
 * Live forensics (run 2026-07-31-semantic-signal5-2, T09 rep1/rep2): an
 * explicit-paths pack came back `coverage:"partial"` +
 * `coverage_reason:"missing-roles"` + `missing:[]` — a self-contradiction with
 * no recovery in it, and the solver had nothing to act on. The producing site
 * is refreshAfterRecursiveClosure, which FILTERS `missing` /
 * `missing_required_surfaces` down to the roles the closure did not cover but
 * only clears the REASON when the unmatched-concern list is empty too; the
 * "roles all covered, a concern token still unmatched" combination therefore
 * shipped the roles claim with nothing behind it. This is the same
 * empty-vs-absent family the 2026-07-31 next_call wave closed elsewhere, so it
 * is enforced HERE, once, at the shared exit — every flow reports identically.
 *
 * Repair order (never weakens an honest gap, never invents one):
 *   1. roles are named somewhere and still unserved → normalize
 *      `missing_required_surfaces` to exactly that set, so the claim and the
 *      role list agree. `missing` is deliberately NOT rewritten: several flows
 *      empty it on purpose once a stronger proof supersedes role bookkeeping
 *      (a ready wiring graph, cumulative coverage), and re-populating it there
 *      would resurrect a gap those flows just closed;
 *   2. nothing named anywhere and nothing else unresolved → the claim is simply
 *      false: drop it. Re-deriving a role set from `required_surfaces` was
 *      considered and rejected — every flow already computes
 *      missing_required_surfaces that way, so an EMPTY one means the flow
 *      decided nothing is missing, and manufacturing a gap it deliberately
 *      closed is a worse error than an unlabeled partial;
 *   3. `missing` still carries NON-role entries (an unreadable caller path, an
 *      unexpanded directory marker) → the label is imprecise but the pack does
 *      name its gap, which is what a caller needs to act on. Leave it: the
 *      invariant is "never a claim with nothing behind it", not a relabeling
 *      pass.
 */
function reconcileMissingRolesContract(result: TaskPackResult): void {
  if (result.coverage_reason !== "missing-roles") return;
  const served = new Set(codeTaskPackSurfaces(result.surfaces).map((s) => s.role));
  const named = [...new Set([
    ...(result.missing_required_surfaces ?? []),
    ...result.missing.filter((entry) => SURFACE_ROLE_NAMES.has(entry)),
  ])].filter((role) => !served.has(role));
  if (named.length > 0) {
    result.missing_required_surfaces = named;
    return;
  }
  if (result.missing.length > 0) return;
  delete result.missing_required_surfaces;
  delete result.coverage_reason;
}

// ---------------------------------------------------------------------------
// P0 defects 2 + 3 — the task epoch's PERSISTED requirement contract.
//
// `requiredSurfacesForTask` already carries the ROLE axis forward at inference
// time (its `carried` block), which is enough while the ordinary pipeline runs.
// It is NOT enough at the exit: `refreshAfterRecursiveClosure`'s wiring branch
// clears `missing` outright, `reconcileMissingRolesContract` can drop a label
// that no longer names anything, and the CONCERN axis never had any
// carry-forward at all. This is the fail-closed backstop for both axes at the
// one shared exit every flow passes through, so a NARROWED continuation cannot
// leave with `coverage:"complete"` — or a terminal certificate — while the
// epoch's own standing requirements are still unserved.
//
// The vocabulary is deliberately the existing one: coverage "partial",
// coverage_reason "missing-roles"/"concerns-uncovered", `missing[]`,
// `missing_required_surfaces[]`. The two markers below are disclosure NOTES in
// `missing[]` — the same shape (and same reason) as F-B3's
// `unserved-obligation:` note: a bare role name says nothing about WHERE the
// requirement came from, and canonicalDecision.ts needs an exact, narrow
// marker to demote THIS response's own certificate on.
// ---------------------------------------------------------------------------

/** Marker: a role an EARLIER pack of this epoch required and no served evidence covers. Parsed by canonicalDecision.ts's `unservedEpochContractZoom` — keep the two in sync. */
const UNSERVED_EPOCH_ROLE_PREFIX = "unserved-required-role:";
/** Marker: a concern token an EARLIER pack of this epoch named and no served evidence matches. Same parser. */
const UNCOVERED_EPOCH_CONCERN_PREFIX = "uncovered-concern:";
/** Bound on epoch-contract disclosures per axis — sized like unmatchedConcernTokens' own 4-token cap, with which it shares the W2 guidance-metadata budget. */
const MAX_EPOCH_CONTRACT_DISCLOSURES = 4;

/**
 * This pack's contribution to the epoch's standing concern universe: the
 * concerns IT PUBLISHED as uncovered, and nothing else.
 *
 * Deliberately not "every identifier-shaped token in the query". A query names
 * tokens that no repository evidence can ever match — the classic one is the
 * value being ADDED ("add STALLED status to the status enum"), which is a
 * requirement on the EDIT, not on the evidence. Harvesting those made the
 * epoch permanently uncertifiable: pack 1 correctly shipped `complete`, and
 * every later pack of the same task was then demoted for failing to "cover" a
 * token that does not exist yet. Storing only what a pack itself declared
 * uncovered keeps the store's claim exactly as strong as the pack's own.
 */
function epochConcernTokensFor(result: TaskPackResult, query: string): string[] {
  const tokens = result.coverage_reason === "concerns-uncovered"
    ? unmatchedConcernTokens(query, codeTaskPackSurfaces(result.surfaces))
    : [];
  // Keep an additive open-universe mutation's requested value as a durable
  // concern so an authoritative absent find can be consumed by the next
  // same-task-handle pack (e.g. REFUNDED), even when the first pack is
  // otherwise complete.
  if (hasOpenUniverseIntent(query) && !isAdditiveEnumIntent(query)) {
    const requestedValue = query.match(/\b[A-Z][A-Z0-9_]{2,}\b/)?.[0];
    if (requestedValue !== undefined && !tokens.includes(requestedValue)) tokens.push(requestedValue);
  }
  return tokens;
}

/**
 * The evidence the CALLER holds for this epoch: this pack's own surfaces plus
 * every same-epoch surface an earlier pack served. The second half is not
 * generosity — `recordServedEditAdmissibility`'s serve-time rule already makes
 * any content-bearing serve edit-admissible for the rest of the epoch, so a
 * requirement an earlier pack served is genuinely discharged. Prior entries
 * carry no body, so they can only match on path/role identity: the strict
 * direction.
 */
function epochServedEvidence(
  workspace: string,
  result: TaskPackResult,
  epochTokens: readonly string[],
): TaskPackSurface[] {
  return [
    ...codeTaskPackSurfaces(result.surfaces),
    ...queryServedSurfaces(workspace, workspace, { epochTokens }).map((entry) => ({
      role: entry.role,
      handle: entry.handle ?? "",
      path: entry.path,
      range: "",
      // The serve log deliberately stores identities rather than response
      // bodies.  Re-read the still-local source only for this server-side
      // proof check, so a same-epoch challenge can prove an earlier concern
      // from evidence it genuinely served instead of reopening it merely
      // because the current pack selected a different frontier.
      code: readCached(workspace, entry.path),
    })),
  ];
}

/**
 * Demote a pack that would certify against a universe smaller than the one its
 * own task epoch already established. Strictly tightening: it can only lower
 * `coverage` and add disclosures, never raise or remove either.
 */
function reconcileEpochTaskContract(
  workspace: string,
  result: TaskPackResult,
  query: string,
): void {
  const epochTokens = tokenizeForEpoch(query);
  const stored = queryTaskContract(workspace, epochTokens);
  if (stored === undefined) return;
  const evidence = epochServedEvidence(workspace, result, epochTokens);
  const covered = new Set([...stored.servedRoles, ...evidence.map((surface) => surface.role)]);
  recordServedRoleEvidence(workspace, [...covered]);
  // A BACKSTOP, not a second narrator. When this pack already names the gap in
  // the ordinary vocabulary — which is the normal case now that
  // `requiredSurfacesForTask` carries roles forward at inference time — there
  // is nothing left to repair, and restating it would only churn the response
  // bytes (declining the subset receipt whose whole claim is that nothing
  // changed). What survives this filter is exactly the wiped case: a branch
  // that cleared `missing` after the requirement was established.
  const alreadyDisclosed = [
    ...result.missing,
    ...(result.missing_required_surfaces ?? []),
    ...(result.blocking_next_steps ?? []),
    ...(result.checks ?? []),
  ].join("\n").toLowerCase();
  const unservedRoles = stored.requiredRoles
    .filter((role) => !covered.has(role) && !alreadyDisclosed.includes(role.toLowerCase()))
    .slice(0, MAX_EPOCH_CONTRACT_DISCLOSURES);
  const coveredConcerns = new Set(stored.coveredConcernTokens);
  const newlyServedConcerns = stored.concernTokens.filter((token) =>
    concernTokenMatchesSurfaces(token, evidence)
  );
  // Make the durable ledger agree with the evidence reconciliation before the
  // final contract is built.  A challenge may legitimately serve the second
  // of two same-epoch frontiers; retaining the first frontier's proof is what
  // makes their union terminal rather than an awaiting-input regression.
  recordServedConcernEvidence(workspace, newlyServedConcerns);
  newlyServedConcerns.forEach((token) => coveredConcerns.add(token));
  const uncoveredConcerns = stored.concernTokens
    .filter((token) =>
      !coveredConcerns.has(token)
      && !alreadyDisclosed.includes(token.toLowerCase())
      && !concernTokenMatchesSurfaces(token, evidence))
    .slice(0, MAX_EPOCH_CONTRACT_DISCLOSURES);
  if (unservedRoles.length === 0 && uncoveredConcerns.length === 0) {
    // `buildTaskExecutionContract` is evaluated once before this reconciliation
    // and once for the final wire result.  Drop only its *derived* prior
    // projection here so the second evaluation can project the now-discharge
    // ledger.  User-facing missing evidence and explicit capability gaps stay
    // untouched.
    const discharged = new Set([
      ...[...covered].map((role) => `unresolved-ledger:required-role:${role}`),
      ...[...coveredConcerns].map((token) => `unresolved-ledger:concern-token:${token}`),
    ]);
    result.missing = result.missing.filter((entry) => !discharged.has(entry));
    if (
      result.coverage_basis === "cumulative"
      && result.missing.length === 0
      && (result.missing_required_surfaces?.length ?? 0) === 0
    ) {
      result.coverage = "complete";
      delete result.coverage_reason;
    }
    return;
  }

  // The epoch's SOURCE query, not this narrowed call's — a continuation that
  // re-asks with the truncated text is the defect one file over.
  const sourceQuery = continuationQuery(stored.query !== "" ? stored.query : query);
  result.missing = [...new Set([
    ...result.missing,
    ...unservedRoles.map((role) =>
      `${UNSERVED_EPOCH_ROLE_PREFIX}${role} (required by an earlier pack in this task;`
      + ` re-request via read_file mode=task_pack query="${sourceQuery}" surfaceRoles=["${role}"])`),
    ...uncoveredConcerns.map((token) =>
      `${UNCOVERED_EPOCH_CONCERN_PREFIX}${token} (named by an earlier pack in this task;`
      + ` re-request via search_files action=find query=${token})`),
  ])];
  if (unservedRoles.length > 0) {
    // These ARE roles, so they belong in the role fields too — that is what
    // makes the route/next machinery name them instead of guessing.
    result.required_surfaces = mergeUnique(result.required_surfaces ?? [], unservedRoles);
    result.missing_required_surfaces = mergeUnique(result.missing_required_surfaces ?? [], unservedRoles);
  }
  result.coverage = "partial";
  if (result.coverage_reason === undefined) {
    result.coverage_reason = unservedRoles.length > 0 ? "missing-roles" : "concerns-uncovered";
  }
}

/**
 * Fold THIS pack into the epoch's standing contract: what it required, what it
 * named as a concern, and what it actually served. Runs AFTER the
 * reconciliation above, so a pack is never measured against its own record.
 */
interface PersistedEvidenceExpansion {
  targets: readonly string[];
  remaining: readonly string[];
  gaps: readonly string[];
}

const MAX_EVIDENCE_EXPANSION_TARGETS = 4;

async function importEvidenceExpansion(
  workspace: string,
  primaries: readonly ImpactCandidate[],
  query: string,
  surfaces: readonly TaskPackSurface[],
): Promise<PersistedEvidenceExpansion> {
  // P1-g (2026-08-28 review-fix wave): this called walkCodeFiles(workspace)
  // — an ignore-aware walk of the ENTIRE workspace — unconditionally for
  // every pack, including the overwhelming majority that are not an
  // open-universe/exhaustive request and have no use for the result: both of
  // this function's own callers feed `evidenceExpansion` into
  // dedupeTrimAndPersist, which reads only `.gaps` (via
  // hasCurrentEvidenceExpansionGap, itself gated on hasOpenUniverseIntent)
  // and passes the whole object into recordEpochTaskContract, whose
  // `expansionTargets`/`expansionGaps` both force to `[]` when
  // `!hasOpenUniverseIntent(query)`. A non-open-universe task's expansion
  // result is therefore fully inert downstream today — gating the walk
  // (and the importEdgeCandidates/calleesOf work below it) behind the same
  // canonical predicate changes no observable behavior for that majority and
  // removes the per-pack full-repository walk from its cost. Deliberately
  // NOT also gated on `primaries.length === 0`: even with zero primaries the
  // pre-wave code still derived `directCallees` from already-served surface
  // bodies via calleesOf below, independent of `files`/importEdgeCandidates
  // — preserving that path keeps this change scoped to the walk-avoidance
  // fix alone.
  if (!hasOpenUniverseIntent(query)) {
    return { targets: [], remaining: [], gaps: [] };
  }
  // In-pack memoization: `files` is computed once here and shared by every
  // `primary` in the Promise.all below — this function's own two call sites
  // (the general and answer-shaped pack builders) are mutually exclusive per
  // pack build, so there is no second walk to memoize across within one pack.
  const files = walkCodeFiles(workspace);
  const queryTokens = query.toLowerCase().match(/[a-z_$][a-z0-9_$]*/g) ?? [];
  const details = await Promise.all(primaries.slice(0, MAX_EVIDENCE_EXPANSION_TARGETS).map((primary) =>
    importEdgeCandidates(workspace, primary as unknown as Parameters<typeof importEdgeCandidates>[1], queryTokens, files),
  ));
  const served = codeTaskPackSurfaces([...surfaces]).filter(hasServedCode);
  const directCallees = served.flatMap((surface) =>
    calleesOf(surface.code!, surface.symbol ? [surface.symbol] : []),
  );
  const candidates = [...new Set([
    ...details.flatMap((detail) => detail.candidates.map((candidate) => candidate.symbol ?? candidate.path)),
    ...directCallees,
  ])];
  const remaining = details.flatMap((detail) => detail.remaining.map((candidate) => candidate.symbol ?? candidate.path));
  const primaryPath = served[0]?.path ?? primaries[0]?.path;
  const language = primaryPath === undefined ? "unknown" : (languageForPath(primaryPath) ?? "unknown");
  const expansion = expandOneHop(language, candidates, MAX_EVIDENCE_EXPANSION_TARGETS);
  return {
    targets: expansion.targets.map((entry) => entry.target),
    remaining: [...expansion.remaining, ...remaining],
    gaps: [
      ...(expansion.explicitGap === undefined ? [] : [expansion.explicitGap]),
      ...(remaining.length === 0 ? [] : [`import-edge expansion has ${remaining.length} unserved target(s)`]),
    ],
  };
}

/** Whether this response itself carries an extractor-capability proof that
 * must be visible to its final readiness rebuild. Ordinary unresolved
 * expansions remain post-decision epoch bookkeeping so their prescribed
 * discovery next is emitted before the ledger grows. */
function hasCurrentEvidenceExpansionGap(
  result: TaskPackResult,
  query: string,
): boolean {
  if (!hasOpenUniverseIntent(query) || isAdditiveEnumIntent(query)) return false;
  const requestedSymbols = explicitCodeIdentifiers(query).slice(0, 1);
  return codeTaskPackSurfaces(result.surfaces)
    .filter(hasServedCode)
    .some((surface) => expandOneHop(
      languageForPath(surface.path) ?? "unknown",
      calleesOf(servedSurfaceText(surface), surface.symbol ? [surface.symbol] : requestedSymbols),
      MAX_EVIDENCE_EXPANSION_TARGETS,
    ).explicitGap !== undefined);
}

function recordEpochTaskContract(
  workspace: string,
  result: TaskPackResult,
  query: string,
  expansion?: PersistedEvidenceExpansion,
): boolean {
  const epochTokens = tokenizeForEpoch(query);
  if (epochTokens.length === 0) return false;
  const served = codeTaskPackSurfaces(result.surfaces).filter(hasServedCode);
  // The satisfied half is harvested from the WHOLE query, not just the
  // published gap: a token this pack's evidence matches is discharged for the
  // epoch even though it was never a published requirement.
  const shaped = identifierShapedConcernSet(query);
  const queryTokens = concernAnchorTokens(query, 8).filter((token) => shaped.has(token.toLowerCase()));
  const requestedSymbols = explicitCodeIdentifiers(query).slice(0, 1);
  const oneHop = served.flatMap((surface) =>
    calleesOf(servedSurfaceText(surface), surface.symbol ? [surface.symbol] : requestedSymbols),
  );
  const localExpansions: PersistedEvidenceExpansion[] = !hasOpenUniverseIntent(query)
    || isAdditiveEnumIntent(query)
    ? []
    : served.map((surface) => {
      const local = expandOneHop(
        languageForPath(surface.path) ?? "unknown",
        calleesOf(servedSurfaceText(surface), surface.symbol ? [surface.symbol] : requestedSymbols),
        MAX_EVIDENCE_EXPANSION_TARGETS,
      );
      return {
        targets: local.targets.map((entry) => entry.target),
        remaining: [...local.remaining],
        gaps: local.explicitGap === undefined ? [] : [local.explicitGap],
      } satisfies PersistedEvidenceExpansion;
    });
  // The imported expansion is advisory input from the selected primaries. The
  // served primary itself is the authoritative current evidence, so always
  // compute its local one-hop result as well. This prevents an empty/cached
  // import-edge result from erasing an unsupported-language explicit gap.
  const expansionParts = [expansion, ...localExpansions].filter(
    (entry): entry is PersistedEvidenceExpansion => entry !== undefined,
  );
  const effectiveExpansion = expansionParts.length === 0 ? undefined : {
    targets: [...new Set(expansionParts.flatMap((entry) => entry.targets))].sort((a, b) => a.localeCompare(b)),
    remaining: [...new Set(expansionParts.flatMap((entry) => entry.remaining))].sort((a, b) => a.localeCompare(b)),
    gaps: [...new Set(expansionParts.flatMap((entry) => entry.gaps))].sort((a, b) => a.localeCompare(b)),
  } satisfies PersistedEvidenceExpansion;
  recordTaskContract(workspace, epochTokens, {
    query,
    requiredRoles: result.required_surfaces ?? [],
    // Evidence-expansion targets are dependency obligations, never query
    // concerns. recordEvidenceExpansion below owns their lifecycle.
    concernTokens: epochConcernTokensFor(result, query),
    servedRoles: served.map((surface) => surface.role),
    coveredConcernTokens: queryTokens.filter((token) => concernTokenMatchesSurfaces(token, served)),
  });
  // One-hop dependency proof is the evidence model for an actual
  // open-universe request.  Applying it to a finite generic list manufactures
  // a new dependency requirement after all of that list's named roles have
  // been served, making a cumulative same-epoch pack impossible to close.
  const expansionTargets = !hasOpenUniverseIntent(query) || isAdditiveEnumIntent(query)
    ? []
    : [...oneHop, ...(effectiveExpansion?.targets ?? [])];
  // -------------------------------------------------------------------------
  // R2 (2026-08-28): THE SERVED-DEFINITION WITNESS IS THE BODY, NOT THE LABEL.
  //
  // `servedDefinitions` was `served.map(surface => surface.symbol)` — the
  // per-surface NAMED-SELECTOR label. A whole-file serve carries no `symbol`,
  // so a pack that served `export function applyDiscount(…)` in full proved
  // NOTHING about `applyDiscount`: every expansion target stayed `open`, the
  // open-universe obligation could never be discharged by success, and the
  // `act.answer` it gated was suppressed over evidence the response actually
  // held (measured on the `ledgerDecisionIntegration` TS 4-definition fixture:
  // five open dependency obligations, zero resolved, five served bodies).
  //
  // The honest witness is a DECLARATION in a served body, which is exactly what
  // `extractSymbolsFromLines` — `mode=map`/`digest`'s own declaration scanner —
  // reports. Definition-grade by construction: it matches declaration forms,
  // never call sites, so a body that merely CALLS `applyDiscount` still proves
  // nothing. The old label witness is kept alongside it, since a symbol-scoped
  // serve is a definition serve by construction.
  // -------------------------------------------------------------------------
  const servedDefinitions = [...new Set([
    ...served.map((surface) => surface.symbol).filter((symbol): symbol is string => symbol !== undefined),
    ...served.flatMap((surface) => extractSymbolsFromLines(
      servedSurfaceText(surface),
      MAX_SERVED_DEFINITION_SYMBOLS,
      // The expansion targets are the preferred names, so a large body's
      // declaration scan runs to EOF for exactly the identifiers under proof
      // instead of stopping at the first `maxSymbols` declarations.
      new Set(expansionTargets.map((target) => target.toLowerCase())),
    ).map((entry) => entry.name)),
  ])];
  recordEvidenceExpansion(workspace, expansionTargets, servedDefinitions);
  const expansionGaps = hasOpenUniverseIntent(query)
    ? [...new Set(effectiveExpansion?.gaps ?? [])].sort((a, b) => a.localeCompare(b))
    : [];
  if (expansionGaps.length > 0) {
    recordExpansionExplicitGap(
      workspace,
      { kind: "dependency-definitions", target: "open-universe" },
      expansionTargets,
      // One immutable proof witnesses the whole extraction limitation. Calling
      // prove once per diagnostic string attempted to overwrite the same
      // parent/target proof on multi-gap packs.
      expansionGaps.join("; "),
    );
  }
  return expansionGaps.length > 0;
}
/**
 * R2: bound on declarations scanned out of ONE served body when proving which
 * expansion targets are resident. Generous next to `MAX_EVIDENCE_EXPANSION_TARGETS`
 * so a dependency file's own helpers cannot crowd out the target under proof,
 * and bounded so a large served body cannot make the recorder super-linear.
 */
const MAX_SERVED_DEFINITION_SYMBOLS = 64;

/** Cap on how many unserved caller paths are disclosed (they share the W2 guidance-metadata budget). */
const MAX_UNSERVED_CALLER_PATHS = 6;

/** Bound on the proof-preserving byte-budget fallback's own `missing` list (its cap check has no trim pass after it). */
const MAX_FALLBACK_MISSING_ENTRIES = 4;

/**
 * B1c (2026-08-01 retrieval-scope): a candidate-list pack's recovery must be
 * EXECUTABLE.
 *
 * buildRoute tells such a pack's caller "if required sites are missing from the
 * candidates, ONE task_pack with paths=[chosen + missing] is sanctioned" — as
 * PROSE inside route.reason, which the caller has to parse and transcribe into
 * a call. Live forensics (run 2026-07-31-semantic-signal5-2, T05c rep0): the
 * caller never did; it re-issued the same query-only pack and got another
 * candidate list. Ship the call itself.
 *
 * Only fires when the pack is genuinely still short of something the caller
 * NAMED — a candidate list whose bodies are all served and whose query named
 * nothing unserved keeps today's no-`next` close (choosing IS the next act; a
 * `next` there would re-serve served handles, the 2026-07-19a T13 thrash).
 */
export function attachCandidateListRecovery(
  result: TaskPackResult,
  query: string,
  workspace: string,
): void {
  if (!isCandidateListPack(result)) return;
  if (result.next !== undefined) return;
  const candidateSurfaces = codeTaskPackSurfaces(result.surfaces);
  const served = new Set(candidateSurfaces.map((s) => s.path));
  // R0 (2026-08-21, W4-A): same root-disambiguation as augmentQueryNamedFileSurfaces.
  const unresolved = queryNamedWorkspaceFiles(query, workspace, trustworthySurfaceAnchorPaths(candidateSurfaces))
    .filter((rel) => !served.has(rel));
  if (unresolved.length === 0) return;
  const paths = [...new Set([...unresolved, ...served])].slice(0, MAX_BATCH_NEXT_HANDLES);
  result.next = `read_file mode=task_pack query="${continuationQuery(query)}" paths=${JSON.stringify(paths)}`;
}

/**
 * B1c: the candidate paths of the most recent candidate-list pack per
 * workspace. Paired with the session's own `candidatePackPending` brake (which
 * records only that a choice is outstanding, not WHAT the choices were), it
 * lets a query-only re-pack bind the pending candidates as paths instead of
 * degrading into a second candidate list over the same ambiguity.
 */
const pendingCandidatePaths = new Map<string, { paths: string[]; tokens: string[] }>();

/** Test hook: drop the pending-candidate binding for every workspace. */
export function resetPendingCandidatePaths(): void {
  pendingCandidatePaths.clear();
}

// ---------------------------------------------------------------------------
// C2 (2026-08-09): a qref re-pack that only adds CONTEXT keeps its certificate.
//
// The documented qref contract is "keeps the certified working set" — the guide
// tells a paths-bearing retry to drop `query` and re-send `qref=… paths=[…]`
// precisely so the certified set survives. It did not. Measured on the T05c
// cell: after the (over-shrunk) first pack omitted CONTRACT.md, every rep
// re-packed with qref + paths:[CONTRACT.md] and got a pack that
//   - demoted execution_contract.typestate.phase prepared → discovery,
//   - flipped all three concerns covered → needs-followup, and
//   - prescribed blocking_next_steps ["find saturation","find contract",
//     "find failsafe"] — three more searches
// for a request that asked for one context document. One rep obeyed two of the
// three literally. Nothing had been falsified: the seeded re-pack simply
// rebuilt its concern coverage from the ONE file it was handed and reported the
// other seven as unresolved, forgetting that the caller still holds them.
//
// So: remember what a prepared pack certified, and when a qref replay adds only
// context material (doc-role, not edit-frontier scope), carry that certificate
// forward instead of re-deriving it from the re-pack's narrower surface set. A
// replay that adds genuine NEW CODE scope is a real re-scope and keeps today's
// behaviour — the working set changed, so the proof must be redone.
// ---------------------------------------------------------------------------

/** The proof a prepared pack shipped, retained per (workspace, lane) for one epoch. */
interface CertifiedWorkingSet {
  /**
   * F-V13-3 (2026-08-30): the lane the certificate was ISSUED in. The map key
   * already partitions by lane, so this is defence in depth — a certificate is
   * an authorization, and an authorization must be able to state who it was
   * issued to, independently of where it happens to be filed. The live incident
   * was a cross-lane clone of `contract` (see carryForwardCertifiedWorkingSet),
   * which reinstalls a peer lane's `execution_contract` wholesale: in one
   * direction that fenced lane `canon-plan`'s create behind lane
   * `canon-ledger`'s frontier, and in the other it would have AUTHORIZED an
   * edit on evidence the calling lane never held.
   */
  lane: string;
  epochTokens: string[];
  contract: TaskExecutionContract;
  concerns: TaskPackResult["concerns"];
  coverage: TaskPackResult["coverage"];
  coverageReason: TaskPackResult["coverage_reason"];
  coverageBasis: TaskPackResult["coverage_basis"];
  routeAction: NonNullable<TaskPackResult["route"]>["action"];
  /** Workspace-relative paths of the certified surfaces (normalized). */
  paths: string[];
  /** Content sha of each certified surface file — the staleness proof. */
  fileShas: Array<{ path: string; sha: string }>;
}

const certifiedWorkingSets = new Map<string, CertifiedWorkingSet>();

/** Test hook: drop every retained certificate. */
export function resetCertifiedWorkingSets(): void {
  certifiedWorkingSets.clear();
}

/** Remember a prepared pack's certificate so a context-only replay can reuse it. */
function rememberCertifiedWorkingSet(
  workspace: string,
  result: TaskPackResult,
  query: string,
): void {
  const contract = result.execution_contract;
  if (contract?.typestate?.phase !== "prepared") return;
  const surfaces = codeTaskPackSurfaces(result.surfaces);
  if (surfaces.length === 0) return;
  const fileShas: Array<{ path: string; sha: string }> = [];
  const seen = new Set<string>();
  for (const surface of surfaces) {
    if (seen.has(surface.path)) continue;
    seen.add(surface.path);
    const sha = surfaceFileSha(workspace, surface.path);
    if (sha !== undefined) fileShas.push({ path: surface.path, sha });
    if (fileShas.length >= MAX_FINGERPRINT_FILES) break;
  }
  certifiedWorkingSets.set(laneScopedKey(path.resolve(workspace)), {
    lane: currentSessionLane(),
    epochTokens: tokenizeForEpoch(query),
    contract: structuredClone(contract),
    concerns: result.concerns === undefined ? undefined : structuredClone(result.concerns),
    coverage: result.coverage,
    coverageReason: result.coverage_reason,
    coverageBasis: result.coverage_basis,
    routeAction: result.route?.action ?? "edit_from_handles",
    paths: [...seen].map(normalizedRequestPath),
    fileShas,
  });
}

/**
 * C2: carry a retained certificate onto a qref replay whose added paths are all
 * context material. Runs BEFORE applyCompleteStopRouteToResult so the restored
 * route flows through the normal stop-clause / doc-sliver-honesty pipeline and
 * the replay ends up shaped exactly like the first pack.
 */
function carryForwardCertifiedWorkingSet(
  workspace: string,
  result: TaskPackResult,
  args: TaskPackArgs | undefined,
  query: string,
): void {
  // Only a qref replay. A freshly-typed query is a new task epoch by
  // definition, and the caller is not claiming to still hold anything.
  if (args?.taskQueryRefReplay !== true) return;
  const requested = (args.paths ?? []).map((entry) =>
    normalizedRequestPath(typeof entry === "string" ? entry : entry.path));
  if (requested.length === 0) return;
  // Nothing to repair on a replay that certified itself.
  if (result.execution_contract?.typestate?.phase === "prepared") return;
  const prior = certifiedWorkingSets.get(laneScopedKey(path.resolve(workspace)));
  if (prior === undefined) return;
  // F-V13-3: a certificate rides forward only into the lane it was issued in.
  // Redundant with the lane-scoped key by construction — deliberately so: this
  // is the line that clones a whole `execution_contract` onto another response,
  // so it fails closed on its own evidence rather than trusting its filing.
  if (prior.lane !== currentSessionLane()) return;
  // Same task, and the certified files are still byte-identical: a certificate
  // is a claim about a working set, so it may only ride forward while that set
  // is provably the one it was issued over.
  if (!epochsOverlap(prior.epochTokens, tokenizeForEpoch(query))) return;
  if (prior.fileShas.length === 0) return;
  for (const { path: p, sha } of prior.fileShas) {
    if (surfaceFileSha(workspace, p) !== sha) return;
  }
  // Every path this replay ADDS must be context, not new edit scope. An added
  // path with no surface in the response is unproven, so it fails closed.
  const certified = new Set(prior.paths);
  const surfaces = codeTaskPackSurfaces(result.surfaces);
  for (const requestedPath of requested) {
    if (certified.has(requestedPath)) continue;
    const surface = surfaces.find((s) => normalizedRequestPath(s.path) === requestedPath);
    if (surface === undefined) return;
    if (surface.role !== "doc") return;
    if (surface.required === true) return;
  }

  result.coverage = prior.coverage;
  if (prior.coverageReason === undefined) delete result.coverage_reason;
  else result.coverage_reason = prior.coverageReason;
  if (prior.coverageBasis !== undefined) result.coverage_basis = prior.coverageBasis;
  if (result.concerns !== undefined && prior.concerns !== undefined) {
    const covered = new Set(
      prior.concerns.filter((concern) => concern.status === "covered").map((concern) => concern.id),
    );
    for (const concern of result.concerns) {
      if (covered.has(concern.id)) concern.status = "covered";
    }
  }
  // The three searches this pack was about to prescribe are for concerns the
  // caller already holds proof of.
  delete result.blocking_next_steps;
  if (result.checks !== undefined) {
    result.checks = result.checks.filter((check) => !check.startsWith("concern(s) not covered"));
    if (result.checks.length === 0) delete result.checks;
  }
  result.execution_contract = structuredClone(prior.contract);
  result.route = {
    action: prior.routeAction,
    reason: `certified working set carried forward (${prior.paths.length} handles, qref replay);`
      + ` this call adds context only`,
    max_additional_tl_calls: 0,
  };
}

/** Minimum share of the smaller token set two queries must share to be "the same request restated". */
const PENDING_CANDIDATE_TOKEN_OVERLAP = 0.6;

/**
 * B1c: is `query` a restatement of the request whose candidate list is still
 * pending? The session brake only records THAT a choice is outstanding, so
 * without this check an unrelated NEXT task issued while the brake is armed
 * would silently inherit the previous task's candidate paths as a hard scope.
 * Token-overlap gated (never a substring test) so a genuine re-ask binds and a
 * new subject does not.
 */
function isPendingCandidateRestatement(entry: { tokens: string[] }, query: string): boolean {
  const current = new Set(tokenizeForEpoch(query));
  const prior = new Set(entry.tokens);
  if (current.size === 0 || prior.size === 0) return false;
  let shared = 0;
  for (const token of current) if (prior.has(token)) shared++;
  return shared / Math.min(current.size, prior.size) >= PENDING_CANDIDATE_TOKEN_OVERLAP;
}

/**
 * B1b (2026-08-01 retrieval-scope): a caller-named path is a REQUIREMENT, so
 * one that exists, is readable, and is in scope must either be SERVED or be
 * named as unresolved. Live forensics (run 2026-07-31-semantic-signal5-2, T09
 * rep1/rep2): a pack given 12 explicit paths — including the enum file the task
 * was about — served ONE surface and reported nothing about the other eleven,
 * so the caller could not tell "already covered" from "silently dropped" and
 * re-asked for the same files.
 *
 * Placed at the shared exit rather than in buildSeededTaskPack: every seeded
 * read does become a surface there, and the narrowing happens LATER (a
 * specialized builder that selects a subset, the artifact_build explicit-target
 * splice, a whole-surface drop under the byte cap). Normally a no-op.
 *
 * Deliberately additive only: it never changes coverage or the route (those are
 * already reconciled by this point), it only refuses to stay silent.
 */
function reconcileCallerPathCoverage(
  result: TaskPackResult,
  args: TaskPackArgs | undefined,
  workspace: string,
): void {
  const entries = (args?.paths ?? []).map(normalizePathEntry);
  if (entries.length === 0) return;
  // Case-normalized on BOTH sides (2026-08-01): normalizedRequestPath
  // lowercases the request while served surface paths keep repo case — the
  // raw comparison marked genuinely-served camelCase files "named but
  // unserved" (observed live: leanExecutionContract.ts served 1-70 yet listed
  // missing).
  const servedPaths = new Set(result.surfaces.map((s) => normalizedRequestPath(s.path)));
  const servedEarlier = new Set((result.served_earlier ?? []).map((entry) => normalizedRequestPath(entry.path)));
  const unserved: string[] = [];
  for (const entry of entries) {
    const rel = normalizedRequestPath(entry.path);
    if (rel.length === 0) continue;
    if (servedPaths.has(rel) || servedEarlier.has(rel)) continue;
    // Already disclosed — as a bare path (unreadable/outside), as the
    // "<dir>/ (directory)" marker, or by this very pass on an earlier call.
    if (result.missing.some((item) => {
      const norm = item.toLowerCase();
      return norm === rel || norm.startsWith(`${rel} `) || norm.startsWith(`${rel}/`);
    })) continue;
    // A directory entry is honored by any surface UNDER it.
    if (result.surfaces.some((s) => isUnderAncestor(normalizedRequestPath(s.path), rel))) continue;
    const abs = safeResolve(rel, workspace);
    if (abs === undefined || !fs.existsSync(abs)) continue; // a nonexistent path is already reported by its own flow
    unserved.push(rel);
    if (unserved.length >= MAX_UNSERVED_CALLER_PATHS) break;
  }
  if (unserved.length === 0) return;
  for (const rel of unserved) result.missing.push(`${rel} (named but unserved)`);
}

/**
 * D1 (2026-08-07): does this pack disclose content it did NOT serve?
 *
 * `remaining_ranges` alone does not mean "unserved": a windowed surface
 * routinely discloses the rest of its FILE while a SIBLING surface on the same
 * path already carries those lines (an answer pack that serves a symbol plus
 * the whole file is the common shape). Granting a zoom budget for that is what
 * the 2026-08-01 serve-honesty split was defending against, and it was right
 * to — it just used `content_completeness==="partial"` as the proxy, which also
 * excluded the case where the remainder is genuinely absent (an anchor-focused
 * symbol served alone out of a large file).
 *
 * Decide it directly instead: subtract every served span on the same path from
 * each disclosed remainder. Anything left is content the caller cannot reach
 * without another call, which is exactly when one same-handle zoom is
 * sanctioned. A trimmed body (content_completeness "partial") always counts.
 */
function packHasUnservedDisclosure(result: TaskPackResult): boolean {
  const parseSpan = (text: string): [number, number] | undefined => {
    const match = /^(\d+)-(\d+)$/.exec(text);
    if (!match) return undefined;
    const start = Number(match[1]);
    const end = Number(match[2]);
    return Number.isInteger(start) && Number.isInteger(end) && end >= start ? [start, end] : undefined;
  };
  const surfaces = codeTaskPackSurfaces(result.surfaces);
  const servedByPath = new Map<string, Array<[number, number]>>();
  for (const surface of surfaces) {
    if (!hasServedCode(surface)) continue;
    const span = parseSpan(surface.range);
    if (!span) continue;
    const spans = servedByPath.get(surface.path) ?? [];
    spans.push(span);
    servedByPath.set(surface.path, spans);
  }
  for (const surface of surfaces) {
    if (surface.content_completeness === "partial") return true;
    const disclosed = surface.remaining_ranges ?? [];
    if (disclosed.length === 0) continue;
    const served = [...(servedByPath.get(surface.path) ?? [])].sort((a, b) => a[0] - b[0]);
    for (const item of disclosed) {
      const span = parseSpan(item);
      if (!span) continue;
      let cursor = span[0];
      for (const [from, to] of served) {
        if (from > cursor) break;
        if (to >= cursor) cursor = to + 1;
        if (cursor > span[1]) break;
      }
      if (cursor <= span[1]) return true;
    }
  }
  return false;
}

/**
 * iter-2 W2: stamp the complete-stop signal on a freshly-built pack whose
 * coverage is complete/focused (single-call OR cumulative). GUIDANCE ONLY — see
 * applyCompleteStopRoute; never a refusal, never raises the call budget.
 */
function applyCompleteStopRouteToResult(result: TaskPackResult): void {
  if (result.coverage !== "complete" && result.coverage !== "focused") return;
  const isAnswer = result.route?.action === "answer_from_handles" || result.task_profile === "answer";
  // A5 (2026-08-01 signal5-2 T03): answer packs disclose partial primaries via
  // content_completeness/remaining_ranges (the primary is typically
  // required:false by design, so no required-filter here).
  //
  // D1 (2026-08-07): this predicate used to accept ONLY
  // content_completeness==="partial" — the "label-gated" narrowing added on
  // 2026-08-01 because "granting it a call budget re-opened every certified
  // answer pack". That re-opening was not caused by the grant: it was
  // routeCandidate (buildTaskExecutionContract) refusing `prepared` to any
  // route carrying a nonzero budget, so ANY zoom grant destroyed the
  // certificate. That collision is now fixed at its own site
  // (servedZoomAffordance), so the narrowing is no longer load-bearing — and
  // it was actively harmful: buildRoute's zoomNote counts the WIDER signal
  // (content_completeness partial OR remaining_ranges present), so an
  // anchor-focused primary — a whole symbol body served with the rest of the
  // FILE in remaining_ranges — got the zoom PROSE with a zero budget. The
  // ROUTE_REASON_ENVELOPE_CAP trim then cut the trailing "working set
  // complete — stop discovery" clause that justified the 0, leaving a route
  // that says "re-slice to zoom" and grants no call. Observed live
  // 2026-08-07: an operator-usage query anchor-focused on a matched symbol,
  // the answer sat in the file's leading doc block inside remaining_ranges,
  // and the caller had to spend an unsanctioned call to reach it.
  //
  // Decide on UNSERVED content instead of on the label — see
  // packHasUnservedDisclosure. That keeps the 2026-08-01 defence intact (a
  // file-complement disclosure whose lines a sibling surface already carries
  // still gets budget 0) while granting the AGENTS.md zoom ("prepared+partial
  // primary grants route.max_additional_tl_calls=1 — spend it on the served
  // zoom") when the remainder genuinely is not in the caller's hands.
  const partialPrimary = isAnswer && packHasUnservedDisclosure(result);
  const route = applyCompleteStopRoute(result.route, result.coverage, isAnswer, partialPrimary);
  if (route !== undefined) result.route = route;
}

/**
 * L1 (2026-08-08 doc-authority serve honesty) — full defect writeup in
 * features/task-pack/docSliver.ts.
 *
 * A pack may legitimately have a COMPLETE code working set while one of its
 * surfaces is a named authority document served at 0.066% of its own length
 * (anchor-focus matching a literal query token at its LAST occurrence). The
 * pack then asserted `coverage:"complete"` + `prepared` + budget 0 + "working
 * set complete — stop discovery" over that document, and every observed solver
 * disbelieved it and went hunting (10-20 discovery calls).
 *
 * This pass changes three specific signals and nothing else:
 *   (a) the sliver surface gains the heading index `mode=slice` ALREADY emits
 *       for the same handle (same field names) plus one executable
 *       section-scoped `next_call`;
 *   (b) `content_completeness:"partial"` — the existing per-surface vocabulary
 *       — marks the surface itself, while pack-level `coverage` keeps whatever
 *       the code working set earned;
 *   (c) the route's stop clause is replaced by an honest one and the budget is
 *       raised to >=1 so the zoom is sanctioned rather than stolen.
 *
 * `prepared` and the certificate are deliberately untouched: first_pack_ready
 * is a proven win, and wholesale demotion would re-open the discovery-loop
 * class. Runs AFTER applyEnvelopeWeightTrim so the reason cap cannot shear the
 * honesty clause off (the same ordering annotateEditZoomAffordance relies on).
 */
export function applyDocSliverHonesty(
  workspace: string,
  result: TaskPackResult,
  query: string,
): DocSliverFloorRestore[] {
  if (result.coverage !== "complete" && result.coverage !== "focused") return [];
  const route = result.route;
  if (route === undefined) return [];
  if (route.action !== "edit_from_handles" && route.action !== "answer_from_handles") return [];
  // Idempotent: this pass can run twice over the same result (a cached re-serve
  // replays the stored route). Without this guard the honesty clause is
  // appended a second time, doubling the reason and burning envelope on a
  // duplicate sentence.
  const routeReason = route.reason ?? "";
  if (routeReason.includes(DOC_SLIVER_ROUTE_MARKER)) return [];
  // A compact `pack_unchanged` re-serve is a sub-1KB acknowledgement by
  // design; hanging an index off it would defeat the whole receipt.
  if (result.pack_unchanged === true) return [];
  const cache = new FileReadCache();
  const planned: Array<{ surface: TaskPackSurface; plan: DocSliverPlan }> = [];
  for (const surface of codeTaskPackSurfaces(result.surfaces)) {
    if (surface.role !== "doc") continue;
    // C2 (2026-08-09): a `code_unchanged` surface is NOT a compact receipt —
    // it is a full pack whose body was deduped because the caller already holds
    // those bytes. Measured on the qref recovery: skipping it let "working set
    // complete — stop discovery" ride a pack whose authority doc was still
    // 1/1514 lines served, resurrecting on the replay exactly the false closure
    // L1 exists to retire. What the correction describes is the FILE, not
    // whether these particular bytes were re-sent.
    if (surface.code === undefined && surface.code_unchanged === undefined) continue;
    const content = cache.read(workspace, surface.path);
    if (content === undefined) continue;
    const plan = planDocSliver(surface, content, query);
    if (plan === undefined) continue;
    planned.push({ surface, plan });
  }
  const first = planned[0];
  if (first === undefined) return [];

  // ---- C1 (2026-08-09): size the affordance to the budget, don't shear it ----
  //
  // L1 attached the whole affordance unconditionally and its follow-up shrank
  // whatever did not fit. Measured on the real T05c pack: a 1,465B overage sent
  // the ladder through every rung, removing 3,158B — the CONTRACT.md surface
  // itself included — and landed 1,693B UNDER the ceiling with the false "stop
  // discovery" restored. Strictly worse than pre-L1, which at least served the
  // 83B sliver and its handle.
  //
  // Build UP from the floor instead. Each level is attached only once the pack
  // has been measured with the level below it in place, so the affordance is
  // sized to the headroom that actually exists rather than trimmed after the
  // fact by a rank-blind pop:
  //
  //   floor       the sliver body + handle + remaining_ranges (untouched here)
  //               + the three cheap corrections below — never given up
  //   + clause    the route honesty that retires "working set complete"
  //   + zoom      the one sanctioned, section-scoped, executable next_call
  //   + index     as many heading entries as fit, in planDocSliver's RANK order
  //
  // The floor lands first: `partial`, `required:false` and `total_lines` are
  // true at any budget and cost a few dozen bytes. They are still BYTES, so the
  // pre-flag values are captured and handed to the backstop — a pack that was
  // already over the ceiling before this pass ran must be able to give them
  // back rather than pay for them with the doc surface itself.
  const floorRestore: DocSliverFloorRestore[] = planned.map(({ surface }) => ({
    surface,
    content_completeness: surface.content_completeness,
    required: surface.required,
    total_lines: surface.total_lines,
  }));
  for (const { surface, plan } of planned) attachDocSliverSurfaceHonesty(surface, plan);

  // Read the cap only now — `required:false` moves a doc surface out of
  // capForResult's required-code tier test, so the floor can change the cap.
  const cap = capForResult(result);
  const size = (): number => Buffer.byteLength(JSON.stringify(result), "utf8");

  const clause = docSliverRouteClause(first.surface, first.plan);
  const reason = routeReason.includes(COMPLETE_STOP_REASON)
    ? routeReason.replace(COMPLETE_STOP_REASON, clause)
    : routeReason.includes(COMPLETE_STOP_REASON_ANSWER)
      ? routeReason.replace(COMPLETE_STOP_REASON_ANSWER, clause)
      : routeReason.length > 0 ? `${routeReason}; ${clause}` : clause;
  result.route = {
    ...route,
    reason,
    max_additional_tl_calls: Math.max(route.max_additional_tl_calls ?? 0, 1),
  };
  if (size() > cap) reclaimForDocSliver(result, cap, 0);
  if (size() > cap) {
    // Not even the honesty clause fits. Revert to the untouched route: the
    // stop text may only come back when NO affordance level is present, which
    // is exactly this state (floor only).
    result.route = route;
    return floorRestore;
  }

  for (const { surface, plan } of planned) {
    attachDocSliverZoom(surface, plan);
    if (size() > cap) reclaimForDocSliver(result, cap, 0);
    if (size() > cap) {
      surface.next_call = undefined;
      return floorRestore;
    }
  }

  for (const { surface, plan } of planned) {
    reclaimForDocSliver(result, cap, DOC_SLIVER_NAV_OVERHEAD_BYTES + DOC_SLIVER_INDEX_TARGET_BYTES);
    const headroom = cap - size() - DOC_SLIVER_NAV_OVERHEAD_BYTES;
    if (headroom < DOC_SLIVER_MIN_INDEX_BYTES) return floorRestore;
    const index = buildDocSliverHeadingIndex(plan, Math.min(DOC_SLIVER_HEADINGS_CAP_BYTES, headroom));
    if (index.headings.length === 0) return floorRestore;
    attachDocSliverNavigation(surface, index);
    // DOC_SLIVER_NAV_OVERHEAD_BYTES is an estimate of the field names and the
    // truncation note; this makes the result exact, popping by rank (lowest
    // first) so the focus entry is the last one standing.
    while (size() > cap && (surface.headings?.length ?? 0) > 0) {
      dropLowestRankedHeadingIndexEntry(surface.headings!, plan.focus);
      if (surface.headings!.length === 0) clearDocSliverNavigation(surface);
    }
    if (surface.headings === undefined) return floorRestore;
  }
  return floorRestore;
}

/**
 * C1: what a doc-sliver surface looked like BEFORE this pass stamped its floor
 * honesty on it. Handed to the backstop so an overage the affordance did not
 * cause is paid for by giving the flags back, not by deleting the surface.
 */
export interface DocSliverFloorRestore {
  surface: TaskPackSurface;
  content_completeness: TaskPackSurface["content_completeness"];
  required: TaskPackSurface["required"];
  total_lines: TaskPackSurface["total_lines"];
}

/**
 * C1: bytes the navigation level costs BESIDE its entries — the `headings` /
 * `sections_hint` / `headings_truncated` / `headings_total` / `headings_note`
 * field names, the sections_hint text itself (~120B) and the truncation note
 * (~110B). Deliberately generous: an over-estimate only costs a heading entry
 * or two, while an under-estimate would push the pack over the ceiling (the
 * exact pop loop above corrects either way).
 */
const DOC_SLIVER_NAV_OVERHEAD_BYTES = 420;

/** C1: below this, an index cannot hold even one entry worth naming. */
const DOC_SLIVER_MIN_INDEX_BYTES = 48;

/**
 * C1: index bytes worth reclaiming advisory metadata for. ~8 entries — the
 * focus section plus its siblings, which is what makes the doc navigable. Not a
 * cap (a pack with free headroom still gets up to DOC_SLIVER_HEADINGS_CAP_BYTES);
 * only the point past which the affordance stops outbidding the pack's own
 * advisory prose.
 */
const DOC_SLIVER_INDEX_TARGET_BYTES = 512;

/**
 * C1: fund the affordance from the pack's own ADVISORY metadata, in the exact
 * order trimToCap already sheds it — never from a code body, a handle, the
 * certificate, or any coverage claim.
 *
 * Why any reclaim at all: measured on the real T05c cell, the pack arrives here
 * 36B OVER its 32768 ceiling before the affordance is even considered (the
 * uncapped route/zoom/runtime prose added after the last fitsInCap gate), so
 * "size the affordance to the remaining headroom" would honestly resolve to
 * "there is none" and the 1,514-line authority doc would go on being dropped
 * outright. The pack's composition says where the bytes are: 26.6KB of code
 * bodies and 1.8KB of certificate — both untouchable — beside 1,942B of
 * per-surface `likely_edits` / `edit_intent` / `done_check`, advisory
 * speculation that trimToCap Phases C and D already shed BEFORE any code body.
 * Trading a restatement of the edit vocabulary for the one call that reaches
 * the authority the task cites is the same trade trimToCap makes, made earlier
 * and only when a doc-sliver affordance is actually being attached.
 *
 * Sheds the minimum: stops the moment `want` bytes of headroom exist.
 */
function reclaimForDocSliver(result: TaskPackResult, cap: number, want: number): void {
  const headroom = (): number => cap - Buffer.byteLength(JSON.stringify(result), "utf8");
  if (headroom() >= want) return;
  const surfaces = codeTaskPackSurfaces(result.surfaces);
  for (const surface of surfaces) {
    if (surface.likely_edits === undefined) continue;
    surface.likely_edits = undefined;
    if (headroom() >= want) return;
  }
  for (const surface of surfaces) {
    if (surface.edit_intent !== undefined) {
      surface.edit_intent = undefined;
      if (headroom() >= want) return;
    }
    if (surface.done_check !== undefined) {
      surface.done_check = undefined;
      if (headroom() >= want) return;
    }
  }
}

/**
 * L1 follow-up (2026-08-08, T05c smoke-gate escape) — degrade the heading-index
 * affordance applyDocSliverHonesty just attached when it pushed an
 * already-near-ceiling pack over its transport cap. Runs only when the pack is
 * over cap; a comfortably-under-budget pack keeps the full index untouched.
 *
 * C1 (2026-08-09) rewrote it as a BACKSTOP. applyDocSliverHonesty now sizes the
 * affordance to the pack's real headroom at construction, so this only ever
 * absorbs estimation drift from the passes that run after it. It unwinds the
 * exact levels that pass built up, in reverse, and stops at the FIRST size that
 * fits (the old ladder measured once per rung and ran on past a fitting state):
 *
 *   1  heading entries, popped LOWEST-RANK-FIRST (the focus section is last out)
 *   2  the navigation level entirely (index + sections_hint)
 *   3  the sanctioned zoom (next_call)
 *   4  the route honesty clause — only once no affordance level remains, since
 *      restoring it resurrects "working set complete — stop discovery"
 *   5  advisory pack prose (create_note / verify / checks), because past rung 4
 *      the overage is demonstrably not this pass's doing
 *   6  the doc surface itself — forbidden while the floor fits, reachable only
 *      when the pack cannot transport even the pre-L1 sliver + handle
 */
function shrinkDocSliverAffordanceToFit(
  result: TaskPackResult,
  routeBeforeDocSliverHonesty: TaskPackResult["route"],
  floorRestore: readonly DocSliverFloorRestore[],
): void {
  const sliverSurfaces = codeTaskPackSurfaces(result.surfaces).filter(
    (surface) =>
      surface.headings !== undefined
      || surface.next_call !== undefined
      || surface.sections_hint !== undefined,
  );

  // Rung 1 — heading entries, popped by RANK and re-measured after EVERY pop.
  // The old ladder measured once per rung and used headings.pop(), which drops
  // the LAST-IN-DOCUMENT entry: on CONTRACT.md that killed "7.6
  // `<control/mixer.hpp>`" (line 1018 of 1514) before the §1-§3 layout entries
  // nobody asked for, and it kept popping past the first size that fit.
  for (const surface of sliverSurfaces) {
    const focus = docSliverFocusFromSurface(surface);
    while (!fitsInCap(result) && (surface.headings?.length ?? 0) > 0) {
      dropLowestRankedHeadingIndexEntry(surface.headings!, focus);
      // An index of zero entries plus an instruction for slicing by heading
      // name is half an affordance — the two ride together.
      if (surface.headings!.length === 0) clearDocSliverNavigation(surface);
    }
    if (fitsInCap(result)) return;
  }

  // Rung 2 — the navigation level entirely (index + sections_hint).
  for (const surface of sliverSurfaces) {
    clearDocSliverNavigation(surface);
    if (fitsInCap(result)) return;
  }

  // Rung 3 — the sanctioned zoom. The surface's own honesty corrections
  // (content_completeness:"partial", required:false, total_lines) are cheap and
  // stay; they are true regardless of budget.
  for (const surface of sliverSurfaces) {
    surface.next_call = undefined;
    if (fitsInCap(result)) return;
  }

  // Rung 4 — the route's own doc-sliver clause, and ONLY now that every other
  // affordance level is gone: restoring it brings back "working set complete —
  // stop discovery", which may never ship beside a live affordance. Restores
  // the route exactly as it stood right before applyDocSliverHonesty touched
  // it — undoing this pass's contribution, not route text assembled earlier.
  if (routeBeforeDocSliverHonesty !== undefined) {
    result.route = routeBeforeDocSliverHonesty;
  }
  if (fitsInCap(result)) return;

  // Rung 5 — the pack is over the ceiling with NO affordance attached at all,
  // so the overage was never this pass's to cause: it is the pre-existing drift
  // from the uncapped route/verify/runtime prose added after the last
  // fitsInCap gate. Shed that prose in trimToCap's own priority order (advisory
  // speculation first) rather than charging it to the doc surface.
  if (result.create_note !== undefined) {
    result.create_note = undefined;
    if (fitsInCap(result)) return;
  }
  if (result.verify !== undefined) {
    result.verify = undefined;
    if (fitsInCap(result)) return;
  }
  if (result.checks !== undefined) {
    result.checks = undefined;
    if (fitsInCap(result)) return;
  }

  // Rung 6 — the floor honesty flags themselves. They are TRUE, and they are
  // still ~70B: a pack that entered this pass already over the ceiling must be
  // able to give them back, because the alternative below deletes a 1,514-line
  // authority doc's only mention in the pack. Restores the exact pre-pass
  // values rather than blanking the fields, so a surface that already carried
  // content_completeness keeps it.
  for (const entry of floorRestore) {
    entry.surface.content_completeness = entry.content_completeness;
    entry.surface.required = entry.required;
    entry.surface.total_lines = entry.total_lines;
    if (fitsInCap(result)) return;
  }

  // Rung 7 — absolute last resort, reached only when the FLOOR itself does not
  // fit. Dropping the doc surface is forbidden while the floor fits (that is
  // the whole C1 defect: a 1,465B overage cost a 1,514-line authority doc its
  // only mention in the pack, and every rep paid a re-pack to get it back), but
  // a pack that cannot transport its floor has nothing left to give: every
  // doc-sliver surface is required:false by construction, so this costs no edit
  // evidence and does not touch missing/coverage.
  const handles = new Set([
    ...sliverSurfaces.map((surface) => surface.handle),
    ...floorRestore.map((entry) => entry.surface.handle),
  ]);
  if (handles.size > 0) {
    result.surfaces = result.surfaces.filter((surface) => !handles.has(surface.handle));
  }
}

/** iter-2 W5: extensions whose file is a runnable module (a functional check can execute it). */
const RUNNABLE_TARGET_EXTS = new Set([
  ".ts", ".tsx", ".mts", ".cts", ".js", ".jsx", ".mjs", ".cjs",
  ".py", ".go", ".rs", ".java", ".rb", ".php", ".cs", ".kt", ".swift",
  ".c", ".cc", ".cpp", ".cxx", ".scala", ".ex", ".exs",
]);

/** iter-2 W5: the runnable create/edit target this artifact-sourced pack produces, if any. */
function runnableArtifactTarget(result: TaskPackResult): string | undefined {
  if ((result.artifact_requirements?.length ?? 0) === 0) return undefined;
  const runnable = (p: string): boolean => RUNNABLE_TARGET_EXTS.has(path.posix.extname(p).toLowerCase());
  if (result.create_target !== undefined && runnable(result.create_target.path)) {
    return result.create_target.path;
  }
  // Edit frontier on a runnable module: a required, code-bearing surface that is
  // not itself one of the artifact source files.
  const sources = new Set((result.artifact_requirements ?? []).map(normalizedRequestPath));
  const editTarget = codeTaskPackSurfaces(result.surfaces).find((s) =>
    s.required !== false
    && hasServedCode(s)
    && runnable(s.path)
    && !sources.has(normalizedRequestPath(s.path))
  );
  return editTarget?.path;
}

/**
 * iter-2 W5 / iter-3 F5b: honest, generic wording — no sheet/filename specifics.
 * Softened to name the CHEAPEST sufficient proof so the obligation cannot be read
 * as license for an extended validation campaign (the T10-class tail): ONE smoke
 * run of the produced module against the served values, OR a diff review of the
 * produced values (the existing diff discharge). A passing type-check/compile
 * alone is still not validation.
 */
const FUNCTIONAL_VALIDATION_NOTE =
  "validate the produced module the cheap way before closing — ONE smoke run against the served artifact values, "
  + "OR a diff review of the produced values (a passing type-check/compile alone is not validation); "
  + "no extended validation campaign is expected";

/**
 * iter-2 W5: when an artifact-sourced pack targets a runnable create/edit, add
 * ONE named functional-validation obligation to the pack's verify output and
 * record it in the session so a self-check consult can list it OPEN until a
 * verification-evidence event discharges it. Honest, not a refusal. Detection is
 * purely structural (artifact_requirements present + runnable target extension);
 * no title/filename matching of specific sheets.
 */
function attachFunctionalValidationObligation(
  workspace: string,
  result: TaskPackResult,
): void {
  if (result.route?.action === "answer_from_handles") return;
  const target = runnableArtifactTarget(result);
  if (target === undefined) return;
  // Recorded under THIS task epoch (packServeLog resets it on a flip), so a
  // self-check consult can list it OPEN until a verification-evidence event —
  // done unconditionally (state, not bytes), even if the verify line below is
  // shed for budget.
  recordFunctionalValidationObligation(workspace, { note: FUNCTIONAL_VALIDATION_NOTE, targetPath: target });
  const verify = result.verify ?? [];
  if (verify.some((v) => v.startsWith(FUNCTIONAL_VALIDATION_NOTE))) return;
  // Added AFTER trimToCap, so commit it speculatively and keep it only when it
  // still fits (mirrors annotateSpannedRoots) — never reopen a budget trim just
  // closed.
  const line = `${FUNCTIONAL_VALIDATION_NOTE} [target ${target}]`;
  if (fitsInCap({ ...result, verify: [...verify, line] })) {
    result.verify = [...verify, line];
  }
}

// ---------------------------------------------------------------------------
// iter-3 F5: runtime hint (python interpreter + module presence, fs-stat only)
// ---------------------------------------------------------------------------

/** iter-3 F5: Office source extension → the python site-packages module dir an artifact of that kind needs. */
const PY_ARTIFACT_MODULE: Record<string, string> = {
  ".xlsx": "openpyxl",
  ".xlsm": "openpyxl",
  ".xltx": "openpyxl",
  ".docx": "docx", // python-docx installs as the 'docx' package
  ".pptx": "pptx", // python-pptx installs as the 'pptx' package
  // NOTE: no ".csv"/".tsv" entry on purpose — Python's `csv` module is in the
  // stdlib, so a csv-sourced build needs no site-packages module and gets no
  // runtime hint (attachRuntimeHint returns early when moduleDir is undefined).
};

/** iter-3 F5: overall byte ceiling for the whole runtime hint (kept well under the pack budget). */
const RUNTIME_HINT_MAX_BYTES = 500;

/**
 * iter-3 F5: python3* interpreters discovered by filesystem stat only — PATH
 * entries plus common Homebrew/System locations. NEVER executed (no child
 * process); a stat that resolves to a file (symlinks are followed) counts. This
 * is a hint source, not a guarantee the interpreter runs.
 */
export function discoverPythonInterpreters(): string[] {
  const found: string[] = [];
  const seen = new Set<string>();
  const consider = (candidate: string): void => {
    if (seen.has(candidate)) return;
    seen.add(candidate);
    try {
      if (fs.statSync(candidate).isFile()) found.push(candidate); // statSync follows symlinks
    } catch { /* absent / broken symlink — skip */ }
  };
  const names = ["python3", "python3.13", "python3.12", "python3.11", "python3.10", "python"];
  for (const dir of (process.env.PATH ?? "").split(path.delimiter)) {
    if (dir.length === 0) continue;
    for (const name of names) consider(path.join(dir, name));
  }
  for (const common of [
    "/opt/homebrew/bin/python3", "/usr/local/bin/python3", "/usr/bin/python3",
    "/opt/homebrew/bin/python3.13", "/opt/homebrew/bin/python3.12", "/opt/homebrew/bin/python3.11",
    "/Library/Frameworks/Python.framework/Versions/Current/bin/python3",
  ]) consider(common);
  return found;
}

/**
 * iter-3 F5: does `binPath`'s install tree appear to contain `moduleDir` in its
 * site-packages? fs.stat only (no import, no exec): from the interpreter prefix
 * (dirname twice), scan each `<prefix>/lib/python3.<n>/site-packages/<moduleDir>`.
 * A true result means the package DIRECTORY exists on disk — a hint that the
 * import will resolve, not a proof (a broken/partial install still stats).
 */
function pythonHasModule(binPath: string, moduleDir: string): boolean {
  const prefix = path.dirname(path.dirname(binPath));
  const libDir = path.join(prefix, "lib");
  let libEntries: string[];
  try {
    libEntries = fs.readdirSync(libDir);
  } catch {
    return false;
  }
  for (const entry of libEntries) {
    if (!/^python3(\.\d+)?$/.test(entry)) continue;
    const modPath = path.join(libDir, entry, "site-packages", moduleDir);
    try {
      if (fs.statSync(modPath).isDirectory()) return true;
    } catch { /* not here — keep scanning versions */ }
  }
  return false;
}

/**
 * iter-3 F5: when an artifact-sourced pack's sources are Office files AND its
 * runnable create/edit target is a Python module, attach ONE compact `runtime`
 * hint — discovered python3* interpreters and whether each appears to carry the
 * module the artifact kind needs (xlsx→openpyxl, docx→python-docx). Purely a
 * hint (fs presence, not import-tested); bounded to RUNTIME_HINT_MAX_BYTES and
 * skipped silently on a non-Python target, non-Office sources, or when no
 * interpreter is found. Attached speculatively and kept only if it still fits the
 * pack cap (mirrors attachFunctionalValidationObligation / annotateSpannedRoots).
 */
export function attachRuntimeHint(result: TaskPackResult): void {
  if (result.route?.action === "answer_from_handles") return;
  const target = runnableArtifactTarget(result);
  if (target === undefined) return;
  if (path.posix.extname(target).toLowerCase() !== ".py") return; // Python targets only
  let moduleDir: string | undefined;
  for (const src of result.artifact_requirements ?? []) {
    const mod = PY_ARTIFACT_MODULE[path.posix.extname(normalizedRequestPath(src)).toLowerCase()];
    if (mod !== undefined) { moduleDir = mod; break; }
  }
  if (moduleDir === undefined) return; // sources aren't a known Office kind
  const bins = discoverPythonInterpreters();
  if (bins.length === 0) return; // nothing found → skip silently
  // Interpreters carrying the module first, so byte-trimming keeps the useful ones.
  const interpreters = bins
    .map((p) => ({ path: p, has_module: pythonHasModule(p, moduleDir!) }))
    .sort((a, b) => Number(b.has_module) - Number(a.has_module));
  const note =
    `hint only (fs presence, NOT import-tested): a python3 whose site-packages has "${moduleDir}" can build ${target}`;
  const build = () => ({ kind: "python" as const, note, module: moduleDir!, interpreters });
  while (interpreters.length > 1
    && Buffer.byteLength(JSON.stringify({ runtime: build() }), "utf8") > RUNTIME_HINT_MAX_BYTES) {
    interpreters.pop();
  }
  const hint = build();
  if (Buffer.byteLength(JSON.stringify({ runtime: hint }), "utf8") > RUNTIME_HINT_MAX_BYTES) return;
  if (fitsInCap({ ...result, runtime: hint })) {
    result.runtime = hint;
  }
}

// ---------------------------------------------------------------------------
// iter-3 F4: envelope weight — trim orientation-only metadata on packs 2+ and
// on any prepared/complete pack, where the caller already holds the code body.
// ---------------------------------------------------------------------------

/**
 * iter-3 F4: is this the FIRST pack of the active epoch (no prior overlapping-
 * epoch record captured yet)? The first exploratory pack keeps today's full
 * envelope for orientation value; later packs shed it (the caller already has
 * the map). Read BEFORE captureServedPack runs, so it reflects prior calls only.
 */
function isFirstEpochPack(workspace: string, epochTokens: readonly string[]): boolean {
  const records = servedPacksByWorkspace.get(laneScopedKey(workspace));
  if (records === undefined || records.length === 0) return true;
  if (epochTokens.length === 0) return records.length === 0;
  return !records.some((r) => epochsOverlap(r.epochTokens, epochTokens));
}

/**
 * iter-3 F4: shed orientation-only envelope weight.
 *
 * Two tiers, because the pieces differ in whether they carry FIRST-TIME
 * information a caller still needs:
 *  (a) session-once orientation that a caller who has seen this working set no
 *      longer needs — the top-level `tree` navigation inventory, per-surface
 *      `outline` and `why` on FULL-code surfaces (a partial/code-less surface
 *      keeps its outline — it drives slice zooming over the unfetched span).
 *      Dropped only on a REPEAT pack (packs 2+) of the epoch: the first pack —
 *      even a complete/focused one — is the caller's first orientation to this
 *      set, so its per-surface provenance (anchor-focus `why`, doc heading
 *      `outline`) is first-time information and is preserved.
 *  (b) redundancy independent of position — `likely_edits` on a read/answer
 *      profile (nothing to edit), and an overlong route.reason — dropped on any
 *      pack that is NOT the first exploratory pack OR is prepared/complete.
 *
 * The first exploratory pack (partial, not prepared/complete) is returned
 * untouched. Returns the bytes saved (for measurement/tests). Mutates in place.
 */
/**
 * Serve-honesty (2026-08-01): an EDIT-terminal route names the zoom affordance
 * whenever required surfaces disclose remaining_ranges. Runs AFTER
 * applyEnvelopeWeightTrim (same discipline as annotateSpannedRoots' span
 * remedy): the reason cap cuts on the last clause boundary, which is exactly
 * where this suffix would sit, and it is load-bearing honesty — the
 * multi_concern builder mints its route directly (never through buildRoute's
 * Wave-4 zoomNote), so without this pass a windowed serve read as a silent
 * "complete" and the caller's only visible continuation was native IO (T05c
 * rep2 desertion trigger).
 */
function annotateEditZoomAffordance(result: TaskPackResult): void {
  const route = result.route;
  if (route === undefined || route.action !== "edit_from_handles") return;
  if (route.reason.includes("remaining_ranges")) return;
  const partialDisclosed = codeTaskPackSurfaces(result.surfaces).filter((s) =>
    s.required !== false
    && (s.content_completeness === "partial" || (s.remaining_ranges?.length ?? 0) > 0)).length;
  if (partialDisclosed === 0) return;
  result.route = {
    ...route,
    reason: `${route.reason}; ${partialDisclosed} surface(s) have remaining_ranges (re-slice to zoom, or ONE read_file mode=full of that handle when most of the file is needed)`,
  };
}

export function applyEnvelopeWeightTrim(
  result: TaskPackResult,
  isFirstPack: boolean,
  isChangeProfile: boolean,
): number {
  const phase = result.execution_contract?.typestate?.phase;
  const preparedOrComplete =
    phase === "prepared" || result.coverage === "complete" || result.coverage === "focused";
  if (isFirstPack && !preparedOrComplete) return 0;
  const before = Buffer.byteLength(JSON.stringify(result), "utf8");
  // (b) position-independent redundancy.
  if (!isChangeProfile) {
    for (const surf of codeTaskPackSurfaces(result.surfaces)) delete surf.likely_edits;
  }
  if (result.route !== undefined && result.route.reason.length > ROUTE_REASON_ENVELOPE_CAP) {
    // Cut on a clause boundary, not mid-word. route.reason is assembled as
    // "<base>; <decorator>; <decorator>", so a hard slice left readers and
    // telemetry with fragments like "frontier_index is the complete" — a clause
    // that reverses its own meaning once the rest is gone. Fall back to the hard
    // cut only when there is no boundary to land on.
    const capped = result.route.reason.slice(0, ROUTE_REASON_ENVELOPE_CAP);
    const lastBoundary = capped.lastIndexOf("; ");
    result.route.reason = lastBoundary > 0 ? capped.slice(0, lastBoundary) : capped;
  }
  // (a) orientation the caller already holds — REPEAT packs only.
  if (!isFirstPack) {
    delete result.tree;
    for (const surf of codeTaskPackSurfaces(result.surfaces)) {
      const carriesFullCode = surf.code !== undefined && surf.content_completeness !== "partial";
      if (carriesFullCode) {
        delete surf.why;
        delete surf.outline;
      }
    }
  }
  return before - Buffer.byteLength(JSON.stringify(result), "utf8");
}

/** iter-2 W4: sorted, de-duped input paths a pack "already had" — its surfaces plus any caller-supplied paths. */
function collectPackInputPaths(result: TaskPackResult, args: TaskPackArgs | undefined): string[] {
  const set = new Set<string>();
  for (const s of result.surfaces) if (typeof s.path === "string" && s.path.length > 0) set.add(normalizedRequestPath(s.path));
  if (typeof args?.path === "string" && args.path.length > 0) set.add(normalizedRequestPath(args.path));
  for (const entry of args?.paths ?? []) {
    const p = normalizePathEntry(entry).path;
    if (typeof p === "string" && p.length > 0) set.add(normalizedRequestPath(p));
  }
  return [...set].sort();
}

/** iter-2 W4: content fingerprint of the referenced files — a change clears the awaiting-input latch. */
function packInputFingerprint(workspace: string, inputPaths: readonly string[]): string {
  const parts: string[] = [];
  for (const p of inputPaths.slice(0, MAX_FINGERPRINT_FILES)) {
    parts.push(`${p}:${surfaceFileSha(workspace, p) ?? "missing"}`);
  }
  return shaOfText(parts.join("\u0000"));
}

/**
 * iter-2 W4: a genuine unresolved-PROOF wait (the D4 artifact-input escape), NOT
 * a human candidate/concern choice — those have their own resolution machinery
 * (the caller picks a served candidate) and must not be latched.
 */
function isGenuineProofWait(result: TaskPackResult, contract: TaskExecutionContract): boolean {
  if (contract.next_action !== "request-user-input") return false;
  if (result.route?.action === "confirm_candidates") return false;
  if (result.coverage_reason === "candidate-list") return false;
  return (result.artifact_requirements?.length ?? 0) > 0
    || /unresolved proof/i.test(contract.reason);
}

/**
 * iter-2 W4: awaiting-input idempotency. Consulted at the finalizer AFTER the
 * contract is settled.
 *   - awaiting-input (genuine proof) → record the latch so a later re-ask cannot
 *     silently escape it; serve the pack as-is (return undefined).
 *   - prepared, latch still holds (same epoch, no new inputs, files unchanged) →
 *     DOWNGRADE to a compact awaiting-input receipt (return it) so re-issuing the
 *     query cannot re-grant `prepared` for a still-unresolved proof.
 *   - otherwise (latch cleared / different task / discovery) → return undefined.
 */
function applyAwaitingInputIdempotency(
  workspace: string,
  result: TaskPackResult,
  args: TaskPackArgs,
  effectiveQuery: string,
): TaskPackResult | undefined {
  const contract = result.execution_contract;
  const phase = contract?.typestate?.phase;
  const epochTokens = tokenizeForEpoch(effectiveQuery);
  const inputPaths = collectPackInputPaths(result, args);
  const fileFingerprint = packInputFingerprint(workspace, inputPaths);

  if (phase === "awaiting-input" && contract !== undefined) {
    if (isGenuineProofWait(result, contract)) {
      const unresolved = contract.semantic_closure?.unresolved?.[0]
        ?? contract.evidence_model?.unresolved?.[0]
        ?? "the required input";
      const residentHandles = codeTaskPackSurfaces(result.surfaces).map((s) => s.handle).slice(0, 8);
      recordAwaitingInputLatch(workspace, {
        unresolvedProof: unresolved,
        inputPaths,
        fileFingerprint,
        note: `awaiting input to resolve "${unresolved}"; re-issuing the query does not resolve it — supply the missing input `
          + `(new paths/handles) or challenge the certificate. Resident handles already served: ${residentHandles.join(",") || "(none)"}`,
      });
    }
    return undefined;
  }

  if (phase === "prepared") {
    const latch = consultAwaitingInputLatch(workspace, epochTokens, inputPaths, fileFingerprint);
    if (latch === undefined) {
      // Genuinely prepared (or a different task) — drop any stale latch.
      clearAwaitingInputLatch(workspace);
      return undefined;
    }
    return downgradeToAwaitingInput(workspace, result, latch);
  }
  return undefined;
}

/**
 * iter-2 W4: rebuild `result` as a compact awaiting-input receipt that re-emits
 * the latched verdict. Surfaces the caller already holds (resident this epoch)
 * are stripped to identity; any genuinely new surface keeps its body so no fresh
 * evidence is hidden. The execution_contract is forced to awaiting-input with
 * the latch note; workspace_state is preserved so the caller can still revalidate.
 */
function downgradeToAwaitingInput(
  workspace: string,
  result: TaskPackResult,
  latch: AwaitingInputLatch,
): TaskPackResult {
  const resident = residentSurfaceKeys(workspace, result);
  const surfaces = codeTaskPackSurfaces(result.surfaces).map((s) => {
    const isResident = resident.has(`${s.path} ${s.range}`);
    return {
      handle: s.handle,
      path: s.path,
      range: s.range,
      role: s.role,
      // Keep a NEW surface's body; strip a resident one (the caller holds it).
      ...(isResident ? {} : (s.code !== undefined ? { code: s.code } : {})),
    } as TaskPackSurface;
  });
  const prior = result.execution_contract;
  const awaitingContract: TaskExecutionContract = {
    version: prior?.version ?? 1,
    state: "needs-followup",
    readiness: "needs-followup",
    discovery_complete: false,
    next_action: "request-user-input",
    max_additional_discovery_calls: 0,
    reason: latch.note,
    ...(prior?.workspace_state ? { workspace_state: prior.workspace_state } : {}),
    ...(prior?.evidence_model ? { evidence_model: prior.evidence_model } : {}),
    ...(prior?.semantic_closure
      ? { semantic_closure: { ...prior.semantic_closure, state: "awaiting-input" } }
      : {}),
    typestate: {
      phase: "awaiting-input",
      allowed_actions: ["request-user-input"],
      challenge_required_for: [],
    },
  } as TaskExecutionContract;
  return {
    mode: "task_pack",
    // Honestly NOT complete — the proof is unresolved, so a complete-stop route
    // (and any cumulative-complete claim) must not survive the downgrade.
    coverage: "partial",
    coverage_reason: "missing-roles",
    surfaces,
    missing: [...result.missing],
    route: {
      action: "locate_missing_surfaces",
      reason: latch.note,
      max_additional_tl_calls: 0,
    },
    ...(result.task_profile ? { task_profile: result.task_profile } : {}),
    ...(result.artifact_requirements ? { artifact_requirements: [...result.artifact_requirements] } : {}),
    execution_contract: awaitingContract,
  };
}

/**
 * iter-2 W4/W1: `${path} ${range}` keys of surfaces that an earlier serve in
 * this session delivered AND whose file is still byte-identical (so the caller
 * holds an up-to-date copy). Epoch scoping is the caller's responsibility — the
 * W4 latch consult has already confirmed the same task epoch before this runs.
 */
function residentSurfaceKeys(workspace: string, _result: TaskPackResult): Set<string> {
  const out = new Set<string>();
  const records = servedPacksByWorkspace.get(laneScopedKey(workspace));
  if (records === undefined) return out;
  for (const rec of records) {
    const shaByPath = new Map(rec.fileShas.map(({ path: p, sha }) => [p, sha]));
    for (const s of rec.surfaces) {
      const capturedSha = shaByPath.get(s.path);
      if (capturedSha === undefined) continue;
      if (surfaceFileSha(workspace, s.path) !== capturedSha) continue;
      out.add(`${s.path} ${s.range}`);
    }
  }
  return out;
}

/**
 * iter-3 F3: content-addressed index of `${path}\u0000${range}` -> the prior
 * serve's { handle, sha } for every surface an EARLIER same-epoch pack in this
 * session delivered AND whose file is still byte-identical. Epoch-scoped here
 * (unlike residentSurfaceKeys, whose caller pre-confirms the epoch): a body
 * served under a DIFFERENT task may have been compacted out of the caller's
 * context, so only records whose epoch overlaps the current one count. First
 * writer wins (the earliest serve owns the resident handle the caller holds).
 */
function residentBodyIndex(
  workspace: string,
  epochTokens: readonly string[],
): Map<string, { handle: string; sha: string }> {
  const idx = new Map<string, { handle: string; sha: string }>();
  const records = servedPacksByWorkspace.get(laneScopedKey(workspace));
  if (records === undefined || epochTokens.length === 0) return idx;
  for (const rec of records) {
    if (!epochsOverlap(rec.epochTokens, epochTokens)) continue;
    const shaByPath = new Map(rec.fileShas.map(({ path: p, sha }) => [p, sha]));
    for (const s of rec.surfaces) {
      const capturedSha = shaByPath.get(s.path);
      if (capturedSha === undefined) continue;
      if (surfaceFileSha(workspace, s.path) !== capturedSha) continue;
      const key = `${s.path}\u0000${s.range}`;
      if (!idx.has(key)) idx.set(key, { handle: s.handle, sha: capturedSha });
    }
  }
  return idx;
}

/** iter-3 F3: compact 12-hex form of a stored `sha256:<hex>` content sha, for the resident marker. */
function shortSha(sha: string): string {
  return sha.replace(/^sha256:/, "").slice(0, 12);
}

/**
 * iter-3 F3 (cross-pack resident-file dedup): legitimately-DIFFERENT packs in one
 * session (an enum change, a backend query, a frontend view) each re-served the
 * same unchanged shared file body (the forensic: a shared enums file re-sent up
 * to 3×). applyPackDedupe only catches the immediately-PRIOR pack and keys on the
 * handle, so a shared file resurfacing two packs later — or under a fresh handle
 * — slipped through. This pass, run right after applyPackDedupe, replaces the
 * `code` of any NON-required surface whose exact (path,range) body was already
 * served this epoch AND whose file is byte-unchanged with a compact code-less
 * resident marker naming the prior handle + sha, so the caller (who already holds
 * that body) is told where it is instead of paying for it again. The real body is
 * preserved server-side (dedupedSurfaceBody) so proof passes stay deterministic,
 * exactly as applyPackDedupe does. Conservative:
 *   - only bodies >= RESIDENT_DEDUP_MIN_BYTES (tiny ones aren't worth indirection);
 *   - never a `required` edit-frontier surface (its body stays fully visible);
 *   - exact (path,range) match with the file proven byte-unchanged (so the marker
 *     truthfully points at bytes the caller already has).
 */
export function applyResidentFileDedup(
  workspace: string,
  result: TaskPackResult,
  epochTokens: readonly string[],
): void {
  if (result.pack_unchanged === true) return;
  const idx = residentBodyIndex(workspace, epochTokens);
  // B2b (2026-08-01 serving-completeness): PACK PADDING. Live defect
  // (2026-07-31-semantic-signal5-2, T09): a caller-scoped pack for ONE new doc
  // padded itself with 14.6KB of surfaces this session had ALREADY served —
  // they missed this pass because the index keyed on an EXACT (path,range) pair
  // from a prior PACK. Two generalizations, both holding the "a marker must
  // truthfully name bytes the caller already has" bar:
  //   (a) a range SUBSUMED by an already-served range of the same file counts
  //       (the caller holds a superset), and
  //   (b) the session’s own served-range ledger counts as evidence, not only
  //       prior packs — a slice/symbol/full serve of those lines leaves them
  //       exactly as resident as a pack surface did.
  // Per-path list form of the pack index, for the subsumption test.
  const residentByPath = new Map<
    string,
    Array<{ span: { start: number; end: number }; handle: string; sha: string }>
  >();
  for (const [key, value] of idx) {
    const cut = key.lastIndexOf("\u0000");
    if (cut <= 0) continue;
    const span = parseSurfaceSpan(key.slice(cut + 1));
    if (span === undefined) continue;
    const relPath = key.slice(0, cut);
    const list = residentByPath.get(relPath) ?? [];
    list.push({ span, handle: value.handle, sha: value.sha });
    residentByPath.set(relPath, list);
  }
  for (const surf of codeTaskPackSurfaces(result.surfaces)) {
    if (surf.code === undefined) continue; // nothing to strip (or already deduped)
    if (surf.required === true) continue; // keep the edit target's body inline
    if (Buffer.byteLength(surf.code, "utf8") < RESIDENT_DEDUP_MIN_BYTES) continue;
    // A disclosed-partial body is NOT the whole `range`; claiming residency
    // over that range would over-state what the caller holds.
    if (surf.content_completeness === "partial") continue;
    const span = parseSurfaceSpan(String(surf.range ?? ""));
    let hit = idx.get(`${surf.path}\u0000${surf.range}`);
    if (hit === undefined && span !== undefined) {
      hit = (residentByPath.get(surf.path) ?? []).find(
        (entry) => entry.span.start <= span.start && entry.span.end >= span.end,
      );
    }
    if (hit !== undefined) {
      if (hit.handle === surf.handle) continue; // same handle → applyPackDedupe/pack_unchanged own this
      dedupedSurfaceBody.set(surf, surf.code);
      surf.code_unchanged =
        `${hit.handle} — already served this session (${surf.path}:${surf.range}, sha ${shortSha(hit.sha)}); `
        + `resident in your context — re-read ${hit.handle} only if the body is needed`;
      surf.code = undefined;
      continue;
    }
    // B2b RESIDUAL (deliberate): the session's served-range LEDGER also proves
    // residency for slice/symbol/full serves and for packs of a different task
    // epoch — but packServeIter3's "does NOT strip across a DIFFERENT task
    // epoch (the body may be out of context)" contract owns that ground, and
    // the ledger carries no epoch to honor it with. Cross-epoch padding is left
    // un-deduped rather than silently weakening that guarantee.
  }
}

/**
 * Mutates `result.surfaces` in place: any surface whose embedded `code`
 * fingerprints identically (same handle, same range, same content hash) to
 * an entry already seen — either from the immediately prior pack for this
 * workspace, or from an earlier surface already processed in THIS pack —
 * has its `code` replaced with a short `code_unchanged` pointer instead.
 * Runs BEFORE trimToCap so the bytes it frees are available to the trim
 * ladder like any other savings, and is a no-op for surfaces with no `code`
 * to begin with (undefined-code surfaces have nothing to dedupe).
 *
 * P1 fix: this ONLY reads (suppresses) — it does NOT persist the workspace
 * cache. Persistence is deferred to persistPackFingerprints(), run AFTER
 * trimToCap, so the cache records ONLY the code actually delivered in the
 * final response. Persisting here (pre-trim) recorded fingerprints for code
 * that trimToCap then stripped (Phase 5 drops `surf.code`; Phase 1 drops whole
 * surfaces), so the NEXT pack would emit `code_unchanged: "<handle> — see
 * prior pack"` pointing at code the prior pack never actually delivered.
 */
export function applyPackDedupe(workspace: string, result: TaskPackResult): void {
  const prior = lastPackBlocksByWorkspace.get(laneScopedKey(workspace)) ?? [];
  const running: PackedBlockFingerprint[] = prior.map((f) => ({ ...f, fromPriorPack: true }));

  for (const surf of result.surfaces) {
    if (surf.code === undefined) continue;
    const hash = shaOfText(surf.code);
    const match = running.find((f) => f.handle === surf.handle && f.range === surf.range && f.hash === hash);
    if (match) {
      // A different request can reuse the same handle while making that body
      // action-bearing (artifact target, edit frontier, etc.). Exact request
      // re-calls are already intercepted by pack_unchanged; never replace a
      // newly required body with a pointer to unrelated prior-pack context.
      if (match.fromPriorPack && surf.required === true) continue;
      // W4: preserve the real body server-side (identity-keyed) so proof passes
      // stay deterministic across identical re-calls; the response still ships
      // only the compact pointer below.
      dedupedSurfaceBody.set(surf, surf.code);
      surf.code_unchanged = match.fromPriorPack
        ? `${surf.handle} — see prior pack`
        : `${surf.handle} — duplicate of an earlier surface in this pack`;
      surf.code = undefined;
      continue;
    }
    running.push({ handle: surf.handle, range: surf.range, hash, fromPriorPack: false });
  }
}

/**
 * P1 fix: persist the FINAL response's code-bearing surfaces as this
 * workspace's "last pack" fingerprints, replacing the cache wholesale (true
 * last-pack semantics — the cache is what the immediately-next pack dedupes
 * against, so it must describe exactly what THIS pack actually delivered).
 *
 * Runs AFTER trimToCap. Records ONLY surfaces that still carry embedded `code`
 * in the final result: a surface whose code trimToCap stripped (Phase 5), or
 * a surface trimToCap dropped entirely (Phase 1), delivered no code, so it
 * must not seed a `code_unchanged` pointer on the next pack. Surfaces already
 * carrying `code_unchanged` (suppressed by applyPackDedupe above) delivered no
 * fresh code either and are likewise not re-recorded — the prior entry they
 * point at is already in the (previous) cache, but since we replace wholesale,
 * only surfaces that shipped code THIS pack are what the next pack can match.
 */
export function persistPackFingerprints(workspace: string, result: TaskPackResult): void {
  const fingerprints: PackedBlockFingerprint[] = [];
  for (const surf of result.surfaces) {
    if (surf.code === undefined) continue;
    fingerprints.push({
      handle: surf.handle,
      range: surf.range,
      hash: shaOfText(surf.code),
      fromPriorPack: true,
    });
  }
  lastPackBlocksByWorkspace.set(laneScopedKey(workspace), fingerprints);
}

/**
 * P1 fix: the standard "dedupe (pre-trim suppression) → trim → persist the
 * delivered code (post-trim)" pipeline every task_pack call site runs. Wraps
 * the ordering so the fingerprint WRITE can never again drift back before
 * trimToCap (which was the stale-`code_unchanged` bug).
 *
 * DESIGN-v0.8 coverage-honesty (Part 5): also the ONE choke point where the
 * structured check records are persisted for closure tracking — fires for
 * EVERY pack flow (locate, seeded, partial, diff, no-details abstain), AFTER
 * trim, so the session keeps the full record set even when the response's
 * `checks[]` lines were trimmed away. When `query`/`checkRecords` are omitted
 * (a caller with no checks to record), the session write is skipped.
 */
/**
 * DESIGN-v0.9 §4.3: recompute `route` and `next` against a post-trim pack whose
 * coverage was FLIPPED by a whole-surface drop. Uses the SAME buildRoute /
 * nextHintForCoverage the pre-trim path uses, so the values are
 * correct-by-construction for the reduced surface set — never a stale
 * "edit_file handle=… (you're done)" next on a now-partial pack, and never an
 * absent route. Mutates `result` in place. Fired only on a coverage-STRING
 * change; a partial→partial reason-only change keeps its already-appropriate
 * partial `next`.
 */
export function recomputeRouteAndNextAfterTrim(
  result: TaskPackResult,
  rc: { query: string; missingRequired: string[]; unmatchedConcern: string[] },
): void {
  if (
    result.wiring?.status === "ready"
    && result.wiring.evidence_graph !== undefined
    && result.missing.length === 0
  ) {
    // A post-trim coverage flip must not replay the generic role gaps captured
    // before wiring proof was built. The proved consumer insertion frontier is
    // the authoritative route for this profile.
    result.route = {
      action: "edit_from_handles",
      reason: "wiring source, consumer, and insertion handle are bundled",
      max_additional_tl_calls: 0,
    };
    delete result.next;
    delete result.continuation;
    return;
  }
  result.route = buildRoute(
    result.surfaces, rc.missingRequired, result.coverage, result.missing, rc.query, result.coverage_reason,
  );
  if (result.route.action === "answer_from_handles") {
    delete result.next;
    delete result.continuation;
    return;
  }
  // rc.missingRequired (roles), NOT result.missing (roles + unresolvable caller
  // paths). This function's own doc comment already promised the pre-trim
  // values; only the argument had drifted, so a byte-trimmed pack emitted a
  // `surfaceRoles=[<a path>]` follow-up the next pack could never resolve.
  const nextHint = nextHintForCoverage(
    result.surfaces, result.coverage, result.coverage_reason, rc.query, rc.missingRequired, rc.unmatchedConcern,
  );
  if (nextHint !== undefined) result.next = nextHint;
  else delete result.next;
}

/**
 * DESIGN-v0.9 §4.4: stamp the response-level content_completeness ROLLUP.
 * CONVENTION (see the field docs on TaskPackSurface/TaskPackResult): the rollup
 * is emitted ONLY as "partial" and OMITTED when complete (absence = complete),
 * so it is "partial" iff any surface carries content_completeness:"partial"
 * (cliff-trimmed or body-stripped). Idempotent — clears a stale stamp when no
 * surface is partial (e.g. a pack_unchanged compact re-serialization).
 */
export function stampRollupContentCompleteness(result: TaskPackResult): void {
  if (result.surfaces.some((s) => s.content_completeness === "partial")) {
    result.content_completeness = "partial";
  } else {
    delete result.content_completeness;
  }
}

function contentFollowupTarget(surface: TaskPackSurface, preferOutline = false): string | undefined {
  const outlineTargets = surface.outline
    ?.map((line) => line.match(/:\s+(read_file\s+.+)$/)?.[1])
    .filter((target): target is string => target !== undefined) ?? [];
  if (preferOutline && outlineTargets.length > 0) {
    const handles = outlineTargets
      .map((target) => target.match(/\bhandle=(h[0-9a-z]+)\b/)?.[1])
      .filter((handle): handle is string => handle !== undefined);
    if (handles.length === outlineTargets.length && handles.length > 1) {
      return `read_file handles=${JSON.stringify(handles)}`;
    }
    return outlineTargets[0]!;
  }
  const remaining = surface.remaining_ranges?.[0];
  if (remaining) return `read_file mode=slice handle=${surface.handle} range=${remaining}`;
  const runnerUp = outlineTargets[0];
  if (runnerUp) return runnerUp;
  const inspect = surface.likely_edits?.find((hint) => hint.kind === "inspect-slice")?.target;
  if (inspect !== undefined && !inspect.includes(`handle=${surface.handle}`)) return inspect;
  // 2026-07-31: a same-handle re-slice only progresses when the caller does NOT
  // already hold the body. A fully-served surface reaches here through
  // hasSufficientEditContent's low-confidence branch (outline present,
  // inspect-slice confidence under the threshold) — and re-reading the identical
  // window cannot raise that confidence. Name no call at all rather than a
  // no-op read; the caller keeps the honest needs-followup verdict either way.
  if (!hasServedCode(surface)) return `read_file mode=slice handle=${surface.handle}`;
  return undefined;
}

/** Keep coverage structural; gate editing separately on the bodies actually served. */
function reconcileContentSufficiency(result: TaskPackResult): void {
  // W9 defense-in-depth (2026-08-22): a resolved create route's terminal
  // fields are authoritatively owned by assertCreateRoute (the create-target
  // block sets them once; assertCreateRoute re-asserts them verbatim at the
  // pre-contract choke point). This function's default actionTargets rule has
  // no create-route awareness — with change_contract cleared by the create
  // block, it falls back to "surfaces[0] (or the first required surface) is
  // the mandatory edit target", which for a create task is a locator-era
  // surface that will never carry the new file's content. Bail out before
  // that default ever runs; create_target is still undefined the ONE time
  // this function legitimately runs before create-target resolution (from
  // refreshAfterRecursiveClosure), so this guard never masks that earlier,
  // still-relevant call.
  if (result.create_target !== undefined && result.route?.action === "edit_from_handles") {
    return;
  }
  const surfaces = codeTaskPackSurfaces(result.surfaces);
  const editHandles = new Set(
    result.change_contract?.obligations
      .filter((obligation) => obligation.action === "edit")
      .map((obligation) => obligation.handle) ?? [],
  );
  const answerRequiredTargets = result.task_profile === "answer"
    ? surfaces.filter((surface) => surface.required === true)
    : [];
  const actionTargets = answerRequiredTargets.length > 0
    ? answerRequiredTargets
    : result.task_profile === "multi_concern"
    ? surfaces.filter((surface, index) => index === 0 || surface.required === true)
    : editHandles.size > 0
      ? surfaces.filter((surface) => editHandles.has(surface.handle))
      : surfaces.filter((surface, index) => index === 0 || surface.required === true);
  const hasFocusedTestEvidence = surfaces.some((surface) =>
    surface.role === "test"
    && hasServedCode(surface)
    && /query-(?:identifier|evidence)-test-match/.test(surface.why ?? "")
  );
  const incomplete = actionTargets.find((surface) => {
    if (hasSufficientEditContent(surface)) return false;
    // When the caller supplied both the exact implementation and a test slice
    // that mentions it, runner-up navigation is optional evidence rather than
    // a reason to re-read the implementation. A lone source seed does not get
    // this exemption: propagation queries still have to discover their real
    // contract/test/doc consumers.
    return !(
      hasFocusedTestEvidence
      && surface.why?.startsWith("anchor-focus: explicit query identifier ") === true
      && hasServedCode(surface)
      && surface.content_completeness !== "partial"
    );
  });
  if (!incomplete) {
    delete result.content_sufficiency;
    return;
  }

  result.content_sufficiency = "needs-followup";
  // Use the bound profile, not only the current route: trim reconciliation may
  // already have changed an answer route to inspect_handles before this pass.
  const answerTask = result.task_profile === "answer";
  // §3.4.1 / D1: a route already carrying the sanctioned served-zoom
  // affordance (answer_from_handles + budget exactly 1 over a partial
  // required surface) must not be downgraded to inspect_handles just because
  // the profile is answer — that IS the joint "prepared+partial primary"
  // shape AGENTS.md states, and downgrading it here would throw the
  // affordance away before buildTaskExecutionContract ever gets to honour it.
  const canInspect = result.route?.action === "edit_from_handles"
    || (answerTask && !hasServedZoomAffordance(result))
    || (result.route === undefined && result.coverage !== "partial" && result.missing.length === 0);
  if (!canInspect) return;
  const followup = contentFollowupTarget(incomplete, answerTask);
  // No progressing read remains (see contentFollowupTarget's tail). Keep
  // content_sufficiency:"needs-followup" — that verdict is still true — but do
  // not install an inspect route whose one sanctioned action re-reads bytes the
  // response already served.
  if (followup === undefined) return;
  result.route = {
    action: "inspect_handles",
    reason: answerTask
      ? "answer evidence is incomplete; inspect the bundled relevant handles before answering"
      : "edit surface content is incomplete or low-confidence; inspect one handle before editing",
    max_additional_tl_calls: 1,
  };
  result.next = followup;
  delete result.continuation;
}

/**
 * Final post-trim guard: a required body which still names a bounded range is
 * not terminal evidence.  Several late seams append `missing[]` disclosures or
 * shed bodies after the first readiness contract has been built; merely
 * canonicalizing that old contract can otherwise publish `prepared` with no
 * executable way to read the range it just disclosed.
 */
function preservePartialRangeProgress(result: TaskPackResult): boolean {
  // Multi-concern and answer profiles have their own certified document /
  // cumulative affordances. This guard repairs the generic native-body trim
  // path, where the clipped implementation itself is the only next evidence.
  if (result.profile_binding?.selected !== "generic") return false;
  // The response-level rollup is set by the byte-trim phase. A later optional
  // doc-sliver may carry navigation ranges without making the task's evidence
  // incomplete; those established prepared answers stay terminal.
  if (result.content_completeness !== "partial") return false;
  const surface = codeTaskPackSurfaces(result.surfaces).find((candidate) =>
    candidate.required !== false
    && /\.(?:c|cc|cpp|cxx|h|hh|hpp|hxx)$/i.test(candidate.path)
    && (candidate.remaining_ranges?.length ?? 0) > 0
  );
  const range = surface?.remaining_ranges?.[0];
  if (surface === undefined || range === undefined) return false;
  const marker = `remaining-range:${surface.path}:${range}`;
  if (!result.missing.includes(marker)) {
    result.missing = [...result.missing, marker];
  }
  result.content_sufficiency = "needs-followup";
  const call: ContinuationCall = {
    tool: "read_file",
    arguments: { mode: "slice", handle: surface.handle, range },
  };
  const plan = enforceContinuationBudget({
    version: 1,
    stages: [{ execution: "sequential", calls: [call] }],
  });
  if (plan !== undefined) {
    result.continuation = plan;
    const next = deriveNextFromPlan(plan);
    if (next !== undefined) result.next = next;
  }
  result.route = {
    action: "inspect_handles",
    reason: "a required served surface is partial; continue with its bounded remaining range",
    max_additional_tl_calls: 1,
  };
  return true;
}

/** Rebuild readiness after every late trim/disclosure seam, before wire projection. */
function rebuildFinalExecutionContract(
  result: TaskPackResult,
  profile: TaskProfile,
  query: string,
  workspace: string,
  workspaceInventory: WorkspaceInventoryState,
  proofTransition = false,
  /** A-F2: the executed-next ledger lane, so the rebuilt contract's no-dead-end guard cannot re-open a consumed call. */
  lane: string = DEFAULT_CONTRACT_LANE,
): void {
  const rangeProgress = preservePartialRangeProgress(result);
  const proofTransitionMissing = result.missing.filter(
    (entry) => !entry.startsWith("unresolved-ledger:"),
  );
  // The provisional contract runs before recordEpochTaskContract persists a
  // current-pack capability limitation. It can therefore honestly reject an
  // otherwise complete frontier for an open-universe proof which the same
  // response subsequently discharges. Reconciliation deliberately demotes
  // the route at that provisional point; restore only this exact reversible
  // demotion before the proof-transition rebuild, never a general discovery
  // route. All required bodies and the explicit-gap-only missing projection
  // remain the guards on the terminal edit grant below.
  const restoreOpenUniverseFrontier = proofTransition
    && result.route?.action === "locate_missing_surfaces"
    && /^ready rejected: unresolved proof open-universe:/.test(result.route.reason ?? "")
    && result.coverage === "complete"
    && proofTransitionMissing.every((entry) => entry.startsWith("explicit-gap:"))
    && codeTaskPackSurfaces(result.surfaces)
      .filter((surface) => surface.required !== false)
      .every(hasServedCode);
  if (restoreOpenUniverseFrontier) {
    result.route = {
      action: "edit_from_handles",
      reason: "current-pack evidence-expansion capability gap discharges the exhaustive frontier",
      max_additional_tl_calls: 0,
    };
    delete result.next;
    delete result.continuation;
  }
  // F-B3's prior-pack disclosure is the other late mutation that can make a
  // pre-existing certificate stale. Leave unrelated post-contract adornments
  // on their established projection path; they do not change readiness.
  const stalePriorObligation = result.missing.some((entry) => entry.startsWith("unserved-obligation:"));
  if (!rangeProgress && !stalePriorObligation && !proofTransition) {
    applyCanonicalTaskDecision(result);
    return;
  }
  result.execution_contract = buildTaskExecutionContract(
    result,
    profile,
    query,
    buildTaskWorkspaceState(result, workspace, workspaceInventory),
    workspace,
    proofTransition,
  );
  reconcileTaskPackExecutionContract(result);
  enforceNoDeadEndContract(result, query, { workspace, lane });
  applyCanonicalTaskDecision(result);
}

/**
 * Bind an already-prepared response to the terminal ledger snapshot written
 * later in this same pack.  The normal epoch write is intentionally after the
 * first decision (otherwise an unresolved dependency loses its initial find),
 * but a certificate ID must not differ merely because the identical request
 * is reconstructed from that durable row after a restart.
 *
 * This is deliberately an ID-only rebind.  Re-running the contract builder
 * here would re-evaluate late route/continuation seams and can turn a valid
 * prepared wiring pack into discovery.  The proof, decision, and surface
 * projection were already adjudicated; only the durable digest component of
 * the certificate identity became available after that adjudication.
 */
function rebindPreparedCertificateToLedger(
  result: TaskPackResult,
  profile: TaskProfile,
  query: string,
  workspace: string,
): void {
  const contract = result.execution_contract;
  const certificate = contract?.readiness_certificate;
  if (contract?.typestate.phase !== "prepared" || certificate === undefined) return;
  // A-F6: the ledger-bound id form is a proof-completion artifact. This rebind
  // was ungated, so with the compatibility switch OFF an id still acquired the
  // `ready-<ledger16>-<proof16>` shape that v0.12 never emits — measured live
  // on an OFF pack. OFF must reproduce v0.12's single-segment id.
  if (!proofCompletionEnabled()) return;
  const ledger = taskContractDischargeCertificate(workspace, tokenizeForEpoch(query));
  const workspaceFingerprint = contract.workspace_state?.fingerprint;
  if (ledger === undefined || workspaceFingerprint === undefined) return;
  const taskHash = shaOfText(`${profile}\u0000${query}`).replace(/^sha256:/, "").slice(0, 16);
  const proofHash = shaOfText(JSON.stringify({
    taskHash,
    obligations: certificate.obligations.map((obligation) => [
      obligation.id,
      obligation.status,
      (obligation.evidence ?? []).map((item) => item.handle),
    ]),
    actionFrontier: certificate.action_frontier,
    workspaceState: workspaceFingerprint,
    ledgerDigest: ledger.digest,
  })).replace(/^sha256:/, "").slice(0, 16);
  const id = `ready-${ledger.digest.slice(0, 16)}-${proofHash}`;
  if (certificate.id === id) return;
  certificate.id = id;
  contract.typestate.certificate_id = id;
  if (contract.semantic_closure?.state === "closed") {
    contract.semantic_closure.closure_id = id;
  }
}

/**
 * DESIGN-v0.9 §4.6b deterministic-next internal execution. When a
 * complete/focused pack would have emitted `read_file handles=[...]` as its
 * `next` (>=2 code-less surfaces whose handles were minted moments ago), the
 * PACK BUILDER instead serves those bodies inline in the SAME response — the
 * follow-up read is deterministic and the file content is already in the pack's
 * FileReadCache, so prompting a round trip is pure waste.
 *
 * Runs INSIDE pack assembly, BEFORE trimToCap (the caller's ordering): the
 * §4.3 trim inversion then guarantees any body that does not fit is stripped
 * (content_completeness:"partial") before any handle is dropped, so an inlined
 * body can never cost a handle. Each embedded body reuses the SAME
 * centeredSliceForCap the cliff fix uses (match-centered trimmed window +
 * remaining_ranges), and stamps one "surface-body:<handle>" entry on
 * `result.inlined`. attachSupply (§4.7) later verifies every stamped handle is
 * content-bearing and drops any that trimToCap subsequently stripped.
 *
 * Budget: bounded by fitsInCap, whose capForResult grants the §4.8 must-fetch
 * expansion ONLY when the flag is on AND `inlined[]` is non-empty. With the
 * flag OFF the loop inlines only what fits today's caps and reverts the first
 * body that would overflow (the rest stay code-less; the `next`/continuation
 * covers them). A code-less surface whose path cannot be safely resolved
 * inside the workspace is skipped (centeredSliceForCap returns undefined for an
 * out-of-workspace or unreadable path — the §6 sanity guard).
 *
 * Gated on >=2 code-less surfaces to mirror EXACTLY the nextHintForCoverage
 * branch it replaces; returns the count actually inlined. Mutates `result`.
 */
export function inlineCodelessSurfaces(
  result: TaskPackResult,
  workspace: string,
  cache?: FileReadCache,
): number {
  if (result.coverage !== "complete" && result.coverage !== "focused") return 0;
  const codeless = result.surfaces.filter(
    (s) => s.code === undefined && s.code_unchanged === undefined,
  );
  if (codeless.length < 2) return 0;

  let count = 0;
  for (const surf of codeless) {
    // §6 sanity guard: never serve content for a path outside the workspace
    // root. readCached's no-cache branch does a bare path.join with no realpath
    // check, so the boundary is enforced HERE (safeResolve returns undefined
    // for an escaping/absolute path) rather than relying on the reader.
    if (safeResolve(surf.path, workspace) === undefined) continue;
    const embed = centeredSliceForCap(workspace, surf.path, surf.range, cache);
    if (!embed) continue; // I/O error, malformed range, or unreadable path.

    // Tentatively apply, then revert if the pack no longer fits its (possibly
    // must-fetch-expanded) cap. Push the inlined stamp BEFORE the fit check so
    // capForResult sees the non-empty `inlined[]` and grants the §4.8
    // expansion when the flag is on.
    const prevCode = surf.code;
    const prevCC = surf.content_completeness;
    const prevRR = surf.remaining_ranges;
    const prevInlined = result.inlined;
    surf.code = embed.code;
    if (embed.remaining_ranges.length > 0) {
      surf.content_completeness = "partial";
      surf.remaining_ranges = embed.remaining_ranges;
    }
    result.inlined = [...(prevInlined ?? []), `surface-body:${surf.handle}`];

    if (fitsInCap(result)) {
      count++;
      continue;
    }
    // Over budget — revert this body and stop; remaining surfaces stay
    // code-less and the recomputed `next` (below) covers them.
    surf.code = prevCode;
    surf.content_completeness = prevCC;
    surf.remaining_ranges = prevRR;
    result.inlined = prevInlined;
    break;
  }

  if (result.inlined !== undefined && result.inlined.length === 0) delete result.inlined;
  return count;
}

// ===========================================================================
// IMPROVEMENT A: cumulative (session-stateful) coverage, served-surface
// logging, and the complete-working-set frontier_index. IMPROVEMENT D: honest
// verification runnability verdict. All state lives in util/packServeLog.ts
// (an independent per-workspaceRoot registry — never session.ts).
// ===========================================================================

/** Cap on frontier_index entries and its serialized byte budget. */
const MAX_FRONTIER_INDEX_ENTRIES = 200;
const MAX_FRONTIER_INDEX_BYTES = 8192;

/** Relative import/require specifier in an already-served JS/TS body (no parse). */
const REL_IMPORT_SPEC_RE = /(?:\bfrom\b|\bimport\b|\brequire\s*\()\s*['"](\.[^'"]+)['"]/g;

/**
 * IMPROVEMENT A part 2: flip coverage to cumulative-complete when the UNION of
 * this call's surfaces and the still-valid surfaces earlier calls served closes
 * every required role that THIS pack alone reported missing. Mutates `result`
 * into the same trustworthy shape a single-call complete pack carries (coverage
 * complete, no missing/blocking, edit/answer route with 0 extra calls) and adds
 * `coverage_basis:"cumulative"` + a `served_earlier` list naming exactly what
 * prior calls already served (so the model does not re-fetch it). Returns true
 * when the flip fired. Conservative: fires ONLY when a required role is
 * genuinely absent from the current surfaces yet fully covered by valid prior
 * surfaces of the SAME task epoch.
 */
function applyCumulativeCoverage(
  result: TaskPackResult,
  prior: ServedSurfaceEntry[],
  query: string,
): boolean {
  const required = new Set(result.required_surfaces ?? []);
  if (required.size === 0) return false;
  const missingRequired = result.missing_required_surfaces ?? [];
  if (missingRequired.length === 0 || prior.length === 0) return false;
  const priorRoles = new Set(prior.map((p) => p.role));
  // Every currently-missing required role must be covered by a valid prior surface.
  if (missingRequired.some((role) => !priorRoles.has(role))) return false;
  const servedEarlier = prior
    .filter((p) => missingRequired.includes(p.role))
    .map((p) => ({ path: p.path, role: p.role, ...(p.handle ? { handle: p.handle } : {}) }));
  if (servedEarlier.length === 0) return false;

  result.coverage = "complete";
  result.coverage_basis = "cumulative";
  result.served_earlier = servedEarlier;
  result.missing = (result.missing ?? []).filter((m) => !missingRequired.includes(m));
  delete result.missing_required_surfaces;
  delete result.blocking_next_steps;
  if (result.coverage_reason === "missing-roles") delete result.coverage_reason;
  result.content_sufficiency = undefined;
  // The role-specific structure was proven across the earlier calls; the
  // stateless contract cannot re-verify it, so drop the change_contract whose
  // per-role obligations would otherwise read "uncovered" for the prior roles.
  result.change_contract = undefined;
  delete result.next;
  const coveredRoles = [...new Set(servedEarlier.map((s) => s.role))].join(",");
  // Route must STOP emitting locate_missing_surfaces for roles now in the union.
  result.route = editFromHandlesRoute(
    `required roles covered cumulatively (${coveredRoles} served by earlier calls this task; see served_earlier)`,
    0,
    query,
  );
  return true;
}

/**
 * IMPROVEMENT A part 3: depth-1 import neighbours of the served surfaces,
 * resolved WITHOUT any new tree-sitter parse — a bounded regex scan of each
 * surface's ALREADY-available body for RELATIVE import specifiers, each resolved
 * against the surface's own directory with a cheap statKindCached extension
 * probe. Bare/package specifiers are ignored; anything that does not resolve to
 * an existing workspace file is skipped silently. Returns workspace-relative
 * paths not already in `seen`.
 */
function directImportNeighbors(
  workspace: string,
  surfaces: TaskPackSurface[],
  seen: ReadonlySet<string>,
): string[] {
  const out: string[] = [];
  const emitted = new Set<string>();
  const exts = [".ts", ".tsx", ".js", ".jsx", ".mts", ".cts", ".mjs", ".cjs"];
  for (const s of surfaces) {
    if (out.length >= 40) break;
    if (!/\.(ts|tsx|js|jsx|mts|cts|mjs|cjs)$/i.test(s.path)) continue;
    const body = s.code ?? readCached(workspace, s.path);
    if (body === undefined) continue;
    const dir = path.posix.dirname(s.path);
    let count = 0;
    for (const m of body.matchAll(REL_IMPORT_SPEC_RE)) {
      if (count >= 12 || out.length >= 40) break;
      const spec = m[1];
      if (spec === undefined) continue;
      count++;
      const base = path.posix.normalize(path.posix.join(dir, spec));
      if (base.startsWith("..")) continue; // escaped the workspace root — ignore
      const candidates = [
        base,
        ...exts.map((e) => `${base}${e}`),
        ...exts.map((e) => `${base}/index${e}`),
      ];
      for (const cand of candidates) {
        if (seen.has(cand) || emitted.has(cand)) continue;
        if (statKindCached(workspace, workspace, cand) === "file") {
          emitted.add(cand);
          out.push(cand);
          break;
        }
      }
    }
  }
  return out;
}

/** Byte+count cap for frontier_index; the overflow tail becomes per-directory rollup entries. */
function capFrontierIndex(
  entries: Array<{ path: string; role: string; handle?: string }>,
): Array<{ path: string; role: string; handle?: string }> {
  const head: Array<{ path: string; role: string; handle?: string }> = [];
  const overflow: Array<{ path: string; role: string; handle?: string }> = [];
  for (const e of entries) {
    if (
      head.length < MAX_FRONTIER_INDEX_ENTRIES
      && Buffer.byteLength(JSON.stringify([...head, e]), "utf8") <= MAX_FRONTIER_INDEX_BYTES
    ) {
      head.push(e);
    } else {
      overflow.push(e);
    }
  }
  if (overflow.length === 0) return head;
  const byDir = new Map<string, number>();
  for (const e of overflow) {
    const dir = path.posix.dirname(e.path);
    byDir.set(dir, (byDir.get(dir) ?? 0) + 1);
  }
  for (const [dir, n] of byDir) {
    if (head.length >= MAX_FRONTIER_INDEX_ENTRIES) break;
    head.push({ path: `${dir}/`, role: `dir-rollup:+${n}` });
  }
  return head;
}

/**
 * IMPROVEMENT A part 3: attach a complete, code-less inventory of the ENTIRE
 * known working set whenever coverage is NOT single-call complete (or the
 * working set overflowed byte caps): current surfaces + still-valid prior
 * served surfaces + budget-trimmed candidates + resolvable depth-1 import
 * neighbours. The model zooms via these handles/paths instead of re-searching.
 */
function attachFrontierIndex(
  workspace: string,
  result: TaskPackResult,
  prior: ServedSurfaceEntry[],
): void {
  if (result.ok === false) return; // broad-overview envelope: nothing to inventory
  // iter-2 W3: NEVER on a complete/cumulative-complete pack. The iter-1 forensics
  // paid frontier_index (9KB across 6 packs) that the model re-searched instead
  // of navigating; a complete pack should say "stop", not hand back a re-search
  // inventory. (Previously an overflowed-complete pack still attached it.)
  if (result.coverage === "complete" || result.coverage === "focused") return;

  const surfaces = codeTaskPackSurfaces(result.surfaces);
  const seen = new Set<string>();
  const entries: Array<{ path: string; role: string; handle?: string }> = [];
  const add = (relPath: string, role: string, handle?: string): void => {
    if (relPath.length === 0 || seen.has(relPath)) return;
    seen.add(relPath);
    entries.push({ path: relPath, role, ...(handle ? { handle } : {}) });
  };
  for (const s of surfaces) add(s.path, s.role, s.handle);
  for (const p of prior) add(p.path, p.role, p.handle);
  for (const t of result.trimmed ?? []) add(t, "candidate");
  for (const nb of directImportNeighbors(workspace, surfaces, seen)) add(nb, "import-neighbor");
  if (entries.length === 0) return;

  const capped = capFrontierIndex(entries);
  if (capped.length === 0) return;
  // iter-2 W3: attach only when it carries NEW information — the first
  // attachment of the epoch, or a changed inventory since the last one. An
  // identical re-attachment is pure resident-byte tax (the model already holds
  // the same paths/handles), so suppress it.
  const sig = shaOfText(JSON.stringify(capped));
  if (!shouldAttachFrontierIndex(workspace, sig)) return;
  result.frontier_index = capped;
  if (result.route !== undefined && !/frontier_index/.test(result.route.reason)) {
    result.route = {
      ...result.route,
      reason: `${result.route.reason}; frontier_index is the complete known working set — zoom via its handles/paths instead of re-searching`,
    };
  }
}

/**
 * IMPROVEMENT A orchestration — runs once at dedupeTrimAndPersist's exit
 * (before the contract build). (1) consults the served-surface log for
 * still-valid prior surfaces of this task epoch, (2) flips to cumulative
 * coverage when the union closes the required roles, (3) records THIS call's
 * surfaces for future calls, (4) attaches frontier_index. Returns true when the
 * cumulative flip fired.
 */
function finalizePackServeState(
  workspace: string,
  result: TaskPackResult,
  effectiveQuery: string,
  profile: TaskProfile,
): boolean {
  // Route classification is a response affordance, not the caller's task
  // identity. A generic task can temporarily route through answer handles and
  // must still retain required-role evidence for its same-epoch cumulative
  // continuation; declared answer tasks remain excluded from edit closure.
  const cumulativeEligible = profile !== "answer" || result.profile_binding?.source !== "explicit";
  const surfaces = codeTaskPackSurfaces(result.surfaces);
  const currentPaths = new Set(surfaces.map((s) => s.path));
  const epochTokens = tokenizeForEpoch(effectiveQuery);

  // Consult BEFORE recording this call's surfaces (exclude the current paths).
  const prior = (cumulativeEligible && surfaces.length > 0)
    ? queryServedSurfaces(workspace, workspace, { excludePaths: currentPaths, epochTokens })
    : [];
  const flipped = applyCumulativeCoverage(result, prior, effectiveQuery);

  // iter-2 W3: served_earlier is attached ONCE — on the cumulative FLIP call —
  // not repeated on every subsequent cumulative pack (the iter-1 forensics paid
  // the same ~3.5KB list ×N). The model already holds the earlier surfaces after
  // the first flip; the frontier_index / coverage_basis still signal cumulative.
  if (flipped && result.served_earlier !== undefined && !shouldAttachServedEarlier(workspace)) {
    delete result.served_earlier;
  }

  // A1 (2026-08-01 signal5-2): EVERY content-bearing served surface — answer
  // packs included — grounds a later edit of that file. Feed the edit gate's
  // admissible union at serve time; ready-certificate install alone left
  // partial/discovery-pack surfaces permanently outside the frontier, so the
  // gate refused handles this server itself had served (T09 enums.ts).
  const admissibleServed = surfaces.filter(hasServedCode);
  if (admissibleServed.length > 0) {
    recordServedEditAdmissibility(workspace, {
      handles: admissibleServed
        .map((s) => s.handle)
        .filter((h): h is string => typeof h === "string" && h.length > 0),
      paths: admissibleServed.map((s) => s.path),
    });
  }

  // Record only CHANGE-pack surfaces (an answer pack has no edit closure to
  // cumulatively complete a later change pack from, and its read-only surfaces
  // should not stand in for edit-served roles).
  if (cumulativeEligible && surfaces.length > 0) {
    recordServedSurfaces(
      workspace,
      workspace,
      surfaces.map((s) => ({ path: s.path, role: s.role, handle: s.handle })),
      epochTokens,
    );
  }

  attachFrontierIndex(workspace, result, prior);
  return flipped;
}

/**
 * IMPROVEMENT A part 4: when >=2 edit-frontier surfaces are known and the pack
 * is ready to edit, tell the caller to batch every known edit site into ONE
 * edits[] call. Short, appended once to the edit route's reason.
 */
function annotateBatchEditFrontier(result: TaskPackResult): void {
  if (result.route?.action !== "edit_from_handles") return;
  const editSites = codeTaskPackSurfaces(result.surfaces).filter((s) => s.required !== false);
  if (editSites.length < 2) return;
  if (/one edits\[\] call/.test(result.route.reason)) return;
  result.route = {
    ...result.route,
    reason: `${result.route.reason}; batch all ${editSites.length} known edit sites into ONE edits[] call (multi-file allowed)`,
  };
}

type ExactReplacement =
  | { search: string; replace: string }
  | { identifierOldValue: string; replace: string };

// T1 (2026-08-27, v0.12 wave D2): a code-shaped identifier token adjacent to
// the literal(s) IN THE SAME MATCH, verified against explicitCodeIdentifiers'
// own "looks like code, not prose" heuristic (has `_`, camelCase, PascalCase,
// or ALL_CAPS) — never a plain English word. Values are restricted to
// numeric/boolean/sentinel literals (never a bare word), so an actual rename
// request ("X from oldName to newName") cannot be mistaken for a value edit.
// Every parse still dies safely downstream: attachSingleSiteUniqueMatchFastPath
// re-verifies the resolved search text against the served file and requires a
// single unique occurrence before ever proposing an edit.
const EXACT_REPLACEMENT_IDENT_SRC = "[A-Za-z_$][A-Za-z0-9_$]*";
const EXACT_REPLACEMENT_VALUE_SRC =
  "(?:[+-]?(?:\\d+\\.\\d+|\\d+)%?|true|false|null|undefined|on|off|enabled|disabled)";

/**
 * T1: resolve IDENT's current literal value from an "IDENT: value" / "IDENT =
 * value" assignment-shaped span inside already-served surface code, for a
 * query that named only the NEW value ("set PACKET_LOSS_FAILURE to 0.15").
 * Requires the identifier to be assigned in EXACTLY one place in the served
 * text — assigned more than once, or not found at all, is not a safe
 * single-site rewrite, so this returns undefined rather than guess.
 */
function resolveOldValueReplacement(
  code: string,
  identifier: string,
  newValue: string,
): { search: string; replace: string } | undefined {
  const escaped = identifier.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
  const re = new RegExp(
    `\\b${escaped}\\b(\\s*[:=]\\s*)(${EXACT_REPLACEMENT_VALUE_SRC})\\b`,
    "g",
  );
  const matches = [...code.matchAll(re)];
  if (matches.length !== 1) return undefined;
  const [wholeMatch, separator, oldValue] = matches[0]!;
  if (oldValue === newValue) return undefined;
  return { search: wholeMatch!, replace: `${identifier}${separator}${newValue}` };
}

/** Parse only user-authored, unambiguous replacement notation; never infer an edit from prose. */
function exactReplacementFromQuery(query: string): ExactReplacement | undefined {
  const found: Array<{ search?: string; replace: string; identifierOldValue?: string; selfEvidentMutation?: boolean }> = [];
  const add = (search: string | undefined, replace: string | undefined, selfEvidentMutation?: boolean): void => {
    if (search === undefined || replace === undefined || search.length === 0 || search === replace) return;
    if (Buffer.byteLength(search, "utf8") > 8192 || Buffer.byteLength(replace, "utf8") > 8192) return;
    found.push({ search, replace, selfEvidentMutation });
  };
  const addUnresolved = (identifierOldValue: string, replace: string): void => {
    if (identifierOldValue.length === 0 || replace.length === 0) return;
    if (Buffer.byteLength(replace, "utf8") > 8192) return;
    found.push({ identifierOldValue, replace, selfEvidentMutation: true });
  };
  for (const match of query.matchAll(/\breplace\s+`([^`\n]+)`\s+with\s+`([^`\n]+)`/giu)) add(match[1], match[2]);
  for (const match of query.matchAll(/\breplace\s+"([^"\n]+)"\s+with\s+"([^"\n]+)"/giu)) add(match[1], match[2]);
  for (const match of query.matchAll(/\breplace\s+'([^'\n]+)'\s+with\s+'([^'\n]+)'/giu)) add(match[1], match[2]);
  for (const match of query.matchAll(/「([^」\n]+)」(?:を)?「([^」\n]+)」(?:に)?(?:置換|変更|修正)/gu)) add(match[1], match[2]);
  for (const match of query.matchAll(/^(?!-{3})-([^\n]+)\n^(?!\+{3})\+([^\n]+)$/gmu)) add(match[1], match[2]);

  const codeShapedTokens = new Set(explicitCodeIdentifiers(query).map((token) => normIdent(token)));
  const isBoundIdentifier = (token: string): boolean => codeShapedTokens.has(normIdent(token));
  const IDENT = EXACT_REPLACEMENT_IDENT_SRC;
  const VALUE = EXACT_REPLACEMENT_VALUE_SRC;

  // Arrow form: `IDENT[:=|を] OLD (→|->|=>) NEW` — e.g. "PACKET_LOSS_FAILURE
  // を 0.12→0.15" or "TIMEOUT_MS: 30 -> 60".
  for (const match of query.matchAll(new RegExp(`\\b(${IDENT})\\s*(?:[:=]|を)?\\s*(${VALUE})\\s*(?:→|->|=>)\\s*(${VALUE})\\b`, "gu"))) {
    if (isBoundIdentifier(match[1]!)) add(match[2], match[3], true);
  }
  // "change|set|update IDENT from OLD to NEW" / bare "IDENT from OLD to NEW".
  // Excluded outright when the query separately reads as a rename request —
  // "from X to Y" is also how a caller phrases renaming an identifier, and a
  // rename is a multi-site concern this fast path must never approximate.
  if (!/\b(?:rename|renamed|renaming)\b/iu.test(query) && !/(?:改名|リネーム)/u.test(query)) {
    for (const match of query.matchAll(new RegExp(`\\b(?:change|set|update)?\\s*(${IDENT})\\s+from\\s+(${VALUE})\\s+to\\s+(${VALUE})\\b`, "giu"))) {
      if (isBoundIdentifier(match[1]!)) add(match[2], match[3], true);
    }
  }
  // JA "IDENTをOLDからNEWに/へ".
  for (const match of query.matchAll(new RegExp(`(${IDENT})を(${VALUE})から(${VALUE})(?:に|へ)`, "gu"))) {
    if (isBoundIdentifier(match[1]!)) add(match[2], match[3], true);
  }
  // "set IDENT to NEW" / JA "IDENTをNEWに" — old value is not stated in the
  // query; resolved later against the actually-served file.
  for (const match of query.matchAll(new RegExp(`\\bset\\s+(${IDENT})\\s+to\\s+(${VALUE})\\b`, "giu"))) {
    if (isBoundIdentifier(match[1]!)) addUnresolved(match[1]!, match[2]!);
  }
  for (const match of query.matchAll(new RegExp(`(${IDENT})を(${VALUE})に`, "gu"))) {
    if (isBoundIdentifier(match[1]!)) addUnresolved(match[1]!, match[2]!);
  }

  if (found.length !== 1) return undefined;
  const replacement = found[0]!;
  const explicitMutation = replacement.selfEvidentMutation === true
    || /\b(?:apply|replace|change|update|fix)\b/iu.test(query)
    || /(?:適用|置換|変更|修正)/u.test(query);
  if (!explicitMutation) return undefined;
  return replacement.identifierOldValue !== undefined
    ? { identifierOldValue: replacement.identifierOldValue, replace: replacement.replace }
    : { search: replacement.search!, replace: replacement.replace };
}

function attachSingleSiteUniqueMatchFastPath(
  result: TaskPackResult,
  workspace: string,
  query: string,
  profile: TaskProfile,
  cache?: FileReadCache,
): void {
  delete result.fast_path;
  if (profile !== "generic" || result.route?.action !== "edit_from_handles") return;
  if (result.coverage !== "focused" && result.coverage !== "complete") return;
  if (result.missing.length !== 0 || result.concern_ambiguities?.length) return;
  if (hasIndependentEditConcernClauses(query) || hasExplicitIndependentConcernMarker(query)) return;
  // Field-eval fix (2026-08-27, v0.12 wave D2): "X, and ALSO refactor Y" used
  // to slip past this fence -- the optional connector only ever recognized a
  // bare "and", so an interposed filler word left the verb unreached. Found
  // via T2's own fail-open test, which reuses this exact fence.
  if (/(?:,|;|\n)\s*(?:and\s+)?(?:also\s+)?(?:fix|add|implement|change|replace|rename|refactor|wire|connect|update|create)\b/iu.test(query)
    || /(?:、|。|\n)\s*(?:さらに|加えて)?[^\n]{0,80}(?:修正|追加|実装|変更|置換|改名|接続|作成)/u.test(query)) return;
  const contract = result.execution_contract;
  if (contract?.typestate.phase !== "prepared" || !contract.typestate.allowed_actions.includes("edit")) return;
  const parsed = exactReplacementFromQuery(query);
  if (parsed === undefined) return;
  const surfaces = codeTaskPackSurfaces(result.surfaces);
  if (surfaces.length !== 1 || result.surfaces.length !== 1) return;
  const surface = surfaces[0]!;
  if (surface.required === false || surface.code === undefined) return;
  const replacement = "identifierOldValue" in parsed
    ? resolveOldValueReplacement(surface.code, parsed.identifierOldValue, parsed.replace)
    : parsed;
  // A fast-path receipt repeats both payloads; keep it genuinely small even
  // where the surrounding pack has headroom. Large replacements retain the
  // normal handle-first path and cannot create a post-trim cap inversion.
  if (replacement === undefined || Buffer.byteLength(replacement.search, "utf8") + Buffer.byteLength(replacement.replace, "utf8") > 4096) return;
  if (!surface.code.includes(replacement.search)) return;
  const resolved = safeResolve(surface.path, workspace);
  if (resolved === undefined) return;
  let real: string;
  try {
    real = fs.realpathSync(resolved);
    if (!isWithin(real, resolveReal(workspace)) || !fs.statSync(real).isFile()) return;
    fs.accessSync(real, fs.constants.W_OK);
  } catch {
    return;
  }
  const source = readCached(workspace, surface.path, cache);
  if (source === undefined || source.split(replacement.search).length - 1 !== 1) return;
  const certificate = result.execution_contract?.readiness_certificate;
  const taskId = result.qref ?? surface.handle;
  result.fast_path = {
    kind: "single-site-unique-match",
    handle: surface.handle,
    path: surface.path,
    search: replacement.search,
    replace: replacement.replace,
    precondition: "unique-match",
    root_bound: true,
    occurrence_count: 1,
    ...(certificate?.id !== undefined
      ? { certificate: {
          id: certificate.id,
          sha: certificate.task_fingerprint.startsWith("sha256:")
            ? certificate.task_fingerprint
            : `sha256:${certificate.task_fingerprint}`,
        } }
      : {}),
    task: { id: taskId },
  };
  // The fast path is a proof-carrying edit recipe; re-send no source body or
  // likely_edits that the normal evidence surface already carried.
  surface.code = undefined;
  surface.facts = undefined;
  surface.likely_edits = undefined;
  surface.done_check = undefined;
  // This duplicates only the bounded recipe/proof, so recheck the hard cap.
  if (!fitsInCap(result)) delete result.fast_path;
}

/** No-install verification guidance shared by every "not runnable" verdict. */
const VERIFY_NO_INSTALL_SUGGESTION =
  "verify by reviewing the diff against served evidence; do not install dependencies unless the task explicitly requires running tests";

/**
 * A C/C++ surface set's verdict comes from the SAME host probe the edit-time
 * verification manifest uses (util/verificationPack.ts `probeToolchain`), never
 * from an npm script: the 2026-07-30 forensics caught a firmware fix task being
 * told verification was unavailable while clang++ sat on PATH, and another being
 * handed `npm test` because an ancestor directory held an unrelated
 * package.json. A native `command` names the compiler plus whichever build
 * system (if any) actually exists — it is a starting point for a standalone
 * harness, not a canned one-liner, and says so.
 */
function nativeVerificationVerdict(
  workspace: string,
  paths: string[],
): NonNullable<TaskPackResult["verification"]> {
  const { cxx, cc } = probeToolchain();
  const compiler = cxx ?? cc;
  if (compiler === undefined) {
    return { available: false, reason: "no C/C++ toolchain found", suggestion: VERIFY_NO_INSTALL_SUGGESTION };
  }
  let buildEntry: string | undefined;
  for (const rel of paths) {
    const dir = path.posix.dirname(rel);
    buildEntry = findBuildEntry(workspace, dir === "." ? "" : dir);
    if (buildEntry !== undefined) break;
  }
  return {
    available: true,
    command: buildEntry === undefined
      ? `${compiler} (standalone harness; no project build system detected)`
      : `${compiler} (project build system at ${buildEntry})`,
  };
}

/**
 * IMPROVEMENT D: stat the served surfaces' ACTUAL toolchain and return a
 * machine-readable runnability verdict. The surface set's own language decides
 * which toolchain is even asked about (`surfaceToolchainDomain`) — native:
 * host compiler probe; python: pyproject/pytest config + an installed runner
 * marker; node: nearest package.json with a scripts.test + node_modules
 * presence. `available:false, reason:"dependencies not installed"` tells the
 * agent NOT to burn turns installing a toolchain the checkout was never set up
 * to run. Trivial fs cost (statKindCached). Returns undefined when there is no
 * verification toolchain to speak to at all.
 */
function buildVerificationVerdict(
  workspace: string,
  paths: string[],
): NonNullable<TaskPackResult["verification"]> | undefined {
  const domain = surfaceToolchainDomain(paths);
  if (domain === "native") return nativeVerificationVerdict(workspace, paths);
  if (domain === "python") {
    const pyConfig =
      statKindCached(workspace, workspace, "pyproject.toml") === "file"
      || ["pytest.ini", "tox.ini", "setup.cfg"].some((f) => statKindCached(workspace, workspace, f) === "file")
      || paths.some((p) => statKindCached(workspace, workspace, `${path.posix.dirname(p)}/pyproject.toml`) === "file");
    if (!pyConfig) return undefined;
    const runner = anyFileExistsCached(workspace, workspace, [
      ".venv/bin/pytest", "venv/bin/pytest", ".venv/bin/python", "venv/bin/python",
    ]);
    return runner
      ? { available: true, command: "pytest" }
      : { available: false, reason: "dependencies not installed", suggestion: VERIFY_NO_INSTALL_SUGGESTION };
  }
  if (domain !== "node") return undefined;
  for (const rel of paths) {
    const found = findNearestNpmScripts(workspace, rel);
    if (found?.pkg.scripts?.test !== undefined) {
      const { dir } = found;
      const nodeModules = dir ? `${dir}/node_modules` : "node_modules";
      const installed = statKindCached(workspace, workspace, nodeModules) === "dir";
      const cmd = dir ? `cd ${dir} && npm test` : "npm test";
      return installed
        ? { available: true, command: cmd }
        : { available: false, reason: "dependencies not installed", suggestion: VERIFY_NO_INSTALL_SUGGESTION };
    }
  }
  return undefined;
}

/** IMPROVEMENT D: attach result.verification for edit-shaped packs with surfaces. */
function attachVerificationVerdict(workspace: string, result: TaskPackResult): void {
  if (result.route?.action === "answer_from_handles") return;
  const surfaces = codeTaskPackSurfaces(result.surfaces);
  if (surfaces.length === 0) return;
  const root = dominantRootOf(surfaces);
  const paths = surfaces.map((s) => s.path).filter((p) => projectRootOf(p) === root);
  const verdict = buildVerificationVerdict(workspace, paths.length > 0 ? paths : surfaces.map((s) => s.path));
  if (verdict === undefined) return;
  // iter-2 W3: attach the verdict ONCE per session (first edit pack), and again
  // only when it CHANGED (e.g. dependencies were installed). The iter-1
  // forensics paid the identical ~209B verdict on 7 packs for zero behavior
  // change; a repeat of the same verdict is suppressed here.
  const sig = JSON.stringify(verdict);
  if (!shouldAttachVerification(workspace, sig)) return;
  result.verification = verdict;
}

function dedupeTrimAndPersist(
  workspace: string,
  result: TaskPackResult,
  opts?: {
    protectedSurfaces?: ReadonlySet<TaskPackSurface>;
    query?: string;
    checkRecords?: PackCheckRecord[];
    /** Behavior 3: the original request args, so this choke point can capture the served-pack essentials for a future exact re-call dedup. Omit to skip capture (a caller with nothing to dedup against). */
    args?: TaskPackArgs;
    /**
     * DESIGN-v0.9 §4.3: recompute inputs. When supplied AND trimToCap flips
     * coverage (whole-surface drop), route/next are recomputed against the
     * post-trim state. The three locate-derived flows (buildTaskPack,
     * buildSeededTaskPack, buildPartialPack) pass this; buildDiffTaskPack
     * deliberately omits it to keep its hardcoded next (§4.5).
     */
    recompute?: { query: string; missingRequired: string[]; unmatchedConcern: string[] };
    /**
     * DESIGN-v0.9 §4.6b: the pack's shared FileReadCache, so the codeless-
     * surface internal execution can re-slice bodies without extra disk reads.
     * The three locate-derived flows (buildTaskPack, buildSeededTaskPack,
     * buildPartialPack) pass it; buildDiffTaskPack omits it (its surfaces are
     * already embedded via centeredSliceForCap and it keeps its hardcoded next,
     * §4.5), so it never inlines here.
     */
    cache?: FileReadCache;
    evidenceExpansion?: PersistedEvidenceExpansion;
    /** artifact_build already attached its ranked discovery surfaces. */
    artifactSurfacesAlreadyAttached?: boolean;
  },
): TaskPackResult {
  const effectiveQuery = opts?.query
    ?? opts?.args?.query
    ?? opts?.args?.symbol
    ?? opts?.args?.path
    ?? "";
  const servedZoomScope = servedZoomScopeForArgs(opts?.args, effectiveQuery);
  let semanticCheckRecords: PackCheckRecord[] = [];
  let binding = bindTaskProfile(opts?.args?.taskProfile, effectiveQuery);
  let profile = binding.selected;
  result.profile_binding = binding;
  if (profile !== "generic") result.task_profile = profile;
  if (
    profile === "answer"
    && result.route?.action === "edit_from_handles"
  ) {
    result.route = {
      action: "answer_from_handles",
      reason: "validated answer profile — no edit expected",
      max_additional_tl_calls: 0,
    };
    delete result.next;
    delete result.continuation;
  }
  if (
    profile !== "answer"
    && binding.requested !== "auto"
    && binding.requested !== "answer"
    && result.route?.action === "answer_from_handles"
  ) {
    result.route = {
      action: "edit_from_handles",
      reason: `caller-declared ${profile} change profile — structural readiness still required`,
      max_additional_tl_calls: 0,
    };
  }

  // 2026-07-12d answer-pack purification: route:"answer_from_handles" is a
  // promise that no edit is expected (see editFromHandlesRoute's own
  // isAnswerIntentQuery gate, shared by every buildRoute caller) — the pack
  // must not then turn around and carry edit-flavored payload. likely_edits
  // (per-surface "here is how to change this"), checks[] (closure/wiring
  // completion checks), and verify[] (test/build commands) are all
  // edit-shaped content with nothing honest to say to a pack that was never
  // asked to produce an edit. dedupeTrimAndPersist is the single choke point
  // every builder (buildTaskPack, buildSeededTaskPack, buildPartialPack,
  // buildDiffTaskPack) already funnels through, so gating here covers
  // route:"answer_from_handles" regardless of which flow produced it — no
  // per-flow duplication needed.
  if (result.route?.action === "answer_from_handles") {
    delete result.next;
    delete result.continuation;
    for (const surf of codeTaskPackSurfaces(result.surfaces)) {
      delete surf.likely_edits;
      delete surf.edit_intent;
      delete surf.done_check;
    }
    delete result.checks;
    delete result.verify;
  }
  result.surfaces = dedupeAgentGuideSurfaces(result.surfaces, opts?.args);
  // PI-09 close-out: both SURFACE-level body strips below replace real bodies
  // with a `code_unchanged` pointer at "you already hold this". Under
  // `force_serve` that premise is exactly what the caller has denied, and a
  // pack that skipped the three RESPONSE-level receipts only to hand back
  // pointer strings would honour the argument in name and not in bytes — the
  // failure mode this whole close-out exists to remove.
  //
  // The intra-pack half of applyPackDedupe (a surface duplicating an EARLIER
  // surface in the SAME response) is suppressed with it, and deliberately: the
  // response then repeats those bytes, which is what "send it again" means
  // when the caller cannot be assumed to have read the first copy.
  const forceServePack = opts?.args?.forceServe === true;
  if (!forceServePack) {
    applyPackDedupe(workspace, result);
    // iter-3 F3: after the prior-pack (handle-keyed) dedup, strip any NON-required
    // body whose exact (path,range) was already served this epoch and is
    // byte-unchanged — the cross-pack, content-addressed case applyPackDedupe's
    // single-slot handle match misses (a shared file resurfacing two packs later or
    // under a fresh handle). Runs pre-trim so the freed bytes feed the trim ladder.
    applyResidentFileDedup(workspace, result, tokenizeForEpoch(effectiveQuery));
  }

  let evidencePromotedWiring: TaskWiringProfile | undefined;
  // D10 (2026-08-14): `TL_EVIDENCE_RELATIONS` / `TL_SEMANTIC_WIRING` are deleted;
  // evidence-driven profile promotion is unconditional.
  if (
    (
      profile === "generic"
      || (
        profile === "multi_concern"
        && result.concerns?.some((concern) => concern.status !== "covered")
      )
    )
    && (hasRequestedMutationIntent(effectiveQuery) || profile === "multi_concern")
    && result.route?.action !== "answer_from_handles"
  ) {
    // Probe on a copy because a failed profile promotion must not silently
    // widen the caller's surface set. A successful graph-backed promotion
    // adopts the bounded endpoint closure built by the probe.
    const probeSurfaces = result.surfaces.map((surface) => ({ ...surface }));
    const candidate = profile === "multi_concern"
      ? buildSemanticTaskWiringProfile(effectiveQuery, probeSurfaces, workspace, opts?.cache)
      : buildTaskWiringProfile(effectiveQuery, probeSurfaces, workspace, opts?.cache);
    if (candidate !== undefined) {
    const connection = candidate.connections[0];
    const crossRelations = candidate.evidence_graph?.relations.filter((relation) =>
      relation.kind === "imports" || relation.kind === "references" || relation.kind === "direct_calls"
    ) ?? [];
    const callableInsertionEvidence = connection?.source !== undefined
      && connection.source.evidence.includes("workspace-definition")
      && hasCallableDeclarationSibling(
        connection.source.token,
        connection.source.path,
        workspace,
        opts?.cache,
      )
      && connection.destination !== undefined
      && connection.insertion_handle === connection.destination.handle
      && connection.supporting_handles.length > 0
      && connection.evidence.includes("callable-insertion-site");
    if (
      candidate.status === "ready"
      && connection?.source !== undefined
      && connection.destination !== undefined
      && connection.source.handle !== connection.destination.handle
      && (crossRelations.length > 0 || callableInsertionEvidence)
    ) {
      result.surfaces = probeSurfaces;
      evidencePromotedWiring = candidate;
      profile = "wiring";
      // Generic role guesses are no longer authoritative after the graph has
      // proved the wiring-specific producer/consumer obligations.
      result.missing = [];
      delete result.missing_required_surfaces;
      if (result.concerns) {
        const evidenceHandles = [...new Set([
          ...candidate.edit_frontier,
          ...candidate.review_frontier,
        ])];
        for (const concern of result.concerns) {
          concern.status = "covered";
          concern.handles = evidenceHandles;
        }
        delete result.concern_ambiguities;
      }
      if (result.coverage_reason === "candidate-list" || result.coverage_reason === "concerns-uncovered") {
        // The graph resolved the locator tie with an observed directional
        // relation; keeping choose-candidate would discard that evidence.
        result.coverage = "complete";
        delete result.coverage_reason;
        delete result.content_sufficiency;
        delete result.blocking_next_steps;
      }
      binding = {
        requested: binding.requested,
        selected: "wiring",
        source: "evidence",
        confidence: crossRelations.length > 0
          ? Math.min(0.97, Math.max(...crossRelations.map((relation) => relation.confidence)))
          : 0.9,
        reason: crossRelations.length > 0
          ? "served repository evidence proves a producer-to-consumer relation"
          : "unique producer definition and callable consumer insertion site close the new connection",
      };
      result.profile_binding = binding;
      result.task_profile = "wiring";
    }
    }
  }

  // A wiring profile is a structured projection over the same conservative
  // locator/closure evidence used by the generic pack. It can add a missing
  // endpoint surface, but never generates the connecting code itself.
  if (profile === "wiring" && result.route?.action !== "answer_from_handles") {
    const wiring = evidencePromotedWiring ?? buildTaskWiringProfile(
      effectiveQuery,
      result.surfaces,
      workspace,
      opts?.cache,
    );
    result.wiring = wiring;
    pruneIrrelevantWiringImplementationSurfaces(result);
    if (wiring.status === "ready" && wiring.evidence_graph !== undefined) {
      result.missing = [];
      delete result.missing_required_surfaces;
      if (result.coverage_reason === "candidate-list") {
        result.coverage = "complete";
        delete result.coverage_reason;
        delete result.content_sufficiency;
      }
    }
    if (wiring.status === "ready" && result.missing.length === 0) {
      result.route = {
        action: "edit_from_handles",
        reason: "wiring source, consumer, and insertion handle are bundled",
        max_additional_tl_calls: 0,
      };
      delete result.next;
      delete result.continuation;
    } else {
      // Endpoint sweeps run ONLY on caller-typed identifier-shaped tokens the
      // pack could NOT ground in its served surfaces. 2026-07-31 forensics
      // (T13 rep0): follow-up detail queries under a pinned wiring profile
      // ("<struct> fields (accel gyro) … for update()") carry no unresolved
      // identifier; tokens.at(-1) plus the effectiveQuery fallback
      // manufactured `references query="for"` sweeps while the pack itself
      // was coverage:"complete" — a self-contradictory route ("complete" +
      // "missing wiring endpoint") that pushed solvers off TL for the rest of
      // the cell. An UNRESOLVED identifier (named by the caller, absent from
      // the served set — e.g. the not-yet-located consumer) is exactly what
      // the born-dead gate must keep sweeping; when every query identifier
      // grounds, the wiring layer leaves the coverage pipeline's route alone
      // and the finalizer's annotateUnsweptWiringEndpoints says why.
      const unresolvedIdentifiers = unmatchedConcernTokens(
        effectiveQuery,
        codeTaskPackSurfaces(result.surfaces),
      ).filter(isWiringIdentifierShape);
      const sourceToken = unresolvedIdentifiers[0];
      const destinationToken = unresolvedIdentifiers.length > 1
        ? unresolvedIdentifiers.at(-1)
        : unresolvedIdentifiers[0];
      const calls: ContinuationCall[] = [];
      for (const missing of wiring.missing) {
        const token = missing === "source" ? sourceToken : destinationToken;
        if (token === undefined) continue;
        calls.push({
          tool: "search_files",
          arguments: {
            action: missing === "source" ? "find" : "references",
            query: token,
          },
          purpose: missing === "source" ? "locate wiring producer" : "locate wiring consumer",
        });
      }
      if (calls.length > 0) {
        result.route = {
          action: "locate_missing_surfaces",
          reason: "missing wiring endpoint(s): " + wiring.missing.join(","),
          max_additional_tl_calls: calls.length,
        };
        const plan = enforceContinuationBudget({
          version: 1,
          stages: [{ execution: "parallel", calls }],
        });
        if (plan !== undefined) {
          result.continuation = plan;
          const next = deriveNextFromPlan(plan);
          if (next !== undefined) result.next = next;
        }
      }
      // No else: annotateUnsweptWiringEndpoints (the emit-time finalizer
      // invariant) explains a missing-endpoint state whenever no sweep
      // survives — later trim/reconcile passes can strip a sweep planned
      // here, so a branch-local note would under-cover.
    }
  }
  // DESIGN-v0.9 §4.6b internal execution — BEFORE trimToCap so the §4.3 trim
  // inversion strips any over-budget inlined body (not a handle). Only the
  // locate flows pass `cache`; the diff flow omits it and never inlines here.
  if (opts?.cache !== undefined && result.route?.action !== "answer_from_handles") {
    const inlinedCount = inlineCodelessSurfaces(result, workspace, opts.cache);
    // The `next` was computed against the PRE-inline codeless set (a
    // `read_file handles=[...]` batch); once bodies are inlined that batch is
    // stale, so recompute it against the post-inline surfaces. Route is
    // unchanged (a complete/focused pack stays edit_from_handles), so only
    // `next` is refreshed. Skipped when nothing inlined.
    if (inlinedCount > 0 && opts.recompute !== undefined) {
      const nh = nextHintForCoverage(
        codeTaskPackSurfaces(result.surfaces), result.coverage, result.coverage_reason,
        opts.recompute.query, opts.recompute.missingRequired, opts.recompute.unmatchedConcern,
      );
      if (nh !== undefined) result.next = nh;
      else delete result.next;
    }
  }
  const coverageBefore = result.coverage;
  if (
    opts?.args !== undefined
    && opts.artifactSurfacesAlreadyAttached !== true
    && result.route?.action !== "answer_from_handles"
    // 残Stage2 (2026-08-01 probe F1): this wire choke point appended
    // query-token-matched artifact files to EVERY pack — a code question
    // mentioning "skeleton" picked up a microbench CSV as a surface. Gate on
    // the same caller-spoke-artifacts test prefetch already uses; the T10
    // caller-NAMED artifact path in paths[] keeps passing it.
    && mayNameCallerArtifact(opts.args, effectiveQuery)
  ) {
    const artifacts = collectArtifactSurfaces(opts.args, workspace);
    const seenPaths = new Set(result.surfaces.map((surface) => surface.path));
    (result.surfaces as TaskPackResultSurface[]).push(
      ...artifacts.filter((surface) => !seenPaths.has(surface.path)),
    );
  }
  // A1: a resolved, CALLER-NAMED artifact surface must carry its extracted text,
  // not just a size + an `extract` call. Attached HERE — after the surfaces
  // exist, before coverage/readiness/the execution contract are computed — so
  // the certificate is decided over the artifact content this pack actually
  // serves (a post-exit inline would ship content beside an "ask the user"
  // contract). No-op for artifact_build (it bundles its own sections) and for
  // any pack whose request named no artifact.
  attachPrefetchedArtifactSections(result, workspace);
  if (
    effectiveQuery.length > 0
    && shouldBuildTaskChangeContract(effectiveQuery, result, profile)
  ) {
    result.change_contract = buildTaskChangeContract(effectiveQuery, result, workspace);
  }
  // A provisional ready route is not authoritative until a bounded negative
  // retrieval pass has tried to disprove it. Counterexamples are embedded in
  // this same response, then every proof/risk decision below is recomputed
  // over the enriched surface set. This spends server I/O, not another agent
  // turn, and is deliberately capped at two surfaces / 120 files.
  const artifactTargetNames = new Set([
    ...moduleShapedQueryTokens(effectiveQuery).map(normIdent),
    ...(effectiveQuery.match(/[A-Za-z_$][A-Za-z0-9_$.-]*\.(?:c|cc|cpp|cxx|go|java|js|jsx|py|rs|ts|tsx)\b/gi) ?? [])
      .map((fileName) => normIdent(path.basename(fileName, path.extname(fileName)))),
  ]);
  const exactArtifactTargetServed = profile === "artifact_build"
    && hasRequiredArtifactSections(result, effectiveQuery)
    && (
      result.create_target !== undefined
      || codeTaskPackSurfaces(result.surfaces).some((surface) =>
        artifactTargetNames.has(normIdent(path.basename(surface.path, path.extname(surface.path))))
        && hasServedCode(surface)
      )
    );
  const exactAnswerTargetServed = profile === "answer"
    && (
      result.answer_resolution !== undefined
      // B1d (2026-08-01 retrieval-scope): `workspace` is what lets the facet
      // check see that a facet's call sites are still in remaining_ranges.
      || hasExactNamedAnswerFacetEvidence(result, effectiveQuery, workspace)
    );
  const falsificationAdded = exactArtifactTargetServed || exactAnswerTargetServed
    ? 0
    : runInternalReadinessFalsification(
        result,
        profile,
        effectiveQuery,
        workspace,
        opts?.args?.paths?.map((entry) => typeof entry === "string" ? entry : entry.path),
      );
  if (falsificationAdded > 0) {
    // Counterexample retrieval can reveal the real wiring consumer after an
    // API/header was provisionally chosen as the endpoint. Rebuild all
    // derived frontiers before certifying the enriched evidence set.
    refreshAfterRecursiveClosure(result, profile, effectiveQuery, workspace, opts?.cache);
  }
  if (
    falsificationAdded > 0
    && effectiveQuery.length > 0
    && shouldBuildTaskChangeContract(effectiveQuery, result, profile)
  ) {
    result.change_contract = buildTaskChangeContract(effectiveQuery, result, workspace);
  }
  if (profile !== "answer") {
    // Answer packs need responsibility evidence, not transitive edit closure.
    // Expanding imports from an explanatory surface can pull unrelated
    // exceptions/serializers into the pack and displace the actual structural
    // map under the byte cap.
    runRecursiveReadOnlyClosure(
      result,
      profile,
      effectiveQuery,
      workspace,
      opts?.cache,
      [
        ...(typeof opts?.args?.path === "string" ? [opts.args.path] : []),
        ...(opts?.args?.paths?.map((entry) => typeof entry === "string" ? entry : entry.path) ?? []),
      ],
    );
  }
  // For a broad independent-concern pack, the per-surface concern map and
  // execution certificate already encode the action frontier. A second
  // change_contract repeats every handle and can force a required header or
  // the authoritative typestate out of the fixed response budget. Keep the
  // richer staged contract for smaller packs, where it still fits cheaply.
  if (
    profile === "multi_concern"
    && result.wiring?.strategy !== "semantic-multihop"
    && codeTaskPackSurfaces(result.surfaces).filter((surface) => surface.required === true).length >= 5
  ) {
    result.change_contract = undefined;
  }
  // W3 (turn-economy wave 2): a create-intent query that NAMES a not-yet-existing
  // file must answer with the create ROUTE — create_target + an `edit_file
  // create:true` next, alongside whatever related context the pack already
  // served — never a contentless "search for it" envelope (E3: a clear "Add a
  // new pytest regression test file tests/…" returned ~511 bytes with no
  // coverage/surfaces/create_target twice in a row). create_target is resolved
  // HERE (before the execution contract is built) so buildReadinessObligations'
  // provedCreate treats the new-file frontier as an actionable target and the
  // contract certifies prepared, instead of falling to needs-followup and
  // demoting the create route. The concrete `next`/`route` are re-asserted
  // AFTER the contract machinery (which drops a prepared pack's discovery
  // `next`) — see the assertCreateRoute call below. Idempotent: skipped when a
  // flow (e.g. artifact_build) already resolved create_target, and never on an
  // answer pack (a create is a mutation).
  if (
    result.create_target === undefined
    && profile !== "artifact_build"
    && result.route?.action !== "answer_from_handles"
    && effectiveQuery.length > 0
  ) {
    const createTarget = resolveCreateTargetFromQuery(effectiveQuery, workspace, result.surfaces);
    if (createTarget !== undefined) {
      result.create_target = createTarget;
      // The new file IS the self-complete action frontier: its path came from
      // the caller's explicit request and its parent directory exists, so the
      // pack is a single confident create site (not an "uncovered concern").
      // Set the terminal create route HERE so the contract certifies prepared
      // rather than needs-followup (which would demote it back to "search").
      result.coverage = "focused";
      result.coverage_reason = "single-site";
      result.missing = [];
      result.content_sufficiency = undefined;
      // The old "go find the file that doesn't exist yet" guidance is exactly
      // the manufactured extra turn this create route replaces.
      result.blocking_next_steps = undefined;
      result.create_note = undefined; // superseded by the concrete create_target
      result.change_contract = undefined; // the create frontier supersedes it
      result.route = {
        action: "edit_from_handles",
        reason: "requested new file resolved; create it with edit_file create:true, imitating the served sibling(s)/module",
        max_additional_tl_calls: 0,
      };
      result.next = `edit_file create:true path=${createTarget.path}`;
    }
  }
  // IMPROVEMENT A: consult/record the session-stateful served-surface log,
  // flip coverage to cumulative-complete when the union of this call's surfaces
  // and still-valid prior surfaces closes every required role, and attach the
  // complete-working-set frontier_index. Runs BEFORE the contract build so a
  // cumulative flip certifies exactly like a single-call complete pack. When it
  // fires, the remaining proof is a plain surface-content proof over the served
  // surfaces (the role-specific structure was proven across the earlier calls,
  // which the stateless contract builder cannot re-verify), so the contract is
  // certified under the generic obligation set.
  // B1b (2026-08-01 retrieval-scope): enforce the missing-roles naming
  // invariant BEFORE the contract/route machinery reads it, so a repaired
  // (or dropped) reason is what the readiness proof and `next` are built from.
  reconcileMissingRolesContract(result);
  semanticCheckRecords = semanticCompletionCheckRecords(result.wiring, result.surfaces);
  if (semanticCheckRecords.length > 0) {
    result.checks = semanticCheckRecords.map((check) => check.desc);
  }
  // T-L1: for change tasks only, enrich the already-selected C/C++ frontier
  // with declaration authority from direct project-local includes. This runs
  // before serve-state persistence and trimming so complete bodies are booked
  // normally, while over-budget handle-only tails remain honestly unserved.
  if (
    interfaceAuthorityEnabled()
    && profile !== "answer"
    && result.route?.action !== "answer_from_handles"
  ) {
    const currentSurfaces = codeTaskPackSurfaces(result.surfaces);
    const knownSurfaceKeys = new Set(
      currentSurfaces.map((surface) => JSON.stringify([surface.path, surface.range ?? ""])),
    );
    const authoritySurfaces = buildInterfaceAuthoritySurfaces({
      workspace,
      frontier: currentSurfaces,
    }).filter((surface) => !knownSurfaceKeys.has(JSON.stringify([surface.path, surface.range ?? ""])));
    if (authoritySurfaces.length > 0) {
      result.surfaces = [...result.surfaces, ...authoritySurfaces];
    }
  }
  const cumulativeComplete = finalizePackServeState(workspace, result, effectiveQuery, profile);
  // IMPROVEMENT D: honest, machine-readable verification runnability verdict.
  attachVerificationVerdict(workspace, result);
  const workspaceInventory = computeWorkspaceInventoryState(workspace);
  result.execution_contract = contractForTrim(
    buildTaskExecutionContract(
      result,
      cumulativeComplete ? "generic" : profile,
      effectiveQuery,
      buildTaskWorkspaceState(result, workspace, workspaceInventory),
      workspace,
      false,
      servedZoomScope,
    ),
  );
  // IMPROVEMENT A part 4: a prepared edit pack with >=2 known edit sites should
  // be closed in ONE edits[] batch, not site-by-site — say so on the route.
  // Wiring packs are exempt: their writable set is the edit_frontier alone, so
  // a "batch all N sites" nudge would contradict the wiring write protocol
  // (and its presence must not depend on which wiring flow assembled the pack).
  if (result.execution_contract?.typestate.phase === "prepared" && result.wiring === undefined) {
    annotateBatchEditFrontier(result);
  }
  if (result.wiring?.strategy === "semantic-multihop") {
    // The readiness proof still needs the generic contract while it evaluates
    // the edit frontier. Once certified, wiring itself carries the same
    // handle-backed obligations, so omit only the serialized duplicate.
    result.change_contract = undefined;
  }
  if (
    result.wiring?.strategy === "semantic-multihop"
    && result.execution_contract?.typestate.phase === "prepared"
  ) {
    // The semantic frontier supersedes locator-era discovery checks. Keeping
    // those checks beside a prepared/edit-only contract gives clients two
    // contradictory next actions and reopens the follow-up loop this pack
    // already closed.
    if (semanticCheckRecords.length === 0) delete result.checks;
  }
  const trimmed = trimToCap(
    result,
    opts?.protectedSurfaces,
    prefetchedCallerNamedArtifacts(workspace),
  );
  // DESIGN-v0.9 §4.3 post-trim recompute: a whole-surface drop that FLIPS
  // coverage invalidates the next/route computed against the fuller pre-trim
  // pack (the classic stale "you're done" next on a now-partial pack).
  // IMPROVEMENT A: a cumulative-coverage flip (coverage_basis:"cumulative") is a
  // DELIBERATE partial->complete promotion the recompute must not undo — it
  // recomputes route/next from the pre-cumulative `missingRequired`, which would
  // resurrect the very locate_missing_surfaces route cumulative coverage closes.
  if (
    opts?.recompute !== undefined
    && trimmed.coverage !== coverageBefore
    && trimmed.coverage_basis === undefined
  ) {
    recomputeRouteAndNextAfterTrim(trimmed, opts.recompute);
  }
  // DESIGN-v0.9 §4.4 rollup: stamped BEFORE annotateSpannedRoots so its
  // fitsInCap accounting sheds the advisory `roots`/route-suffix first under
  // any residual byte pressure (content_completeness is load-bearing honesty,
  // never shed).
  stampRollupContentCompleteness(trimmed);
  reconcileTaskChangeContract(trimmed);
  reconcileContentSufficiency(trimmed);
  if (
    profile === "answer"
    && (
      // R3 (2026-08-01, B1d completion): the answer_resolution arm is now
      // gated on its (post-B1d honest) matched_facets covering every
      // requested facet — an answer_resolution whose evidence covers only
      // SOME of the queried behaviors must not delete `next` and pin the
      // call budget to 0 over the uncovered rest (the T03 contradiction in
      // its second form). Facet-less queries keep the old behavior: no
      // requested facets means nothing to be uncovered.
      (
        trimmed.answer_resolution !== undefined
        && requiredQueryFacets(effectiveQuery).every((facet) =>
          (trimmed.answer_resolution?.matched_facets ?? []).some((m) =>
            m.toLowerCase() === facet.toLowerCase()))
      )
      // B1d (2026-08-01 retrieval-scope): THIS is the branch that deletes
      // `next`/`continuation` and pins max_additional_tl_calls to 0 on an
      // answer pack. Claiming "every requested behavior facet is bundled" while
      // a facet's own call sites sit unserved in remaining_ranges is exactly
      // the T03 contradiction — `workspace` is what makes the claim checkable.
      || hasExactNamedAnswerFacetEvidence(trimmed, effectiveQuery, workspace)
    )
  ) {
    // The answer's named target and every requested behavioral facet are
    // already served. Preserve the surface-level partial stamp (the whole
    // large class is intentionally not dumped), but do not reopen discovery
    // merely to fetch unrelated members from that class.
    delete trimmed.content_sufficiency;
    delete trimmed.blocking_next_steps;
    delete trimmed.next;
    delete trimmed.continuation;
    trimmed.route = {
      action: "answer_from_handles",
      reason: "named implementation and requested behavior facets are bundled",
      max_additional_tl_calls: 0,
    };
  }
  // A1 (2026-07-30): an ANSWER pack whose evidence IS caller-named artifact
  // content has nothing left to locate — the inlined sections are the answer's
  // source. buildRoute's zero-code-surface branch labels it "no editable handles
  // returned", which with 0 remaining discovery calls certifies
  // `request-user-input` while the pack holds the exact content the request asked
  // about (T14 two-deck audit). Gated on every caller-named artifact carrying a
  // MEANINGFUL section (hasRequiredArtifactSections) and on `missing` being empty,
  // so a genuinely incomplete pack still routes to discovery.
  if (
    profile === "answer"
    && codeTaskPackSurfaces(trimmed.surfaces).length === 0
    && (trimmed.artifact_sections?.length ?? 0) > 0
    && trimmed.missing.length === 0
    && hasRequiredArtifactSections(trimmed, effectiveQuery)
  ) {
    delete trimmed.content_sufficiency;
    delete trimmed.blocking_next_steps;
    delete trimmed.next;
    delete trimmed.continuation;
    trimmed.route = {
      action: "answer_from_handles",
      reason: "every caller-named artifact source is inlined content-bearing",
      max_additional_tl_calls: 0,
    };
  }
  if (
    profile === "wiring"
    && trimmed.wiring?.status === "ready"
    && (
      trimmed.change_contract?.status === "ready"
      || trimmed.wiring.strategy === "semantic-multihop"
    )
    && trimmed.route === undefined
  ) {
    // trimToCap may shed advisory route text before the final proof is rebuilt.
    // A proved wiring frontier still needs its terminal action reconstructed;
    // otherwise the same evidence is mislabeled awaiting-input.
    trimmed.route = {
      action: "edit_from_handles",
      reason: "proved wiring consumer and insertion frontier",
      max_additional_tl_calls: 0,
    };
  }
  if (
    profile !== "answer"
    && binding.requested !== "auto"
    && binding.requested !== "answer"
    && trimmed.route?.action === "answer_from_handles"
  ) {
    // Post-trim route recomputation sees only the raw query and can otherwise
    // resurrect a lexical answer route after a structured change declaration.
    trimmed.route = {
      action: "edit_from_handles",
      reason: `caller-declared ${profile} change profile — structural readiness proved separately`,
      max_additional_tl_calls: 0,
    };
  }
  if (trimmed.route?.action === "answer_from_handles") {
    // Recursive closure and post-trim route repair run after the early answer
    // purification pass. Reassert the answer invariant at the finalizer so a
    // stale edit-style `next` can never be folded into a discovery contract.
    delete trimmed.next;
    delete trimmed.continuation;
  }
  // Persist same-epoch role/concern evidence before the final certificate is
  // evaluated. A continuation may legitimately serve a different frontier;
  // the ledger must see the union it already served, not only this body.
  reconcileEpochTaskContract(workspace, trimmed, effectiveQuery);
  // A resolved create target is the terminal frontier. Post-trim route
  // recomputation sees zero editable surfaces for a not-yet-existing file and
  // can restore `locate_missing_surfaces`; reassert the create route BEFORE
  // rebuilding the contract so its prepared certificate agrees with the
  // executable edit next_call (the later assertion preserves wire guidance).
  assertCreateRoute(trimmed, profile, workspace);
  trimmed.execution_contract = buildTaskExecutionContract(
    trimmed,
    // IMPROVEMENT A: a cumulative-complete pack certifies as a generic
    // surface-content proof (the role-specific structure was proven across the
    // earlier calls) — the FINAL rebuild must honor that too, else a non-generic
    // profile's role obligations reopen the contract this pack already closed.
    cumulativeComplete && trimmed.coverage_basis === "cumulative" ? "generic" : profile,
    effectiveQuery,
    buildTaskWorkspaceState(trimmed, workspace, workspaceInventory),
    workspace,
    false,
    servedZoomScope,
  );
  reconcileTaskPackExecutionContract(trimmed);
  if (
    trimmed.wiring?.strategy === "semantic-multihop"
    && trimmed.execution_contract?.typestate.phase === "prepared"
  ) {
    // Re-apply after trim/reconciliation because both can rebuild the final
    // contract. A prepared semantic response exposes one authoritative
    // pre-edit instruction: execute the certified frontier.
    if (semanticCheckRecords.length === 0) delete trimmed.checks;
  }
  if (
    trimmed.execution_contract?.readiness === "answer-ready"
    || trimmed.execution_contract?.call_budget?.discovery_allowed === false
  ) {
    delete trimmed.next;
    delete trimmed.continuation;
  }
  // After the last pass that can strip a planned sweep (the answer-ready /
  // exhausted-budget deletion above) — an earlier annotate call read
  // continuation state that no longer reached the wire.
  annotateUnsweptWiringEndpoints(trimmed);
  if (!fitsInCap(trimmed) && trimmed.execution_contract) {
    // Preserve authoritative typestate when the rich proof payload is the
    // only thing pushing an otherwise valid pack over its byte cap. Dropping
    // execution_contract entirely recreates the exact ambiguity the contract
    // is meant to eliminate (observed on T10 after inlining the DOCX section).
    // The compact form retains phase/certificate/allowed actions and the one
    // bounded next_call, while shedding duplicated proof evidence.
    trimmed.execution_contract = contractForTrim(trimmed.execution_contract);
  }
  if (!fitsInCap(trimmed) && trimmed.execution_contract) {
    // Certificate ids commit to the full proof, so remove duplicated proof
    // narration before sacrificing source evidence needed to review the edit.
    trimmed.execution_contract = contractForEvidenceRetention(trimmed.execution_contract);
  }
  if (!fitsInCap(trimmed) && trimmed.execution_contract) {
    // Prefer the authoritative typestate over advisory review surfaces only
    // after both proof-compaction levels have been exhausted. The edit
    // endpoints and every explicitly required surface remain protected.
    const protectedHandles = new Set([
      trimmed.wiring?.connections[0]?.source?.handle,
      trimmed.wiring?.connections[0]?.destination?.handle,
    ].filter((handle): handle is string => handle !== undefined));
    const optional = codeTaskPackSurfaces(trimmed.surfaces)
      .filter((surface) => surface.required !== true && !protectedHandles.has(surface.handle))
      .sort((a, b) =>
        Buffer.byteLength(b.code ?? b.code_unchanged ?? "", "utf8")
        - Buffer.byteLength(a.code ?? a.code_unchanged ?? "", "utf8")
      );
    for (const surface of optional) {
      const index = trimmed.surfaces.findIndex((candidate) => candidate.handle === surface.handle);
      if (index < 0) continue;
      trimmed.surfaces.splice(index, 1);
      const shedWhy = surface.why ?? "";
      if (
        shedWhy.includes("caller-supplied")
        || shedWhy.includes("query-named-file")
        || shedWhy.includes("header-source-pair")
      ) {
        // Never silently drop an explicitly requested file (or a query-named /
        // pair-contract enrichment surface): the caller cannot tell "trimmed
        // for byte budget" apart from "does not exist", and the observed
        // fallback is a native read of exactly that path.
        trimmed.missing = [...new Set([
          ...trimmed.missing,
          `dropped-by-byte-budget:${surface.path} (re-request via read_file handle=${surface.handle} or paths=[…])`,
        ])];
      }
      const servedHandles = new Set(trimmed.surfaces.map((candidate) => candidate.handle));
      if (trimmed.wiring) {
        trimmed.wiring.edit_frontier = trimmed.wiring.edit_frontier.filter((handle) => servedHandles.has(handle));
        trimmed.wiring.review_frontier = trimmed.wiring.review_frontier.filter((handle) => servedHandles.has(handle));
        for (const connection of trimmed.wiring.connections) {
          connection.supporting_handles = connection.supporting_handles.filter((handle) => servedHandles.has(handle));
        }
      }
      if (trimmed.change_contract) {
        trimmed.change_contract = buildTaskChangeContract(effectiveQuery, trimmed, workspace);
        reconcileTaskChangeContract(trimmed);
      }
      if (fitsInCap(trimmed)) break;
    }
  }
  if (!fitsInCap(trimmed)) {
    // Every rich trim pass failed. Preserve one concrete piece of evidence and
    // the exact read-only continuation that can expand it; never turn an
    // internal byte budget into a user decision, and never bless an empty pack
    // as a terminal answer.
    const retained = trimmed.surfaces[0];
    if (retained === undefined) {
      throw new Error("task-pack invariant: byte-budget fallback has no evidence surface");
    }
    const next = isArtifactTaskPackSurface(retained)
      ? retained.extract
      : `read_file mode=slice handle=${retained.handle} range=${retained.remaining_ranges?.[0] ?? retained.range}`;
    const fallback: TaskPackResult = {
      mode: "task_pack",
      coverage: "partial",
      coverage_reason: "missing-roles",
      surfaces: [retained] as TaskPackSurface[],
      missing: trimmed.missing.slice(0, 3),
      ...(trimmed.task_profile === undefined ? {} : { task_profile: trimmed.task_profile }),
      // W9 follow-up (2026-08-22): `trimmed.create_target` must survive onto
      // this rebuilt envelope. Before this, a create pack that overflowed the
      // byte cap lost create_target here (this object never carried it), so
      // the assertCreateRoute call below no-op'd on its early-return guard and
      // the response fell back to a plain "inspect this one retained surface"
      // discovery envelope for a file that does not exist yet.
      ...(trimmed.create_target === undefined ? {} : { create_target: trimmed.create_target }),
      route: {
        action: "inspect_handles",
        reason: "byte budget retained one evidence surface; expand only its bounded continuation",
        max_additional_tl_calls: 1,
      },
      next,
    };
    // W9 follow-up: re-assert the create route BEFORE any of the passes below
    // (reconcile/contract/decision) so — mirroring the un-trimmed pack's own
    // "assertCreateRoute before rebuilding the contract" ordering just above —
    // buildReadinessObligations' provedCreate sees a coherent create-route
    // envelope (missing=[], route=edit_from_handles) at contract-build time
    // and certifies "prepared" here exactly as it does for the un-trimmed
    // pack, instead of the single retained sibling being judged a codeless/
    // uncovered edit target.
    assertCreateRoute(fallback, profile, workspace);
    // B1b (2026-08-01 retrieval-scope): this fallback keeps exactly ONE surface
    // and stamps `missing-roles` unconditionally — so when `trimmed.missing`
    // was empty it shipped the T09 shape verbatim (`partial` + `missing-roles`
    // + `missing:[]`, one surface for a twelve-path request, nothing said about
    // the other eleven). Both invariants apply here BEFORE the contract is
    // built: name the roles the label is asserting, and disclose the
    // caller-named paths this shed away. A no-op on the create-route path
    // above (coverage_reason is already "single-site", not "missing-roles").
    reconcileMissingRolesContract(fallback);
    reconcileCallerPathCoverage(fallback, opts?.args, workspace);
    // This envelope is the last thing standing between the caller and a hard
    // invariant throw below, so keep the disclosure list at the same bounded
    // size the pre-B1b fallback shipped.
    fallback.missing = fallback.missing.slice(0, MAX_FALLBACK_MISSING_ENTRIES);
    fallback.execution_contract = buildTaskExecutionContract(
      fallback,
      profile,
      effectiveQuery,
      buildTaskWorkspaceState(fallback, workspace, workspaceInventory),
      workspace,
      false,
      servedZoomScope,
    );
    // P0a §6.1: this envelope returns BEFORE the shared exit below, so it must
    // run the same canonical projection itself — otherwise the last envelope
    // standing between the caller and a hard invariant throw is the one exit
    // that never proved route/contract/continuation agree. Applied before the
    // cap assertion so any continuation it stamps is inside the measured size.
    applyCanonicalTaskDecision(fallback);
    if (!fitsInCap(fallback)) {
      throw new Error("task-pack invariant: proof-preserving byte-budget fallback exceeds cap");
    }
    // W3: the create route is the final word even on the proof-preserving
    // fallback (its next is the edit action, not a dropped discovery call).
    assertCreateRoute(fallback, profile, workspace);
    // W2: bound the guidance-metadata inventory at ANY pack size.
    capGuidanceMetadata(fallback);
    persistPackFingerprints(workspace, fallback);
    if (opts?.args !== undefined) {
      captureServedPack(workspace, opts.args, computePackFingerprint(opts.args, workspace), fallback);
    }
    return fallback;
  }
  // W3: re-assert the concrete create route as the FINAL word (the prepared
  // contract above legitimately drops a discovery `next`; a create's next is
  // the edit action itself and must survive).
  assertCreateRoute(trimmed, profile, workspace);
  // C2 (2026-08-09): BEFORE the stop-route/doc-sliver passes, so a qref replay
  // that only adds context is shaped by exactly the same pipeline as the first
  // pack instead of shipping a demoted certificate plus three prescribed finds.
  carryForwardCertifiedWorkingSet(workspace, trimmed, opts?.args, effectiveQuery);
  // iter-2 W2: a complete/focused working set (single-call OR cumulative) carries
  // an explicit STOP signal on its route, so a pack whose coverage just flipped
  // complete reads as "edit now", never as license to issue more discovery packs
  // (the iter-1 loss cell issued 4 more; the win cell issued 0). Guidance only.
  applyCompleteStopRouteToResult(trimmed);
  // iter-2 W5: name the functional-validation obligation for an artifact-sourced
  // runnable target — an honest verify line + a session-recorded OPEN item, never
  // a block. Runs before the guidance cap so its verify line is accounted for.
  attachFunctionalValidationObligation(workspace, trimmed);
  // iter-3 F5: for an Office-source → Python-module build, attach the compact
  // runtime hint (discovered interpreters + module presence) so the build cell
  // does not burn its tail hunting a python that can import the library.
  attachRuntimeHint(trimmed);
  // iter-3 F4: shed orientation-only envelope weight (tree / full-code outline+why
  // / read-pack likely_edits / overlong route.reason) on packs 2+ and on any
  // prepared/complete pack — the first exploratory pack keeps the full envelope.
  // BEFORE annotateSpannedRoots so the reason cap precedes the span-remedy suffix
  // (which is load-bearing honesty and must survive), and before capture so the
  // served AND captured pack is the trimmed one.
  applyEnvelopeWeightTrim(
    trimmed,
    isFirstEpochPack(workspace, tokenizeForEpoch(effectiveQuery)),
    profile !== "answer",
  );
  // Serve-honesty (2026-08-01): AFTER the reason cap — see its doc comment.
  annotateEditZoomAffordance(trimmed);
  // L1 (2026-08-08): same reason — the doc-authority honesty clause replaces the
  // stop clause in place, so it must land after applyEnvelopeWeightTrim or the
  // 200-char reason cap would shear it (and the code working set off) again.
  const routeBeforeDocSliverHonesty = trimmed.route;
  const docSliverFloorRestore = applyDocSliverHonesty(workspace, trimmed, effectiveQuery);
  // L1 follow-up (2026-08-08, T05c smoke-gate escape): applyDocSliverHonesty
  // runs after the last fitsInCap-enforcing gate above (line ~19084), so its
  // heading-index affordance (up to DOC_SLIVER_HEADINGS_CAP_BYTES=2048/surface,
  // plus sections_hint/next_call/the longer route clause) was landing with no
  // size check at all. Measured: a multi_concern pack already at the 32768
  // ceiling (large required C++ surfaces across 3 concerns, plus small but
  // uncapped additions from applyCompleteStopRouteToResult and
  // annotateEditZoomAffordance just above) plus one doc-sliver affordance came
  // back at 34233B — refused by the bench smoke gate before any solver cell
  // launched. Degrade the just-attached affordance (never touched by the
  // earlier trim phases, so it has never been size-checked) rather than let it
  // silently blow the transport ceiling.
  if (!fitsInCap(trimmed)) {
    shrinkDocSliverAffordanceToFit(trimmed, routeBeforeDocSliverHonesty, docSliverFloorRestore);
  }
  // B1b (2026-08-01 retrieval-scope): re-run the missing-roles naming invariant
  // against the POST-trim state (a whole-surface drop or the cumulative flip can
  // both rewrite the role bookkeeping), and disclose any caller-named path this
  // pack narrowed away. Both run BEFORE capGuidanceMetadata so their additions
  // share the same bounded guidance budget as every other unserved-file note.
  reconcileMissingRolesContract(trimmed);
  reconcileCallerPathCoverage(trimmed, opts?.args, workspace);
  // B1c (2026-08-01 retrieval-scope): make a candidate list's own advice
  // runnable, and remember WHICH candidates are pending so a query-only re-pack
  // can bind them instead of re-deriving the same ambiguity.
  attachCandidateListRecovery(trimmed, effectiveQuery, workspace);
  if (isCandidateListPack(trimmed)) {
    const candidatePaths = [...new Set(codeTaskPackSurfaces(trimmed.surfaces).map((s) => s.path))];
    if (candidatePaths.length > 0) {
      pendingCandidatePaths.set(path.resolve(workspace), {
        paths: candidatePaths,
        tokens: tokenizeForEpoch(effectiveQuery),
      });
    }
  } else {
    pendingCandidatePaths.delete(path.resolve(workspace));
  }
  // W2: bound the combined size of the candidates/missing/notes guidance lists
  // so the raised pack budget flows to code bodies, not to a longer inventory
  // of unserved files. Applied here (post-trim, at the shared choke point) so
  // it fires at ANY pack size, not only when the overall cap binds.
  capGuidanceMetadata(trimmed);
  annotateSpannedRoots(trimmed);
  // A-F2 ORDER: suppress the consumed next FIRST, then repair whatever that
  // leaves. The reverse order let a suppression that ran after the guard strip
  // the last machine-executable action with nothing left to notice.
  const packLane = normalizeContractLane((opts?.args as { lane?: unknown } | undefined)?.lane);
  suppressNonProgressingNextCall(workspace, trimmed, opts?.args, effectiveQuery, servedZoomScope);
  enforceNoDeadEndContract(trimmed, effectiveQuery, { workspace, lane: packLane });
  attachEvidenceCompletion({
    workspace,
    pack: trimmed as never,
    tokensByConcern: concernTokensById(trimmed, effectiveQuery),
    packCapBytes: capForResult(trimmed),
  });
  // Evidence-expansion is a proof transition, not post-response bookkeeping.
  // Persist its explicit capability gap before the final contract rebuild so
  // this very pack can discharge its open-universe parent. Recording it below
  // the rebuild made the first fresh exhaustive Go pack emit `discover` and
  // only a later re-pack see the terminal explicit-gap proof.
  const importedEvidenceExpansionGaps = opts?.evidenceExpansion?.gaps ?? [];
  const preFinalExpansionGap = hasCurrentEvidenceExpansionGap(
    trimmed,
    effectiveQuery,
  );
  const evidenceExpansionGap = preFinalExpansionGap
    && importedEvidenceExpansionGaps.length > 0;
  if (evidenceExpansionGap) {
    trimmed.missing = [...new Set([...trimmed.missing, ...importedEvidenceExpansionGaps.map((gap) => `explicit-gap:${gap}`)])];
    trimmed.coverage = "partial";
    trimmed.coverage_reason ??= "concerns-uncovered";
  }
  const recordedExpansionGap = preFinalExpansionGap
    ? recordEpochTaskContract(workspace, trimmed, effectiveQuery, opts?.evidenceExpansion)
    : false;
  // The seams above can add a missing disclosure or retain a clipped range
  // after the first contract build. Rebuild and project together so the wire
  // never pairs that post-contract state with a stale prepared certificate.
  rebuildFinalExecutionContract(
    trimmed,
    cumulativeComplete && trimmed.coverage_basis === "cumulative" ? "generic" : profile,
    effectiveQuery,
    workspace,
    workspaceInventory,
    preFinalExpansionGap && recordedExpansionGap,
    packLane,
  );
  attachSingleSiteUniqueMatchFastPath(trimmed, workspace, effectiveQuery, profile, opts?.cache);
  persistPackFingerprints(workspace, trimmed);
  // Session-state registration is skipped too for an answer pack — a later
  // `read_file mode=closure` call must have nothing open to report for a
  // pack that never registered a closure/wiring check in the first place
  // (registering one here, then never expecting the edit that would satisfy
  // it, is exactly the stale-check shape closure hardening exists to avoid).
  const checkRecords = [...new Map([
    ...(opts?.checkRecords ?? []),
    ...semanticCheckRecords,
  ].map((check) => [check.id, check] as const)).values()];
  if (
    opts?.query !== undefined &&
    (opts.checkRecords !== undefined || semanticCheckRecords.length > 0) &&
    result.route?.action !== "answer_from_handles"
  ) {
    recordPackChecks(workspace, opts.query, checkRecords);
  }
  // ---------------------------------------------------------------------
  // V11-03 SECOND SEAM (TL_COVERAGE_PACKER_V2, requires TL_COVERAGE_PACKER) —
  // change_contract obligation threading, post-surfaces.
  //
  // WRITE: `trimmed.change_contract` is fully finalized by this point (every
  // rebuild above has already run) and `trimmed.surfaces` is the FINAL served
  // set (post-trim, post-shed). Record its obligations into priorPackStore so
  // the NEXT pack in the SAME task (via SEAM 1's `queryPriorPackObligations`
  // read, above in this file) packs against them as real coverage-packer
  // obligations — the "prior-pack store + second seam" the V10-09 shipping
  // notes (DESIGN-v0.10-expansion-plan-reconciliation.md §6 "B2-redo") named
  // as undone: "change_contract obligations NOT threaded (contract is built
  // after surfaces — a prior-pack store + second seam would be needed)".
  //
  // DETECT (gap disclosure), the mirror direction: an EARLIER pack's open,
  // required, action:"edit" obligation whose target path THIS pack neither
  // served (a code-bearing surface) nor inventory-listed (named in this
  // pack's own change_contract obligations) must never go silently missing —
  // appended to `trimmed.missing`, the EXISTING general-disclosure array this
  // file already uses for the identical shape of note (see the
  // "dropped-by-byte-budget:" entries above). This is the detection half of
  // "reconcile... where cheaply possible without reordering the build": it
  // deliberately does NOT attempt to re-flip `coverage` / re-run
  // `execution_contract` (both are long finalized above, and
  // `buildTaskExecutionContract`/its capability_gaps computation sit in the
  // receipt/certificate territory this workstream does not touch) — a fully
  // re-certified in-pack reconciliation was judged to need pipeline
  // restructuring and is recorded as a finding instead, per the brief's own
  // escape hatch for this seam.
  //
  // POSITION matters: this MUST run before `returned` (a few lines below) is
  // derived from `trimmed`/`subset`/`idempotent` — a receipt/idempotent
  // downgrade is a SEPARATE object, and a `trimmed.missing` mutation applied
  // after that point would never reach the wire (found live: an earlier
  // version of this seam sat after `captureServedPack`, past the `returned`
  // assignment, and its disclosure silently never shipped).
  //
  // Kept surgical: one contiguous block, gated on the same flag as SEAM 1,
  // runs once, mutates only `trimmed.missing` (an existing optional field)
  // plus priorPackStore's own state — never a receipt/certificate function.
  // ---------------------------------------------------------------------
  if (coveragePackerV2Enabled() && opts?.args !== undefined) {
    const obligationEpochTokens = tokenizeForEpoch(effectiveQuery);
    const servedPaths = new Set(codeTaskPackSurfaces(trimmed.surfaces).map((s) => s.path));
    const inventoryPaths = new Set((trimmed.change_contract?.obligations ?? []).map((o) => o.path));
    const priorObligations = queryPriorPackObligations(workspace, obligationEpochTokens);
    const unservedPriorEdits = priorObligations.filter((o) =>
      o.open && o.required && o.action === "edit"
      && !servedPaths.has(o.path) && !inventoryPaths.has(o.path)
    );
    if (unservedPriorEdits.length > 0) {
      trimmed.missing = [...new Set([
        ...trimmed.missing,
        ...unservedPriorEdits.map((o) =>
          `unserved-obligation:${o.path} (edit obligation from an earlier pack this task; re-request via read_file paths=["${o.path}"])`
        ),
      ])];
    }

    // Every obligation a change_contract carries is recorded as OPEN: a
    // change_contract is rebuilt fresh from THIS pack's own surfaces and
    // carries no "already edited" signal (that is the edit tool's ledger,
    // out of scope here — see the module header's honest-limitation note).
    // Treating a recorded obligation as still-open is the conservative
    // direction: it can cost the caller one avoidable re-check; silently
    // treating a genuinely-open one as closed would drop a real gap.
    const currentObligations: PriorObligationRecord[] = (trimmed.change_contract?.obligations ?? []).map((o) => ({
      id: o.id,
      path: o.path,
      role: o.role,
      kind: o.kind,
      action: o.action,
      required: o.required,
      open: true,
    }));
    if (currentObligations.length > 0) {
      recordPriorPackObligations(workspace, obligationEpochTokens, currentObligations);
    }
  }
  // Non-gap expansion remains deliberately after final decision construction:
  // it must not suppress the initial authoritative discovery call for an
  // unresolved direct callee/value. Gap-bearing current evidence was recorded
  // above because it is terminal proof for this very response.
  if (!preFinalExpansionGap) {
    recordEpochTaskContract(workspace, trimmed, effectiveQuery, opts?.evidenceExpansion);
    const persistedDischarge = taskContractDischargeCertificate(
      workspace,
      tokenizeForEpoch(effectiveQuery),
    );
    // A complete same-pack expansion is terminal proof, not merely a
    // post-decision cache write. Rebuild the contract after persistence so the
    // certificate and act.* decision are based on the fully discharged ledger.
    // Keep the first unresolved expansion conservative: without a discharge
    // certificate it retains its evidence-grounded discovery path.
    if (
      persistedDischarge !== undefined
      && (trimmed.route?.action === "answer_from_handles"
        || trimmed.route?.action === "edit_from_handles")
    ) {
      trimmed.execution_contract = buildTaskExecutionContract(
        trimmed,
        cumulativeComplete && trimmed.coverage_basis === "cumulative" ? "generic" : profile,
        effectiveQuery,
        buildTaskWorkspaceState(trimmed, workspace, workspaceInventory),
        workspace,
        false,
        servedZoomScope,
      );
      reconcileTaskPackExecutionContract(trimmed);
    }
    // The normal expansion path deliberately records only AFTER the first
    // decision: an unresolved direct dependency must still emit its initial
    // authoritative discovery call (I1), rather than being hidden by a
    // ledger row made too early. A pack which was already prepared is the
    // opposite case: its certificate is about to become a public identity,
    // and it must bind the very ledger snapshot this call just persisted.
    if (
      trimmed.execution_contract?.typestate.phase === "prepared"
      && persistedDischarge !== undefined
    ) {
      rebindPreparedCertificateToLedger(
        trimmed,
        cumulativeComplete && trimmed.coverage_basis === "cumulative" ? "generic" : profile,
        effectiveQuery,
        workspace,
      );
    }
  }
  // iter-2 W4 (awaiting-input idempotency) + W1 (subset receipt): consult the
  // session state BEFORE capturing this pack, so neither can match the current
  // pack against itself. W4 first: a still-unresolved proof re-grants the SAME
  // awaiting-input verdict (never `prepared`) as a compact receipt. Otherwise a
  // pack whose every body is already resident-and-unchanged re-serves a compact
  // subset receipt instead of the bodies. The full `trimmed` record is still
  // captured below, so a future EXACT re-call dedups against it.
  const epochTokens = tokenizeForEpoch(effectiveQuery);
  const idempotent = opts?.args !== undefined
    ? applyAwaitingInputIdempotency(workspace, trimmed, opts.args, effectiveQuery)
    : undefined;
  // PI-09 close-out: `force_serve:true` skips the subset receipt for the same
  // reason it skips the two dedup paths at the entry — "every body is already
  // resident" is a claim about a context the caller has just told us it lost.
  // `applyAwaitingInputIdempotency` above is deliberately NOT skipped: it is a
  // DECISION idempotency guarantee (the same unresolved proof must not flip to
  // `prepared` on a re-ask), not a body-withholding dedup, and re-serving
  // bodies must never move a verdict.
  const subset = idempotent === undefined && opts?.args !== undefined && opts.args.forceServe !== true
    ? tryServeSubsetReceipt(workspace, trimmed, epochTokens, opts.args.taskQueryRefReplay === true)
    : undefined;
  // Behavior 3: capture LAST (after recordPackChecks) so the captured check ids
  // reflect this pack's session state. Recompute the fingerprint from the same
  // args the top-level dedup interception used — deterministic, so both agree by
  // construction. The capture BASELINE differs by served shape:
  //   - W4 downgrade → capture the AWAITING-INPUT verdict, so a future exact
  //     re-call of the same signature also stays awaiting-input (the idempotency
  //     guarantee would leak if we captured the pre-downgrade prepared pack).
  //   - subset receipt → capture the full `trimmed` (the receipt is only a wire
  //     optimization for THIS call; keep the full baseline for exact re-calls).
  const returned = idempotent ?? subset ?? trimmed;
  // Receipts are alternate wire shapes, not alternate decision semantics.
  markServedZoomCandidates(
    workspace,
    returned,
    opts?.args?.forceServe === true,
    servedZoomScope,
  );
  applyCanonicalTaskDecision(returned);
  // A receipt can omit the body whose exact text this shortcut certifies. Never
  // carry it across an idempotent/subset downgrade or a non-ready exit.
  if (returned !== trimmed || returned.execution_contract?.typestate.phase !== "prepared") {
    delete returned.fast_path;
  }
  // Cache only after the final returned projection. An awaiting-input or
  // receipt downgrade must never leave its earlier prepared certificate
  // available for qref carry-forward; when the returned contract remains
  // prepared, retain trimmed's full working set rather than a compact receipt.
  if (returned.execution_contract?.typestate.phase === "prepared") {
    rememberCertifiedWorkingSet(workspace, trimmed, effectiveQuery);
  } else {
    // The map is one certificate per workspace. A non-prepared final result
    // supersedes any earlier certificate for this task/session; retaining it
    // would let a later qref replay resurrect authority the current evidence
    // just withdrew.
    certifiedWorkingSets.delete(laneScopedKey(path.resolve(workspace)));
  }
  if (opts?.args !== undefined) {
    const captureTarget = idempotent ?? trimmed;
    // §3 residual: dedup baseline vs. wire — `captureTarget` is the fingerprint
    // baseline (see the comment block above), `returned` is what the caller
    // actually receives and therefore what the served-range ledger may book.
    captureServedPack(
      workspace, opts.args, computePackFingerprint(opts.args, workspace), captureTarget, returned,
    );
  }
  // P1 evidence completion shadow runs against the pack the caller actually
  // receives. It remains a response no-op; evidenceShadow.spec.ts test 17
  // pins normalized response equivalence with TL_EVIDENCE_SHADOW enabled.
  emitEvidenceShadow({
    workspace,
    pack: returned as never,
    ...(opts?.args?.evidenceShadowQref !== undefined
      ? { qref: opts.args.evidenceShadowQref }
      : {}),
    tokensByConcern: concernTokensById(returned, effectiveQuery),
    surfaceBytes: Buffer.byteLength(JSON.stringify(returned), "utf8"),
    packCapBytes: capForResult(returned),
  });
  return returned;
}

/**
 * Concern id -> the significant tokens of its own query clause, for the P1
 * shadow resolver. Uses the SAME clause splitter the multi_concern builder
 * uses, so the anchors a concern is measured against are the ones it was
 * created from.
 */
function concernTokensById(result: TaskPackResult, query: string): Map<string, string[]> {
  const out = new Map<string, string[]>();
  const concerns = result.concerns ?? [];
  if (concerns.length === 0) return out;
  const groups = concernGroupsForQuery(query);
  concerns.forEach((concern, index) => {
    out.set(concern.id, groups[index]?.tokens ?? concernAnchorTokens(query));
  });
  return out;
}

/**
 * 09e pollution honesty (see TaskPackResult.roots): when the FINAL (post-trim)
 * surfaces span >= 2 distinct non-workspace project roots, name them — field
 * plus a route.reason suffix carrying the re-scoping remedy — so a mixed pack
 * is visibly mixed instead of silently trusted. Sits at the shared exit so
 * every flow (locate, seeded paths[], partial fallback, diff) reports the
 * same way. Runs AFTER trimToCap so the roots reflect the surfaces actually
 * served — but that also means none of trimToCap's fitsInCap checks ever see
 * this addition. 2026-07-12 cap-splice fix: real root names run well past
 * "a few bytes" (every name is counted twice — once in `roots[]`, again
 * inside route.reason's parenthetical list — across up to MAX_SURFACES_
 * DISTINCT roots), so splicing it on unconditionally could reopen a budget
 * trimToCap just closed. `roots` is also NOT in trimToCap's own "Protected
 * top-level" list (mode/coverage/missing/next) — it was never meant to be
 * exempt from the cap. Apply speculatively and commit only what still fits:
 * full annotation, then roots[] alone (the compact, higher-signal-per-byte
 * form), then neither — the same shed-if-over-budget discipline findText.ts's
 * applyRoles uses for its own advisory per-file `role` annotations.
 */
function annotateSpannedRoots(result: TaskPackResult): void {
  const countByRoot = new Map<string, number>();
  for (const s of result.surfaces) {
    const root = projectRootOf(s.path);
    if (root === "") continue;
    countByRoot.set(root, (countByRoot.get(root) ?? 0) + 1);
  }
  if (countByRoot.size < 2) return;
  const spanned = [...countByRoot.keys()].sort((a, b) => a.localeCompare(b));
  const route =
    result.route !== undefined && !result.route.reason.includes("surfaces span")
      ? {
          ...result.route,
          reason:
            result.route.reason +
            `; surfaces span ${spanned.length} project roots (${spanned.join(", ")}) — if the task targets one, re-scope with paths=["<root>"] or name it in the query`,
        }
      : result.route;

  if (fitsInCap({ ...result, roots: spanned, route })) {
    result.roots = spanned;
    result.route = route;
    return;
  }
  if (fitsInCap({ ...result, roots: spanned })) {
    result.roots = spanned;
    return;
  }
  // Neither fits inside the budget trimToCap already closed — leave `result`
  // exactly as trimToCap delivered it rather than reopening that budget.
}

/**
 * turn-economy wave 2 (W2): combined byte budget for a served pack's GUIDANCE
 * metadata. E2 forensics: under the raised 24KB pack budget, packs carried
 * ever-longer inventories of NAMED-BUT-UNSERVED files (the `trimmed` candidate
 * paths, the `missing` role list, the free-text `create_note`) — every extra
 * entry is another crawl target that tends to cost the caller a turn. This
 * bounds those three lists TOGETHER so the raised budget flows to code BODIES
 * (surfaces), not to a bigger unserved-file inventory. Surfaces,
 * execution_contract and coverage are never touched.
 */
export const GUIDANCE_METADATA_CAP_BYTES = 1280;

/** Combined serialized size of the three W2 guidance fields (values only). */
function guidanceMetadataBytes(result: TaskPackResult): number {
  let bytes = 0;
  if (result.trimmed !== undefined) bytes += Buffer.byteLength(JSON.stringify(result.trimmed), "utf8");
  if (Array.isArray(result.missing) && result.missing.length > 0) {
    bytes += Buffer.byteLength(JSON.stringify(result.missing), "utf8");
  }
  if (result.create_note !== undefined) bytes += Buffer.byteLength(JSON.stringify(result.create_note), "utf8");
  return bytes;
}

/**
 * W2: hold the combined guidance metadata under `budgetBytes` by dropping the
 * TAIL of each list least-essential-first (keeping the highest-ranked head:
 * `trimmed` is already ranked by candidate confidence, `missing` is ordered):
 *   1) `trimmed` candidate paths — "nothing required is missing on their
 *      account", so the least task-closing; dropped to empty if needed.
 *   2) the advisory `create_note`.
 *   3) `missing` roles/concerns — load-bearing honesty, trimmed only past its
 *      head (always keep >= 1 so a pack never silently hides that SOMETHING is
 *      missing) and only as a last resort.
 * Sets `guidance_trimmed:true` iff anything was dropped. Applied at the shared
 * emit choke point at ANY pack size, independent of the overall byte cap.
 */
export function capGuidanceMetadata(result: TaskPackResult, budgetBytes = GUIDANCE_METADATA_CAP_BYTES): void {
  if (guidanceMetadataBytes(result) <= budgetBytes) return;
  let dropped = false;
  if (Array.isArray(result.trimmed)) {
    while (result.trimmed.length > 0 && guidanceMetadataBytes(result) > budgetBytes) {
      result.trimmed.pop();
      dropped = true;
    }
    if (result.trimmed.length === 0) result.trimmed = undefined;
  }
  if (guidanceMetadataBytes(result) > budgetBytes && result.create_note !== undefined) {
    result.create_note = undefined;
    dropped = true;
  }
  if (Array.isArray(result.missing)) {
    while (result.missing.length > 1 && guidanceMetadataBytes(result) > budgetBytes) {
      result.missing.pop();
      dropped = true;
    }
  }
  if (dropped) result.guidance_trimmed = true;
}

// ---------------------------------------------------------------------------
// Trim the pack to capForResult(result) using the design-mandated order.
//
// Trim order: tree/create_note → outline → verify/checks/blockers/route → secondary surfaces → facts → why → likely_edits → intents
// Protected per surface: handle, path, range, role, code
// Protected top-level: mode, coverage, missing, next
//
// A1: `protectedSurfaces` (identity set, never serialized) marks
// caller-supplied surfaces that Phase 1 must never REMOVE — every supplied
// path must appear in the final pack, code-bearing or handle+skeleton-only.
// Phase 5 (code removal) still applies to protected surfaces same as any
// other: degrading a protected surface's code to handle+skeleton is exactly
// the sanctioned overflow behavior, only outright removal is barred.
// ---------------------------------------------------------------------------

/**
 * A1 shed helpers for inlined artifact bodies. Two distinct operations, applied
 * at two very different points in the trim order:
 *
 *  - `shrinkArtifactSections` walks each matching entry down artifactSectionCandidates'
 *    bounded ladder (largest variant that helps), keeping the entry — and therefore
 *    the source identity and the content — present but smaller;
 *  - `dropArtifactSections` removes the entry outright. The artifact's stub surface
 *    (handle + `extract` call) always survives, so nothing becomes unreachable; the
 *    removal is recorded in `artifact_sections_trimmed` rather than left silent.
 *
 * Returns true when anything changed, so the caller can re-check the cap.
 */
function shrinkArtifactSections(
  result: TaskPackResult,
  matches: (artifactPath: string) => boolean,
): boolean {
  const sections = result.artifact_sections;
  if (sections === undefined || sections.length === 0) return false;
  let changed = false;
  for (const entry of sections) {
    if (!matches(normalizedRequestPath(entry.path))) continue;
    const ladder = artifactSectionCandidates(entry.section);
    const current = Buffer.byteLength(JSON.stringify(entry.section), "utf8");
    const smaller = ladder
      .map((candidate) => ({ candidate, bytes: Buffer.byteLength(JSON.stringify(candidate), "utf8") }))
      .filter((item) => item.bytes < current)
      .sort((a, b) => b.bytes - a.bytes)[0];
    if (smaller === undefined) continue;
    entry.section = smaller.candidate;
    entry.content_completeness = "partial";
    entry.note = "artifact body reduced to fit the pack byte budget; fetch the full section with next";
    if (entry.next === undefined) entry.next = `read_file mode=artifact path=${entry.path}`;
    changed = true;
  }
  return changed;
}

function dropArtifactSections(
  result: TaskPackResult,
  matches: (artifactPath: string) => boolean,
): boolean {
  const sections = result.artifact_sections;
  if (sections === undefined || sections.length === 0) return false;
  const kept = sections.filter((entry) => !matches(normalizedRequestPath(entry.path)));
  if (kept.length === sections.length) return false;
  const shed = sections
    .filter((entry) => matches(normalizedRequestPath(entry.path)))
    .map((entry) => entry.path);
  if (kept.length > 0) result.artifact_sections = kept;
  else delete result.artifact_sections;
  result.artifact_sections_trimmed = [...(result.artifact_sections_trimmed ?? []), ...shed];
  // An `inlined[]` stamp may not outlive the body it names (§11.4). attachSupply
  // would drop it at the wire exit anyway; dropping it here keeps the byte
  // accounting of the remaining trim phases honest.
  if (result.inlined !== undefined) {
    const shedNormalized = new Set(shed.map(normalizedRequestPath));
    const inlined = result.inlined.filter((stamp) => {
      const stamped = /^artifact-section:(.+)#/.exec(stamp)?.[1];
      return stamped === undefined || !shedNormalized.has(normalizedRequestPath(stamped));
    });
    if (inlined.length > 0) result.inlined = inlined;
    else delete result.inlined;
  }
  return true;
}

export function trimToCap(
  result: TaskPackResult,
  protectedSurfaces?: ReadonlySet<TaskPackSurface>,
  /**
   * A1: normalized paths of the artifacts the CALLER named. Mirrors
   * protectedSurfaces semantics for artifact bodies — such a body may be
   * degraded, but only after every other body (including code) is gone, because
   * it is the very content the caller asked for. An artifact NOT in this set is
   * a discovery affordance and its body is shed early.
   */
  protectedArtifactPaths?: ReadonlySet<string>,
): TaskPackResult {
  if (fitsInCap(result)) return result;

  // DESIGN-v0.8 §A7: the partial-pack `tree` is a navigation convenience (find
  // replacement), not task-closing content — drop it first, before any
  // per-surface affordance. `explore action=tree` remains reachable from
  // `next` regardless.
  if (result.tree !== undefined) {
    result.tree = undefined;
  }
  if (fitsInCap(result)) return result;

  // Item 4: create_note is the newest, least-essential pack-level field — a
  // "you might need to create a new file" tip is advisory speculation, not
  // task-closing content, so it is shed at the same earliest priority as
  // `tree`.
  if (result.create_note !== undefined) {
    result.create_note = undefined;
  }
  if (fitsInCap(result)) return result;

  // Drop the least-essential per-surface affordance first.
  // DESIGN-v0.8 §A5 deliverable 3: `outline` is a navigation convenience for
  // a DIFFERENT section of a contract doc, not needed to close THIS task
  // (the matched section's `code` already covers that), so it is dropped at
  // the earliest priority. DESIGN-v0.8 §C1 item 3: `slice_wider` was dropped
  // from the surface shape entirely (implied by the always-present
  // `handle`), so this phase no longer has a slice_wider field to clear.
  for (const surf of result.surfaces) {
    if (surf.outline !== undefined) {
      surf.outline = undefined;
    }
  }
  if (fitsInCap(result)) return result;

  // Phase 0: drop advisory metadata first. Handles and surfaces are the value.
  if (result.verify !== undefined) {
    result.verify = undefined;
  }
  if (fitsInCap(result)) return result;
  if (result.checks !== undefined) {
    result.checks = undefined;
  }
  if (fitsInCap(result)) return result;
  // The stop/continue summary duplicates route at this point. Under a tight
  // cap, preserve the actionable route and blocking calls; the execution
  // contract is re-added after trimming only when it still fits.
  //
  // P0a §6.2 honesty (2026-08-13): `profile_binding` used to be deleted HERE,
  // in the same breath as the contract — so the one field the spec says is
  // always emitted silently vanished under cap pressure, and a caller could
  // not tell an inferred profile from a declared one exactly when the pack was
  // most aggressive. It is ~150B of provenance; it now has the ladder's LOWEST
  // priority instead (see the tail after Phase G).
  result.execution_contract = undefined;
  if (fitsInCap(result)) return result;
  // A4: when the route actually flipped to locate_missing_surfaces,
  // blocking_next_steps carries the per-unmatched-concern-token follow-up
  // calls — dropping it here is exactly backwards: it strips the honest
  // "what to do next" guidance precisely when the pack is fattest (and thus
  // most likely to need trimming at all). Protect it in that case; it still
  // falls to the same fate as `route` below when action is NOT
  // locate_missing_surfaces (nothing to protect — item 3's original
  // reasoning still applies there).
  const protectBlockingNextSteps = result.route?.action === "locate_missing_surfaces";
  if (!protectBlockingNextSteps && result.blocking_next_steps !== undefined) {
    result.blocking_next_steps = undefined;
  }
  if (fitsInCap(result)) return result;
  if (result.route !== undefined) {
    result.route = undefined;
  }
  if (fitsInCap(result)) return result;
  // blocking_next_steps was protected above specifically because `route`
  // still existed to prove the flip was live; now that route itself has
  // been dropped (pack still over cap even without it), the protection
  // reason no longer applies — drop it last, after every other Phase-0
  // field, rather than never.
  if (protectBlockingNextSteps && result.blocking_next_steps !== undefined) {
    result.blocking_next_steps = undefined;
  }
  if (fitsInCap(result)) return result;

  // DESIGN-v0.9 §4.3 TRIM-ORDER INVERSION. The old order ran the
  // whole-surface-drop loop (HANDLE loss) BEFORE the code-body strip (handle
  // preserved) — the expensive, handle-destroying operation first, violating
  // the codebase's own "losing a handle is bad; losing embedded code alone is
  // fine" philosophy. Inverted below: shed advisory metadata, then strip code
  // BODIES (handles kept), and only drop whole surfaces as the LAST resort,
  // once the code-less envelope alone still exceeds cap.

  // Phase A: drop facts from all surfaces.
  for (const surf of result.surfaces) {
    if (surf.facts !== undefined) {
      surf.facts = undefined;
    }
  }
  if (fitsInCap(result)) return result;

  // Phase B: drop why from all surfaces.
  for (const surf of result.surfaces) {
    if (surf.why !== undefined) {
      surf.why = undefined;
    }
  }
  if (fitsInCap(result)) return result;

  // Phase C: drop likely_edits from all surfaces. DESIGN-v0.8 §C1 item 1
  // already confines likely_edits to the PRIMARY surface at emission time
  // (secondary surfaces never carry it) — this phase now clears it from at
  // most one surviving surface in practice. Left as a full loop rather than
  // indexing surfaces[0] directly: it stays trivially correct regardless of
  // which surface(s) actually still hold the field, at no extra cost (the
  // list is already bounded to MAX_SURFACES_DISTINCT).
  for (const surf of result.surfaces) {
    if (surf.likely_edits !== undefined) {
      surf.likely_edits = undefined;
    }
  }
  if (fitsInCap(result)) return result;

  // Phase D: drop advisory edit intents/checks only after handle targets.
  for (const surf of result.surfaces) {
    if (surf.edit_intent !== undefined) {
      surf.edit_intent = undefined;
    }
    if (surf.done_check !== undefined) {
      surf.done_check = undefined;
    }
  }
  if (fitsInCap(result)) return result;

  // Phase D2 (A1): an artifact the caller never NAMED is a discovery affordance,
  // so its inlined body is the cheapest real content in the pack — shed before
  // any code body. Shrink down the bounded ladder first, then drop the entry
  // (its stub surface + `extract` call always survive).
  //
  // An EMPTY/absent `protectedArtifactPaths` means "which artifacts the caller
  // named is unknown here" (the artifact_build flow bundles its own sections and
  // does not compute the set), NOT "none of them were named" — so this phase
  // stays a no-op and every artifact body falls to Phase G's last-resort shed.
  const artifactNamingKnown = protectedArtifactPaths !== undefined && protectedArtifactPaths.size > 0;
  const unnamedArtifact = (artifactPath: string): boolean =>
    artifactNamingKnown && !protectedArtifactPaths!.has(artifactPath);
  if (shrinkArtifactSections(result, unnamedArtifact) && fitsInCap(result)) return result;
  if (dropArtifactSections(result, unnamedArtifact) && fitsInCap(result)) return result;

  // Phase E (§4.3): strip embedded code BODIES — the surface, its role, and its
  // HANDLE all survive; the agent re-slices via the same handle. Stamping
  // content_completeness:"partial" + remaining_ranges (the whole embedded range
  // is now un-embedded) NEVER flips coverage (§4.4: a byte budget may not flip
  // coverage by itself — that was the "partial-by-budget" anti-pattern that
  // branded complete packs partial and listed covered roles as "missing").
  // NOTE capForResult() SHRINKS as code is removed (32768→28672→24576), so
  // fitsInCap is re-evaluated against the LIVE cap after each strip.
  for (let i = result.surfaces.length - 1; i >= 0; i--) {
    const surf = result.surfaces[i]!;
    if (surf.code !== undefined) {
      surf.code = undefined;
      surf.content_completeness = "partial";
      surf.remaining_ranges = [surf.range];
      if (fitsInCap(result)) return result;
    }
  }

  // Phase F (§4.3): whole-surface drop — the LAST resort, reached ONLY when the
  // pack still exceeds cap after every body has been stripped (the code-less
  // envelope alone is over budget — rare now the order is inverted). This is
  // the ONE path that may flip coverage: the dropped surface's role really is
  // gone (its handle, not just its inline code), so it becomes an honest
  // "missing-roles" partial. A1: never drop a surface in `protectedSurfaces` —
  // remove the LAST unprotected surface instead (skip over protected ones
  // rather than assuming they occupy a contiguous prefix, since native-pair
  // augmentation can splice an unprotected sibling in between two protected
  // seeds). Always keep at least 1 surface overall. `surface_drops` counts
  // these so the bench sees size-triggered handle loss; dedupeTrimAndPersist
  // recomputes route/next against the post-drop state so a coverage flip never
  // ships a stale "you're done" next.
  let surfaceDrops = 0;
  for (;;) {
    if (result.surfaces.length <= 1 || fitsInCap(result)) break;
    let dropIdx = -1;
    for (let i = result.surfaces.length - 1; i >= 0; i--) {
      if (!protectedSurfaces?.has(result.surfaces[i]!)) {
        dropIdx = i;
        break;
      }
    }
    if (dropIdx === -1) break; // every remaining surface is protected
    const dropped = (result.surfaces as TaskPackResultSurface[]).splice(dropIdx, 1)[0]!;
    // Artifact surfaces are discovery affordances, never coverage/ranking roles.
    if (!isArtifactTaskPackSurface(dropped)) {
      if (!result.missing.includes(dropped.role)) {
        result.missing.push(dropped.role);
      }
      result.coverage = "partial";
      result.coverage_reason = "missing-roles";
      surfaceDrops++;
    }
  }
  if (surfaceDrops > 0) result.surface_drops = surfaceDrops;

  // Phase G (A1) — LAST. A CALLER-NAMED artifact's inlined body is the content
  // the request was about, so it outranks every other body in the pack: it is
  // shed only here, after advisory metadata, every code body, and even whole
  // unprotected surfaces are gone. Shrink first (identity + some content
  // survive); degrade to handle+stub only if the pack is STILL over cap with
  // nothing else left to give — the same "degrade, never silently vanish"
  // contract protectedSurfaces gives a caller-supplied code surface.
  const namedArtifact = (artifactPath: string): boolean =>
    !artifactNamingKnown || protectedArtifactPaths!.has(artifactPath);
  if (shrinkArtifactSections(result, namedArtifact) && fitsInCap(result)) return result;
  if (dropArtifactSections(result, namedArtifact) && fitsInCap(result)) return result;

  // P0a §6.2 honesty (2026-08-13) — profile provenance is the ABSOLUTE last
  // resort. It used to be deleted in Phase 0 alongside the execution contract,
  // so the one field the spec calls always-emitted vanished on exactly the
  // packs that trimmed hardest, and a caller could no longer tell a declared
  // profile from an inferred one. It is ~150B: shedding it before any code
  // body could never have been the difference between fitting and not, so it
  // was pure loss. It still PARTICIPATES in the ladder (a pack that is over
  // cap with every body, surface, and artifact already gone gives it up too),
  // it simply has the lowest priority of anything the ladder can shed.
  if (result.profile_binding !== undefined) {
    result.profile_binding = undefined;
  }

  // The pack is as trimmed as it can be — return it even if slightly over cap.
  return result;
}

function fitsInCap(result: TaskPackResult): boolean {
  return Buffer.byteLength(JSON.stringify(result), "utf8") <= capForResult(result);
}
